---
paths:
  - "Electron/src/**"
---

Dokunduğun dosya **Electron yönetim paneli** katmanındadır. Kod yazmadan/değiştirmeden ÖNCE `docs/standart/ELECTRON.md` dosyasını oku (sayfa kalıbı, iskelet, `apiClient` tek kapısı, sunucu süzmesi, tek hata toast'ı, izin aynası, şema ↔ Prisma `VarChar` hizası, boyut tavanları). Yeni sayfa açıyorsan prosedür `docs/RECETELER.md` § Yeni Electron sayfası'ndadır. Bitince `npm run typecheck` + `npm run lint` + `npx vitest run` koş.
