import { describe, it, expect } from "vitest";

// Altyapı doğrulama — Vitest + ts + alias çözümü çalışıyor mu?
describe("vitest altyapı", () => {
  it("çalışıyor", () => {
    expect(1 + 1).toBe(2);
  });
});
