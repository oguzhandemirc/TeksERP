# Docker compose için ortam değişkenleri.
# baslat.sh ilk çalıştığında bu dosyayı kopyalayıp rastgele şifre/secret üretir.
# Elle düzenleyeceksen tüm değerleri değiştir, GIT'E COMMITLEME.

POSTGRES_USER=tekserp
POSTGRES_PASSWORD=change_me_strong_password
POSTGRES_DB=tekserp
JWT_SECRET=change_me_64_char_random_hex_secret
