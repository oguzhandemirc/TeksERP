# MATRIX — VARLIK × YAZMA YOLU × DEĞİŞMEZ MATRİSİ (aşama ① KEŞİF sentezi)

**Tarih:** 2026-08-28 · dal `adnansahin` · HEAD `ce8681d1` · Yazan: matris sentezleyicisi (salt-okunur; kaynak ağaca dokunulmadı).
**Girdi:** `audit/00-map/` altındaki 16 harita — K1a/K1b (rota), K2a (şema), K2b (migration+DB kataloğu), K3a/K3b (tx+kilit), K4 (yazma yolları), K5 (yetki), K6 (hata yolu), K7a/K7b (hesap), K8 (scheduler), K9 (entegrasyon), K10 (değişmezler), K11 (bekçi), K12 (önceki denetim uzlaştırma). **Eksik harita yok** (16/16 okundu). Bu dosya yeni ölçüm YAPMAZ; her hücre ilgili haritanın satırına atıf taşır (`Kx §n` / `Kx H-n`). Yargı yok — hücreler ② denetçisinin "önce buraya bak" listesidir.

**Okuma anahtarı (sütunlar):**

| Sütun | İçerik | Kaynak |
|---|---|---|
| **Yazma yolları** | `servis (site sayısı; havuz N = tx DIŞI yazım)` · helper · job · script · trigger · ham SQL · nested | K4 §1a/§1b/§3 |
| **Değişmezler** | K10 INV-ID'leri (`SYS/STK/SM/SEV/SIP/FAS/PAR/DOC/WO/YM/AUD/MD/YT`) | K10 §1-§2 |
| **DB kısıtı** | `unique` · `partial` (predicate'li) · `CHECK` · `trigger` · FK (`C`/`R`/`SN`; `*` = Prisma varsayılanı) · DEFERRABLE | K2a §1-§3, K2b §1 |
| **Tx & kilit** | tx sitesi (K3a `S/W/F/O/I/R/M/K/P/V/B`, K3b satır) · advisory NS (8021-8027) · satır kilidi (`W` WO, `S` Sack, `H` Shipment, `L` OrderLine, `K` sayaç) · **claim** = `updateMany WHERE {id, beklenen}` + `count` · **YOK** | K3a §2-§6, K3b §1-§6 |
| **Bekçi** | `scripts/test_*.ts` (K11 §1-§4); `EŞZ ✓` = gerçek paralel yazma sondası var; `EŞZ ✗` = yok | K11 |
| **② işaret** | haritaların hotspot numarası (ör. `K10 H-1`) | ilgili harita |

Kısaltmalar: R=Roll · RM=RollMovement · WO=WorkOrder · WOS=WorkOrderStep · OL=OrderLine · SA=SackAllocation · SD/SDI=fason sevk/kalem · SR/SRI=fason makbuz/kalem · DSA=doğrudan sevk tahsisi · TC=TravelerCard · PD=PrintedDocument · wBR=`withBarcodeRetry`.

---

## 1. TOP / STOK (Roll ailesi)

| Varlık | Yazma yolları (K4) | Değişmezler (K10) | DB kısıtı (K2a/K2b) | Tx & kilit (K3a/K3b) | Bekçi (K11) | ② işaret |
|---|---|---|---|---|---|---|
| **Roll** (`rolls`) | **21 dosya** (K4 §2): batch (6 updateMany) · inventory (11; havuz 2: `restoreCancelledRoll` :3452, `prepareRawForSale` :4033) · kartela (6; havuz 2) · kursun-bypass (2) · kursun-qc (3) · label (2; havuz 2) · order (1: `quickOrderFromRolls` STOCK→WAREHOUSE) · return (2) · shipping (15) · subcontractor (20) · tambur-manual (1) · tambur-undo (7) · tambur (16) · workorder-batch-drop (3) · workorder-manual-move (5) · workorder-split (4) · workorder (7; havuz 1: `prepareFasonCancelDecision` :3281 **koşulsuz**) + helper: roll-disposition (3), roll-finalize (1), roll-step (1), workorder-clone (2). `status` **57 site**, kanonik tek nokta YOK. **Trigger** `rolls_stamp_production_timestamps` → `finalizedAt/statusChangedAt` (uygulama yazmaz; K4 §4). **Ham SQL:** merge `:921/:938` `labelDirty`, 44 MOVE kuralının 4'ü (`itemId/colorId/labelCustomerId`); script `backfill_roll_production_timestamps.ts:176/187`. **Script:** backfill ×3, `fix_fire_rolls_to_scrap`, `repair_sack_ghost_rolls`, `clean_test_residue`, 12 seed/demo. | STK-01..15 · SM-01 (geçiş matrisi: 20 kenar, DB seddi YOK) · SEV-03/04 · STK-10 (clientToken 4 durum) · AUD-04 (iptal izi) · MD-04 (kalite snapshot) · YM-01/02 | `barcode?` unique · `clientToken` **partial** (`IS NOT NULL`) · CHECK `currentQty≥0`, `initialQty≥0`, `weightKg≥0` (**`currentQty ≤ initialQty` YOK**, K2a H8) · **DEFERRABLE** bileşik FK `(sackId,shipmentId)→sacks(id,shipmentId)` ON UPDATE CASCADE (K2b H-4) · trigger (kaynak listesi sabit; `WAREHOUSE→SCRAP` damgalamaz, `SHIPPED/CANCELLED` bilerek dışarıda) · 16 FK: `itemId` R\*, 15 SN\* · 21 index (8 partial) | KK1 I1: **8021** (bayrak `kk1.duplicateGuardEnabled`, sahada AÇIK) → ikiz `findFirst` → **K** sayaç → create; replay tx DIŞI catch (`existing.status` bakılmıyor). İptal I2 claim `{id,status,shipmentId}` (sackId pin'lenmez). `applyManualProperties` I4 claim yalnız `shipmentId/sackId` pin'ler (statü/`currentQty` pin'lenmez). Tambur: claim `{id,IN_PRODUCTION,currentStepId}` + `raceLost` idempotent; `K` tx ÖNCESİ (finalize) / tx İÇİ (KK1, finish-step, bypass, rescue, disposition, batch-drop — 7 yer). Undo: claim'ler tekil ama **WO kilidi YOK** (K3b H-1). Sevk: grup `updateMany {shipmentId,status}`; iade: claim `{id,SHIPPED}` + 8023; `cancelReturn` 8023 YOK. Havuz-client statü yazımı 3 nokta. | `test_consistency` §7/§8/§10/§11/§12/§13 · `test_kk1_duplicate_guard` (EŞZ ✓ 5→1 + negatif sonda) · `test_p1b_barcode_collision` · `test_client_token_idempotency` · `test_roll_cancel_undo` · `test_roll_edit_unified` · `test_tambur_undo` §5/§11 · `test_manual_roll_undo` (iptal-sonrası replay 409 — **tek örnek**) · `test_sack_status_invariant` (EŞZ ✓) · `test_tambur_cut_idempotency` (aynı token) · `test_db_invariants` §2/§6 · `test_barcode_reservation` | K10 H-1 (KK1 replay iptal statüsü) · H-3 (parent `initialQty=0`, 2 kayıt) · H-4 (`reopenStep` SCRAP'ı diriltir) · H-5 (47/52 iptal izi boş) · H-12 (`applyManualProperties` metraj pin'siz) · K3a #1 (tx-öncesi havuz `updateMany`) · K7a #1/#2 (undo SET vs increment; 0/0 m WAREHOUSE) · K11 H-5 (farklı-token aynı-parent paralel kesim bekçisiz) · K4 H10 (üç "önceki statü" kolonu üç serviste) |
| **RollMovement** (`roll_movements`) | 10 servis + 2 helper (K4 §2). Açma: `roll-step.helper openMovementForNextStep`, `workorder:4534`, `subcontractor:581/1347/3091`, `tambur-manual`, `undo:1315/1545`. **Kapanış İKİ mekanizma:** ham SQL **13 site** (`workorder:3531/4038/4330/6032`, `subcontractor:2071/2828/3455/6093/6185`, `batch-drop:439`, `roll-disposition:378`, `kursun-bypass:2339`, `kursun-qc:888`) + Prisma `exitedAt` **9 site**; `qtyOut` kuralı yere göre 3 varyant + **NULL** (manual-move :649, split :369/:486, sub :2071 — K7a §2.E). **deleteMany ×4** (kursun-qc:1097, sub:4835/5373, manual-move:643). Durum = `exitedAt IS NULL` + `notes` marker metni. Ham UPDATE'ler `updatedAt`'i tazelemez (K4 H5). | STK-05 (kapanış semantiği) · STK-11 (tek açık) · STK-13 (hayalet yok) | **partial unique** `roll_movements_one_open_per_roll_step_uq (rollId, workOrderStepId) WHERE exitedAt IS NULL` (şemada YOK) · CHECK `qtyIn/qtyOut/weightIn/weightOut ≥ 0` · FK `rollId` R\*, `workOrderStepId` R\* · partial `exitedAt_idx` | Koşullu kapanış (`exitedAt IS NULL` WHERE) = claim; `finishStep` raw `RETURNING` ile kapsam paritesi; `attachRolls` W5 wBR predicate'siz → partial unique P2002 retry'a girer (K3a W5) | `test_consistency` §11/§12 (muaf desen listesi `:280-330`) · `test_roll_cancel_step_recompute` · `test_db_invariants` §1 · `test_manual_move_backflush` · `test_kursun_unassigned_close` (EŞZ ✓ 3→1) | K4 H4 (iki kapanış mekanizması) · K7a #5 (qtyOut NULL, saha 14 satır) · K7b H17 (WIP `qtyIn` kısmi kabulde değişmez) |
| **RollOperation** (`roll_operations`) | 7 dosya: inventory (1 createMany), kursun-qc (2 upsert `update:{}`), subcontractor (8 createMany/deleteMany), tambur-undo (2 deleteMany), tambur (6 createMany/upsert), manual-move (1 deleteMany) + workorder-clone (updateMany). **deleteMany ×7** — append-only tabloda silme (K4 H11); okuyucu `reports/production.report.service.ts:109`. | (SM-01 dolaylı) · üretim bildirimi izi | unique `(rollId, workOrderStepId, operationType)` (adım başına TEK satır) · FK `rollId` R\*, `workOrderStepId` R\* · `updatedAt` YOK (append-only) · `metadata` Json upsert'te ilk yazım kalır | upsert idempotent (QC2) · `skipDuplicates` createMany | `test_qc2_idempotency` (SIRALI; paralel ✗) · `test_o19_operator_trace` | K4 H11 (log silme rapor sayımını düşürür — bilinçli/yorumlu) |
| **RollError** (`roll_errors`) | 6 dosya: inventory (createMany), kursun-qc (create/**deleteMany havuz** :720 `isProcessed:false` claim), tambur-undo (updateMany), tambur (updateMany/update/create), split (updateMany), clone (updateMany) | (SM-01) · `startMeter ≤ currentQty` (KOD, iki yazım A15/B9) | **partial unique** `roll_errors_roll_meter_defect_uq WHERE defectTypeId IS NOT NULL` · CHECK `startMeter ≥ 0` · FK `rollId` R\*, `defectTypeId` SN\* | `reportError` Roll claim `updatedAt` + P2002 catch tx dışı → 409 | `test_phase6_reporterror_concurrency` (EŞZ ✓ 2→1 + DB seddi) | K12 H5 / DB-MIMARI **O-18**: `finalizeRollsAtLastStep` açık `RollError` sormuyor (kursun-qc/bypass yolu karar vermeden depoya) |
| **RollProperty** (`roll_properties`) | 6 dosya, **replace deseni** (`deleteMany`+`createMany`) ×5; `station-capability-transfer.copyStationCapabilitiesToRoll` upsert; `applyManualProperties` yalnız FLAG evrenini siler (`:3947`) | MD-06 (KAT değer pivotta değil kolonda) | unique `(rollId, propertyId)` · FK `rollId` C, `propertyId` R\*, `valueId` **R** (açık) | replace tx içi; `loadStationPropertyCaps(prisma)` pre-tx bayat `caps` (K3b T9) | `test_property_value_selection` · `test_station_property_mode` · `test_fold_edit_and_label` | K4 §2: `tambur:1208` finalize parent'ın TÜM property'lerini siler (CHOICE dahil?) — ② doğrular |
| **RollVariance** (`roll_variances`) | **Tek yazıcı** `helpers/roll-variance.helper.ts:50 recordVarianceTx` (tx İÇİNDE; `qty≤0` → satır yok); çağıranlar tambur ×5, tambur-undo ×4, subcontractor ×2 (K7a §2.N). Tersleme `updateMany reversedAt` (undo :1610/:1627, sub :4986). | AUD-05 (defter tam) · STK-02/03 (aşım/OVERAGE) · FAS-06 (çekme, `sourceRefId`) | CHECK `qty > 0` · FK `rollId` **R\*** (RESTRICT → test cleanup'ları `deleteMany` şart) · `workOrderStepId` SN\* (adım silme guard'ı saymıyor, K2a H5) · `sourceRefId` index (unique DEĞİL, bilinçli) · `updatedAt` YOK | tx içi; `sourceRefId` = tersleme adresi | `test_roll_variance` · `test_tambur_undo` §11 · `test_scrap_scorecard` · `test_fason_partial_receive` · `test_plan_deviation_scorecard` | K10 H-3 (2 defter-siz aşım) · K7b H4 (karne `rollId+stepId` eşler, `sourceRefId` kullanmaz → çift eşleşme adayı) |
| **RollPlanDeviation** (`roll_plan_deviations`) | Tek yazıcı `tambur-plan-gate.helper.ts:168 recordPlanDeviationTx` (createMany); çağıranlar tambur :1252/:2902/:3279, subcontractor :3120. `assertRollMatchesPlan` global `prisma` pre-tx (audit içerir). | AUD-06 (çift-sayım kilidi `confirmationId`) · FAS-09 · WO-08 | `confirmationId` index (unique DEĞİL) · FK `rollId` R\*, `workOrderId` R\*, `workOrderStepId` SN\* (guard saymıyor) · `updatedAt` YOK | tx içi | `test_tambur_plan_gate` (C6 aynı token → 1 satır) · `test_plan_deviation_scorecard` (negatif sondalı) · §26/§26b (saha 0 satır → vakumen yeşil) | — |
| **RollBarcodeCounter** (`roll_barcode_counters`) | **Delegate ile HİÇ yazılmıyor** — tek yol ham SQL `roll-barcode.helper.ts:82` `INSERT … ON CONFLICT (day,type) DO UPDATE SET n=n+count RETURNING`. Çağrı 12: tx-ÖNCESİ havuz 5 (tambur :906/:2116/:2462/:2775/:3178) · tx-İÇİ 7 (inventory :905/:4439, subcontractor :517/:3046, batch-drop :418, roll-finalize :172, roll-disposition :296) (K12 satır 20) | STK-07 (satılabilir top barkodlu) · STK-09 (biçim `T+GGAAYY+{H|F}+NNNN`, boşluk bilinçli) · SYS-04 | PK `(day, type)` · `createdAt/updatedAt` YOK · şema yorumu `day=YYMMDD` **bayat** (veri GGAAYY, K10 H-13) | Satır kilidi **commit'e kadar** tutulur; `finishStep`/bypass son-adım dalında WO'dan SONRA tx içinde → tüm KK1 girişlerini serileştirir (K3b H-2: 1345 ms vs 39 ms ölçümü) | `test_barcode_reservation` (EŞZ ✓ 20×3 + 30/30 havuz; `KNOWN_TX_INTERNAL` iki yönlü liste) · `test_roll_barcode` (EŞZ ✓ 20 benzersiz) | K3b H-2 · K4 H15 · K12 H15/H17 (bilinçli boşluk F-CORE-VER-008; `finalizeWarehouseCut` yorumu clientToken'a dayanıyor ama kod yazmıyor — K3b H-5) |

---

## 2. İŞ EMRİ / ÜRETİM

| Varlık | Yazma yolları (K4) | Değişmezler (K10) | DB kısıtı (K2a/K2b) | Tx & kilit (K3a/K3b) | Bekçi (K11) | ② işaret |
|---|---|---|---|---|---|---|
| **WorkOrder** (`work_orders`) | **11 dosya** (8 servis + 3 helper): workorder (9; havuz 1 `lockWorkOrder` :6100) · order (**4** — `cancelWithActions` CANCELLED/COMPLETED/APPROVED + `type`, K4 H2) · workorder-link (4; havuz 2 `changeTargetColor/changeWidth` claim) · tambur-undo (2) · split (2) · kursun-qc (1) · tambur-manual (1) · manual-move (1) + roll-step (`ensureWorkOrderInProgress`, `completeWorkOrderIfStepsDone`), workorder-clone (create), workorder-locks (`touchWorkOrderTx`). `status` 15 site; **IN_PROGRESS diriltmesi 7 sitede doğrudan** (K4 H3). Ham SQL: merge MOVE `targetItemId/targetColorId`. Script `fix_workorder_type_from_links.ts` (dry-run). | SM-02 (PLANNED→IN_PROGRESS→COMPLETED; CANCELLED/SUPERSEDED terminal) · WO-01 (tip = bağın aynası) · WO-02 · WO-04 · WO-08 (plan kilidi MALA bakar) · WO-09 (kapanış dispozisyonu) · WO-11/12/13 · FAS-10 · SYS-03 | unique `workOrderNumber` (`IE+GGAAYY+NNNN`) · `clientToken` partial · **CHECK** `work_orders_stockprod_targetItem` (STOCK_PRODUCTION ⇒ targetItemId) · 7 FK hepsi SN\* · içe R\*: steps, batches, traveler_cards, dispatches, receipts, bypass, plan_deviations, links · C: target_properties · `parameters` Json (tam-yaz) | **"WO-first" ailesi** (W→R→A22): dispatch F1, receive F3, complete W3, batch-drop B1, I2/I3/I5/I7, W2/W4/W6/W7, O4/O5. **"roll-first"** (R→A22→W): `attachRolls` W5 (W en sonda :4566), `manualMove` V1, split P1/P2 → **ABBA-1** (K3a §3.2). `create` W1 tx içinde **havuz** `generateWorkOrderNumber` :989 + `readWorkOrderDefaultPlanDurationDays` :1002 (K3a L1/L2). `updateTargetProperties` W8 **kilit YOK, claim YOK, statü guard YOK**. Tip flip (`linkOrderLines` K1 :381, `unlink` K2 :463, `cancelOrderLine` O1 :500) W kilitsiz, `targetItemId` guard'sız → CHECK'e çarpar. `tambur-undo` restore/full **WO kilidi HİÇ almaz** (Step→WO sırası, K3b S1). `tambur-manual` W'yi claim'den SONRA alır (K3b H-11). | `test_wo_terminal_guard` (çıplak COMPLETED 0) · `test_phase1_uretim_hardening` (EŞZ ✓ softDelete 2→1) · `test_phase3_stok_hardening` (EŞZ ✓ lockWorkOrder) · `test_consistency_derived` §21/§22 · `test_client_token_idempotency` CT3/CT4 · `test_quick_start_wo` · `test_wo_cancel_disposition` · `test_wo_cancel_fason` · `test_wo_manual_complete` · `test_wo_target_color_guard` (59) · `test_wo_route_coverage_goods` · `test_manual_move*` (5) · `test_k14_lock_edges` · `test_kursun_regime_lock` (WO satır kilidi SIRASI bekçisiz) | K3a #1 (`prepareFasonCancelDecision` tx-öncesi yazım) · #2 ABBA-1 · #6 `updateTargetProperties` · #11 tx-içi havuz · #18 tip flip guard'sız · K3b H-1/H-4/H-11 · K10 H-6/H-10/H-11 · K11 H-6 (dispozisyon/bağ/iptal paralel bekçisiz) · K6 H-11 (quickStart telafi düşerse yetim PLANNED WO + ACTIVE kart) |
| **WorkOrderStep** (`work_order_steps`) | 6 dosya: **TÜRETİM** tek yazıcı `roll-step.helper.ts:145 recomputeStepStatus` (12 dosyadan çağrı) **AMA** `subcontractor.service` 7 sitede `status` DOĞRUDAN yazıyor (:1108 ACTIVE, :2118/:2128 COMPLETED/PENDING, :2959, :3474, :6175 COMPLETED, :6199 SKIPPED) — K4 H1; workorder (7: replace `delete` :5411 / update / create nested :1007 + SKIPPED :3547/:4058/:4344); manual-move (SKIPPED/PENDING :785/:801); tambur (update `stepData` RMW :1140-1153, tek akış, saha 0 dolu); kursun-qc (havuz `isUrgent` :1697, **ham SQL `priority` havuz** :1665). Nested create: clone helper, seed/demo 12 site. | SM-03 (türetim + giriş noktası kuralı) · WO-04 · WO-07 (sıra tekil, `completedAt ≥ startedAt`) · SM-11 (bypass bağı) | unique `(workOrderId, stepSequence)` · **CHECK** `work_order_steps_time_order` · FK `workOrderId` R\*, `stationId` R\* · içe R\*: movements, operations, dispatches.stepId, receipts.stepId, bypass · **SN\***: rolls.currentStepId/producedInStepId, roll_errors.detected/processed, scans, **variances, plan_deviations** (silme guard'ı `_count` listesinde YOK — K2a H5) · partial index `status<>'COMPLETED'` | çağıran WO satır kilidi altında (sözleşme `roll-step.helper.ts:190-191`) — tambur-undo karşılamıyor; `replace` :5411 `refCount=0` guard + W claim; `stepIds` pre-tx 4 yerde (K3a §6 #5) | `test_consistency` **§20** (`:436-520`; saha yedeğinde 0 satır) · `test_roll_cancel_step_recompute` · `test_manual_move_backflush` · `test_route_skip_warning` · `test_kursun_machine_order` (priority) | K4 H1 (bounded-context sızıntısı: fason servisi statü yazıyor) · K2a H5 · K2b §4 A-30 (adım sırası boşluksuz DB'de yok) · K10 §20 = sorgunun kör noktası (CLAUDE.md 2026-08-22) |
| **WorkOrderToOrderLine** (`work_order_to_order_lines`) | workorder-link (createMany `skipDuplicates` :376 / `delete` :454) · order.service **deleteMany ×3** (:493 cancelOrderLine, :2814 softDelete, :3219 cancelWithActions) · workorder (deleteMany :5502 replace; nested create :1007/:5510) · clone helper nested | WO-01 (tip ↔ bağ; §21) · SIP-02 | `@@id(workOrderId, orderLineId)` (PK = tekillik) · CHECK `allocatedQty ≥ 0` · FK `workOrderId` R\*, **`orderLineId` C** (kalem replace `orderLine.deleteMany` → bağ sessizce düşer; guard yalnız CANCELLED/SUPERSEDED **olmayan** WO'ları sayar — K2a H3) · `allocatedQty` **her zaman 0** (ölü kolon, K7a §1.1) | PK seddi idempotent; pivot replace deseni (K4 §5a d) | `test_consistency_derived` §21 · `test_workorder_order_link` · `test_order_cancel_card_dirty` | K2a H3 · K3a #18/#20 |
| **WorkOrderTargetProperty** (`work_order_target_properties`) | workorder (deleteMany/createMany :5503/:5674-5678; nested :1007/:5510) | WO-08/12 (hedef ÖNERİDİR) | unique `(workOrderId, propertyId)` · FK `workOrderId` **C** (açık) · `updatedAt` YOK | `updateTargetProperties` W8 **kilitsiz/claim'siz/statü guard'sız**; `computeWorkOrderLocks(prisma)` yalnız pre-tx | `test_property_targetable` · `test_route_step_targets` | K3a #6 · K10 H-6 |
| **KursunBypassAssignment** (`kursun_bypass_assignments`) | kursun-bypass (4: updateMany/create; `assign` :955 claim ∨ :966 create) + guard helper (`repointPendingBypassAssignmentsTx` :149, `voidStalePendingBypassAssignmentsTx` :194 — çağıranlar workorder :4228/:4068/:3560, split :299/:528/:680). Script `demo_kursun_planlama` deleteMany. | SM-11 (adım başına ≤1 açık; açık ⇒ WO canlı) · milestone confirmation (dağıtım ön koşul DEĞİL) | **partial unique** `kursun_bypass_one_pending_per_step_uq (workOrderStepId) WHERE completedAt IS NULL AND cancelledAt IS NULL` (şemada YOK) · 6 FK: 3 R\* + `completedById/cancelledById` **R** (açık) · `completedVia` enum | `assign` W İLK → tüm guard'lar kilit altında (K3b §1.5 ✓); `cancelAssignment` KBA claim (WO kilidi almaz); `completeFromDistribution` W → KBA claim → raw movement kapanış → **K tx içi** → finalize; P2002 catch tx dışı → 409 | `test_kursun_bypass` (EŞZ ✓ 2× assign → 1 satır; 2× ham INSERT → P2002) · `test_kursun_unassigned_close` (EŞZ ✓ 3→1) · `test_kursun_bypass_repoint` · `test_consistency_derived` §23 | K3b H-2 (sayaç kilidi tx içi) |
| **Manifest** (`manifests`) | workorder `createManifest` :6552 **havuz** create + `nextDailySeq` (wBR :6537); Zod YOK (`notes` typeof) | DOC-01 (`manifestNo`) | unique `manifestNo` · FK `workOrderId` R\* · `snapshot` Json donmuş | havuz; sayaç yarışı wBR ile (K4 H6) | — (bekçi bulunamadı; saha 0 satır) | K4 H6 · K1b #12 (Zod yok) |

---

## 3. FASON (üretim fasonu + kartela)

| Varlık | Yazma yolları (K4) | Değişmezler (K10) | DB kısıtı (K2a/K2b) | Tx & kilit (K3a/K3b) | Bekçi (K11) | ② işaret |
|---|---|---|---|---|---|---|
| **SubcontractorDispatch** (`subcontractor_dispatches`) | subcontractor (5; havuz 1) · batch (3 updateMany — K15/K16 cerrahi) · split (1 update `undyedMove`) · `batch-dispatch-surgery.helper` (5 updateMany/create/update, `generateDispatchNoTx` **findMany sayacı kilitsiz**) · script `smoke_fason_http` deleteMany · seed. `totalQty` snapshot **6 yazıcı** (K7a §4.5: create Σ`currentQty`, iptal 0, merge/split Σ`dispatchedQty`). Damgalar `cancelledAt`/`directShippedAt` (enum yok). | FAS-01 (AT_SUB ⇒ açık SDI) · FAS-08 (bir sevk = bir parti, cross-WO guard `FOREIGN_BATCH`) · SM-06 (OPEN_OUTSTANDING dörtlüsü tek kaynak `fason-open-dispatch.helper:48-80`) · SEV-15 (`totalQty = Σ SDI`) · DOC-03 (`SUBCONTRACTOR_DISPATCH` belgesi) | unique `dispatchNo` (`FS+GGAAYY+NNNN`) · **clientToken YOK** (replay = pre-tx küme-eşitliği guard `:742`) · 8 FK R\*/SN\* · içe **C**: items, direct_ship_allocations (defter cascade — K2a H4) · `direct_shipments.dispatchId` R (açık) | F1 `dispatch`: W İLK → autoAttach R claim → step ACTIVE → parti (**A22**) → **Q** FS no → create(+items nested) → `freezeForSource` → R çoklu claim (hepsi-ya-hiç) → movement/op/scan. F2 `cancel`: W → dispatch claim `{id,cancelledAt:null}` → R claim `{AT_SUB,currentStepId}` → raw movement `qtyOut NULL` → op deleteMany → step yeniden değerlendirme → `completeWorkOrderIfStepsDone`; `acceptedReceiptItem/movedRolls` **pre-tx** (kısmi kabul penceresi #6). `bulkDispatchStep` SEPARATE: parti başına ayrı tx, **`failed[]` YOK** (ilk hata fırlar, öncekiler commit'li). Cerrahi `moveRolls/mergeBatches/splitBatch` W İLK + claim count kontrollü + wBR. | `test_dispatch_claim_step_match` (EŞZ ✓ 2→1 steal + çift) · `test_fason_open_dispatch_single_source` (AST) + `_semantics` · `test_consistency` §15/§19, derived §24a/§24b · `test_dispatch_cross_wo_batch_guard` · `test_batch_k15_merge`/`k16_split_move` (bayat "kilitsiz/P2002" yorumu, K11 H-1) · `test_bulk_dispatch_rollids` · `test_dispatch_without_color` | K3a #13 (SEPARATE sessiz kısmi) · K3a #6 · K7b H9 (karne ham SQL kopyası AST bekçisi dışında) |
| **SubcontractorDispatchItem** (`subcontractor_dispatch_items`) | subcontractor (updateMany `remainderClosedAt` :3444; **create yalnız nested** :1244) · batch (updateMany) · surgery helper (updateMany ×2, claim count kontrollü :253/:344) | FAS-02 (kalem yalnız TAM satırla kapanır; `remainderClosedAt`) · FAS-03 (`Σ receivedQty ≤ dispatchedQty` — KOD) · FAS-11 | unique `(dispatchId, rollId)` · CHECK `dispatchedQty > 0`, `dispatchedWeight ≥ 0` · FK `dispatchId` **C**, `rollId` R\* · `remainderClosedAt` sonradan yazılır ama **`updatedAt` YOK** (K2a H14) | `closeRemainder` F4: W → roll taze → R claim `{id,AT_SUB}` → variance → SDI updateMany toplu → raw movement → step COMPLETED → complete-if-done | `test_fason_partial_receive` · `test_fason_partial_receive_overcount` · `test_k14_lock_edges` (zombi açık sevk) | K1b H11 (close-remainder 409 yarış claim'i) · K11 H-4 (close-remainder paralel bekçisiz) |
| **SubcontractorReceipt / Item / Property** (`subcontractor_receipts`, `_items`, `_properties`) | subcontractor (3 create/updateMany · 1 createMany · 3 createMany/deleteMany) · script `smoke_fason_http` deleteMany. Kabul `receive` F3 doğan toplar `roll.createMany` :3055 + `recordVarianceTx` (çekme) + `recordPlanDeviationTx` + **tx DIŞI** best-effort `changeWidth`/`changeTargetColor` (:3270/:3297; `changeWidth` hatası `console.warn`, `postWarnings` en için doldurulmuyor — K6 H-6). | FAS-02/03/04/06/07/09/11 · SYS-03 (replay kimliği `SubcontractorReceipt.clientToken` — saha 2/143 dolu) · PAR-05 (2.+ teslimat yeni parti) | unique `receiptNo` (`FK+…`), `clientToken` **düz** unique, `(receiptId, newRollId)` (`newRollId` = ORİJİNAL top, ad yanıltıcı), `(receiptId, propertyId)` · FK `receiptId` **C** (defter cascade), `newRollId` R\*, `sourceDispatchItemId` SN\* · `receivedQty` nullable (**saha 635/638 NULL**), `isPartial` · CHECK YOK (`receivedQty ≤ dispatchedQty` yalnız KOD) · `updatedAt` YOK | F3 `receive`: W İLK → hedef renk taze (409 `TARGET_COLOR_CHANGED`) → `freshReturns` → takip-teslimat count → **Q** FK no → receipt create (clientToken) → raw movement → R tam-kabul claim `{id in,AT_SUB}` count===len → kısmi decrement claim döngüsü `{id,AT_SUB,currentQty gt}` → SRI createMany → step COMPLETED → complete-if-done → [newRolls: **A22** takip parti → **K** tx içi → createMany → deviation → variance]. **wBR predicate'siz** → aynı clientToken P2002 5× retry → yanıltıcı 409 (K3a #9). F5 `cancelReceipt`: W İLK → bornRolls CANCELLED (**statü koşulsuz** :4846) → receipt claim (mutasyonlardan SONRA, aynı tx) → movement yeniden aç → R claim `SUBCONTRACTOR_CONSUMED` → kısmi increment claim → op deleteMany → variance reversedAt; **LIFO guard pre-tx** (:4757, K3a #7). | `test_fason_partial_receive` (P3 aynı token cached) · `test_fason_receive_idempotency_concurrency` (EŞZ ✓ **zayıf**: born=1, ok sayısı ölçülmez) · `test_fason_kabul_partial_overlap` (küme replay 409) · `test_fason_receive_cancel_rereceive` · `test_race_conditions` C (receive‖dispatch invariant) · `test_consistency` §14 · `test_fason_receipt_color_width` · `test_wo_fason_quick_receive` | K3a #9 · #7 · K2a H9 (token'sız makbuzda replay koruması yalnız `isPartial=false`) · K1b H10 (en uzun tx adayı: `returns ≤300` + `newRolls ≤300`; K12 H3: `rollShipQtys` record tavansız) · K11 H-4 (kısmi kabul / iptal / close-remainder paralel bekçisiz) · K6 H-6 · K7b H3/H4 (karne legacy `receivedQty NULL` → canlı `currentQty` fallback) |
| **SubcontractorDirectShipAllocation / DirectShipment** (`subcontractor_direct_ship_allocations`, `direct_shipments`) | subcontractor (`executeDirectShip` F7: DSA create döngüsü :6288, DirectShipment create :6134) · shipping (`setDirectShipmentInvoice` havuz updateMany) · merge MOVE `customerId` | FAS-05 (terminal; `DSA` `shippedQty`'ye sayılır) · SEV-01 (defter 2. kaynağı) · SEV-14 (fatura izi) · DOC-03 | unique `(directShipmentId?, orderLineId)` (NULL'lanabilir, eski kayıt), `shipmentNo` (`DSK`, `text`) · CHECK `qty > 0`, `totalQty > 0`, `rollCount > 0` · FK `dispatchId` **C** (defter cascade — K2a H4), `orderLineId` R\*, `directShipmentId` SN\*; DirectShipment `dispatchId`/`customerId` **R** (açık) · DSA `updatedAt` YOK | F7: W İLK → `applyDirectShipSplits` (`createFasonShipChild` :496: **K** tx içi, parent decrement claim `currentQty gte`, **`initialQty` de düşer** — K7a #18) → dispatch claim `{directShippedAt:null,cancelledAt:null}` → raw movement → R consumed claim → **Q** DSK no → DirectShipment create → step COMPLETED → [**L** sıralı TAM küme → cap kontrolü tx-içi → DSA create → recompute **O**] → freeze. wBR predicate'siz. | `test_race_conditions` B/D (EŞZ ✓ over-cover 2→1; çifte-ateş 2→1) · `test_direct_ship_scenarios` (EŞZ ✓ ≥1, alreadyDirectShipped) · `test_direct_ship_api` (HTTP) · `test_direct_ship_fason` · `test_input_rolls_directship` · `test_accounting_direct_ship` | K7a #18 · K7b H5 (export Σ`rolls.currentQty` vs `totalQty` üç yüzey iki tanım) · K12 H1 (`cancelReturn`/`createReturn` `lockShipmentScopeTx` koşullu — doğrudan sevk iadesinde kilit YOK) · saha 0 satır (ölçülemez) |
| **KartelaDispatch / Item / KartelaReceipt / Item / Swatch / SwatchStockReduction** | kartela (dispatch 2 · item **yalnız nested** :287 · receipt 2 · item 1 createMany · swatch 3 · reduction 1) · shipping Swatch **10 updateMany** (çuval işlemleri) · merge MOVE swatches/reductions · script `clean_test_residue` deleteMany | SM-01 kartela kenarları (`WAREHOUSE → AT_KARTELA → KARTELA_CONSUMED`) · STK-15 (§7b/§7c) · SYS-03 (yalnız `SwatchStockReduction.clientToken`) · L3: kaynak top `KARTELA_CONSUMED` olur ama **`currentQty` korunur** (K7a #20) | unique `dispatchNo` (KD), `receiptNo` (KR), `Swatch.cardNumber/barcode`, `(dispatchId, rollId)`, `(receiptId, consumedRollId)`, `SwatchStockReduction.clientToken` partial · CHECK `dispatchedQty > 0`, `dispatchedWeight ≥ 0`, `kartelaCount > 0`, `count > 0` · FK item cascade; `KartelaReceipt.dispatchId` SN\* · Swatch DEFERRABLE FK (sackId,shipmentId) | `dispatch` :282 wBR: **sayaç findMany kilitsiz İLK ifade** → create(+items) → freeze → R çoklu claim (AppError 409, retry'a girmez ✓); idempotent kapı :223-246 **heuristik** (aynı firma + küme), **clientToken YOK** (K1a H18). `receive` :610 wBR: sayaç → receipt → items → R çoklu claim → Swatch createMany (P2002 → tüm tx retry). `reduceStock` :1358: reduction create (`clientToken` İLK = çapa) → FIFO `take N` → çoklu claim count===N (kısmi → rollback). `cancelDispatch/cancelReceipt` üç claim count kontrollü. | `test_kartela_stock_and_ship` · `test_sack_status_invariant` (EŞZ ✓ kartela dispatch ‖ scanIntoSack 2→1) · `test_phase5_kartela_hardening` · `test_client_token_idempotency` CT5 (reduceStock) · `test_consistency` §7b/§7c (Tur 2) | K1a H18 (dispatch/receive clientToken yok → mobil retry mükerrer) · K7a #20 (KARTELA_CONSUMED metraj iki sözleşme) · K7a #9 (`computeWoMaterial.finished` kartela çıktısını saymaz) · saha kartela akışı **0 kayıt** |

---

## 4. ÇUVAL / SEVK / İADE

| Varlık | Yazma yolları (K4) | Değişmezler (K10) | DB kısıtı (K2a/K2b) | Tx & kilit (K3a/K3b) | Bekçi (K11) | ② işaret |
|---|---|---|---|---|---|---|
| **Sack** (`sacks`) | shipping (**14**: create/updateMany/update/**delete** `removeSack` :1017 fiziksel — bilinçli istisna; havuz 2: `setSackNotes` :1704 guard'sız annotation, `labelDirty` :1723) · label (1 havuz `recordSackPrintEvent` updateMany) · `shipment-locks.helper touchWarehouseSackTx` (updateMany) · ham SQL merge `labelDirty` :928 + MOVE `customerId` · script `repair_sack_ghost_rolls` (2), seed ×3. `labelDirty` 11 yazım noktası (K9 A8). `weightSource` **41/41 NULL** sahada. | SM-05 (depo çuvalı içeriği yalnız `touchWarehouseSackTx` altında; sevkiyata atanmış çuval içerik değişmez) · SEV-09 (`sackNo`=barkod; `seq ⇔ shipmentId`) · SEV-10 · SEV-12 (EXPORT tartı — pre-tx) · STK-15 (hayalet içerik yok) · DOC-05 (SACK etiketi fail-closed) | unique `sackNo` (`CV+GGAAYY+NNNN`), `clientToken` **düz**, `(shipmentId?, seq?)` (ikisi NULL'lanabilir — havuz çuvalı bilinçli), `(id, shipmentId)` (bileşik FK hedefi; `migrate dev` DROP etmek ister) · CHECK `weightKg ≥ 0` · FK `customerId/branchId/shipmentId/weighedById` SN\* · içe `sack_allocations.sackId` **R** (açık) · index `[customerId, createdAt]` FIFO | S1 `openSack` wBR (clientToken/sackNo P2002 retry edilmez, replay catch ✓) → **Q** `nextSackNo(tx)`. S3/S5 `scanIntoSack` taşı: **kaynak çuval kilitlenmez** (yalnız hedef `touchWarehouseSackTx` + R claim `sackId:from`). S10/S12 `moveRoll(s)ToSack`: iki çuval kilidi **id-sırasız** (ABBA-2). S12 `updateMany` **count kontrolü YOK** (`moved` sayısı). S13 `splitSack` wBR kaynak kilitli, "kilit altında taze sayım" S3/S5 ile kesişir. S14 `weighSack` claim = touch + simüle kantar üç sinyal. S15 `removeSack` taze sayım → toplu → `delete`. S16/S17 çuval claim döngüsü **istemci `sackIds` sırası** (ABBA-3). | `test_sack_pool_lifecycle` · `test_sack_status_invariant` (EŞZ ✓) · `test_shipping_client_token` (openSack aynı token → tek çuval) · `test_sack_split_and_relabel` · `test_sack_reassign_customer` · `test_sack_weigh_source` · `test_sack_notes` · `test_sack_mismatch` · `test_sack_label` (fail-closed) · `test_consistency` §6/§7 · `test_customer_sack_delete_guard` | K3a #12 (ABBA-2/3) · #15 (`scanIntoSack` kaynak kilitsiz; `splitSack moved<claimable` sessiz) · K11 H-3 (openSack çift / çuval kapatma paralel bekçisiz) · K7a #7 (`weightKg` BRÜT şemada, belgede `netKg`; kg çuvalın ilk topuna) · K2b H-4 (composite FK ON UPDATE CASCADE — çuval sevkiyat değiştirince topların `shipmentId` DB tarafından yazılır) |
| **Shipment** (`shipments`) | shipping (**8**: create/update/updateMany; havuz 2: `setDispatchNote` :1610, `setShipmentInvoice` :1627 claim `{id,status}`) · `shipment-locks.helper touchShipmentPlannedTx` · merge MOVE `customerId` · seed ×3. `status` 4 site (`:1404` PLANNED create · `:1812` DISPATCHED · `:1966` CANCELLED · `:2179` PLANNED storno). **`cancelledAt` kolonu YOK** (K2a H13). | SM-04 (`∅→PLANNED|DISPATCHED`, `PLANNED→DISPATCHED`, `PLANNED→CANCELLED`, `DISPATCHED→PLANNED` storno; DB seddi YOK) · SEV-06 (storno ≠ iade; 409 engelleri fatura/aktif iade/aynı gün) · SEV-11 (`ShipmentOrder.isActive ⇔ PLANNED`) · SEV-13 (boş sevkiyat yok — pre-tx) · SEV-14 (fatura izi claim) · SEV-16 (belge donar aynı tx) · SEV-07 (**tek aktif sevkiyat kuralı KALDIRILDI**) | unique `shipmentNo` (`SVK`), `clientToken` **düz** · 8 index · FK `customerId` R\*, diğerleri SN\* · içe SN\*: sacks/rolls/swatches.shipmentId, roll_returns.fromShipmentId; R (açık): shipment_orders · DB-only 2 DEFERRABLE bileşik FK · **statü enum'u geçiş seddi YOK** | S16 `createShipment` wBR (predicate: clientToken hariç P2002 retry): **Q** SVK no → create → **S** claim döngüsü (istemci sırası) → roll/swatch toplu → shipmentOrder → sackAllocation → [onay KAPALI ise `performDispatchTx` aynı tx]. S21 `dispatchShipment` → `performDispatchTx`: **H** claim `{id,PLANNED}` → hayalet guard `SACK_ABSENT` (fail-closed) → R grup `updateMany {shipmentId,status}` + `preShipStatus` → **L** sıralı tam küme → recompute **O** → `freezeForSource` (v max+1); boş/tartı guard **yalnız pre-tx** (#17). S22 `cancelShipment` → `cancelPlannedShipmentTx` H claim → L → alloc deleteMany → toplar/çuvallar havuza → recompute. S23 `undoDispatch`: **8023 tx'in İLK ifadesi** (`rollReturn.count`'tan ÖNCE ✓) → H claim DISPATCHED→PLANNED → R grup → L → recompute → PD void → [`releaseSacks` → `cancelPlannedShipmentTx` aynı tx]. Yeni `updateMany` :2179/:2186/:2197 (K12 H2 sınıflandırılmalı). | `test_shipment_undo_dispatch` §10 · `test_shipment_scope_lock` (SIRA `indexOf` + EŞZ ✓ undo‖createReturn invariant) · `test_shipping_client_token` · `test_shipment_invoice` · `test_shipment_list_gross` (RepeatableRead metin) · `test_dispatch_report_gross` · `test_shipment_filters/sort` · `test_consistency` §4/§5/§9 · `test_db_invariants` §3 | K11 H-3 (**`createShipment`/`dispatchShipment` paralel bekçisiz** — en yüksek para/stok etkili yol) · K3a #16 (`getActiveShipmentLinks` pre-tx; iptal edilmiş siparişe PLANNED sevkiyat) · #17 · K1b H1 (`cancelShipment` gövdesiz/sebepsiz, WRITE kümesi mobil) · K12 H2/H14 (`cancelShipment` 8023 alıyor mu) · K2b H-5 (iki PLANNED sevkiyat DB'de bloklanmıyor) |
| **ShipmentOrder** (`shipment_orders`) | shipping (5: deleteMany/createMany `setShipmentOrdersTx` :1290, updateMany `isActive`) · seed | SEV-11 (denorm `isActive`; bakım uygulama katmanında) · SEV-02 (tahsis ⊆ sipariş kümesi) · SIP-05 (müşteri/şube uyumu pre-tx) | `@@id(shipmentId, orderId)` · FK `shipmentId` **R** (açık — muhasebe defteri), `orderId` R\* · **eski partial unique `shipment_orders_active_order_uq` KALDIRILDI** (`20260711120000:87`, A-17) · `isActive` mutasyonlu ama **`updatedAt` YOK** | dispatch/cancel/undo tx'lerinde `updateMany isActive` | `test_consistency` §5 | K2b H-5 · K2a H14 (updatedAt konvansiyonu) |
| **SackAllocation** (`sack_allocations`) | shipping (3: `writeShipmentAllocationsTx` :1339 **sil-yaz** `deleteMany`+`createMany`; :1976 iptal deleteMany) · seed/demo · **`distributeSacksToLines`/`allocate` motoru** `allocation.helper.ts:93-117, 211-239` | SEV-01 (**defter-otoritatif**: `OL.shippedQty = Σ SA.qty [DISPATCHED] + Σ DSA`; PLANNED SAYILMAZ; rezerv YOK) · SEV-02 · SEV-08 (**`Σ sevk ≤ OL.quantity` HİÇBİR YERDE**) · SEV-05 (brüt: iadede DOKUNULMAZ) | unique `(sackId, orderLineId)` (top DEĞİL, çuval × satır) · CHECK `qty > 0` · FK `sackId` **R** (açık), `orderLineId` R\* · `updatedAt` YOK (sil-yaz) · **tek DB seddi olmayan denormalize alan** (`order-status.helper` başlığı) | sevkiyat kurulum/onay tx'i içinde; `need = quantity − shippedQty` **yalnız DISPATCHED'i düşer** → iki PLANNED sevkiyat aynı ihtiyacı iki kez tahsis (K10 H-2); `computeSackAllocations` çuval topları **SACK_ABSENT süzgeçsiz** (K7a #6) | `test_consistency` §1/§2 (Q-SEV-01/01b saha 0 drift) · `test_sack_pool_lifecycle` · `test_dispatch_claim_step_match` · `test_open_order_coverage` | K10 H-2 · K7a #6 · K2a H4 (DSA cascade) |
| **RollReturn** (`roll_returns`) | return (4: create/updateMany grup `returnGroupId`/update; havuz 1 `:998`) · script `clean_test_residue`, seed · merge MOVE `customerId/itemId/colorId` | SEV-05 (BRÜT: `RR.qty = iade anındaki currentQty`, kısmi iade yok; `shippedQty`/`SA` dokunulmaz; iade yalnız SHIPPED topa; `prevSackId` geri-ekleme) · SEV-06 (storno ↔ aktif iade) · DOC-03 (`RETURN_DISPATCH` grup lideri) | CHECK `qty > 0` · 15 index · FK `rollId/customerId/itemId/receivedById` R\*, `fromShipmentId/orderId/…` SN\* · **clientToken YOK** (≤200 top toplu iade) · `cancelledAt` soft | R1 `createReturn`: **8023 İLK ifade** — ama **KOŞULLU** `if (lockShipmentId)` (`orderedRolls[0]?.shipmentId`; K12 H1: doğrudan sevk/shipmentId'siz iade kilitsiz) → R tekil claim döngüsü `{id,SHIPPED}` (istemci sırası; A23 serileştirir) → create → grup updateMany → freeze. R2 `cancelReturn`: R claim `{id,expected,shipmentId:null,sackId:null}` → `rollReturn.update` koşulsuz → PD void/reissue; **8023 YOK, sevkiyat statüsü okunmuyor** (K3a #15 / K10 H-8). | `test_shipment_scope_lock` (EŞZ ✓ undo‖createReturn) · `test_return_bulk_group` · `test_iade_enhancements` · `test_return_scorecard` · `test_shipment_detail_gross` · `test_shipment_list_gross` · `test_dispatch_report_gross` · `test_consistency` (Q-SEV-05a/b/c saha 0) | K3a #4 / K10 H-8 (`cancelReturn` ∥ `undoDispatch` → SHIPPED top ↔ PLANNED/CANCELLED sevkiyat) · K12 H1 (koşullu kilit) · K1b H18 (clientToken yok, çoklu iade retry) · K11 H-3 (iade×iade paralel bekçisiz) |

---

## 5. SİPARİŞ

| Varlık | Yazma yolları (K4) | Değişmezler (K10) | DB kısıtı (K2a/K2b) | Tx & kilit (K3a/K3b) | Bekçi (K11) | ② işaret |
|---|---|---|---|---|---|---|
| **Order** (`orders`) | order.service (5 updateMany + **BaseService generic**: `create` override `:1951/:1971` `this.delegate.create` wBR, `update`, `softDelete`, `hardDelete` **= `softDelete`** `:2718-2720`) · `order-status.helper.ts:196 recomputeOrderStatus` (update — çağıranlar shipping dispatch/cancel/undo, subcontractor directShip, order cancelOrderLine/update/reopen) · scripts `clean_test_residue`, seed ×4 · merge MOVE `customerId`. `status` 4 site (`:2760/:3144` CANCELLED · `:3319` COMPLETED · `:3397` APPROVED). | SM-08 (**TÜRETİM** `recomputeOrderStatus`: `shipped≤0→APPROVED`; `totalRequired−shipped ≤ 5 m tolerans → COMPLETED`; CANCELLED ve manuel-kapalı terminal) · SIP-03 (`SIP+GGAAYY+NNNN` boşluksuz) · SIP-05/06/07 · SEV-01 (`Order.shippedQty = Σ OL`) · SYS-03 (clientToken) | unique `orderNumber`, `clientToken` partial · 7 index (`orders_active_createdAt_idx` **sahada YOK**) · FK `customerId` R\*, `branchId` SN\* · içe **C**: order_lines · R\*: shipment_orders · `cancelledAt/cancelReason/cancelReasonCode` (**sahada YOK**, migration `20260826130100`) · `orderDate/completedAt/cancelledAt` **index YOK** (K7b H18) | O3 `update`: **FOR UPDATE** (tek yer :2434, `= ANY(array)` sırası) → deleteMany (**kalem hard delete**, cascade `WorkOrderToOrderLine`) → update/create → **O** claim `{id, status: gözlenen}` → recompute **tam-küme `touchOrderLinesTx` OLMADAN** (#23). O4 `softDelete`: O claim → W sıralı → link deleteMany → WO type flip guard'lı. O5 `cancelWithActions`: **`CANCEL_WO` aksiyonları tx-ÖNCESİ ayrı tx'ler** (telafi yok) → O claim → W sıralı → CONVERT/UNLINK. O6 `manualComplete` claim ✓. O7 `reopen` claim → recompute **kilitsiz** (#23). O2 `quickOrderFromRolls`: R claim tx → `create()` **tx DIŞI** (yorum kabul ediyor). `getActiveShipmentLinks` yalnız pre-tx (#10). | `test_client_token_idempotency` CT1/CT2 · `test_phase4_fason_hardening` (EŞZ ✓ manualComplete 2→1) · `test_phase5_kartela_hardening` (EŞZ ✓ reopen 2→1) · `test_order_cancellation` · `test_order_cancel_reason` · `test_order_cancel_card_dirty` · `test_order_stats` (negatif sonda) · `test_hide_cancelled_lists` · `test_raw_sale_quick_order` · `test_mobile_order_permission` (HTTP token, 4xx'te tazeler) · `test_p2_order` · `test_helpers` | K3a #5 / K10 H-9 (reopen/update lost-update penceresi) · K3a §7 (`cancelWithActions` WO iptalleri tx dışı) · #11 (quickOrder yetim WAREHOUSE) · #16 (iptal siparişe PLANNED sevkiyat) · K1b H7 (`/permanent` = soft; Swagger "fiziksel" der) · K11 H-9 (iptal-sonrası token replay bekçisiz) |
| **OrderLine** (`order_lines`) | order.service (5: updateMany/deleteMany/update/create; havuz 1 `changeLineColor` :2694 claim; nested `lines` create) · **`label.service.ts:1391 updateOrderLineCustomerNames` havuz** (etiket domain'i sipariş satırı yazıyor — K4 H13, K1a H14) · `order-status.helper touchOrderLinesTx` (updateMany id-sıralı kilit) + `recomputeOrderStatus` (`shippedQty` yazımı **yalnız burada**) · script/seed · merge MOVE `itemId/colorId` · GENERATED `customerItemNameFold` | SIP-01 (`Açık = quantity − shippedQty`, **13 kopya hesap** K7a §4.1; `netGap` kelepçesiz) · SIP-02 (kalem iptali SOFT, `shipped` ile sayılır; `ACTIVE_LINE` tek kaynak AST bekçili) · SIP-04 · SEV-01 · **SEV-08 HİÇBİR YERDE** | **unique YOK** (kimlik `id`) · CHECK `quantity > 0`, `shippedQty ≥ 0` · FK `orderId` **C**, `itemId` R\*, `colorId` SN\*, `cancelledById` SN\* (**sahada YOK**) · içe **C**: required_properties, **work_order_to_order_lines** (kalem replace cascade — K2a H3); R\*: sack_allocations, direct_ship_allocations · `cancelledAt` (sahada YOK) · 6 index | O1 `cancelOrderLine`: **L(tek) claim `{id,cancelledAt:null}` → W type flip (`targetItemId` guard'sız) → L(hepsi sıralı) → O** = **iki-parti edinim** (ABBA-4; helper protokolü `order-status.helper.ts:205-209` bunu yasaklıyor). `touchOrderLinesTx` tam-küme sıralı: shipping :1878/:1975/:2210, subcontractor :6233 ✓; **kilitsiz** recompute: order :2537/:3411. `changeLineColor` havuz claim `{id,orderId,colorId:eski}` ✓ (uuid✗ K1b H19). | `test_order_line_scope_single_source` (AST; FOR UPDATE metin muafı — davranış sondası YOK) · `test_order_line_cancel` · `test_order_lines_available` · `test_spec_availability` · `test_consistency` §1 | K3a #3 (ABBA-4) · #9b (tip flip `targetItemId` guard'sız → CHECK) · K2a H3 (cascade) · K4 H12 (hard delete istisna listesinde yok) · K4 H13 (label.service sınır sızıntısı) · K7a #8 (`netGap` kelepçesiz) · K11 H-6 (kalem iptali paralel bekçisiz) |
| **OrderLineRequiredProperty** (`order_line_required_properties`) | order.service (deleteMany/createMany :2487; nested create :2498) | (WO-08 hedef türetimi: sipariş bağlanınca `orderLineRequiredProperty` birleşimi) | unique `(orderLineId, propertyId)` · FK `orderLineId` **C** · `updatedAt` YOK | O3 tx içi replace | `test_property_targetable` | — |

---

## 6. PARTİ / BELGE NUMARASI / BARKOD

| Varlık | Yazma yolları (K4) | Değişmezler (K10) | DB kısıtı (K2a/K2b) | Tx & kilit (K3a/K3b) | Bekçi (K11) | ② işaret |
|---|---|---|---|---|---|---|
| **Batch** (`batches`) | batch.service (4: `createBatchTx` create :233 → 8 çağıran (workorder :4239/:4553, manual-move :699, subcontractor :1140/:1231/:3014, split :359/:504) · `delete` :337 `deleteIfEmptyAndTraceless` (**hard delete**, boş+izsiz) · updateMany merge/split) · split (1 update `undyedMove` :666 koşulsuz `workOrderId`) · demo/fixture scripts | PAR-01 (**benzersiz DEĞİL**, `P01…P99` körlemesine sarar; 92 mükerrer grup sahada — **BİLİNÇLİ**) · PAR-02 (sayaç veriden türer; regex load-bearing) · PAR-03 (parti tek WO'ya ait — **HİÇBİR YERDE DB seddi**) · PAR-04 · PAR-05 · PAR-06 (tombstone `mergedIntoId`) · SM-07 · DOC-01 P istisnası (dolgusuz) · **`orderBy batchNumber` YASAK** | **unique YOK** (`batches_batchNumber_key` DROP `20260805120000:22`; yalnız pkey) · index `[createdAt]` (sayaç kaynağı `createdAt DESC LIMIT 1`), partial `splitFromId/mergedIntoId`, trgm · FK `workOrderId` R\*, `splitFromId/mergedIntoId` SN\* (soy) · içe SN\*: rolls.batchId; R\*: subcontractor_dispatches.batchId | **8022** `pg_advisory_xact_lock(8022,1)` sabit anahtar = sistem geneli tek seri kapı; `generateBatchNumberTx`'in **İLK ifadesi** (:126) ✓ — ama tx'in ilk ifadesi DEĞİL: kapsam içi WO→8022 (split :880→:920), dış çağıranlarda **8022→W** (`attachRolls` :4553→:4566, `manualMove` :699) → ters sıra (K3b S2/H-4, K3a ABBA-1). Kilit kalan tx boyunca tutulur. Cerrahi `moveRolls/mergeBatches/splitBatch` W İLK + claim count===len ✓ + wBR (mergeBatches retry YOK). | `test_batch_number_format` (39; **SIRA sahte-tx** `calls[0]===lock(8022,1)`; negatif sonda 4 sınıf; **gerçek DB paralel parti doğumu YOK**) · `test_tambur_manual_batch` · `test_batch_k15_merge` / `k16_split_move` / `k8_tools` (**bayat "kilitsiz/P2002/@unique" yorumları** — K11 §2d) · `test_batch_drop` · `test_split_per_roll` · `test_split_card_lineage` · `test_consistency` (Q-PAR-01/02/03/07 saha 0) | K3b H-4 / K10 H-11 (8022↔W ters sıra) · K11 H-1 (gerçek paralel parti doğumu bekçisiz; `withBarcodeRetry` parti yolunda ne koruyor?) · K4 H12 (Batch hard delete istisna listesinde yok) · K2a H16 (`findFirst({batchNumber})` yazan yeni yol yanlış partiyi bulur — AST bekçi var mı?) |
| **Belge numaraları** (`IE/SIP/SVK/CV/FS/FK/DSK/MUS/RNK/HATA/OZL/REC/ROT/IST/MAK/IADE/KD/KR/manifest`) | `utils/code-format.ts:82-113` `buildDailyCode`/`nextDailySeq` (**max+1, kilitsiz Q**) — 19 çağrı / 12 dosya; üreticiler `shipping:161/171` (`nextShipmentNo/nextSackNo(tx)` ✓), `workorder:575-600` **havuz client'ı, tx AÇIKKEN** (K3a L1; tx ikizi `workorder-clone.helper.ts:31 generateWorkOrderNumberTx` var, kullanılmıyor), `subcontractor:648`, `customer:61`, `base.service:976` autoCode, `batch-dispatch-surgery:69`, `kartela:284/:612`; `withBarcodeRetry` **25 çağrı** (P2002 → max 5, jitter 5-30 ms; **predicate'li yalnız 4** — K6 §2.4; tükenme 409 **audit'siz** K6 H-2) | DOC-01 (`PREFIX+GGAAYY+NNNN`, backend-authoritative; manuel `workOrderNumber`/`sackNo`/`orderNumber` izinli) · DOC-02 (günlük sayaç boşluksuz; Q-DOC-02 saha 0 boşluk) · SYS-04 | **her no `@unique`** (Prisma) — biçim/sıra sürekliliği DB'de YOK (A-12) · `DirectShipment.shipmentNo` `text` (VarChar yok) | **Q sınıfı**: sayaç okuması kilitsiz, çakışma DB unique + P2002 → wBR tx baştan (saha gün başına max 12-21 → nadir ama sıfır değil) · wBR closure'larında **tx-dışı iş** (customer.service:290-302, item:225-232, order:1942-1950 — her retry'da tekrar koşar) | `test_item_code_autogen` (EŞZ ✓ 2× otomatik kod → ikisi ok) · `test_order_number_override` · `test_batch_number_format` (jitter) · `test_traveler_print_active_card` (P2002 dalı gerçekten tetiklenir) · `test_race_conditions`/`test_work_session` (P2002 dalı) | K3a L1/#11 (IE no tx içi havuz — `roll-barcode.helper.ts:56-64` havuz doygunluğu emsali) · K6 H-2 (wBR tükenmesi iz bırakmaz) · K6 §2.4 (21/25 predicate'siz → her P2002 5× koşar) |
| **RollBarcodeCounter** | → §1 son satır (tek yazma yolu ham SQL; 12 çağrı; sayaç kilidi tx-içi/öncesi) | STK-09 | PK `(day,type)` | K satır kilidi commit'e kadar | `test_barcode_reservation` | K3b H-2 · K12 H15 |
| **PrintedDocument `version`** | → §8 (version = `max+1` kilitsiz `findFirst`; TravelerCard `version+1` read-then-write) | SM-10 · DOC-03/04 | unique `(docType, sourceId, version)` | claim `{id, ACTIVE}` reissue; lazy-init P2002 kazananı okur | `test_printed_documents` · `test_traveler_card_versions` | K2a H6/H7 |

---

## 7. ANA VERİ (master-data)

| Varlık | Yazma yolları (K4) | Değişmezler (K10) | DB kısıtı (K2a/K2b) | Tx & kilit (K3a/K3b) | Bekçi (K11) | ② işaret |
|---|---|---|---|---|---|---|
| **Item** (`items`) + `ItemAllowedColor/Property` | item.service (4: create wBR :228 / update :453 / updateMany reaktivasyon claim :246; pivot deleteMany/createMany :257-258/:455-467 **skipDuplicates YOK**; havuz 1 ×2) + BaseService generic (`item.routes.ts:13`) · import `item.adapter` → servis · scripts `normalize-names` (update `name`), seed-* · merge MOVE **7 kural** + CONFLICT `DELETE` · `quickCreateFabric` (`mobile:kk1-desen`, `pendingReview`) | MD-01 (katlanmış ad tekilliği — **yumuşak kapı**) · MD-02 (tombstone) · MD-03 (kod tekilliği; katlanmış kod **yalnız Item'da 8026 kilitli**) · MD-06 (izin listesi pivotları) · DOC-01 (`STK-NNNNNN` düz sayaç; 209 elle kod meşru) | unique `code` · `@@unique([nameFold])` → DB **partial** `WHERE mergedIntoId IS NULL` — **sahada YOK** (1 canlı mükerrer `v-1430`; dev'de VAR) · GENERATED `nameFold/descriptionFold` · FK `mergedIntoId` self **SN\*** (survivor permanent silinince tombstone dirilir — K2a H1; guard `mergedChildren` saymıyor) · içe R\*: rolls, order_lines, swatches, product_recipes, roll_returns, reductions; SN\*: work_orders.targetItemId; C: pivotlar, customer_item_aliases | `create` :228 wBR (`nextItemCode()` **havuz, tx öncesi**) → **8026 `lockCodeScopeTx` İLK ifade** (yalnız manuel kod) → `item.findMany` **TÜM tablo** (kod adayları, kilit altında — K3b veri-performans) → claim `{id,isActive:false}` → pivot replace. `update` :453 pivot replace tx; ad/kod/aktiflik doğrulamaları **pre-tx**. `assertNameNotDuplicate` `base.service:729` check-then-act (havuz) + DB seddi (varsa). | `test_item_code_case_uniqueness` (EŞZ ✓ 5→1, negatif sonda) · `test_item_code_autogen` (EŞZ ✓) · `test_master_data_name_dup` §9 (DB seddi + tombstone) · `test_item_quick_create` · `test_masterdata_guards` · `test_name_normalization` · `test_name_uppercase_storage` · `test_fold_contract` · `test_db_invariants` §1 (**sahada bilerek KIRMIZI**) · `test_consistency` §18 · `test_hard_delete_guard_coverage` | K2a H1 (mergedInto SN) · K2b H-1 (yumuşak kapı; enforce elle `db execute`) · K3b veri-performans (`findMany` tüm ürün tablosu kilit altında) · K12 H11b (SET NULL FK 168 — hard-delete guard allowlist 69 yeni bağı kapsıyor mu) |
| **Customer / CustomerBranch / CustomerItemAlias / CustomerColorAlias** | Customer **delegate yok** — BaseService generic (`customer.routes.ts:17`; `nestedCreateFields: branches`; `validateName`) · customer-branch.service (3; **havuz 3**) · customer-alias.service (upsert/`delete` havuz ×5) · color.service `syncCustomerAssignments` (deleteMany/updateMany/createMany tx :319-358) · order.service `promoteCustomerAliases` (tx dışı, best-effort) · import adaptörleri ×4 · merge: 7 MOVE + MERGE_FIELDS (alias) · seed | MD-01/02 · SIP-05 (şube uyumu) · Customer **VKN unique YOK** (bilinçli P5) | unique `code` (`MUS`), `@@unique([nameFold])` partial ✓ (saha+dev) · `CustomerBranch @@unique([customerId, code?])` (code NULL'lanabilir) · alias `(customerId,itemId)`/`(customerId,colorId)` · GENERATED fold ×5 · FK `mergedIntoId` self SN\* (H1) · içe R\*: orders, shipments, direct_shipments (açık R), roll_returns; **C**: branches, aliases, template_routes, standalone_labels; SN\*: sacks.customerId, routes.customerId, rolls.labelCustomerId (permanent guard `labelledRolls` saymıyor — **reddedildi**, ayna kolon) | BaseService `create` :1001 wBR autoCode (closure'da tx-dışı `assertTaxNumberAvailable` + `super.create` — K6 §2.4) · `hardDelete` :1259 P2003→409 + guard sayımları havuz (branch/sack/route sayar) · `update` `:1101`; `reactivate` nested düşürme ulaşılamaz (F-CORE-VER-007 red) · alias upsert havuz (idempotent) | `test_customer` · `test_customer_branch` · `test_customer_card_fields` · `test_customer_sack_delete_guard` · `test_color_assignment` · `test_order_alias_promote` · `test_master_data_name_dup` · `test_hard_delete_guard_coverage` (allowlist :65-70) · `test_import_permissions` | K2a H1 (survivor silinince tombstone dirilir) · K2a H15 (bilinçli boşluk, yeniden açılmamalı) · K4 H13 (`label.service` alias/OrderLine yazımı sınır) |
| **Color** (`colors`) | **delegate yok** — BaseService generic (`color.routes.ts:14`; `ColorService.assertNameAvailable` renk katlaması) · `syncCustomerAssignments` · import `color.adapter` · `normalize-names` · merge MOVE **9 kural** · `StationColor` deprecated yazımı (`station-capability:369`) | MD-01 (renk `tr_fold_color(name)` ifade seddi; ad şemada kısıt YOK — bilinçli) · MD-05 (renk istasyon kısıtı DEĞİL; `StationColor` deprecated, okuyan kod yok) | unique `code` (`RNK`) · **expr+partial** `colors_nameFoldColor_key ON tr_fold_color(name) WHERE mergedIntoId IS NULL` — **dev'de YOK** (1 grup), sahada VAR · FK `mergedIntoId` SN\* · içe **SN\*** 10 tablo (rolls/order_lines/work_orders.targetColorId/…): **renk `/permanent` ucu YOK** — açılırsa 10 tablo sessizce renksiz (K2a §5) | BaseService create/update (DMMF, Zod yok) + `assertNameAvailable` check-then-act | `test_color_name_dup` · `test_fold_contract` §2b (JS≡SQL) · `test_db_invariants` §5 (**dev'de bilerek KIRMIZI**) · `test_station_capability` (8b renk kilidi) · `test_route_step_targets` | K2b H-1 · K4 H17 (`StationColor` ölü yazma yolu) · K2a §5 (`/permanent` açılırsa yüksek) |
| **Subcontractor / SubcontractorCategory / SubcontractorToCategory** | subcontractor-management.service (M1 `create` :528 tx: reaktivasyon claim `{id,isActive:false}` / create → `assertActiveCategories(tx)` → category createMany; M2 `update` :665 koşulsuz update + pivot replace; **havuz 4+1**) · fixture/seed upsert · merge MOVE 6 kural | MD-01/02/03 (**katlanmış kod tekilliği KİLİTSİZ** — `decideCodeUniqueness` :512 havuz; `code-unique.helper.ts:63-69` yarışı adıyla tarif ediyor) · VKN yok (bilinçli) | unique `code` (tam eşleşme ✓), `@@unique([nameFold])` partial ✓ (saha+dev) · `SubcontractorCategory.code` unique · pivot `@@id` · FK `mergedIntoId` SN\* (H1) · içe R\*: dispatches/receipts/kartela | ad/kod/VKN kontrolleri **pre-tx, kilitsiz** (K3a M1/M2, §6 #16) — `sefa`/`SEFA` yarışı açık | `test_subcontractor_management` (paralel ✗) · `test_master_data_name_dup` · `test_item_code_case_uniqueness` **yalnız Item'ı ölçüyor** | K3a #10 / K3b H-6 (8026 yalnız Item; `base.service.ts:1052` de kilitsiz) |
| **FabricProperty / FabricPropertyValue / StationProperty / StationColor** | FabricProperty **delegate yok** — BaseService generic (`nestedCreateFields: stationCapabilities, values`; `stationIds` ZORUNLU aynı insert); `fabric-property.service update` :378 **`super.update` AYRI yazım → sonra tx** (pivot/değer replace; bilinçli yarım durum, K3b T1) · `FabricPropertyValue` updateMany/upsert · `StationProperty` fabric-property + station-capability (replace `notIn`) · `StationColor` **deprecated ama yazılıyor** · seed · script `seed_fold_catalog_and_modes` | MD-06 (özellik boş doğamaz; `StationProperty` İKİ İŞ: planlama filtresi + `copyStationCapabilitiesToRoll`) · MD-05 · üretim karakteristiği (KAT kataloğu; kod ASCII) | unique `code` (`OZL`), `(propertyId, code)`, `(stationId, propertyId)`, `(stationId, colorId)` · `StationProperty.mode` enum (AUTO/OPTIONAL/REQUIRED) · FK cascade'ler · `FabricPropertyValue.property` **C**; `roll_properties.valueId` R (değer kataloğu korunur) | `setCapabilities` :361 tx replace (`colorIds` yoksa renk satırlarına DOKUNMAZ); `loadStationPropertyCaps(prisma)` pre-tx bayat (T9) | `test_property_station_binding` (negatif sonda) · `test_property_targetable` · `test_property_value_selection` · `test_fold_catalog` · `test_station_property_mode` · `test_station_capability` · `test_station_capability_flags` | K3b T1 (iki adım atomik değil — bilinçli) · K4 H17 · K1b H22 (PUT replace sözleşmesi) |
| **QualityGrade / ReturnReason / DefectType** | BaseService generic ×3 (QualityGrade `validateQgCreate/Update` Zod 2 alan + passthrough; DefectType `/permanent` GHR) · seed · migrations `20260820020000/030000` **iş kuralı verisi** (`code='FIRE'` → `targetStatus=SCRAP`, `skipLabel`) · `seed-return` updateMany | MD-04 (`Roll.qualityGrade` snapshot = `code`; final statü `targetStatus`tan; FIRE→SCRAP) · MD-07 benzeri katalog | unique `code` ×3 · `QualityGrade.targetStatus` enum ZORUNLU ∈ {WAREHOUSE, A1_STOCK, STOCK, SCRAP}; `returnTargetStatus?` · FK SN\*: rolls.qualityGradeId, roll_returns.reasonId, roll_errors.defectTypeId (GHR pencerede sessiz NULL — K3b T3) | DefectType `guarded-hard-remove`: guard sayımları **havuz**, silme tx (K3b L8/H-9) | `test_scrap_grade_label` · `test_iade_enhancements` · `test_null_quality_visibility` · `test_guarded_hard_remove` · `test_produced_buckets` | K2b H-8 (migration'a gömülü kod sabiti `'FIRE'`) · K7b H7 (karne "en üst kalite" `isActive` süzmez) · K7b §6.N (A1 `targetStatus=WAREHOUSE` → kova etiketi) |
| **Station / Machine** | BaseService generic ×2 (`station.routes.ts:15/34`; Station autoCode `IST`, Machine `MAK`) · `/permanent` GHR (`stationHardRemove` :106 → **`tx.workSession.deleteMany` + `machine.deleteMany` + delete**; `machineHardRemove` :256 → `peripheralDevice.updateMany machineId=null` + `workSession.deleteMany`) · migrations `20260702121000`, `20260810010330` UPDATE stations · seed | (istasyon türü ↔ izin `STATION_KIND_PERM`; `appliesColor/appliesProperty`) | unique `code` ×2 · FK `Machine.stationId` R\* · içe: `work_sessions` (userId/deviceId/stationId R\*, machineId **R** açık — "ayak izi korunur" niyeti) · `rolls.entryStationId/createdMachineId`, `roll_operations.machineId`, `devices.machineId`, `peripheral_devices.*` **SN\*** | GHR: guard `count`'lar havuz (:62-72), silme tx (:74) → SetNull FK'lı 5 referans pencerede sessiz NULL (K3b T3/L8) | `test_guarded_hard_remove` · `test_hard_delete_guard_coverage` · `test_station_capability_flags` · `test_work_session_stamping` | **K2a H2**: şema niyeti (`WorkSession` FK'ları "hiçbir taraf silinirken oturum geçmişi kaybolmaz") ↔ GHR oturum geçmişini fiziksel siliyor · K3b H-9 (Express handler helpers/ altında; guard havuzda) |
| **Route / RouteStep / RouteStepProperty / ProductRecipe / ProductRecipeProperty** | Route BaseService generic (`route.service.ts:26`, `nestedCreateFields: steps`; `plannedProperties.create` 2. seviye nested :393 — `RouteStepProperty` delegate ile HİÇ yazılmıyor) · **import `route.adapter.ts:296` `steps: { deleteMany: {}, create }` nested hard delete** · ProductRecipe BaseService (nested `properties`; `product-recipe.service:176 this.delegate.update`) · `/permanent` GHR (RouteStep/ProductRecipeProperty deleteMany) · seed nested | (rota şablonu HEDEF saklar: `RouteStep.plannedColorId` + `RouteStepProperty`; hedef ÖNERİ) · WO-12 (rota kapsaması UYARI) | unique `Route.code?` (`ROT`), `(routeId, sequence)`, `(routeStepId, propertyId)`, `ProductRecipe.code` (`REC`), `(recipeId, propertyId)` · FK `RouteStep.routeId` **C**, `stationId` R\*, `plannedColorId/plannedSubcontractorId` SN\* · içe SN\*: `work_orders.routeTemplateId` | BaseService update nested replace (RouteService `create/update` override :398/:403 — `assertRouteRefsActive`); GHR guard havuz | `test_phase7_route_validation` · `test_route_step_targets` (20) · `test_route_template_fason` · `test_route_firm_roundtrip` · `test_recipe` · `test_recipe_property_validation` · `test_guarded_hard_remove` | K4 §5c (import yolunda nested `deleteMany`) · K2a §4 #33-35 |
| **ReasonPreset** (`reason_presets`) | reason-preset.service (5: create/update/updateMany; **havuz 3**; `duplicate` :464 tx — `nextFreeCode` tx DIŞI; `reorder` :505 **batch tx** `update ×N`) · **job `reason-preset-catalog.job.ts:57` create havuz — audit YOK, servis atlanır** (K4 H7) · untracked script `_seed_custom_reason.ts` · senkron önbellek `cachedRows` (bayat-serve + arka plan tazeleme, 2026-08-26) | MD-07 (`(kind, code)` tekil; `code` rapor anahtarı ASLA değişmez; `legacyTexts[]`; son aktif satır gizlenemez; önbellek boş dönmez) · AUD-04 (`resolveReasonCode` sunucu türetir → 400 `REASON_CODE_INVALID`) | unique `(kind, code)` · `kind` enum 6 üye (**sahada `ORDER_CANCEL` YOK**, `WORK_ORDER_REWORK` defter dışı açılmış — K2b H-14) · `isActive` | `duplicate` check-then-act (DB unique P2002 → 409/500 haritası ② baksın — K3b T6); `reorder` batch: `kind` ile `ids` uyuşmazlığı? (K1b H24) | `test_reason_presets` §3b/c/d (bayat önbellek negatif sonda) · §5 · §6 | K4 H7 (job audit'siz) · K3b T6 · K1b H24 |
| **DuplicateReview** (`duplicate_reviews`) | duplicate-review.service (upsert/`delete` :151 **fiziksel** `reopen`; havuz 3) · `MasterDataMergeService.merge` sonrası `markMerged` **best-effort tx dışı** (K3b T11) | (mükerrer kuyruğu: karar ÇİFT bazlı; NOT_DUPLICATE kalıcı gizler; MERGED motor yazar) | unique `(entity, pairKey)` · `decision` enum · `evidence` Json snapshot | havuz upsert; MERGED → 409 | `test_duplicate_detection` · `test_merge_field_picks` | K6 H-8 (çakışma önizlemesi SQL hatasında `count=0` — "0 çakışma") |
| **Master-data birleştirme motoru** (`master-data-merge.service.ts merge`) | **17 `$queryRawUnsafe`/`$executeRawUnsafe`** (K5 §10: tanımlayıcılar `MERGE_MAP` sabit 44 kural, değerler `$n`) · 29 hedef tablo `UPDATE … SET col=$1 WHERE col=ANY($2)` · CONFLICT/UNION/MERGE_FIELDS `DELETE FROM` ×5 · `rolls/sacks.labelDirty` raw · entity çoklu claim `mergedIntoId` · script köprüsü `apply_merge_decisions.ts`/`fix_duplicate_master_data.ts` (motor üzerinden, dry-run) | MD-02 (tombstone, 42 kurallık harita; ham UPDATE haritayı ve audit'i atlar) · MD-01 (ad çakışması 8027 altında; sıradan `customer.update` 8027 almaz — K3b L7) | RESTRICT FK'lı hedefler; `MAX_MERGE_SOURCES=20`, `MAX_ROWS_TO_MOVE=200_000` | **8027 sabit anahtar İLK ifade** (:546) → taze findMany → guard'lar → conflict → raw UPDATE döngüsü → entity claim count===len → fieldPicks · **tek özel tx opsiyonu** `{timeout:120_000, maxWait:10_000}` (DB `statement_timeout=50s` ile ayrışıyor — K3b H-10) · `updatedAt` ham yollarda tazelenmez (K4 H5) | `test_master_data_merge` (3) · `test_master_data_merge_fk_coverage` (harita kapsama) · `test_merge_field_picks` · **paralel/advisory bekçisi YOK** (K3b H-7, K11 H-2) | K3b H-7/H-10 · K4 H14 · K1b H12 · K11 H-2 |

---

## 8. BELGE / ETİKET / REFAKAT KARTI

| Varlık | Yazma yolları (K4) | Değişmezler (K10) | DB kısıtı (K2a/K2b) | Tx & kilit (K3a/K3b) | Bekçi (K11) | ② işaret |
|---|---|---|---|---|---|---|
| **PrintedDocument** (`printed_documents`) | printed-document.service (6: `freezeForSource` :313 create · `voidForSource` :722 updateMany · `reissueForSourceTx` :747 · `reissue` :670 claim+create · **`getCurrent` :357 lazy-init `create` havuz — GET yazıyor**, audit `userId: undefined`, P2002 kazananı okur) · traveler-card.service (2: `archivePrintedVersionTx` **upsert** :443 + updateMany SUPERSEDED :468) · çağıranlar tx içi: shipping :1881/:2214, subcontractor :1283/:2037/:5357/:6305, return :564/:920/:927, kartela :310/:428 · migration `20260728171853` DELETE (0 satır) · script `smoke_fason_http` deleteMany | SM-10 (`ACTIVE→SUPERSEDED|VOIDED`; `version = max+1`; kaynakta tek ACTIVE; `TRAVELER_CARD` self-managed) · DOC-03 (donmuş belge = sevk/iade anı; brüt kuralı belge yolunda da — `collectShipmentDocContent` sevkten SONRA da koşar) · SEV-16 · DOC-04 | unique `(docType, sourceId, version)` · `sourceId` polimorfik **FK YOK** · `snapshot` Json immutable · `status` enum · `supersededAt/voidedAt` | `version` sayımı **kilitsiz `findFirst version desc`** :332 (K3b L4/L5; K2a H6) — yarış kaybedeni P2002; catch yalnız lazy-init dalında (:414-419), `reissue` dalında doğrulanmalı · `reissue` claim `{id, ACTIVE}` ✓ ama `fresh(prisma)`/`buildSnapshotEnvelope(prisma)` **havuz** (T5) · builder'lar `db` parametreli (tx sızıntısı 0 doğrulandı) | `test_printed_documents` (14 del) · `test_printed_doc_builders` (sorgu şekli) · `test_dispatch_report_gross` · `test_traveler_card_versions` · `test_workorder_documents` (liste izni ↔ baskı izni hizası) · Q-DOC-03/04/05/06 saha 0 | K1b H3 (GET lazy-init yazımı) · K2a H6/H7 · K2b §4 A-22 (saha 2 `TRAVELER_CARD` `version=2` tek satır, v1 yok) · K1b H14 (`sample-html` sabit `admin:settings` vs kart ucu `DOCUMENT_DESIGN_READ`) |
| **TravelerCard / TravelerCardScan** (`traveler_cards`, `_scans`) | traveler-card.service (4: `createForWorkOrder(tx)` create :190 [çağıranlar workorder :1081, clone :187] · `print` :213 kendi tx · `reprint` :268 update `version: existing.version + 1` **koşulsuz** (uç çağrılmıyor) · `recordPrintEvent` :368 tx · **`voidCard` :499 havuz claim** · `scan` :608 havuz) · `traveler-card-dirty.helper` (`markTravelerCardDirtyTx` 20+ tx çağrı; **havuz** çağrı `workorder-link:577/:640` claim'den AYRI) · `traveler-card-fanout.helper setWorkOrderCardStatuses` (6 servis) · `TravelerCardScan` subcontractor (2 create), traveler-card (1 havuz) · migration `20260714120000` DELETE/INSERT · seed/demo | SM-09 (`ACTIVE→COMPLETED|VOIDED`, `COMPLETED→ACTIVE`; bir WO = TEK kart) · WO-05/06 (`contentDirty` tek yazma noktası, "fazla işaretlemek güvenli") · DOC-04 (`resolvePrintPlan` TEK karar noktası; `planKey` sıra-bağımsız; ACTIVE olmayan kart revize edilmez) · PLAN CANLI / SUNUM DONMUŞ (2026-08-05/06) | unique `workOrderId`, `cardNumber`, `barcode` (üçü aynı değer = `workOrderNumber`; eski partial'lar düşürüldü → tam unique) · `snapshot` Json, `version` **tekillik YOK** · `status` enum (`REPRINTED` saha 0) · FK `workOrderId` R\* · Scan FK `cardId` **C**, `stationId` R\*, `workOrderStepId` SN\* · scan `updatedAt` YOK | `print` :213: findUnique→create TOCTOU, DB unique + **tx dışı BİR retry** → `{created:false}`; ikinci yarış → ham P2002 dal 6a teknik mesaj (K6 H-21) · `recordPrintEvent`: `card` + `resolvePrintPlan` **havuz** (bilinçli sınır :340-347) → tx: update `version: plan.version` koşulsuz + `printed_documents` upsert · `reprint` read+1 claim'siz (L4) · scan dedup **10 sn penceresi** (`clientScanId` DB çapası YOK — DB-MIMARI D-1 kısmi) | `test_traveler_print_active_card` (EŞZ ✓ 2× print → aynı kart, tek ACTIVE) · `test_phase2_broad_hardening` (EŞZ ✓ voidCard 2→1) · `test_traveler_card_stale` §6 (26; 4 negatif sonda) · `test_traveler_card_versions` · `test_traveler_card_a5_batches` · `test_traveler_card_fields` · `test_traveler_scan_dedup` (SIRALI) · `test_order_cancel_card_dirty` · `test_consistency` §16 · Q-WO-05/06/13 saha 0 · **`traveler-card-fanout.helper` hiçbir test adını anmıyor** (K11 H-12) | K2a H7 (`version` read+1, iki eşzamanlı print-event) · K1b H21 · K3b L4/L5/T12 · K6 H-21 · K9 A7 (kart yolu tx, top yolu tx'siz — aynı sınıf iki sözleşme) · K12 §7.2 D-1 |
| **TravelerCardTemplate** (`traveler_card_templates`) | traveler-template.service (4: updateMany/update; havuz 2; `setDefault` :193 tx findFirst→updateMany→update, sıra load-bearing; `clearDefault` :221; `remove` :245 soft) · config-bundle import → servis | (ÜÇ KADEME BUILTIN/SECTIONS/RAW_HTML; FAIL-CLOSED 404 seçili şablon pasifse; `sanitizeTemplateHtml` kayıt+render iki katman) | `@@unique([name])` · `@@unique([isDefault])` → DB **partial `WHERE isDefault=true`** (düz olsaydı 2 şablonla sınırlı) · `deletedAt` · `config` Json · saha **0 satır** | `setDefault` yarışı P2002 → `rethrowNameConflict` **yok** → generic (error middleware `_key` regex `isDefault_key`'i tanıyor mu [VARSAYIM 409] — K3b) | `test_traveler_template` (29; 5 negatif sonda) · `test_db_invariants` (partial envanteri) | K3b §1.10 (P2002 HTTP kodu doğrulanmadı) |
| **LabelTemplate / Variant / LabelContextDefault / CustomerTemplateRoute / PeripheralTemplateRoute / CustomerStandaloneLabel** | label-template.service (12/4/6/1/1: `create` :315 updateMany `isDefault` + create + upsert context; `update` :428; `setDefault` :497; `hardDelete` :579 = **`deletedAt` tombstone** (peripheral/customer route deleteMany); `createFromEnvelope` :653 create ×N (variants **tavansız** Zod); `setPrimaryVariant` :904; `setContextDefault` :975/:996; `deleteVariant` :886 havuz **fiziksel**) · peripheral.service `setTemplateRoute` (havuz upsert/deleteMany; **Zod YOK**, uuid✗) · customer-template-route (havuz deleteMany/upsert) · customer-standalone-label (tx replace-set ≤200) · config-bundle import · seed (nested variants) · migrations `20260820040000/050000` **`jsonb_set` şablon içeriği** (FIRE showIf) · script `migrate_label_templates_to_canvas` | DOC-05 (SACK etiketi barkodu = `sackNo`; şablon çözülemezse **FAIL-CLOSED 400**; koşullu eleman `showIf` = `QualityGrade.code`; `prepareElements` tek uygulama noktası) · **kind başına tek varsayılan İKİ bağımsız sed** (`label_templates_one_default_per_kind` + `label_context_defaults.kind`) çift-yazımla senkron — ikisi farklı şablonu gösterebilir (K2b §4 A-20) · YT-01 (`label-template:read/write` çiftinde READ⊇WRITE YAPILMADI — bilinen tuzak) | `@@unique([name])` · DB **partial** `label_templates_one_default_per_kind (kind) WHERE isDefault=true` (şemada yok; `kind` deprecated, null doğar) · `label_template_variants_one_primary` partial · `(templateId, widthMm, heightMm)` · `LabelContextDefault.kind` unique · `(customerId, kind)` · `(peripheralId, kind)` · `(customerId, templateId)` · FK `templateId` **R** (açık, 3 yönlendirme tablosunda) · `deletedAt` · saha: 4 `isDefault=true` + 3 `LabelContextDefault` (iki kaynak) | `hardDelete`/`deactivate` "default mı" kontrolü **havuz**, tx içi ikizi YOK ↔ `setContextDefault` tx dışı okuma → çapraz yarış → bağlam varsayılanı silinmiş/pasif şablona işaret → sonraki baskı FAIL-CLOSED 404 (K3b L1/H-8, T4) · `assertNameAvailable` tx dışı (nameFold app-level) · `rethrowDefaultConflict` partial unique P2002 → 409 | `test_label_context_default` · `test_label_defs_lifecycle` (deletedAt kalıbı) · `test_label_template_io` (round-trip) · `test_customer_template_route` · `test_customer_standalone_label` · `test_label_context_fit` (fail-closed) · `test_sack_label` · `test_label_element_condition` · `test_label_variant_selection` · `test_peripheral_registry_crud` · `test_label_bulk_seed` | K3b H-8 · K2b H-6 (iki sed bağımsız) · K3b sınır ötesi (`variants` tavansız) · K1a §5.4 (`/:id` 9 literal yola bağımlı, bekçi yok) · K1b H17 (peripheral inline yazma uçları Zod'suz) |
| **DocumentProfile / FreeDocument** (`document_profiles`, `free_documents`) | document-profile.service (2 create/update **havuz**) · free-document.service (2 havuz; `create` wBR autoCode `documentNo`) · config-bundle import → servis · `customers/subcontractors.documentProfileId` SN\* | (belge kişiselleştirme; DÖRT KAPI: SystemSetting tipi + sanitize + controller Zod + Electron aynası — CLAUDE.md 2026-08-05) | unique `name` / `documentNo` · `config` Json tam-yaz · `isActive` | havuz create/update; P2002 → 409 çevirisi servis içinde (K6 §2.3) | `test_document_customization` (Faz 2-4) · `test_new_documents` · `test_document_style` · `test_doc_sample_html` · `test_document_template_permission` (81; üç negatif sonda) · `test_doc_pagesize_override` | K1a H4 (`GET /document-profiles` `VT`-only — kardeşler `DOCUMENT_DESIGN_READ` ister; asimetri) |
| **Etiket baskı izi** (`Roll.lastLabelSnapshot/labelCustomerId/labelDirty/labelPrintedAt`, `Sack.labelDirty`, audit `LABEL_PRINT_EVENT`) | label.service `recordPrintEvent` :2075-2160 — **ÜÇ AYRI YAZIM, tx YOK**: ① `seedRollLabelSnapshot` `roll.update` :1989 (hata → `{success:true, seeded:false}` 200 + console.error — K6 H-5) ② `roll.update(labelDirty:false, labelPrintedAt)` :2112 ③ audit :2145; çuval `recordSackPrintEvent` :1737 `updateMany WHERE labelDirty:true` (idempotent); `labelDirty` **11 yazım noktası** (shipping :458/:468/:1069/:1723 **havuz**, kartela :1227, inventory :3913, manual-move :695, subcontractor :1152/:1189, merge raw, `sack-content-mismatch.helper`); `printRollNative` RAW TCP (bayrak `label.nativeSendEnabled` sahada **false**; sonuç `delivered:false` **HTTP 200** içinde) · `test-native` gövdeden `printerIp:port` (SSRF sınıfı, K1a H6) | DOC-05 (`labelDirty` = "basılı etiket veriyle uyuşuyor mu" tek cevabı; `recordPrintEvent` temizler) · AUD-01 (`LABEL_PRINT_EVENT` dil izi: saha 2.517 `RASTER_HTML` / 37 `PPLB` — yanlış dil izi 2026-08-19 öncesi) · ölü etiket onayı **KALDIRILDI** (2026-08-25; `confirmLabelPrinted` kabul edilir ama kapı açmaz) | `Roll.labelCustomerId` **ayna kolon** (kaynak `lastLabelSnapshot` JSON; SN\*, guard saymıyor — reddedildi) · partial index `labelCustomerId IS NOT NULL` · `labelDirty` boolean | tx yok (top yolu) ↔ kart yolu tx (A7) — aynı sınıf iki sözleşme (K9 H-7); `labelDirty` çift-mod yazım listesi (K9 A8) ② eşzamanlılık | `test_label_dirty_sources` (K3 kartelalık + D2 çuval) · `test_label_snapshot_audit_split` · `test_label_bulk_seed` · `test_label_preview_single_copy` · `test_printer_transport` (sahte TCP, `delivered`) · `test_consistency_derived` §25 (audit'ten, 6 ay sınırı) · `test_stock_label_no_customer_inference` | K9 H-7 (kısmi durum: snapshot güncel, `labelPrintedAt` boş, kâğıt çıkmış) · K6 H-5/H-7 (barkod/QR üretimi patlarsa eleman çizilmeden 200) · K9 H-8 (CPU-bound render event loop'ta; 2000×5 toplu) · K9 H-10 (`RENDERERS` `Partial<Record>` fail-open) · K1a H6 |

---

## 9. YETKİ / KİMLİK / CİHAZ / OTURUM

| Varlık | Yazma yolları (K4) | Değişmezler (K10) | DB kısıtı (K2a/K2b) | Tx & kilit (K3a/K3b) | Bekçi (K11) | ② işaret |
|---|---|---|---|---|---|---|
| **User** (`users`) | auth.service (4 update **havuz**: `setQuickPin` :145+, `rotateCardToken` — **`tokenVersion` bump YOK**, bilinçli) · permission-management.service (10; havuz 5: `createUser` :484 nested permissions + PIN/kart üretimi `.catch(() => undefined)` **sessiz** — K6 H-12; `resetUserPassword` bcrypt tx dışı + tokenVersion++; `deactivateUser` :686 / `deleteUser` :747 tx + 8025; `updateUser` :379-389) · fixture `fixture-test-user` · seed | YT-03 (**son admin koruması**: `admin:users`/`admin:*` etkin pencerede ≥1) · YT-04 (`tokenVersion` DB-backed; JWT izinleri bayat olabilir) · KIM: PIN/kart **düz** saklanır, `GET /users/:id/credentials` geri okur (K5 H13) | unique `username` + **expr `users_username_lower_uq`** · `cardToken?` · `quickPin?` (6 hane, sistem genelinde unique; saha 8/9 dolu) · `deletedAt` + `isActive` · `email` kolonu YOK · içe **R** açık: sessions, work_sessions, bypass (3 FK), roll_returns.receivedById; ~90 "kim yaptı" FK SN\* · `system_logs.userId` SN\* (K-23 "audit'li kullanıcı silinemez" — K2b §3 yorumu; ② FK tipini doğrulasın) | **8025** sabit anahtar `acquireAdminGuardLock` :609 → `assertAdminCoverageAfterChange` — `setUserPermissions` (yalnız `removesAdmin` ise :306), `revoke` (koşullu), `deactivate` :687, `delete` :748 ✓ · **`grantPermission` yolunda son-admin guard'ı HİÇ yok**; `validUntil` geçmişe çekilince `effectiveAdminWindow` dışına düşer, `removesAdmin=false` → guard koşmaz (K3b H-3/L3: **mantık boşluğu**) · `deleteUser` `username → del_<hex>`, PIN/kart temizlenir; `revokeAllForUser` tx sonrası best-effort (tokenVersion ikinci katman) | `test_admin_guard_race` (EŞZ ✓ 2× deactivate → 1 ok + 409) · `test_user_lifecycle` · `test_user_default_perms` · `test_p2_auth` (F253/F254) · `test_permission_management` · `test_card_login` · `test_login_methods` · `test_login_access` | K3b H-3 (validUntil ile son admin sönmesi) · K6 H-12 (201 ama PIN/kart üretilmemiş) · K5 H13 (düz sır) · K5 H5 (PIN-only giriş 10⁶ uzay + IP-anahtarlı bellek lockout) |
| **UserPermission / Permission / PermissionTemplate / PermissionTemplateItem** | permission-management.service (UserPermission 6: `grantPermission` upsert :195; `setUserPermissions` :304 deleteMany+createMany (**`skipDuplicates` YOK**)+updateMany; `revokePermission` deleteMany :393; `applyTemplate` merge **batch tx** `$transaction([...])` :1011 createMany + tokenVersion++, replace → `setUserPermissions`; PermissionTemplate create/update/`delete` :951 (yalnız fabrika şablonu; sistem rolü pasifleştirilir); Item deleteMany/createMany) · **job'lar havuz**: `permission-catalog.job.ts:94` Permission `createMany skipDuplicates` (audit ✓), `role-template-catalog.job.ts:118/147/172` (audit ✓) · scripts `sync-kursun-bypass-permissions`, `sync-quick-wo-permission` (atama, tarihsel) · migration `20260801020000` INSERT katalog · seed | MD-08 (katalog ⊆ DB; boot EKLER, silmez, **ATAMAZ** — "katalog koda, atama panele") · YT-01 (READ⊇WRITE) · YT-02 (SoD tasarımı: `shipping:invoice` Muhasebe; `undo-dispatch`+`manual-adjust` Süpervizör — **sahada 6/8 kullanıcıda**, tasarım ≠ atama) · YT-04 | unique `Permission.code` (70 = katalog 70, `*` yok), `(userId, permissionId)` (pencereli grant aynı çift için ikinci satır açamaz), `PermissionTemplate.code?`, `name` + expr `permission_templates_name_lower_uq`, `@@id(templateId, permissionId)` · FK `UserPermission.user/permission` **C**; `grantedById` SN\* · `validFrom/validUntil` (saha süreli 0) | `setUserPermissions`: diff (`toAdd/toRemove/removesAdmin`) **tx DIŞI**, `createMany` `skipDuplicates`siz → eşzamanlı grant → P2002 → tüm tx rollback (K3b L2, 500/409 haritası ② baksın) · `applyTemplate` merge batch tx (aynı P2002 riski) · `grantPermission` upsert idempotent, guard yok (H-3) · boot zinciri `started` bayrağı + `skipDuplicates`; **SIRA load-bearing** (rol izne FK) — tükenirse Admin dışı 403 (K8 §1) | `test_permission_catalog` (627; AST; tek-kolon kuralı) · `test_role_template_catalog` (216; kapsam iki yönlü) · `test_permission_management` · `test_timed_permissions` · `test_p2_auth` · `test_import_permissions` · `test_document_template_permission` · `test_mobile_screen_permissions` · **`grant/revoke/set/applyTemplate` paralel bekçisiz** (K11 §4b) | K3b H-3 · K3b L2 · K4 H18 (createUser çok adımlı tx dışı) · K5 H7 (SoD sahada uygulanmamış; `INVOICE_WRITE` `shipping:write`'ı da kabul eder) · K5 H14 (tablet şablonları `web` kategorili `label:edit` taşır → masaüstü kapısından geçer) · K12 H13 (`applyTemplate replace → setUserPermissions` devir satırı grep'te eşleşmedi) |
| **Session** (`sessions`) | session-registry.service (5: `openLoginSession` :72 tx create + kick updateMany; `revokeSession/revokeAllForUser` updateMany havuz idempotent; `purgeDeadSessions` :170 **fiziksel deleteMany** havuz — admin ucu) · `auth.middleware.ts:35` `touchSessionLastSeen` `void updateMany.catch` (60 sn throttle) · migration `20260708140000` UPDATE | YT-05 (`(userId, deviceType)` politikası `off/notify/kick` — **prod `off`**; 121 aktif oturum, bir kullanıcıda 40 MOBILE) · KIM: `verifyToken` her istekte `User.tokenVersion/isActive` + `Session(jti).revokedAt` DB'den; **`expiresAt` middleware'de OKUNMAZ** (JWT `exp`; prod 30 gün) | unique `jti` · `deviceType` enum · `revokedAt` · FK `userId` **R** (açık) · `deviceId` **FK DEĞİL** serbest string (bilinçli) · saha: 10 satır `expiresAt < now() ∧ revokedAt NULL` (K2a sınır ötesi G) | **8024** `hashtext(userId|deviceType)` **İLK ifade** :79 → findFirst :84 → kick updateMany :110 → create :115 (sıra ✓) — **bekçisiz** (K3b H-7) · logout `revokeSession(jti).catch(() => undefined)` → registry satırı canlı kalabilir, bu yolda tokenVersion bump YOK (K6 §3.B) | `test_session_registry` (politika, middleware iptali; **paralel/advisory 0**) · `test_session_duration_minutes` · `test_session_purge` · `test_login_access` (token exp) | K3b H-7 / K11 H-2 · K5 H8 (30 gün token, sınırsız paralel oturum, HTTP düz metin — kabul edilen risk mi?) · K5 §2.4 (purge yalnız elle) |
| **Device / DevicePeripheral / PeripheralDevice** (`devices`, `device_peripherals`, `peripheral_devices`) | device.service (8 **havuz**: `announce` :157 upsert — **kimliksiz yazma**, PENDING tavanı 200 → 429; `approveAndAssign`; `assignHardware` :263 **batch tx** deleteMany+createMany skipDuplicates; `revoke/rename/deactivate/reactivate`; `hardDelete` :342 pivot guard'lı **fiziksel**) · peripheral.service (2 havuz update `setTemplateRoute`/`setFieldAddress` :362-411 BT-only + oturum kapsamı; `hardDelete` :204 = `deletedAt` **tombstone**) + BaseService generic · `guarded-hard-remove` `peripheralDevice.updateMany machineId=null` · migrations ×5 UPDATE/INSERT · seed | KIM: cihaz kimliği **sırsız bearer** (`Math.random` UUID, `x-device-id` düz başlık; başka cihazın id'sini gönderen onun `req.device`'ını alır — K5 H4) · `device.pairingRequired=false` **prod** → hiçbir istek bloklanmaz; başlık göndermeyen istemciye kapı HİÇ uygulanmaz · DON: kantar/metre simülasyon bayrağı `PeripheralDevice.simulate` (saha 0), `weighSack` üç sinyal | unique `deviceId`, `PeripheralDevice.code`, `(deviceId, peripheralId)`, `(peripheralId, kind)` · `Device.status` enum (saha 28 APPROVED, 0 PENDING, **`machineId` dolu 0**) · FK `machineId` SN\*; PeripheralDevice `machineId/deviceId/stationId` **SN** (açık) · `deletedAt` (peripheral) · DevicePeripheral `updatedAt` YOK | havuz yazımlar; `assignHardware` batch tx (`found` pre-tx) · `announce` idempotent upsert (`deviceId` unique) · `resolveDevice` middleware DB hatasında **fail-open** (pairing kapalı) / 503 (açık) — bayrak da okunamazsa fail-open (K5 H12, K6 H-10) | `test_device_assignment` · `test_audit_followups` (EŞZ ✓ 2× announce → ≥1 ok, tek satır) · `test_device_activity` · `test_device_transport` · `test_peripheral_for_device` · `test_peripheral_registry_crud` · `test_peripheral_station_owner` · `test_field_address` · `test_route_auth_coverage` (public uçlar EXEMPT) | K5 H4 (sırsız cihaz kimliği → başka cihazın oturumunu kapatma, yazıcı hedefi) · K5 H15 (statik makine atfı hiç kurulmamış; enforce'suz 9 uçta `machineId=null`) · K1a H10 (announce/mobile-users/health rate-limit yok) · K1b H17 (peripheral inline yazma Zod'suz, `/:id/test` dış TCP) |
| **WorkSession** (`work_sessions`) | work-session.service (7: `open` :273 tx updateMany ×3 (NEW_LOGIN/TAKEOVER/NEW_LOGIN) + create; `closeForDevice` :349 **cihaz bazlı** updateMany (kullanıcı eşleşmesi yok); `forceClose`; havuz 3) · `work-session.helper` (3 havuz: `resolveActiveSession`/`getStampContext` öz-onarım `NEW_LOGIN`/IDLE `sweepIdleSessions` — **global `prisma`**, tx dışı sözleşmesi) · **`guarded-hard-remove` `tx.workSession.deleteMany`** (istasyon/makine permanent) · `work-session.controller GET /current` **öz-onarım yazımı** (K1b H4) · migration `20260702121000` | YT-05 (makine/cihaz başına TEK aktif) · KIM `STATION_KIND_PERM` (**eksik kind'da fail-OPEN** — K5 H3) · izlenebilirlik: `getStampContext` `machineId/entryStationId/createdMachineId` damgası (9 uçta enforce'suz → null) | **partial unique** `work_sessions_active_machine_uq (machineId) WHERE endedAt IS NULL AND machineId IS NOT NULL`, `work_sessions_active_device_uq (deviceId) WHERE endedAt IS NULL` (şemada YOK) · FK `userId/deviceId/stationId` R\*, `machineId` **R** (açık) — "ayak izi korunur" niyeti ↔ GHR siler (K2a H2) · `endReason` enum · 5 index | `open`: pre-check occupant tx dışı → tx koşullu kapanışlar (idempotent) → create → **P2002 catch tx dışı → 409 `SESSION_RACE`**; `confirmTakeover` → `MACHINE_OCCUPIED` 409 · `takenOver` :262 tx dışı yalnız audit (T14) | `test_work_session` (EŞZ ✓ 2× open aynı makine → tam 1; LOGOUT idempotent) · `test_work_session_close_all` (öz-onarım) · `test_work_session_stamping` · `test_device_activity` · `test_kursun_regime_lock` | K2a H2 (oturum geçmişi fiziksel siliniyor) · K5 H3 (fail-open harita) · K5 §7.1 (`closeForDevice` başkasının oturumunu kapatır) · K1b H4 (GET yan etkili; iki tablet aynı cihaz kimliğiyle yarış) · K1b H23 (partial unique claim — doğru desen adayı) |
| **UserPreference** (`user_preferences`) | user-preference.service (upsert/update **havuz**; P2002∨P2003 → düz update) · **audit MUAF** (tek istisna) · sahiplik `userId = req.user.userId` (gövdede id yok) | SYS-05 istisnası (audit muaf) | unique `userId` · FK **C** · `preferences` Json tam-yaz (≤64 KB Zod) | tam-yaz, kullanıcı-başına tek yazar | **fiilen bekçisiz** (yalnız `test_record_provenance` muafiyet olarak anıyor — K11 §4a) | K11 H-12 |

---

## 10. SİSTEM / OPS

| Varlık | Yazma yolları (K4) | Değişmezler (K10) | DB kısıtı (K2a/K2b) | Tx & kilit (K3a/K3b) | Bekçi (K11) | ② işaret |
|---|---|---|---|---|---|---|
| **SystemSetting** (`system_settings`) | system-setting.service `set()` :1032-1050 (findUnique yalnız audit oldData → **upsert**; `setFeatureFlags` DÖRT KAPI: strictObject + sanitize; `setDocumentsLogo` süreç-içi kuyruk A8) · **`PUT /api/admin/settings/:key` → `set()` — `:key` allowlist YOK** (yalnız 3 STRUCTURED anahtar reddedilir; K1a H3) · db-copy.service `writeCopyRecords` :288 **JSON RMW havuz** (`dbRestore.copies`) · **job upsert'leri havuz, `set()` sanitize DIŞINDAN**: `archive-scheduler:42`, `backup-scheduler:53`, `installation-identity:141` (K4 H7) · migrations ×3 INSERT/DELETE · seed · 38 test yazıyor (paylaşımlı flag — K11 H-13) | (feature-flag üç-yer sözleşmesi: route şeması ↔ servis ↔ Electron paneli; `PATCH /feature-flags` anahtar-kapsamlı fail-closed `flagWriteGuard`; `documentsConfig/travelerCardConfig` dar izinle) · `getFeatureFlags` **30 sn TTL process-local cache** + `cacheGeneration` lost-invalidation guard · YT-01 | PK `key` (doğal) · `value` Json · FK `updatedById` SN\* · saha 34 anahtar (`kk1.duplicateGuardEnabled=true`; `tambur.overQuantityEnabled`/`shipping.toleranceMeters`/`fason.shrink*`/`auth.pinLockout*` satırı YOK → kod varsayılanları) | anahtar başına tam-yaz (istemci TAM nesne gönderir → iki admin farklı alt alan: **istemci tarafı son-yazan-kazanır**, K2a H11) · db-copy `COPIES_SETTING_KEY` RMW tek yazar (kuyruklu) | `test_feature_flag_contract` (üç yer) · `test_document_template_permission` (flagWriteGuard 3 negatif sonda) · `test_observability_cache` · `test_kk1_weight_flag` · `test_session_duration_minutes` · `test_workstation_permission` | K1a H3 (`PUT /settings/:key` strictObject sınırlarını atlar) · K4 H7 · K2a H11 · K11 H-13 (38 test flag yazıyor; bellek önbelleği ile çakışma) · K9 H-5 (offsite yerel dizin: panel SystemSetting yazar, iş yalnız env okur — **iki gerçek**) |
| **SystemLog / SystemLogArchive** (`system_logs`, `system_log_archives`) | audit.service (`log/logMany/logEvent` create/createMany **havuz, tx DIŞI, commit SONRASI, best-effort** — catch → `recordAuditFailure` + `/api/admin/health.auditWriteFailures`; `archiveOlderThan` :220 tx: **`SET LOCAL teks.audit_purge='on'` İLK** → archive createMany skipDuplicates (≤5000) → `deleteMany`) · route katmanından `await logEvent` ×8 (admin ×7, db-copy ×1) · `job-failure.ts` `JOB_FAILED:<job>` · scripts: **`reset-operational.ts:20` TRUNCATE 30+ tablo (kapı yok)**, `bench_audit_summary.ts:141` **sentetik INSERT** (trigger INSERT'i engellemez), demo/fixture `deleteMany` ×5, `seed-load-scale` createMany · `archive-scheduler` 24 sa / 30 gün eşik, 200 batch/tur | AUD-01 (**best-effort, BİLİNÇLİ**; kayıt düzeyinde kapsama boşluğu: 55/2.391 top audit'siz) · AUD-02 (**değiştirilemezlik yalnız `teks.audit_guard='on'` iken** — saha kopyasında **KAPALI ölçüldü**, SURUM-2.9.0 §7b ☐) · AUD-03 (6 ay arşiv; künye KOLONDA yaşar) · AUD-04 (iptal izi kolonda; **47/52 eksik**) | **trigger** `system_logs_block_tamper` / `system_log_archives_block_tamper` (BEFORE UPDATE/DELETE/TRUNCATE FOR EACH STATEMENT; GUC kapılı; `audit_purge` istisnası; **INSERT serbest**) · index'ler `[tableName, recordId]`, `[createdAt]`, `[category, createdAt]`, `[userId, createdAt]`, `[requestId]` · `sl_day_exact` extended statistics (`factoryDaySql` metniyle birebir) · FK `userId` SN\* · arşiv FK YOK; `updatedAt` elle kopya · `system_logs.updatedAt` kolonu düştü (`20260819034413`) | `SET LOCAL` sırası load-bearing (`deleteMany`'den önce; tx'e özel, havuza sızmaz) · havuz tükenince `POOL_TIMEOUT` audit'i **aynı havuzu** kullanır → olay anında audit de düşebilir (K6 H-18) · `unhandledRejection` süreci ayakta tutar + audit; `uncaughtException` 2 s audit + shutdown · `AppError.internal` 500 ve wBR tükenme 409 **iz bırakmaz** (K6 H-2); `PrismaClientValidationError` 400 audit'siz (H-3) | `test_audit_depth` §10 (SET LOCAL sırası + gerçek taşıma) · `test_audit_p0` · `test_audit_labels` (Türkçe sözlük) · `test_audit_followups` · `test_system_log` · `test_observability_contract` (Prisma kod kümeleri, `reportJobFailure`) · `test_observability_cache` · `test_record_provenance` · `test_db_invariants` §6 (trigger varlığı) · **process handler'ları / statü taşıyan http-errors bekçisiz** (K6 §7) | K8 H-3/H-4 (arşiv purge yolu canlıda hiç koşmadı; audit_guard sahada KAPALI) · K10 H-5/H-7 · K4 H8 (kapısız script'ler TRUNCATE/sentetik INSERT) · K6 H-1 (57014/40P01 generic 500, 503 sinyali yok) · K6 H-4 (statü taşıyan http-errors → 500 + sahte SYSTEM/ERROR audit) · K6 H-18 · K9 §2.i (job/boot bağlamında origin alanları null — bilinçli) |
| **EndpointLatencyDaily** (`endpoint_latency_daily`) | latency-persist.service (havuz 3: `flushLatencyNow` :113 `findUnique → update(increment, max, bucket)/create` **oku-topla-yaz** + retention `deleteMany(day < bugün−90)` günde 1 — **fiziksel silme, istisna listesinde YOK**) · istek-güdümlü 5 dk throttle `setImmediate`; timer YOK; SIGTERM'de flush 2 s tavan | (metrik toplayıcı — scheduler DEĞİL; idempotent, atlanması zararsız) · SYS-01 (tek process: `pending Map` + `inFlush` bayrağı) | unique `(day, routeKey)` (çakışan create P2002) · `day` **`@db.Date`** (O-11 tek istisna; `factoryDayKeyUtcMidnight`) · `buckets` Json | tek process + tek flush; dış try'da **catch YOK** → `unhandledRejection`'a yaslanır (K6 H-20) | `test_latency_persist` (301; flush/merge/retention/history; DB `day` = fabrika bugünü) · `test_latency_middleware` · `test_latency_stats` | K6 H-20 · K7b sınır ötesi (retention DELETE istisna belgelenmemiş) |
| **ImportRun** (`import_runs`) + 17 içe aktarım adaptörü | import.service `apply` :552 `importRun.create` **havuz, döngüden SONRA** (koşum satırı sonda) · satır satır `adapter.createOne/updateOne` → **mevcut servis** (kendi tx'i + audit'i; tek tx YOK — bilinçli `:12-24`) · `order.adapter` **create-only, doğal anahtarsız** · `route.adapter` nested `steps deleteMany` · `preview` yazmaz [K1a VARSAYIM] · config-bundle `applyBundle` satır satır servis (`onConflict rename|overwrite|skip`) | SYS-03 (`clientToken` yalnız **tamamlanmış** koşumu korur — uçuştaki tekrar ikinci koşumu başlatır, K9 H-2) · kısmi-başarı semantiği YAZILI (`PARTIAL/FAILED` + `stoppedAtRowNo`) · izin iki katman (`data:import` VE varlık write; export yalnız READ — D6) · ad-mükerrer ön kontrolü adaptör beyan etmezse **fail-open** | unique `clientToken` düz · `status` enum · `options/errorReport` Json · saha **0 satır** (özellik hiç kullanılmamış); dev 297 koşum (bekçi kalıntısı) | kilit/bayrak YOK; koşum satırı sonda → uçuşta çift koşum ikisi de yazar (ikincisi `importRun.create` P2002'ye çarpar ama yazımlar bitmiştir) · gövde tavanı **fiilen 1 MB** (route-level 10 MB parser global parser arkasında — ÖLÇÜLDÜ 413; K1a/K9 H-1) | `test_import_framework` (#10 aynı token ikinci kez yazmaz — SIRALI) · `test_import_permissions` (HTTP) · `test_import_fix_hints` · `test_import_cycles` · `test_config_bundle` · **gövde limiti / uçuşta çift koşum ölçülmüyor** | K9 H-1/H-2 · K1a H1/H13 · K12 H10 (`import-lookup` `mode:insensitive` İ/ı katlamaz) · K4 §5c (route.adapter nested deleteMany) |
| **Veritabanı DDL** (`CREATE/DROP/ALTER DATABASE` — db-copy) | db-copy.service `startCopyJob` :368 (bellek claim `currentJob` **senkron blokta**, `isBackupRunning` kontrolü, disk guard `free < 1.2×canlı`) → `CREATE DATABASE … TEMPLATE template0` :320 (`pg.Client` havuzsuz, `statement_timeout=0`) → `pg_restore --exit-on-error` (3 sa SIGKILL) → `ALTER DATABASE … SET` GUC replay :619 (hata yalnız console) → doğrulama · `dropCopy` :716 `DROP DATABASE … WITH (FORCE)` (allowlist `isRestoreCopyName` + `name===live` reddi + taze `pg_database`) · `getSwapCommands` :739 **yalnız METİN** (backend RENAME etmez; `audit/PLAN.md:1118` öncülü YANLIŞ — K12 H16) · `reverifyCopy` :798 **yol parametresi allowlist'siz** (K9 H-3) · `db-copy-verify` `$queryRawUnsafe` count (sabit tablo listesi), `readdirSync` istek yolunda | (geri yükleme kopyası → doğrulama → operatör takası; `_old_` DB'lerinin DELETE ucu YOK — geri dönüş noktası) | allowlist `<canlı>_restore_<damga>` (saniye çözünürlüklü) · `MAX_IDENTIFIER_BYTES=63` · `quoteIdent`/`quoteLiteral` · 5 uç **çift guard** `admin:settings`+`admin:users` (F-OPS-VER-008 GEÇERSİZ — hiç tek izinli değildi) | bellek claim (tek process); `reverifyCopy` sınırsız paralel, yalnız kayıt yazımı kuyruklu; meşgul → **400** (409 değil) | `test_db_copy` (413; **`TEST_DB_COPY=1` kapılı — varsayılan koşumda atlanır**) · `test_db_copy_single_start` (EŞZ ✓ sahte run/list, 60 ms gecikme) | K9 H-3/H-12 · K1a H12 · K12 H16 · K8 sınır ötesi (`statementTimeoutMs:0` restore) |
| **Yedek dosyaları / offsite / rclone.conf** (FS) | backup.service (`pg_dump -Fc` → `.dump.part` → `pg_restore --list` doğrula (30 s `Promise.race` **child'ı öldürmez** — K9 H-4) → `rename` atomik → rotasyon (30 g, `RETENTION_MIN_KEEP=3`, yalnız `NIGHTLY_PREFIX`) → `BACKUP_OFFSITE_DIR` `copyFile` (**yalnız env**; panel `SystemSetting backup.offsiteDir` yazar ama iş okumaz — K9 H-5)) · `offsite-backup.helper` `rclone copy --immutable --no-traverse` (uzağa silme yok; uzak ad **doğrulamasız pozisyonel argüman** — K9 H-6) · `writeRcloneDriveToken` `rclone.conf` `0o600` · `backup-scheduler` damga **BAŞTA** (idempotent değil → başarısız gece o gün tekrar denenmez; `BACKUP_FAILED` audit görünür — K8 H-1) · sahada `BACKUP_SCHEDULE_ENABLED=false` ama kopyada `nightly` 1 koşum (K9 H-9) | SYS-01 (tek process: `running`/`checking` bayrakları process-local; dev DB'de çift-dump yarışı ÖLÇÜLDÜ — K8 H-2) · `.part` her yüzeyde görünmez (bilinçli) | — (FS; `resolveBackupPath` `basename` + `.dump` + kök altı) | bellek `running` → ikinci 400; scheduler 3 sa watchdog; offsite-sweeper watchdog YOK (child 3 sa timeout tek koruma) | `test_backup` (605; EŞZ ✓ 2× runBackupJob → 1 red; rename/timeout/retention) · `test_offsite_sweep` (sahte rclone; copy≠sync) · `test_backup_impact`? (backup-impact 1 test) · **`test_single_process` YOK** (K8 §0) | K8 H-1/H-2/H-5/H-6 · K9 H-4/H-5/H-6/H-9 · K12 H6/H7 (`.env` git'te; offsite yapılandırması boş; PITR yok) |
| **Mobil OTA deposu** (FS, `MOBILE_UPDATE_DIR`) | mobile-update.service: **yazma YOK** (sunucu manifest üretmez; yayın script'i dondurur+imzalar) · `GET /api/mobile/updates/ota/:runtimeVersion/manifest` + `{*yol}` **PUBLIC** (`readFileSync` :56; path traversal guard resolve-sonrası prefix; dizin 404) | (LAN ikizi; asıl kanal VPS nginx; kod imzalama AÇIK) | — | — | `test_mobile_update` (37; tek baytlık bozulma sondası, §6 yol kaçışı) · `test_route_auth_coverage` (EXEMPT; **HEAD'de KIRMIZI** — `GET /api/client-policy/` beyansız, K1a §0.2) | K1b H13 · K9 H-11 · K1a H2 (bekçi kırmızı) |

---

## ÇAPRAZ OKUMA

### A. Değişmezi KODDA zorlanan ama DB'de kısıtı OLMAYAN varlıklar

(K10 §4 "HİÇBİR YERDE" + §5 "yalnız kodda" + K2b §4 A-listesi; DB'nin koruduğu 24 kural K2b §3'te — burada yalnız korumadıkları.)

| Varlık | Değişmez (ID) | Kodda nerede zorlanıyor | DB'de ne var / neden yok | Saha ölçümü (K10/K2b/K7a) |
|---|---|---|---|---|
| Roll | `currentQty ≤ initialQty` (STK-02, A-1) | `tambur.service:732-753` validasyon; undo bump `tambur-undo:1130-1141`; `applyManualProperties` rollWhole pre-tx | yalnız `≥ 0` CHECK; ilişki CHECK'i konmadı (aşım meşru + geri alma bump'ı) | **2 satır** (bilerek düzeltilmedi, §13) + **2 parent `initialQty=0`** çocuklu (H-3) |
| Roll | statü geçiş matrisi (SM-01, A-7) — 20 izinli kenar | **merkezi tablo YOK**; 13+ akış-başına liste (`CANCELABLE_ROLL_STATUSES`, `MOVABLE_STATUSES`, `K18_DEAD`, `SACK_ABSENT`, …); 57 yazma sitesi | enum yalnız küme; trigger kaynak/hedef listesini kendi içinde tekrarlar (yeni statüde fail-open) | `reopenStep` SCRAP→IN_PRODUCTION kenarı belgesiz (H-4) |
| Roll | `currentStepId ⇔ üretim statüsü` (STK-14, A-10) | finalize/dispozisyon/fason tüketim `currentStepId:null` | YOK; bekçi yalnız dolaylı | 0 |
| Roll | `SHIPPED ⇒ preShipStatus` (SEV-04) | `performDispatchTx:1858-1869` | CHECK konamaz (1 eski satır) | 1 |
| Roll | `CANCELLED ⇒ cancelledAt/By/Reason` (AUD-04) | `inventory.softDelete:3253`, `roll-disposition:223`; `hardDelete:3561` **YAZMIYOR** | kolon nullable | **47/52 eksik** (H-5) |
| Roll | çuvalda hayalet içerik yok (STK-15, A-9); bir top tek adımda açık (STK-11b) | `sack-invariants.helper`, dispatch tx-içi assertion; `openMovementForNextStep` | partial unique yalnız `(roll, step)` çifti; çuval↔statü seddi YOK | 0 |
| Roll | barkod biçimi `T+GGAAYY+{H|F}+NNNN` (A-13); satılabilir top barkodlu (STK-07) | `roll-barcode.helper`, `roll-finalize:138-199` | unique var, biçim YOK | 0 |
| Roll / KK1 | clientToken replay 4. durum "iptal edilmiş → 409" (STK-10) | Tambur-manual + fason kabulde var; **KK1'de YOK** | partial unique yalnız çakışmayı önler | 227 iptal/fire top token taşıyor (replay yüzeyi) |
| RollMovement | kapanışta `qtyOut = qtyIn` / storno 0 (STK-05, A-23) | 13 raw + 9 Prisma site, 3 fallback varyantı + NULL | yalnız `≥ 0` | **15/883** sapma; 14 `qtyOut NULL` |
| OrderLine | `Σ sevk(DISPATCHED) ≤ quantity` (SEV-08, A-2) | **HİÇBİR YERDE** — `COMPLETED` toleransla, UI kelepçeler | yalnız `shippedQty ≥ 0`, `quantity > 0` | 0 (bugün) |
| OrderLine / Order | `shippedQty` = defter Σ (SEV-01, A-3/A-4); `Order.status` türetimi (SM-08) | `recomputeOrderStatus` (tek yazıcı) — `reopen/update` **kilitsiz** | trigger YOK; enum yalnız küme | 0/281, 0/278 drift |
| SackAllocation | çift tahsis yok (SEV-02/08) | `allocate` yalnız DISPATCHED'i düşer | unique `(sackId, orderLineId)` çuval×satır; PLANNED çakışması YOK | 0 (sevk onayı kapalı rejim — pencere kısa) |
| Shipment / ShipmentOrder | tek aktif sevkiyat (SEV-07, A-17) — **bilinçli kaldırıldı**; `isActive` denorm (A-18); statü makinesi (A-31); EXPORT tartı / boş sevkiyat (SEV-12/13) | `shipping.service` claim'ler; pre-tx guard'lar | partial unique DROP (`20260711120000:87`); enum yalnız küme | 0 |
| WorkOrder | `status ← adım durumları` (WO-04, A-5); `type ↔ bağ` (WO-01, A-6); adım sırası boşluksuz (A-30); plan kilidi (WO-08) | `completeWorkOrderIfStepsDone`, `linkOrderLines/unlink`, `PLAN_CHANGE_FROZEN_STATUSES` — `updateTargetProperties` **guard'sız** | yalnız `stockprod_targetItem` CHECK (dolaylı); unique sıra çakışmayı önler, boşluğu değil | §21/§22 0 |
| WorkOrderStep | statü TÜRETİM (SM-03) | `recomputeStepStatus`; **fason 7 sitede doğrudan yazıyor** | YOK | §20 Tur 2; 3 CANCELLED WO'da PENDING adım (Temmuz) |
| Fason | `Σ receivedQty ≤ dispatchedQty` (FAS-03, A-14); kalem kapanış kuralı (FAS-02); LIFO (FAS-07); parti↔adım WO (PAR-03); iade ≤ sevk (A-15) | `subcontractor.service` claim'ler (`currentQty > receivedQty`); `fason-open-dispatch.helper` (AST bekçili, **ham SQL kopyası bekçisiz**); LIFO **pre-tx** | `receivedQty` nullable kısıtsız; `remainderClosedAt` | 0; 635/638 `receivedQty NULL` (legacy) |
| Batch | parti no benzersiz değil (PAR-01 **bilinçli**); sayaç veriden türer (PAR-02) | 8022 + regex | unique DROP | 92 grup (bilinçli); sıra kopuk 0 |
| Belge no | biçim + günlük sıra sürekliliği (DOC-01/02, A-12); PD `version` sürekliliği (A-22); TC yaşam döngüsü (A-21) | `nextDailySeq` max+1; `printed-document` max+1; `traveler-card` fan-out | yalnız tekillik | 0 boşluk; TC 2 kayıt `version=2` v1'siz |
| LabelTemplate | kind başına tek varsayılan (A-20) | çift-yazım `label-template.service:319-342/432-447/499-509` | **iki bağımsız sed** birbirine bağlı değil | 4 `isDefault` + 3 context (iki kaynak) |
| Item/Customer/Subcontractor/Color | katlanmış ad tekilliği (MD-01, K-12) | `assertNameNotDuplicate` check-then-act (havuz) | partial unique **YUMUŞAK KAPI** — ortama göre eksik (saha `items`, dev `colors`) | items 1 canlı mükerrer (`v-1430`) |
| Item/Subcontractor/Customer/… | katlanmış **kod** tekilliği (MD-03) | 8026 **yalnız Item**; diğer 3 çağıran kilitsiz | unique tam eşleşme (`sefa`≠`SEFA`) | — |
| Ana veri | pasif kayıt yeni kayıtta kullanılmaz (A-25) | servis `isActive` doğrulamaları | FK pasifliğe bakmaz | son 30 günde pasif kumaşa bağlı 2 top (saha) |
| Audit | her CUD → SystemLog (AUD-01, A-27) | `AuditService` best-effort tx dışı | yalnız yazılanın değiştirilemezliği (GUC kapılı — sahada KAPALI) | 55/2.391 top audit'siz |
| Sistem | tek process (SYS-01) | `ecosystem.config.js` belge; kod-içi zorlama YOK | — | dev DB'de çift-dump yarışı ölçüldü |
| Rota | kapsaması (WO-12) — **bilinçli UYARI**, reddetmez | `warnings.push` | — | — |

### B. Birden çok servisten DURUM yazılan modeller (K4 §2; ≥3 dosya, enum/durum alanı)

| Model | Yazan dosya | Durum alanı yazan siteler (özet) | Kanonik nokta var mı | ② sorusu |
|---|---|---|---|---|
| **Roll** | **21** (17 servis + 4 helper) | `status` **57 site**: inventory (create/softDelete CANCELLED\|SCRAP/restore **havuz**/hardDelete/createOpenFabric/prepareRawForSale **havuz**/rescue), kartela (AT_KARTELA/WAREHOUSE/KARTELA_CONSUMED), kursun-qc `reopenStep` (final→IN_PRODUCTION, **SCRAP dahil**), order `quickOrderFromRolls` (STOCK→WAREHOUSE), return (SHIPPED↔appliedStatus), shipping (→SHIPPED/preShipStatus; storno geri), subcontractor 11 site (AT_SUB/STOCK/CONSUMED/CANCELLED), tambur 11 site, tambur-undo 5, manual-move/split/batch-drop/workorder (`prepareFasonCancelDecision` **havuz koşulsuz**, attach `updateManyAndReturn`) | **YOK** — yalnız ortak helper'lar (`finalizeRollsAtLastStep`, `applyRollDispositionsTx`, `K18_DEAD_STATUSES`) + trigger damgası; **üç "önceki statü" kolonu üç serviste** (`preShipStatus` shipping · `preCancelStatus` inventory/disposition · `preTamburCloseStatus` tambur/undo) | Geri alma yollarının çapraz tutarlılığı (aynı top iki "önceki" taşıyabilir); `order.quickOrderFromRolls` / `return.createReturn` / `shipping.dispatch` yabancı servisler (§7.2 bounded context) |
| **RollMovement** | **12** (10 + 2) | enum yok; "durum" `exitedAt IS NULL` + `notes` marker (`WO_CANCELLED`, `QC2_STEP_FINISHED:<uuid>`, `KURSUN_BYPASS_FINISHED:UNASSIGNED:`, `RETURNED_VIA_RECEIPT:`, `DETACHED_FROM_WO`, `MANUAL_MOVE_OUT`, `REDYE_REWIND`, `BATCH_DROP_STOCK:`) | açma `openMovementForNextStep` ✓; kapanış **13 raw + 9 Prisma** (iki mekanizma) | OCP/tek kaynak; `finishStep :888` yazdığı marker'ı `reopen :1097` okuyor |
| **WorkOrder** | **11** (8 + 3) | `status` 15 site; IN_PROGRESS diriltmesi **7 sitede doğrudan** (kursun-qc :1144, tambur-manual :1194, tambur-undo :1378/:1669, manual-move :816, split :394, workorder :6100 havuz) kanonik `ensureWorkOrderInProgress` dururken; **order.service :3242** CANCELLED/COMPLETED/APPROVED + `:3210` type | `roll-step.helper` (`ensureWorkOrderInProgress` yalnız PLANNED'ı; `completeWorkOrderIfStepsDone` terminal guard'lı), `touchWorkOrderTx` | Her sitede terminal guard var mı (bekçi `test_wo_terminal_guard` çıplak COMPLETED 0 — IN_PROGRESS diriltmesini ölçüyor mu?); order.service iptal gövdesini tekrarlıyor mu |
| **WorkOrderStep** | **6** (5 + 1) | TÜRETİM `recomputeStepStatus` ↔ **subcontractor 7 sitede doğrudan** (ACTIVE/COMPLETED/PENDING/SKIPPED); manual-move SKIPPED/PENDING; workorder SKIPPED ×3 | `recomputeStepStatus` (12 dosyadan) | `:2118` "geçmişte kabul var → COMPLETED" recompute ile çelişebilir mi (K4 H1) |
| **RollOperation** | 7 | `operationType` append-only; **deleteMany ×7** | istasyon servisleri | silinen log = rapor sayımından düşer (yorumlu) |
| **RollError** | 6 | `actionTaken`, `isProcessed`; **havuz deleteMany** kursun-qc :720 | kursun-qc açar / tambur kapatır | O-18 (finalize açık hata sormuyor) |
| **RollProperty** | 6 | küme replace ×5 | `copyStationCapabilitiesToRoll`; `applyManualProperties` FLAG evreni | `tambur:1208` CHOICE dahil siler mi |
| **TravelerCard** | 3 (+ fan-out 6 servisten) | `:190` ACTIVE · `voidCard :499` **havuz** claim · `setWorkOrderCardStatuses` parametrik (6 servis) · `contentDirty` helper | `traveler-card.service` + fan-out helper | — |
| **WorkSession** | 3 | `endReason` — **hepsi havuz** (`updateMany endedAt:null` claim benzeri) | work-session.service | GHR siliyor |
| **Sack** | 3 | `weightSource` (shipping ×3), `labelDirty` (label havuz, shipping :1723 havuz, merge raw) | shipping | çift-mod `labelDirty` listesi (K9 A8) |
| **SubcontractorDispatch** | 4 | damgalar `cancelledAt/directShippedAt` (enum yok); `totalQty` 6 yazıcı | subcontractor + surgery helper | — |
| **OrderLine** | 3 | `shippedQty` (yalnız recompute), `cancelledAt`; **label.service :1391 havuz** | order + `touchOrderLinesTx` | sınır sızıntısı (K4 H13) |
| **RollVariance** | 3 | `kind` (tek yazıcı) + `reversedAt` terslemeleri (tambur-undo, subcontractor) | `roll-variance.helper` | ✓ |
| 2 dosyalı | Shipment, Order, PrintedDocument (printed-document + traveler-card), Swatch (kartela + shipping ×10), Batch (batch + split), DirectShipment, KursunBypassAssignment, SystemSetting (system-setting + db-copy), StationProperty, CustomerColorAlias, PeripheralDevice, TravelerCardScan | — | — | — |

### C. Yazma yolu VAR ama bekçisi OLMAYAN varlıklar / yollar

(K11 §4a/§4b + haritaların "bekçi yok" satırları. "Bekçi yok" = doğrudan davranış/eşzamanlılık sondası bulunamadı; dolaylı iz varsa belirtildi.)

| Sınıf | Varlık / yol | Ne eksik | Kaynak |
|---|---|---|---|
| **Eşzamanlılık bekçisi YOK — para/stok etkili** | `createShipment` :1376, `dispatchShipment` :1890 | paralel yazma sondası hiç yok (yalnız storno‖iade) | K11 H-3 |
| | Fason **kısmi kabul** (`currentQty` decrement), `cancelReceipt` (LIFO), `closeRemainder` | paralel sonda yok; `receive` yarışı yalnız born=1 invariant (kaybeden 409 mu cached mi bilinmiyor) | K11 H-4 |
| | Tambur geri alma (`tambur-undo`), finalize→WO kapama, **farklı-token aynı-parent paralel kesim** (metraj aşımı yarışı) | yalnız aynı-token idempotency ölçülü | K11 H-5 |
| | `completeWorkOrder` (kapanış dispozisyonu), `linkOrderLines`/`unlink`, `cancelOrderLine`, sipariş iptali | paralel yok; `FOR UPDATE` yalnız metin muafı | K11 H-6 |
| | Parti cerrahisi (`splitBatch/mergeBatches/moveRolls`) + **gerçek DB'de paralel parti doğumu** | yalnız sahte-tx SIRA bekçisi; 3 bekçide bayat "P2002/kilitsiz" yorumu | K11 H-1 |
| | Master-data `merge` (8027), session-registry (8024) | advisory kilit var, **ne sıra ne davranış bekçisi** | K3b H-7, K11 H-2 |
| | `openSack` çift, çuval kapatma, iade×iade, storno×storno, kartela dispatch/receive (token yok), KK2 `completeQc2` paralel, `import.apply` paralel, `grant/revoke/set/applyTemplate` paralel | — | K11 §4b |
| | WO satır kilidi SIRASI (8021 dahil 5 advisory noktasında sıra bekçisi yok; yalnız 8022 + 8023) | — | K11 H-14 |
| **İdempotency 4. durum** | Order / WorkOrder / Sack / Shipment / SubcontractorReceipt / ImportRun `clientToken` dalları: **iptal edilmiş kaydın token replay'i** | yalnız Roll manuel top ölçülü (`test_manual_roll_undo`); KK1 kodda da yok | K11 H-9, K10 H-1 |
| **Doğrudan test izi 0** | `helpers/traveler-card-fanout.helper.ts`, `helpers/label-intent.helper.ts`, `import/import-name-guard.ts`, `user-preference.service.ts` | hiçbir test adını anmıyor | K11 H-12 |
| **Dolaylı / zayıf** | `workorder-batch-drop.service` (WorkOrderService üzerinden), `mobile-update.service` (efemeral app), `code-unique.helper` (yalnız Item), `allocation.helper` (16 test dolaylı), `batch-dispatch-surgery.helper` (6 dolaylı) | çağrı grafiği ölçülmedi | K11 §4a |
| **Bekçi var, kör noktalı** | Kalite/Fire karnesi rework damgası (H2 K7b); Sevk günlük seri NET (H1 K7b); fason karne ham SQL kopyası (AST dışı); `test_dashboard` "bugün" sınırı; `test_db_invariants` §1/§5 **bilerek kırmızı** (kırmızı körlüğü); `test_kk1_duplicate_guard` bayrak açarak ölçer (kapalıyken yol kilitsiz); 24/33 sonda N=2, gecikme enjeksiyonu yok | K7b §9, K2b H-11, K11 H-10/H-11 |
| **Hata yolu bekçisiz** | process handler'ları (`unhandledRejection`/`uncaughtException`/graceful shutdown/pm2 mesajı); statü taşıyan http-errors → 500; `AppError.internal` + wBR tükenmesi audit'sizliği; 57014/40P01/25P03 varış dalı; `PrismaClientValidationError` audit'sizliği | grep 0 | K6 §7 |
| **Yetki bekçisiz** | `requirePermission(undefined)` → 500; `STATION_KIND_PERM ↔ SESSIONABLE_STATION_KINDS` eşitliği (fail-open); `resolveDevice` fail-open/closed dalları; `resolveExact` izin süzgeci; raw SQL tanımlayıcı allowlist'leri; `.env` izlenmemesi | — | K5 §12 |
| **Süreç/ops bekçisiz** | `test_single_process` YOK (tek-process invariantı 3 yerde belgeli); `productionDbGate` yalnız koşucuda (tek dosya koşumu kapısız; host-adı allowlist); `clean_test_residue` 5 önek (480 `TST-WHA` WO birikmiş) | — | K8 H-2, K11 H-7/H-8 |
| **Audit izi olmayan yazma yolları** | `reason-preset-catalog.job` (audit YOK); backfill ×4 + `normalize-names` + `sync-*` + seed'ler; `reset-operational.ts` TRUNCATE (kapı yok); `bench_audit_summary` sentetik INSERT; ham SQL merge MOVE'lar audit `merge()` düzeyinde [VARSAYIM] | — | K4 §3b/§3c, H7/H8 |

### D. Kilit / claim BOŞLUĞU olan yazma yolları (K3a §6, K3b §5, K10 §5 — ② eşzamanlılık hücresi için birleşik liste)

| # | Yol (dosya:satır) | Boşluk | Sınıf |
|---|---|---|---|
| 1 | `workorder.service.ts:3253-3285` `prepareFasonCancelDecision` | iptal tx'inden ÖNCE havuz `roll.updateMany` (AT_SUB→IN_PRODUCTION koşulsuz) + `cancelBulk failed[]` yutuluyor; tx düşerse geri sarılmaz | tx-öncesi yazım |
| 2 | `attachRolls :4444` / `manualMove :596` / split P1/P2 (R→A22→W) ∥ `dispatch :1036` / `completeWorkOrder` / `receive` (W→R→A22) | **ABBA-1**; A22 tek anahtar sistem geneli; P2034 retry yok (409) | deadlock adayı |
| 3 | `order.service.ts:475-514` `cancelOrderLine` (L tek → W → L hepsi) ∥ dispatch/cancel/undo tam-küme sıralı | **ABBA-4** (helper protokolü ihlali); `:500` tip flip `targetItemId` guard'sız | deadlock adayı + guard |
| 4 | `return.service.ts:879-935` `cancelReturn` ∥ `undoDispatch :2152` | 8023 alınmıyor, sevkiyat statüsü okunmuyor → iki tx de commit → SHIPPED top ↔ PLANNED/CANCELLED sevkiyat | check-then-act |
| 5 | `order.service.ts:3411` `reopen`, `:2537` `update` | recompute tam-küme `touchOrderLinesTx` olmadan → `shippedQty` lost-update | kilitsiz recompute |
| 6 | `workorder.service.ts:5602-5719` `updateTargetProperties` | kilit/claim/terminal guard YOK; `computeWorkOrderLocks(prisma)` pre-tx | guard'sız yazım |
| 7 | `inventory.service.ts:3916-3956` `applyManualProperties` | claim statü/`currentQty` pin'lemez; süpervizör kapsamı bayat statüyle; `:3866` metraj Tambur kesimiyle yarışır | claim eksik pin |
| 8 | `inventory.service.ts:963-985` KK1 replay | `existing.status` kontrolsüz → CANCELLED/SCRAP top `success:true` | idempotency 4. durum |
| 9 | `subcontractor.service.ts:2677-2678` `receive` wBR predicate'siz | aynı clientToken P2002 5× retry → yanıltıcı 409 | replay kalitesi |
| 10 | `subcontractor-management.service.ts:512-526` (+ `base.service.ts:1052`) | katlanmış kod tekilliği kilitsiz (8026 yalnız Item) | check-then-act |
| 11 | `workorder.service.ts:989` / `:1002` / `:5505` | tx içinde havuz client'ı (IE no, plan süresi) | tx sızıntısı |
| 12 | `shipping.service.ts:690-696` / `:769-777` (S(from)→S(to) id-sırasız) · `:1412-1418` / `:1551-1555` (istemci sırası) | **ABBA-2/3** | deadlock adayı |
| 13 | `subcontractor.service.ts:1539-1554` `bulkDispatchStep` SEPARATE | döngü ortası hata: önceki sevkler commit'li, yanıtta yok | sessiz kısmi |
| 14 | `subcontractor.service.ts:4757-4775` `cancelReceipt` LIFO | guard pre-tx; kısmi increment çift geri koyma penceresi | check-then-act |
| 15 | `shipping.service.ts:515-523` / `:563-568` `scanIntoSack` taşı; `splitSack :834-857`; `moveRollsToSack :772` | kaynak çuval kilitsiz; `moved` count kontrolsüz | sessiz kısmi |
| 16 | `order.service.ts:2726` / `:3081` `getActiveShipmentLinks` | pre-tx; iptal edilmiş siparişe PLANNED sevkiyat bağlı kalabilir → dispatch recompute CANCELLED'a yazar | check-then-act |
| 17 | `shipping.service.ts:1895-1903` `dispatchShipment` | boş sevkiyat / EXPORT tartı yalnız pre-tx | dar pencere |
| 18 | `workorder-link.service.ts:375-389` / `:453-471` | W kilidi yok; type flip statü/`targetItemId` guard'sız | guard'sız |
| 19 | `tambur-undo.service.ts:1221-1395` / `:1434-1692` | WO kilidi HİÇ yok; Step→WO sırası (`tambur-manual` WO→Step) | ABBA + guard |
| 20 | `permission-management.service.ts:187-221` / `:285-302` | `validUntil` geçmişe çekilince son-admin guard'ı koşmaz | mantık boşluğu |
| 21 | `label-template.service.ts:572-588` / `:533-546` ↔ `:986-1008` | default/context çapraz yarışı havuzda | check-then-act (küçük) |
| 22 | `helpers/guarded-hard-remove.ts:62-76` | guard sayımları havuz, silme tx; SetNull FK'lı 5 referans sessiz NULL | check-then-act |
| 23 | `tambur.service.ts:2448-2458` ↔ `:2466` `finalizeWarehouseCut` | yorum clientToken idempotency'sine dayanıyor, kod yazmıyor → replay 409 + rezervasyon yakar | yorum↔kod |
| 24 | `traveler-card.service.ts:269-291` `reprint`; `:350-386` `recordPrintEvent` | `version+1` claim'siz (uç çağrılmıyor); plan havuzda (bilinçli sınır) | read-then-write |
| 25 | `printed-document.service.ts:329-341` / `:654-685` | `version = max+1` kilitsiz findFirst; reissue snapshot havuz | read-then-write |
| 26 | `import/import.service.ts:447-475, 552-575` | clientToken yalnız tamamlanmış koşum; uçuşta çift koşum ikisi de yazar | idempotency penceresi |
| 27 | `label.service.ts:2075-2160` `recordPrintEvent` (top) | üç ayrı yazım tx'siz (kart yolu tx) | kısmi durum |
| 28 | `session-registry.service.ts:79` (8024), `master-data-merge.service.ts:546` (8027) | sıra doğru ama **bekçisiz**; 8027 120 s timeout ↔ DB 50 s | bekçi |
| 29 | `roll-finalize.helper.ts:172` ← `kursun-qc:936`, `kursun-bypass:1399` | sayaç kilidi WO'dan SONRA tx İÇİNDE, kalan tx boyunca (KK1'i serileştirir) | bilinçli takas (ölçülü) |
| 30 | `batch.service.ts:126` (8022) ↔ `workorder:4553→4566`, `manual-move:699` | 8022 → W ters sıra (WO PLANNED iken) | deadlock adayı |
