# TeksERP — Monorepo Kökü

> **Tek gövde, çok fabrika — kural yazarken sor: çekirdek mi, profil mü?** [ÇEKİRDEK] her kurulumda aynıdır (defter semantiği, brüt sevk, idempotency, kilit sırası, atomik claim, fail-closed kapılar, sır hijyeni, veri bütünlüğü). [PROFİL] bu fabrikanın seçimidir ve bayrak/veriyle değişir (rota, istasyon topolojisi, açık modüller, sayısal ayarlar). Sınıflama ölçütü ve red gerekçeleri: `docs/design/MODUL-BAYRAK-TASARIM.md` §0 ve §11.

Tekstil fabrikası ERP sistemi. Üç alt proje:

| Proje | Stack | Port |
|---|---|---|
| `Teks-Erp/` | Express 5 + Prisma 7 + PostgreSQL backend | 4000 |
| `Electron/` | Electron 42 + React 19 + Vite yönetim paneli | 5174 |
| `mobil/` | React Native + Expo 54, Android tablet (yatay) + telefon (dikey) — saha | — |

Her alt projenin kendi `CLAUDE.md`'si vardır. **Admin frontend değişiklikleri `Electron/`'a yazılır** — `React/` dizini artık yok.

**Dallanma (2026-09-02):** `feature/*` → `main`; **müşteri dalı YOK.** `adnansahin` ve `feature/depo-mal-kabul` dalları emekli edildi (ikisi de `main`'in atası — tek commit kaybolmadı). Tek gövde, tek şema, tek sürüm çizgisi; müşteri farkı yalnız bayrak profilinde yaşar — "adnansahin'de yok" demek "bayrağı kapalı" demek. `if (musteri === 'X')` fork'un ilk sinyalidir, yasak. Tasarım: `docs/design/MODUL-BAYRAK-TASARIM.md` (§12 uygulama kuralları bağlayıcı). Fabrika paketi ve demo derlemesi `main`'den üretilir.

## Üretim Akışı

> ⚠️ **Referans profil: işlemeci (adnansahin)** — aşağıdaki akış bu fabrikanın ROTASIDIR, sistemin zorunlu akışı değil; her adım istasyon kataloğundan + rota şablonundan kurulur. Profil gerçeği — bkz. `docs/design/MODUL-BAYRAK-TASARIM.md` §5 (üretim esnekliği) ve §10 (profil tablosu).

```
Stok (Roll) → İş Emri → KK1 (RAW_QC) → [opsiyonel Fason] →
  Kurşun + KK2 (PROCESS_QC) → Tambur (final karar) →
  Depo (RollStatus.WAREHOUSE) →
  Çuval Depo (Sack — depo nesnesi, müşteri opsiyonel; aç→okut→(opsiyonel tart)→(opsiyonel not/etiket), mühür/rezerv YOK) →
  Sevkiyat (depodan çuval seç + müşteri/sipariş ata → PLANNED → DISPATCHED)
```

Fabrika **çözgü/dokuma yapmaz** — kumaş hazır gelir, sadece process + QC + tambur yapılır.

> ⚠️ Profil gerçeği — bu cümle adnansahin'in rotasını anlatır, sistemin kısıtını değil: devere (levend/çile/bobin aktarma), çözgü, haşıl **yeni mimari istemez** — istasyon kataloğuna istasyon, rotaya adım eklenir. Dokuma diliminin tek tasarım işi "top TEZGAHTAN doğar" damgasıdır (doğum: tezgah + çözgü levendi + iplik lotu). bkz. MODUL-BAYRAK-TASARIM §5.2 · Dilim 4.

**Her rotanın SON adımı** topu final ürüne çeker — Tambur özel değil (2026-07-13). Kaliteden çözülen `RollStatus` (varsayılan `WAREHOUSE`; `QualityGrade.targetStatus` override, seed: 1.Kalite ve A1 → WAREHOUSE (ikisi de SATILABİLİR), FİRE → SCRAP (2026-08-20: 'fire çöpe gider'); kalite null → WAREHOUSE) + `form=ACIK` (Tambur çocukları `TOP`) + barkodsuz açık kumaşa barkod. `PRODUCED` limbosu **kaldırıldı** (enum'dan da düştü). Tambur = kesim/bölme + kalite kararı istasyonu; "final ürün kapısı" DEĞİL. Ham (renksiz) top kesiminde operatör parçayı `STOCK` (üretime devam) da seçebilir. Depo bir istasyon değil, tartı/paket öncesi bekleme statüsüdür.

### Karar Notları Dizini

> **TAM METİNLER: [`docs/history/CLAUDE-NOT-ARSIVI.md`](docs/history/CLAUDE-NOT-ARSIVI.md)** — aşağıdaki her madde oradaki bir notun özetidir. Bir alana dokunmadan önce ilgili TAM notu oku; buradaki satırlar yalnız "dur ve arşive bak" tetiğidir. **Yeni tarihli not CLAUDE.md'ye değil arşive yazılır**, buraya yalnız özet satırı eklenir.
>
> **Yeni not sınıf etiketi TAŞIR:** buraya eklenen her özet satırı `[ÇEKİRDEK]` (her kurulumda aynı — defter semantiği, veri bütünlüğü, idempotency, kilit sırası, fail-closed kapı, sır hijyeni) ya da `[PROFİL]` (bu fabrikanın seçimi; bayrak/veri/rota türevi) etiketiyle başlar; karma notlarda etiket cümle bazında verilir. Ölçüt: MODUL-BAYRAK-TASARIM §11'deki "bayraklanmayacaklar" listesindeki her şey ÇEKİRDEK'tir.

- **Çuval depo modeli (2026-07):** Çuval depo nesnesidir — `Sack.customerId` opsiyonel, mühür/rezerv YOK; sevkiyat depodan çuval seçerek kurulur; `SackAllocation` sevk ANINDA yazılır, stok yalnız DISPATCH'te `SHIPPED` düşer (PLANNED tahsis sayılmaz); sipariş görünümü İstenen|Sevk|Açık. Tasarım: `docs/design/CUVAL-HAVUZU-TASARIM.md`.
    - ⚠️ Profil gerçeği — "rezerv YOK" bugünkü kurulumun seçimidir, kalıcı bir domain kuralı değil: rezervasyon **kendi append-only defteriyle** ayrıca kurulacak (`shipping.reservationEnabled`, karar #6) ve `SackAllocation`'a DOKUNULMAYACAK — o sevk muhasebesidir. Yeni rezervasyon yüzeyi yazmadan önce MODUL-BAYRAK-TASARIM karar #6 + §9. (Aynı şerh yukarıdaki Üretim Akışı bloğunun "mühür/rezerv YOK" satırı için de geçerlidir.)
- **2026-07-30 — Çuval notu + SACK etiketi + tek dokunuş tartı:** `Sack.notes` annotation'dır — `touchWarehouseSackTx` guard'ı UYGULANMAZ, EKLEME (eklersen özellik 409'a düşer); gösterimi üç yerde opt-in (`DocCol.defaultHidden` + `shown` allowlist — blocklist yeni kolonu müşteri irsaliyesine sızdırır). SACK etiketi barkodu = `sackNo` (ayrı kolon YOK); baskı FAIL-CLOSED (şablon yoksa 400, roll'a sapmaz). `LabelKind` genişletirken 4 yer elle güncellenir.
- **2026-08-02 — Koşullu etiket elemanı (showIf):** Koşul ELEMANDA yaşar, değer `QualityGrade.code`'dur (ad DEĞİL); karşılaştırmada `toLocaleUpperCase("tr")` YASAK; kalitesiz topta koşullu eleman BASILMAZ (fail-closed); tek uygulama noktası `prepareElements` (yeni emitter `expandMultilineText`'i doğrudan çağırmaz); koşullu alan bağlam kimliği sayılmaz.
- **2026-08-02 — Sevk rakamı BRÜT (fiş/irsaliye):** `getDispatchReport` donmuş snapshot'tan okur; iade AYRI belgeyle kapanır (`RETURN_DISPATCH` iade ANINDA donar, iptalde VOIDED); muhasebe export'unda sevk satırları BRÜT (`RollReturn` geri-ekleme); "Güncel" rozeti = "son versiyon" demek, "içerik güncel" değil.
- **2026-08-02 — Brüt kuralı: liste + muhasebe ekranı:** `attachTotals` metraj+adet BRÜT (kg geri-ekleme İSTEMEZ); `dispatchedAt` sıralamasında union'ın iki tarafında alan adı FARKLI (`shippedAt`); fatura izi `invoiceNo` (ERP fatura KESMEZ), izin `shipping:invoice` (`shipping:write` vermeden).
- **2026-08-03 — Brüt kuralı: sevkiyat detayı (çuval içeriği):** `getShipmentById` BRÜT — `prevSackId` ile geri-ekleme, snapshot OKUNMAZ; iade satırı çuvalda kalır (`returned` işaretli, salt-okunur); `totalKg` DEĞİŞMEZ; iade sorgusu ayrı tx → canlı id kümesiyle dedup ŞART; mobil istemci brütü elle kurmaz (çift sayar).
- **2026-08-05 — Belge yolu da brüt:** `collectShipmentDocContent`'i `reissue` + lazy-init sevkten SONRA da çağırır → üretici de `RollReturn` geri-eklemeli; "freeze tek koruma" varsayımı YANLIŞTI. Yeni sevk-içeriği yüzeyi eklerken soru: "sevkten SONRA da koşar mı?"
- **2026-08-04 — Roll.foldType + entryReason + manuel top PARTİLİ:** Kat KALICI kolondur ve MİRAS ALINMAZ (kesimde SEÇİLEN yazılır; alan hiç gönderilmezse parent→plan fallback'i sözleşme boşluğudur, miras değil); kanoniklik ZORUNLU (`normalizeFoldType` — ham değer filtrede sessizce 0 satır döndürür). Sebep audit'ten değil KOLONDAN okunur (audit 6 ayda arşivlenir). Parti: tek açık→sormadan bağla · çok→400 `BATCH_REQUIRED` · hiç→null; `SCRAP` partiyi KAPATMAZ; sıra: payload tutarlılığı ÖNCE, parti SONRA.
- **2026-08-06 — Rota şablonu HEDEF saklar:** `RouteStep.plannedColorId` + `RouteStepProperty`; hedef ÖNERİDİR kilit değil; rotadan WO'ya: SON renk veren adım kazanır, özellikler birleşir; renkte `hasDefaultCategory` DE aranır (yoksa Tambur adımına renk yazılır); istemci sözleşmesi DÜZ ID dizisi (nested write reddedilir); istasyon değişince adım hedefi SIFIRLANIR.
- **2026-08-05 — Kurşun Planlama (birleşik ekran + rejim anahtarı):** Tek ekran `/operations/kursun-dagitim`; karo bayraktan bağımsız, bayrak REJİM anahtarı; makine içi sıra `listDistribution`'da JS ile (`sortByPlanOrder` — düşerse sürükleme geri zıplar, hata yok); sekmeler MAKİNE listesinden doğar; toplu sonuç PARÇALI (bilinçli, `failed[]` gösterilir); tablet bayrak açıkken salt-okunur ama yalnız UYGUN adımda — tek kapı `assertKursunTabletMayWrite`.
- **2026-08-06 — Dağıtım ön koşul DEĞİL (milestone confirmation):** Tambur okutması dağıtılmamış kurşunu da kapatır; kapsam: bayrak AÇIK **ve** adım bypass'a UYGUN (aynı kaynaktan beslenir, ayrışırsa çıkmaz geri gelir); makine atfı UYDURULMAZ (`machineId=null` + amber bant); marker `KURSUN_BYPASS_FINISHED:UNASSIGNED:` BASE ön ekiyle başlamak ZORUNDA.
- **2026-08-05 — Manuel top GERİ ALMA + izlenebilirlik yüzeyleri:** `TamburUndoService.MANUAL` → `softDelete` motoru (`qtyOut=0` storno ≠ `qtyOut=qtyIn` dispozisyon — karıştırma); iptal edilen `clientToken` replay EDİLEMEZ (409 `ENTRY_CANCELLED`); barkod topun KİMLİĞİdir, erken doğması sorun değil (statü korur); `roll:history` izni yoksa yaşam döngüsü bölümü HİÇ çizilmez; alan eklemek yetmez — hangi YANITTA döndüğünü doğrula ("Ekleme Nedeni" dersi).
- **2026-08-05 — KK1 mükerrer koruması (atomik + operatör saati):** Guard tx İÇİNDE ve İLK ifade `pg_advisory_xact_lock(8021,…)` — sıra load-bearing (sonra alınan kilit hiçbir şey kazandırmaz, barkod sayacından ÖNCE); pencere `clientEnteredAt` (iki yönlü); istemci uçuş penceresi 90 sn — aynı yük→aynı kimlik; `'link'`→bırak / `'server'`→koru (`shouldReleaseInFlight`); 5xx "ulaşılamıyor" DEĞİLDİR; `onMutate` yeşil basmaz.
- **2026-08-12 — Giriş izlenebilirliği (kim/nereden):** `entry-users`/`entry-stations` lookup uçları GERÇEK veriyi döner; `createdById`/`entryStationId` generic `buildWhereClause`'dan geçer — elle okuma `readIdCondition`'sız P2007 üretir; Electron servis yolları TAM yazılır (`/api/...` — öneksiz 404 + FilterBar "Sonuç yok" yutar); "İstasyon" filtresi yalnız üretim sekmelerinde; `kk1.historyAllEntriesEnabled` DÖRT kapı; mobil "Son Kayıtlar" HER ZAMAN kişiye özel, zorlama saf katmanda (`forcedCreatorFilter`).
- **2026-08-12 — Top listesi filtresi (KK1+Tambur ORTAK):** Süzme SUNUCUDA (cursor'lu liste — istemci süzmesi yanlış "kayıt yok" üretir); `dateField` gönderilmezse aralık SESSİZCE yok sayılır; tarih seçici paketi EKLENMEZ (takvim elle yazıldı); `last7` bugünü içerir; aşım kesimi 0'da tıkanmaz (`gt:0` iki daldan kalktı); kesim çocukları `createdMachineId` damgası alır; NumpadHost `autoActivate` yalnız TEK alanda.
- **2026-08-13 — Kesimde kat sessizce düşüyordu:** İstek gövdesini ELLE kuran her katman (mutationFn) sessiz bir allowlist'tir — Zod dersinin istemci ikizi; kat panelden düzeltilir (üçlü sözleşme: undefined DOKUNMA · null TEMİZLE · kod YAZ; kanoniklik `resolveFoldTypeForWrite`) + etikete basılır (katalog KODU basılır, ad değil; `present:false` → şablonda dursa da atlanır; ÇUVAL etiketinde YOK); bekçi sondası kataloğa eklenebilecek GERÇEK kod kullanamaz; PPLB beklentisi emitter'ın kendi dönüşümüyle (`cleanCtlCp1254`) kurulur.
- **2026-08-12 gece — SINGLE_RESTORE + F0402:** "İş Emrine Geri Al" = tek topun metrajıyla dirilme (kardeşlere dokunulmaz; kapanış sapmaları TERSLENMEZ); metraj geri koyma İKİ DALDA FARKLI (üretim: yalnız currentQty · depo kesimi: ikisi birden); F0402: `computeRestoredQty` çocuk-kapsamlı `TAMBUR_UNDO_SINGLE` düzeltmelerini de sayar ve restore-toplamına giren HER kaynak `applyFull` 5b terslemesine eklenir (yoksa çift sayım); genel "stoktan kaldır" tuşu bilinçli RED (tipli olaylar var).
- **2026-08-12 — Kayıt kuyruğu kaldırıldı (ölü mektup kutusu):** Kalıcı düşüş artık ANLIK TOAST (`announceFailure`); çakışma 409'ları (`POSSIBLE_DUPLICATE`/`CLIENT_TOKEN_COLLISION`) toast BASMAZ — tek yüzey modal; toast "kayıt oluşmadı" DİYEMEZ (timeout ≠ yazılmadı); köprü `setMutationDefaults`'a taşınamaz (component onError ezer); `serverReachability` yoklama adresi CANLI okunur + `revalidateServer`; yazıcı kuyruğu DURUYOR.
- **2026-08-04 — "Giriş noktası" kuralı:** İş emrine AŞAĞIDAN katılan top (fason dönüşü çocuğu, elle eklenen) yukarıdaki adımları BEKLETEMEZ — giriş noktası = en erken hareketin adım sırası; çözülemezse bekleyen say (erken COMPLETED geç olandan kötü); bekçi `test_consistency §20` ürün koduyla AYNI kuralı söylemeli.
- **2026-08-05 — Parti no kâğıda + kart bayat bayrağı:** Fason çeki tekil `batchNumber`, kabul makbuzu ÇOĞUL `batchNumbers`; müşteri belgelerinde OPT-IN (`sections`/`columns.hidden` BLOCKLIST'tir — naif kolon varsayılan GÖRÜNÜR doğar); `TravelerCard.contentDirty` tek yazma noktası `markTravelerCardDirtyTx` (fazla işaretlemek güvenli, eksik hata); `GET .../html` bayrağı TEMİZLEMEZ (`print-event` temizler); rozet yüzeyleri 2026-08-06'da kaldırıldı, backend mekanizması DURUYOR.
- **2026-08-05 — İş emri belgeleri TEK uçtan:** `GET /work-orders/:id/documents` dört kaynağı birleştirir — istemci kendi listesini kurmaz; liste izni ↔ baskı izni (`DOC_PERMISSIONS`) HİZALI olmalı (ayrışma = görünen satır + sessiz 403); iptal belge listede KALIR (`cancelled:true`); Electron'da her satır önizleme açar (generic `PrintedDocDialog` varsayılan dal); sıralama: kart önce, sonra tarih DESC.
- **2026-08-05 — Sevk STORNO ≠ iade + toplu iade:** Storno = mal hiç çıkmadı → belge İPTAL, stok döner, iade defterine GİRMEZ; `Roll.preShipStatus` (2. kalite top WAREHOUSE'a dönmesin); `freezeForSource` versiyonu `max+1` (sabit 1 → yeniden sevkte 500); tahsis SİLİNMEZ (`shippedQty` defterden türer); izin `shipping:undo-dispatch` (`shipping:write` kapsamaz); toplu iade: N defter satırı + TEK irsaliye (`returnGroupId`), üye id'siyle belge ÇÖZÜLMEZ — `documentSourceId` kullan.
- **2026-08-05 — Belge yerleşimi (A5 + alan bazlı punto):** İki ayrı oran (genişlik ~0.68, yazı ~0.85) — tek `scale()` değil; alan CSS'inde `calc()`/`var()` YASAK (`scaleDocCss` regex'i sessizce ölür); belgeye özel kural ortak katmandan SONRA basılır; DÖRT KAPI birlikte güncellenir (SystemSetting tipi + sanitize + controller Zod şeması + Electron aynası) yoksa ayar sessizce kaybolur; şablon literalinde BACKTICK YASAK.
- **2026-08-06 — Sayfa içi arama (Ctrl+F) KALDIRILDI:** Kullanıcı kararı — listenin sunucu araması kullanılır. Yeniden denenirse: Electron `findInPage` seçeneğindeki `findNext` "SONRAKİ eşleşme" değil "YENİ OTURUM BAŞLAT" demektir (ters yazım sessiz 0/0 üretir).
- **2026-08-06 — Rol şablonları KODA taşındı:** `role-template-catalog` + boot uzlaştırma + bekçi (izin kataloğunun ikizi); uzlaştırma İZNİ getirir ATAMAZ — tek görünürlük "N yetki hiçbir kullanıcıda yok" bandı; sistem rolü SİLİNMEZ pasifleştirilir (sert silme restart'ta diriliş); kimlik `code`'dur ad değil; SoD üçlüsü (`shipping:invoice`/`undo-dispatch`, `roll:manual-adjust`) yalnız Muhasebe/Süpervizör rollerinde.
- **2026-08-06 — Filtrelerde çoklu seçim (CSV):** CSV de bir string'dir — elle okunan HER id filtresi `readIdCondition`/`readFilterList`'ten geçer; üç arıza modu: uuid→400 P2007 · düz string→sessiz 0 satır · ön-süzgeçli→filtre sessizce DÜŞER (yanlış liste!); deploy sırası pazarlık dışı: backend ÖNCE; iki değerli NOT NULL enum'da çoklu seçim gürültüdür (`destination` DirectShipment'ları düşürür); kapsam anahtarlarına (`processingStatus` vb.) dokunulmaz.
- **2026-08-10/11 — Üretim karakteristiği (kat kataloğu + istasyon modu):** `FabricProperty.valueType` (BAYRAK|SEÇİM) + `FabricPropertyValue`; kat değeri KOLONDA kalır (`RollProperty` küme modeli, değer pivotta değil); kod ASCII (`TUP`, `TÜP` değil); biçim (`normalizeFoldType`) ↔ geçerlilik (`resolveFoldTypeForWrite`) ayrı, katalog boşsa FAIL-OPEN; SEÇİM tipli özellik hedef seçicilerden SÜZÜLÜR (`isTargetableProperty`); metre cihazı BİREBİR eşleşir, yoksa null (başka cihaza SAPMAZ); `StationProperty.mode` AUTO|OPTIONAL|REQUIRED — kurşun bypass ataması AUTO arar; `Station.appliesColor/appliesProperty` kategoriden ayrıldı; replace'ler yalnız FLAG satırlarına dokunur (CHOICE = operatör verisi, silme); değerli seçim `RollProperty.valueId`, soyağacı valueId taşır.
- **2026-08-09 — Rapor temeli (finalizedAt) + karneler:** `Roll.finalizedAt`/`statusChangedAt` TRIGGER yazar (40+ çağrı noktası — uygulama koduna güvenilmez); kaynak statü listesinde `SHIPPED`/`CANCELLED` BİLEREK YOK (storno eski topu bugünün karnesine sokar); damga üzerine yazılır (kaliteyle aynı olaydan); backfill HAM SQL (Prisma update `updatedAt`'i tazeler → tüm listeler yeniden sıralanır); "1. KALİTE" koda gömülmez (`sortOrder`'dan çözülür); fason atfı ÜÇ kovalı; "dönemde sevk edilen metraj" TEK tanım `reports/_shipped.ts`; ölü stok = eski VE siparişsiz (kesişim); parti no benzersiz DEĞİL → arama daima aday listesi.
- **2026-08-19 (2. paket) — Sektör boşlukları kapatıldı (yeni izin kodu YOK):** ① onay modalına **"Boyahaneye Geri Gönder"** (`/tambur/manual/send-to-dye`; hedefi SUNUCU çözer — KANONİK `stepCanApplyColor`, moveService'in dar `colorStep`'i DEĞİL; mevcut adımdan ÖNCEKİ en yakın boya adımı; AT_SUBCONTRACTOR yazılmaz, çıkış Fason Sevk'ten) · ② **`RollPlanDeviation` kalıcı defteri + Plan-Sapma Karnesi** (audit 6 ayda arşivlenip rapordan düşüyordu; granülerlik geçiş×alan, **`confirmationId` çift-sayım kilidi**, `field` string; migration `20260819190000`) · ③ **kısa-kesim eşiği MERKEZE** (feature-flag dört kapı; cihazda 3 durumlu override `server|on|off`, birleştirme TEK yerde `resolveShortCutConfig`, override yetkisi süpervizör çifti; Electron `FlagDef.numberField` — iç ad `numberKey:` ZORUNLU) · ④ **tabletten bağ sökme** (sheet 3. sekme "Bağlı (N)", son-bağ kuralı istemci aynası — ⚠️ 2026-08-21'de DEĞİŞTİ: son bağ artık reddedilmez, iş emri STOK'a döner). Backend ÖNCE, Electron+APK birlikte.
- **2026-08-19 — Tambur plan-sapma kapısı + Sipariş Bağla:** Renk/en hedeften sapan top depoya ONAYLA iner (409 `PLAN_MISMATCH` → `confirmMismatch`, top başına BİR soru, audit imzalı; üç yol TEK helper `tambur-plan-gate.helper`; en eşiği ±10, scrap/discard + renk-hedefsiz WO kapı dışı); Tambur üst şeridinde "Sipariş Bağla" (`workorder:write`), uyumsuz seçim yalnız süpervizörde → `order-links/override` zinciri (plan+toplar+bağ; kumaş farkı HER ZAMAN red; tek tx DEĞİL, bilinçli); "Rengi Değiştir" diyaloğu boyanmış top dağılımını basar. Backend+Electron+APK birlikte.
- **2026-08-19 — Fason KISMİ KABUL (100 gitti, 51 geldi, 49 sonra):** Kısmi kabulde top TÜKETİLMEZ — AT_SUBCONTRACTOR kalır, `currentQty` kalana iner; her teslimat AYRI makbuz, defter `SubcontractorReceiptItem.receivedQty`+`isPartial`; kalem yalnız TAM satırla kapanır (outstanding filtreleri `isPartial:false` okur — 21 nokta); replay kimliği artık `SubcontractorReceipt.clientToken` (küme-eşitliği guard'ı kısmiyi cached DÖNEMEZ); ikinci+ teslimatın topları YENİ parti (K5 aynası); "kalan gelmeyecek" = `close-remainder` (fire → RollVariance `SUBCONTRACTOR_REMAINDER` + kalem `remainderClosedAt` damgası); iptal LIFO; karne dönen metrajı defterden okur (eski kod fire'ı hep %0 basıyordu). Bekçi: `test_fason_partial_receive.ts`. Backend ÖNCE, Electron+APK sonra.
- **2026-08-19 — Hazır sebep katalogları koddan DB'ye (`ReasonPreset`):** Fire · kayıt düzeltmesi · elle top ekleme · iptal listeleri fabrikanın kendi diliyle düzenlenir; `code` rapor anahtarıdır ve ASLA değişmez, `label` serbest (metin saklayan İKİ kind istisna — orada düzenleme geçmişi eski metinle bırakır). Okuma üç kademeli (sunucu → cihaz → APK zemini; boş liste operatörü kilitler), doğrulama senkron önbellekten beslenir ve GİZLİ kodu da kabul eder; silme yok/son aktif satır gizlenemez; yeni izin kodu YOK (`roll:manual-adjust` ∨ `mobile:tambur-duzelt`). Fire ekranında serbest metin kutusu listenin ÜSTÜNDE; iptal ekranında satır içi kalem YOK (chip dokununca iptal eder).
- **2026-08-21 — Üretim rengi TEK BEKÇİ (`workorder-target-color.helper`), KİLİT MALA BAKAR:** "Düzenle" ve "Rengi Değiştir" AYNI kurallardan geçer — COMPLETED plan değişikliğine kapalı (`PLAN_CHANGE_FROZEN_STATUSES`; uyumlu "Sipariş Bağla" AÇIK), `allowedColors`, **mal–plan uyumu** (`recolorRollIds` düşülmüş boyanmış top ↔ yeni renk: uyum tam → SERBEST (kayıt düzeltmesi) · boyanacak top varken eski renkte top → 409 `COLOR_PARTIAL_CONFIRM` + `confirmPartial` · boyanacak yok → 409 `COLOR_DYED_BLOCKED` → Tebdil / yeni iş emri / tümünü düzelt), rota kapsaması UYARI (`ApiResponse.warnings`); adım statüsü (COMPLETED) kilit DEĞİL. Planlamacı (`workorder:write`) iş emrinin TÜM açık kumaşlarını tek hamlede düzeltir (`apply-attribute-to-rolls` any-perm, motora izin listesi verilmez). Fason kabul: hedef renk KİLİT ALTINDA taze + `expectedTargetColorId` farklıysa 409 `TARGET_COLOR_CHANGED`; plandan farklı renkte **tek soru `planColorAction`** (APPLY_TO_PLAN → plan da döner · ROLLS_ONLY → `RollPlanDeviation source="fason-receipt"`, Tambur kapısı tekrar SORMAZ). "Renk veren adım" tek yüklem `stepCanApplyColor` (taşıma/split/kursunFinish renk yazar); sipariş kalemi rengi dar uç `PATCH /orders/:id/lines/:lineId/color`. Bekçi `test_wo_target_color_guard` (59). Migration/izin YOK; backend ÖNCE.
- **2026-08-21 akşam — Tutarlılık taraması ("türetilmiş alan / ayrışan yüzey" sınıfı):** Fason "açık+outstanding sevk" koşulu TEK KAYNAK `helpers/fason-open-dispatch.helper.ts` (`OPEN_OUTSTANDING`/`OUTSTANDING_ITEM`/`outstandingItemOfOpenDispatch`) — elle kopya AST bekçisiyle YASAK (`test_fason_open_dispatch_single_source`); `repointPendingBypassAssignmentsTx` (devirde bypass ataması taşınır, split'te void; force-void'den ÖNCE, workOrderId+step birlikte); kartelalık toggle + çuval içerik/not (`markSackContentChangedTx`, şablon-koşullu `sackNoteAppearsOnLabel`) + sipariş iptali → `labelDirty`/`contentDirty`; üretilen metraj = 1.kalite+A1, kalite kovası KATALOGDAN (`loadProducedBuckets`, isActive süzgeci YOK, `notIn: []` yok); Electron ölü query-key + liste tazeleme + `lineOpen` tek helper; yeni bekçi çifti `consistency-check-derived.sql`+`test_consistency_derived.ts` §21-§26 (`--probe`; §26 bilgi + §26b `PLAN_GATE_SINCE`). Rapor `docs/design/TUTARLILIK-TARAMA-2026-08-21.md`. Migration/izin/APK YOK; K1 davranış değişikliği (kabul iptali → WO picker'da gizlenir).
- **2026-08-21 — İş emri TİPİ bağın aynası:** `linkOrderLines` STOK iş emrine ilk bağı eklerken aynı tx'te `type→ORDER_PRODUCTION` (atomik `updateMany WHERE type=STOCK`, yanıt `typeChanged`, audit izli); tip HİÇBİR yerde beyan değil bağdan türer (form `buildPayload`, `createFromRolls`) — liste/künye/kart `type` basar, detay `orderLinks` okur, ikisi ayrışmamalı; SİMETRİK (2. tur): `unlinkOrderLine` son bağ kalkınca ORDER → STOK (tek 400: hedef kumaşsız WO — STOK olamaz), sipariş iptali `UNLINK_ONLY` tek-siparişli WO'yu da STOK'a döndürür (PLANNED'da tek izinli aksiyon oydu), mobil ayna `canUnlinkOrderLine(type, n, hasTargetItem)` → `becomesStock` notu (APK ister, backend önce güvenli); geçmiş tutarsızlık `scripts/fix_workorder_type_from_links.ts` (dry-run, `--apply`), migration DEĞİL; `create()`/`replace()` de aynı kuralı uygular ("STOK + satır" gövdesi sunucuda ORDER — istemci disiplinine bırakılmaz).
- **2026-08-21 — Fason kabulü: ÇEKME bir hata değil, ölçülen gerçek:** Boyahanede kumaş çeker (250 giden → 220 dönen, %12); eski ekran bunu "EKSİK DÖNEN / uyuşmuyor" alarmıyla karşılayıp operatörü top-top metraj bölüştürmeye ve sahte yarım toplara itiyordu. ① Kısmi kabul TEK soru: "fasonda kalan var mı" + tek metraj; dağıtım **büyük toptan** (`resolveReturns`, kalan EN AZ topa yığılır, tamamı kalan top dönüş listesine GİRMEZ, sıra deterministik). ② Fark **ÇEKME** adıyla ve NÖTR renkle basılır; uyarı/onay yalnız `fason.shrinkWarnEnabled` + `fason.shrinkTolerancePct` (default açık/%10) aşılınca — ⚠️ toleransta `<=0 → null` kalıbı YASAK (0 = "tolerans yok", null = varsayılan). ③ Çekme/fazla artık **sapma defterinde** (`SUBCONTRACTOR_RETURN`, SCRAP/OVERAGE, sistem sebebi `FASON_CEKME` — `SCRAP_REASONS`'a EKLENMEZ), çok toplu kabulde orantılı dağıtım + artık son satıra; `sourceRefId` (MİGRATION) terslemenin adresidir (kısmi teslimatta rollId ile silmek önceki meşru sapmayı da süpürür). ④ Fason karnesi fire'ı artık gerçek (defter = düşülen ≠ fiziksel dönen; `openQty`'ye GİRMEZ). ⑤ Tek Parça modunda satır sayısı HER ZAMAN 1 (eskisi manuel satırın yanına ön-dolu ekleyip metrajı ikizliyordu); mod geçişi `switchReceiveMode`. ⚠️ RESTRICT FK: fason kabulü yapan HER test cleanup'ı `rollVariance.deleteMany` içermeli (23 dosya güncellendi).
- **2026-08-21 — nameFold DB SEDDİ (3 tablo) + sebep KODU topun satırında (sıfırlama penceresi):** ① `customers/items/subcontractors` üzerinde partial UNIQUE `<tablo>_nameFold_key` (`WHERE "mergedIntoId" IS NULL` — tombstone aynı adı meşru taşır; RENK BİLİNÇLİ HARİÇ, `foldColorNameForCompare` DB fold'undan zengin). Şemada **`@@unique([nameFold])` + varsayılan ad** (index↔unique drift; `_<kolon>_key` regex'i error.middleware'de kolonu çıkarsın, `nameFold`→"ad"). Uygulama bekçisi (`assertNameNotDuplicate`) KALIR — mesajı o verir; DB check-then-act'in yarış/config-unutması/içe-aktarım/elle-SQL yollarını kapatır. Migration ilk sürümde "mükerrer varken DÜŞER" idi — **2026-08-22'de sıfırlama rafa kalkınca YUMUŞAK KAPIYA çevrildi:** mükerrer olan tabloda index ATLANIR (NOTICE), deploy geçer; temizlik sonrası aynı dosya yeniden koşulur (enforce); prod'da o güne dek `test_db_invariants` §1 kırmızı = "enforce bekliyor" (bilerek). Dev'de 8 grup merge aracıyla temizlendi; yeni tabloya sed eklemeden önce `find_fold_duplicates.ts` (artık soy bağı süzgeçli). Sabit adla master-data yaratan test fixture'ları damgalı (artık kalan ad 2. koşumu P2002'ye düşürür). ② `Roll.entryReasonCode/cancelReasonCode` (VARCHAR 64, index yok): metin GÖRÜNEN kayıt, kod RAPOR ANAHTARI; kodu **sunucu türetir** (`resolveReasonCode` — açık kod katalogda doğrulanır → 400 `REASON_CODE_INVALID`; yoksa metin label/fullText ile KATLANMIŞ eşlenir; serbest metin → NULL, uydurulmaz) → APK değişmeden dolar. Çözüm **tx DIŞINDA** (`applyRollDispositionsTx`'e çağıran geçirir). `KIND_STORES_TEXT` bayrağı KALIR (fullText saklama kararı), "kod saklanmaz" anlamı düştü. Mobil açık kod gönderimi sonraki APK işi (çevrimdışı `BUILTIN_*` kodları GÖNDERİLMEZ). **Anomali taraması (aynı gün):** etiket düzenlenince eski metin koda çözülmüyordu → `ReasonPreset.legacyTexts[]` (eski-ad sözlüğü; `update()` tek yazar, sınır 20, güncel ad öncelikli, belirsizlik → null) + `test_reason_presets §6`; `workorder.routes` Swagger YAML bloğu (tırnak) + yeni bekçi `test_swagger_spec` (`failOnErrors`); `seed-fixtures` tombstone'u diriltmez. Bekçiler: `test_master_data_name_dup` §9 (DB seddi + tombstone), `test_reason_presets` §5, `test_roll_cancel_undo`, `test_tambur_manual_*`. ⚠️ `test_consistency` §18 customers'ta tombstone süzgeci (her müşteri birleştirmesi §18'i kırmızı yapıyordu).
- **2026-08-22 — Sevk Kapısı = BAYRAĞIN ekranı; storno kapalı rejimde sevkiyatı KAPATIR:** Karo `visibleWhen` saf bayrak (`shipping.confirmationEnabled`) — "VEYA çıkış bekleyen PLANNED varsa" kuralı + `sack-store/board?limit=1` sondası KALKTI (ctx tek alan; route bayrağa bakmaz, eski sekme/okutma hedefi açılır). Storno `releaseSacks`: storno + `cancelPlannedShipmentTx` AYNI tx (iptal gövdesi TEK KAYNAK — `cancelShipment` de onu çağırır; toplar ÇUVALDA kalır, çuval havuza, tahsis silinir, tüm irsaliye sürümleri VOIDED; storno izni kapanışı kapsar, `shipping:write` aranmaz). Varsayılan İSTEMCİDE, önizlemedeki `confirmationEnabled`'dan (kapalı → işaretli, açık → işaretsiz; kullanıcı değiştirir; yeniden çıkış = Paketleme'den YENİ sevkiyat). Sevkiyatlar detayı (sheet + tam sayfa) PLANNED'a **"Sevk Et"** (ortak `DispatchConfirmDialog`, `shipping:write`) → planlı sevkiyatın Sevk Kapısı'na ihtiyacı yok. Bekçi `test_shipment_undo_dispatch` §10 (50) + `tile-visibility.test`. Migration/izin/APK YOK; backend ÖNCE (eski Electron `releaseSacks` göndermez → PLANNED, geriye uyumlu).
- **2026-08-22 — Mükerrer paneli v2 P1 (tespit kuyruğu) + sıfırlama RAFTA:** DB sıfırlama ertelendi → 28. migration YUMUŞAK KAPI. Panel: `duplicate-detection.service` üç kural (kesin ad · kimlik: VKN/ihracat kodu/e-posta/telefon/kumaş kod harf-ikizi · bulanık ad — **birim KELİME, karakter DEĞİL**: Jaro-Winkler KALDIRILDI (ön ek bonusu `BOYER EMRE`↔`BOYER`i 0.900 ile aday yapıyordu; kullanıcı "farklı firma" dedi). Sıralı kelime hizalaması: FIRM gürültü kelimeleri + çift ORTALAMASI, PRODUCT gürültü YOK + kelime sayısı & numerik token eşit + EN DÜŞÜK çift; sıkıştırılmış metin eşitse %100. Eşik okunabilir ölçek: %100 yazım/boşluk · %90 neredeyse aynı · %80 tek harf; "fazladan anlamlı kelime" hiçbir eşikte gelmez), `duplicate_reviews` (karar ÇİFT bazlı: NOT_DUPLICATE kalıcı gizler, DEFERRED işaretli, MERGED motor yazar — hook `MasterDataMergeService.merge` sonrası best-effort), ayar `duplicatesFuzzyEnabled`/`duplicatesFuzzyThresholdPct` (dört kapı; Ayarlar → Müşteriler), CSV dışa. ⚠️ `token_set_ratio` KULLANILMAZ (alt-küme %100); renk `hex` kimlik DEĞİL; canlı kopyada kumaş bulanık 9 yanlış pozitif → 1 gerçek. Tasarım + canlı ölçüm `docs/design/MUKERRER-PANELI-TASARIM.md`; bekçi `test_duplicate_detection`. **P2** alan-bazlı survivorship (`fieldPicks` — seçim DEĞER değil KAYIT üzerinden; yazım ATOMİK CLAIM'DEN SONRA, yoksa ad seçimi partial UNIQUE ile kavga eder; `code` ve kimlik alanları seçilemez). **P3c** hayalet top (mükerrer ham giriş) sekmesi — birleştirme DEĞİL `MUKERRER` iptali (top işlem kaydıdır, metraj toplanmaz); tespit tek kaynakta (`duplicate-rolls.service`, script onu kullanır), "asıl" = etiketi BASILAN top, iptal topun KENDİ ucundan (guard'lar atlanmasın). **P3a/b (şube·istasyon·makine·kategori) ÖLÇÜMLE ERTELENDİ** — canlıda 0 mükerrer; reçete tasarım §9'da. **P4** `apply_merge_decisions.ts` CSV köprüsü (dry-run varsayılan; inceleme SQL'de, UYGULAMA MOTORDA — ham UPDATE 42 kurallık haritayı ve audit'i atlar). **P5** kimlik alanına (VKN) DB seddi BİLİNÇLİ YOK: aynı tüzel kişiye ikinci cari kart meşru olabilir → kuyruğa aday düşer, yazma engellenmez.
- **2026-08-22 — Mutabakat kapısının kırmızısı ÜÇ AYRI ŞEY demek olabilir (§13/§18/§20 prod ölçümü):** `test_consistency` salt-okunurdur, asıl değeri CANLI DB'ye karşı koşmaktır (prod kopyasında 22 bölümün 19'u temiz). **§13 = KOD HATASI, kapandı:** `tambur-undo.applySingle`ın ÜRETİM dalında (`cutOpenFabric`) aşım koruması YOKTU — arşiv ikizi ve `applyFull` `initialQty`'yi yukarı çekip `OVERAGE`/`TAMBUR_UNDO_RESTORE` yazarken canlı dal yazmıyordu (arşiv dalının yorumu ikisinin "birebir ayna" olduğunu söylüyordu; ayna orada kırıktı). Aşım bayrağı varsayılan AÇIK ve canlıda satırı YOK → yol erişilebilir; ölçüldü: 100 m'lik topa 40+40+40 kesip parçaları tek tek geri alınca üçüncüsünde `cur(120) > init(100)` + deftere HİÇ satır düşmüyordu. Bekçi `test_tambur_undo §11` (§5 bu invariantı yalnız FULL+DEPO kesiminde ölçüyordu — bekçinin kör noktası hatanın kendisiyle aynı yerdeydi). **Canlıdaki 2 eski satır BİLEREK düzeltilmedi** (toplu UPDATE kök nedeni gizler; kapı görünür tutar, düzeltmek iş kararı) — §13'ü DARALTMA. **§18 = İŞ KARARI:** ad mükerrerleri panelin işi; sedli tablolarda (kumaş 5 · fason 2 grup) yumuşak kapı index'i atlıyor, birleştirilince enforce edilir. **§20 = SORGUNUN KÖR NOKTASI:** adımın tek topu sonradan iptal edilince `COMPLETED` adım `PENDING`'e çöküyor; adım durumu tarihsel olgudur, veri bozuk değil.
- **2026-08-25 — Saha deploy sonrası üç arıza (kutu var/uç yok · soru var/süreç yok · ölçek var/sınır yok):** ① `dispatchWithoutColor` ("fasona renksiz git") YEDİ katmanda sessizce düşüyordu — dört Zod bloğu + üç Prisma adım-yazımı + rota allowlist'i; servis katmanı BAŞTAN doğruydu, o yüzden servisi çağıran test yeşil kalırdı (bekçi `test_dispatch_without_color` §1 bilerek METİN tarar); canlıda 623 adımda 0 işaret. Mobil kutusu eklendi, değer her fason adımı için AÇIKÇA gider (true DE false DA — yoksa kapatma niyeti yutulur). ② **Ölü etiket onayı KALDIRILDI** (sektör standardı değildi, karşılığında etiket-toplama süreci hiç kurulmadı; masaüstünde zaten hiç bağlanmamıştı → etiketli top oradan HİÇ iptal edilemiyordu). Kolon DURUYOR, kaldırılan şey SORU; `confirmLabelPrinted` kabul edilir ama kapı açmaz. **"Stoktan kaldır" ikiye ayrıldı:** İptal (`CANCELLED` — "hiç olmamalıydı", stok düşmez, storno `qtyOut=0`, fire raporuna girmez) · Fire (`SCRAP` — "vardı, gitti", stok düşer, `qtyOut=qtyIn`, fire oranına girer; yeni uç `POST /rolls/:id/scrap`, izin `roll:manual-adjust`). Sebep katalogları AYRI ve iki FARKLI kapıdan geçer (`ROLL_CANCEL` metin saklar → `resolveReasonCode`; `ROLL_SCRAP` saklamaz → `validateVarianceReason` — tip düzeyinde ayrı, zorlama). Çıkış izi kolonları BİLİNÇLİ paylaşılır (ayrım `status`; migration YOK); fire geri alınamaz. Ölçüm kararı doğruladı: 230 iptal / 1 fire, sebepli 24 iptalin hepsi kayıt hatası. ②b **"İptal ettim, arşivde göremedim"**: `STATUS_GROUPS.ARCHIVE` iptal/fire taşımıyordu → 231 CANCELLED + 1 SCRAP hiçbir yüzeyde görünmüyordu (barkod araması da çare değil, bir kısmı barkodsuz). `CANCELLED,SCRAP` eklendi + sayfaya ARAMA + "Kayıt Türü" daraltması (anahtar bilerek `statusIn` — `status` DEĞİL: backend önceliği `statusIn>status`, seçenekler arşivin alt kümesi olduğu için ezmek zararsız). Arşiv hâlâ `admin:settings` arkasında. ②c **İptali geri almada "partiye kayıtlı" engeli KALDIRILDI** ("SAP'taki gibi yap"): SAP'ta parti ANA VERİdir, ters kaydı engellemez — engelleyen sonraki hareket/tüketim/sevktir ve onlar ayrı kural. Kalan beş kural sektör ölçütlerinin birebir karşılığı. `recomputeStepStatus` partiye HİÇ BAKMAZ → hareketsiz topu diriltmek adım sayacını değiştiremez (riski taşıyan `movementCount>0` zaten ÖNCE kontrol ediliyor). Ölçüm: geri alınabilir iptal 130→216/231. ⚠️ Açık: eski kayıtlarda `preCancelStatus` NULL → Ham Stok'a döner (bloklamak 42 topta regresyon olurdu); geri alma iptal izini kolondan TEMİZLER, geçmiş yalnız audit'te. ③ Fason Kabul "devasa dikey boşluk" — **ilk teşhis (yazı ölçeği) YANLIŞTI**; tablette `uiautomator dump` ile ÖLÇÜLDÜ: RN Paper `SegmentedButtons` (düğmeleri `flex:1`) bir satıra konunca Yoga onu satırın TAMAMINA açar → yanındaki `flex:1` başlık SIFIR genişlik → metin karakter karakter sarılır (21 karakter ≈ 380 px görünmez boşluk; soru ekrana HİÇ çıkmıyordu = "neyin evet/hayır'ı"). Düzeltme: kart dikey + kendini anlatan seçenekler ("Hepsi geldi / Bir kısmı fasonda kaldı"). **Kural: `SegmentedButtons` `flexDirection:'row'` kabının doğrudan çocuğu OLAMAZ; yanına bir şey konacaksa AÇIK `width` (minWidth YETMEZ).** Bekçi `mobil/src/test/segmented-buttons-row.guard.test.ts`. Yerleşim hatasında tahmin değil ölçüm (`adb exec-out screencap` + `uiautomator dump`). Migration/izin YOK; backend ÖNCE, Electron+APK sonra.
- **2026-08-25 — Bitmiş topu yeniden üretime alma: backend destekliyordu, HİÇBİR istemci kullanamıyordu.** `attachRolls`/`quickStart` `STOCK/WAREHOUSE/A1_STOCK` kabul eder ("her işlem final üretir") ama masaüstünde ekran YOKTU (`attach-rolls` ucu 2026-06-12'de kaldırıldı, `quick-start`ın Electron istemcisi hiç yazılmadı) ve tablet okutmada `status !== 'STOCK'` diye ELİYORDU. Ölçüm: 980 topun 4'ü iki WO'dan geçmiş, dördü de HAM — akış sahada hiç kullanılmamış. Çözüm: Bitmiş Depo'da **"Yeniden Üretime Al"** (`ReworkRollsDialog` → mevcut `quick-start`; yeni uç/migration/izin YOK; Ham Stok'ta gösterilmez). ⚠️ **Fason firma seçicisi LOAD-BEARING** — yoksa "Fasona gönder" sessiz no-op (backend firma çözemezse sevki atlar; sahadaki iki rotada da planlı firma yok); firma `stepPlanning` overlay'iyle gider ve buton metni de aynı yüklemden (`willDispatch`) beslenir. ⚠️ **Ölü etiket uyarısı burada KESİN** (fason kabulünde top `SUBCONTRACTOR_CONSUMED` olur, mal YENİ barkodla döner) — aynı gün kaldırılan genel iptal onayından farkı bu; koşul DAR (etiketli top **ve** ilk adım fason). Uçtan uca ölçüldü: WAREHOUSE → `IE2508260002`/`P90`/`FS2508260001` → `AT_SUBCONTRACTOR`. AÇIK: tablet hâlâ bitmiş topu okutamıyor (ayrı karar, APK ister).
- **2026-08-25 — Prod'un üç "dev'de yapılacaklar" notu teyit + uygulama (kur.ps1 · renk seddi · deploy notu):** ① `deploy/kur.ps1` — otomatik geri alma `app.eski-*` yokken çalışan kurulumu SİLİYORDU (sil→hedef, geri koy→kaynak); onarım: kaynak yoksa dokunma + mevcut `app\`'ı **pm2 ile geri kaldır** (notun yaması yalnız komut basıyordu → fabrika kapalı kalırdı), `-ErrorAction Stop` (hedef doluysa `Move-Item` düşmez **içine taşır**), `KokeDon` `GeriAlOtomatik`'in İLK satırında; Yama 3 (kilit sondası) bilinçli RED; script kendini güncelleyemez → sunucuya ELLE kopya; harness 12/12, orijinal 8/4 (negatif kanıt). ② Renk seddi `tr_fold_color(name)` ifadesinde (`colors_nameFoldColor_key`, yumuşak kapı) — ayırıcı sınıfı AÇIK (`\s` YASAK: saha C locale), BMP genelinde JS≡SQL (`test_fold_contract` §2b); **drift allowlist GEREKMEZ (ölçüldü)**; `EXPRESSION_UNIQUES` girdisi ZORUNLU (+predicate); `find_fold_duplicates` renkte renk kuralı + keskin taramalar; `ColorService` tombstone'u dışlar; rakamlı fixture'ı harfe yapıştır (rakam blokları sırasını korur). ③ `SURUM-2.9.0` damgalandı + 12 düzeltme; `git pull → build` akışı DÖRT dokümanda bayattı (fabrika `kur.ps1` paketle kurulur, `premigrate_` yedeğini script alır); `/health`'te `auditGuard` YOK (`/api/admin/health`). Dev DB prod karar dosyasıyla eşitlendi (13 birleştirme; V-1430 kaldı → `items` seddi dev'de atlanır). **Ders:** prod notunun teşhisi tutar, "repoda şu var / şu bekçi yeter" cümleleri varsayımdır — 6'sı ölçümle düzeltildi.
- **2026-08-25 (akşam) — Mobil Hızlı İş Emri bitmiş topu da alır + sebep kataloğu:** Tablet `if (status !== 'STOCK') reject` ile eliyordu → akış hiçbir istemciden yapılamıyordu. Karar seti: ham+bitmiş aynı WO'da SERBEST (rozetle) · hedef renk BOŞ gelir · sebep İSTEĞE BAĞLI · 2. kalite DAHİL. Okutma kararı saf yükleme (`scanClassify` — ⚠️ statü kontrolü çuvaldan ÖNCE: `SHIPPED` topa "çuvaldan çıkarın" demek malın müşteride olduğunu gizler); `RollPickerModal.scopeTabs` (Ham Stok/Bitmiş Depo — sekme anahtarı sorgu anahtarına GİRMELİ); sebep İKİ hedefe (`buildReworkPlan`: metin → 1. adım notu → **fason çekisine talimat** · kod → `parameters.rework` rapor anahtarı, migration YOK). Yeni `ReasonPresetKind.WORK_ORDER_REWORK` — **beş kapı birlikte** (şema+migration · backend katalog · Electron KIND_TABS · mobil union · mobil zemin); ⚠️ mobil zemin GERÇEK kodları taşır (`BUILTIN_*` DEĞİL — bu kind metin saklamaz, sunucu koddan türetmez). `ReasonPresetPicker` üç kopyadan tek bileşene alındı (serbest metin ÜSTTE). Ölü etiket uyarısı onay adımında, koşul DAR (etiketli **ve** ilk adım fason) — burada geçersizleşme KESİN. Bekçiler: `scanClassify`(12) · `reworkPayload`(8) · `ReasonPresetPicker`(6). APK 2.9.6/vc53.
- **2026-08-26 (akşam) — Sebep adımı ÜÇ BÖLGE + sıra sürüklenerek KALICI:** Fabrika listeye sebep ekledikçe (7. satırda görüldü) Tambur → Bitir → sebep adımında **Geri/Kaydet ekran dışında kalıyordu** (başlık+liste+footer tek `View`, sheet `maxHeight %85` ile kırpıyordu; kırpılan taraf hep EN ALT). Artık: başlık+serbest metin SABİT · liste `Animated.ScrollView` içinde KAYAR (`flexShrink:1`) · footer SABİT. Sıra **basılı tut-sürükle** ile değişir ve `PATCH /reason-presets/reorder` ile KALICI olur (uç 2026-08-19'dan beri vardı, istemcisi yoktu; `react-native-sortables` + `Sortable.Touchable` — tek dokunuş seçer, basılı tutma sürükler). ⚠️ Sunucu TÜM id'leri ister ama ekran yalnız AKTİF satırları çizer → köprü `mergeVisibleOrder`: gizli satır KENDİ YUVASINDA kalır (naif "görünenler önce" pasifleri her sürüklemede dibe toplardı); tam liste YAZMA ANINDA çekilir. `localOrder` başarıda temizlenmez (göz kırpması), hatada temizlenir. Sürükleme yalnız `canEditPresets`'te ve `builtin:` zemin satırlarında KAPALI. Bekçi `reasonPreset.order.test.ts` (8, naif uygulamada 4 kırmızı). Tablette uçtan uca doğrulandı (APK 2.9.7/vc54). Migration/izin/backend YOK, APK gerekir.
- **2026-08-26 — Fabrikanın eklediği sebep 60 sn'lik pencerede yaşıyordu ("taze ya da hiç" yanlış takas):** Senkron doğrulama önbelleği (`cachedRows`) TTL dolunca `null` dönüp KOD kataloğuna düşüyordu — orada yalnız SİSTEM satırları var, yani fabrikanın panelden eklediği her sebep bir YAZMADAN sonraki 60 sn dışında 500 veriyordu (Tambur "Kayıt düzeltmesi": hazır seçenek çalışır, özel sebep "tamamlanmadı — sunucu hatası"). TTL'in işi TAZELİK'tir GEÇERLİLİK değil → bayat liste de döner + arka planda tazeleme TETİKLER (tek-uçuş yalnız ARKA PLANA ait: yazmalar kendi yazdığını görmek zorunda); bayatlık ≠ boşluk (önbellek HİÇ dolmadıysa fail-closed KALIR — `expireReasonPresetCacheForTest` ≠ `invalidateReasonPresetCache`). Geçersiz kod artık `AppError` 400 + `details.code` (düz `Error` 500'e düşüyor, mobil kuyruk da 5xx'i geçici sanıp 3 kez deniyordu). ⚠️ Bekçinin kör noktası hatanın kendisiyle aynı yerdeydi (`§3` doğrulamadan hemen önce tazeliyordu) → `§3b/c/d`; "kendini onarır" kontrolü ilk yazımda ETKİSİZ SONDAYDI ("kod kabul edildi mi" bayat listeyle de EVET der) — ölçüm önbelleğin GÖRMEDİĞİ satırla kurulur. Migration/izin/APK YOK, yalnız backend.
- **2026-08-26 — Electron dağıtımı: güncelleyici kuruluydu, kablosu takılı değildi:** `electron-updater` bağımlılıklardaydı ama `electron/` altında SIFIR referansı vardı ve `build.publish` yoktu (yani `latest.yml` hiç üretilmiyordu) → setup elden ele taşınıyordu. Kurulan akış: generic provider + VPS yayın adresi, açılıştan 30 sn sonra + 4 saatte bir kontrol, arka planda indirme, ekran üstü şerit, kurulum kullanıcı "Yeniden Başlat" deyince. Kararlar: `perMachine` KALDI (⚠️ hesap yönetici değilse o makine sessizce güncellenmez) · `autoInstallOnAppQuit=false` (kapanışta cevapsız UAC bırakmasın) · güncelleme ZORUNLU (inerken şerit → kapatılamaz kapı + 2 dk geri sayım; hata HİÇ gösterilmez, körleştirir) · `artifactName` ASCII'ye çevrildi (`Ş`+boşluk `latest.yml` URL'inde sessizce 404 üretir) · adres TEK KAYNAK `shared/update-feed.ts` ↔ `package.json` (bekçi `src/test/update-feed-url.test.ts`; yayın `guncelleme.etkiliyazilim.com/<müşteri>/<ürün>/` — ⚠️ CF proxy AÇIK kalmalı, sertifika Origin CA; adres pakete DERLEME ANINDA gömülür) · makineye özel adres ezmesi (derlemeye gömülü adres yanlışsa elle tura düşmemek için). **Son bir elle tur kaçınılmaz** (güncelleyici sahadaki sürümlerde yok). Reçete: `docs/ops/ELECTRON-OTOMATIK-GUNCELLEME.md`. Migration/izin/APK YOK.
- **2026-08-27 — Rota kapsaması: hedef PLANDAN değil SİPARİŞTEN türüyordu:** Sipariş bağlanınca hedef renk/özellik sipariş satırından TÜRETİLİR (`resolvedTargetColorId = onlyColorId`, `orderLineRequiredProperty` birleşimi) ve hemen `assertRouteCoversTargets` koşup **400** veriyordu → saha tarifi: *"sipariş bağlarsam hata, siparişsiz açınca geçiyor"*. Boyahanesiz rota (kurşun+tambur) dışarıdan boyalı gelen mal için DOĞRUDUR — kuralın gerekçesi ("hedef asla uygulanmaz") orada geçersiz. ⚠️ Aynı soruya İKİ CEVAP vardı: 2026-08-21'de düzenleme yolu "REDDETME, UYAR"a çevrilmiş, oluşturma yolu sert hata kalmıştı. **Karar (ikinci tur): kapsama artık REDDETMEZ, UYARIR** — create · replace · "Rengi Değiştir" tek kural, yanıt `ApiResponse.warnings` taşır (sektör: SAP PP'de yönlendirme uyarısı emri durdurmaz). ⚠️ KABUL EDİLEN RİSK: eksik rotayla iş emri açılabilir → uyarı metni NE eksik + SONUCU somut söyler. Dar kapı mantığı DURUYOR ama artık engeli değil UYARIYI bastırıyor — `create()` üçüncü parametre `goods` alır (yalnız `quickStart` doldurur) ve nitelik topların **HEPSİNDE** varsa kontrol atlanır; `some` yazmak kapıyı sessizce açar, mal bilgisi yoksa muafiyet de yok (F221 — masaüstü formu top almaz, davranışı değişmedi), muafiyet hedef rengi SİLMEZ. Bekçi `test_wo_route_coverage_goods` (7, iki negatif sonda). Migration/izin/APK YOK, düzeltme tamamen sunucuda. AÇIK: oluşturma↔düzenleme asimetrisi tam kapanmadı.
- **2026-08-27 (3. tur) — Yarı mamul: kalan dört yüzey; AYRIM GÖSTERİMDE, ARZDA DEĞİL:** Ürün Dengesi (`yariMamul` kovası; **`malzemeAcigi` İKİSİNİ BİRDEN düşer**) · sipariş karşılama (`freeSemiFinished`, `netGap` değişmedi) · iptal geri alma mesajı (`entrySource`e bakıp "yarı mamul stoğunda" der) · kapanış dispozisyonu etiketi ("Ham stok" → **"Stoğa geri"**, üç yüzey; `STOCK` bir STATÜdür, sekmeyi `entrySource` belirler). ⚠️ **TEK KURAL:** yarı mamul rafta duran, üretime sokulabilir maldır → arzdan DÜŞÜLMEZ, ayrı GÖSTERİLİR (SAP: HALB ayrı stok türü ama MRP/ATP'de arz). Negatif sonda: arzdan düşünce 400 m talepte **200 m sahte kumaş açığı** doğuyor. Bekçi `test_semi_finished_surfaces` (10); §4 "toplam korunuyor" kontrolü ayrımın aritmetik değil sunum olduğunu kilitler. ⚠️ Ürün Dengesi TALEPTEN doğar — sipariş kalemi olmayan spec listede HİÇ görünmez (fixture sırası load-bearing). Zamanlama gerekçesi ölçümdü: prod'da 0 yarı mamul kaydı varken kapatıldı, fabrika yanlış rakamı hiç görmedi. Migration/izin/APK YOK.
- **2026-08-27 (2. tur) — Yarı mamul ayrımı Kanban + tablete taşındı; Kanban'ın ESKİ sapması da kapandı:** Üretim Akışı'na "Yarı Mamul" kolonu (ayrı renk — komşu kolonla aynı tonda olsa sayaçlar karışır) ve mobil Depo'ya "Yarı Mamul" sekmesi (`rollScope=RAW_STOCK_PURE`/`SEMI_FINISHED`; "Tümü" bilerek statü tabanlı; **APK GEREKMEZ** — saf JS, uzaktan güncellemeyle gider). ⚠️ Aynı dokunuşta Kanban'ın `currentStepId` boşluğu: kolon düz `{status:STOCK}` sorguyordu, adıma bağlı topu sayıyordu → **yarı mamulden BAĞIMSIZ**, panoyla Envanter farklı rakam basıyordu (bugün fark 0 ama tesadüfen). Bekçi artık ANAHTAR değil **SAYI SEMANTİĞİ** ölçüyor (negatif sonda: pano=48 envanter=47). Görsel tur iki "derleyici görmez" hatası buldu: **"Tip" kolonu yarı mamulü "Bitmiş" gösteriyordu** (beşinci unutulmuş-enum vakası) + Stok Karnesi 5 kartı 1024px'te sıkışıyordu. Üç yüzeyde tek rakam doğrulandı (Karne 47 ↔ Envanter 47 ↔ Kanban 47).
- **2026-08-26 — Yarı Mamul kendi sekmesine ayrıldı + envanterden iş emri açılabiliyor:** ① 2026-08-17'nin "Ham Stok listesinde FİLTREYLE süzülür" kararı DÖNDÜ — filtre yetmedi, ayrı **sekme** (ayrı DEPO yine YOK, değişen yalnız görünüm). ⚠️ **ÜÇ kapsam var ve `RAW_STOCK` BİLEREK geniş** (ham+yarı mamul birleşimi — mobil Hızlı İş Emri top seçicisi onu kullanır, DARALTMA = tablette o toplar kaybolur); masaüstü dar ikizleri `RAW_STOCK_PURE` / `SEMI_FINISHED` kullanır, negasyon istemciden söylenemez. ② **"Üretime Al" artık Ham Stok + Yarı Mamul'de de var** — eski gerekçe ("normal iş emri açma yolu kullanılır") GEÇERSİZDİ: o yol masaüstünde yok (form top almaz, attach ucu 2026-06-12'de kalktı), backend ise baştan beri STOCK/WAREHOUSE/A1_STOCK kabul ediyordu. Ölçüm: prod'da SEMI_FINISHED kaydı SIFIR — paketin üç ucu birden açık kalmıştı (rota yok · izin 0 kullanıcıda · masaüstü çıkışı yok). ③ Üç sessiz "altıncı enum değeri unutuldu" hatası kapandı (`entryTitle`→"Ham Giriş" yalanı · mobil KK1 kendi yazdığı topu gizliyordu · `entrySource`'a yazan İKİ filtre birbirini eziyordu). ④ Stok Karnesi de ayrıldı (`semiQty`) — yoksa "ekran 800 / rapor 950" çelişkisini biz üretirdik. ⑤ `rollScope` FAIL-CLOSED. Terim **"Yarı Mamul"** (TDK; "mamül" 12 yerde düzeltildi). Migration/izin/APK YOK; backend ÖNCE. **Panelden: "Yarı Mamul (Kurşun+Tambur)" rotası oluşturulmalı.**
- **2026-08-26 — Mobil uzaktan güncelleme, İNTERNET (VPS) kanalından:** Tabletler güncellemeyi Electron paneliyle AYNI sunucudan alır (`guncelleme.etkiliyazilim.com/adnansahin/mobil/` — yol düzeni `/<musteri>/<urun>/`, Electron ile AYNI konteyner ve standart; müşteri segmenti alt alan adı DEĞİL çünkü wildcard sertifika iki seviyeli adları kapsamıyor); **ERP bağlantısı fabrika ağında kalır** — iki kanal, her biri kendi tek kaynağından (`mobil/scripts/lib/feed.cjs` ↔ `EXPO_PUBLIC_API_URL`), hiçbiri diğerinden türetilmez. Mimariyi ÜÇ ölçüm belirledi: ① imza gövdenin HAM baytları üzerinden doğrulanır → **manifest yayın anında DONDURULUR**, üreten tek yer yayın script'i (`scripts/lib/manifest.mjs`), sunucu yalnız bayt servis eder (backend'in render eden kodu SİLİNDİ; LAN ikizi olarak statik servise indi) · ② istemci mükerrer indirmeyi ÜÇ katta kendisi engeller → statik nginx yeter, yeni konteyner YOK · ③ `runtimeVersion` filtresi SUNUCUNUN işi (istemci indirmede bakmaz, yanlış sürümü indirip SESSİZCE eler) → adres sürümü İÇERİR, her APK yalnız kendi paketini görür. **Kod imzalama AÇIK** (anahtar VPS'te DEĞİL; imza geçersizse tablet paketi reddeder) ve **APK arm64** (ölçüldü 113→49 MB). Isıran üçlü: `codesigning:configure` app.json'daki `updates.enabled`ı sessizce `false` yaptı · mühür eklentisi idempotent değildi (2. prebuild'i düşürdü) · "iki adres farklı" uyarısı kalıcı yanlış alarma dönüşecekti. Bekçiler: `test_mobile_update` (37, tek baytlık bozulma sondası dahil) · `update-feed-url.test` (7); nginx yapılandırması artık REPODA (`deploy/guncelleme-sunucusu/`) ve ⚠️ **uzun-cache kurallarında `add_header … always` YASAK** — `always` başlığı 404'e de ekler, Cloudflare o 404'ü bir hafta tutar (Electron'da 147 MB'lık paket sunucuda dururken adres 404 döndü); yayın script'i artık `?cb=` kalıbıyla bayat önbelleği tespit edip bağırıyor. Reçete `docs/ops/MOBIL-UZAKTAN-GUNCELLEME.md`; migration/izin YOK.
- **2026-08-27 — Sipariş görünürlüğü (özet şerit + 6 rapor + iptal sebebi + kalem iptali):** Liste/cursor/özet artık TEK where (`BaseService.buildListWhere`) — şerit listeden sapamaz. Kalem iptali SOFT: `recomputeOrderStatus`ta iptal kalem `quantity` DEĞİL `shipped` ile sayılır (yoksa sipariş sonsuza dek PARTIAL_SHIPPED); "açık talep" süzgeci TEK KAYNAK `helpers/order-line-scope.helper.ts` (AST bekçili; `cancelledAt == null` **gevşek** — katı `===` alanı seçmeyen çağırana tüm kalemleri iptal saydırır). Karşılanma raporu `getCoverageForLines` KULLANMAZ (havuzu her satıra tam yazar → çift sayım). 4 migration; izin/APK YOK.
- **2026-09-01 — Patron modülü: fabrikaya GELEN PORT AÇMADAN uzaktan takip:** Cloudflare Tunnel + Access; uzaklık **soketten** çözülür (`req.socket.localPort`, tünel dinleyicisi YALNIZ `127.0.0.1` — `clientType` gövdeden gelir, güvenlik sınırı DEĞİLDİR; 4001 `0.0.0.0`a açılırsa ayrım komple çöker). Access JWT ikinci katman ve FAIL-CLOSED (politika panelden kalkarsa sessiz açık yerine gürültülü arıza); JWKS önbelleğinde TTL **tazelik**tir geçerlilik değil (bayat döner + arka planda tazeler; hiç dolmadıysa fail-closed KALIR). Uzakta PIN/kart/cihaz/keşif/swagger **404** (403 keşfe davet eder) — `quickPin` 6 hane, DÜZ METİN, sistem genelinde `@unique`. ⚠️ `CLIENT_IP_HEADER` app-wide okunursa LAN'daki biri `CF-Connecting-IP` uydurup giriş kilidini VE hız sınırını etkisizleştirir → `clientIpHeaderRemoteOnly` (`REMOTE_PORT`ten türer). helmet **İKİ ÖRNEK** (HSTS/CSP-upgrade LAN'a sızarsa panel kullanıcının HSTS önbelleğinde kilitlenir); dispatcher'ın adı `helmetMiddleware` olmak ZORUNDA. **İki dinleyici ≠ iki process** — invariant korunuyor. TOTP kendi kodumuz (RFC 4226/6238 vektörleriyle DIŞARIDAN doğrulandı), kurulumun TEK yolu yöneticinin 15 dk'lık penceresi (TOFU 2FA'nın koruduğu tek senaryoyu kapatır) ve ikinci faktör `issueToken`den ÖNCE koşar; üç hata kodu üç ayrı statü (giriş kilidi yalnız 401'i sayar). `ClientType.WEB` (yoksa telefon tarayıcısı masaüstü oturumunu düşürür; `!== "mobile"` YAZILMAZ). `GET /api/boss/overview` beş bölüm tek uçta, süzme SERVİSTE, **yeni izin kodu YOK**; üretim kartı **ADET** basar (metraj ikinci tanım olurdu). `BossShell` sekme sistemini bypass eder ama router altyapısını KULLANIR (kendi memory router'ı geri okunu sessizce öldürür); hash `useHashPath` ile REAKTİF okunur. `dist-web` backend paketine girer (drift imkânsız; eksikse kök sessizce durum sayfası olur → derleme kapıları). Migration additive, izin/APK YOK. Reçete: `docs/ops/UZAK-ERISIM-KURULUM.md`.
- **2026-09-02/03 — Modül anahtarları P1 (tek gövde / çok fabrika, Dilim 1):** DB anahtarları `finance.enabled` KALIBINDA (`ticaret.enabled` · `iplik.enabled` · `depo.multiEnabled` · `kumasTeknik.enabled` · `tezgah.enabled`; tasarımdaki `modul.*` kavramsal ad, koda GİRMEZ); middleware adı `require`+PascalCase(alan) (`requireProductionEnabled`, `requireDepoMultiEnabled`), yeni dosya `module.middleware.ts`, jenerik `requireModule("x")` YASAK (bekçiler AST/metin arar); 403 kodu **`details.code`** altında (`body.code` hep undefined); yer tutuculara middleware/panel YOK; tek kaynak `constants/module-flags.ts` (P2 süperadmin guard'ı ad kalıbıyla DEĞİL bu kümeyle); bağımlılık iplik→ticaret middleware zinciri + yazma doğrulaması, okuyucular HAM; **iplik kapısı SERVİSTE** (`applyYarnMovementTx` ilk iş — route kapısı yetmedi); ham ayar ucu modül anahtarına 400; grandfathering migration KOŞULLU (`EXISTS rolls`) ve değer=DÜNKÜ DAVRANIŞ (ticaret/iplik := `finance.enabled`, depo := aktif depo>1, sabit false demo'da dört yüzeyi 403'e düşürürdü); `readProductionEnabled` satır-yok sigortası KORUNUR; sıfır fark ölçüldü (70 mount'ta yeni 403 yalnız goods-receipts + warehouse-transfers); "bekçi yeşil ≠ kapı doğru" — beş kör nokta AST ile kapandı; `.env` artık `tekserp_demo`yu gösterir, yazan bekçiler env-override ister (`lib/hedef-db-kapisi`). Backend ÖNCE; APK/izin YOK.
- **2026-09-03 — Süperadmin P2 (satıcı hesabı):** `User.isSystemAccount` (additive); kimlik JWT'ye GİRMEZ — `verifyToken`ın mevcut tazelik okumasına tek kolon → `req.isSystemAccount`; tam yetki `getEffectivePermissions` ilk ifadesi `["*"]` (grant satırı doğmaz; süperadmin ROL DEĞİL, katalogda `*` yok); gizli-hesap süzgeci TEK KAYNAK `helpers/system-account.helper.ts` (`VISIBLE_USER` · `visibleUserWhere` süzgeç SONDA spread · `VISIBLE_ACTOR` · `VISIBLE_ACTOR_OR_SYSTEM` — `SystemLog.userId` NULLABLE, düz yazım fabrikada 641 sistem olayını sessizce düşürür · ham SQL `IS NOT TRUE`), 12 yüzey + 8 gerekçeli muaf, AST/ham-SQL bekçili; audit satırı KALIR, aktör "Sistem Bakımı"/"sistem" ve AUTH satırında `recordId` (login adı) DA maskelenir (maske aktör nesnesinde yetmez — satır düzeyinde); id → PIN zinciri: `/users/:id` ÖNEK kapısı sistem hesabında **404** (403 varlığı doğrular), önek izni alt rotaların BİRLEŞİMİ; `flagWriteGuard` üçüncü dal EN ÖNDE ve `.some` (belge dalı `.every` — yönler ters), senkron, `MODULE_FLAG_SUPERADMIN_ONLY`; **emniyet supabı** `SystemAccountRegistry` (hesap YOKSA admin:settings yeter + audit; bilinmiyor=VAR; supap dalında tembel DB doğrulaması, `/auth/me` aynı yüklem `resolveSystemAccountLock`; defter TEK YÖNDE tazelenir — kaldırma restart ister); hesabın doğuşu ~~`.env` tohumlaması~~ → ⚠️ **2026-09-03 P8 ile KALDIRILDI**, tek yol `npm run superadmin:kur` (aşağıdaki P8 satırı); kurtarma kodu BİLİNÇLİ yok; `*` körlükleri: mobil `has('*')` (planda YOKTU — tablette "yetkin yok"; OTA), Electron `hasAdminAccess`, backend üç `includes("admin:*")` (kısayol korunur); Electron Modüller sekmesi SALT-OKUNUR + bant; `SECRET_FIELDS` yalnız `changes`i maskeler — audit yükü çağıranda temiz olmak ZORUNDA. Süreç: `pkill -f "tsx src/server.ts"` YASAK (tasarım §12-12). Backend ÖNCE; APK YOK.
- **2026-09-03 — Ayar şifresi P3 (ikinci kapı):** hash `security.settingsPasswordHash` ama `systemSettingService.set()` ÜZERİNDEN DEĞİL (audit `oldData/newData`yı HAM yazar) — doğrudan upsert + ayrı olay; `security.*` ön eki listelerden/`config-bundle`dan dışlanır, `PUT /admin/settings/:key` reddeder. Şifre YALNIZ `X-Settings-Password` BAŞLIĞINDA (gövde audit `changes`ine, query erişim loguna düşer — ikisi de ölçüldü) ve **ASCII `^[\x21-\x7E]+$`, 8–72** (HTTP başlığı non-ASCII/boşluk taşıyamaz → fabrika kilitlenirdi; bcrypt 72 BAYT'tan sonrasını sessizce kırpar); `.trim()` YERİNE RED. `requireSettingsPassword` ASYNC ve `flagWriteGuard`'dan SONRA: süperadmin muaf · belge-only gövde muaf · hash yok → UYUR (Adnan sıfır fark) · kilit `bcrypt.compare`'den ÖNCE. **Kapsam ELLE SAYILMAZ** — `src/routes/**` AST tripwire (ayar YAZAN uç → kapı; özyineli; dolaylı yazıcılar ayrı karar kaydı). Kilit kovası girişten AYRI ve ayrım KEY'de (`sp:` ön eki) — `resolveLoginLockoutKeys` varsayılan `ip` kapsamında kimliği DÜŞÜRÜR ve 5 yanlış ayar şifresi tüm IP'nin LOGIN'ini kilitliyordu. Audit yolu `${req.baseUrl}${req.path}` (salt `req.path` mount'a göreli → `"/"`); LOCKED kilit BAŞINA tek satır (`justLocked`, login de hizalandı); `Retry-After` tek noktadan; oturum **jti ↔ userId** bağı (401). Yönetim ucu süperadmin-only, aksi **404**. Migration/izin/APK YOK.
- **2026-09-03 — Kalite = istasyon YETENEĞİ (P4 Faz A):** "bu adım KK yürütür mü" sorusunun tek cevabı `helpers/quality-station.helper.ts` ve **BOĞAZ İKİZ**: saf yüklem `stepCanApplyQuality` (11 nokta) + Prisma parçası `QUALITY_STATION_WHERE` (13 nokta) — saf yüklem where'e GİREMEZ (atomik claim'i check-then-act'e çevirirdi). `Station.appliesQuality` + backfill; Faz A köprüsü `kind===PROCESS_QC || appliesQuality` **backfill'e güvenmez** (seed migration'dan SONRA boş tabloda koşar — 2026-08-10 tuzağı; seed'e de açık `true`). `assertWoAtStepKind(PROCESS_QC)` DOKUNULMAZ (roll-step.helper TAMBUR ile paylaşılıyor — Faz B). `QUALITY_STATION_WHERE` `satisfies` ile ve spread'e ikinci `OR:` eklenmez. Bekçi `test_station_quality_capability`: ikizin CANLI eşdeğerliği + SAYIM bazlı muaf/taban (dosya bazlı ilk yazım KÖRDÜ, sonda ölçtü).
- **2026-09-03 — Tamlık bekçisi + profiller (P6):** `ScreenEntry.modul` ZORUNLU (93/93; `ModulKey` · `cekirdek:*` ön ekli · `planlanan:*`) — AİDİYET beyanı, görünürlük kuralı DEĞİL. Profiller **TS sabiti** (`constants/module-profiles.ts`): plandaki `deploy/profiller/*.json` pakete HİÇ girmiyor (ölçüldü) → job sessiz no-op'a düşerdi. Davranış bayrakları profile GİRMEZ (seed onları `update` ile ezer — iki yazar olmaz). Job `TEKSERP_PROFIL` yoksa HİÇ yazmaz (yanlış profili kalıcı damgalardı); `createMany skipDuplicates`; bağımlılık/audit/K7 job'da YENİDEN kurulur (`setFeatureFlags` `userId` ister). Profil uygula için yazma ucu YOK — `PATCH /feature-flags` tek kapı.
- **2026-09-03 — Panel modül kapıları + Sistem Profili (P5):** Ayar kategorilerinde modül = **KİLİT** (`moduleKey`, `superadminOnly` ikizi), `regime` (GİZLEME) DEĞİL — `regime` açılsaydı §14 kırmızı (okuyucular çekirdek route'lardan ulaşılabilir). ⚠️ Kilit İSTEMCİ-TARAFLI: bant "dondu" der, API yazar (§3.6 Dilim 2'de) — bant "etkisiz" DEMEZ. Süperadmin kapısı TEK KAYNAK `lib/superadmin-gate.ts` (üç kopya ayrışsa supap unutulur, ekran hiç açılmaz). Karo döngüsü DIŞINDAKİ palet girdileri kapıyı ELLE taşır (karosuz ekranda palet TEK keşif yolu). "Kapatırsan gizlenir" MASAÜSTÜ ile TABLETİ AYRI sayar — tableti yutmak satıcıya kapatma bedelini gizliyordu. Manifesto okunamazsa liste ÇİZİLMEZ (boş liste yalanı).
- **2026-09-03 — Süperadmin doğuşu P8 (`.env` YOLU KALDIRILDI, tek yol interaktif script):** Hesabın TEK doğuş/rotasyon yolu sunucuda elle koşulan `npm run superadmin:kur` (`--rotate`) — iki doğuş yolu = iki sır yüzeyi; `.env` yolunda hash+PIN+TOTP sırrı diskte KALICI duruyordu (yedeğe, `kur.ps1`in taşıdığı dosyaya, ekran paylaşımına giriyordu) ve "FORCE_SYNC satırını sonra kaldırın" gibi unutulabilir bir adım istiyordu. Boot job'ı artık hesap YARATMAZ; kalan iki işi kilit defteri tazeleme + yaşam döngüsü audit'i (`SUPERADMIN_PROVISIONED`/`SUPERADMIN_ROTATED` — audit çağrısı BİLEREK `src/` altında: `test_audit_labels` §3 yalnız `src/`i tarar). Katmanlar AYRI: saf `provisionSuperadmin(input, deps)` HİÇBİR ŞEY YAZDIRMAZ (bekçi onu doğrudan çağırır, sır iki kez basılmasın), sırlar yalnız DÖNÜŞ değerinde yaşar. İDEMPOTENT (hesap varsa DOKUNMAZ — "bir daha çalıştırayım" refleksi satıcının parolasını öldürürdü) · **MEVCUT KULLANICI YÜKSELTİLMEZ** (audit maskesi SATIR düzeyinde — yükseltme fabrikanın kendi geçmişini "Sistem Bakımı" adına geçirirdi; büyük/küçük harf duyarsız bakılır, şema-dışı `users_username_lower_uq`). ⚠️ **C1 — GERÇEK TTY ZORUNLU ve kapı FAIL-LOUD:** `readline` TTY'siz girdide ilk sorudan sonra stream'i tüketip `end`e düşüyor → süreç **hata vermeden, zaman aşımına düşmeden SONSUZA KADAR** bekliyordu (ölçüldü: boruda 120 sn donma, DB'ye tek satır yazılmadı, havuz açık kaldı); `ssh sunucu '…'` (`-t` yok) / `docker exec` (`-it` yok) / pm2 / CI ile koşan kurulum SESSİZCE hesapsız kalıyordu ve hesabın TEK doğuş yolu bu script olduğu için kimse fark etmiyordu. Kapı `interaktif()`in ilk işi (yalnız `--help` önünde değil — yardım metni boruya basılabilmeli). ⚠️ **C2 — `.env`de KALAN `SUPERADMIN_*` satırları ölü ama CANLI SIR:** boot'ta uyarı basılır ve **YALNIZ ANAHTAR ADI** (değer ASLA — pm2 log'u fabrika sunucusunda okunabilir); job dosyası tam anahtar adı literali taşıyamaz ve değeri alan adıyla okuyamaz (bekçi §6 tam bu iki deseni arar) → tarama `Object.keys(process.env)` + ön ek. Bekçi `test_superadmin_provision` (§7 non-TTY sondası zaman aşımlıdır: kapı düşerse ölçüm DONMAZ, ❌ basar; §8 uyarıyı sentinel değerle ölçer + "ortam temizken uyarı YOK" körlük zemini). Migration/izin/APK YOK; sıfır fark (fabrikada `SUPERADMIN_*` zaten yoktu).
- **2026-07-13 — Her rota final üretir:** Son adım finalize eder (`finalizeRollsAtLastStep`) — Tambur özel değil; top⟺Tambur, açık kumaş⟺diğer istasyonlar; `Roll.form` otomatik; `qualityGrade` NULLABLE (yalnız kalite istasyonları belirler); WO depo topunu da tüketir; "Durum Düzelt"/"recover-to-production" KALKTI → IN_PRODUCTION-stuck "Kurtar" (`roll:manual-adjust`).

## Domain Kuralları

- **Phase 1:** COM port / donanım entegrasyonları sadece simüle edilir — gerçek donanım kodu yazma.
    - ⚠️ Kural duruyor; ölçek notu: gerçek makine verisi geldiğinde de backend'e seri port/polling GİRMEZ — fabrika LAN'ında **ayrı bir toplayıcı ajan** okur ve API'den basar (tablet gibi bir istemci), tek-process invariantı korunur. Gerçek sürücü ilk dokuma müşterisinin sahasında pilotla gelir. bkz. MODUL-BAYRAK-TASARIM §8 (`tezgah.enabled`).
- **⚠️ PRODUCTION CANLI (gerçek fabrika, gerçek veri).** Şema/migration/veri işlerinde **gerçek veriyi koru**: `migrate reset` / reseed / toplu `DELETE` **yasak**; migration'lar geri-alınamaz kabul edilir (rollback = yedekten restore). Toplu veri düzeltmesi yapan script **dry-run varsayılan** olur ve `--apply` öncesi etkilenecek her kaydı somut listeler.
    - ⚠️ Ölçek notu: "production" artık **tekil değildir** — kural kurulum sayısından bağımsız olarak HER canlı kurulum için geçerlidir. Şema/migration işinde ölçüt "bizim fabrika" değil *en eski canlı kurulumun verisi*dir; prova o kurulumun kendi dump'ı üstünde yapılır (kabul testi: dump restore → `migrate deploy` → bekçiler → profil boot). bkz. MODUL-BAYRAK-TASARIM §0.
- **Şema tasarımında** yine uzun vadeli doğruluk için optimize et — **seed satırlarıyla** backwards compat derdine girme (seed yalnız ilk kurulumda koşar). Bu, canlı veriye dokunma izni DEĞİLDİR: ayrım "seed fixture'ı serbestçe değişir" ile "fabrikanın topları/siparişleri korunur" arasındadır.
- **İş emri esnekliği:** Bir iş emri birden fazla siparişe bağlanabilir veya hiçbir siparişe bağlı olmadan stok için üretilebilir.
- **WO kapsamı = sadece üretim:** `WorkOrder` yalnızca üretimi (istasyonlar, kurşun/QC2, tambur) yönetir. Tartı / paket / sevkiyat ayrı bir domain'dir (`shipping.service.ts`) — WO'ya değil, depoya/çuvala bağlanır.
- **WO kapatma = kapanış dispozisyonu (2026-07-30):** Manuel kapatma artık "işlemde top var" diye **reddetmez** — sektör pratiği (SAP TECO sonrası WIP dispozisyonu): kapatan kişi istasyonda kalan **her top için karar verir** ve karar sebebiyle kayda geçer. Altı karar: `STOCK` (ham stok) · `WAREHOUSE` (bitmiş depo) · `A1_STOCK` (2. kalite) · `SCRAP` (fire — mal vardı) · `CANCELLED` (hatalı kayıt — mal yoktu) · `TRANSFER` (yeni WO klonuna devret; kaynak WO `COMPLETED` kalır, `SUPERSEDED` OLMAZ). Kalite yalnız `WAREHOUSE`/`A1_STOCK`'ta ve **opsiyoneldir**; satılabilir statüde barkodsuz top barkod alır; `Roll.form`'a dokunulmaz. **Hard-block yalnız FASON** (`AT_SUBCONTRACTOR`/`RETURNED_FROM_SUBCONTRACTOR` ya da açık fason sevkine bağlı top) — mal fiziksel olarak dışarıda; ayrıca fason dönüşü top `STOCK`'a çekilemez. Kapsam **birebir** doğrulanır (tx içinde taze in-flight küme ≠ gönderilen liste → 400) ve dispozisyon varsa `workorder:write` **+ `roll:manual-adjust`** aranır. İz: movement `notes='WO_CLOSE_<ACTION>'` + audit `event='WO_CLOSE_DISPOSITION'` (`RollOperationType`'a yeni değer EKLENMEDİ). Tekil `rescueStuckRoll` ("Kurtar") davranışı değişmedi — hâlâ `WAREHOUSE`'a çeker.
- **"Buraya geliş" ≠ "oluşturma" (2026-07-30, saha bulgusu):** Envanter sekmelerinde topun o statüye GELİŞ anı `createdAt` DEĞİLDİR — kurtarma, kapanış dispozisyonu, fason kabulü ve finalize haftalar önce yaratılmış bir topu bugün depoya alır. `createdAt` sıralı + sayfalı listede o top binlerce satırın altına düşer ve operatör **"depoya gitmedi" sanır** (gerçek olay: 700/1200/800 m toplar 5280 satırlık Bitmiş Depo listesinin 5271-5279. sırasındaydı; statüleri doğruydu). Bu yüzden **Ham Stok DIŞINDAKİ tüm sekmeler `updatedAt desc` sıralar** (`rollTabDefaultSortBy`; Ham Stok'ta oluşturma = KK1 girişi olduğu için `createdAt` doğrudur). Destek index: `Roll @@index([status, updatedAt])` (migration `20260730170000`; ölçüm 205ms → 21ms). Listede **TEK tarih kolonu** görünür ve sıralanan kolonun aynısıdır (envanter listesi standardı): Ham Stok'ta "Giriş" (`createdAt`), diğer sekmelerde "Son İşlem" (`updatedAt`) — ikisi birlikte gösterilmez, operatörü karıştırır; diğeri "Sütunlar" menüsünden açılır ve detay panelinde zaten yazılıdır. **`updatedAt` gerçek "hareket" DEĞİL** (etiket yeniden basımı / not düzenlemesi de günceller) — bu yüzden kolon adı "Son Hareket" değil **"Son İşlem"**. Gerçek stok yaşlandırma / FIFO ("en eski topu önce sevk et") istenirse yalnız statü geçişlerinde damgalanan ayrı bir kolon gerekir; `updatedAt` bunun yerine kullanılmamalı. **Aynı ilke metrajda da geçerli:** liste **yalnız tek sayı** basar — topun ŞU ANKİ metrajı. Giriş metrajı da "kesik/eksilmiş" işareti de listede **GÖSTERİLMEZ** (2026-07-30 saha geri bildirimi: eski `700 / 800` gösterimi "hangisi elimde, 800 hedef mi?" diye okunuyordu; işaretli varyantı da karıştırdı). Giriş metrajı + doluluk yalnız **detay panelinde** ("Başlangıç: N m"). Kural: liste yüzeyi = operatörün anlık kararı için tek okunur değer; izlenebilirlik verisi detay yüzeyinde. Yeni bir "topu şu statüye çek" yolu eklerken sıralama sözleşmesini hatırla — statüyü doğru yazmak yetmez, operatörün onu BULABİLMESİ gerekir.
- **Top düzeltme = TEK sözleşme (2026-07-30):** Eskiden üç buton vardı ("Etiket Bas/Önizle", "Yeniden Etiketle/Düzenle", "Manuel Düzelt") ve son ikisi **aynı** `applyManualProperties` motorunu iki farklı kapsamla çağırıyordu; kapsamı belirleyen şey **`Boolean(reason)`** idi. Bu iki tuhaflık üretiyordu: süpervizör istasyondaki topun rengini düzeltebiliyor ama **metrajını** düzeltemiyordu, ve kapsamı genişleten şey yetki değil "sebep alanının dolu olması"ydı (koruma örtük — yalnız relabel Zod şemasının `reason`'ı elemesi sayesinde sömürülemezdi). **Yeni kurgu:** kapsam **topun DURUMUNDAN** çözülür — `ALWAYS_BLOCKED` (fason/kartela/emekli/sevk) her yolla red; `FREE_STOCK` (`STOCK`/`WAREHOUSE`/`A1_STOCK`) sebep opsiyonel + ek yetki yok (depo/mobil bozulmaz); gerisi (örn. `IN_PRODUCTION`) **sebep ZORUNLU + `roll:manual-adjust`** (F221 deseni: `opts.permissions` verilmezse enforcement atlanır — dahili çağrı). Audit ayrımı korunur (`reason` → `MANUAL_ATTRIBUTE`, yoksa `RELABEL`; tablo `ROLL_MANUAL_OVERRIDE`). **Arayüz iki butona indi:** *veriyi mi değiştiriyorum, kâğıt mı basıyorum* çizgisi — **"Düzelt"** (renk/metraj/kalite/en/özellik/kartelalık + koşullu sebep; `RollEditDialog`, **rollId** ile açılır ki barkodsuz açık kumaş da düzeltilebilsin → `GET /api/rolls/:id/relabel-context`) ve **"Etiket"** (önizle + bas + **farklı müşteri için bas**; `PrintForCustomerCard` buraya taşındı). `RelabelDialog` ve `ManualAttributesDialog` **silindi**. Referans test: `scripts/test_roll_edit_unified.ts`.
- **Manuel taşıma = saha sürekliliği (2026-07-30):** "Konumu Düzelt" topu `IN_PRODUCTION` + `currentStepId=hedef` bırakır, hedef adımı `ACTIVE` yapar, `COMPLETED` WO'yu `IN_PROGRESS`'e diriltip kartı yeniden aktifleştirir — böylece saha personeli kaldığı yerden devam eder. **Fason adımına taşıma `AT_SUBCONTRACTOR` YAPMAZ:** mal içeride üretimde bekler, çıkış ayrıca **Fason Sevk** ile yapılır (kabul doğrudan yapılamaz; önizleme bunu uyarı ile söyler). Fason dönüşü top (`entrySource=SUBCONTRACTOR_RETURN`) geri boyahaneye alınıp **yeniden sevk+kabul edilebilir** — ek engel yok. Geri taşımada hedef-sonrası kalite/kurşun kararları VOID olur (grade → Belirsiz), hayalet movement'lar silinir, `SKIPPED` adımlar `PENDING`'e açılır. **`CANCELLED`/`SUPERSEDED` WO'da taşıma REDDEDİLİR** (409 + önizlemede `woBlocked`): iptalde kartlar `VOIDED` olduğu ve `ensureWorkOrderInProgress` yalnız `PLANNED`'ı dirilttiği için taşınan top "canlı ama kimsenin okutamadığı" çıkmaza düşerdi (kart okutma + Tambur finalize guard'ları reddeder). Doğru yol: topu yeni bir iş emrine bağlamak. Tek kaynak: `workorder-manual-move.service.manualMoveWoBlockReason`.
- **Renk istasyon bazlı KISIT DEĞİL (2026-08-02):** Boyahane fiziksel olarak her rengi boyar — renk bir reçetedir, makine kısıtı değil. Rota adımının renk seçicisi **tüm aktif renk kataloğunu** gösterir; yeni tanımlanan renk hiçbir yere işaretlenmeden anında kullanılabilir. `StationColor` tablosu **deprecated** (satırları duruyor, okuyan kod yok). Sebep: kısıt yalnız panelde yaşıyordu — backend zaten bu tabloya hiç bakmıyordu (WO doğrulaması `Item.allowedColors` + `SubcontractorCategory.appliesColor`, fason kabul `wo.targetColorId`) — ve yeni renk hiçbir istasyonun listesinde olmadığı için rota adımında GÖRÜNMÜYORDU (sahada 58 aktif renkten 8'i tam bu durumdaydı). Yan düzeltme: **renk kilidi** artık `requiredCategory.appliesColor` ile çözülür; eski `StationColor` kaynağıyla, hedef renk hiçbir listede değilse boya adımı tamamlansa bile renk kilitlenmiyordu. "Renk veren adım var mı" sorusunun **tek kaynağı `appliesColor`** — buraya ikinci bir renk filtresi ekleme. `setCapabilities` renk tarafına yalnız `colorIds` **açıkça** gönderilirse dokunur (`[]` hepsini siler, alan yokken dokunmaz). Bekçiler: `scripts/test_helpers.ts` (kilit) + `scripts/test_station_capability.ts` (8b). **Özellik (`StationProperty`) bu kararın DIŞINDA** — orada istasyon yeteneği gerçek bir kısıt ve ayrıca "buradan geçen top bunu otomatik kazanır" işini de görür (`copyStationCapabilitiesToRoll`).
- **Özellik istasyon kısıtı KALIR, ama BOŞ DOĞAMAZ (2026-08-02):** Renkten farklı olarak `StationProperty` gerçek bir proses kısıtıdır — boyahane zımpara yapamaz, o yüzden kaldırılmadı. Kaldırılan şey kısıtın **boş doğması**: yeni `FabricProperty` artık **`stationIds` ZORUNLU** olarak yaratılır ve bağ aynı insert'te kurulur (`nestedCreateFields`) — iki ayrı yazım olsaydı ikincisi patlayınca tam da önlenmek istenen bağsız kayıt kalırdı. Sebep saha vakası: `ZIMPARALI` 16 Tem'de tanımlandı, hiçbir istasyonun listesine girmedi, iki hafta boyunca **0 iş emri / 0 top** — çünkü panelde özellik seçmenin TEK yolu rota adımındaki istasyon chip'leri (zengin `TargetPropertyPicker` hiçbir yerden import edilmiyordu, **silindi**). `update`'te `stationIds` verilirse replace, verilmezse bağa DOKUNULMAZ (ad düzenlemesi bağı silmesin); boş dizi her iki yolda da 400. **`create`/`replace` asimetrisi kapatıldı** — özellik-başına rota kapsaması eskiden yalnız edit'te vardı, yani API'den ZIMPARALI hedefli WO **açılabiliyor** ama aynı WO'ya sonradan **eklenemiyordu** (409). Kategori kontrolü ("özellik veren adım var mı") ile bu kontrol ("ŞU özelliği veren adım var mı") farklı sorulardır; ikisi de koşar. Rota adımındaki "Yeni özellik tanımla" istasyonu bağlamdan bilir → özellik bağlı doğar ve anında seçili gelir. Ayrıca "Bu istasyon renk/özellik uygulamaz" yalanı ayrıştırıldı: *uygulayamaz* ile *yetenek listesi boş* artık ayrı cümleler. **`StationProperty` İKİ İŞ yapar** (planlama filtresi + `copyStationCapabilitiesToRoll` ile otomatik uygulama) — ayrımı `schema.prisma`'daki nota bakmadan bu tabloya satır ekleme. Bekçi: `scripts/test_property_station_binding.ts` (negatif sondayla kırmızı verdiği doğrulandı).
- **Belge tasarımı ayrı bir yetkidir, sistem yönetimi DEĞİL (2026-08-05):** Tanımlar → Çıktılar altındaki dört ekran (**Belge Şablonları · Refakat Kartı · Refakat Kartı Şablonları · Serbest Belgeler**) `admin:settings`ten ayrıldı → yeni çift **`document-template:read` / `document-template:write`** (kategori **web**, yani `hasAdminAccess` saymaz: taşıyan kişi "Yönetim" menüsünü ve Sistem hub'ını GÖRMEZ). Gerekçe `settings:workstation` ile aynı: bu ekranlar baskı ÇIKTISININ görünümünü belirler; şablonu düzenleyen büro personeline oturum politikasını, yedek saatini, cihaz onayını ve log arşivini açmak zorunda kalmamak için. **Etiketler kartı bu kapsamın DIŞINDA** — o zaten `label-template:read/write` taşıyor (kartın kendisi hâlâ `station:read` ile süzülüyor; bilinen hiza sorunu).
    - **`admin:settings` DÖRT EKRANI DA AÇMAYA DEVAM EDER** ve kümelerden düşürülmemeli: boot uzlaştırması yeni izin satırını DB'ye getirir ama **kimseye ATAMAZ** (*katalog koda, atama panele*). Sıkı ayrım, deploy anında admin dahil herkesi dışarıda bırakırdı. Kümeler tek kaynakta: `Teks-Erp/src/constants/document-design.ts`.
    - **READ kümesi WRITE kodunu da içerir** — yazabilen okuyabilir. `label-template:read/write` çiftinde bu yapılmamıştı ve orada gerçek bir tuzak var: panelden yalnız "düzenleme" kutusunu işaretleyen admin, ekranı **hiç açamayan** bir kullanıcı üretir ve sebebi hiçbir yerde yazmaz. Salt-okuma gerçek: ekran + canlı önizleme çalışır, Kaydet kapalıdır, stüdyoda yazma aksiyonları **çizilmez** (gri buton olmayan bir yolu vaat eder).
    - ⚠️ **`PATCH /api/feature-flags` guard'ı ANAHTAR-KAPSAMLIDIR — düz OR'a çevirme.** O uç sistemin TÜM ayarlarını taşır (oturum ömrü, yedek saati, kk1 tuzağı…) ama Belge Şablonları + Refakat Kartı da oraya yazar. Kural: gövde **yalnız** `documentsConfig`/`travelerCardConfig` taşıyorsa dar izin yeter; tek yabancı anahtar (ya da boş gövde) → `admin:settings`. FAIL-CLOSED. Küme bilerek dar: `companyName`/`companyLetterhead`/belge logosu firmanın **kimliğidir**, şablon değil — ve onları yazan ekran zaten `admin:settings` arkasında. Yeni anahtar eklerken soru "belge ekranında görünüyor mu" değil, **"yanlış girilirse etkisi belge çıktısıyla SINIRLI mı"**.
    - **Kart ile route AYNI listeyi taşımalı** (`tile-config.permissionAny` ↔ `content-routes.requireAnyPermission`) — ayrışırsa kullanıcı kartı görür, tıklar, `/forbidden`'a düşer. Electron backend'i import edemez, listeyi `Electron/src/lib/permissions.ts`te **aynalar**; bekçi ikisinin birebirliğini mekanik doğrular. Bekçi: `scripts/test_document_template_permission.ts` (81 kontrol; **üç negatif sondayla** kırmızı verdiği doğrulandı — guard düz OR'a çevrilince 7, `admin:settings` READ'ten düşünce 4, kart/route hizası bozulunca 1). ⚠️ İzin DB'ye uzlaştırmayla gelir, **kullanıcılara atama elle yapılır**.
- **Şube bazlı planlama:** `Order.branchId` opsiyonel (eski kayıtlar `null`). Yeni siparişler tek bir şubeye yönlendirilir.
- **Refakat Kartı (Traveler Card):** İş emri **açılışında** doğan barkodlu kart (2026-07-14 "kart iş emriyle doğar"; bir WO = tek kart, `TravelerCard.workOrderId @unique`, karekod = İş Emri No). Fiziksel olarak malla birlikte hareket eder ve okutulduğunda istasyon süreçlerini tetikler. Parti (Batch) yeni kart üretmez.
- **Refakat kartı = A5 varsayılan + parti bloğu CANLI (2026-08-03):** Kart **A5** basılır (`DEFAULT_TRAVELER_CARD_CONFIG.pageSize`); A4 panelden ya da baskı diyaloğundan seçilir. Sayfaya bağlı HER ölçü (yazı boyu, QR, operasyon satır yüksekliği, sütun genişlikleri) tek kaynakta: **`document-render/traveler-card.density.ts`**. Tek `transform: scale()` yerine sayısal profil, çünkü **iki farklı oran** gerekiyor — genişlikler ~0.68 (A5 yazı alanı 132mm ↔ A4 194mm, geometrik zorunluluk), yazı boyları ~0.85 (okunabilirlik tabanı; 0.68 uygulansaydı taban 6.5px olurdu). A4 sütunu eski sabitlerin **birebir aynısı**, çıktı bayt-bayt korundu.
    - ⚠️ Profil gerçeği — A5 **varsayılanı ve doluluk ölçümü** (WO max 3 adım / 1 sipariş / 2 parti → A5'te %75) bu fabrikanın verisinden çıktı. Sayfa boyutu bir kurulum ayarıdır (`DEFAULT_TRAVELER_CARD_CONFIG.pageSize` + panel + baskı başına ezme); daha çok adım/sipariş/parti taşıyan bir kurulumda varsayılan A4 seçilebilir — kod değişikliği değil profil kararı. Yoğunluk profili (`traveler-card.density.ts`) ve donmuş belge kuralı ÇEKİRDEK kalır.
    - **İki katmanın varsayılanı bilerek FARKLI, tekleştirme:** `resolveConfigPageSize` (ayar) → **A5**; `resolveFrozenPageSize` (donmuş snapshot, alan yoksa) → **A4**. Alanı taşımayan eski bir kart o gün A4 basılmış bir belgedir; varsayılan değişti diye geçmiş belgeyi yeniden ölçeklemek donmuş-belge kuralını bozar.
    - **Partiler snapshot'ta DEĞİL, baskı anında canlı çözülür** (`resolveLiveBatches`, meta ile geçer). Sebep zamanlama: kart WO **açılışında** donar, parti `attachRolls`'ta doğar — Hızlı İş Emri'nde sıra create → attach → dispatch. Snapshot'a yazılsaydı parti kartta **her zaman boş** çıkardı, yani özelliğin en çok istendiği akışta hiç işe yaramazdı. Üç süzgeç: `mergedIntoId != null` parti tarihçedir (basılmaz), sayım/metraj `K18_DEAD_STATUSES`'i dışlar, sevk yalnız iptal edilmemişlerden (en yenisi + öncekiler `(+N)`). "Sevk rakamı brüttür / donmuş belgeden okunur" kuralı **buraya uzanmaz** — o kural para/irsaliye yüzeyine aittir; kart malla gezen operasyon kâğıdıdır.
    - **`GET /traveler-cards/:id/html?pageSize=A4|A5` TEK SEFERLİKTİR** — kalıcı ayarı **ve** kartın donmuş config'ini ezer, hiçbir yere yazılmaz, yeni versiyon doğurmaz (`?rowNotes=1` ile aynı sözleşme). Meşruiyeti: sayfa boyutu **sunum** kararıdır, belgenin içeriği değil. Geçersiz değer sessizce yok sayılır — baskı yolunu yazım hatası yüzünden 400'e düşürmek sahayı kâğıtsız bırakır.
    - **Blok kapalıyken tek bayt basılmaz** (tablo da CSS de koşullu emit) — A4 parmak izi bu sayede korunuyor; `${…}`i kendi satırına koymak boş satır bırakıp parmak izini bozar. Yeni bölüm eklerken aynı deseni uygula. Bekçi: `scripts/test_traveler_card_a5_batches.ts` (7 negatif sondayla kırmızı verdiği doğrulandı).
    - **Ölçüm (gerçek fabrika verisi, 2026-08-03):** WO'ların max'ı 3 adım / 1 sipariş / 2 parti → en ağır kart A5'te **%75 doluluk**. Taşma 8 adım + 6 sipariş + 6 partide başlar (A4 de 12/10/10'da taşar) — bu yüzden Belge Şablonu önizlemesinde **sayfa-sığma göstergesi** var: ayarı yapan kişi kaydetmeden taşmayı görür.
- **Refakat kartı: PLAN CANLI, SUNUM DONMUŞ — otomatik revizyon (2026-08-05):** Saha sorusu: *"önizlemedeki bant 'basılı kopya güncel değil' diyor; ben eski hâli mi görüyorum, revize etme nerede?"* Cevap iki parçalıydı ve ikinci parçası **gerçek bir açıktı**: kartın partileri baskı anında canlı çözülüyordu (o taraf güncel), ama İÇERİK kartın doğuşunda donan snapshot'tan basılıyordu. Yani iş emri düzenlemesiyle (`update`/`replace`/`updateTargetProperties`/`updateStepPlanning`) bayatlayan kartta operatör **eski planı** basıyor, `print-event` bayrağı temizliyor ve kâğıt sessizce yanlış kalıyordu — üstelik snapshot'ı tazeleyen `reprint` ucu **hiçbir istemciden çağrılmıyordu** (arayüzde revizyon düğmesi yoktu).
    - **Sektör dayanağı:** kart kontrollü belgedir (ISO 9001 §7.5.3 — geçersiz kopyanın istemsiz kullanımı önlenmeli) ve sahaya inen kâğıt **yürürlükteki planı** göstermelidir. SAP PP karşılığı *değişiklik baskısı* (`Änderungsdruck`): sipariş basıldıktan sonra değişirse yeni baskı siparişin **güncel** hâlini basar, donmuş kopyayı değil. Bizde de artık öyle: ACTIVE kartın önizlemesi + baskısı iş emrinin güncel hâlinden üretilir, `print-event` **basılan planı kaydeder** ve içerik gerçekten değiştiyse **`version++`** (otomatik revizyon). Aynı içeriğin ikinci kopyası revizyon DEĞİLDİR (sürüm şişmez).
    - **AYRIM: içerik ≠ sunum — ama ikisi de CANLI (2026-08-06, "şablon karta donar" kuralı KALDIRILDI).** Ayrım yalnız **sürüm** hesabındadır: revizyon karşılaştırması (`planKey`) `config`/`template`'i **dışlar**, çünkü letterhead/punto düzenlemesi sayılsaydı bir sonraki baskıda sahadaki HER kart sürüm atlar ve numara "içerik değişti" anlamını yitirirdi (aynı asimetri `contentDirty` tarafında zaten var — şablon düzenlemesi kartı bayat İŞARETLEMEZ). **Ama revizyon olmayan bir şeyi dondurmanın işi yoktur:** eskiden sunum kartta donuyordu ve tasarımı değiştirme sebebi genelde *"sahada okunmuyor"* olduğu için düzeltme, tam da düzeltilmesi gereken kâğıtlara ulaşmıyordu (ölçüm 2026-08-06: 30 aktif kartın 11'i bir haftadan eski → haftalarca eski tasarımla basmaya devam ederlerdi; `reprint` ucunun da hiçbir istemcisi yoktu, yani çıkış yolu hiç yoktu). Artık **şablon + sayfa/config her baskıda güncel çözülür**; sayfa boyutu da dahildir (baskı başına A4/A5 ezmesi diyalogda duruyor). Geçersiz kartın (VOIDED/COMPLETED) **İÇERİĞİ** yine donmuş kalır — sunumu güncel gelir, çünkü o belgenin kaydı değil kâğıda nasıl çizildiğidir. ⚠️ Bu, eski `test_traveler_template` **E2** kuralının bilinçli tersidir; geri almadan önce yukarıdaki gerekçeyi çürüt.
    - **TEK KARAR NOKTASI `resolvePrintPlan`** — önizleme, baskı ve versiyon numarası ondan beslenir. Ayrıştırılırsa kâğıda "v2" basılır, DB'ye "v3" yazılır ve revizyon numarası içeriği tanımlamaz olur (kontrolün tamamı bu eşitliğe dayanıyor). Bu yüzden **önizlemedeki `v` numarası kartın mevcut sürümü değil, BU BASKININ alacağı sürümdür**; GET yan etkisizdir, önizleyip kapatan kimse bir şey yazmaz.
    - **Üç sınır, üçü de bekçide:** ① **ACTIVE olmayan kart** (VOIDED/COMPLETED/REPRINTED) hiç revize edilmez — elde olan tarihsel kopyadır, bugünkü planla tazelemek belgeyi geçmişe dönük değiştirmek olurdu; ② **iş emri okunamazsa** eldeki snapshot'a düşülür (baskı yolunu düşürmek, biraz eski kâğıt basmaktan kötüdür); ③ **`snapshot` NULL olan eski kartta sürüm ARTMAZ** — karşılaştırılacak önceki içerik yok, sadece ilk kayıt oluşur.
    - ⚠️ **`buildPlan` dizileri deterministik sıralar** (`targetProperties`, `orderLinks` → `orderBy`). Prisma `orderBy`siz ilişkide satır sırasını garanti etmez; sıra oynadığında `planKey` "içerik değişti" der ve **hiç değişmemiş kart her baskıda sürüm atlar**. `planKey` ayrıca anahtar sırasından bağımsızdır — düz `JSON.stringify` aynı tuzağa düşerdi.
    - ⚠️ **`buildSnapshot` baskı yolunda ÇAĞRILMAZ** (sunumu yeniden çözer → donmuş şablon sessizce değişir). O yalnız kart doğuşu ve `reprint` içindir, yani sunumun MEŞRUEN tazelendiği iki nokta.
    - **Migration YOK, izin YOK, APK YOK** — kolon/sözleşme değişmedi. Metin güncellemeleri dışında istemci davranışı aynı; **backend + Electron aynı pencerede** gitmeli (eski panel metni "önizleme günceldir" derdi, artık doğru ama eksik anlatırdı). Bekçiler: `scripts/test_traveler_card_stale.ts` §6 (26 kontrol) + `test_traveler_template.ts` E2/E4 (30 kontrol) — **dört negatif sondayla** kırmızı verdiği doğrulandı: plan canlı okunmayınca 7, sürüm bumpı kalkınca 4, sunum baskıda taşınırsa (eski donmuş davranış) `stale` 1 + `template` 1. ⚠️ Sunum kontrolü ilk hâlinde **kördü** — fixture'da şablon satırı yok, iki yol da aynı config'i üretiyordu; kartın kayıtlı sayfa boyutu bilerek tersine çevrilerek kırılabilir hâle getirildi. Sonda yazarken `dress()` yardımcısını "base varsa onu koru" diye değiştirmek de **etkisiz sondadır** (base zaten canlı config taşır) — donmuş davranışı `stored?.config ?? …` ile ACTIVE dalında kur.
- **Refakat kartı ŞABLONU = ÜÇ KADEME (2026-08-03, Faz 2):** `TravelerCardTemplate` — `BUILTIN` (acemi: hiç dokunmaz) · `SECTIONS` (orta: Şablon Stüdyosu'nda bölüm sırası/aç-kapa) · `RAW_HTML` (uzman: kartın TÜM HTML'ini kendi yazar, yerleşik CSS yüklenmez). Çözüm zinciri **açık seçim > varsayılan şablon > sistem ayarı**; tablo boşken kart Faz 2 öncesiyle **birebir aynı** basılır (şablon oluşturmak aktif bir karardır). Tek giriş noktası `renderTravelerCard`.
    - **Şablon KARTA DONAR** (`snapshot.template`): basılmış kart, şablon sonradan düzenlense de aynı çıkar; `reprint` yeni şablonu alır. "Şablonu değiştirdim, sahadaki kartlar niye değişmedi?" sorusunun cevabı tasarım gereği **yeniden bas**tır. Şablon silmek geçmiş kartları etkilemez.
    - **FAIL-CLOSED çözüm:** seçili şablon silinmiş/pasifse baskı **404** verir, sessizce yerleşiğe SAPMAZ (çuval etiketi emsali). İstisna DEĞİL: gövdesi boş bir `RAW_HTML` şablonu yerleşiğe düşer — orada fail-closed'ın karşılığı "boş kâğıt basmak" olurdu.
    - **Uzman modu bir ŞABLON DİLİ DEĞİL, metin ikamesidir:** `{{alan}}` + `{{#liste}}…{{/liste}}`. Koşul/ifade/JS **kasıtlı olarak yok** — ihtiyaç doğarsa çözüm yeni sözdizimi değil **katalogda yeni alan**dır (`config/traveler-card-fields.ts`; değer üretimi bizde kalır). Bilinmeyen anahtar **boş basar, baskıyı durdurmaz** (vardiya bir yazım hatası yüzünden durmasın); stüdyo bunu sarı uyarıyla söyler.
    - **GÜVENLİK İKİ KATMANLI, ikisi de gerekli:** ① `sanitizeTemplateHtml` ŞABLONDAN aktif içeriği ayıklar (script/`on*`/`javascript:`/gömülü çerçeve) — **hem kayıtta hem render'da** koşar (render tarafı, elle DB düzenlemesi / geri yükleme gibi kayıt kapısını atlayan yollara karşı); ② veri değerleri gömülürken kaçırılır (yalnız sunucu-üretimi QR SVG'si ham). Aşırı kesme güvenli yöndür. Dış `<img>`/`<a>` **bilerek serbest** (kart bir belgedir, kum havuzu değil).
    - **⚠️ Baskı iframe'leri artık `sandbox`lı** (`Electron/src/lib/print.ts` + önizlemeler). Ölçüldü: sandbox'sız iframe'de gömülü script Electron renderer'ında **çalışıyordu**. `allow-same-origin allow-modals` ikisi de load-bearing (`doc.write` + `win.print()`); `allow-scripts` YOK. Kırpma.
    - **Bölüm listesi genişletilebilir:** `resolveSectionOrder` tanınmayan anahtarı atar ve **eksik bölümü SONA ekler** — yoksa bugün kaydedilmiş bir sıra, yarın eklenen bölümü sessizce yutardı. Yeni bölüm eklerken `traveler-card.sections.ts` + Electron `TravelerCardStudio/types.ts` birlikte güncellenir (Electron backend'i import edemez — mobil `permissions.ts` ile aynı durum; alan kataloğu için de aynısı geçerli).
    - **İzin AÇILMADI:** stüdyo `admin:settings` ile korunur (Belge Şablonları ile aynı kişi). Yeni izin kodu, sahada atanması unutulabilecek bir adım daha demekti (2026-08-01 kurşun bypass vakası). Ayrı "şablon tasarımcısı" rolü gerçekten doğarsa o zaman ayrılır.
    - Bekçi: `scripts/test_traveler_template.ts` (29 kontrol; 5 negatif sondayla kırmızı verdiği doğrulandı). Şema-dışı partial unique `traveler_card_templates_isDefault_key` **`WHERE isDefault=true`** olmak ZORUNDA — düz unique olsaydı sistemde toplam iki şablon tutulabilirdi; `test_db_invariants` envanterinde kayıtlı.
- **İş Emri No ≠ Parti (2026-07-13):** `WorkOrder.workOrderNumber` (İE+GGAAYY+NNNN) üretim emridir; `Batch` üretime aynı anda giren top grubudur (bir WO N parti içerir). ⚠️ Parti no'nun BİÇİMİ rejime bağlıdır ve **varsayılan biçim `P01…P99` (kısa, dönen, benzersiz DEĞİL)** — bkz. hemen aşağıdaki madde; `P+GGAAYY+SIRA` yalnız bayrak kapalıyken üretilir. Eski "dal"/`batchSplitId` kavramı kalktı. Tasarım: `docs/design/PARTI-MODELI-TASARIM.md`.
    - **PARTİ NO KISA ve DÖNEN — `P01…P99`, benzersiz DEĞİL (2026-08-05, kullanıcı kararı):** Bayrak **`batch.shortNumberEnabled`, varsayılan AÇIK**. Parti no artık tarih taşımaz: `P01`'den başlar, `P99`'a gider, sonra **`P01`'e sarar** — fabrikadaki numaralı **fiziksel parti plakası** düzeninin birebir karşılığı. Sarma **KÖRLEMESİNE**: numaranın o an başka bir canlı partide olup olmadığına BAKILMAZ (istendi ve açıkça kabul edildi; "boştaki numarayı bul" alternatifi *"99'u da doluysa ne olacak"* sorusuyla üretimi durdurabilecek bir hata yolu açardı). Bayrak **KAPALI** → aşağıdaki eski günlük kalıba düşülür, birebir.
        - ⚠️ Profil gerçeği — `P01…P99` körlemesine sarma, adnansahin'in **fiziksel parti plakası** setinin karşılığıdır ve bir bayrağa bağlıdır (`batch.shortNumberEnabled`, bugün AÇIK). Plakasız/uzun-parti çalışan bir kurulumda bayrak kapalı (günlük kalıp) profille kurulabilir. Ayar değerini **kurulum profili** yazar; koddaki default yalnız satır-yok sigortasıdır. Sarmayı mümkün kılan invariantlar (kimlik = `Batch.id`, `orderBy: batchNumber` YASAK, advisory kilit 8022) her iki rejimde de ÇEKİRDEK.
        - ⚠️ **`batches.batchNumber` üzerindeki `@unique` KALDIRILDI** (migration `20260805120000_batch_short_number`). Partinin kimliği artık **yalnız `Batch.id`**; hiçbir yerde `batchNumber` ile lookup YAPMA (bugün de yapılmıyor — tüm `findUnique/findFirst` id üzerinden). Kısıtın ikinci bir işi daha vardı ve o **sessizce kaybolurdu**: eski günlük kalıbın yarışını `@unique` + `withBarcodeRetry` (P2002 → tekrar dene) çözüyordu. Yerine `generateBatchNumberTx`'in **İLK ifadesi** olan `pg_advisory_xact_lock(8022, 1)` geçti ve **iki rejimi de** kapsıyor. Kilit sonraya alınırsa hiçbir şey kazanılmaz (TOCTOU — KK1 guard'ında birebir yaşandı); namespace KK1'in **8021**'inden ayrı tutulur, yoksa iki alt sistem birbirini sessizce serileştirir.
        - **Sayacın kaynağı saklanan bir sayı DEĞİL, veriden türetilir:** en son doğan **kısa** parti (`createdAt DESC LIMIT 1`, destek index `batches_createdAt_idx`). ⚠️ Sorgunun regex'i (`^P(0[1-9]|[1-9][0-9])$`) **load-bearing**: gevşerse en son satır olarak bir ESKİ günlük kod döner, parse `null` verir ve sayaç **her seferinde P01'e düşer** (canlı P01 dururken ikinci bir P01 doğar). `batchNumber` ile **SIRALAMA YAPILAMAZ** — numara sardığı için en büyük numara "en yeni" demek değildir (P99'dan sonra doğan P01 en yenisidir).
        - **Bedeli bilinçli:** parti no ile arama tanım gereği çok sonuç döndürür (aynı numarayı yıllar içinde onlarca parti almış olur) — bu kararın doğal sonucudur, `contains` artefaktı değil; tam-eşleşmeye çekmek düzeltmez. Gerçek çözüm arama + tarih aralığıdır, ayrı iş. **Ayarı değiştirmek MEVCUT partilerin numarasını değiştirmez** (numara doğuşta yazılır, yeniden hesaplanmaz); bayrak kapatılıp açılırsa sayaç **kaldığı yerden** devam eder (P42 → kapalı dönem → P43), çünkü kaynak en son kısa partidir. Panelde **"Son kullanılan · sıradaki"** göstergesi var (`GET /api/batches/number-state`) — körlemesine sarmada fabrikanın fiziksel plaka setiyle sistemi karşılaştırabileceği tek yüzey; "sıradaki" **ÖNİZLEMEDİR, rezervasyon değil**. **Sayaç sıfırlama düğmesi BİLİNÇLİ OLARAK YOK**: saklanan ikinci bir sayaç gerektirirdi (repo tüm sıraları veriden türetiyor) ve P01–P40 canlıyken sıfırlamak, kabul edilen sarma çakışmasından (~5-10 gün arayla) **daha kötü** bir aynı-hafta çakışma serisi üretirdi.
        - Bekçi: `scripts/test_batch_number_format.ts` (39 kontrol, iki rejimi de ölçer + canlı DB'de kısıtın gerçekten kalktığını doğrular; **dört negatif sondayla** kırmızı verdiği doğrulandı — kilit silinince 4, SQL süzgeci gevşeyince 2, sarma `%` düşünce 2, `@unique` geri gelince 1).
    - **Bayrak KAPALIYKEN — parti no DOLGUSUZDUR, tek kod istisnası (2026-08-05, kullanıcı kararı):** `P0508261`, `P05082619`, `P050826123`… Sıra gün başına 1'den başlar, zero-pad YOK, hane serbest (9999/gün tavanı da düştü). Diğer tüm kodlar (`SIP`/`İE`/`CV`/`RK`/`FS`…) 4 hane dolgulu KALIR — meşruiyet farkı: parti no **okutulmaz** (barkod/QR değil, kâğıda basılan iz), yani sabit uzunluk varsayan tarayıcı/parser yolu yok (`isDailyCode` "P" ile hiç çağrılmıyor). **Eski dolgulu kayıtlar (2026-08-05 öncesi `P0508260019`) OLDUĞU GİBİ DURUR** — geriye dönük düzeltilmez (canlı veri + o numaralar kâğıda basıldı); prefix `P`+GGAAYY sabit 7 karakter olduğu için `nextDailySeq` kuyruğu `parseInt` ile okur ve sayaç aynı gün içinde bile kaldığı yerden devam eder (`P0508260019` → `P05082620`). Migration YOK.
    - ⚠️ **`orderBy: { batchNumber }` HER İKİ REJİMDE DE YASAK — ama sebepleri FARKLI.** Günlük kalıpta dolgusuzluk yüzünden **sözlüksel sıra ≠ sayısal sıra** (`P05082610` < `P0508262`); kısa kalıpta ise numara sabit genişlikte olduğu için sözlüksel=sayısaldır ama **numara SARDIĞI için sayısal sıra da "yenilik" sırası değildir** (P99'dan sonra doğan P01 en yenisidir). Üstelik iki biçim DB'de kalıcı olarak yan yana yaşıyor. Parti listeleyen hiçbir yer `orderBy: { batchNumber }` KULLANMAZ — hepsi `createdAt` ile sıralar (refakat kartının canlı parti bloğu `resolveLiveBatches` dahil; tek ihlal oydu, düzeltildi). Yeni yüzeyde aynısını yap.
- **Kartela fason (Swatch):** Bitmiş top kartela firmasına gider, fasonda N kartela (`Swatch`) olarak döner (`KartelaDispatch`/`KartelaReceipt`, `AT_KARTELA`/`KARTELA_CONSUMED`). Üretim fasonundan ayrı, WO'suz akış. Tasarım: `docs/design/KARTELA-TASARIM.md`.
- **İdempotency (2026-07-14):** Kayıt-yaratan uçlar istemci `clientToken @unique` taşır (Roll, Order, WorkOrder) — timeout-retry'de mükerrer kayıt önlenir. İstemci token'ı **mantıksal deneme başına bir kez** üretir ve tekrar denemede AYNI token'ı gönderir (mutate çağrısı başına yeni token üretmek korumayı boşa düşürür — 2026-07-27 Tambur kesim düzeltmesi, **2026-08-03 KK1 ham giriş düzeltmesi**); sayaç-bazlı kartela düşümü için ayrı `SwatchStockReduction` olay modeli (`clientToken @unique` onda; `KartelaDispatch`'te token YOK). Durum geçişleri **atomik claim** (`updateMany WHERE {id, beklenen-durum}` + `count===0 → 409`; `findUnique→if→update` YASAK).
  - **"Mantıksal deneme"nin SINIRI var — token seri girişe YAPIŞMAZ (2026-08-03).** Sahada sunucu restart edilince operatör etiket çıkmadığı için tuşa defalarca bastı ve her basış yeni token ürettiği için tek fiziksel top N kayıt doğurdu. Düzeltme "token'ı ref'te sabitlemek" DEĞİLDİR: KK1 tek yüksek-hacimli **seri giriş** ekranıdır (offline'da 5 top arka arkaya kuyruğa girer) ve sabit tek token onları birbirine çakıştırıp **sessizce yutardı** — kopyanın aynadaki ikizi, yani eksik stok. Doğru kural: token yalnız **sonucu BELİRSİZ** bırakan hatadan sonra yapışır (ağ hatası / zaman aşımı / 5xx — timeout "yazılmadı" demek DEĞİLDİR); **kesin 4xx'te yapışmaz**, çünkü orada hiçbir şey yazılmadığı kesindir ve yapışmak "Ürün silinmiş" gibi bir hatada aynı payload'ı sonsuza dek yeniden gönderen bir *Tekrar Dene* döngüsü kurar. Tek kaynak + bekçi: `mobil/src/offline/entryAttempt.ts` (+ `.test.ts`). Retry, formu/makineyi YENİDEN OKUMAZ; düşen payload'ı birebir gönderir (otomatik modda yeniden ölçmek metrajı değiştirip gereksiz 409 üretir).
  - **İstemciye güvenmeyen ikinci hat: `kk1.duplicateGuardEnabled`** (varsayılan **KAPALI**). Açıkken 90 sn içinde aynı operatör/makineden birebir aynı ürün+metraj+en → 409 `POSSIBLE_DUPLICATE`; **engelleme değil onaylatma** (`confirmDuplicate: true` ile geçilir — tekstilde aynı partiden eşit metrajlı toplar arka arkaya meşru olarak girilir). Tuzak `createInitialEntry`'de **`opts.duplicateGuard` ile opt-in**'dir (F221 deseni) — dahili çağıranlar (`tambur-manual.service` ×2) etkilenmez. ⚠️ **Bayrağı açmadan önce sahadaki tabletler 409'u tanıyan APK'ya güncellenmiş olmalı**, yoksa ham giriş çıkışsız kalır. Bekçi: `scripts/test_kk1_duplicate_guard.ts`. Geçmiş kopyaları arayan salt-okunur araç: `scripts/find_duplicate_rolls.ts`.
- **Kurşun + QC2 = tek fiziksel istasyon (`StationKind.PROCESS_QC`):** Tek bir `WorkOrderStep` olarak modellenir. Per-roll `RollOperation` log'u `KURSUN_APPLIED` / `QC2_COMPLETED` olarak iz tutar — her top kurşun görmez.
    - ⚠️ Profil gerçeği — "kurşun ile KK aynı istasyonda" adnansahin'in TOPOLOJİSİDİR; sektörde KK ayrı istasyonda ya da başka bir istasyonla birlikte yapılır (tasarım karar #11, §5.1). ⚠️ **2026-09-03 ölçüm düzeltmesi (mercek raporu yazıldıktan SONRA ölçüldü):** `Station.appliesQuality` artık VAR (`prisma/schema.prisma:843`) ve P4 **Faz A indi** — "bu adım KK yürütür mü" sorusunun cevabı İKİZ boğazdır: bellek-içi `stepCanApplyQuality(station)` + Prisma `QUALITY_STATION_WHERE` (`src/services/helpers/quality-station.helper.ts`). Yeni bir `kind === "PROCESS_QC"` karşılaştırması YAZMA, ikizden birini kullan; Faz B (kalan tek `kursun-qc` çağrısı + `RollError` çok-istasyonluluğu) hâlâ AÇIK.
- **Hata yaşam döngüsü:** `RollError` PROCESS_QC'de (hata Tambur'da görülürse Tambur'da da) açılır; Tambur kararıyla kapanır (`isProcessed = true`, `actionTaken = CUT|NO_CUT`). Redye/parti ayırmada Tambur dışında `NO_CUT` ile idari kapanış olabilir.
    - ⚠️ "PROCESS_QC'de açılır" varsayımı kalite-yetenek dönüşümünün etki listesindedir (UYGULAMA-PLANI, Faz B · R5: hata çok-istasyonlu olunca partial unique 409 riski). Hata AÇILMA/KAPANMA semantiği (Tambur kararıyla kapanır, `isProcessed`, `CUT|NO_CUT`) ÇEKİRDEK'tir; hatanın hangi istasyonda açıldığı PROFİL'dir. Bu varsayıma yaslanan yeni yüzey yazmadan önce Faz B listesine bak.
- **Fason dönüş:** Kabulde orijinal rulolar `SUBCONTRACTOR_CONSUMED` ile emekliye ayrılır; makbuz (receipt) üzerinden `parentReceiptId`'li **yeni açık-kumaş `Roll`'lar doğar** (`entrySource=SUBCONTRACTOR_RETURN`, barcode null). Kabulde metraj girilir (zorunlu, ağırlık opsiyonel); kesin ölçüm sonraki istasyonun `FINISH`'inde damgalanır.
- **Roll split:** Sadece Tambur'da (`CUT` kararı) olur — `parentRollId` + yeni barkod ile çocuk roll yaratılır. Çocuklar **işlemin yapıldığı Tambur adımını** damgalar (`producedInStepId` = Tambur step; 2026-07-27 — parent kalıtımı değil, `cutOpenFabric` ile aynı).
- **Tambur finalize WO disiplini (2026-07-27):** Finalize WO'yu topun **`currentStep`'inden** çözer (köken `producedInStep`'ten DEĞİL — Top Kesme çocuğu `producedInStepId=null` doğar). WO kapaması yalnız terminal-guard'lı `completeWorkOrderIfStepsDone` helper'ıyla yapılır (CANCELLED/SUPERSEDED asla COMPLETED'a dirilmez); iptal/devredilmiş WO'nun adımındaki top finalize/kesim **reddedilir** (pre-tx + kilit-altı taze guard). Ölü top kümesi tek kaynak: `K18_DEAD_STATUSES` (KARTELA_CONSUMED dahil) — liste/lane filtrelerinde elle statü listesi kopyalama. Movement kapanışında `qtyOut = qtyIn` (istasyona giren işlenmiş metraj; finalize öncesi kesimler hacimden düşmez). WO **üretim çıktısı** kümesi tek kaynak: `workorder.service.producedOutputWhere` (Tambur birinci-nesil çocukları + Tambur'suz finalize çıktıları) — liste ÇIKAN metriği ve detay `producedRolls` aynı kümeyi kullanır, elle kopyalama.

## Sürüm Yayınlama (sahaya güncelleme çıkarma)

Panel ve tabletler güncellemeyi **internetten** (`guncelleme.etkiliyazilim.com`)
kendileri indirir. Elden setup/APK taşıma YOK — reçeteler:
[`docs/ops/ELECTRON-OTOMATIK-GUNCELLEME.md`](docs/ops/ELECTRON-OTOMATIK-GUNCELLEME.md) ·
[`docs/ops/MOBIL-UZAKTAN-GUNCELLEME.md`](docs/ops/MOBIL-UZAKTAN-GUNCELLEME.md)

```bash
# 0) NOTU YAZ — her yayın turunda ŞART (kapı: not yoksa paketleme DURUR)
#    ⚠️ TASLAĞI CLAUDE YAZAR, KULLANICI ONAYLAR. Sürüm çıkarma isteği geldiğinde
#    önce o turda ne yapıldığını operatör diline çevirip kullanıcıya SUN; onay
#    almadan paketleme adımına geçme. Notu okuyacak olan fabrika çalışanıdır,
#    doğruluğunu ancak kullanıcı teyit edebilir.
#    surum-notlari.json'a kayıt ekle → operatör diliyle, kapsam etiketli
node scripts/surum-notlari-kopyala.mjs      # panel + tablet paketlerine kopyala
node scripts/check-surum-notlari.mjs        # şema + dil + kopya denetimi

# MASAÜSTÜ PANEL — sürüm OTOMATİK artar (yama hanesi), müşteriyi BELİRT
./deploy/electron-paketle.sh <müşteri>           # son git etiketinden 1.1.0 → 1.1.1
./deploy/electron-paketle.sh <müşteri> 1.2.0     # küçük/büyük hane = KARAR, elle
./deploy/electron-yayinla.sh                     # hedefi paketin kimliğinden çözer
./deploy/electron-yayinla.sh --dogrula           # yükleme YOK, yayını denetle

# TABLET — değişikliğin cinsi kanalı belirler
cd mobil && npm run yayinla -- --musteri=<müşteri>   # JS-only → OTA; sürüm de otomatik artar
cd mobil && npm run yayinla -- --musteri=<müşteri> --surum=1.2.0   # haneyi elle ver
cd mobil && npm run build:apk                        # native değişti → yeni APK (elle kurulur)
node deploy/mobil-yayinla.mjs --apk=<yol> --surum=X --vc=N --musteri=<müşteri>
```

**Beş kural — hepsinin bedeli ölçüldü:**

0. **Sürüm notu yazılmadan sürüm çıkmaz.** Paketleme kapısı durdurur. Notlar
   `surum-notlari.json`'da (tek kaynak, operatör dili, `panel`/`tablet`/`her-ikisi`
   etiketli); panele ve tablete GÖMÜLÜR, güncelleme sonrası ilk açılışta bir kez
   gösterilir. Reçete: `docs/ops/SURUM-NOTLARI.md`.

1. **Sürüm numarası ARTIRILMAZSA hiçbir şey güncellenmez.** Panelde
   `Electron/package.json > version`, tablette `versionCode`. Dosyayı yüklesen
   bile istemci "en güncelim" der. **Yama hanesini artık script artırıyor**
   (2026-09-02) — OTA turları dahil; ayrıntı 5. kural.
2. **Ham `npm run build:win` KULLANMA** — bir önceki müşterinin adresiyle
   derler. Yayın adresi pakete **derleme anında** gömülür; yanlış müşteri kodu
   taşıyan paket **başka bir fabrikanın güncellemesini indirip kurar** ve hata
   SESSİZDİR (dosyalar kendi aralarında tutarlı kalır). Paketleme script'i
   derlemeden SONRA paketin içindeki adresi argümanla kıyaslar — kapı orada.
3. **Manifest EN SON yüklenir** (`latest.yml` / OTA manifest). Ters sırada,
   henüz yüklenmemiş dosyayı işaret eden bir manifest yayında kalır.
4. **Cloudflare proxy'si (turuncu bulut) AÇIK kalmalı** — sertifika Origin CA,
   ona yalnız CF Edge güvenir; DNS-only'ye çevrilirse güncelleme sessizce durur.
5. **Yama hanesi OTOMATİK; taban GİT ETİKETİ, doğrulayan YAYIN SUNUCUSU**
   (`scripts/lib/surum.mjs`, etiketler `panel-v*` / `tablet-v*`).
   Uzaktan güncelleme eskiden numaraya hiç dokunmuyordu: tablette haftalarca
   "1.0.0" yazarken içindeki JS bambaşkaydı ve sürüm notları tek numaraya
   yığılıyordu. Artık her OTA turu da bir yama numarası alır — numara APK'dan
   değil PAKETTEN okunuyor (`Constants.expoConfig.version` → manifestin
   `extra.expoClient`i), yani native'e dokunmadan değişir.
   ⚠️ **Taban neden ETİKET:** numara KODA aittir, kanala değil. Sunucudan
   okunsaydı her müşteri kendi sayısını üretir ve iki farklı kod aynı numarayı
   taşıyabilirdi ("panel 1.1.1'de şu hata var" → hangi 1.1.1?). Yerel dosyadan
   okunsaydı komutun her koşumu numarayı atlatırdı (yayın komutu bir turda
   birden çok kez koşar: not kapısı kırmızı, derleme düşer).
   ⚠️ **Etiketi script atar**, yayın BİTTİKTEN sonra — "şu commit'ten sonrası
   yeni sürüm" diye bir karar verilmez. HEAD etiketin üstündeyken numara
   KORUNUR; bu hem tekrar koşumu hem de *aynı turda ikinci müşteri* için ayrı
   derlemeyi kapsar (yayın adresi pakete derleme anında gömüldüğü için her
   müşteri ayrı derleme ister, ama ikisi de AYNI kodun yayınıdır).
   ⚠️ **Etiket defteri bayatlayabilir** (başka makineden yayın, etiket
   itilmedi, depo yeniden klonlandı) → hesaplanan numara yayındakiyle
   kıyaslanır; eşit ya da geride ise DURULUR. Yayın okunamazsa sessiz geçilir
   (doğrulama, kapı değil — internetsiz paketleme mümkün kalmalı).
   ⚠️ **Küçük/büyük hane elle** verilir; o bir karardır.
   ⚠️ **OTA turunda `android.versionCode`a DOKUNULMAZ** — paket onu da taşır ve
   tablet KURULU APK sürümü sanar (`kuruluVersionCode()`); yükseltilirse gerçek
   APK güncellemesini bir daha teklif etmez. Yayın script'i yayındaki
   `apk/surum.json` ile kıyaslayıp durdurur. Bekçi: `scripts/test_surum.mjs`.

⚠️ **Yeni müşteri:** sunucuda `mkdir <müşteri>/electron` + paketle + yayınla.
DNS, sertifika, servis GEREKMEZ. Müşteri kodu tek kaynakta
(`Electron/shared/musteri.json`, `mobil/musteri.json`).

⚠️ **Sürüm politikası ayrı eksendir:** backend "en az şu sürümü bekliyorum" der
(`Teks-Erp/src/config/client-version-policy.ts`, uç `/api/client-policy/:istemci`).
`minVersion`ı YALNIZ gerçek bir sözleşme kırılmasında yükselt ve **sahadaki
sürümden büyük yapma** — o durumda en güncel istemci bile kapıda kalır ve
indirecek bir şey olmadığı için çıkamaz. Bekçi: `scripts/test_client_policy.ts`.

## Ortak Konvansiyonlar

> Bu bölümün tamamı **[ÇEKİRDEK]**'tir — bayrakla açılıp kapanmaz, profile göre değişmez (UUID/zaman damgası, soft delete, audit, beş sağlamlık sınıfı, yıkıcı işlemde detaylı onay). "Bizde böyle olmasın" talebinin cevabı bayrak değil süreç/eğitimdir; red gerekçesi: MODUL-BAYRAK-TASARIM §11 (defter semantiği · veri bütünlüğü değişmezleri · mevzuat bayraklanmaz).

- UUID primary key, tüm modellerde `createdAt`/`updatedAt` (M:N pivot ve append-only log tabloları hariç — bunlarda sadece `createdAt`).
- Sadece soft delete — `isActive: false` veya `RollStatus.CANCELLED` (`SCRAP` = gerçek fire **kararıdır**, arşivleme değil); **asla** fiziksel DELETE. Bilinçli istisnalar: bağımlılık-guard'lı master-data `DELETE /:id/permanent` uçları, boş çuval silme, cihaz unpair, pivot replace, **`ItemPrice` satırı silme** (2026-08-14, Paket D — şemada `isActive` YOK ve bilinçli: pasif bir fiyat satırı `resolveItemPrice`'ın "müşteri istisnası > kart varsayılanı > null" sırasına ÜÇÜNCÜ bir durum ekler ve "fiyat yok" ile "fiyat vardı, kaldırıldı" ayrımı hiçbir karara girmez; geçmiş belgeler zaten `InvoiceLine.unitPrice` / `Roll.purchasePrice` ile DONMUŞTUR, yani silme geçmişi değiştirmez).
- Her CUD operasyonu → `AuditService.log()` → `SystemLog` tablosu. (İstisna: `UserPreference` kişisel UI blob'u. Audit **best-effort**'tur — yazım hatası isteği düşürmez, `/health` sayacına düşer; çağrı tx **dışında** yapılır.)
- **BEŞ SAĞLAMLIK SINIFI (2026-08-14 — `docs/design/ON-MUHASEBE-SAGLAMLIK-TASARIM.md`; beşi de sahada/denetimde birer kez ısırdı, sınıf adıyla anılır ki altıncı kez açılmasınlar):**
    1. **İki tarih kuralı** — yeni mali belge tipi eklerken sor: *kâğıdın üzerindeki tarih = bizim işlem tarihimiz mi?* Değilse İKİ alan (SAP Belegdatum/Buchungsdatum; emsal `Cheque.issueDate` ↔ `postingDate` — defter/kilit/belge-no/kur DÖRDÜ de işlem tarihinden). Payment/Invoice tek tarihli ve **meşru** (ödeme = paranın el değiştirdiği gün; fatura = tahakkuk tarihi) — çift tarihi her tabloya yaymak reddedilmiş bir karardır.
    2. **Ters yol kuralı** — deftere yazan her ileri kaynak tipinin tipli `*_CANCEL` yolu olmalı; enum'a ileri değer eklerken ters değerini de düşün (emsal: `ADJUSTMENT` ↔ `ADJUSTMENT_CANCEL`, `reversesTxnId` self-FK bağıyla — SAP FB08). Ters kayıt DAİMA bugüne yazılır (kapanmış fotoğraf değişmez).
    3. **Kilit sırası kuralı** — tek tx'te birden çok advisory/satır kilidi = deterministik sıra (çoğul helper `assertPeriodsOpenTx` ya da kanonik anahtar sıralaması); uzaylar arası = uzay numarası artan. Envanter: `helpers/period-guard.helper.ts` başlığı. İkinci katman: PG sınıf-40 (40P01/40001) kullanıcıya 500 değil 409 "tekrar deneyin".
    4. **Çift yüklem kuralı** — durum ↔ sayaç çifti olan her modelde İKİ yazar da karşı tarafın koşulunu kendi atomik WHERE'ine koyar (emsal: `claimTx allocatedTotal=0` ↔ `bumpChequeAllocated status NOT IN terminal`) + DB CHECK seddi (`cheques_terminal_not_allocated`). Tek yönlü CAS, karşı yazarla yarışta sessiz tutarsızlık üretir.
    5. **Tek kaynak satır kuralı** — çok-tipli çocukları olan belge satırlarını TEK assembler'dan verir (emsal: `assembleReceiptLines` kind FABRIC|YARN; `_shipped.ts`, `producedOutputWhere`); tüketici tabloya doğrudan gitmez — beşinci tüketici yazıldığı gün sınıf yeniden açılır.
    - **2026-08-14 inceleme turunun iki eki:** ① **count-0 tanısı sayaç kaynaklarında SİMETRİK** — atomik `updateMany` claim'i 0 dönünce sebep tx içinde TAZE okumayla söylenir ve bu tanı `explain*BumpZeroTx` aile deseniyle ÜÇ kaynakta da aynıdır (çek · tahsilat · fatura); tek kaynağa yazıp diğerlerini bırakmak, yarışın kaybedenine "tavan aşıldı" YALANI bastırır (fatura o sırada iptal edilmişken). ② **Bekçide gate-tx promise'i `await`'ten ÖNCE reddedebilir** — henüz await edilmemiş promise'e no-op `.catch` konmazsa unhandled rejection süreci Sonuç satırı basılmadan öldürür: ❌ bile yok, en sessiz kırmızı (test_payment_allocation §12m'de iki koşumda ölçüldü). ③ **Olay defterinde kronoloji `createdAt`'tir, olay tarihi DEĞİL** — `eventDate` kullanıcı girdisidir ve geriye tarihlenebilir; "en son olay" araması `eventDate desc` ile yapılırsa storno + geriye tarihli yeniden kayıt zincirinde YANLIŞ satır bulunur (cheque cancelCollect'te canlı ölçüldü: para yanlış hesaptan geri çekiliyordu). Append-only defterde yazım sırası = gerçek kronoloji.
    - **Sınıf 4 emsal ekleri (2026-08-15 I paketi):** ① GR addLines‖cancel — satır doğuran her tx'in İLK işi kap-belgesini status şartıyla claim'lemek (`createInitialEntry.txGate` F221 deseni); iptalin top kümesi claim'den SONRA tx İÇİNDE taze okunur. ② 5 finans ucunda eşzamanlı aynı-clientToken çift gönderim PO deseniyle kapalı: `withBarcodeRetry`'a `!isClientTokenP2002` yüklemi + tx-dışı catch → findUnique → CACHED yanıt; cached yanıtın şekli normal yanıtla BİREBİR olmak zorunda (ayrışırsa replay istemciden ayırt edilir). ③ Ekstre satırı ters-kayıt bağını KENDİSİ taşır (`reversedByTxnId`) — panel yüklemleri kesin bilgiyle çalışır, sezgisel sayım yalnız eski-backend fallback'idir.
    - **Çek teslim bordrosu ANLIK çıktıdır** (`Cheques/chequeBordro.ts`): bordro GRUBUNU sahiplenen kaynak model olmadığı için donmuş `PrintedDocument` üretilmez — belge no/sürüm yok ve bu, kâğıdın üstünde de yazılıdır ("düzenlendiği andaki seçimi yansıtır"). Donmuş sürüm istenirse önce `ChequeBatch` benzeri bir kaynak modeli gerekir (J kararı). Aynı sınıf karar: bordro basmak çekin DURUMUNU değiştirmez (bankaya veriş ayrı işlemdir).
    - **Ticaret bayrakları (2026-08-14 sektör analizi):** `finance.blockNegativeCashEnabled` (varsayılan KAPALI — kasa eksiye düşecekse 4 İLERİ yol 409; BANKA ve TERS yollar/storno MUAF, muafiyet `test_cash_negative_guard`ın asıl negatif sondası; guard `FOR UPDATE` derinlik savunmasıdır, asıl serileştirici 8028 advisory kilidi) ve `finance.defaultVatRate` (varsayılan 20 — panel `emptyLine` + mal-kabulden alış taslağı TEK kaynaktan). ⚠️ `test_feature_flag_contract` artık SAYISAL anahtarları da denetler — "bekçi yalnız boolean bakar" varsayımı bayattır. Bayrak OLMAYACAKLAR listesi (defter semantiği · mevzuat · ikinci-kaynak sınıfları) yol haritasının bayrak bölümünde gerekçeli.
    - **Yarış bekçisi yazarken pencere ELLE AÇIK TUTULAN tx ile kurulur** (`Promise.allSettled` tarzı serbest yarış pencereyi bazen ıskalar ve sahte-yeşil kalır); sondanın tuttuğu kilit bilinçli `FOR NO KEY UPDATE` olabilir — `FOR UPDATE` FK KEY SHARE'i de bloklar ve sondanın kendisi kilitlenir (emsal: `test_goods_receipt_invoice` §10/§11). "Bekledi" ölçümleri (süre eşiği) yalnız pozitif yönde anlamlıdır; deterministik kırmızı SONUÇ kontrolleridir.
- Validation hata mesajları Türkçe.
- **TR-only BİLİNÇLİDİR (2026-08-15 kullanıcı kararı):** arayüz ve backend mesajları Türkçe kalır — hedef pazar Türk tekstil firmaları; i18n altyapısı KURULMAZ (sözlük katmanı sürekli bakım borcu doğurur ve bugün okuyucusu yok). Yabancı müşteri gerçekten doğarsa önce YALNIZ ticaret yüzeyleri (cari/fatura/çek/kasa/mal kabul) sınırlı sözlüğe alınır — o gün planlanır, bugünden hazırlık yapılmaz.
- **Yıkıcı işlemlerde detaylı onay zorunlu** (iptal/sil/scrap): confirm dialog'unda etkilenen her kaydı (WO, rulo, sipariş vb.) somut olarak listele. Backend tarafında preview endpoint döner, frontend per-record seçim sunar — "X kayıt etkilenecek" gibi soyut sayı yetmez.

## Test Kullanıcıları

En sık kullanılan: `admin` / `123123` (tam yetki). Seed kullanıcılar `Teks-Erp/ARCHITECTURE.md §13`'te listeli. Admin dışı tüm kullanıcılar `test123` şifresini kullanır.

> ⚠️ **Düzeltme (2026-09-03 ölçümü):** **seed yalnız `admin` üretir** — `Teks-Erp/prisma/seed.ts` (`username: "admin"`, `hashPassword("123123")`) ve çıktı satırı ("Tam yetki (tek seed kullanıcısı)"); `ARCHITECTURE.md §13` de "Seed'de YALNIZ admin var" diyor. "Admin dışı tüm kullanıcılar `test123`" cümlesi 2026-07-03'te kaldırılan eski test kullanıcılarına (mehmet.planlama, ali.operator…) aittir; yeni kullanıcı panelden (`POST /api/admin/users`) açılır, 0-izinli testler kendi geçici kullanıcısını üretir.

> ⚠️ Buraya **yalnız seed/dev kimlikleri** yazılır. Süperadmin (satıcı hesabı) parolası/PIN'i ve ayar şifresi repoya, log'a, sürüm notuna ve audit diff'ine GİRMEZ; dağıtım yalnız `.env` + parola yöneticisiyle olur (`quickPin` emsali gibi redaksiyon listesine eklenir). bkz. MODUL-BAYRAK-TASARIM §7 ve §12.8 (sır hijyeni).
