node_modules
dist
build
.DS_Store
.next
coverage


# Claude Code çalışma-zamanı kilidi (per-session artefakt; .claude/settings.json İZLENİR)
.claude/scheduled_tasks.lock
.claude/worktrees/

# Test artefaktları (token, raporlar)
test-results/
TEST_BUG_REPORT.md

# Geçici diagnostic / e2e scratch dosyaları (repo'ya girmemeli)
Teks-Erp/_e2e/
Teks-Erp/_*.mjs

# Ödev / sunum dosyaları — GitHub'a gönderilmeyecek (proje koduyla ilgisi yok)
/odev.md
/odev-cozum.md
/TeksERP-Sunum.pptx
*.pptx
/sunum-gorseller/
/.pptx-build/

# Yerel çalıştırma/derleme logları (dev artefaktı — repo'ya girmez)
*.log

# Veritabanı dökümleri — fabrika verisi, repoya GİRMEZ
*.dump
*.sql.gz

# İmza anahtarları — mühür + kod imzalama. ASLA repoya girmez.
# Kaybı telafi edilemez: mühür değişirse her tablette uygulama silinip
# yeniden kurulur. Yedeği şifresiyle birlikte repo DIŞINDA da tutulmalı.
/_anahtarlar/
