# Kod Kuralları — teknik desenler (backend · panel · tablet)

> Anlama turu 2026-09-05'te 24 karar hikâyesinin içinden ayrıştırıldı. Her madde tek cümle, emir kipi; gerekçe arşivde. Yeni desen eklerken: kural buraya, hikâye `docs/history/CLAUDE-NOT-ARSIVI.md`'ye. Alan-özgü kurallar `docs/kurallar/`, yasaklar aşağıda ve kök `CLAUDE.md`'de.


## Değişmezler (23)

- **belge** · columns[tablo].labels (kolon başlığı override'ı) KULLANICI GİRDİSİDİR, yerleşik label'ların aksine HTML olarak güvenli değil → applyColumnCfg TEK noktada kaçırır; yeni tablo motoru bu kaçırmayı taşımak zorunda. Boş dize = varsayılana dön (başlıksız kolon üretilmez), 40 karakterde kırpılır. · bekçi: `scripts/test_document_customization.ts + test_shipment_doc_customer_na` <sub>(CLAUDE.md:111)</sub>
- **deploy** · Paketin TAŞIMADIĞI yola bağımlı kod yazılmaz: paket `deploy/` ve `scripts/`yi ALMAZ, `npm ci --omit=dev` tsx'i eler. Sunucu araçları `scripts/build-araclar.mjs` ile `dist/tools/*.cjs`'e derlenir (`npm run build` = tsc + araçlar); profiller TS sabitinde, `deploy/profiller` yok. · bekçi: `deploy/paketle.ps1:148 (dist/tools kapısı) + kur.ps1:290` <sub>(CLAUDE.md:101, CLAUDE.md:56, CLAUDE.md:97)</sub>
- **filtre** · Liste sayfalamada VARSAYILAN cursor (keyset) + sonsuz kaydırma; offset yalnız açıkça istenirse ya da kalıcı küçük master-data'da. Yüksek hacimli tablolar (Roll, RollMovement, RollOperation, SystemLog, TravelerCardScan) HER ZAMAN cursor; skip>MAX_OFFSET(10000) → 400. · bekçi: `runtime: query-parser.ts:385 (skip>MAX_OFFSET → 400); test yok` <sub>(CLAUDE.md:424, CLAUDE.md:176)</sub>
- **filtre** · Kapsam zorlaması SAF katmanda (`rollHistoryFilter.forcedCreatorFilter`), ekrandaki bir `if`te DEĞİL (tersine çevrilmesi hiçbir testi kırmazdı); KK1 bunu filters'a EN SON yayar (çipten sızan createdById'yi ezer); kimlik yüklenmemişse filtre üretilmez (boş string listeyi sessizce boşaltır). · bekçi: `mobil rollHistoryFilter.test.ts` <sub>(CLAUDE.md:55)</sub>
- **finans** · Atomik `updateMany` claim'i 0 dönünce sebep tx İÇİNDE taze okumayla söylenir ve tanı sayaç kaynaklarında SİMETRİK (`explainCheque/Invoice/PaymentBumpZeroTx`); tek kaynağa yazıp diğerlerini bırakmak yarışın kaybedenine 'tavan aşıldı' YALANI bastırır. · bekçi: `test_payment_allocation.ts` <sub>(CLAUDE.md:286)</sub>
- **finans** · Satır doğuran her tx'in İLK işi kap-belgesini status şartıyla claim'lemek (`createInitialEntry.txGate` F221 deseni; GR addLines ‖ cancel); iptalin top kümesi claim'den SONRA tx İÇİNDE taze okunur. · bekçi: `test_goods_receipt_invoice.ts §10/§11` <sub>(CLAUDE.md:286)</sub>
- **is** · Tip çevrimi ATOMİK: `updateMany WHERE {id, type: eski}` + tx içinde TAZE bağ sayımı (`count===0` ise dokunma); `findUnique→if→update` yasak. Değişim sessiz olmaz: yanıt `typeChanged`, mesajda söylenir, audit `oldData.type/newData.type` (künyede 'Ne değişti' satırı). · bekçi: `test_workorder_order_link.ts (typeChanged/mesaj/audit kontrolleri; neg` <sub>(CLAUDE.md:76)</sub>
- **kalite** · Yüklemi çağıran her Prisma select `STEP_QUALITY_SELECT`'i spread eder (alan atlamasın); `STEP_CAPABILITY_SELECT` kaliteyle GENİŞLETİLMEZ — dar select kuralı: yalnız `appliesColor` okuyan çağıran `appliesProperty` seçmeye zorlanmaz. · bekçi: `test_station_quality_capability §5 (dosya başına TABAN SAYIM)` <sub>(CLAUDE.md:100, CLAUDE.md:68)</sub>
- **kk1** · Token replay'i DÖRT durumludur ve tek kaynağı `helpers/token-replay.helper.ts`: yazılmadı → yaz · aynı yük → cached dön · BAŞKA yük → 409 `CLIENT_TOKEN_COLLISION` · kayıt İPTAL/ölü → 409 `ENTRY_CANCELLED`. 'success:true + iptal edilmiş barkod' sessizce yanlış cevaptır. <sub>(CLAUDE.md:171, CLAUDE.md:53)</sub>
- **kucuk** · Kapalı modül HER ZAMAN 403 döner ve kod `details.code = "MODULE_DISABLED"` altındadır (`body.code` hep undefined okur — sahte yeşil). 404 dönmek destek ekibini 'uç deploy edilmemiş' diye yanlış yöne gönderir; uzakta bile 403 kalır. · bekçi: `test_module_flag_off §1d/§4` <sub>(CLAUDE.md:315, CLAUDE.md:95)</sub>
- **kursun** · Toplu uçları (`assign-bulk`/`cancel-bulk`, ≤100 satır) hepsi-ya-hiç tek transaction'a ÇEVİRME: her satır kendi tx'inde koşar, atlanan her satır somut sebebiyle `failed[]` içinde döner ve arayüz onu gösterir. '42 atandı' deyip 8'inin sebebini yutmak en kötü davranıştır. · bekçi: `scripts/test_kursun_bulk.ts (28 kontrol; hepsi-ya-hiç'e çevrilince kır` <sub>(CLAUDE.md:51)</sub>
- **kursun** · Kalıcı sayaç/rapor `SystemLog`'tan değil KALICI İZDEN okunur (burada movement marker'ı) — `archive-scheduler` audit'i 6 ayda taşır ve rapor sessizce sıfırlanır. Emsal: `Roll.entryReason` kolonu. <sub>(CLAUDE.md:52)</sub>
- **modul** · Kapalı modül 403 + `details.code="MODULE_DISABLED"` + `details.modul` (bağımlıda `dependent`); kod TOP-LEVEL değil — `body.code` hep undefined. Tünel ayrımı YOK: uzaktaki 404 yalnız REMOTE_DENIED listesidir, modül kapısı orada değil. · bekçi: `test_module_flag_off §1h/§4 (AST details.code + details.modul)` <sub>(CLAUDE.md:96, CLAUDE.md:315)</sub>
- **mukerrer** · Bulanık ad eşleştirmede birim KELİMEdir, karakter DEĞİL: Jaro-Winkler/trigram isim alanlarında yanıltıcıdır (ön ek bonusu 'BOYER EMRE'↔'BOYER'i 0.900 yapıyordu). Skor sıralı kelime hizalaması + profil: FIRM = gürültüsüz çift ORTALAMASI, PRODUCT = kelime/numerik eşitliği + EN DÜŞÜK çift. · bekçi: `test_duplicate_detection` <sub>(CLAUDE.md:80)</sub>
- **mukerrer** · Kimlik kuralı 'aynı değer = aynı tüzel kişi' varsayar ve bu yalnız değer AZ kayıtta geçerken güçlüdür: bir değer `IDENTITY_MAX_GROUP` (=4) kayıttan fazlasında geçiyorsa YER TUTUCU sayılır ve çift ÜRETMEZ. Bastırma SESSİZ OLAMAZ — yanıtta `suppressedIdentities` ile raporlanır. <sub>(KOD:bba838ce (2026-09-01))</sub>
- **parti** · Kısa parti no İKİ HANE DOLGULUDUR (`P01`), günlük parti no DOLGUSUZDUR (hane serbest, 9999/gün tavanı yok); diğer tüm günlük kodlar (SIP/İE/CV/RK/FS) 4 hane dolgulu KALIR — onlar okutulur, parti no okutulmaz. · bekçi: `scripts/test_batch_number_format.ts §3/§4 (dolgu + istisna sızması)` <sub>(CLAUDE.md:161)</sub>
- **sebep** · Senkron katalog okumasında TTL yalnız TAZELİĞİ ölçer, GEÇERLİLİĞİ değil: bayat liste olduğu gibi döner ve bayatlık okumayı düşürmek yerine arka planda bir tazeleme TETİKLER (çağıran beklemez). · bekçi: `test_reason_presets §3b` <sub>(CLAUDE.md:87)</sub>
- **sebep** · Bayatlık ≠ boşluk: önbellek HİÇ dolmadıysa (ya da kind boşsa) kod kataloğuna düşülür ve fabrika kodu reddedilir — elde doğrulanacak bir şey yokken FAIL-CLOSED doğrudur. · bekçi: `test_reason_presets §3d` <sub>(CLAUDE.md:87)</sub>
- **superadmin** · `router.use("/users/:id", verifyToken, requireAnyPermission("admin:users","admin:settings"), protectSystemAccountTarget)` ÖNEK kapısı SİLİNMEZ (yeni uç kapısız doğmasın); önek izin kümesi alt rotaların BİRLEŞİMİ, daraltmayı alt rotanın kendi zinciri yapar. · bekçi: `test_superadmin §M (AST: önek kümesi ⊇ her alt rota kümesi)` <sub>(CLAUDE.md:104, CLAUDE.md:97)</sub>
- **surum** · DAİRESEL KAPI YASAĞI: beklenen değeri gerçek değerle AYNI kaynaktan alan kapı o kaynağın yanlışını yakalayamaz → `--musteri` HER yayın/derleme komutunda ZORUNLU, beklenen değer argümandan (musteri.json'dan DEĞİL); kontrol beyana değil ARTEFAKTA bakar (manifest varlık URL'leri, app-update.yml). · bekçi: `her script'te argüman yoksa dur(); update-feed-url.test.ts (sınırı: üç` <sub>(CLAUDE.md:19, CLAUDE.md:128, CLAUDE.md:88)</sub>
- **tambur** · SIRA sözleşmesi: payload'ın kendi tutarlılığı (ürün/renk — `ITEM_MISMATCH`/`COLOR_MISMATCH` hard guard) ÖNCE, bağlam çözümü (parti) SONRA; ürün/renk/en operatöre SORULMAZ, iş emrinden gelir; kalite Tambur kararında. · bekçi: `scripts/test_tambur_manual_batch.ts` <sub>(CLAUDE.md:49, arşiv:80)</sub>
- **top** · İptal ve fire sebep katalogları AYRI ve iki FARKLI kapıdan geçer: ROLL_CANCEL metin saklar → resolveReasonCode (async, tx DIŞINDA; kodu sunucu türetir, serbest metne kod UYDURULMAZ) · ROLL_SCRAP saklamaz → validateVarianceReason (senkron). TextReasonKind tip düzeyinde zorlar. · bekçi: `test_reason_presets §5/§6 · test_roll_cancel_undo` <sub>(CLAUDE.md:82, CLAUDE.md:73, kök CLAUDE.md:78 (2026-08-21 nameFold + )</sub>
- **yari** · Masaüstü/tablet dar sekmeleri `RAW_STOCK_PURE` (yalnız ham) ve `SEMI_FINISHED` (yalnız yarı mamul) kullanır; ayrım SERVİS katmanında yaşamak zorundadır çünkü negasyon istemciden söylenemez (`buildWhereClause` yalnız eşitlik/CSV-in/boolean üretir). <sub>(CLAUDE.md:92, CLAUDE.md:91)</sub>

## Yasaklar (18)

- **belge** · Alan bazlı belge CSS'inde (DocumentConfig.fields) calc()/var() YASAK — scaleDocCss yazı ölçeğini font-size:\s*([\d.]+)px regex'iyle uygular, calc() takılmaz, ölçek elle ayarlı alanda SESSİZCE ölür. Override yoksa TEK BAYT CSS basılmaz; her seçici '.sheet ' önekli. · bekçi: `scripts/test_doc_density_fields.ts` <sub>(CLAUDE.md:64)</sub>
- **deploy** · Transaction kısa tutulur; external I/O (HTTP, dosya) tx İÇİNDE YAPILMAZ (idle_in_transaction_session_timeout=5min). `tx.*` ile `Promise.all/allSettled` YASAK — pg adapter tek connection seri çalıştırır; ESLint kuralı yakalar. · bekçi: `Teks-Erp/eslint.config.mjs no-restricted-syntax` <sub>(CLAUDE.md:248)</sub>
- **deploy** · Decimal kolonda JS float aritmetiği yok — DB-side `increment`/`decrement` ya da `Prisma.Decimal` (`.plus()/.minus()`). <sub>(CLAUDE.md:324)</sub>
- **deploy** · Fabrika günü = `Europe/Istanbul`, TEK KAYNAK `src/constants/time.ts` (`FACTORY_TIMEZONE`, `factoryDaySql/Start/…`); literal kopyalanmaz. TAKVİM GÜNÜ tz'ye bağlı (açık yaz) ↔ MUTLAK PENCERE bağımsız (`-- tz-ok:`). `factoryDaySql` metni değişirse `sl_day_exact` istatistiği için YENİ migration. · bekçi: `Teks-Erp/scripts/test_report_day_boundary.ts (beş cephe)` <sub>(CLAUDE.md:301)</sub>
- **fason** · Çekme/fazla sapmasının TERSLEME ADRESİ `sourceRefId = receipt.id`; makbuz iptalinde sapmayı `rollId` ile SİLME — kısmi teslimatta önceki meşru sapmayı da süpürür. Fason kabulü yapan HER test cleanup'ı `rollVariance.deleteMany` içermeli (RESTRICT FK; yoksa temizlik 23001 ile yarıda kalır). <sub>(CLAUDE.md:77)</sub>
- **fason** · Tolerans okumasında `<= 0 → null` kalıbı YASAK: `0` geçerli değerdir ve 'tolerans yok, her fark uyarır' demektir; `null` = 'varsayılana dön'. Susturmanın yolu eşik değil BAYRAKtır. <sub>(CLAUDE.md:77)</sub>
- **filtre** · 'Daha Fazla Yükle' butonu ve 'Önceki/Sonraki · Sayfa X/Y' pager'ı YOK — dibe gelince otomatik yükleme. Electron: useDataTable+DataTable hazır; özel useInfiniteQuery listelerinde useInfiniteScroll (rootRef→PageBody, sentinelRef→AutoLoadMore). Mobil: FlashList onEndReached / RemoteListSheet. <sub>(CLAUDE.md:176, CLAUDE.md:424)</sub>
- **kalite** · Saf yüklem Prisma WHERE'inin yerine KONMAZ — sorgu DB'de koşar; 'önce oku, yüklemle kontrol et, sonra update' biçimi `setQueueUrgent`in F167 atomik claim'ini check-then-act yarışına geri döndürür. · bekçi: `test_station_quality_capability §3` <sub>(CLAUDE.md:100)</sub>
- **kalite** · `QUALITY_STATION_WHERE` hijyeni: `satisfies` ile yazılır (`as const` DEĞİL — readonly literal `OR:` konumunda Prisma input tipine oturmaz); nesne MUTATE EDİLMEZ, spread ile kopyalanır; spread'in bulunduğu kapsama ikinci `OR:` YAZILMAZ (biri sessizce kaybolur) — gerekirse `AND: [{...WHERE}, {OR:[…]}]`. · bekçi: `test_station_quality_capability §6` <sub>(CLAUDE.md:100)</sub>
- **kk1** · Durum geçişleri ATOMİK CLAIM ile yazılır: `updateMany WHERE {id, beklenen-durum}` + `count===0 → 409`. `findUnique → if → update` (check-then-act) YASAK. <sub>(CLAUDE.md:171)</sub>
- **modul** · Modüle ait router ADLANDIRILMIŞ kapı taşır: `require<Alan>Enabled` (`module.middleware.ts`, argümansız `readXEnabled()`, cache'siz); jenerik `requireModule("x")` YASAK — bekçi adı AST/metin arar. Kapısız uç gerekçesiyle başlıkta/`PANEL_EXEMPT`te yazılı; yer tutucu modüle middleware/panel eklenmez. · bekçi: `test_{ticaret,iplik,depo_multi,production}_regime_gate (lib/regime-gat` <sub>(CLAUDE.md:96, CLAUDE.md:315)</sub>
- **modul** · §3.6 tek resolver (`resolveXEnabled = modülAçık && altBayrak`) YALNIZ ebeveyni (modül anahtarı) OLAN 7 bayrağa; çekirdek/ebeveynsiz bayrağa (kk1.*, sevkiyat, ana veri) resolver YAZILMAZ — ölü sabit doğurur. Motor çekirdek, sunum modülde. · bekçi: `yok (helper başlık yorumları)` <sub>(CLAUDE.md:106)</sub>
- **raporlar** · Katalog değeri koda gömülmez: 'en üst kalite' quality_grades.sortOrder'dan çözülür, code === '1.KALITE' araması kaliteyi yeniden adlandıran fabrikada SESSİZCE boş karne üretir. Aynı kalıp her katalog anahtarı için geçerli. · bekçi: `test_quality_scorecard.ts (kaynak taramasıyla mekanik doğrular — arşiv` <sub>(CLAUDE.md:69)</sub>
- **rota** · Rota adımı istemci sözleşmesi DÜZ ID DİZİSİ (`plannedPropertyIds`); `RouteService.ALLOWED_STEP_KEYS` nested write'ı (`plannedProperties`/connect/deleteMany) REDDEDER — F209 seddi; çeviriyi servis yapar. · bekçi: `test_route_step_targets.ts (allowlist gevşeyince kırmızı — arşiv beyan` <sub>(CLAUDE.md:50)</sub>
- **sebep** · Tek-uçuş (dedup) YALNIZ arka plan tazelemesine aittir; `refreshReasonPresetCache` o kapıdan geçirilmez — create/update/duplicate/reorder kendi yazdığını GÖRMEK zorundadır, uçuştaki eski okumaya iliştirilemez. <sub>(CLAUDE.md:87)</sub>
- **sebep** · Geçersiz sebep kodu/eksik metin bir İSTEMCİ hatasıdır: `AppError.badRequest` + `details.code` (`REASON_CODE_INVALID` / `REASON_TEXT_REQUIRED`). Düz `Error` ATILMAZ — middleware onu 500'e çevirir, operatör sebebi göremez ve mobil kuyruk 5xx'i geçici sanıp üç kez daha dener. · bekçi: `test_reason_presets §3c` <sub>(CLAUDE.md:87)</sub>
- **sevkiyat** · Filtrede iki değerli NOT NULL enum'a çoklu seçim EKLENMEZ: `destination` çoklu seçilince `listShipments` DirectShipment'ları union'dan düşürür; kapsam anahtarları (`shipmentScope`, `invoiced`, `hasReturns`) if/else dalıdır. Elle okunan her id filtresi `readIdCondition`/`readFilterList`'ten geçer. · bekçi: `scripts/test_filter_multi_select.ts` <sub>(CLAUDE.md:67)</sub>
- **sevkiyat** · Bekçi ortamdaki veriye BAĞIMLI OLMAZ: `findFirst()` ile 'herhangi bir çuval/top' bulup üzerine test kurma — dev DB dolu olduğu için yerelde geçer, temiz CI DB'sinde düşer; fixture'ı test kendisi yaratır (CI: `npm run seed` + `seed:fixtures`). <sub>(CLAUDE.md:391)</sub>

## Tuzaklar (46)

- **belge** · buildPlan dizileri deterministik sıralanır (targetProperties, orderLinks → orderBy); orderBy'sız Prisma ilişkisi sıra garanti etmez, oynayan sıra planKey'i 'içerik değişti' yapar (değişmemiş kart her baskıda sürüm atlar). planKey anahtar sırasından bağımsız — düz JSON.stringify YASAK. · bekçi: `yok (dolaylı: test_traveler_card_stale §6)` <sub>(CLAUDE.md:144)</sub>
- **belge** · 'Bu değer katalogda yok' diyen bekçi sondası kataloğa eklenebilecek GERÇEK bir kod kullanamaz (${PROBE}-KAT) — fabrika 6-KAT'ı ekleyince test_fold_catalog P2002 ile çöktü. Teşhis: şablonu pasifleştirip testi tekrar koş; geçiyorsa hata VERİDEN doğuyor. · bekçi: `scripts/test_fold_catalog.ts (kendisi)` <sub>(CLAUDE.md:57)</sub>
- **belge** · PPLB beklentisi emitter'ın KENDİ dönüşümüyle (cleanCtlCp1254; EPL2 I8,E → gerçek Türkçe, 'Şube'→'Þube') kurulur, asciiFold ile DEĞİL — yoksa Türkçe içeren her yeni alan sahte kırmızı üretir (branchName/sackNote'ta yaşandı). · bekçi: `scripts/test_label_canvas_equivalence.ts (kendisi)` <sub>(CLAUDE.md:57)</sub>
- **belge** · İstek gövdesini ELLE kuran her katman (mutationFn, offline mutations) tıpkı z.object gibi SESSİZ allowlist'tir — gövdeye alan eklerken kaynak nesnedeki her alanın geçtiğini kontrol et (tanı: aynı isteği servis/HTTP/tablet üçlüsünden atıp farkı bul). [Çapraz küme: kat/kesim] <sub>(CLAUDE.md:57)</sub>
- **deploy** · Seed, migration'lardan SONRA boş tabloda koşar → yeni kolonun backfill'ine seed'de GÜVENİLMEZ, değer seed'e açıkça yazılır (2026-08-10 appliesColor / 2026-09-03 appliesQuality tuzağı). <sub>(CLAUDE.md:100)</sub>
- **filtre** · Generic `buildWhereClause` CSV'yi `{in:[…]}`e otomatik çevirir — `createdById`/`entryStationId` gibi yeni id filtreleri bu yoldan geçer, `buildRollWhere`'de elle okumaya başlanırsa readIdCondition ŞART (bekçi §2b kilitler). · bekçi: `test_filter_multi_select.ts §2b` <sub>(CLAUDE.md:55, CLAUDE.md:67)</sub>
- **filtre** · Tarih aralığı: `dateField` gönderilmez ya da whitelist dışıysa `applyDateRange` aralığı SESSİZCE yok sayar (filtre seçili görünür, liste süzülmez); rolls whitelist'i yalnız `createdAt` (ROLL_DATE_FIELDS); dateField kolonunda index ŞART. `last7` BUGÜNÜ içerir, özel aralık kısa yolu EZER, query anahtarı itemLabel/now'dan bağımsız. · bekçi: `mobil rollHistoryFilter.test.ts (16); Electron tarafı yok` <sub>(CLAUDE.md:56, CLAUDE.md:55)</sub>
- **filtre** · `resolveSortBy` whitelist dışı sortBy'ı SESSİZCE fallback'e (createdAt) düşürür: `SortableHeader` basılan HER kolon `ROLL_SORTABLE_FIELDS`'te olmalı, yoksa tıklama sıralamaz VE sekmenin updatedAt varsayılanı createdAt'e kayar (hata yok, log yok). İlişki üzerinden sıralama desteklenmez — 'İstasyon' kolonu SortableHeader almaz. <sub>(arşiv:134, CLAUDE.md:125)</sub>
- **filtre** · `cancelledAt` yüklemi çağıranın `select`ine BAĞIMLIDIR: alan seçilmezse `undefined` gelir ve katı `=== null`/`!== null` TÜM kalemleri iptal sayar (sessiz felç — sipariş sevk yokken CANCELLED'a düşer). Ya gevşek `== null` yazılır ya da alan seçimi LOAD-BEARING yorumla korunur. · bekçi: `yok (test_helpers sahte tx'i 2026-08-27'de dört senaryoda yakalamıştı ` <sub>(arşiv:1521)</sub>
- **finans** · `attachTotals` üç sayımı TEK anlık görüntüde alır: batch `$transaction` + RepeatableRead (READ COMMITTED'da her ifade ayrı görüntü → iade çift sayılır ya da kaybolur). `Promise.all` bunu sağlamaz. · bekçi: `yok (F-SEV-ESZ-002 denetim bulgusu; adlı bekçi doğrulanmadı)` <sub>(CLAUDE.md:46)</sub>
- **finans** · Bekçide henüz await edilmemiş gate-tx promise'ine no-op `.catch` konur (`void tx.catch(() => undefined)`); yoksa erken red sahipsiz rejection olur ve süreç Sonuç satırı basılmadan ölür — en sessiz kırmızı. · bekçi: `yok (bekçi yazım kuralı)` <sub>(CLAUDE.md:286)</sub>
- **kalite** · Bekçide muaf/taban kontrolleri DOSYA bazlı değil SAYIM bazlı yazılır: dosya bazlı muaf o dosyaya eklenen yeni tür kontrolünü sessizce kapsar; 'dosyada X geçiyor mu' ikinci select'in alan atlamasını gizler. 'kind seçen her select yüklem içindir' varsayımı yanlış — görüntüleme select'leri (currentStepKind) de kind okur. · bekçi: `test_station_quality_capability (S3/S4 negatif sondaları)` <sub>(CLAUDE.md:100)</sub>
- **kalite** · `setCapabilities` renk tarafına yalnız `colorIds` AÇIKÇA gönderilirse dokunur: `[]` hepsini siler, alan hiç yokken mevcut satırlara dokunulmaz. · bekçi: `test_station_capability (8b — bölüm BELİRSİZ)` <sub>(CLAUDE.md:128)</sub>
- **kesif** · Cihaz satırı tavanı REJİMDEN BAĞIMSIZ olmalı (`MAX_DEVICE_ROWS`): `MAX_PENDING_DEVICES` kapalı rejimde PENDING sayacı hiç artmadığı için sessizce etkisizleşir. Genel kural — bir kapıyı bayrağa bağlarken sor: bu bayrak KOMŞU kapının saydığı şeyi sıfırlıyor mu? · bekçi: `test_device_pairing_flag.ts (toplam tavan silinince 1 ❌)` <sub>(CLAUDE.md:108)</sub>
- **kesif** · Derlenmiş paket üzerinden yapılan istemci çıkarımı o paketin YAŞINI ölçmez: mobil `usePermission` global joker (`["*"]`) kestirmesi KALDIRILMAZ — 2026-09-04'te 'kaldırın' önerisi ölçümle çürütüldü, tablette ESKİ BUNDLE koşuyordu. · bekçi: `mobil/src/hooks/usePermission.test.ts` <sub>(CLAUDE.md:108)</sub>
- **kesif** · 'Yazmaz' kontrolü, kayıt zorlanan değerle AYNI başlatılırsa VAKUMDUR — sızan yazım mevcut değeri yazar ve bekçi fark etmez. Sonda kurarken başlangıç değerini bilerek TERS seç. <sub>(CLAUDE.md:196)</sub>
- **kesif** · Mobil kendi izin union'ını TAŞIR (`mobil/src/types/permissions.ts`): ayrı proje, backend katalogunu import edemez — mobil ekran izni eklerken oradaki liste ELLE güncellenir ve bekçi orayı TARAMAZ. Aynı sınıf: mobil `Electron/shared/discovery.ts`i de import edemez (→ KEŞİF-İKİZ). · bekçi: `discovery için var (contract.test), izin union'ı için YOK` <sub>(CLAUDE.md:240, CLAUDE.md:109)</sub>
- **kursun** · `completeUnassignedFromTambur`'un tx İÇİ tazelemesi (`resolveUnassignedTamburClosure(tx)`) bir DERİNLİK SAVUNMASIDIR ve bekçi ona erişemez — tek iş parçacıklı testte tx dışı ön kontrol her zaman önce reddeder, körleştirilse test yeşil kalır. Silmeden önce yerine ne koyduğunu bil. · bekçi: `yok (bilinçli — arşivde yazılı)` <sub>(CLAUDE.md:52)</sub>
- **modul** · Kapı, tek yazarın İÇİNDE olmalı: `applyYarnMovementTx`'in ilk ifadesi `readIplikEnabled(tx)` (route kapısı yetmedi — ticaret açık + iplik kapalı kurulumda kg defteri doluyordu); çağıranlara ikinci kopya kontrol EKLENMEZ. Köprü bayrağı tek resolver `resolveYarnOutOnInvoiceEnabled`. · bekçi: `test_iplik_regime_gate` <sub>(CLAUDE.md:96)</sub>
- **modul** · 'Bekçi yeşil ≠ kapı doğru': kapı kapsamı elle sayılmaz, AST ile ölçülür; her bekçi negatif sondayla kırmızı verdiği görülerek sayılır (HTTP ayağı çalışan sürecin kodunu ölçer, kaynak bozulunca yeşil kalır); kapı taşıma ile bekçi güncellemesi AYNI commit'te. <sub>(CLAUDE.md:96, CLAUDE.md:106)</sub>
- **modul** · Sayısal ayarda 'değeri temizle' 0 yazılarak kurulur (`set()` null/JsonNull almaz); `<=0 → null` okuma kalıbı ALAN BAZLIDIR: kısa-kesim eşiğinde 0 = girilmemiş, fason toleransında 0 = 'tolerans yok' (kalıp orada YASAK). Yeni sayısal anahtarda 0'ın anlamı kararlaştırılır. <sub>(CLAUDE.md:211)</sub>
- **mukerrer** · Kapsamlı ad kuralı olan tabloda sed BİLEŞİK olur: `machines` seddi `(stationId, nameFold)`dir — düz `nameFold` seddi 'Makine 1' adını fabrikada tek bir istasyona hapsederdi. · bekçi: `test_master_data_name_dup §10d` <sub>(KOD:20260831120000_namefold_sed_kalan_ta)</sub>
- **mukerrer** · Sabit adla master-data yaratan test fixture'larını damgala (rastgele sonek): sed kurulduktan sonra kalan ad ikinci koşumu P2002'ye düşürür. Fold bekçisi fixture'ında sonek HARFE yapıştırılır (`X${suffix}`) — renk kuralında rakam blokları kendi sırasını korur, salt rakamlı sonek sondayı çürütür. <sub>(CLAUDE.md:78, CLAUDE.md:84)</sub>
- **mukerrer** · Tombstone'a dokunan her yüzey onu DIŞLAR: `ColorService.assertNameAvailable` (aksi 'PASİF var, aktifleştirin' deyip birleşmişi diriltmeye davet eder), `import-name-guard` önizlemesi, `seed-fixtures` (diriltmez) ve `test_consistency` §18 customers sorgusu (yoksa her birleştirme kırmızı yapar). · bekçi: `test_consistency §18, test_master_data_name_dup §9` <sub>(CLAUDE.md:78, CLAUDE.md:84)</sub>
- **raporlar** · Script çıkışı: `$disconnect()` TEK BAŞINA YETMEZ — havuz idleTimeoutMillis 600_000, idle handle event loop'u 10 dk açık tutar, koşucu 180 sn'de SIGTERM'ler (ZAMAN AŞIMI). Ya process.exit(fail>0?1:0) ya `$disconnect()+pool.end()` (uzun çıktıda ikincisi). <sub>(CLAUDE.md:382)</sub>
- **refakat** · Bayt-bayt korunan belge çıktısında kapalı blok TEK BAYT basmaz (tablo da CSS de koşullu emit) ve `${…}` kendi satırına konmaz — boş satır A4/eski-belge parmak izini bozar; opsiyonel yeni alan eski donmuş snapshot'ta satır/kolon üretmez, geriye dönük doldurma YAPILMAZ (tazeleme yalnız `reissue`). · bekçi: `scripts/test_traveler_card_a5_batches.ts · test_fason_ceki_html.ts §10` <sub>(CLAUDE.md:137, CLAUDE.md:61)</sub>
- **sebep** · `expireReasonPresetCacheForTest` (satırları KORUR, yalnız bayatlatır) ile `invalidateReasonPresetCache` (satırları da düşürür) KARIŞTIRILMAZ; bekçi ikisini ayırmazsa düzeltmeyi değil başka bir şeyi ölçer. · bekçi: `test_reason_presets §3b/3d` <sub>(CLAUDE.md:87)</sub>
- **sebep** · Önbellek davranışı ölçen bekçi doğrulamadan hemen ÖNCE tazeleme yapmaz (ölçümünü hep taze pencerede yapar, arızayı göremez); 'kendini onarır' ölçümü önbelleğin GÖRMEDİĞİ bir satırla (servisle değil doğrudan prisma ile yazılan) kurulur — 'kod kabul edildi mi' sorusu bayat listeyle de EVET der. · bekçi: `test_reason_presets §3b/c/d (dört negatif sonda)` <sub>(CLAUDE.md:87)</sub>
- **sevkiyat** · Belge kolonu/bölümü İÇ veri taşıyorsa OPT-IN: renderer `DocCol.defaultHidden` + `shown` allowlist, panel `DocSectionDef.defaultHidden`; `hidden`/`sections` BLOCKLIST'tir — naif kolon müşteri irsaliyesinde varsayılan GÖRÜNÜR doğar. İki taraf birlikte işaretlenir. · bekçi: `scripts/test_shipment_doc_batch_column.ts` <sub>(CLAUDE.md:43, CLAUDE.md:247, arşiv:230)</sub>
- **sevkiyat** · Brüt geri-ekleme tuzakları: (1) çuval satırı + özet + çeki TEK gross kaynağından (`grossRolls`/`sacksGross`); (2) ana sorgu ile iade sorgusu tx'siz → canlı id kümesiyle DEDUP şart; (3) `totalKg` DEĞİŞMEZ. Sentetik satır `sackId=prevSackId` taşır; `prevSackId`siz eski iade ATLANIR. · bekçi: `scripts/test_shipment_detail_gross.ts (33), test_dispatch_report_gross` <sub>(CLAUDE.md:47, CLAUDE.md:48)</sub>
- **sevkiyat** · `dispatchedAt` sıralamasında union'ın iki tarafında alan adı FARKLI (`Shipment.dispatchedAt` nullable ↔ `DirectShipment.shippedAt` NOT NULL): direct dalı için `orderBy` VE cursor `where` AYRI kurulur; nulls-last (`sortNullable`). Muhasebe listesi varsayılanı `dispatchedAt desc`. · bekçi: `scripts/test_shipment_list_sort.ts` <sub>(arşiv:53, CLAUDE.md:45)</sub>
- **sevkiyat** · Sevk topun önceki rafını `Roll.preShipStatus`a yazar (statüye göre gruplu updateMany), storno oradan döner ve NULL'lar; körlemesine WAREHOUSE'a döndürmek 2. kalite (A1_STOCK) topu 1. kalite rafına yazar. NULL (karar öncesi sevk) → WAREHOUSE. Aile: `prevSackId/prevQualityGrade`, `preCancelStatus`. · bekçi: `scripts/test_shipment_undo_dispatch.ts (preShipStatus kaldırılınca 5 k` <sub>(CLAUDE.md:63)</sub>
- **sevkiyat** · `freezeForSource` versiyonu `max+1` (findFirst orderBy version desc → (prev?.version ?? 0)+1); sabit `version:1` bir kaynağın İKİNCİ dondurulmasında (storno sonrası yeniden sevk) `docType_sourceId_version` unique'ine çarpıp tam sevk anında 500 verir. · bekçi: `scripts/test_shipment_undo_dispatch.ts (version:1 sabitlenince exit 1)` <sub>(CLAUDE.md:63)</sub>
- **superadmin** · `*` jokerini HER kapı tanır: yüklem `matchesPermission`; backend `includes("admin:*")` YASAK (kısayol `admin:*` korunur); Electron `hasAdminAccess` = ADMIN_PERMISSION_LIST.some(matchesPermission) (isAdmin yetki kısayolu DEĞİL, yalnız adminOnly görünürlük); mobil `has()` ilk satır `set.has('*')`. · bekçi: `test_superadmin_visible (`*` körlüğü kolu) + Electron auth.test.ts + m` <sub>(CLAUDE.md:97, CLAUDE.md:117, CLAUDE.md:104)</sub>
- **superadmin** · `SECRET_FIELDS` (audit-diff.helper.ts) yalnız `changes`i maskeler ve TAM AD eşleşir (`Set.has`; '…Hash' son eki yakalanmaz) — payload/oldData/newData HAM, çağıranın yükü temiz olmak ZORUNDA (`rotated: boolean` emsali). Sır grep'i tam bcrypt gövdesiyle, ön ek yanlış pozitif verir. · bekçi: `test_superadmin (sır hijyeni AST) + test_settings_password (sır grep)` <sub>(CLAUDE.md:97, CLAUDE.md:98, CLAUDE.md:105)</sub>
- **superadmin** · `SystemLog.userId` NULLABLE — aktör/kullanıcı süzgeçlerini düz eşitlikle yazma; ham SQL'de `IS NOT TRUE` / null-güvenli yazım (fabrikada 641 `userId IS NULL` sistem olayı sessizce düşer). Gizleme süzgeçleri kalktı, tuzak her yeni aktör süzgeci için yaşıyor. · bekçi: `yok (eski test_superadmin_hidden_single_source ham-SQL kolu silindi)` <sub>(CLAUDE.md:97)</sub>
- **tambur** · İstek gövdesini ELLE kuran her katman (mobil `mutationFn`, offline `mutations.ts`) sessiz bir allowlist'tir — Zod dersinin istemci ikizi; yeni alan eklerken ekran→tip→gövde üçünü birlikte yaz ve tabletten uçtan uca ölç (servis/HTTP yeşilken yalnız tablet NULL yazdı). · bekçi: `yok (mutations.ts gövdesinde 'bu satır unutulursa' yorumu)` <sub>(arşiv:192)</sub>
- **tambur** · Cursor'lu (sonsuz kaydırmalı) listede süzme SUNUCUDA — istemci süzmesi yalnız o sayfayı süzer, yanlış 'kayıt yok' üretir; tarih aralığıyla `dateField` ŞART (`applyDateRange` alan adı yoksa aralığı SESSİZCE yok sayar); Zod `.uuid()` süzgeçler (ham CSV P2007). · bekçi: `mobil rollHistoryFilter.test.ts + scripts/test_tambur_recent_output_fi` <sub>(CLAUDE.md:56)</sub>
- **tambur** · Bir invariantı TEK dalda ölçen bekçiye güvenme — kör nokta hatanın kendisiyle aynı yerde olabilir (§5 aşımı yalnız FULL+DEPO'da ölçüyordu). Koddaki 'birebir ayna' yorumu kanıt değildir; ayna kırık olabilir — yorum ≠ kod, iki dalı da sondayla ölç. · bekçi: `yok (ilke)` <sub>(CLAUDE.md:58, CLAUDE.md:81)</sub>
- **top** · Alanı eklemek yetmez — hangi YANITTA döndüğünü doğrula (liste satırı ≠ detay ucu): 'Ekleme Nedeni' listeden okunuyordu, manualReason yalnız detayda dönüyordu; özellik commit edildi, kullanıcıya hiç ulaşmadı. <sub>(CLAUDE.md:53)</sub>
- **top** · F221 deseni: servis opts.permissions ALMAZSA enforcement atlanır (güvenilen dahili çağrı) — güvenlik sınırı controller'dadır; dış uçtan çağırırken permissions geçirilir. Aynı desen createInitialEntry.duplicateGuard/txGate'de. <sub>(CLAUDE.md:126)</sub>
- **yari** · 'Altıncı enum değeri unutuldu' sınıfı: `RollEntrySource` gibi bir enum'a değer eklerken TİP-GÜVENLİ OLMAYAN yüzeyleri elle ara — switch `default` dalı, `Record<string,string>`, dizi/CSV literali, ham SQL, istemcideki string haritaları. Derleyici oralarda sessiz kalır; beş vaka ölçüldü. <sub>(CLAUDE.md:92, CLAUDE.md:91)</sub>
- **yari** · Ürün Dengesi TALEPTEN doğar: sipariş kalemi olmayan spec listede HİÇ görünmez — bekçi fixture'ında önce talep, sonra ölçüm kurulur (ters sırada test 'grup=0' ile düşer). · bekçi: `scripts/test_semi_finished_surfaces.ts` <sub>(CLAUDE.md:90)</sub>
- **yari** · Playwright + `_electron.launch` görsel turunda temiz `--user-data-dir` profili kullan; yoksa önceki turun oturumu geri yüklenir, giriş ekranı çıkmaz, seçiciler tutmaz. Birim testi + typecheck yeşilken bile 'derleyici görmez' hatalarını (string haritası, CSS breakpoint) yalnız ekrana bakmak bulur. <sub>(CLAUDE.md:91)</sub>
- **yetki** · Ayar şifresi kilit kovası GİRİŞTEN AYRI, ayrım KEY'de (`sp:` ön eki): `resolveLoginLockoutKeys` varsayılan `ip` kapsamında kimliği düşürür, 5 yanlış ayar şifresi IP'nin LOGIN'ini kilitlerdi. LOCKED audit kilit başına tek satır (`justLocked`); `Retry-After` tek noktadan; audit yolu `baseUrl+path`. · bekçi: `scripts/test_settings_password.ts (ayar 429 iken login 200 sondası)` <sub>(arşiv:2096)</sub>
- **yetki** · Elle sayılmış KAPSAM listeleri ('üç yazma yüzeyi') bir sonraki yüzeyde delinir: kapsamın yüklemi ekran değil YAZMA davranışıdır, `src/routes/**` AST tripwire ile ölçülür; 'bekçi yeşil ≠ kapı doğru' — harness çerçeve semantiğini (Express mount göreliliği, senkron sözleşme) modellemeli. · bekçi: `scripts/test_settings_password.ts §J + test_document_template_permissi` <sub>(arşiv:2096, CLAUDE.md:96, arşiv:2057)</sub>

## Reçeteler (17)

- **belge** · Etiket alan→değer eşlemesi label-field-values.ts TEK noktadır (HTML/PPLA/PPLB/ZPL ortak) — yeni emitter oradan geçer; Etiket Stüdyosu paleti backend katalogundan (label-fields.ts) beslenir, alan eklemek Electron'da kod istemez. · bekçi: `scripts/test_label_canvas_equivalence.ts` <sub>(CLAUDE.md:57)</sub>
- **belge** · Kat (foldType) yazımı üçlü sözleşme: undefined DOKUNMA · null TEMİZLE · kod YAZ; kanoniklik daima resolveFoldTypeForWrite (ham '6 kat' kaydedilirse kat filtresi topu bulamaz, sessiz); kat değişimi labelDirty işaretler; satırı YENİDEN ADLANDIRMAK yeni değer değildir — kod KİMLİKTİR. · bekçi: `scripts/test_fold_edit_and_label.ts` <sub>(CLAUDE.md:57)</sub>
- **deploy** · Partial index drift-free: şemada `@@index([col])` BIRAK, migration'da DROP + CREATE … WHERE — Prisma 7 predicate farkını drift saymaz, index↔unique farkını SAYAR → partial UNIQUE için şemada `@@unique`. 'items/customers partial'ı henüz YOK' cümlesi BAYAT (2026-08-21'den beri var). · bekçi: `Teks-Erp/scripts/test_db_invariants.ts` <sub>(CLAUDE.md:248, CLAUDE.md:78)</sub>
- **deploy** · Grandfathering / davranış-koruyan veri migration'ı KOŞULLU yazılır (`WHERE EXISTS (SELECT 1 FROM rolls LIMIT 1)`) ve değer = DÜNKÜ DAVRANIŞ; sabit `false` demo/boş kurulumda yüzeyleri 403'e düşürür. <sub>(CLAUDE.md:96)</sub>
- **deploy** · Ham SQL'de çıplak `now()` GÜVENLİ ve TERCİH EDİLEN yazım; `now() AT TIME ZONE 'UTC'` turu artık kırılgan taraf (10 yazma noktası kaldı). `test_raw_sql_hygiene` çıplak zaman fonksiyonunu yalnız gerekçeli `-- tz-ok: <neden>` ile geçirir; şablon içi açıklamada BACKTICK yok. · bekçi: `Teks-Erp/scripts/test_raw_sql_hygiene.ts` <sub>(CLAUDE.md:285)</sub>
- **deploy** · Bekçi yazım dersleri: (1) aynı invariant'a İKİNCİ bekçi yazılmaz (muaf listeleri ayrışır) · (2) KÖRLÜK ZEMİNİ — sayım kontrolüne alt sınır, 'ihlal yok' ile 'bakılmadı' aynı yeşile çıkmasın · (3) muaflar KEŞFEDİLİR, bayatlığa karşı denetlenir · (4) negatif sonda ZORUNLU. · bekçi: `yok (meta kural)` <sub>(CLAUDE.md:285, CLAUDE.md:100, CLAUDE.md:84)</sub>
- **filtre** · Cursor sözleşmesi `mode=cursor&limit&cursor&withTotal`; toplam yalnız İLK sayfada ve YAKLAŞIK (`totalEstimate`). BaseService sıralamaya HER ZAMAN `id` tie-breaker ekler (offset+keyset) → yeni sıralama index'i `(kolon, id)` BİLEŞİK olmalı; tekil kolon Incremental Sort'ta kalır. <sub>(CLAUDE.md:424, arşiv:1521)</sub>
- **finans** · Eşzamanlı aynı-`clientToken` çift gönderim PO deseniyle kapanır: `withBarcodeRetry`'a `!isClientTokenP2002` yüklemi + tx-dışı catch → findUnique → CACHED yanıt; cached yanıtın şekli normal yanıtla BİREBİR (ayrışırsa replay istemciden ayırt edilir). 5 finans ucunda uygulandı. · bekçi: `BELİRSİZ (adlı bekçi doğrulanmadı)` <sub>(CLAUDE.md:286)</sub>
- **finans** · Yarış bekçisinde pencere ELLE AÇIK TUTULAN tx ile kurulur (`Promise.allSettled` pencereyi ıskalar, sahte-yeşil); sonda kilidi bilinçli `FOR NO KEY UPDATE` (`FOR UPDATE` FK KEY SHARE'i bloklar, sonda kendini kilitler); 'bekledi' süre ölçümü yalnız pozitif yönde anlamlı, kırmızı SONUÇ kontrolüdür. · bekçi: `emsal: test_goods_receipt_invoice.ts §10/§11` <sub>(CLAUDE.md:286)</sub>
- **kesif** · Grup içi adres sırası: adres tercihi → `tieBreak` → gecikme (rttMs) → ad. Son basamak (ad) DETERMİNİZM içindir — atlanırsa eşit adaylarda sıra turdan tura oynar ve 'otomatik seçilen adres' de oynar. · bekçi: `Electron/src/test/discovery-logic.test.ts` <sub>(CLAUDE.md:109)</sub>
- **kursun** · Hata mesajı zenginleştirmesi YALNIZ gerçekten fırlatılacak dalda koşar: `explainTamburScanBlock` sebep sorgusu bypass rejiminin EN SIK yolunda (bypassPending dolu, hata yutuluyor) boşa koşmamalı. <sub>(CLAUDE.md:52)</sub>
- **modul** · Feature-flag sözleşmesi ÜÇ AYAK `aBool`/`aNum`/`aEnum`: her anahtar A(api Zod)+B(`SETTING_KEYS`)+C(`setFeatureFlags`)+panel; sayısal alan `FlagDef.numberField` iç adı `numberKey:` ZORUNLU (`key:` regex'e sızar); enum şeması KAPALI küme + panel `enumKey` + çöp-değer sigortası. · bekçi: `test_feature_flag_contract (§16 dahil)` <sub>(CLAUDE.md:211, CLAUDE.md:106, CLAUDE.md:70)</sub>
- **raporlar** · Toplu geçmiş düzeltmesi (backfill) HAM SQL yazar: Prisma update updatedAt'i tazeler ve envanter sekmelerinin 'Son İşlem' sıralaması o kolondan çözülür → script sahadaki HER listeyi yeniden sıralar. Backfill kuralı trigger ile BİREBİR aynı olmalı. <sub>(CLAUDE.md:69)</sub>
- **rota** · Kapı alanı eklerken ÜÇ Zod şeması (`finalizeSchema`/`cutOpenFabricSchema`/`finalizeOpenFabricSchema`) VE mobil offline `mutations.ts` gövdesi birlikte güncellenir — Zod tanımadığı anahtarı sessizce siler, kuyruk gövdeyi elle kurar; idempotent-retry erken dönüşleri kapıdan ÖNCE koşar. · bekçi: `mobil mutations.test.ts (sözleşme sondası)` <sub>(CLAUDE.md:71)</sub>
- **sebep** · Sebep kodu çözümü tx DIŞINDA yapılır: `applyRollDispositionsTx` gibi tx içi motorlar katalog OKUMAZ, hazır kodu çağıran geçirir (senkron/async ayrımı bozulmasın). <sub>(CLAUDE.md:78)</sub>
- **sebep** · Arka plan tazelemesi DB'ye ulaşamazsa ELDEKİ liste korunur ve kısa bir geri çekilme uygulanır (`refreshBlockedUntil`/`REFRESH_RETRY_MS`) — DB kapalıyken her doğrulama yeni sorgu açmasın. Async metin-kind yolu (`rowsForTextKind`) da aynı dayanıklılığı taşır. <sub>(CLAUDE.md:87)</sub>
- **superadmin** · Ayar şifresi kapısının KAPSAMI ELLE SAYILMAZ: `src/routes/**` özyineli AST tripwire — `systemSettingService.set(`/`setFeatureFlags(` çağıran HER route zinciri `requireSettingsPassword` taşır (iki yönlü muaf); dolaylı yazıcı ayrı karar kaydı ister. Elle 'üç yüzey' listesi bir sonraki yüzeyde delinir. · bekçi: `test_settings_password tripwire kolu` <sub>(CLAUDE.md:98)</sub>

## Kararlar (5)

- **mukerrer** · Bulanık ad kuralı KAYIT AİLESİNİN YAPISINA göre profillenir; tek genel skor ya gürültü ya körlük üretir. Eşik okunabilir ölçek olmalı (%100 yazım · %90 neredeyse aynı · %80 tek harf) ve 'fazladan anlamlı kelime' hiçbir eşikte aday olmamalı; ayar dört kapıdan geçer. · bekçi: `test_duplicate_detection + test_feature_flag_contract` <sub>(CLAUDE.md:80)</sub>
- **raporlar** · Casing hataları (AppError ↔ app-error.ts) için EK ÖNLEM GEREKMEZ — forceConsistentCasingInFileNames açık ve tsc TS2307 ile yakalar; macOS'ta 'derlendi sanma'nın tek sebebi yukarıdaki grep tuzağıydı. · bekçi: `tsc TS2307` <sub>(CLAUDE.md:73)</sub>
- **sebep** · Doğrulama kuralı kapının KENDİSİNDE yaşar; çağıran taraftaki elle try/catch sarmalayıcıları kaldırılır — sarmalayıcı `details.code`'u yutar ve hata sınıfını kaybettirir. <sub>(CLAUDE.md:87)</sub>
- **tambur** · Davranış bayrağı varsayılanı = BUGÜNKÜ davranış ve bu ÖLÇÜLÜR; ÇIKIŞSIZ KAPI tasarım hatasıdır — kapı takmadan önce istemcinin çıkış yolu var mı sor, ad yaptığı işi söylesin (autoCreate ≠ required); kapı takarken 'bu malın çıktığı BAŞKA yol var mı' kardeş bayrağın kapsam listesinden sorulur. · bekçi: `scripts/test_feature_flag_contract.ts §16 (aEnum) — modül kümesi` <sub>(arşiv:2185)</sub>
- **yari** · Bir giriş yüzeyi ('backend hazır, arayüz sonra') çıkışıyla birlikte kurulmazsa sıfır kullanım üretir ve bunu kimse hata olarak raporlamaz — özellik paketi giriş+çıkış+veri (rota/izin) üçlüsüyle kapanır. <sub>(CLAUDE.md:92)</sub>

## Ölçümler (3)

- **raporlar** · tsc ÇIKTISI BORUDA DA RENKLİDİR: `typecheck | grep 'error TS'` HİÇ eşleşmez (ANSI kodu araya girer; 6 hata 'temiz' raporlandı). Temiz/kirli ölçüm birimi ÇIKIŞ KODUDUR (`npm run typecheck; echo $?`); metin süzeceksen typecheck:plain. <sub>(CLAUDE.md:73)</sub>
- **tambur** · Nullable kolon eklemek PG11+'ta metadata-only'dir (dolu `rolls`'ta 6 ms ölçüldü); 'canlı tabloya kolon pahalı' sezgisi DEFAULT'lu kolonlar içindir — kolon yerine JSON seçme gerekçesi olamaz. <sub>(arşiv:80)</sub>
- **yari** · Kolon/kapsam bekçisi ANAHTAR varlığını değil SAYI SEMANTİĞİNİ ölçer: kolon toplamları Envanter kapsamlarıyla karşılaştırılır ve örtüşmezlik kontrol edilir. Yalnız anahtar ölçen bekçi, panonun envanterden sapmasını yıllarca görmedi (negatif sonda: pano=48 ↔ envanter=47). · bekçi: `test_production_flow_columns.ts` <sub>(CLAUDE.md:91)</sub>

## Yorum politikası (2026-09-05 kararı)

- Koddaki yorum 1–3 satırdır ve yalnız NEDEN'i ya da değişmezi söyler; koruduğu satırın hemen üstünde durur.
- Tarih, ölçüm anlatısı, 'bekçi §3 kırmızı verdi' hikâyesi yoruma GİRMEZ; gerekirse tek satırlık arşiv çapası: `// bkz. arşiv 2026-08-05 kilit sırası`.
- Politika ileriye dönüktür: dokunulan dosyada eski hikâye yorumu çapaya indirilir; toplu temizlik yapılmaz (ölçüm: backend src %31 yorum satırı, 1.143 tarihli yorum — 2026-09-05).
- `NEDEN:` etiketi tek satırlık gerekçe için serbesttir; `ÖLÇÜLDÜ:` ve tarihli paragraf arşive gider.

## Yasaklar — normalize liste

> Kök `CLAUDE.md`'deki kısa liste bunun özetidir.
>
> **Mekanik olarak zorlananlar (ESLint, `Teks-Erp/eslint.config.mjs`):** `tx.*` + `Promise.all` · `notIn: []` · `orderBy: { batchNumber }` · jenerik `requireModule("x")` · `'Europe/Istanbul'` literali (yalnız `src/constants/time.ts` muaf) · route/controller'da `lib/prisma` import. Bunlar için "hatırlamak" gerekmez, `npm run lint` düşer.
>
> **Yeni kural eklerken ölçüt:** kural buraya değil önce ESLint'e girer — ama YALNIZ AST'den kesin yakalanabiliyorsa ve mevcut kodda ölçülen ihlal SIFIRSA. Kırmızı veren bir kural, kuralın değil kapsamın yanlış okunduğunun işaretidir. Ölçülüp REDDEDİLEN emsal: `toLocaleUpperCase("tr")` yasağı DARDIR (yalnız koşullu etiket elemanı ↔ `QualityGrade.code`); `src/`de 62 meşru kullanım var, bazıları ZORUNLU — kural yazılmadı. Kaynak: B5 yasak toplayıcı (100) + Faz C hakemlerinin `keepIn=yasaklar` kuralları, tekilleştirilmiş. `stillValid=HAYIR` olanlar bilerek dışarıda.

- **[PROFİL]** Cihaz üzerinde `simulate` işaretliyken uydurulmuş ölçüm CANLI VERİYE YAZILMAZ; simülasyon per-cihaz veri bayrağıdır, koda gömülü 'ASLA' değil. <sub>(mobil, electron · CLAUDE.md:329, CLAUDE.md:224)</sub>
- **[PROFİL]** Fason kabul modunu değiştirirken (`switchReceiveMode`) ön-dolu satır EKLEME; tek→parça yalnız dokunulmamışsa açılır. <sub>(mobil · arşiv:437)</sub>
- **[PROFİL]** Kurşun rejim bayrağı açıkken tablet yazmasını tek kapıdan reddet (`assertKursunTabletMayWrite`) ve yalnız UYGUN adımda salt-okunur yap. · bekçi: `test_kursun_regime_lock.ts` <sub>(backend, mobil · arşiv:103)</sub>
- **[PROFİL]** Listeye tarih seçici paketi EKLEME; takvim elle yazıldı. <sub>(electron, mobil · CLAUDE.md:56)</sub>
- **[PROFİL]** Mevcut iş emrine top ekleyen `PATCH /:id/attach-rolls` ucunu geri getirme; her iki istemci de YENİ iş emri açar. <sub>(backend, electron · arşiv:840)</sub>
- **[PROFİL]** Parti bayrağını `batch.requiredEnabled` diye adlandırma — reddedildi; ad `batch.autoCreateEnabled`'dır. · bekçi: `test_quality_batch_flags.ts, test_feature_flag_contract.ts` <sub>(backend · arşiv:2185, CLAUDE.md:106)</sub>
- **[PROFİL]** Sayaç sıfırlama düğmesi EKLENMEZ (bilinçli red): saklanan ikinci bir sayaç gerektirir — repo tüm sıraları veriden türetir — ve P01-P40 canlıyken sıfırlamak, kabul edilen sarma çakışmasından daha kötü bir aynı-hafta çakışma serisi üretir. <sub>(parti-no-batch · CLAUDE.md:161)</sub>
- **[PROFİL]** Sebep sırasını yazarken TAM listeyi yazma anında çek; ekran yalnız aktif satırları çizdiği için gizli satır kendi yuvasında kalmalı (`mergeVisibleOrder`). · bekçi: `reasonPreset.order.test.ts` <sub>(mobil · CLAUDE.md:86)</sub>
- **[PROFİL]** Yeni paketi eski `demo.etkiliyazilim.com/guncelleme/electron/` adresine kurma; o adres yalnız ilk kurulumun tarihçesidir. · bekçi: `update-feed-url.test.ts` <sub>(ops · arşiv:958)</sub>
- **[PROFİL]** Zorunlu güncelleme kapısını ertelenebilir yapma; inerken şerit, indikten sonra kapatılamaz kapı + 2 dk geri sayım. <sub>(electron · arşiv:958, CLAUDE.md:88)</sub>
- **[ÇEKİRDEK]** 2026-08-05 öncesi dolgulu parti kayıtları (`P0508260019`) OLDUĞU GİBİ DURUR — geriye dönük düzeltme/migration yapılmaz (canlı veri + o numaralar kâğıda basıldı); `nextDailySeq` parseInt ile okur, aynı gün içinde kaldığı yerden devam eder. · bekçi: `scripts/test_batch_number_format.ts §3` <sub>(parti-no-batch · CLAUDE.md:161)</sub>
- **[ÇEKİRDEK]** Ajan/test sunucusunu `pkill -f "tsx src/server.ts"` ile öldürme; yalnız kendi PID'ini öldür. <sub>(ops · CLAUDE.md:97)</sub>
- **[ÇEKİRDEK]** Aynı URL parametresine (`entrySource`) yazan iki ayrı filtre bulundurma — birbirlerini sessizce ezerler; seçenekler TEK filtrede toplanır. <sub>(yari-mamul · CLAUDE.md:92)</sub>
- **[ÇEKİRDEK]** Backend'de ikinci bir NODE SÜRECİ başlatma; tek-process invariantı korunur (uzak erişim iki DİNLEYİCİ ile çözülür). · bekçi: `test_middleware_order.ts, test_remote_access_guard.ts` <sub>(backend · arşiv:1791)</sub>
- **[ÇEKİRDEK]** Backend'e seri port / donanım polling'i KOYMA; makine verisini fabrika LAN'ındaki ayrı toplayıcı ajan okuyup API'den basar. <sub>(backend · CLAUDE.md:117)</sub>
- **[ÇEKİRDEK]** Belge alan CSS'inde `calc()` / `var()` KULLANMA; `scaleDocCss` punto regex'i onları görmez, ölçek sessizce ölür. · bekçi: `test_fason_ceki_html.ts (KURAL 2), test_document_style.ts` <sub>(backend · arşiv:267, CLAUDE.md:64)</sub>
- **[ÇEKİRDEK]** Belge şablonu JS template literal'i içinde (CSS yorumlarında bile) BACKTICK KULLANMA — literal ortadan bölünür, dosya derlenmez (aynı işte iki kez ısırdı). · bekçi: `yok (derleme hatası)` <sub>(belge-etiket-sablon · CLAUDE.md:64)</sub>
- **[ÇEKİRDEK]** Belge şablonu literalinde BACKTICK kullanma. <sub>(backend · CLAUDE.md:64)</sub>
- **[ÇEKİRDEK]** Cihaz override'ını iki durumlu yapma; üç durum ŞART (`server|on|off`) — aksi hâlde 'girilmemiş' ile 'sunucuyu izle' aynı değere düşer. <sub>(mobil · CLAUDE.md:335)</sub>
- **[ÇEKİRDEK]** Cloudflare proxy'sini (turuncu bulut) DNS-only'ye çevirme; sertifika Origin CA'dır, güncelleme sessizce durur. <sub>(ops · arşiv:958)</sub>
- **[ÇEKİRDEK]** Davranış bayraklarını kurulum profiline KOYMA; `prisma/seed.ts` onları `upsert.update` ile ezer, iki yazar olmaz. · bekçi: `test_module_profile.ts, module-flags.test.ts` <sub>(backend · arşiv:2134, CLAUDE.md:101)</sub>
- **[ÇEKİRDEK]** Dev sunucusunu `pkill -f "tsx src/server.ts"` ile öldürme (kullanıcının :4000'ini düşürdü, iki ajan iki tur): yalnız kendi PID'in, paralel ajanlara ayrı port (tasarım §12-12). <sub>(superadmin-gizlilik-ayar-sifresi · CLAUDE.md:97)</sub>
- **[ÇEKİRDEK]** Donanım okuma yolu TEK: HAL. Eski `hardware.service.ts` mock'unu geri getirme. <sub>(kk1-idempotency-kuyruk · CLAUDE.md:223)</sub>
- **[ÇEKİRDEK]** Durum geçişini `findUnique → if → update` ile yazma; atomik claim kullan (`updateMany WHERE {id, beklenen-durum}`, count===0 → 409). · bekçi: `test_race_conditions.ts, test_wo_terminal_race.ts` <sub>(backend · CLAUDE.md:322, CLAUDE.md:171)</sub>
- **[ÇEKİRDEK]** Electron `FlagDef.numberField` tanımında iç ad `numberKey:` ZORUNLU. · bekçi: `test_feature_flag_contract.ts` <sub>(electron · CLAUDE.md:70)</sub>
- **[ÇEKİRDEK]** Electron `build.<mac|win>.artifactName` ASCII kalmalı; `${productName}` KULLANMA (ad `latest.yml` içinde URL'dir). · bekçi: `update-feed-url.test.ts` <sub>(electron, ops · CLAUDE.md:78)</sub>
- **[ÇEKİRDEK]** Elle okunan her id filtresini `readIdCondition`/`readFilterList`'ten geçir; ham okuma uuid'de P2007, düz string'de sessiz 0 satır üretir. · bekçi: `test_filter_multi_select.ts` <sub>(backend, electron · arşiv:296)</sub>
- **[ÇEKİRDEK]** Fason "açık + outstanding sevk" koşulunu elle kopyalama; tek kaynak `helpers/fason-open-dispatch.helper.ts`. · bekçi: `test_fason_open_dispatch_single_source.ts (AST)` <sub>(backend · CLAUDE.md:75)</sub>
- **[ÇEKİRDEK]** Fason kabulü yapan HER testin cleanup'ı `rollVariance.deleteMany` içermeli (RESTRICT FK). <sub>(backend · arşiv:437, arşiv:423)</sub>
- **[ÇEKİRDEK]** Genel "stoktan kaldır" tuşu yapma; stok kaydı silinmez, sebebi söyleyen TİPLİ olay yazılır ve buton o olayın adını taşır. · bekçi: `test_roll_variance.ts` <sub>(backend, electron · arşiv:203)</sub>
- **[ÇEKİRDEK]** Genel 'X / stoktan kaldır' tuşu YASAK — stok kaydı silinmez; sebebi söyleyen TİPLİ olay yazılır (iptal CANCELLED qtyOut=0 · fire SCRAP qtyOut=qtyIn · storno · sayım farkı · tersleme) ve düğme o olayın adını taşır. <sub>(tambur-finalize-kesim · CLAUDE.md:58)</sub>
- **[ÇEKİRDEK]** Genel 'X / stoktan kaldır' tuşu YASAK: stok kaydı silinmez, sebebi söyleyen TİPLİ olay yazılır (iptal/storno · fire · sayım farkı · tersleme) ve buton olayın adını taşır (Kesimi Geri Al · Topu İptal Et · İş Emrine Geri Al · Tümden Geri Al · Kaydı İptal Et · Fire). Ön seçim YOK. <sub>(top-duzeltme-iptal-fire-geri-alma · CLAUDE.md:58, CLAUDE.md:82)</sub>
- **[ÇEKİRDEK]** Geri yükleme etki raporunda ölçülemeyen UPDATE hacmine 0 YAZMA; "ölçülemedi" yaz. · bekçi: `test_backup.ts` <sub>(backend, ops · CLAUDE.md:281)</sub>
- **[ÇEKİRDEK]** Ham SQL'de yeni `now() AT TIME ZONE 'UTC'` yazımı EKLEME; kolonlar timestamptz olduğu için çıplak `now()` tercih edilir. · bekçi: `test_timestamptz_contract.ts` <sub>(backend, db · CLAUDE.md:285)</sub>
- **[ÇEKİRDEK]** Ham `npm run build:win` KULLANMA; bir önceki müşterinin yayın adresiyle derler ve hata SESSİZDİR. · bekçi: `electron-paketle.sh kapısı, update-feed-url.test.ts` <sub>(ops, electron · arşiv:958, CLAUDE.md:182)</sub>
- **[ÇEKİRDEK]** Ham `npm run build:win` ve elle `gradlew assembleRelease` YASAK. Panel yalnız `deploy/electron-paketle.sh <müşteri>` (derleme SONRASI paketteki `app-update.yml` adresi ARGÜMANLA kıyaslanır); tablet yalnız `scripts/build-apk.mjs` (önbellek siler, bundle'dan adresi geri okur; düşerse `.DOGRULANMADI`). · bekçi: `electron-paketle.sh gömülü adres kapısı; build-apk.mjs doğru` <sub>(surum-yayin · CLAUDE.md:182, CLAUDE.md:66)</sub>
- **[ÇEKİRDEK]** Jenerik `requireModule("x")` fabrikası YAZMA; middleware adı `require`+PascalCase(alan) olmalı, bekçiler adı AST/metin arar. · bekçi: `test_ticaret_regime_gate.ts, test_production_regime_gate.ts ` <sub>(backend · arşiv:2033, CLAUDE.md:315, CLAUDE.md:96)</sub>
- **[ÇEKİRDEK]** KEŞİF-İKİZ bloğuna PROJEYE ÖZGÜ hiçbir şey girmez: Electron'un kaynak güvenilirliği sırası (`bySourceRank`, mDNS ↔ tarama) blok DIŞINDA yaşar ve içeri `tieBreak` parametresiyle verilir (mobilde `via` alanı yok). · bekçi: `discovery.contract.test.ts ikiz metin kilidi` <sub>(kesif-cihaz-ag · CLAUDE.md:109)</sub>
- **[ÇEKİRDEK]** Kalite/özellik KODU bir kimliktir: ASCII yazılır (`TUP`, `TÜP` değil), karşılaştırma `toUpperCase()` ile — `toLocaleUpperCase("tr")` YASAK (i/İ dönüşümü eşleşmeyi sessizce bozar). · bekçi: `test_label_element_condition` <sub>(kalite-istasyon-yetenek · CLAUDE.md:44, CLAUDE.md:68)</sub>
- **[ÇEKİRDEK]** Kapsam anahtarlarına (`processingStatus` vb.) çoklu seçim ekleme; onlar filtre değil kapsamdır. · bekçi: `test_filter_multi_select.ts` <sub>(backend, electron · arşiv:296)</sub>
- **[ÇEKİRDEK]** Kapsam listelerini ELLE sayma ("üç yazma yüzeyi"); kapsamın yüklemi ekran değil YAZMA'dır ve AST tripwire ile ölçülür. · bekçi: `test_settings_password.ts (AST tripwire)` <sub>(backend · arşiv:2096)</sub>
- **[ÇEKİRDEK]** Kat (`foldType`) değerini kanonikleştirmeden yazma/filtreleme; ham değer filtrede sessizce 0 satır döndürür. · bekçi: `test_fold_type.ts, test_fold_catalog.ts` <sub>(backend · arşiv:80, CLAUDE.md:49)</sub>
- **[ÇEKİRDEK]** Kat alanında üçlü sözleşmeyi uygula: `undefined` DOKUNMA, `null` TEMİZLE, kod YAZ. · bekçi: `test_fold_edit_and_label.ts` <sub>(backend, electron · arşiv:192, CLAUDE.md:57)</sub>
- **[ÇEKİRDEK]** Klavye/fare geri hareketlerinde `preventDefault()` çağır; tarayıcının kendi geçmişi memory router'ı tanımaz. <sub>(electron · CLAUDE.md:238)</sub>
- **[ÇEKİRDEK]** Koşullu etiket elemanı karşılaştırmasında `toLocaleUpperCase("tr")` KULLANMA; koşul `QualityGrade.code` üzerinden çalışır. · bekçi: `test_label_element_condition.ts` <sub>(backend · CLAUDE.md:44)</sub>
- **[ÇEKİRDEK]** Kurşun bypass marker'ını BASE ön eki olmadan yazma (`KURSUN_BYPASS_FINISHED:UNASSIGNED:`); makine atfı uydurulmaz, `machineId=null` kalır. · bekçi: `test_kursun_bypass.ts` <sub>(backend · arşiv:121)</sub>
- **[ÇEKİRDEK]** Kısmi fason kabulünde kalanı BÜYÜK toptan dağıt (`resolveReturns`); tamamı fasonda kalan top dönüş listesine HİÇ GİRMEZ ve sıra deterministik olsun. · bekçi: `test_fason_partial_receive.ts` <sub>(backend, mobil · arşiv:437, CLAUDE.md:77)</sub>
- **[ÇEKİRDEK]** Makbuz iptalinde sapma defteri satırlarını SİLME; `reversedAt` damgala (append-only defter) ve tersleme adresi `sourceRefId` olsun. · bekçi: `test_roll_variance.ts, test_fason_partial_receive.ts` <sub>(backend, db · arşiv:437, arşiv:423)</sub>
- **[ÇEKİRDEK]** Manifest (`latest.yml` / OTA manifesti) EN SON yüklenir; ters sırada henüz yüklenmemiş dosyayı işaret eden manifest yayında kalır. <sub>(ops · arşiv:958)</sub>
- **[ÇEKİRDEK]** Manuel taşımada fason adımına giden topa `AT_SUBCONTRACTOR` YAZMA; mal üretimde bekler, çıkış ayrıca Fason Sevk'ten yapılır. · bekçi: `test_manual_move.ts, test_fason_dispatch_picker.ts` <sub>(backend · arşiv:404)</sub>
- **[ÇEKİRDEK]** Migration doğrulamasında `applied_steps_count` KULLANMA; 130 migration'ın 8'i meşru şekilde sıfırdır. · bekçi: `test_db_copy.ts, test_migration_hygiene.ts` <sub>(db, ops · CLAUDE.md:280)</sub>
- **[ÇEKİRDEK]** Mobil istemci brüt rakamı elle kurmasın (`rollCount + returnedCount`); backend brütleştikten sonra çift sayar. <sub>(mobil · arşiv:62)</sub>
- **[ÇEKİRDEK]** Modül kapısı tarayıcısının baktığı dosyalara dosya düzeyinde `let`/mutable sabit/`Date.now` koyma (gizli önbellek yasağı). · bekçi: `regime-gate-scan.ts (AST), test_ticaret_regime_gate.ts` <sub>(backend · arşiv:2033)</sub>
- **[ÇEKİRDEK]** Mutabakat bekçisinin bir bölümünü kırmızı diye DARALTMA; kapı görünürlüğü korur, düzeltmek ayrı bir iş kararıdır. · bekçi: `test_consistency.ts, test_consistency_derived.ts` <sub>(backend, db · arşiv:485, CLAUDE.md:81)</sub>
- **[ÇEKİRDEK]** Müşteri belgesinde kolon gösterimini blocklist ile kurma; opt-in allowlist kullan (`DocCol.defaultHidden` + `shown`). · bekçi: `docRows.test.ts` <sub>(backend, electron · CLAUDE.md:43)</sub>
- **[ÇEKİRDEK]** Müşteri dalı YOK (`feature/*` → `main`); `if (musteri === 'X')` fork'un ilk sinyalidir, YASAK; 'adnansahin'de yok' = 'bayrağı kapalı'. Hiçbir eski not müşteri koşulu gerekçesi olamaz. Fabrika paketi ve demo derlemesi `main`'den üretilir. <sub>(modul-bayraklari-profiller · CLAUDE.md:1, CLAUDE.md:96)</sub>
- **[ÇEKİRDEK]** OTA turunda `android.versionCode`a DOKUNMA; tablet onu kurulu APK sürümü sanar (`kuruluVersionCode()`) ve gerçek APK güncellemesini bir daha teklif etmez. · bekçi: `test_surum.mjs, yayinla-ota.mjs kapısı` <sub>(ops, mobil · CLAUDE.md:182)</sub>
- **[ÇEKİRDEK]** Paketleme kapısının beklenen müşteri kodunu `musteri.json`dan okuma; argümandan al, yoksa yanlış kodu ASLA yakalayamaz. · bekçi: `electron-paketle.sh` <sub>(ops · arşiv:958)</sub>
- **[ÇEKİRDEK]** Parti numarası üretiminde `pg_advisory_xact_lock(8022,…)` tx'in İLK ifadesi olsun; sonraya alınan kilit TOCTOU'yu kapatmaz. · bekçi: `test_batch_number_format.ts (§3)` <sub>(backend, db · CLAUDE.md:161)</sub>
- **[ÇEKİRDEK]** Partinin kimliği yalnız `Batch.id`'dir; `batchNumber` ile lookup YAPMA. · bekçi: `test_batch_number_format.ts` <sub>(backend · CLAUDE.md:161)</sub>
- **[ÇEKİRDEK]** Prisma sorgusunda `notIn: []` yazma (2026-08-15 saha çökmesinin kök nedeni). <sub>(backend · arşiv:375)</sub>
- **[ÇEKİRDEK]** Profil uygulamak için yeni yazma ucu AÇMA; tek kapı `PATCH /feature-flags` (kapı çoğaltma yasağı). · bekçi: `test_module_flags.ts, test_superadmin.ts` <sub>(backend · arşiv:2134)</sub>
- **[ÇEKİRDEK]** Profilleri `deploy/profiller/*.json` gibi paket dışı dosyaya koyma; TS sabiti kullan (`constants/module-profiles.ts`) — `deploy/` pakete girmiyor. · bekçi: `test_module_profile.ts` <sub>(backend, ops · arşiv:2134)</sub>
- **[ÇEKİRDEK]** REQUIRED modlu istasyon özelliği işaretlenmeden adım KAPANMAZ; SEÇİM tipli özellikte değer ZORUNLU. · bekçi: `test_station_property_mode.ts, test_property_value_selection` <sub>(backend · arşiv:307)</sub>
- **[ÇEKİRDEK]** React Native Paper `Card`'ı `onPress` ile kullanma; `Card.Content` dokunmayı yutar — tıklanacaksa `TouchableRipple` ile sar. <sub>(mobil · CLAUDE.md:289)</sub>
- **[ÇEKİRDEK]** Renk değişikliğinde uyum tamsa toplara DOKUNMA; kilit yalnız boyanmış topların HEPSİ döndüğünde uygulanır. · bekçi: `test_wo_color_change_lock.ts, test_wo_target_color_guard.ts` <sub>(backend · arşiv:423)</sub>
- **[ÇEKİRDEK]** Renk istasyon bazlı KISIT DEĞİL: rota adımı renk seçicisi tüm aktif kataloğu gösterir; `StationColor` deprecated — hiçbir karar/filtre ondan türemez (yetenek sayfası satırları listeler ve `setCapabilities` yazar, o kadar). Renk kısıtını geri EKLEME. · bekçi: `test_station_capability.ts (8b) + test_helpers.ts (kilit)` <sub>(rota-renk-ozellik-kapsama · CLAUDE.md:128)</sub>
- **[ÇEKİRDEK]** Route/controller katmanında `prisma` import ETME; iş mantığı serviste kalır (ince read uçları bile). <sub>(backend · CLAUDE.md:140)</sub>
- **[ÇEKİRDEK]** SQL fold fonksiyonunda ayırıcı sınıfı AÇIK yazılır, `\s` YASAKTIR: PostgreSQL'de `\s` ASCII dışında veritabanının ctype'ına bağlıdır (dev ICU ↔ saha C locale) ve aynı ad iki kurulumda farklı katlanır. JS ikiziyle BMP genelinde birebirlik bekçiyle ölçülür. · bekçi: `test_fold_contract §2b` <sub>(mukerrer-namefold · CLAUDE.md:84)</sub>
- **[ÇEKİRDEK]** Sack.notes yazma yoluna `touchWarehouseSackTx` guard'ı EKLEME; not bilinçli guard'sızdır, eklersen özellik 409'a düşer. · bekçi: `test_sack_notes.ts (§8a kontrol grubu)` <sub>(backend · CLAUDE.md:325, CLAUDE.md:43)</sub>
- **[ÇEKİRDEK]** Sebep listesini boş bırakma: Tambur çevrimdışı çalışır ve fire kararında sebep zorunludur — boş liste operatörü kilitler. · bekçi: `test_reason_presets.ts` <sub>(backend, mobil · arşiv:375)</sub>
- **[ÇEKİRDEK]** Serbest olmayan statüdeki (ör. `IN_PRODUCTION`) topu düzeltirken sebep ZORUNLU ve `roll:manual-adjust` ara. · bekçi: `test_roll_edit_unified.ts, test_manual_attributes_reason.ts` <sub>(backend · CLAUDE.md:126)</sub>
- **[ÇEKİRDEK]** Seri giriş ekranında (KK1) token'ı ref'te SABİTLEME — arka arkaya kuyruğa giren topları tek kayda çakıştırıp sessizce yutar (kopyanın aynadaki ikizi: eksik stok). · bekçi: `mobil/src/offline/entryAttempt.test.ts` <sub>(kk1-idempotency-kuyruk · CLAUDE.md:171)</sub>
- **[ÇEKİRDEK]** Sevk stornosunu iade sayma: storno belgeyi İPTAL eder, stok döner, iade defterine GİRMEZ. · bekçi: `test_shipment_undo_dispatch.ts` <sub>(backend · arşiv:251, CLAUDE.md:63)</sub>
- **[ÇEKİRDEK]** Sevki geri alırken tahsisi SİLME; `shippedQty` defterden türetilir ve yalnız DISPATCHED tahsisleri sayar. · bekçi: `test_shipment_undo_dispatch.ts` <sub>(backend · arşiv:241, arşiv:251, CLAUDE.md:63)</sub>
- **[ÇEKİRDEK]** Siparişe bağlı iş emrinde hedef renge DOKUNMA; orada renk siparişin şartıdır. · bekçi: `test_wo_target_color_guard.ts` <sub>(backend · arşiv:92)</sub>
- **[ÇEKİRDEK]** Sistem geneli ayar kategorilerini dar izinli kullanıcıya salt-okunur bile olsa GÖSTERME; `visibleSettingsCategories` süzer. · bekçi: `settings-surface.test.ts` <sub>(electron · CLAUDE.md:117)</sub>
- **[ÇEKİRDEK]** Sistem rolünü SERT SİLME; pasifleştir (`deleteTemplate`) — sert silme bir sonraki `pm2 restart`ta diriliş demektir. · bekçi: `test_role_template_catalog.ts` <sub>(backend · arşiv:287, CLAUDE.md:237, CLAUDE.md:66)</sub>
- **[ÇEKİRDEK]** Sistem sebebi `FASON_CEKME`'yi operatör listesine (`SCRAP_REASONS`) EKLEME. · bekçi: `test_fason_partial_receive.ts (P10), test_reason_presets.ts` <sub>(backend · CLAUDE.md:77)</sub>
- **[ÇEKİRDEK]** Süperadmin kimliğini JWT'ye KOYMA; `verifyToken`'ın tazelik okumasına tek kolon eklenir → `req.isSystemAccount`. · bekçi: `test_superadmin.ts, test_superadmin_visible.ts` <sub>(backend · arşiv:2057, CLAUDE.md:97)</sub>
- **[ÇEKİRDEK]** Süperadmin kurulum script'ini TTY'siz koşturma; kapı FAIL-LOUD olmalı (`process.stdin.isTTY` kontrolü `interaktif()`in ilk işi). · bekçi: `test_superadmin_provision.ts (§7 zaman aşımlı non-TTY sondas` <sub>(backend, ops · CLAUDE.md:105, CLAUDE.md:107)</sub>
- **[ÇEKİRDEK]** Süperadmin parolası/PIN'i ve ayar şifresi repoya, log'a, sürüm notuna ve audit diff'ine GİRMEZ. · bekçi: `test_settings_password.ts, test_superadmin.ts` <sub>(backend, ops · arşiv:2057, CLAUDE.md:301)</sub>
- **[ÇEKİRDEK]** Sürüm notu yazılmadan sürüm çıkarma; paketleme kapısı durdurur ve notun taslağını Claude yazar, kullanıcı onaylar. · bekçi: `check-surum-notlari.mjs` <sub>(ops · CLAUDE.md:182)</sub>
- **[ÇEKİRDEK]** Sır hijyeni: süperadmin parolası/PIN/TOTP ve ayar şifresi/hash repoya, log'a, sürüm notuna, audit diff'ine, yanıt gövdesine GİRMEZ; `SECRET_FIELDS` yalnız `changes`i maskeler (oldData/newData/payload HAM) → audit yükü çağıranda temiz. CLAUDE.md'ye yalnız seed/dev kimliği yazılır. · bekçi: `scripts/test_superadmin.ts (sır hijyeni AST) + test_settings` <sub>(yetki-izin-rol · CLAUDE.md:301, arşiv:2057, arşiv:2096)</sub>
- **[ÇEKİRDEK]** Tasarım metnindeki `modul.*` kavramsal adını KODA sokma; DB anahtarları `finance.enabled` kalıbındadır. · bekçi: `module-flags.test.ts, test_module_flags.ts` <sub>(backend · CLAUDE.md:96)</sub>
- **[ÇEKİRDEK]** Termini olmayan siparişi zamanında-teslim oranına KATMA ama listeden de gizleme. · bekçi: `test_order_leadtime.ts` <sub>(backend · arşiv:333)</sub>
- **[ÇEKİRDEK]** Test fason firmalarını seed'den çözme; `scripts/fixture-subcontractor.ts` ile üret ve paylaşıldığı için SİLME. <sub>(backend · CLAUDE.md:357)</sub>
- **[ÇEKİRDEK]** Tolerans/eşik ayarlarında `<=0 → null` kalıbını KULLANMA; 0 = 'tolerans yok', null = 'varsayılan' — zıt davranışlar. · bekçi: `test_fason_partial_receive.ts` <sub>(backend · arşiv:437, arşiv:423, CLAUDE.md:77)</sub>
- **[ÇEKİRDEK]** Türkçe fold ifadesinde ayırıcı sınıfını AÇIKÇA yaz; `\s` KULLANMA (saha sunucusu C locale). · bekçi: `test_fold_contract.ts (§2b)` <sub>(db · CLAUDE.md:84)</sub>
- **[ÇEKİRDEK]** Uygulama guard'ı OLMAYAN tabloya (customer_branches · label_templates · permission_templates · subcontractor_categories) sed EKLEME: orada mükerrer ad bugün MEŞRUDUR, sed yeni bir KISIT olur — iş kararıdır. `stations` da muaf (tarihsel-mükerrer senaryosu + damgalanmamış fixture'lar). · bekçi: `test_master_data_name_dup §10 (muaf gerekçeleri)` <sub>(mukerrer-namefold · KOD:20260831120000_namefold_sed_kalan_ta)</sub>
- **[ÇEKİRDEK]** Uzman modu (RAW_HTML) ŞABLON DİLİ DEĞİL, metin ikamesidir: {{alan}} + {{#liste}}…{{/liste}} (iç içe YOK). Koşul/ifade/JS EKLENMEZ — ihtiyaç doğarsa katalogda yeni ALAN (config/traveler-card-fields.ts). Bilinmeyen anahtar boş basar, baskıyı DURDURMAZ. (Emsal: handlebars yerine kendi motor.) <sub>(belge-etiket-sablon · CLAUDE.md:152, CLAUDE.md:152)</sub>
- **[ÇEKİRDEK]** Uzman modu bir ŞABLON DİLİ DEĞİL metin ikamesidir (`{{alan}}` + `{{#liste}}…{{/liste}}`, iç içe YOK); koşul/ifade/JS EKLENMEZ — ihtiyaç doğarsa çözüm `config/traveler-card-fields.ts` kataloğuna yeni ALAN, değer üretimi sunucuda kalır. <sub>(refakat-karti · CLAUDE.md:152)</sub>
- **[ÇEKİRDEK]** Uzun-cache nginx kurallarında `add_header … always` KULLANMA; `always` başlığı 404'e de eklenir ve Cloudflare o 404'ü bir hafta tutar. · bekçi: `test_mobile_update.ts (nginx conf kontrolü)` <sub>(ops · arşiv:958, arşiv:1117, CLAUDE.md:93)</sub>
- **[ÇEKİRDEK]** Yedek portta kimlik doğrulamasını atlama; `{"status":"UP"}` TeksERP'e özgü değildir (`identityRequiredForPort`). · bekçi: `test_discovery_identity.ts` <sub>(electron, mobil · arşiv:2575, CLAUDE.md:110)</sub>
- **[ÇEKİRDEK]** Yeni `FabricProperty`'yi `stationIds` olmadan yaratma; bağ AYNI insert'te kurulur (`nestedCreateFields`), boş dizi 400'dür. · bekçi: `test_property_station_binding.ts` <sub>(backend, electron · CLAUDE.md:129)</sub>
- **[ÇEKİRDEK]** Yeni bir `kind === "PROCESS_QC"` karşılaştırması YAZMA; kalite yeteneği ikizinden birini kullan (`stepCanApplyQuality` ya da `QUALITY_STATION_WHERE`). · bekçi: `test_station_quality_capability.ts` <sub>(backend · CLAUDE.md:174)</sub>
- **[ÇEKİRDEK]** Yeni izin kodu için migration yazma; `20260801020000_kursun_bypass_permission_catalog` emsal olmaktan çıktı — katalog koda, atama panele. · bekçi: `test_permission_catalog.ts` <sub>(backend · CLAUDE.md:191)</sub>
- **[ÇEKİRDEK]** Yeni kolon eklerken geriye dönük doldurma YAPMA; eski satırlar olduğu gibi kalır. <sub>(db · arşiv:230)</sub>
- **[ÇEKİRDEK]** `.env`de kalan `SUPERADMIN_*` satırları için boot uyarısında YALNIZ ANAHTAR ADINI bas, değeri ASLA. · bekçi: `test_superadmin_provision.ts (§6, §8)` <sub>(backend, ops · arşiv:2204, CLAUDE.md:105)</sub>
- **[ÇEKİRDEK]** `CANCELLED` / `SUPERSEDED` iş emrinde manuel taşımayı REDDET (409 + önizlemede `woBlocked`). · bekçi: `test_manual_move.ts, test_wo_terminal_guard.ts` <sub>(backend · CLAUDE.md:127)</sub>
- **[ÇEKİRDEK]** `CLIENT_IP_HEADER` app-wide OKUNMAZ; yalnız uzak istekte geçerlidir (`clientIpHeaderRemoteOnly`, `REMOTE_PORT`ten türer) — aksi hâlde LAN'daki biri `CF-Connecting-IP` uydurup giriş kilidini VE hız sınırını etkisizleştirir. · bekçi: `test_remote_access_guard.ts:266` <sub>(kesif-cihaz-ag · CLAUDE.md:95)</sub>
- **[ÇEKİRDEK]** `ClientType.WEB` ayrı bir değerdir; istemci ayrımını `!== "mobile"` ile YAZMA — telefon tarayıcısı masaüstü oturumunu düşürür. İkinci faktör (TOTP) `issueToken`den ÖNCE koşar; üç hata kodu üç AYRI HTTP statüsü döner ve giriş kilidi yalnız 401'i sayar. · bekçi: `Teks-Erp/scripts/test_remote_access_guard.ts` <sub>(kesif-cihaz-ag · CLAUDE.md:95)</sub>
- **[ÇEKİRDEK]** `ClientType.WEB` ayrı bir oturum türüdür; istemci ayrımını `!== "mobile"` ile YAZMA — `clientType` opsiyoneldir ve `undefined` tarihsel olarak MOBİL demektir, negatif yazım alan göndermeyen eski mobil istemcileri masaüstü sayıp 403'e düşürür. <sub>(kucuk-kumeler · CLAUDE.md:95)</sub>
- **[ÇEKİRDEK]** `FASON_CEKME` sistem sebebi `SCRAP_REASONS`'a / `RECORD_CORRECTION_REASONS`'a EKLENMEZ: `validateVarianceReason` kabul eder, `reasonsForKind` döndürmez — operatörün Tambur fire listesine sızmasın. · bekçi: `scripts/test_roll_variance.ts` <sub>(fason · CLAUDE.md:77)</sub>
- **[ÇEKİRDEK]** `QUALITY_STATION_WHERE` spread edilirken aynı kapsamda ikinci bir `OR:` YAZMA; biri sessizce kaybolur. · bekçi: `test_station_quality_capability.ts (§3)` <sub>(backend · arşiv:2116)</sub>
- **[ÇEKİRDEK]** `RAW_STOCK` kapsamını 'yarı mamul hariç' diye DARALTMA; mobil Hızlı İş Emri top seçicisi onu kullanır, daraltma tabletteki tek yolu kapatır. · bekçi: `test_semi_finished_surfaces.ts` <sub>(backend, electron, mobil · arşiv:1011, CLAUDE.md:92)</sub>
- **[ÇEKİRDEK]** `ReasonPreset.code` rapor anahtarıdır; doğuşta yazılır ve ASLA düzenlenmez — yalnız `label` serbesttir. · bekçi: `test_reason_presets.ts` <sub>(backend, electron · arşiv:365, arşiv:375, CLAUDE.md:73)</sub>
- **[ÇEKİRDEK]** `ScreenEntry.modul` alanını opsiyonel bırakma; her ekran `ModulKey` · `cekirdek:*` · `planlanan:*` ön ekli bir aidiyet beyanı taşır. · bekçi: `test_screen_catalog.ts, screen-view.test.ts` <sub>(electron · arşiv:2134, CLAUDE.md:101)</sub>
- **[ÇEKİRDEK]** `addressPreferenceRank` bir SIRALAMADIR, ELEME DEĞİL: 172.16/12 (Hyper-V/Docker), 100.64/10 (Tailscale), loopback ve link-local adaylarını listeden DÜŞÜRME — o adresten gerçekten hizmet veriliyor olabilir. Sıra: LAN → ad → 172.16/12 → 100.64/10 → 127/8 → 169.254/16. · bekçi: `Electron/src/test/discovery-logic.test.ts + mobil/src/lib/di` <sub>(kesif-cihaz-ag · CLAUDE.md:109)</sub>
- **[ÇEKİRDEK]** `assertWoAtStepKind(PROCESS_QC)`'e DOKUNMA; `roll-step.helper` Tambur ile paylaşılıyor (Faz B açık). · bekçi: `test_station_quality_capability.ts` <sub>(backend · CLAUDE.md:100)</sub>
- **[ÇEKİRDEK]** `autoUpdater`a modül gövdesinde DOKUNMA; o bir getter, ilk erişimde Electron `app`ine dokunur. <sub>(electron · CLAUDE.md:78)</sub>
- **[ÇEKİRDEK]** `git pull` sonrası senkronizasyon sırasını atlama; eksik adım sessiz bozulma üretir. <sub>(ops · CLAUDE.md:101)</sub>
- **[ÇEKİRDEK]** `installationId` NULL olan aday BAŞKA HİÇBİR adayla birleşmez — kendi `addr:<host>:<port>` anahtarında kalır. Kimliksiz iki sunucuyu 'aynı' saymak gerçek ikinci sunucuyu listeden SİLER; eski adres bazlı davranış orada birebir korunur. · bekçi: `Electron/src/test/discovery-logic.test.ts (33)` <sub>(kesif-cihaz-ag · CLAUDE.md:109)</sub>
- **[ÇEKİRDEK]** `isDesktopClient`te `!== "mobile"` YAZMA; `clientType` opsiyoneldir ve `undefined` tarihsel olarak MOBİL demektir. · bekçi: `test_p2_auth.ts, test_login_methods.ts` <sub>(backend · arşiv:1791, CLAUDE.md:95)</sub>
- **[ÇEKİRDEK]** `minVersion`'ı 'eski istemci etkilenmez' olan değişikliklerde YÜKSELTME; her sürümde artırmak güncellemeyi indirememiş makineyi üretim dışı bırakır. · bekçi: `test_client_policy.ts` <sub>(backend, ops · CLAUDE.md:30)</sub>
- **[ÇEKİRDEK]** `orderBy: { batchNumber }` HER İKİ REJİMDE DE YASAK: günlük kalıpta sözlüksel ≠ sayısal (P05082610 < P0508262), kısa kalıpta numara sardığı için sayısal sıra da yenilik sırası değildir. Parti listeleyen her yer `createdAt` ile sıralar. · bekçi: `scripts/test_batch_number_format.ts §5 (src/ regex taraması ` <sub>(parti-no-batch · CLAUDE.md:161)</sub>
- **[ÇEKİRDEK]** `orderBy: { batchNumber }` her iki numara rejiminde de YASAK; ne sözlüksel ne sayısal sıra 'yenilik' sırasıdır. · bekçi: `test_batch_number_format.ts (§5)` <sub>(backend · CLAUDE.md:161)</sub>
- **[ÇEKİRDEK]** `quality.gradeRequiredEnabled` kapsamı DAR: fason kabulü ve son-adım finalize DIŞARIDA; kapsam genişletmek sıfır-fark ihlalidir ve bayrak açıldığı gün patlar. · bekçi: `scripts/test_quality_batch_flags.ts` <sub>(fason · CLAUDE.md:106)</sub>
- **[ÇEKİRDEK]** `token_set_ratio` KULLANILMAZ — alt-küme adları %100 verir ('MODA' ⊂ 'MODA ANKARA'). Renkte `hex` kimlik alanı DEĞİLDİR (canlıda 7 meşru `#ffffff`). <sub>(mukerrer-namefold · CLAUDE.md:80)</sub>
- **[ÇEKİRDEK]** `tx.*` çağrılarını `Promise.all` ile paralelleştirme; pg adapter tek connection'ı seri çalıştırır. <sub>(backend · CLAUDE.md:248)</sub>
- **[ÇEKİRDEK]** `unlinkOrderLine`'da son bağı REDDETME; aynı tx'te iş emri tipini ORDER→STOCK çevir ve yanıtta `typeChanged` döndür. · bekçi: `test_workorder_order_link.ts` <sub>(backend, electron · arşiv:414)</sub>
- **[ÇEKİRDEK]** `update`'te `stationIds` gönderilmemişse istasyon bağına DOKUNMA (ad düzenlemesi bağı silmesin). · bekçi: `test_property_station_binding.ts` <sub>(backend · CLAUDE.md:129)</sub>
- **[ÇEKİRDEK]** nginx uzun-cache (`.exe`/`.apk`/OTA varlık) kurallarında `add_header … always` YASAK — 404'e de eklenir, Cloudflare 404'ü bir hafta tutar; ikinci hat `error_page 404 → no-store`. Yapılandırma REPODA (`deploy/guncelleme-sunucusu/`), sunucuda elle düzenlenmez. · bekçi: `yok (default.conf yorumu)` <sub>(surum-yayin · CLAUDE.md:93, CLAUDE.md:88)</sub>
- **[ÇEKİRDEK]** Çekme/fire farkı fasoncunun açık bakiyesine (`openQty`) GİRMEZ; bakiye fasonda bekleyen, düşülmemiş metrajdır. · bekçi: `test_fason_partial_receive.ts` <sub>(backend · arşiv:437, CLAUDE.md:77)</sub>
- **[ÇEKİRDEK]** Çoklu seçim HER filtreye uymaz: iki değerli NOT NULL enum (`destination`, `WorkOrder.type`) TEKİL — `destination` çoklu seçilince listShipments DirectShipment'ları union'dan DÜŞÜRÜR; nullable alanda (foldType) çoklu anlamlı. Kapsam anahtarlarına (processingStatus, rollScope, shipmentScope, rollKind, scope, status, invoiced, hasReturns) dokunulmaz — dışlayan if/else, OR yok. <sub>(filtre-liste-arama · CLAUDE.md:67, CLAUDE.md:46)</sub>
- **[ÇEKİRDEK]** Özellik/değer kodu ASCII yazılır (`TUP`, `TÜP` değil); karşılaştırma `toUpperCase()`, `toLocaleUpperCase("tr")` YASAK (2026-08-02 `showIf` dersinin ikizi). · bekçi: `test_fold_catalog.ts (24)` <sub>(rota-renk-ozellik-kapsama · CLAUDE.md:68)</sub>
- **[ÇEKİRDEK]** İade sorgusu ayrı tx'te koştuğu için sonucu canlı id kümesiyle DEDUP et; etmezsen brüt rakam çift sayar. · bekçi: `test_shipment_detail_gross.ts` <sub>(backend · CLAUDE.md:47)</sub>
- **[ÇEKİRDEK]** İfade tabanlı UNIQUE eklerken `EXPRESSION_UNIQUES` envanterine girdiyi (+predicate) ZORUNLU olarak ekle; yoksa şema drift bekçisi index'i fazlalık sayar. · bekçi: `test_db_invariants.ts, test_schema_drift.ts` <sub>(db · CLAUDE.md:84)</sub>
- **[ÇEKİRDEK]** İptal edilen sipariş kalemini `quantity` ile sayma; `shipped` ile say — aksi hâlde sipariş sonsuza dek PARTIAL_SHIPPED kalır. · bekçi: `test_order_line_cancel.ts, test_order_line_scope_single_sour` <sub>(backend · arşiv:1521)</sub>
- **[ÇEKİRDEK]** İstek gövdesini elle kuran istemci katmanı sessiz bir allowlist'tir; Zod `z.object` tanımadığı anahtarı sessizce siler — alanı iki uçta da sözleşmeye ekle. <sub>(backend, electron, mobil · arşiv:134)</sub>