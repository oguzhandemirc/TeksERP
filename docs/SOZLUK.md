# Sözlük — TeksERP domain terimleri

> Tekstil ve bu sistemin dili. Anlam koddan/şemadan doğrulandı (anlama turu 2026-09-05); `BELİRSİZ:` ile başlayanlar teyit bekler.

| Terim | Anlam | Kod karşılığı | Karıştırılan |
|---|---|---|---|
| **'İçerik donuk · sunum canlı' —** | 'İçerik donuk · sunum canlı' — belge ailesinin ortak ilkesi: hukuki/tarihsel kayıt (kart planı, irsaliyedeki ad) snapshot'a DONAR; kâğıdın çizimi (şablon, punto, sayfa boyutu, ad kolonu) her baskıda canlı. Rejim de donarsa ayar eski belgeye ulaşmaz; içerik de canlı olursa geçmiş değişir. | `` |  |
| **A1 / 2. kalite (A1_STOCK)** | Satılabilir ikinci kalite stok statüsü. Fire DEĞİLDİR — sevk edilebilir; çuvalda 1. kalite topla birlikte bulunabilir (storno bu yüzden preShipStatus okur). | `RollStatus.A1_STOCK` | SCRAP (fire) — A1 satılabilir, fire değil |
| **Adım (WorkOrderStep)** | İş emrinin bir istasyondaki somut adımı; durumu PENDING/ACTIVE/COMPLETED/SKIPPED. Adım COMPLETED olması plan değişikliği kilidi DEĞİLDİR. | `WorkOrderStep / StepStatus` | RouteStep — o şablon, WorkOrderStep canlı adımdır |
| **Alias (müşterideki ad)** | Müşterinin bizim ürün/renk için kullandığı isim (bizde PATOS, müşteride AKTOS). Etikette ve sevk belgesinde 'müşterideki ad' rejiminde basılabilir. | `CustomerItemAlias / CustomerColorAlias / OrderLine.customerItemName` | Gruplama anahtarı BİZİM adımız KALIR — alias'a göre gruplamak iki ürünü sessizce |
| **Atomik claim** | Durum geçişlerinin yazım deseni: updateMany WHERE {id, beklenen-durum} + count===0 → 409. findUnique→if→update (check-then-act) YASAKTIR. | `updateMany claim deseni` | Advisory kilit — claim satır bazlı, advisory kilit sayaç/sıra üretimi içindir |
| **Ayar şifresi** | Ayar yazan uçların önündeki İKİNCİ kapı: X-Settings-Password başlığıyla gelir, hash security.settingsPasswordHash'te durur. Hash yoksa kapı UYUR. | `SETTINGS_PASSWORD_HASH_KEY / requireSettingsPassword` | Kullanıcı parolası — bu kişiye değil EKRANA aittir; süperadmin muaftır |
| **Açık kumaş (RollForm.ACIK)** | Rulo yapılmamış, katlanmamış kumaş parçası. Tambur DIŞI bir istasyondan çıkan / fason dönüşü olan / ham üretime giren toplar bu formdadır ve genelde barkodsuz doğar. | `RollForm.ACIK` | TOP formu — TOP ⟺ Tambur'dan geçti; ayrım fizikseldir, kalite/bitmişlik demek DE |
| **Barkod (Roll.barcode)** | Sunucunun sıralı ürettiği fiziksel etiket kodu (`T`+GGAAYY+`H|F`+sıra, ör. `T230926H0001`). KK1 girişinde ve Tambur kesim çıktısında basılır; açık kumaş Roll'larında NULL kalır. | `Roll.barcode / RollBarcodeCounter` | clientToken — o idempotency anahtarıdır, barkod fiziksel etikettir |
| **Bekçi (guard script)** | Bir kararı MEKANİK olarak doğrulayan salt-okunur test script'i (Teks-Erp/scripts/test_*.ts). Kararın yanına 'negatif sonda' ile kırmızı verdiği doğrulanır. | `Teks-Erp/scripts/test_*.ts` | Birim test — bekçi çoğu zaman CANLI DB'ye karşı da koşar (ör. test_consistency) |
| **Bitmiş depo (WAREHOUSE)** | Tambur/son adım sonrası satışa-sevke hazır top statüsü. Depo bir istasyon değil, tartı/paket öncesi BEKLEME statüsüdür. | `RollStatus.WAREHOUSE` | Warehouse modeli (fiziksel depo) — isim benzer, biri statü biri lokasyon |
| **Brüt sevk rakamı** | Fiş/irsaliye/liste/muhasebe ekranlarında sevk metrajı iade DÜŞÜLMEDEN basılır; iade ayrı bir belgeyle (RETURN_DISPATCH) kapanır. | `getDispatchReport / attachTotals / getShipmentById` | Net sevk — istemci brütü elle kurarsa ÇİFT sayar |
| **Cari (CariAccount)** | Müşteri/fason kartının MUHASEBE yüzü: vergi dairesi, vade, risk limiti. Lazy açılır (ilk fatura/tahsilat anında); customerId XOR subcontractorId. | `CariAccount / CariKind / CariBalance` | Customer/Subcontractor — onlar operasyon kartlarıdır |
| **Cihaz (Device) ve eşleştirme** | Sahada makinenin yanında duran tablet. Allowlist modeli: tablet announce eder → PENDING → yönetici onaylar+makineye atar → APPROVED. Bayrak kapalıysa doğrudan APPROVED doğar. | `Device / DeviceStatus / devicePairingRequired` | PeripheralDevice — o yazıcı/kantar gibi çevre donanımıdır |
| **currentQty** | Karnelerde GERİYE DÖNÜK DÜZELTME (restatement) BİLİNÇLİDİR: depo topu sonradan kesilirse ebeveyn üretildiği dönemin karnesinden düşer (metrajı çocuklarında), kalite sonradan 'Düzelt' ile değişirse eski dönem yeni kaliteyle okunur. Metraj currentQty'dir — finalize anındaki değer saklanmıyor. | `currentQty` |  |
| **deadQty** | ÖLÜ STOK = ESKİ **VE** SİPARİŞSİZ (kesişim); ikisinden biri tek başına ölü stok değildir (bekçi ölçümü: yalnız eskiye +400, yalnız siparişsize +500, doğrusu +300). Siparişsizlik SPEC bazındadır — hangi FİZİKSEL topun karşılıksız olduğu iddia edilmez. | `deadQty, DEAD_STOCK_DAYS` |  |
| **Depo (Warehouse) ve depo hareketi** | Warehouse fiziksel stok lokasyonudur (istasyon DEĞİL). WarehouseMovement append-only KONUM defteridir; statü terfisi (STOCK→WAREHOUSE) satır YAZMAZ. | `Warehouse / WarehouseMovement / WarehouseTransfer` | RollMovement — o iş emri ADIMINA giriş/çıkıştır, depo transferi oraya sığmaz |
| **Devere / çözgü / haşıl (planlanan)** | Dokuma hazırlık işlemleri (levend/çile/bobin aktarma, çözgü, haşıl). Bu fabrikanın rotasında YOK; sisteme yeni mimari olarak değil istasyon kataloğuna istasyon + rotaya adım olarak girerler. | `` | Fason adımı — devere iç istasyondur; dokuma diliminin tek tasarım işi 'top TEZGA |
| **Doğrudan sevk (DirectShipment)** | Malın fabrikaya dönmeden fason firmasından doğrudan müşteriye sevkedilmesi. Çuval/Shipment modeline hiç girmez, roll-bazlı ayrı bir kayıttır. | `DirectShipment / SubcontractorDirectShipAllocation` | Shipment (çuval sevkiyatı) — ikisi 'Sevkiyatlar' listesinde birleşik görünür ama |
| **Ekstre / cari hareket (CariTransaction)** | Cari hareket defteri — APPEND-ONLY. debit/credit AYRI kolonlardır; UPDATE/DELETE yoktur, düzeltme daima TERS SATIRDIR (storno). | `CariTransaction / CariTxnSource` | CariBalance — o denormalize özet, kaynak bu defterdir |
| **En (width)** | Kumaşın genişliği, CM cinsinden. KK1 girişinde ölçülür; sipariş satırında ve iş emrinde de hedef en alanı vardır. | `Roll.width / OrderLine.width / WorkOrder.width` | Metraj — en cm, metraj m; ikisi farklı boyut |
| **Etiket (LabelTemplate / LabelKind)** | Basılan fiziksel etiketin şablonu. Dört bağlam: ROLL_RAW (KK1 ham), ROLL_FINISHED (Tambur sonrası bitmiş), SWATCH (kartela), SACK (çuval; barkod = sackNo). | `LabelTemplate / LabelKind / LabelTemplateVariant` | Refakat kartı — o malla gezen operasyon kâğıdı, etiket topun kimliğidir |
| **FabricPropertyValue.code** | Katalog değerinde `code` KİMLİKTİR, `label` serbest: satırı yeniden adlandırmak ile yeni değer eklemek farklı işlerdir (TUP→'6 Kat' vakası); tablet/filtre/metre eşleşmesi kodla çalışır. | `FabricPropertyValue.code` |  |
| **Fason / Subcontractor** | Dışarıda işlem yapan firma (boyahane, yıkama, zımpara). Müşteriden AYRI bir domain ve ayrı tablodur; bir firma her ikisiyse iki kayıt açılır. | `Subcontractor / SubcontractorCategory` | Customer — aynı firma olsa bile ayrı kayıt |
| **Fason kabul / makbuz (receipt)** | Fasondan dönen malın kabulü: orijinal toplar SUBCONTRACTOR_CONSUMED ile emekliye ayrılır, makbuz üzerinden yeni açık-kumaş Roll'lar doğar (entrySource=SUBCONTRACTOR_RETURN). | `SubcontractorReceipt / SubcontractorReceiptItem` | Kısmi kabul — orada top TÜKETİLMEZ |
| **Fason sevk (dispatch)** | Topların fason firmasına çıkışı; kalemleri SubcontractorDispatchItem, belgesi SUBCONTRACTOR_DISPATCH irsaliyesidir. Top statüsü AT_SUBCONTRACTOR olur. | `SubcontractorDispatch` | Manuel taşıma — fason adımına taşıma AT_SUBCONTRACTOR YAPMAZ, çıkış ayrıca Fason |
| **Finalize** | Rotanın SON adımının topu final ürüne çekmesi: statü kaliteden çözülür (varsayılan WAREHOUSE), form otomatik yazılır, barkodsuz satılabilir topa barkod verilir. | `finalizeRollsAtLastStep` | Tambur — finalize Tambur'a özel DEĞİLDİR; PRODUCED limbosu kaldırıldı |
| **Fire (SCRAP)** | 'Mal vardı, gitti' kararı: gerçek fiziksel hurda. Stok DÜŞER, hareket qtyOut=qtyIn ile kapanır, fire oranı raporuna girer ve geri alınamaz. | `RollStatus.SCRAP / POST /rolls/:id/scrap` | İptal (CANCELLED) — 'hiç olmamalıydı'; fire raporuna GİRMEZ |
| **Fiyat (ItemPrice)** | Kalem fiyatı: kart varsayılanı (customerId NULL) + müşteri istisnası, yön (alış/satış) ve para birimi boyutlarıyla. Item'a fiyat kolonu EKLENMEDİ. | `ItemPrice / PriceKind` | InvoiceLine.unitPrice — belgeye DONMUŞ fiyat; ItemPrice canlı listedir |
| **Giriş kaynağı (entrySource)** | Topun sisteme NEREDEN girdiği: SUPPLIER_RECEIPT, MANUAL_ENTRY, TAMBUR_MANUAL, TAMBUR_SPLIT, SUBCONTRACTOR_RETURN, PURCHASE_RECEIPT, SEMI_FINISHED. Raporlama ayrımı için kritiktir. | `RollEntrySource` | itemType 'ne olduğunu', entrySource 'nereden geldiğini' söyler |
| **Ham stok** | Hiçbir iş emri adımına bağlı olmayan, status=STOCK ve entrySource ≠ SEMI_FINISHED olan toplar (envanterin 'Ham Stok' sekmesi). | `rollScope='RAW_STOCK_PURE'` | rollScope='RAW_STOCK' — o BİLEREK geniştir (ham + yarı mamul birleşimi), mobil o |
| **Hata (RollError)** | Top üzerinde tespit edilen kumaş hatası: Kurşun/QC2'de açılır, Tambur kararıyla kapanır (isProcessed=true, actionTaken CUT/NO_CUT). Tipi DefectType kataloğundan seçilir + snapshot'lanır. | `RollError / RollErrorAction / DefectType` | RollVariance — o metraj sapması, RollError kalite kusuru |
| **İade (RollReturn)** | Müşteriden geri dönen topun append-only kaydı. Sevk muhasebesine (shippedQty / SackAllocation) DOKUNMAZ; top SHIPPED→WAREHOUSE çekilir. | `RollReturn / PrintedDocType.RETURN_DISPATCH` | Storno; ayrıca ReturnReason (iade nedeni kataloğu) ile karıştırılmamalı |
| **İdempotency / clientToken** | İstemcinin ürettiği tekrar-koruma anahtarı: timeout-retry'de mükerrer kayıt doğmaz. Token MANTIKSAL DENEME başına bir kez üretilir ve yalnız sonucu BELİRSİZ bırakan hatada (ağ/timeout/5xx) yapışır. | `Roll.clientToken / Order.clientToken / WorkOrder.clientToken` | Barkod — dedup görevini artık clientToken yapar; kesin 4xx'te token YAPIŞMAZ |
| **İplik (Yarn)** | Kg bazlı ayrı stok modeli. YarnStock kalem×depo denormalize bakiyesi, YarnMovement append-only defter; miktar hep pozitif, yönü YarnMovementKind söyler. | `YarnStock / YarnMovement / ItemType.YARN` | Roll (kumaş) — iplik top olarak değil kg olarak izlenir |
| **İptal (CANCELLED)** | 'Bu kayıt hiç olmamalıydı' kararı: kayıt geri alınır, stok DÜŞMEZ, storno qtyOut=0 ile yazılır, fire oranına girmez. Geri alınabilir. | `RollStatus.CANCELLED / Roll.cancelReasonCode` | Fire (SCRAP); ayrıca çuvalda 'iptal' sevkiyat iptalinden farklıdır |
| **İrsaliye / donmuş belge (PrintedDocument)** | Bir sevk olayının versiyonlu resmi belgesi: içerik + şablon + firma künyesi freeze anında snapshot'a donar. ACTIVE / SUPERSEDED / VOIDED. | `PrintedDocument / PrintedDocType / PrintedDocStatus` | Manifest (çeki listesi) — o iş emri baskısıdır, resmi sevk belgesi değil |
| **İstasyon (Station)** | Üretim noktası; domain rolü StationKind (RAW_QC/PROCESS_QC/TAMBUR/SUBCONTRACTOR/SHIPPING/OTHER) ile taşınır. Depo bir istasyon DEĞİLDİR. | `Station / StationKind` | Warehouse — depo stok lokasyonudur, istasyon üretim noktası |
| **İstasyon yeteneği** | İstasyonun ne uygulayabildiği: appliesColor (renk), StationProperty (özellik), appliesQuality (kalite). Renk kısıtı 2026-08-02'de KALDIRILDI (StationColor deprecated), özellik kısıtı KALDI. | `Station.appliesColor / appliesQuality / StationProperty` | StationColor — tablo duruyor ama okuyan kod YOK |
| **İş emri (WorkOrder)** | Üretim emri; numarası İE+GGAAYY+NNNN. YALNIZ üretimi yönetir (istasyonlar, kurşun/QC2, Tambur); tartı/paket/sevkiyat ayrı domaindir. | `WorkOrder / WorkOrderType / WorkOrderStatus` | Parti (Batch) — WO üretim emri, parti aynı anda giren top grubudur |
| **K18_DEAD_STATUSES** | Üç statü kümesi ÜÇ AYRI soru, elle kopyalanmaz: K18_DEAD_STATUSES (SCRAP YOK — 'lane/etikette göster', 'parti açık mı') · CHILD_CANCELABLE_STATUSES (SCRAP VAR — Tambur çocuğu kesimle geri alınır) · STATUS_GROUPS.ARCHIVE (4 tüketilmiş + CANCELLED + SCRAP — arşiv sekmesi). | `K18_DEAD_STATUSES, CHILD_CANCELABLE_STATUSES` |  |
| **Kalite (QualityGrade)** | Kalite derecesi kataloğu (1.KALITE, A1, FIRE…). Kod topun satırına snapshot olarak yazılır; targetStatus alanı topun finalize'da alacağı RollStatus'ü belirler. | `QualityGrade / Roll.qualityGradeId` | Kalite NULLABLE'dır: yalnız kalite istasyonları belirler, kalitesiz top WAREHOUS |
| **Kapanış dispozisyonu** | İş emri manuel kapatılırken istasyonda kalan HER top için verilen karar: STOCK, WAREHOUSE, A1_STOCK, SCRAP, CANCELLED veya TRANSFER (yeni WO klonuna devir). | `WO_CLOSE_DISPOSITION audit / WO_CLOSE_<ACTION> movement notu` | Hard-block yalnız FASON'dadır (mal fiziksel olarak dışarıda) |
| **Kartela / Swatch** | Bitmiş toptan üretilen küçük numune/örnek kart. Kartela firmasına top gönderilir, fasonda N adet Swatch olarak döner; kaynak top KARTELA_CONSUMED ile kapanır. | `Swatch / KartelaDispatch / KartelaReceipt` | Üretim fasonu — kartela fasonu WO'suz ayrı akıştır |
| **Kartelalık (markedForKartela)** | Tambur finalize'da işaretlenen, depoda kartelaya ayrılacak top. Etikete 'Kartelalık' damgası basılabilir. | `Roll.markedForKartela` | AT_KARTELA statüsü — işaret niyet, statü fiili konum |
| **Kat (foldType)** | Kumaşın katlanma biçimi (2-KAT / 4-KAT / TUP). Roll'da KALICI kolondur ve kesimde MİRAS ALINMAZ — çocuk topun katı o kesimde seçilen değerdir. | `Roll.foldType / FabricPropertyValue (SEÇİM tipli KAT)` | Kimlik code'dur (ASCII: 'TUP', 'TÜP' değil); ad değişse de kod bozulmamalı |
| **Kesim / split** | Tambur'da topun bölünmesi: parentRollId ile çocuk toplar yeni barkodla doğar, parent TAMBUR_CONSUMED ile emekliye ayrılır. Çocuklar işlemin yapıldığı Tambur adımını damgalar. | `Roll.parentRollId / RollStatus.TAMBUR_CONSUMED` | Kat mirası — çocuk ebeveynin katını DEVRALMAZ |
| **Keşif (discovery)** | İstemcinin ERP sunucusunu fabrika ağında kendi bulması: mDNS ilanı (_teks-erp._tcp) + kimlik ucu + kademeli port taraması. Bir satır = bir SUNUCU (installationId ile tekilleştirilir). | `DISCOVERY_MDNS_TYPE / DISCOVERY_PORTS` | Cihaz eşleştirme (Device announce) — keşif sunucuyu bulur, eşleştirme cihazı ona |
| **KK1 / RAW_QC** | Ham mal giriş istasyonu: tedarikçiden gelen kumaş burada tartılıp ölçülür, barkodu basılır ve sisteme top olarak girer. | `StationKind.RAW_QC / seed istasyon KK1_1` | KK2 — KK1 giriş noktasıdır, KK2 üretim içi kalite kontroldür |
| **KK2 / Kurşun+KK2 (PROCESS_QC)** | Kurşun işlemi ile ikinci kalite kontrolün yapıldığı TEK fiziksel istasyon; tek WorkOrderStep olarak modellenir, per-roll iz RollOperation ile tutulur. | `StationKind.PROCESS_QC` | Kalite = istasyon YETENEĞİ oldu (appliesQuality); yeni kod kind===PROCESS_QC kar |
| **Kumaş özelliği (FabricProperty)** | Kumaşın kazandığı nitelik kataloğu (Kurşunlu, Zımparalı, Kat…). Tipi BAYRAK (FLAG) veya SEÇİM (CHOICE); SEÇİM tiplinin değerleri FabricPropertyValue'dadır. | `FabricProperty / FabricPropertyValueType / RollProperty` | Renk — renk ayrı bir katalogdur (Color), özellik değil |
| **Kurulum profili** | Müşterinin 'hangi ürünü satın aldı' sorusunun kurulum anındaki cevabı: yalnız modül anahtarlarını taşır ve YALNIZ taze kurulumda uygulanır. Davranış bayrakları profile GİRMEZ. | `ModuleProfile / module-profiles.ts` | [PROFİL] etiketi — o kural sınıflamasıdır, bu kurulum verisidir |
| **Kurşun** | BELİRSİZ: fiziksel prosesin ne olduğu repoda anlatılmıyor. Kodda karşılığı KURSUN kodlu kumaş özelliğidir ('Kurşunlu', kategori İşlem) ve Kurşun+KK2 istasyonu onu AUTO modda topa yazar. | `FabricProperty code=KURSUN / StationProperty mode=AUTO` | Kurşun bypass — o, işlemin kendisi değil dijital izinin atlanmasıdır |
| **Kurşun bypass** | Kurşun makinelerinde tablet olmadığı için adımın dijital izlenmemesi: yetkili adımı bir MAKİNEYE atar, adım sonra Tambur okutmasıyla veya dağıtım ekranından COMPLETED olur. | `KursunBypassAssignment` | SKIPPED adım — bypass'ta adım ATLANMAZ, movement'lar normal kapanır |
| **Kısmi kabul (fason)** | 100 m gitti, 51 m geldi, 49 m fasonda kaldı durumu: top tüketilmez, AT_SUBCONTRACTOR kalır ve currentQty kalana iner; her teslimat AYRI makbuzdur. | `SubcontractorReceiptItem.receivedQty / isPartial` | Çekme — kısmi kabul 'kalan sonra gelecek', çekme 'mal kısaldı' |
| **Makine (Machine)** | İstasyona bağlı fiziksel makine. Kurşun bypass ataması ve üretim atfı (RollMovement.machineId) MAKİNE bazındadır, istasyon bazında değil. | `Machine` | Device (tablet) — cihaz makineye atanır ama makine değildir |
| **Mal kabul (GoodsReceipt)** | Satın alınan malın üretimsiz depo girişi fişi. Toplarını createInitialEntry üretir (forcedStatus WAREHOUSE, entrySource PURCHASE_RECEIPT); alış siparişi opsiyoneldir. | `GoodsReceipt / GoodsReceiptStatus` | KK1 ham giriş — o üretim tarafının giriş noktasıdır |
| **Metraj** | Topun uzunluğu, metre. Liste yüzeyinde YALNIZ currentQty (şu anki metraj) basılır; giriş metrajı (initialQty) yalnız detay panelinde gösterilir. | `Roll.initialQty / Roll.currentQty` | Ağırlık (weightKg) ve en (width) — üçü ayrı ölçüdür |
| **Modül anahtarı** | Bir kurulumun 'hangi modülleri kullanıyorum' cevabını veren yedi REJİM anahtarı (production, finance, ticaret, iplik, depo.multi, kumasTeknik, tezgah). Kapalıyken menü çizilmez ve route 403 verir. | `MODULE_FLAG_KEYS / MODULE_SETTING_KEYS` | Davranış bayrağı (feature flag) — o fabrikanın davranış seçimidir, modül şalteri |
| **NON_SACKABLE_STATUSES** | Barkod topun KİMLİĞİdir ve doğduğu an verilir ('bitmiş ürün işareti' değil); erken barkodun sanılan riskleri barkod varlığıyla değil STATÜ ile kapalıdır (IN_PRODUCTION ∈ NON_SACKABLE_STATUSES). Barkod anlamı değiştirilmez — üçüncü kez sorulmasın. | `NON_SACKABLE_STATUSES` |  |
| **OTA / APK** | OTA = tabletin yalnız JS paketini internetten indirmesi (native değişmez, değişikliklerin ~%90'ı). APK = kurulum dosyası; yalnız native değiştiğinde çıkar ve versionCode artar. | `deploy/mobil-yayinla.mjs / android.versionCode` | OTA turunda versionCode'a DOKUNULMAZ — yükseltilirse gerçek APK güncellemesi bir |
| **Panel / tablet** | Panel = Electron masaüstü yönetim uygulaması (port 5174). Tablet = React Native + Expo saha uygulaması (Android tablet yatay, telefon dikey). Backend ikisine de aynı API'yi verir. | `Electron/ ve mobil/ alt projeleri` | WEB istemcisi ELECTRON'dan AYRI ClientType'tır (oturum politikası için) |
| **Parti (Batch)** | Üretime aynı anda giren top grubu; bir iş emri N parti içerir, attach dalgasında doğar. Kısa rejimde numara P01…P99 arasında DÖNER, benzersiz DEĞİLDİR. | `Batch / batch.shortNumberEnabled` | İş emri no — parti no kâğıda basılan iz, okutulmaz; orderBy batchNumber YASAK |
| **Plan sapması (RollPlanDeviation)** | Hedeften (renk veya en) sapan topun depoya onayla inişinin kalıcı kaydı. Satır = onaylı kapı-geçişi × sapan alan; confirmationId çift-sayım kilididir. | `RollPlanDeviation / PLAN_MISMATCH 409` | RollVariance — o metraj sapması, bu nitelik sapmasıdır |
| **qtyOut=0 (storno: mal bu istas** | qtyOut=0 (storno: mal bu istasyondan HİÇ geçmedi) ile qtyOut=qtyIn (finalize / Kurtar / kapanış dispozisyonu / fire: mal geçti ve çıktı) karıştırılmaz — 0 yazmak hacimden metraj siler (hayalet kayıp), qtyIn yazmak hiç var olmamış metrajı hacme ekler. | `` |  |
| **Refakat kartı (Traveler Card)** | İş emri açılışında doğan, malla birlikte fiziksel olarak gezen barkodlu kart. Bir WO = tek kart; karekod = İş Emri No, istasyonda okutulunca süreç tetiklenir. | `TravelerCard / TravelerCardScan` | Etiket (LabelTemplate) — kart operasyon kâğıdı, etiket topun kimliği |
| **Refakat kartı şablonu** | Kartın görünümünün üç kademeli kişiselleştirmesi: BUILTIN (yerleşik), SECTIONS (bölüm sırası/aç-kapa), RAW_HTML (uzman, tam HTML + {{alan}} ikamesi). | `TravelerCardTemplate / TravelerTemplateMode` | Belge Şablonu (documentsConfig) — o irsaliye/çeki ayarıdır |
| **Renk (Color)** | Renk kataloğu; boyahane fiziksel olarak HER rengi boyar — renk bir REÇETEDİR, makine kısıtı değil. Roll, OrderLine, WorkOrder.targetColor, Swatch buradan referans alır. | `Color / Roll.colorId` | Roll.colorId NULL = ham (henüz renk kazanmamış) — 'renk yok' anlamına gelmez |
| **Reçete (ProductRecipe)** | Bir ürünü tek isim/kod altında paketleyen katman: kumaş + renk + özellikler + en + rota. İş emri açılışında seçilince hedef alanlar ve rota otomatik dolar. | `ProductRecipe / ProductRecipeProperty` | Route — reçete rotanın ÜSTÜNDEDİR, rotayı dönüştürmez |
| **Rota (Route)** | Bir ürünün geçeceği istasyon adımlarının şablonu; adımlarda hedef renk (plannedColorId) ve özellik saklanabilir — hedef ÖNERİDİR, kilit değil. | `Route / RouteStep / RouteStepProperty` | ProductRecipe — reçete rotanın ÜSTÜNDE ayrı bir katmandır |
| **Sapma defteri (RollVariance)** | Üretim sapmalarının kalıcı defteri: SCRAP (fire), RECORD_CORRECTION (kayıt düzeltmesi), OVERAGE (aşım/fazla). qty HER ZAMAN pozitiftir, yönü kind söyler. | `RollVariance / RollVarianceKind` | Fire ile kayıt düzeltmesini aynı kovaya atmak fire oranını SİSTEMATİK şişirir |
| **Sayım (StockCount)** | Bir deponun belirli andaki fotoğrafı + sayım sonucu. Fotoğraf oluşturmada, fark fişi TAMAMLANMADA yazılır; satırın found alanı üç durumludur (null = henüz sayılmadı). | `StockCount / StockCountLine / StockCountLineKind` | İşaretsiz satırı 'eksik' saymak deponun yarısını sessizce silmek olurdu |
| **Sebep kataloğu (ReasonPreset)** | Fabrikanın kendi diliyle düzenlediği hazır sebep listeleri (fire, kayıt düzeltmesi, elle top ekleme, iptal, sipariş iptali, yeniden üretim). code RAPOR ANAHTARIDIR ve asla değişmez, label serbesttir. | `ReasonPreset / ReasonPresetKind` | Sistem satırı (isSystem) SİLİNMEZ, GİZLENİR — sert silme boot'ta diriliş olur |
| **Sevk Kapısı** | shipping.confirmationEnabled bayrağının ekranı: sevkiyat kurulduktan sonra çıkışın ayrı bir onay adımıyla teyit edildiği rejim. Bayrak kapalıyken karo hiç çizilmez. | `shipping.confirmationEnabled` | DispatchConfirmDialog ('Sevk Et') — planlı sevkiyatın Sevk Kapısı'na ihtiyacı YO |
| **Sevkiyat / Shipment** | Çuval havuzundan seçilen çuvallarla kurulan 'kamyon'; tek müşteri+şube. PLANNED (kuruldu) → DISPATCHED (yüklendi, toplar SHIPPED). | `Shipment / ShipmentStatus` | DirectShipment — o fasondan çıkan ayrı bir sevk türüdür |
| **SheetSpec.notes** | Versiyon rozetindeki 'Güncel' = 'en son versiyon, hiç revize edilmedi' demektir; 'içerik güncel' DEMEZ. İrsaliye diyaloğu 'sevk sonrası N top (M m) iade alınmıştır' bandını, Excel aynı bilgiyi dipnot olarak (`SheetSpec.notes`) basar — ekranda sessizlik yok. | `SheetSpec.notes` |  |
| **shippedQty** | Sipariş görünümü üç kolon: İstenen / Sevk / Açık (Açık = quantity − shippedQty). `OrderLine.packedQty`/`Order.packedQty`/`rebalanceCustomerPool` KALDIRILDI, geri getirilmez. | `shippedQty` |  |
| **Sipariş (Order / OrderLine)** | Müşteri siparişi ve kalemleri. shippedQty sevkten türer (SackAllocation + doğrudan sevk tahsisleri); iptal kalem soft'tur (cancelledAt). | `Order / OrderLine / OrderStatus` | PurchaseOrder (alış siparişi) — o ayrı modeldir, Order büKÜLMEDİ |
| **Storno (sevk geri alma)** | 'Mal hiç çıkmadı' kararı: irsaliye iptal edilir, stok geri döner, iade defterine GİRMEZ. Ayrı izin gerektirir (shipping:undo-dispatch). | `shipping:undo-dispatch / releaseSacks` | İade — iade 'mal çıktı, geri geldi'; storno 'çıkış hiç olmadı' |
| **Süperadmin (satıcı hesabı)** | DB'den görevlendirilen en yetkili gerçek kullanıcı: getEffectivePermissions ilk ifadesi ['*'] döner, modül anahtarlarını yalnız o yazabilir. Panelden ATANAMAZ, tek doğuş yolu npm run superadmin:kur. | `User.isSystemAccount` | admin rolü — süperadmin bir ROL DEĞİLDİR, katalogda '*' yoktur |
| **Tahsis / allocation (SackAllocation)** | Bir çuvalın metrajının sipariş satırlarına dağıtımı — sevk defteri. Sevk kurulurken yazılır; PLANNED'da shippedQty'ye SAYILMAZ, yalnız DISPATCHED sayılır. | `SackAllocation` | Rezervasyon — tahsis sevk muhasebesidir; rezervasyon ayrı bir defterle kurulacak |
| **Tambur** | Kesim/bölme ve kalite kararının verildiği istasyon; rulo (TOP formu) burada doğar. Kalite kararı + kesim yapar, 'final ürün kapısı' DEĞİLDİR. | `StationKind.TAMBUR` | Finalize — her rotanın SON adımı finalize eder, Tambur özel değildir |
| **Tebdil / devir (SUPERSEDED)** | Tüm canlı toplarını yeni bir iş emrine devredip boşalan KAYNAK iş emrinin terminal durumu. İptal DEĞİLDİR: veri kaybı yok, malzeme devam WO'sunda (splitFromId). | `WorkOrderStatus.SUPERSEDED / TebdilWizard` | CANCELLED — raporda 'iptal' ile karışmamalı |
| **Terim TDK yazımıyla 'Yarı Mamu** | Terim TDK yazımıyla 'Yarı Mamul'dür ('mamül' yazılmaz); tekdüzen hesap planında 151 Yarı Mamuller, SAP'de HALB. 'Ham bez' boyasız malın adıdır, bu kovayı adlandırmaz. | `` |  |
| **Top / Roll** | Fabrikadaki tek bir kumaş parçasının kaydı. Metraj currentQty (kalan) ve initialQty (giriş) ile metre cinsinden taşınır; aynı tablo hem ruloyu hem açık kumaşı tutar. | `Roll (Prisma modeli)` | Rulo — sahada 'top' fiziksel ruloyu ima eder; ERP'de Roll her iki formu da kapsa |
| **WorkOrder.workOrderNumber** | `WorkOrder.workOrderNumber` (İE+GGAAYY+NNNN) ÜRETİM EMRİDİR; `Batch` üretime aynı anda giren top grubudur — bir WO N parti içerir. İkisi ayrı kimliktir, birbirinin yerine kullanılmaz. | `WorkOrder.workOrderNumber, Batch` |  |
| **Yarı mamul (SEMI_FINISHED)** | Dışarıdan alınan boyalı/işlenmiş, üretime sokulabilir kumaş. Ayrı DEPO değil ayrı GÖRÜNÜMDÜR; arz hesabından DÜŞÜLMEZ, yalnız ayrı gösterilir. | `RollEntrySource.SEMI_FINISHED / rollScope='SEMI_FINISHED'` | Bitmiş depo (WAREHOUSE) — yarı mamul statüsü STOCK'tur, WAREHOUSE değil |
| **Çalışma oturumu (WorkSession)** | Ayak izi: kim, hangi cihazla, nerede (makine/istasyon), ne zaman çalıştı. Donanım çözümü ve üretim atfı AKTİF oturumdan okunur; stationId her zaman doludur. | `WorkSession / WorkSessionEndReason` | Session (login oturumu) — o JWT oturumudur, bu vardiya/ayak izidir |
| **Çek / senet (Cheque)** | Portföy varlığı. Çek ALINDIĞI AN cari alacaklanır; status yalnız ChequeEvent defterinin denormalize ÖZETİDİR ve geçişler atomik claim ile yazılır. | `Cheque / ChequeStatus / ChequeEvent` | PaymentMethod — çek bir ödeme yöntemi DEĞİL, kendi yaşam döngüsü olan varlıktır |
| **Çeki listesi (Manifest)** | İş emrinin baskı anındaki top listesinin donmuş kopyası (manifestNo + snapshot). Fason sevkinde de 'çeki' adıyla anılır. | `Manifest` | İrsaliye — PrintedDocument zinciri ayrıdır |
| **ÇEKİRDEK / PROFİL (kural sınıfı)** | Her kural ya [ÇEKİRDEK]'tir (her kurulumda aynı: defter semantiği, idempotency, kilit sırası, fail-closed kapı, sır hijyeni) ya [PROFİL] (bu fabrikanın bayrak/veri/rota seçimi). | `CLAUDE.md not etiketleri` | Modül anahtarı — sınıflama bir etikettir, çalışan bir şalter değil |
| **Çekme (shrink)** | Boyahanede kumaşın fiziksel olarak kısalması (ör. 250 m gitti, 220 m döndü). Hata değil ölçülen gerçektir; farkı sapma defterine sistem sebebiyle yazılır. | `SHRINK_REASON_CODE = 'FASON_CEKME'` | Fire — çekme SCRAP kind'ında yazılsa da operatör fire sebep listesinde GÖRÜNMEZ |
| **Çuval / Sack** | Depoda duran fiziksel paket birimi ve çuval depo havuzunun çekirdeği: açılır, içine top okutulur, brüt tartılır. Müşteri opsiyoneldir; mühür/rezerv YOKTUR. | `Sack` | Shipment — çuval depo nesnesi, sevkiyat kamyondur; sipariş bağı yalnız sevkte ku |
| **Ölü top statüleri (K18_DEAD_STATUSES)** | Sayım/metraj/lane filtrelerinde dışlanan terminal top statüleri kümesi (KARTELA_CONSUMED dahil). Tek kaynaktır; elle statü listesi kopyalanmaz. | `K18_DEAD_STATUSES` | STATUS_GROUPS.ARCHIVE — o arşiv sekmesinin kapsamıdır, ayrı liste |
| **Şube (Branch)** | Müşterinin teslimat/planlama şubesi. Order.branchId OPSİYONELDİR (eski kayıtlar null); yeni siparişler tek bir şubeye yönlendirilir. | `CustomerBranch / Order.branchId` | Warehouse (depo) — şube müşteri tarafı, depo bizim tarafımız |