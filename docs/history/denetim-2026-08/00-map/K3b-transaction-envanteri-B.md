# K3b — Transaction & Kilit Envanteri (B: kalan servisler + helpers/)

**Aşama:** ① KEŞİF (harita — yargı yok, bulgu yok; ② için HOTSPOT işaretli).
**Tarih / dal / HEAD:** 2026-08-28 · `adnansahin` · `ce8681d1`.
**Yöntem:** Her `$transaction(` sitesinin gövdesi elle okundu (satır aralıkları tabloda) **ve** mekanik bir parantez-kapsamı tarayıcısıyla (`scratchpad/txscan.mjs`; tx gövdesinde global `prisma.`, `Promise.all`, dış I/O, `await import(`, kilit/sayaç çağrıları, `tx.*` yazım sayısı) doğrulandı. Kilit sırası her site için "kilit satırı → korunan okumanın satırı" olarak yazıldı. DB'ye dokunulmadı (bu harita için gerekmedi). Yollar repo köküne göre; `Teks-Erp/` öneki kısaltma için düşürüldü (`src/...` = `Teks-Erp/src/...`).

## 0. Sayılar (grep ile ölçüldü)

| Ölçüm | Değer | Kaynak |
|---|---|---|
| Kapsamdaki `$transaction(` metin eşleşmesi | **66** (23 servis + `helpers/guarded-hard-remove.ts`) | `grep -c '\$transaction('` |
| Bunlardan GERÇEK tx sitesi | **63** (batch.service.ts:16/103/213 yorum satırıdır) | txscan |
| Interactive (`async (tx) =>`) | 60 | txscan |
| Batch (`$transaction([...])` / `ids.map(...)`) | 3 — `permission-management:1011`, `reason-preset:505`, `device:263` | txscan |
| Tx gövdesinde **global `prisma.`** (interactive) | **0** (3 batch sitesi meşru olarak `prisma.x.create…` dizisi taşır) | txscan |
| Tx gövdesinde `Promise.all` | **0** | txscan |
| Tx gövdesinde dış I/O (`fetch/spawn/fs/bcrypt/setTimeout`) | **0** (`bcrypt.hash` `permission-management:423` tx DIŞI) | txscan + okuma |
| Tx gövdesinde `await import(` | **0** (`kursun-qc.service.ts:839` tx'ten ÖNCE, dışarıda) | txscan |
| `isolationLevel` | kapsamda **0** (repo geneli 1: `shipping.service.ts:2610` — K3a) | grep |
| Site-özel tx opsiyonu | **1**: `master-data-merge.service.ts:711` `{ timeout: 120_000, maxWait: 10_000 }` (`MERGE_TX_TIMEOUT_MS`, :64); diğer 62 site global `lib/prisma.ts:89-92` (`maxWait 5s / timeout 20s`) | grep |
| `withBarcodeRetry` sarmalı tx | **5**: `batch:374`, `batch:870`, `kartela:282`, `kartela:610`, `item:228` | txscan |
| `pg_advisory_xact_lock` çağrı noktası (repo) | **8** (hepsi 2-argümanlı; 1-arg `hashtext` formu **0**; session-tipi `pg_advisory_lock(` **0**) | grep |
| `FOR UPDATE` | 1 (`order.service.ts:2434` — K3a) | grep |
| helpers/ dosya sayısı | 71 `.ts` + `codec/` + `raster/` dizinleri | ls |
| helpers/ içinde DB yazan / kilit alan / ham SQL | **20** dosya (bkz. §3) | grep |

---

## 1. SITE TABLOSU

Sütun anahtarı: **Tip** I=interactive, B=batch · **Opsiyon** G=global (`maxWait 5s/timeout 20s`) · **Kilit sırası** tx içinde alınan kilitler/claim'ler satır numarasıyla, `→` edinim sırası; `[…]` tx DIŞI (havuz client'ı) · **GP** = tx gövdesinde global `prisma.` · **I/O** = tx içinde dış dünya · **PA** = tx client'ında `Promise.all` · **Audit** = `AuditService.log` konumu · **Claim** = §3.2 sınıfı · **CTA** = check-then-act okumaları (tx dışında okunup tx içinde karar/yazıma giren değerler).

### 1.1 `src/services/label-template.service.ts` (8 site)

| Satır | Fonksiyon | Tip/Ops | Kilit sırası | Yazılan tablolar | GP/I/O/PA | Audit | Claim sınıfı | CTA |
|---|---|---|---|---|---|---|---|---|
| 315-349 | `create` | I/G | yok | `label_templates` (updateMany :318 → create :323), `label_context_defaults` (upsert :342) | -/-/- | tx sonrası :351 | :318 = **toplu güncelleme** (kind+isDefault) — count gereksiz; `.catch(rethrowDefaultConflict)` :349 partial unique `label_templates_one_default_per_kind` (şema-dışı, `schema.prisma` LabelTemplate notu) | `assertNameAvailable` :308 tx DIŞI (nameFold app-level; DB `@@unique([name])` yalnız birebir ad) |
| 428-452 | `update` | I/G | yok | `label_templates` (updateMany :431, update :436), `label_context_defaults` (upsert :439 / deleteMany :447) | -/-/- | :454 | :431 toplu; :436 koşulsuz update (son yazan kazanır — beklenen) | `existing` :368, `templateHasAssignments` :415, `assertTemplateAssignable` :425 — hepsi tx DIŞI |
| 497-511 | `setDefault` | I/G | yok | `label_templates` (updateMany :498, update :507), `label_context_defaults` (upsert :502) | -/-/- | :513 | toplu + koşulsuz; partial unique yarışı P2002→`rethrowDefaultConflict` | `existing` :480 (isActive/deletedAt), `current` :490, `assertTemplateAssignable` :495 tx DIŞI |
| 579-588 | `hardDelete` | I/G | yok | `peripheral_template_routes` (deleteMany :580), `customer_template_routes` (deleteMany :583), `label_templates` (update deletedAt :584) | -/-/- | :589 (best-effort) | koşulsuz | `asDefault` :572 + `existing.isDefault` tx DIŞI → **tx içinde ikizi YOK** (bkz. §5-L1) |
| 653-682 | `createFromEnvelope` (import/duplicate) | I/G | yok | `label_templates` (create :654), `label_template_variants` (create ×N, döngü :669) | -/-/- | :689 (best-effort) | — ; P2002 catch :683 → 409 | `findAvailableName` :633 tx DIŞI; **N = `env.variants`** — `label-template.controller.ts:130` `z.array(` **tavansız** (JSON zarf) |
| 904-910 | `setPrimaryVariant` | I/G | yok | `label_template_variants` (updateMany :905, update :909) | -/-/- | :911 | toplu + koşulsuz; partial unique "one primary per template" (raw SQL, şema notu :8) | `existing` :895 tx DIŞI (idempotent erken dönüş :900) |
| 975-978 | `setContextDefault(null)` | I/G | yok | `label_context_defaults` (deleteMany), `label_templates` (updateMany) | -/-/- | :979 | idempotent temizlik | — |
| 996-1008 | `setContextDefault(id)` | I/G | yok | `label_context_defaults` (upsert :997), `label_templates` (updateMany :1004, update :1006) | -/-/- | :1010 | toplu + koşulsuz | `template` findFirst :986 (isActive, deletedAt) + `assertTemplateAssignable` :994 tx DIŞI → `hardDelete`/`deactivate` ile yarış (§5-L1) |

### 1.2 `src/services/permission-management.service.ts` (7 site)

| Satır | Fonksiyon | Tip/Ops | Kilit sırası | Yazılan tablolar | GP/I/O/PA | Audit | Claim sınıfı | CTA |
|---|---|---|---|---|---|---|---|---|
| 190-221 | `grantPermission` | I/G | yok | `user_permissions` (upsert :195), `users` (update tokenVersion :218, koşullu) | -/-/- (`Promise.all` :180 HAVUZ client'ı — meşru) | :223 | — (upsert idempotent) | `before` :191 tx İÇİ (bump kararı); **son-admin guard'ı bu yolda HİÇ yok** — bkz. HOTSPOT H-3 |
| 304-336 | `setUserPermissions` | I/G | **8025** :306 (`acquireAdminGuardLock`) → `assertAdminCoverageAfterChange` :307 (okuma :623) — **yalnız `removesAdmin` ise** | `user_permissions` (deleteMany :310, createMany :315, updateMany döngü :326), `users` (:334) | -/-/- | :338 | :327 toplu (userId+permissionId, count gereksiz); createMany **`skipDuplicates` YOK** | `existing` :272, `toAdd/toRemove/toUpdate` :279-289, `removesAdmin` :295-302 **tx DIŞI diff**; pencerede eklenen admin satırı `toRemove`'a girmez (güvenli yön); eşzamanlı grant → createMany P2002 |
| 377-395 | `revokePermission` | I/G | **8025** :379 (`revokesAdminCode` ise) → findFirst :381 → assert :389 (okuma :623) → deleteMany :393 → bump :394 | `user_permissions`, `users` | -/-/- | :397 | deleteMany id (idempotent, yorum :391) | `existing` :364 tx DIŞI (kod değişmez) |
| 686-695 | `deactivateUser` | I/G | **8025** :687 → `assertNotLastActiveAdmin` :688 (okumalar :647/:652) → update :689 | `users` | -/-/- | :700 | koşulsuz update (id) | `existing` :674 (deletedAt) tx DIŞI; post-tx `SessionRegistryService.revokeAllForUser` :696 havuz, best-effort |
| 747-762 | `deleteUser` | I/G | **8025** :748 → assert :749 → update :750 | `users` | -/-/- | :767 | koşulsuz | `existing` :735; `randomBytes` :744 tx DIŞI |
| 896-914 | `updateTemplate` | I/G | yok | `permission_templates` (update :902), `permission_template_items` (deleteMany :905, createMany :907) | -/-/- | :916 | — | `assertTemplateNameAvailable` :887 tx DIŞI (nameFold; DB `name @unique` birebir, `@@index([nameFold])` unique DEĞİL — `schema.prisma` PermissionTemplate); `validatePermissionIds` :892 |
| 1011-1023 | `applyTemplate` (merge) | **B**/G | yok | `user_permissions` (createMany), `users` (update) | GP=:1012,:1019 (batch formu — meşru) | :1026 | createMany `skipDuplicates` YOK | `existing` :1000 tx DIŞI → `toAdd`; eşzamanlı grant → P2002 → tüm batch rollback |

### 1.3 `src/services/tambur.service.ts` (6 site)

| Satır | Fonksiyon | Tip/Ops | Kilit sırası | Yazılan tablolar | GP/I/O/PA | Audit | Claim sınıfı | CTA |
|---|---|---|---|---|---|---|---|---|
| 925-1291 | `finalize` (:540) | I/G | **[CNT: `reserveRollBarcodesInOrder(prisma)` :906 — ayrı bağlantı, tx AÇILMADAN ÖNCE]** → **WO satırı** `touchWorkOrderTx` :931 (İLK) → `freshWo` :935 → **Roll(parent) claim** :953 → `freshQtyRow` :981 → RollError ×N :1004 → `closeOrphanRollErrors` :1019 → Roll(child) insert ×N :1043 → **WorkOrderStep `stepData` read-modify-write** :1140-1153 → RollMovement :1173 → Roll(parent) :1191 → RollProperty :1208 → RollOperation upsert :1212 → `recomputeStepStatus` :1244 (WorkOrderStep + WO PLANNED) → `recordPlanDeviationTx` :1252 → `recordVarianceTx` :1269 → `completeWorkOrderIfStepsDone` :1282 (WO + TravelerCard) | `rolls`, `roll_errors`, `roll_properties`, `roll_operations`, `work_order_steps`, `roll_movements`, `roll_plan_deviations`, `roll_variances`, `work_orders`, `traveler_cards` | -/-/- | tx sonrası: `logMany` :1299 + :1309 + :1326 (childAudits tx içinde toplanır :921) | :953 **tekil claim** (id+IN_PRODUCTION+currentStepId → TAMBUR_CONSUMED), count 0 → `raceLost` idempotent :967 / 409 :971; :1173 koşullu kapanış (`exitedAt:null`) | segment hesabı + `totalQtyD` tx DIŞI → tx içi `freshQtyRow` ikizi :985 ✓; cuts N ≤ 200 (`tambur.controller.ts:50 .max(200)`) + kuyruk |
| 1810-1842 | `reportError` | I/G | **Roll satırı** claim :1818 (`updateMany updatedAt` where IN_PRODUCTION+currentStepId) → RollError create :1831 | `rolls`(updatedAt), `roll_errors` | -/-/- | :1859 | tekil claim count 0 → 409 :1826; partial unique `roll_errors_roll_meter_defect_uq` → P2002 catch :1844 (tx DIŞI) → 409 | `findFirst` :1790 tx DIŞI (savunma; DB unique kapatır) |
| 2131-2275 | `cutWarehouseRoll` | I/G | **[CNT `generateRollBarcode(prisma)` :2116 tx ÖNCE]** → Roll(child) insert :2132 → RollProperty :2171 → RollOperation :2205 → **Roll(parent) guarded update** :2236/:2242 → `recordVarianceTx` :2264 | `rolls`, `roll_properties`, `roll_operations`, `roll_variances` | -/-/- | :2302 | :2242 **guarded update = claim** (id+status+shipmentId null+sackId null+currentQty gte); P2025 → 409 :2254; WO kilidi YOK (depo kesimi — WO yok) | pre-check :2064-2085 tx DIŞI → WHERE ikizi ✓; `readTamburOverQuantityEnabled()` :2097 tx DIŞI; idempotency P2002(clientToken) catch :2280 |
| 2466-2607 | `finalizeWarehouseCut` | I/G | **[CNT :2462 koşullu, tx ÖNCE]** → **Roll(parent) claim** :2473 → `freshRow` :2486 → Roll(child) :2498 → RollProperty → RollOperation → `closeOrphanRollErrors` :2575 → Roll(parent) :2581 → `recordVarianceTx` :2593 | `rolls`, `roll_properties`, `roll_operations`, `roll_errors`, `roll_variances` | -/-/- | :2609 | :2473 tekil claim (status+ship+sack null → TAMBUR_CONSUMED) count 0 → 409 | pre-check tx DIŞI (:2401 vb.); **`clientToken` bu yolda YAZILMIYOR** (2343-2640 aralığında yalnız yorumda geçer) — yorum :2448-2458 idempotency'yi ona dayandırıyor → HOTSPOT H-5 |
| 2789-2969 | `cutOpenFabric` | I/G | **[CNT :2775 tx ÖNCE]** → **WO** :2793 (İLK) → `freshWo` :2794 → Roll(child) :2805 → RollProperty → RollOperation → `recordPlanDeviationTx` :2902 → **Roll(parent) guarded update** :2922/:2935 → `recordVarianceTx` :2958 | `rolls`, `roll_properties`, `roll_operations`, `roll_plan_deviations`, `roll_variances`, `work_orders`(updatedAt) | -/-/- | :2992 | :2935 claim (IN_PRODUCTION+currentStepId+gte) P2025 → 409 | `resolveLabelIntent` :2777, kalite id :2783 tx DIŞI; idempotency P2002(clientToken) :2974 |
| 3180-3394 | `finalizeOpenFabric` | I/G | **[CNT :3178 koşullu, tx ÖNCE]** → **WO** :3184 → `freshWo` → **Roll(parent) claim** :3202 → `freshRow` :3218 → Roll(child) :3232 → RollProperty → `recordPlanDeviationTx` :3279 → `closeOrphanRollErrors` :3292 → Roll(parent) :3300 → RollMovement :3324 → RollOperation upsert :3340 → `recordVarianceTx` :3374 → `recomputeStepStatus` :3386 → `completeWorkOrderIfStepsDone` :3391 | + `roll_movements`, `work_order_steps`, `traveler_cards` | -/-/- | :3396 | :3202 tekil claim; :3324 koşullu kapanış | idempotency ön-kontrolü tx DIŞI (yorum :3197); `clientToken` yok (bilinçli, ön-kontrol tabanlı) |

**JSON lost-update (§3.3) notu:** `WorkOrderStep.stepData` read-modify-write (:1140-1153) — kolonu yazan **iki** yer var: bu site ve `helpers/workorder-clone.helper.ts:137` (kopya, tam nesne). Aynı satıra farklı anahtar yazan ikinci bir akış yok → §3.3 üçlü koşul sağlanmıyor (kayıt: bulgu adayı değil, ② yeniden ölçmesin).

### 1.4 `src/services/batch.service.ts` (3 gerçek site; :16/:103/:213 yorum)

| Satır | Fonksiyon | Tip/Ops | Kilit sırası | Yazılan tablolar | GP/I/O/PA | Audit | Claim sınıfı | CTA |
|---|---|---|---|---|---|---|---|---|
| 374-516 | `moveRolls` | I/G **+withBarcodeRetry** :373 | ön-okuma `targetRef` :379 (yalnız kilit anahtarı) → **WO satırı** :384 → taze `target` :386 + tazelik kontrolü :401 → `performDispatchSurgeryTx` :464 (içinde: SDI claim :253/:344, SD claim :278/:304, `generateDispatchNoTx` :324 **findMany sayacı, kilitsiz**, `freezeForSource` :369 → `printed_documents`) → Roll :478/:487 → TravelerCard :485 → Batch claim :499 / `deleteIfEmptyAndTraceless` :507 | `work_orders`(updatedAt), `subcontractor_dispatch_items`, `subcontractor_dispatches`, `printed_documents`, `rolls`, `traveler_cards`, `batches` | -/-/- | :519 | :499 **tekil claim** (`mergedIntoId:null`) count===1 kontrolü :503 ✓; surgery içindeki claim'ler count kontrollü | hepsi kilit SONRASI taze okunur ✓ (MAJOR-3c yorumu :375-378) |
| 580-827 | `mergeBatches` | I/G (retry YOK — yeni sevk doğmaz) | ön-okuma `pre` :585 → **WO** :594 → taze `batches` :597 (+WO eşitliği :612) → SD retarget :685 → Roll :694/:704 → TravelerCards :702 → SDI :740 → SD close claim :748 → `voidForSource` :765 → SD keeper claim :792 → Batch claim :810 | `subcontractor_dispatches`, `rolls`, `traveler_cards`, `subcontractor_dispatch_items`, `printed_documents`, `batches` | -/-/- | :829 | :748 tekil claim count 0→409; :792 tekil claim; :810 **çoklu claim** `count !== sourceIds.length → 409` (hepsi-ya-hiç, yazılı) ✓ | `batchIds` Zod `min(2)` **max YOK** (`batch.controller.ts:23`) — aynı WO'nun partileriyle sınırlı |
| 870-947 | `splitBatch` | I/G **+withBarcodeRetry** :869 | ön-okuma `srcRef` :873 → **WO** :880 → taze `src` :883 (+:889/:895) → `createBatchTx` :920 → **8022** (`generateBatchNumberTx` :126 — fonksiyonun İLK ifadesi ✓) → `readBatchShortNumberEnabled(tx)` :128 → `readLastShortBatchSeqTx` :162 / findMany :133 → Batch insert :233 → Roll :243 → TravelerCard :254 → `performDispatchSurgeryTx` :931 → Roll :941 | `work_orders`, `batches`, `rolls`, `traveler_cards`, + surgery tabloları | -/-/- | :950 | Roll :941 toplu | — |

**8022 sırası (görev sorusu):** `pg_advisory_xact_lock(8022,1)` `generateBatchNumberTx`'in **İLK ifadesi** (:126) ve koruduğu üç okumadan (:128, :133, :162) **öncedir** ✓. Ancak **tx'in ilk ifadesi DEĞİLDİR**: bu servisteki tek çağıran (`splitBatch`) önce WO satır kilidini alır (:880 → :920). Kilit anahtarı sabit `1` → sistem geneli tek seri kapı; tutulma süresi = kalan tx (Batch insert + roll updateMany + kart bayrağı + sevk cerrahisi + labelDirty). Dış çağıranlar için §2.

### 1.5 `src/services/kursun-bypass.service.ts` (5 site)

| Satır | Fonksiyon | Tip/Ops | Kilit sırası | Yazılan tablolar | GP/I/O/PA | Audit | Claim sınıfı | CTA |
|---|---|---|---|---|---|---|---|---|
| 768-992 | `assign` | I/G | **WO** :771 (İLK) → taze WO :774 → `findPendingBypassAssignmentTx(tx)` :819 → `readKursunBypassEnabled(tx)` :826 → uygunluk okumaları :836-887 → machine :902 → stationProperty :934 → KBA claim :955 **veya** create :966 | `work_orders`(updatedAt), `kursun_bypass_assignments` | -/-/- | :1002 | :955 tekil claim (completedAt/cancelledAt null) count 0→409; create yarışı partial unique `kursun_bypass_one_pending_per_step_uq` → P2002 catch :996 (tx DIŞI) → 409 | **yok — tüm guard'lar kilit altında tx içinde** ✓ |
| 1173-1214 | `cancelAssignment` | I/G | KBA claim :1176 (WO kilidi ALINMAZ) → findUniqueOrThrow :1198 | `kursun_bypass_assignments` | -/-/- | :1216 | tekil claim; count 0 → ayrım :1185-1193 | — |
| 1368-1404 | `completeFromDistribution` | I/G | **WO** :1369 → `assertWorkOrderAliveTx` :1370 → `claimAssignmentTx` :1372 (KBA claim :2272; count 0 → :2282 ayrım → idempotent `false`/409) → `closeBypassMovementsTx` :1383 (raw `UPDATE roll_movements … WHERE exitedAt IS NULL AND rollId=ANY … RETURNING` :2339; parite :2358; leftover :2365) → `copyStationCapabilitiesToRoll` ×N :1395 → **`finalizeRollsAtLastStep`** :1399 → **CNT `reserveRollBarcodesInOrder(tx)` (`roll-finalize.helper.ts:172`) — sayaç satır kilidi TX İÇİNDE** → Roll ×N :189 → `recomputeStepStatus` :1400 → `completeWorkOrderIfStepsDone` :1401 | `work_orders`, `kursun_bypass_assignments`, `roll_movements`, `roll_properties`, `roll_barcode_counters`, `rolls`, `work_order_steps`, `traveler_cards` | -/-/- | :1415 | `exitedAt IS NULL` koşullu kapanış = claim (kapsam paritesi ile hepsi-ya-hiç) | `a = loadAssignmentForCompletion` :1360 tx DIŞI (machineId/stationId bayat olabilir; KBA claim korur); `rollIds` Zod tavanı: route YAML :313 (`rollIds`) — controller şeması ayrıca doğrulanmadı [kapsam dışı — kursun-bypass.controller okunmadı] |
| 1815-1872 | `completeFromTambur` | I/G | **WO** :1816 → alive :1817 → KBA claim :1819 → close movements :1828 → caps ×N :1838 → RollMovement createMany :1852 (partial unique `roll_movements_one_open_per_roll_step_uq` → P2002 catch :1874) → Roll updateMany :1863 → `recomputeStepStatus` ×2 :1868-1869 | + `roll_movements`(insert), `rolls`(currentStepId) | -/-/- | :1895 | :1863 toplu (id in) | `pending`/`a`/`next` :1801-1811 tx DIŞI; KBA claim + movement claim korur |
| 1967-2035 | `completeUnassignedFromTambur` | I/G | **WO** :1968 → alive :1969 → `resolveUnassignedTamburClosure(tx)` :1983 **taze (TOCTOU ikizi)** → close movements :1996 (marker `KURSUN_BYPASS_FINISHED:UNASSIGNED:`) → caps ×N :2006 → RollMovement createMany :2015 → Roll :2026 → recompute ×2 | aynı | -/-/- | :2045 | claim = `exitedAt IS NULL` (bilinçli, yorum :1945-1948) | pre-check tx DIŞI → tx içi taze çözüm ✓ |

### 1.6 `src/services/kartela.service.ts` (5 site)

| Satır | Fonksiyon | Tip/Ops | Kilit sırası | Yazılan tablolar | GP/I/O/PA | Audit | Claim sınıfı | CTA |
|---|---|---|---|---|---|---|---|---|
| 282-339 | `dispatch` | I/G **+withBarcodeRetry** :281 | **sayaç `nextKartelaDocSequence(tx)` :284 — İLK ifade, findMany tabanlı, KİLİTSİZ** (`dispatchNo @unique` + retry) → KartelaDispatch+items create :287 → `freezeForSource(tx)` :310 (`printed_documents`, version `max+1` :332-341) → **Roll çoklu claim** :323 | `kartela_dispatches`, `kartela_dispatch_items`, `printed_documents`, `rolls` | -/-/- | :342 | :323 **çoklu claim** (WAREHOUSE+shipmentId null+sackId null → AT_KARTELA) `count !== len → 409` (AppError, P2002 değil → retry'a girmez ✓) | rolls pre-check :249-274 tx DIŞI → WHERE ikizi ✓; **idempotent kapı :223-246 heuristik** (aynı firma + aynı top kümesi açık sevk), `clientToken` YOK (CLAUDE.md: KartelaDispatch'te token yok) |
| 413-446 | `cancelDispatch` | I/G | KartelaDispatch claim :416 → `voidForSource` :428 → Roll çoklu claim :437 | `kartela_dispatches`, `printed_documents`, `rolls` | -/-/- | :448 | :416 tekil (cancelledAt null); :437 çoklu (AT_KARTELA → WAREHOUSE) count≠len → 409 | `accepted` :383, `movedRolls` :399 tx DIŞI → Roll claim ikizi ✓ |
| 610-690 | `receive` | I/G **+withBarcodeRetry** :609 | sayaç :612 (KK) → KartelaReceipt create :615 → `nextSwatchSequence(tx)` :632 (bellekte ardışık) → döngü :638/:652 (N = Σcount) → KartelaReceiptItem createMany :672 → **Roll çoklu claim** :678 → Swatch createMany :687 (`cardNumber`/`barcode @unique` → P2002 → **tüm tx retry**) | `kartela_receipts`, `kartela_receipt_items`, `rolls`, `swatches` | -/-/- | :693 | :678 çoklu claim count≠len → 409 (yorum :673-677: retry'ı çift makbuza çevirmez) | `returns[].count` Zod `.max(1000)` (`kartela.controller.ts:36`), `returns` dizisi tavansız → N swatch = Σcount tek `createMany` |
| 791-831 | `cancelReceipt` | I/G | KartelaReceipt claim :794 → Swatch çoklu claim :808 → Roll çoklu claim :821 | `kartela_receipts`, `swatches`, `rolls` | -/-/- | :833 | üç claim de count kontrollü (hepsi-ya-hiç) ✓ | `receipt.swatches` downstream guard :777 tx DIŞI → :808 WHERE ikizi ✓ |
| 1358-1405 | `reduceStock` | I/G | SwatchStockReduction create :1363 (**`clientToken @unique` İLK ifade = idempotency çapası**) → Swatch findMany FIFO `take N` :1377 → Swatch çoklu claim :1395 | `swatch_stock_reductions`, `swatches` | -/-/- | :1438 | select-then-claim; `count !== N → 409` :1399 (kısmi → rollback, token boşa gitmez) | `count` Zod `.max(1000)` :55; P2002(clientToken) catch :1408 → payload özdeşliği / `CLIENT_TOKEN_COLLISION` |

### 1.7 `src/services/traveler-card.service.ts` (3 site)

| Satır | Fonksiyon | Tip/Ops | Kilit sırası | Yazılan tablolar | GP/I/O/PA | Audit | Claim sınıfı | CTA |
|---|---|---|---|---|---|---|---|---|
| 213 | `print` | I/G | `createForWorkOrder(tx)` :175: findUnique :180 → workOrder :183 → `buildSnapshot(tx)` :189 → TravelerCard create :190 (`workOrderId @unique`) | `traveler_cards` | -/-/- | :230 (created ise) | findUnique→create (TOCTOU) **DB unique + tx dışı tek tekrar** :217-227 kapatır; sayaç YOK (cardNumber = WO no :169-170) | — |
| 268-292 | `reprint` | I/G | findUnique :269 → status kontrolü :275 → `buildSnapshot(tx)` :280 → update `version: existing.version + 1` :284 (koşulsuz) | `traveler_cards` | -/-/- | :305 | **read-then-write, claim YOK** — iki eşzamanlı reprint aynı `version+1`'i yazar (bkz. §5-L4); P2002 yalnız kartsız-WO dalında (:297-303) | — (yorum :252: hiçbir istemci çağırmıyor) |
| 368-386 | `recordPrintEvent` | I/G | TravelerCard update :369 (koşulsuz; `version: plan.version`) → `archivePrintedVersionTx` :379 (`printed_documents` upsert :443 + updateMany SUPERSEDED :468) | `traveler_cards`, `printed_documents` | -/-/- | :388 | koşulsuz | `card` :350 + `resolvePrintPlan` :364 **tx DIŞI** — bilinçli/dokümante sınır :340-347 (aynı sürüm numarası iki farklı içerikle basılabilir) |

### 1.8 `src/services/tambur-undo.service.ts` (3 site)

| Satır | Fonksiyon | Tip/Ops | Kilit sırası | Yazılan tablolar | GP/I/O/PA | Audit | Claim sınıfı | CTA |
|---|---|---|---|---|---|---|---|---|
| 1058-1171 | `applySingle` | I/G | **WO kilidi YOK** → child findUnique :1059 → count :1068 → **Roll(child) claim** :1073 → [arşiv: `recordVarianceTx` :1098] / [üretim: parent findUnique :1124 → variance :1134 → **Roll(parent) claim** :1143] / [depo: **Roll(parent) claim** :1157] | `rolls`, `roll_variances` | -/-/- | :1173 | :1073 tekil claim (CANCELABLE+sack/ship/step null → CANCELLED) count≠1 → 409; :1143/:1157 tekil claim + `increment` (atomik) | `childBlockReason` :1069 tx içi ama kilitsiz okuma → claim WHERE'i aynı koşulları taşır ✓ |
| 1221-1395 | `applySingleRestore` | I/G | **WO kilidi YOK** → child :1223 → RollOperation findFirst :1237 → Step+WO status okuma :1246 (kilitsiz) → Roll(child) claim :1260 → parent :1273 → variance :1295 → **RollMovement** update/create :1313/:1318 → **Roll(parent) claim** :1330 (TAMBUR_CONSUMED → revive) → RollOperation deleteMany :1353 → RollProperty :1367 → `recomputeStepStatus` :1377 (**WorkOrderStep** update :145 + WO `PLANNED` updateMany :180) → **WO updateMany COMPLETED→IN_PROGRESS** :1378 → `setWorkOrderCardStatuses` :1384 | `rolls`, `roll_variances`, `roll_movements`, `roll_operations`, `roll_properties`, `work_order_steps`, `work_orders`, `traveler_cards` | -/-/- | :1397 | claim'ler tekil count===1 ✓; WO/Step yazımları koşullu updateMany (idempotent) | WO status guard :1251-1256 tx içi ama **kilitsiz** (eşzamanlı WO iptali arada girebilir — kardeşler `touchWorkOrderTx` ile serileşir, burası serileşmez) — HOTSPOT H-1 |
| 1434-1692 | `applyFull` | I/G | **WO kilidi YOK** → RollOperation :1435 → Step+WO :1450 → children findMany :1464 → groupBy :1475 → **Roll(children) çoklu claim** :1488 → parent :1503 → `computeRestoredQty` :1507 → variance :1524 → RollMovement :1543/:1548 → **Roll(parent) claim** :1563 → RollError updateMany :1585 → RollOperation deleteMany :1597 → RollVariance updateMany :1610, :1627 (tersleme) → RollProperty :1640-1655 → `recomputeStepStatus` :1668 → **WO updateMany** :1669 → `setWorkOrderCardStatuses` :1675 | + `roll_errors` | -/-/- | tx sonrası (:1697 civarı — okunmadı, desen aynı) | :1488 çoklu claim `count !== ids.length → 409` ✓; :1563 tekil claim | aynı H-1 |

### 1.9 `src/services/kursun-qc.service.ts` (3 site)

| Satır | Fonksiyon | Tip/Ops | Kilit sırası | Yazılan tablolar | GP/I/O/PA | Audit | Claim sınıfı | CTA |
|---|---|---|---|---|---|---|---|---|
| 441-497 | QC2 tamamlama (metot ~:380-537; adı `[a-zA-Z]+` regex'ine takıldı — rakam içeriyor [VARSAYIM: `completeQc2`]) | I/G | **WO kilidi YOK** → `assertKursunTabletMayWrite(tx)` :448 (İLK; KBA findFirst + bayrak + uygunluk okumaları) → RollOperation upsert :450 (+:470) → `copyStationCapabilitiesToRoll(tx)` :489 (RollProperty upsert ×N :211) | `roll_operations`, `roll_properties` | -/-/- | :501 (existedBefore'a göre CREATE/UPDATE) | upsert idempotent (`update:{}`) | `assertRollInStep` :407, step :409, `loadStationPropertyCaps(prisma)` :415, `existedBefore` :430 **tx DIŞI**; bayat `caps` tx'e girer (mod değişimi penceresi) |
| 843-947 | `finishStep` | I/G | **WO** :850 (İLK) → `assertKursunTabletMayWrite(tx)` :869 (tx DIŞI ikizi :812 mesaj sırası için) → **raw `UPDATE roll_movements … WHERE exitedAt IS NULL AND rollId=ANY(rollIds) RETURNING`** :888 → [sonraki adım: RollMovement createMany :914 → Roll updateMany :925 → `recomputeStepStatus` :930] / [SON adım: **`finalizeRollsAtLastStep(tx)` :936 → CNT sayaç satır kilidi TX İÇİNDE** → Roll ×N] → `recomputeStepStatus` :939 → `completeWorkOrderIfStepsDone` :943 | `work_orders`, `roll_movements`, `rolls`, `roll_barcode_counters`, `work_order_steps`, `traveler_cards` | -/-/- (`await import` :839 tx DIŞI) | :949 | `exitedAt IS NULL` koşullu kapanış = claim; `closed.length===0` → idempotent :907 | `openMovements` :789, `rollIds` :816, QC2 kontrolü :817-832 tx DIŞI → kapsam daraltması (`rollId=ANY`) ile yeni giren top süpürülmez ✓ (yorum :878-882) |
| 1090-1164 | `reopenStep` | I/G | **WO** :1093 → [sonraki adım: RollMovement deleteMany :1097 → **Roll çoklu claim** :1110] / [son adım: **Roll çoklu claim** :1128 → WO updateMany :1144 → `setWorkOrderCardStatuses` :1148] → RollMovement updateMany :1152 | `work_orders`, `roll_movements`, `rolls`, `traveler_cards` | -/-/- | (tx sonrası — :1164 sonrası okunmadı) | :1110/:1128 çoklu claim `count !== rollIds.length → 409` ✓ | `step` :1032, `closedMovements`/`loadLatestFinishTurn` :1062, güvenlik döngüsü :1071-1084 tx DIŞI → claim ikizi ✓ |

### 1.10 Tek/iki siteli servisler

| Dosya:satır | Fonksiyon | Tip/Ops | Kilit sırası / yazım | GP/I/O/PA | Audit | Claim / CTA |
|---|---|---|---|---|---|---|
| `reason-preset.service.ts:464-484` | `duplicate` | I/G | ReasonPreset updateMany `sortOrder gt → increment` :466 → create :470 (`@@unique([kind, code])`) | -/-/- | :486 (+`refreshReasonPresetCache` :485 bellek) | `nextFreeCode` :463 **tx DIŞI** (check-then-act; DB unique P2002 kapatır → error middleware) |
| `reason-preset.service.ts:505-513` | `reorder` | **B**/G | `prisma.reasonPreset.update ×N` (N = kind'ın satır sayısı) | GP=:507 (batch — meşru) | :515 | küme eşitliği guard'ı :498-503 tx DIŞI; `ids` Zod `min(1)` max YOK (`reason-preset.routes.ts:57`) ama sunucuda `ids.length === known.size` |
| `item.service.ts:228-304` | `create` (:131) | I/G **+withBarcodeRetry** :225 (closure: `nextItemCode()` :227 **HAVUZ**, tx öncesi) | **8026** `lockCodeScopeTx` :237 **İLK ifade** (yalnız `manualCode`) → `item.findMany` :240 (TÜM ürünler, `ITEM_CODE_CANDIDATE_SELECT` — tam tablo) → `resolveReactivateTarget` → Item claim :246 → pivot deleteMany :257-258 → update(nested create) :259 / create :280 | -/-/- | (tx sonrası — desen) | :246 tekil claim (`isActive:false` → true) count 0 → 409 ✓; `items_code_key` birebir eşleşme + katlanmış eşleşme kilit+tarama |
| `item.service.ts:453-478` | `update` (:359) | I/G | ItemAllowedColor deleteMany/createMany :455-459, ItemAllowedProperty :463-467 (**skipDuplicates YOK**), Item update :470 | -/-/- | :480 | renk/özellik doğrulamaları :420-450 tx DIŞI; `restData` sanitize katmanı — K-controller (bkz. sınır ötesi) |
| `work-session.service.ts:273-312` | `open` (:171) | I/G | WorkSession updateMany ×3 (:275 aynı cihaz NEW_LOGIN → :285 makine TAKEOVER (confirmTakeover) → :294 diğer cihaz NEW_LOGIN) → create :302 (**partial unique**, migration `20260702121000` — şema notu WorkSession:1031) → P2002 catch :314 (tx DIŞI) → 409 `SESSION_RACE` | -/-/- | :323 + TAKEOVER izleri :332 | koşullu kapanışlar idempotent; occupant pre-check ~:215-233 tx DIŞI → DB partial unique kapatır ✓; `takenOver` :262 tx DIŞI (yalnız audit) |
| `traveler-template.service.ts:193-209` | `setDefault` | I/G | findFirst(tx) :194 → updateMany `isDefault:false NOT id` :200 → update `isDefault:true` :204 (partial unique `traveler_card_templates_isDefault_key WHERE isDefault=true`; sıra load-bearing, yorum :189-190) | -/-/- | :210 | koşulsuz; yarışta P2002 → **`rethrowNameConflict` yok** → generic (error middleware `_key` regex'i) [VARSAYIM 409] |
| `tambur-manual.service.ts:1113-1207` | `createManualRoll` (:878) FAZ 2 | I/G | **[FAZ 1: `inventoryService.createInitialEntry` :1062 — AYRI tx (inventory:818): 8021 → CNT → Roll]** → FAZ 2: Roll findUnique :1114 → idempotent dal :1128-1147 / **Roll claim** :1151 (STOCK|WAREHOUSE + sack/ship/step null → IN_PRODUCTION + batchId) → RollMovement create :1174 → **WO satırı `touchWorkOrderTx` :1191 — claim'den SONRA** → `recomputeStepStatus` :1192 → `ensureWorkOrderInProgress` :1193 → WO updateMany COMPLETED→IN_PROGRESS :1194 → `setWorkOrderCardStatuses` :1199 | -/-/- | :1210 | :1151 tekil claim count 0 → 409 `ROLL_STATE_CHANGED`; FAZ1/FAZ2 iki tx (bilinçli; idempotent tekrar FAZ 2'yi bağlar) — §4 |
| `station-capability.service.ts:361-403` | `setCapabilities` (:257) | I/G | StationColor deleteMany `notIn` :369 + createMany skipDuplicates :373 (**deprecated tablo hâlâ yazılıyor**) → StationProperty deleteMany `notIn` :380 + createMany skipDuplicates :384 + updateMany mode ×N :397 | -/-/- | :405 | hedef-set replace (bayat diff yok) ✓; `oldColors/oldProperties` :350 tx DIŞI yalnız audit |
| `session-registry.service.ts:72-119` | `openLoginSession` | I/G | **8024** `hashtext(userId|deviceType)` :79 **İLK** → Session findFirst :84 (notify) → Session updateMany kick :110 → create :115 | -/-/- (`$executeRaw` 1) | audit YOK (login yolu; AuthService tarafı — K-KIM) | koşullu kapanış (`revokedAt:null`) idempotent; sıra ✓ (kilit → okuma) |
| `printed-document.service.ts:670-693` | `reissue` | I/G | PrintedDocument claim :674 (id+ACTIVE → SUPERSEDED) → create `version: latest.version+1` :681 (`@@unique([docType, sourceId, version])`) | -/-/- | :695 | tekil claim count 0 → 409 ✓ | `latest` :652, `requireBuilder(...).fresh(prisma)` :664, `buildSnapshotEnvelope(prisma)` :668 **tx DIŞI (havuz)** → snapshot pencerede eskiyebilir |
| `master-data-merge.service.ts:543-712` | `merge` (:505) | I/**`{timeout:120_000, maxWait:10_000}`** :711 | **8027** (1) :546 **İLK ifade** → taze findMany :553 → guard'lar :558-577 → `describeConflictTx` ×kural :589 → `markSideEffectsTx` :606 → `resolveConflictTx` :627 + **`$executeRawUnsafe` UPDATE ×kural** :629 (tablo/kolon `MERGE_MAP` sabiti, değerler `$1/$2` bind) → entity çoklu claim :639 → fieldPicks ad çakışma okuması :686 → entity updateMany :698 | -/-/- (`$executeRaw` 1 + `$executeRawUnsafe` döngüde) | :716 (döngü) + :737 + `DuplicateReviewService.markMerged` :756 (best-effort, tx DIŞI) | :639 çoklu claim `count !== sourceIds.length → 409` ✓; `MAX_MERGE_SOURCES=20` (:54, route `.max`), `MAX_ROWS_TO_MOVE=200_000` (:61) | ad çakışması :686-696 8027 altında ama sıradan `customer.update` 8027 almaz (§5-L7) |
| `helpers/guarded-hard-remove.ts:74-76` | `makeGuardedHardRemove` (5 handler: station/route/machine/recipe/defectType) | I/G | `config.deleteTx(tx,id)` — station: WorkSession deleteMany → Machine deleteMany → Station delete (:183-187); route: RouteStep → Route (:217-218); machine: PeripheralDevice updateMany null → WorkSession deleteMany → Machine delete (:265-268); recipe: ProductRecipeProperty → ProductRecipe (:326-327); defectType: delete (:351) | -/-/- | :78 | **guard sayımları `prisma.*.count` HAVUZ, tx DIŞI** (:62-72) → §5-L8; Express handler (`req/res`) helpers/ altında yaşıyor (beceri §7.5 — dosya adı bulgu değil) |
| `fabric-property.service.ts:378-392` | `update` (:269) | I/G | **`super.update` :376 AYRI yazım (BaseService, kendi tx/tekil yazımı) ÖNCE commit** → tx: StationProperty deleteMany `notIn` :383 + createMany skipDuplicates :386 → `replaceValuesTx` :391 | -/-/- | (super.update içinde) | iki adım atomik değil — **bilinçli** (yorum :372-375: bağ yazımı düşerse özellik eski bağlarıyla kalır) → §4 |
| `device.service.ts:263-268` | `assignHardware` | **B**/G | DevicePeripheral deleteMany + createMany skipDuplicates (F218) | GP=:264,:266 (batch — meşru) | :269 | `found` :258 tx DIŞI (aktif donanım doğrulaması) |
| `customer-standalone-label.service.ts:85-93` | `set` | I/G | CustomerStandaloneLabel deleteMany :86 → createMany :89 (dedup edilmiş `ids`; `@@unique([customerId, templateId])`) | -/-/- | :95 | replace-set; `existing` :75 tx DIŞI (`hiddenPreserved`), Zod `.max(200)` (`customer-standalone-label.routes.ts:22`) |
| `color.service.ts:319-358` | `syncCustomerAssignments` (:267) | I/G | CustomerColorAlias deleteMany :322 → updateMany :327 → updateMany :333 → createMany skipDuplicates :339 (F271) → updateMany ×N alias :353 | -/-/- | :360 | `existing` :279 tx DIŞI diff (skipDuplicates ile yumuşatılmış) |
| `audit.service.ts:220-262` | `archiveOlderThan` | I/G | **`SET LOCAL teks.audit_purge='on'` :233 İLK (sıra load-bearing; bekçi `test_audit_depth §10`)** → SystemLogArchive createMany skipDuplicates :235 (≤5000 satır, `ARCHIVE_BATCH_SIZE` :15) → SystemLog deleteMany :261 | -/-/- (`$executeRaw` 1) | — (arşivleyici) | `logsToArchive` :208 tx DIŞI (id listesi; idempotent) — scheduler tarafı K-OPS |

---

## 2. KİLİT SIRASI HARİTASI

Kaynak kısaltmaları: **A**=advisory 8021 (KK1 mükerrer) · **B**=8022 (parti no) · **C**=8023 (sevkiyat kapsamı) · **D**=8024 (oturum) · **E**=8025 (son-admin) · **F**=8026 (kod tekilliği) · **G**=8027 (birleştirme) · **WO**=`work_orders` satır kilidi (`touchWorkOrderTx` = `updateMany updatedAt`, `workorder-locks.helper.ts:54`) · **CNT**=`roll_barcode_counters` satır kilidi (`INSERT … ON CONFLICT DO UPDATE`, `roll-barcode.helper.ts:82-87`) · **KBA**=`kursun_bypass_assignments` · **SD/SDI**=fason sevk/kalem · **PD**=`printed_documents` · **TC**=`traveler_cards` · **Step**=`work_order_steps` · **Mv**=`roll_movements`.

### 2.1 Gözlenen edinim sıraları (kapsam içi)

| Akış | Sıra (tx içi; `[…]` ayrı tx/önce) |
|---|---|
| `tambur.finalize` :925 | [CNT] → **WO** → Roll(parent) → RollError → Roll(child ins.) → Step(stepData) → Mv → Roll(parent) → RollOperation → Step(recompute) → WO(updateMany) → TC |
| `tambur.cutOpenFabric` :2789 | [CNT] → **WO** → Roll(child ins.) → Roll(parent) |
| `tambur.finalizeOpenFabric` :3180 | [CNT] → **WO** → Roll(parent) → Roll(child) → Mv → RollOperation → Step → WO → TC |
| `tambur.cutWarehouseRoll` :2131 | [CNT] → Roll(child ins.) → Roll(parent) — WO yok |
| `tambur.finalizeWarehouseCut` :2466 | [CNT] → Roll(parent) → Roll(child) — WO yok |
| `tambur.reportError` :1810 | Roll → RollError |
| `kursun-qc.finishStep` :843 | **WO** → Mv(raw UPDATE) → {Mv ins. → Roll → Step} \| {**CNT** → Roll×N} → Step → WO → TC |
| `kursun-qc.reopenStep` :1090 | **WO** → Mv(del) → Roll → {WO → TC} → Mv |
| `kursun-qc` QC2 :441 | RollOperation → RollProperty (WO yok) |
| `kursun-bypass.assign` :768 | **WO** → KBA |
| `kursun-bypass.completeFromDistribution` :1368 | **WO** → KBA → Mv → RollProperty → **CNT** → Roll → Step → WO → TC |
| `kursun-bypass.completeFromTambur/Unassigned` :1815/:1967 | **WO** → KBA(yalnız atanmış) → Mv → RollProperty → Mv(ins.) → Roll → Step |
| `kursun-bypass.cancelAssignment` :1173 | KBA |
| `batch.moveRolls` :374 | **WO** → SDI/SD/PD (cerrahi) → Roll → TC → Roll → Batch |
| `batch.mergeBatches` :580 | **WO** → SD → Roll → TC → Roll → SDI → SD → PD → Batch |
| `batch.splitBatch` :870 | **WO → B(8022)** → Batch(ins.) → Roll → TC → SDI/SD/PD → Roll |
| `tambur-manual.createManualRoll` FAZ 2 :1113 | [FAZ1 ayrı tx: A → CNT → Roll] → **Roll(claim) → Mv(ins.) → WO** → Step → WO → TC |
| `tambur-undo.applySingle` :1058 | Roll(child) → Roll(parent) |
| `tambur-undo.applySingleRestore` :1221 / `applyFull` :1434 | Roll(child) → Mv → Roll(parent) → RollOperation → RollProperty → **Step → WO** → TC — **WO kilidi HİÇ alınmaz** |
| `kartela.dispatch` :282 | (sayaç findMany, kilitsiz) → KartelaDispatch → PD → Roll(claim) |
| `kartela.receive` :610 | KartelaReceipt → KartelaReceiptItem → Roll(claim) → Swatch |
| `kartela.cancelDispatch/Receipt` :413/:791 | KartelaDispatch→PD→Roll / KartelaReceipt→Swatch→Roll |
| `item.create` :228 | [havuz: `nextItemCode`] → **F(8026)** → Item(claim) → pivotlar |
| `session-registry` :72 | **D(8024)** → Session |
| `permission-management` :304/:377/:686/:747 | **E(8025)** → UserPermission → User |
| `master-data-merge` :543 | **G(8027)** → (N tablo raw UPDATE) → entity(claim) |
| `inventory.createInitialEntry` :818 (K3a — namespace için) | **A(8021)** :844 → CNT (`generateRollBarcode(tx)` :905) → Roll ins. :906 |

### 2.2 Sıra tutarsızlıkları (② için)

| # | Gözlem | Kanıt | Çevrim (deadlock) adayı mı? |
|---|---|---|---|
| S1 | **WO kilidinin konumu üç farklı yerde:** kardeşlerde tx'in İLK ifadesi; `tambur-manual` FAZ 2'de Roll claim + Mv insert'ten **SONRA** (:1191); `tambur-undo` restore/full'de **HİÇ yok** ama Step + WO satırlarına yazılıyor (:1377-1378, :1668-1669) | `tambur-manual.service.ts:1151→1174→1191`; `tambur-undo.service.ts:1330→1377→1378` | **Evet, dar pencere:** undo tutar Step (recompute UPDATE) → ister WO(COMPLETED→IN_PROGRESS); manual tutar WO → ister Step (recompute). Koşul: aynı **COMPLETED** WO'da eşzamanlı "manuel top ekle" + "iş emrine geri al". [VARSAYIM] olasılık düşük — ② ölçmeli. Finalize↔undo çevrimi için WO'nun COMPLETED olması gerekir ve o WO'da IN_PRODUCTION top kalmaz → pratikte kapalı. |
| S2 | **B(8022)** kapsam içinde WO → B (split). Dış çağıranlar: `workorder.service.ts:4553` `createBatchTx` → :4565 `recomputeStepStatus` → :4566 `ensureWorkOrderInProgress` (WO `PLANNED` ise WO satır kilidi **B'den SONRA**); aynı dosyada `touchWorkOrderTx` yalnız :4778 (başka fonksiyon). `workorder.service.ts:4239` (clone/transfer) → :4249 recompute (yeni WO). `workorder-manual-move.service.ts:699` B alır, dosyada `touchWorkOrderTx` yok (grep 0). | grep `createBatchTx(` + `touchWorkOrderTx(` | **Aday:** split tutar WO → ister B; attach (:4553) tutar B → ister WO (yalnız WO PLANNED iken, ilk attach). [VARSAYIM] dar pencere; K3a doğrulasın (sınır ötesi). |
| S3 | **CNT** üç farklı konumda: tambur → ayrı tx (önce, 39 ms — `test_barcode_reservation`); `finishStep`/bypass complete → WO'dan SONRA tx İÇİNDE (`roll-finalize.helper.ts:164-169` bilinçli); KK1 → A'dan SONRA tx içinde. | ilgili satırlar | **Çevrim yok:** CNT'den sonra WO/A alan akış yok. Bedel: `finishStep` son-adım dalında sayaç satırı kalan tx boyunca (Roll×N + recompute + complete) tutulur → tüm KK1 girişleri o kadar bekler (helper ölçümü: 1345 ms vs 39 ms). HOTSPOT H-2 (bütçe/gecikme sorusu, veri değil). |
| S4 | Advisory kilitlerin hiçbiri bir başka advisory ile aynı tx'te alınmıyor (A/B/D/E/F/G birbirinden bağımsız; B yalnız WO'dan sonra) → advisory↔advisory ABBA yok. | §2.1 | Hayır |
| S5 | Tambur'un WO-siz iki yolu (`cutWarehouseRoll`, `finalizeWarehouseCut`) yalnız Roll satırlarını kilitler; aynı parent için sıralar farklı (child→parent vs parent→child) ama child insert kilit çakıştırmaz. | :2132→:2242 vs :2473→:2498 | Hayır (P2025/claim ile 409) |

### 2.3 Bekçi: kilit VARLIĞI mı, SIRASI mı ölçülüyor? (beceri §4.2)

| Kilit | Bekçi | Ne ölçüyor | Sıra bekçili mi |
|---|---|---|---|
| A 8021 | `scripts/test_kk1_duplicate_guard.ts` | `Promise.allSettled` paralel sonda (:286) | ✓ (N paralel → 1 başarı) |
| B 8022 | `scripts/test_batch_number_format.ts` | `$executeRaw` çağrılarını proxy ile yakalayıp `lock(...)` / `execRaw` **çağrı SIRASINI** kaydediyor (:79); paralel sonda YOK | ✓ sıra (çağrı dizisi), paralel değil |
| C 8023 | `scripts/test_shipment_scope_lock.ts` | kaynak metni regex (:97) + 1-arg formu `src` genelinde tarar (:111-117) + 1 paralel (K3a) | kısmen |
| D 8024 | `scripts/test_session_registry.ts` | **paralel YOK, advisory geçmiyor** (grep 0/0) | ✗ — kilit tamamen bekçisiz |
| E 8025 | `scripts/test_admin_guard_race.ts` | `Promise.allSettled` (:121) | ✓ |
| F 8026 | `scripts/test_item_code_case_uniqueness.ts` | `Promise.allSettled` (:533) | ✓ |
| G 8027 | `scripts/test_master_data_merge*.ts` | **paralel YOK, advisory geçmiyor** (grep 0/0) | ✗ — kilit bekçisiz |
| CNT konumu | `scripts/test_barcode_reservation.ts` | T1<100 ms VE T2=30/30 (:337-338) + kaynak regex `reserveRollBarcodes*(tx` (:169) | ✓ (konum + havuz) |
| WO satır kilidi | (dolaylı) `test_kursun_regime_lock`, `test_k14_lock_edges` — paralel YOK | — | ✗ sıra bekçisiz |

---

## 3. ÇİFT-MOD HELPER ENVANTERİ (`src/services/helpers/` — 71 dosya + `codec/`, `raster/`)

### 3.1 DB YAZAN / KİLİT ALAN / HAM SQL koşturan helper'lar (20 dosya — beceri §7.5 "helpers/ tuzağı")

| Helper (dosya:satır) | İmza (client parametresi) | Yazıyor / kilit | Çağrı yerleri → geçilen client | Not |
|---|---|---|---|---|
| `workorder-locks.helper.ts:50` `touchWorkOrderTx(tx)` | `Prisma.TransactionClient` (yalnız tx) | **WO satır kilidi** (updateMany updatedAt) | kapsam içi: tambur :931/:2793/:3184 · kursun-qc :850/:1093 · kursun-bypass :771/:1369/:1816/:1968 · batch :384/:594/:880 · tambur-manual :1191 — hepsi `tx` ✓; kapsam dışı: subcontractor ×7, workorder :4778, shipping/inventory (K3a) | Yan etki: her kilit alımı `updatedAt`'i günceller → "Son İşlem" sıralaması (CLAUDE.md `updatedAt desc`) kilit alan her akışta oynar (sınır ötesi not) |
| `workorder-locks.helper.ts:90` `computeWorkOrderLocks(db)` | `Db = PrismaClient \| TransactionClient` | salt okur | workorder.service :2272/:4675/:4912/:5627 → `prisma`; :4779/:5274 → `tx` | çift-mod, yalnız okuma — sızıntı sınıfı değil |
| `batch-dispatch-surgery.helper.ts:116` `performDispatchSurgeryTx(tx)` | tx | SDI/SD/PD yazar; `generateDispatchNoTx` :69 **findMany sayacı (kilitsiz, `dispatchNo @unique` + çağıranın retry'ı)**; `freezeForSource` :369; claim'ler count kontrollü (:257, :287, :308, :348) | batch :464/:931 → `tx` (ikisi de `withBarcodeRetry` içinde ✓) | tek kaynak; K3a'nın `subcontractor.nextPrefixedSequence`'ının kopyası (yorum :30-33) |
| `traveler-card-dirty.helper.ts:34/:49` `markTravelerCard(s)DirtyTx(tx)` | tx | TC updateMany (`contentDirty:false` koşullu → idempotent) | batch :254/:485/:702 → `tx`; (K3a: subcontractor/workorder) | koşullu kapanış sınıfı, count gereksiz |
| `traveler-card-fanout.helper.ts:18` `setWorkOrderCardStatuses(tx)` | tx | TC updateMany (from→to) | roll-step :212 → tx; tambur-undo :1384/:1675; kursun-qc :1148; tambur-manual :1199 → tx | idempotent |
| `roll-step.helper.ts:29` `recomputeStepStatus(tx)` | tx | Step update (statü değişirse) + `ensureWorkOrderInProgress` (WO updateMany PLANNED) | tambur :1244/:3386; kursun-qc :930/:939 (dinamik import); kursun-bypass :1400/:1868-1869/:2031-2032; tambur-undo :1377/:1668; tambur-manual :1192; (K3a: inventory/subcontractor/workorder) — hepsi tx | **türetilmiş invariant** (3 sayım → statü) READ COMMITTED altında; yorum :190 "çağıran WO'yu kilitlemeli" — tambur-undo bu sözleşmeyi **karşılamıyor** (H-1) |
| `roll-step.helper.ts:193` `completeWorkOrderIfStepsDone(tx)` | tx | WO updateMany (notIn terminal) + TC | tambur :1282/:3391; kursun-qc :943; kursun-bypass :1401 → tx | aynı sözleşme (:190-191) |
| `roll-step.helper.ts:352` `assertWoAtStepKind` | **global `prisma`** (parametre yok) | salt okur | kart okutma yolları (K3a) | helper `lib/prisma` import ediyor (:19) — yalnız bu fonksiyon için |
| `roll-finalize.helper.ts:144` `finalizeRollsAtLastStep(tx)` | `TxClient` | **CNT satır kilidi tx içinde** (:172) + Roll update ×N | kursun-qc :936; kursun-bypass :1399 → tx | bilinçli (yorum :164-169) — H-2 |
| `roll-finalize.helper.ts:75` `loadProducedBuckets(db)` | `ReadDb` | salt okur | workorder.service :1842/:2209 → `prisma` | — |
| `roll-barcode.helper.ts:39/:74/:109` `generateRollBarcode / reserveRollBarcodes(InOrder)(db)` | tipi `TransactionClient` ama **havuz `prisma` de geçiliyor** (cast'siz — `$queryRaw` ortak) | **CNT satır kilidi** (`INSERT…ON CONFLICT DO UPDATE`) | `prisma` (tx ÖNCE): tambur :906/:2116/:2462/:2775/:3178 · `tx` (tx İÇİ): inventory :905 (K3a), roll-finalize :172 | sözleşme :52-72: tx dışında çağrılmak için; tx içi çağrılar bilinçli (yorumlu) |
| `roll-disposition.helper.ts:151` `applyRollDispositionsTx(tx)` | `TxClient` | Roll updateMany claim :239 (+:275), Roll update barkod :299, `$executeRaw` :378 (movement kapanışı) | workorder.service :3491/:4019; workorder-batch-drop :360 → tx (K3a) | "Ek WO kilidi ALINMAZ" (yorum :189-194) — çağıranlar alıyor; `DISPOSITION_MAX_ROLLS=200` :76 |
| `roll-variance.helper.ts:50` `recordVarianceTx(tx)` | `Tx` | RollVariance create (qty≤0 → atlar) | tambur ×5, tambur-undo ×4, subcontractor ×2 → hepsi tx ✓ | RESTRICT FK (CLAUDE.md) |
| `tambur-plan-gate.helper.ts:168` `recordPlanDeviationTx(tx)` | tx | RollPlanDeviation createMany | tambur :1252/:2902/:3279; subcontractor :3120 → tx ✓ | `assertRollMatchesPlan` :65 **global `prisma`** (salt okur, pre-tx; `Promise.all` :75 havuz — meşru) |
| `station-capability-transfer.helper.ts:183` `copyStationCapabilitiesToRoll(tx)` | `TxClient` | RollProperty upsert ×N | kursun-qc :489; kursun-bypass :1396/:1839/:2007; inventory :4736 → tx ✓ | `loadStationPropertyCaps(tx)` :55 tipi tx ama **`prisma` geçiliyor**: kursun-qc :415/:1405, inventory :4654 (pre-tx okuma → bayat `caps` tx'e taşınır) |
| `kursun-bypass-guard.helper.ts:149/:194` `repointPendingBypassAssignmentsTx / voidStalePendingBypassAssignmentsTx(tx)` | `TxClient` | KBA updateMany | workorder.service :4228/:4068/:3560; workorder-split :299/:528/:680 → tx (K3a) | — |
| `kursun-bypass-guard.helper.ts:54/:78/:105` `findPendingBypassAssignmentTx / assertStepNotBypassAssigned / hasBypassClosureOnProcessQcTx(db)` | `Db` çift-mod | salt okur | :819 → tx; :1629 → prisma; :1527 → db; inventory :4580 → prisma | okuma; tx içi çağrı guard amaçlı ✓ |
| `order-status.helper.ts:32/:96/:212` `touchOrderLinesTx / recomputeOrderStatus(ForOrders)(tx)` | tx | OrderLine satır kilidi (id-sıralı döngü :36-39), Order/OrderLine update | shipping :1878/:1975/:2210; subcontractor :6233/:6298; order :512-513/:2537/:3411 → tx (K3a) | **kilit protokolü** yorum :205-210 (alt-küme kilidi + tam-küme yazımı → deadlock riski) — K3a doğrulasın |
| `work-session.helper.ts:40/:86/:151` `resolveActiveSession / getStampContext / sweepIdleSessions` | **global `prisma`** (parametre yok) | WorkSession updateMany (IDLE/NEW_LOGIN öz-onarım) | controller'lar (`getStampContext`, tx DIŞI kuralı yorum :84) | helper'da iş kuralı + yazım; sözleşmesi "tx dışında çağrıl" |
| `workorder-clone.helper.ts:31/:67/:202` `generateWorkOrderNumberTx / cloneWorkOrderTx / repointRollsTx(tx)` | tx | **İE sayacı findMany (kilitsiz; `withBarcodeRetry` kapsamında olmalı — yorum :26-27)**, WO+Step+TC insert, Roll/Mv/RollOperation/RollError updateMany ×adım | workorder.service :4181/:4213; workorder-split :471/:481/:639/:663 → tx (K3a) | çağıranların retry sarmalı ayrıca doğrulanmadı [kapsam dışı — K3a] |
| `code-unique.helper.ts:74` `lockCodeScopeTx(tx)` | tx | **F(8026)** | **yalnız `item.service.ts:237`**; `decideCodeUniqueness` (saf) ayrıca `subcontractor-management.service.ts:202/:512` ve `base.service.ts:1052`'den **kilitsiz** çağrılıyor | başlık :19-21 altı model sayıyor, kilit yalnız Item'da → HOTSPOT H-6 |
| `duplicate-guard.helper.ts` | saf (DB'ye dokunmaz, :4) — `DUPLICATE_GUARD_LOCK_NS` sabiti + anahtar üretici | — | inventory :835/:844 | yorum :22-23 **bayat** (1-arg uzay iddiası) |
| `shipment-locks.helper.ts:52/:65/:84` `lockShipmentScopeTx / touchWarehouseSackTx / touchShipmentPlannedTx(tx)` | tx | **C(8023)**; Sack/Shipment satır kilidi + claim (count 0 → 409) | shipping :2159; return :490; (touch*: shipping/sack — K3a) | yorum :21-23 **bayat** (1-arg uzay iddiası) |
| `guarded-hard-remove.ts:47` | Express handler fabrikası; global `prisma` (guard'lar) + `$transaction` (:74) | 5 handler yazar | route dosyaları | §1.10 |
| `audit-value-resolver.ts:86` `resolveChangeValues` | global `prisma`, `$queryRaw` (:110; tablo/kolon SABİT haritadan, `Prisma.raw` ile) | salt okur | audit okuma yolları | `Prisma.raw(\`"${label}"\`)` — girdi haritadan (:57-67), kullanıcıdan değil |

### 3.2 Çift-mod (Db/TxOrPrisma/Client) SALT-OKUR helper'lar

| Helper | İmza | Çağrı yerleri → client | Not |
|---|---|---|---|
| `workorder-target-color.helper.ts:109` `assertTargetColorChange(db)` | `Db` | workorder :4794 → tx; workorder :4697, workorder-link :525 → **prisma** | guard'ın havuzla çağrılan iki yolu: karar tx dışı → K3a (sınır ötesi) |
| `kursun-bypass-eligibility.helper.ts:96/:191/:256` `loadBypassEligibilitySignals / resolveStepBypassEligibility / assertKursunTabletMayWrite(db)` | `Db` | :448/:869, inventory :4164/:4680 → tx; kursun-qc :812/:613/:1500, kursun-bypass :597 → prisma | tx içi ikizleri var (:812↔:869) ✓ |
| `quality-grade.helper.ts:16/:35` `resolveQualityGradeId(Strict)(code, client = prisma)` | **varsayılan `prisma`** | tambur :2106/:2435/:2784-2785/:3169; inventory :773/:3877 — **hepsi ikinci argüman VERMİYOR → havuz** (pre-tx okuma) | okuma; tx'e bayat kalite id taşınır (katalog değişimi penceresi) — düşük |
| `coverage.helper.ts:56/:91/:168` | `Client` (yapısal tip) | order/production-balance → prisma; :128 → client | okuma |
| `customer-name.helper.ts:77` `batchLoadAliases(client)` | yapısal | label.service :1325 → prisma | okuma |
| `fold-type.ts:86/:112` `loadFoldTypeCatalog / resolveFoldTypeForWrite` | global `prisma` | tambur ×4, tambur-manual ×2, inventory :3884 — pre-tx | okuma |
| `targetable-property.helper.ts:56/:77` | global `prisma` | workorder/item/route/subcontractor/inventory — pre-tx | okuma |
| `label-format.resolver.ts`, `label-intent.helper.ts`, `label-routing.resolver.ts`, `sack-content-mismatch.helper.ts`, `color-assignment.helper.ts` | global `prisma` | render/pre-tx yolları | okuma |
| `roll-step-scope.helper.ts:88` `collectRollStepScopeTx(tx)` | tx | inventory :3195/:3393/:3558; roll-disposition :195 → tx | okuma (tx içi) |
| `system-setting.service.ts` `read*(tx?: Pick<prisma,"systemSetting">)` — 58 `const client = tx ?? prisma` (:2090-:3454) | **opsiyonel tx** (`tx?:` deseni — repoda bu kalıp yalnız burada + `customer-alias.service.ts:223`) | batch :128 → tx; kursun-bypass :826/:1491 → tx/db; order-status :218 → tx; `readTamburOverQuantityEnabled()` tambur :2097/:2748 → **havuz** | enforcement okuyucuları cache'siz (yorum :2399, :3283) |

### 3.3 Saf / DB'ye dokunmayan helper'lar (yazım=0, raw=0, prisma import=0)

`allocation` · `audit-diff` · `backup-naming` · `db-swap-command` · `device-transport` · `fason-open-dispatch` (where-parçası tek kaynak, AST bekçili) · `fason-work-instructions` · `hidden-status` · `kursun-bypass-eligibility` (okuma fonksiyonları hariç saf kısımlar) · `label-*` (14 dosya: canvas/html/ppla/pplb/zpl/rawcode/renderer/variant/context-fit/field-values/flow-to-canvas/shared/landscape) · `name-normalize` · `native-label.shared` · `native-preview` · `offsite-backup` (child process — K-OPS) · `order-line-scope` · `pg-admin-client` / `pg-conn` / `pg-tool` (ayrı `pg` bağlantısı, DDL/dump — K-OPS) · `printer-transport` · `roll-cancel-restore` · `roll-entry-station` · `sack-invariants` · `step-capability` · `subcontractor-cancel` · `subcontractor-shrink` · `workorder-rolls` · `codec/` · `raster/`.

---

## 4. TX SIZINTISI ADAYLARI (atomik olması beklenebilecek işin bir parçası havuz client'ında / ayrı tx'te)

| # | Yer | Ne dışarıda | Nasıl telafi ediliyor | Sınıf |
|---|---|---|---|---|
| T1 | `fabric-property.service.ts:376 → :378` | `super.update` (skaler alanlar) ayrı yazım; pivot/değer replace ikinci tx | yorum :372-375: ikinci düşerse özellik ESKİ bağlarıyla kalır (adı değişmiş) — **bilinçli, yarım durum kabul edilmiş** | bilinçli |
| T2 | `tambur-manual.service.ts:1062 → :1113` | FAZ 1 `createInitialEntry` (top doğar, 8021 + CNT) ayrı tx; FAZ 2 bağlama tx | FAZ 2 düşerse top `STOCK/WAREHOUSE`'ta adımsız kalır; aynı `clientToken` ile tekrar FAZ 1'de P2002→mevcut top, FAZ 2 :1128 idempotent dal bağlar | bilinçli (dosya başlığı) |
| T3 | `guarded-hard-remove.ts:62-72 → :74` | bağımlılık sayımları havuz | Restrict FK'ler P2003; **SetNull FK'ler (Roll.entryStationId :165, RollOperation.machineId :236, Roll.createdMachineId :246, PeripheralDevice.machineId :172/:265, RollError.defectTypeId :345) DB'de kapatılmaz** | §5-L8 |
| T4 | `label-template.service.ts:572 → :579` (`hardDelete`), :533 (`deactivate`, tx yok) | "default mı" kontrolü havuz | tx içinde ikiz yok; `setContextDefault` :996 ile yarış | §5-L1 |
| T5 | `printed-document.service.ts:664-668 → :670` (`reissue`) | `fresh(prisma)` + `buildSnapshotEnvelope(prisma)` havuz | claim :674 versiyon yarışını kapatır; snapshot içeriği pencerede eski olabilir | düşük |
| T6 | `reason-preset.service.ts:463 → :464` | `nextFreeCode` havuz | `@@unique([kind, code])` P2002 | düşük (409/500 haritası ② baksın) |
| T7 | `item.service.ts:227 → :228` | `nextItemCode()` havuz (otomatik kod) | `items_code_key` + `withBarcodeRetry` (P2002 → yeni kod) | bilinçli (yorum :222-224) |
| T8 | `tambur.service.ts:906/:2116/:2462/:2775/:3178` | barkod rezervasyonu havuz, tx öncesi | bilinçli, ölçülmüş (F-CORE-VER-001); replay'de boşluk (F-CORE-VER-008 açık) | bilinçli |
| T9 | `kursun-qc.service.ts:415/:1405`, `inventory.service.ts:4654` | `loadStationPropertyCaps(prisma)` havuz → `caps` tx'e param olarak girer | mod değişimi penceresi; `copyStationCapabilitiesToRoll` upsert idempotent | düşük |
| T10 | `permission-management.service.ts:696/:763` | `SessionRegistryService.revokeAllForUser` tx sonrası best-effort | `tokenVersion++` tx içinde (:692/:758) — oturum zaten düşer; registry ikinci katman | bilinçli |
| T11 | `master-data-merge.service.ts:756` | `DuplicateReviewService.markMerged` tx dışı | best-effort (yorum :754-755) — kuyruk izi kaybolabilir, birleştirme geri sarılmaz | bilinçli |
| T12 | `traveler-card.service.ts:350-364 → :368` | `card` + `resolvePrintPlan` havuz | yorum :340-347 "bilinen sınır" | bilinçli |
| T13 | `audit.service.ts:208 → :220` | `logsToArchive` havuz | `skipDuplicates` + id listesi → idempotent | — |
| T14 | `work-session.service.ts:262 → :273` | `takenOver` havuz (yalnız audit) | — | — |
| T15 | `station-capability :350`, `color :279`, `customer-standalone-label :75`, `device :258`, `permission-management :272/:1000` | eski küme/diff havuz | replace-set + `skipDuplicates` (device/color/station) ✓; `permission-management` createMany **skipDuplicates YOK** (:315, :1012) | §5-L2 |
| T16 | Audit çağrılarının tamamı tx sonrası (konvansiyon) | — | rollback'te audit yok ✓; retry sarmalı sitelerde (`withBarcodeRetry`) audit tek kez (tx dışında) ✓ | — |

---

## 5. CHECK-THEN-ACT ADAYLARI (beceri §3.2 sınıflandırmasıyla)

| # | Yer | Okuma → karar → yazma | Koruma var mı | Sınıf |
|---|---|---|---|---|
| L1 | `label-template.service.ts:572-588` `hardDelete` (ve `deactivate` :533-546, tx yok) | `labelContextDefault.findFirst` → değilse sil/pasifleştir | tx içi ikizi **yok**; `setContextDefault(kind,id)` :986-1008 kendi tarafında da tx dışı okuyor → iki istek çaprazlanırsa bağlam varsayılanı silinmiş/pasif şablona işaret eder; sonraki baskı FAIL-CLOSED 404 (CLAUDE.md etiket kuralı) | **gerçek CTA**, blast radius küçük (admin ekranı) |
| L2 | `permission-management.service.ts:272-336` `setUserPermissions`; :1000-1023 `applyTemplate` | `existing` diff → `createMany` **skipDuplicates yok** | `userId_permissionId` unique → P2002 → tüm tx rollback (500/409 haritası ② baksın); veri bozulmaz | düşük |
| L3 | `permission-management.service.ts:190-221` `grantPermission`, :304 `setUserPermissions` (`toUpdate` dalı) | admin grant'ının `validUntil`'ı **geçmişe** çekilirse `effectiveAdminWindow` (:587-594) dışına düşer, ama `removesAdmin` (:302) = "hedefte admin id yok mu?" → **false** → 8025 kilidi ve `assertAdminCoverageAfterChange` koşmaz | Yarış değil, **mantık boşluğu**: son admin'in yetkisi tarihle söndürülebilir → sistem yöneticisiz kalır (F253 amacı) | HOTSPOT H-3 (KIM'e de yönlendir) |
| L4 | `traveler-card.service.ts:269-291` `reprint` | `existing.version` → `version: existing.version + 1` koşulsuz | claim yok; iki eşzamanlı reprint aynı sürümü yazar (sürüm bir kez artar) | düşük; uç çağrılmıyor (:252) |
| L5 | `traveler-card.service.ts:350-386` `recordPrintEvent` | `resolvePrintPlan` havuz → `version: plan.version` koşulsuz | dokümante sınır (:340-347) | bilinçli |
| L6 | `reason-preset.service.ts:498-513` `reorder` | `known` kümesi → batch update | arada eklenen satır batch'e girmez (kendi sortOrder'ıyla kalır) — bozulma değil | — |
| L7 | `master-data-merge.service.ts:686-698` ad çakışma kontrolü | findMany → clash → updateMany | 8027 yalnız merge'leri serileştirir; sıradan ad güncellemesi (`customer.update` vb.) 8027 almaz → katlanmış ad yarışı; DB partial UNIQUE `<tablo>_nameFold_key` **yumuşak kapı** (mükerrer varken atlanmış olabilir — CLAUDE.md 2026-08-22) | gerçek CTA, olasılık düşük |
| L8 | `helpers/guarded-hard-remove.ts:62-76` | count'lar → delete | Restrict FK → P2003 400; **SetNull FK'ler** sessiz NULL (T3) | gerçek CTA, admin işlemi |
| L9 | `kartela.service.ts:223-246` idempotent kapı | "aynı firma + aynı top kümesi açık sevk" heuristiği → erken dön | Roll claim :323 çift sevki kapatır (ikinci istek 409); kapı yalnız replay konforu | — |
| L10 | `kursun-qc.service.ts:407-439` QC2 | `assertRollInStep`, `existedBefore` havuz | upsert idempotent; `existedBefore` yalnız audit türü | — |
| L11 | `tambur-undo.service.ts:1246-1256, :1450-1460` WO status guard'ı | tx içi ama **kilitsiz** okuma → sonra Step/WO yazımı | kardeşler WO satır kilidiyle serileşir; burada eşzamanlı WO iptali (`softDelete cancelClaim`, K3a) araya girebilir → iptal edilmiş WO'da top diriltilir | HOTSPOT H-1 |
| L12 | `tambur.service.ts:2343-2640` `finalizeWarehouseCut` | idempotency yorum :2448-2458 "tx içinde clientToken @unique → P2002 → idempotent yanıt" | **`clientToken` yazılmıyor, catch yok** (aralıkta yalnız yorumda geçer); replay → claim :2473 count 0 → **409**, üstelik :2462 rezervasyonu bir numara yakmış olur | HOTSPOT H-5 (yorum↔kod ayrışması) |
| L13 | Kapsamdaki **atomik claim'ler** (sınıflandırma — YP önleyici): tekil `{id,status}` claim count kontrollü: tambur :953/:1818/:2473/:3202, kartela :416/:794, kursun-bypass :955/:1176/:2272, batch :499/:748/:792, printed-document :674/:761, item :246, tambur-undo :1073/:1143/:1157/:1260/:1330/:1563, tambur-manual :1151 · **çoklu claim** count===len (hepsi-ya-hiç, yazılı): kartela :323/:437/:678/:808/:821/:1395, batch :810, kursun-qc :1110/:1128, tambur-undo :1488, master-data-merge :639, surgery :253/:344 · **koşullu kapanış** (`exitedAt IS NULL` / `revokedAt null` / `contentDirty:false` / `mergedIntoId null`): kursun-qc :888, kursun-bypass :2339, tambur :1173/:3324, session :110, traveler-dirty :38, work-session :275/:285/:294 — count gereksiz (idempotent) · **toplu/idempotent temizlik**: label-template updateMany'ler, station-capability, color, permission :327 | — | referans |

---

## 6. ADVISORY LOCK NAMESPACE TABLOSU

| NS | Key | Tanım (dosya:satır) | Alındığı yer | Korunan okuma (satır) | Sıra ✓ | Tutulma süresi | Bekçi (§2.3) |
|---|---|---|---|---|---|---|---|
| **8021** | `hashtext("kk1dup\|entrySource\|itemId\|colorId\|qty(3)\|width(3)\|userId\|machineId")` (`duplicate-guard.helper.ts:59-80`) | `duplicate-guard.helper.ts:28` | `inventory.service.ts:844` (guard bayrağı açıksa) | `roll.findFirst` :848 (ikiz) → sonra CNT :905 → create :906 | ✓ kilit → okuma → sayaç (yorum :820-830 ABBA gerekçesi) | tx sonu (kısa) | paralel ✓ |
| **8022** | `1` (sabit — sistem geneli tek seri kapı) | `batch.service.ts:96-99` | `batch.service.ts:126` (`generateBatchNumberTx` İLK ifade) | :128 bayrak, :129/:162 son kısa parti, :133 günün partileri | ✓ (fonksiyon içinde); tx içinde WO'dan **sonra** (split :880→:920) | **kalan tx boyunca** (Batch ins. + roll + kart + sevk cerrahisi …) — sayaç-kilidi-tx-başında sınıfı (beceri §2 3. kural) ama parti doğuşu seyrek | çağrı-sırası proxy'si ✓, paralel ✗ |
| **8023** | `hashtext(shipmentId)` | `shipment-locks.helper.ts:28` | `shipping.service.ts:2159`, `return.service.ts:490` (K3a) | K3a | K3a | K3a | kaynak-metin + 1 paralel |
| **8024** | `hashtext("userId\|deviceType")` | `session-registry.service.ts:32` | `session-registry.service.ts:79` (İLK ifade) | `session.findFirst` :84, `updateMany` :110 | ✓ | tx sonu (3 ifade) | **bekçisiz** |
| **8025** | `1` (sabit) | `permission-management.service.ts:21-22` | :609 (`acquireAdminGuardLock`, `$queryRaw` alt-sorgu sarmalı — void deserialize gerekçesi :605-606) ← :306 (koşullu), :379 (koşullu), :687, :748 | :623 / :647,:652 / :381 | ✓ | tx sonu (kısa) | paralel ✓ |
| **8026** | `hashtext("item\|foldCode(code)")` (`ITEM_CODE_LOCK_SCOPE="item"` `item.service.ts:77`) | `code-unique.helper.ts:57` | `item.service.ts:237` (manuel kodda; tx İLK ifade) | `item.findMany` :240 (tüm ürün kodları) | ✓ | tx sonu | paralel ✓ |
| **8027** | `1` (sabit) | `master-data-merge.service.ts:50-51` | :546 (İLK ifade) | :553 taze findMany + tüm guard'lar + :686 ad kontrolü | ✓ | **tx sonu — 120 s'ye kadar** (özel timeout :64/:711; `MAX_ROWS_TO_MOVE` 200k) → tüm birleştirmeler seri | **bekçisiz** |
| 1-argümanlı form (`pg_advisory_xact_lock(bigint)` / `hashtext(...)` tek arg) | — | — | **0 kullanım** (grep; `test_shipment_scope_lock.ts:117` bunu bekçiliyor) | — | — | — | ✓ |
| Session-tipi `pg_advisory_lock(` / `pg_try_advisory` | — | — | **0** | — | — | — | — |

**hashtext çakışma olasılığı:** 8021/8023/8024/8026 `hashtext` (int4, 2³² uzay) kullanır; uzaylar NS ile ayrıldığı için çakışma yalnız **aynı NS içinde** iki farklı anahtarın aynı int4'e düşmesidir. KK1 anahtar kardinalitesi gün başına yüzler/binler → doğum-günü olasılığı ihmal edilebilir; sonucu gecikme (beceri §4.1). Sabit `1` anahtarlı üç kilit (8022/8025/8027) tanım gereği kendi alt sistemini tümden serileştirir (bilinçli, yorumlu).

**Bayat yorum envanteri (1-arg uzay iddiası — 2026-08-09 F-KIM-GUV-003 sonrası geçersiz):** `duplicate-guard.helper.ts:22-23` · `shipment-locks.helper.ts:21-23` · `batch.service.ts:90-91` · `audit/surface/12-tx-global-gercekler.md:91`. Doğru envanter: `code-unique.helper.ts:45-53` ve `session-registry.service.ts:26-30`.

---

## HOTSPOTLAR (② denetçileri öncelikle baksın — burada yargı yok, "neden bakılmalı" var)

| # | dosya:satır | Neden |
|---|---|---|
| **H-1** | `src/services/tambur-undo.service.ts:1221-1395` (`applySingleRestore`) ve `:1434-1692` (`applyFull`) — özellikle :1246-1256, :1377-1378, :1668-1669 | Kardeşlerinin (finalize/cutOpenFabric/finishStep/bypass/manual) **tx başında aldığı WO satır kilidi burada hiç alınmıyor**, ama WO status guard'ı kilitsiz okunuyor ve sonra `WorkOrderStep` + `work_orders` (COMPLETED→IN_PROGRESS) yazılıyor. `roll-step.helper.ts:190-191` sözleşmesi ("çağıran WO'yu kilitlemeli") karşılanmıyor. İki soru: (a) eşzamanlı WO iptali (`softDelete cancelClaim`, K3a) ile top iptal edilmiş WO'da dirilebilir mi; (b) edinim sırası `Step → WO`, `tambur-manual`'de `WO → Step` (:1191→:1192) → ABBA adayı (§2.2 S1). Ölçüm önerisi: aynı COMPLETED WO'da `createManualRoll` + `applyUndo(RESTORE)` paralel sonda. |
| **H-2** | `src/services/helpers/roll-finalize.helper.ts:172` ← `kursun-qc.service.ts:936`, `kursun-bypass.service.ts:1399` | Barkod sayacı satır kilidi (**tüm KK1 girişlerini serileştiren tek satır**) burada WO kilidinden sonra **tx içinde** alınıyor ve kalan tx boyunca (Roll×N update + recompute + WO complete + kart) tutuluyor. Helper'ın kendi ölçümü 1345 ms vs 39 ms (`roll-barcode.helper.ts:56-59`). Bilinçli (yorum :164-169: rollIds tx içinde doğuyor) — ② "en uzun iş" üzerinden failure_mode yazmalı ya da bilinçli takas olarak kapatmalı (beceri §2 sayaç kuralı). |
| **H-3** | `src/services/permission-management.service.ts:187-221` (`grantPermission` upsert `validUntil`), `:285-302` (`toUpdate` + `removesAdmin` hesabı) | Son-admin guard'ı (8025 + `assertAdminCoverageAfterChange`) yalnız admin **id'si** listeden düşünce koşuyor (:302). Aynı grant'ın `validUntil`'ı geçmişe çekilirse `effectiveAdminWindow` (:587-594) dışına düşer, guard koşmaz → sistem admin'siz kalabilir. Yarış değil, mantık boşluğu; KIM hücresine de yönlendirildi. [VARSAYIM: `admin:*` wildcard'ı da aynı yoldan söner.] |
| **H-4** | `src/services/batch.service.ts:126` ↔ `src/services/workorder.service.ts:4553→4566`, `workorder-manual-move.service.ts:699` (K3a dosyaları) | 8022 sabit-anahtar kilidi kapsam içinde **WO satırından sonra** alınıyor (`splitBatch` :880→:920); `workorder.service:4553` yolunda 8022 → `ensureWorkOrderInProgress` (WO PLANNED ise WO satırı **8022'den sonra**) → ters sıra (§2.2 S2). K3a doğrulamalı: o yollarda `touchWorkOrderTx` var mı, yoksa çevrim penceresi (WO PLANNED + eşzamanlı split) gerçek mi. |
| **H-5** | `src/services/tambur.service.ts:2448-2458` (yorum) ↔ `:2498-2526` (child create) ↔ `:2466` (tx, catch yok) | `finalizeWarehouseCut` yorumu idempotency'yi "tx içi `clientToken @unique` P2002 → idempotent yanıt"a dayandırıyor; kodda `clientToken` yazılmıyor ve P2002 catch yok. Replay'de claim :2473 → **409** + tx öncesi rezervasyon (:2462) bir barkod numarası yakmış olur. Ayrıca F-CORE-VER-008 (açık) bu yolu "bilinçli takas" diye kaydediyor — takasın dayandığı mekanizma yerinde değil. `finalizeOpenFabric` (:3197) ön-kontrol tabanlı, orası tutarlı. |
| **H-6** | `src/services/helpers/code-unique.helper.ts:19-21, :59-82` ↔ `subcontractor-management.service.ts:202/:512`, `base.service.ts:1052` | Helper başlığı katlanmış kod tekilliği kapsamını 6 modelde sayıyor ve "kilit ŞART, findMany'den önce" diyor; `lockCodeScopeTx` **yalnız Item**'da çağrılıyor. Diğer üç çağıran `decideCodeUniqueness`'ı kilitsiz (havuz) koşturuyor → `sefa2`/`SEFA2` yarışı o modellerde açık (beceri §3.1 vaka şablonu). Bekçi `test_item_code_case_uniqueness` yalnız Item'ı ölçüyor. |
| **H-7** | `src/services/session-registry.service.ts:72-119` (8024) ve `master-data-merge.service.ts:543-712` (8027) | İki advisory kilidin de **eşzamanlılık bekçisi yok** (`test_session_registry`, `test_master_data_merge*`: paralel sonda 0, advisory referansı 0 — §2.3). Kilit yanlış yere taşınsa ya da silinse test paketi yeşil kalır. (Not: sıra bugün doğru — :79/:546 ilk ifade.) |
| **H-8** | `src/services/label-template.service.ts:572-588` + `:533-546` ↔ `:986-1008` | Bağlam varsayılanı ile şablon silme/pasifleştirme çapraz yarışı (§5-L1): iki taraf da kontrolü havuzda okuyor, hiçbir tx içi ikiz/claim yok. Blast radius küçük (admin), sonuç etiket baskısında 404. |
| **H-9** | `src/services/helpers/guarded-hard-remove.ts:62-76` | Guard sayımları havuzda, silme tx'te; Restrict FK'siz (SetNull) beş referans için pencere sessiz NULL üretir (§4-T3). Ayrıca Express handler'ı helpers/ altında (beceri §7.5 — dosya adı bulgu değil; ② "gerçek ikinci çağrı yolu" arasın). |
| **H-10** | `src/services/master-data-merge.service.ts:629-636` (`$executeRawUnsafe` döngüde) + :711 (120 s timeout) | Tablo/kolon adları `MERGE_MAP` sabitinden, değerler bind ✓; ama 8027 sabit anahtar + 120 s tavan = tek uzun birleştirme tüm birleştirmeleri 2 dakika bekletebilir ve `statement_timeout=50s` (DB) ile ayrışıyor (K-ORTAK/OPS: KUNYE `statement_timeout 50s`). ② bütçe/sıralama sorusunu netleştirsin. |
| **H-11** | `src/services/tambur-manual.service.ts:1191` | `touchWorkOrderTx` Roll claim'i (:1151) ve movement insert'ten (:1174) **sonra** — "kardeşlerle tutarlı WO→roll" sırasının tek istisnası; H-1 ile birlikte okunmalı. |

---

## SINIR ÖTESİ NOTLAR

- **K3a (inventory/workorder/subcontractor/shipping):** (1) §2.2 S2 / H-4 — `createBatchTx` çağıran 8 yer (`workorder.service.ts:4239/:4553`, `workorder-manual-move.service.ts:699`, `subcontractor.service.ts:1140/:1231/:3014`, `workorder-split.service.ts:359/:504`): her birinde 8022'den ÖNCE `touchWorkOrderTx` alınıyor mu? (`subcontractor` :1041→:1140/:1231, :2681→:3014 görünüşe göre evet; `workorder.service` :4553 ve `workorder-manual-move` :699'da grep'e göre hayır.) (2) `assertTargetColorChange(prisma)` `workorder.service.ts:4697`, `workorder-link.service.ts:525` — karar havuzda; tx içi ikizi var mı? (3) `order-status.helper.ts:205-210` kilit protokolü (alt-küme/tam-küme) — uygulayan üç yol doğrulanmalı. (4) `generateWorkOrderNumberTx` (`workorder-clone.helper.ts:31`) findMany sayacı: çağıranlar (`workorder.service.ts:4181`, `workorder-split.service.ts:471/:639`) `withBarcodeRetry` içinde mi? (5) `touchWorkOrderTx`'in `updatedAt` yan etkisi: "Son İşlem" sıralaması (CLAUDE.md 2026-07-30) kilit alan her akışta oynar — parti taşıma/birleştirme, bypass atama, KK2 kapatma iş emrini listede yukarı taşır.
- **KIM (kimlik/yetki):** H-3 (`validUntil` ile son-admin sönmesi); `session-registry` audit yazmıyor (login yolu AuthService'te mi?); `applyTemplate` merge `createMany` `skipDuplicates`'siz (:1012).
- **BLG / belge:** `printed-document.reissue` snapshot havuzda kuruluyor (:664-668) — "belge yolu da brüt" kuralıyla (CLAUDE.md 2026-08-05) kesişiyor mu? `traveler-card.recordPrintEvent` dokümante sınırı (:340-347).
- **OPS / scheduler:** `audit.service.archiveOlderThan` (`SET LOCAL` + 5000'lik parti, global 20 s tx bütçesi) ve `work-session.helper.sweepIdleSessions` — scheduler beş sorusu (beceri §6) K-OPS'ta; `pg-tool/pg-admin-client/offsite-backup` child process'leri (F-OPS-VER-004 düzeltildi) yine oraya.
- **Controller/Zod (K-giriş doğrulama):** tavansız diziler: `label-template.controller.ts:130 variants`, `batch.controller.ts:19/:23/:26` (`rollIds`/`batchIds`), `kartela.controller.ts:12 rollIds` + `:32 returns` (kalem başına `count ≤ 1000`), `admin.routes.ts:390-391/:646/:667` (izin dizileri), `reason-preset.routes.ts:57`, `station-capability.routes.ts:22/:27`, `tambur.controller.ts:36 relatedErrorIds`; `kursun-bypass` `rollIds` (route YAML :313) controller şeması okunmadı. `item.service.update` `restData` → `sanitizeWriteData` katmanı (KUNYE) doğrulanmalı.
- **Veri-performans (code-review-skill):** `item.service.ts:240` `tx.item.findMany` **tüm ürün tablosu** (kod adayları) kilit altında; `label-template.findAvailableName` :605 tüm şablon adları; `permission.assertTemplateNameAvailable` :815 `nameFold` index'li ✓.
- **Dokümantasyon bayatlığı:** 1-arg advisory uzayı yorumları (§6 listesi) + `audit/surface/12-tx-global-gercekler.md:91`; `tambur.service.ts:2448-2458` (H-5).

---

## KAPSANMAYAN / ERİŞİLEMEYEN

- **Kapsam dışı — görev sınırı:** `inventory`, `workorder*`, `subcontractor*`, `shipping`, `order`, `return`, `sack*` servislerindeki 60+ `$transaction` sitesi (K3a). Bu dosyalardan yalnız kilit-sırası/namespace tablosu için `inventory.service.ts:815-960` ve `createBatchTx` çağıran satırlar grep düzeyinde okundu; gövdeleri okunmadı.
- **Kapsam dışı — sebep: dosya bütünü okunmadı:** `tambur-undo.service.ts:1640-1692` (applyFull kuyruğu — grep ile `recomputeStepStatus`/WO updateMany/kart doğrulandı, satır satır okunmadı); `kursun-qc.service.ts:1160-1200` (reopenStep audit satırı); `tambur.service.ts:540-925` (finalize ön-kontrolleri — claim ikizleri tx içinde doğrulandığı için ön-kontrol satırları tek tek yazılmadı); `kursun-bypass.controller.ts` (Zod `rollIds` tavanı); `label-template.service.ts:1-290` (assertNameAvailable/assertTemplateAssignable gövdeleri).
- **Uygulanamadı — DB ölçümü:** Kilit tutulma sürelerini (`pg_locks`/`pg_stat_activity`) ve `roll_barcode_counters` bekleme süresini canlı/saha kopyasında ölçmek bu aşamada yapılmadı (salt-okunur kopyada eşzamanlı yük üretilemez; ② `scripts/test_barcode_reservation.ts` §6 T1/T2 ile dev DB'de ölçebilir).
- **Uygulanamadı — deadlock sondası:** H-1/H-4 çevrim adayları için N-paralel sonda koşulmadı (harita aşaması, script yazılmadı). Bu adaylar `[VARSAYIM]` etiketlidir.
- **Anlaşılamayan:** kursun-qc :441 sitesinin metot adı (regex rakam içeren adı kaçırdı — `[VARSAYIM: completeQc2]`); `traveler-template.setDefault` yarışında P2002'nin hangi HTTP koduna düştüğü (error middleware `_key` regex'i `isDefault_key`'i tanıyor mu — K-CORE).
- **Bilinçli atlandı:** helpers/ altındaki 14 `label-*` render dosyası ve `native-*`/`codec`/`raster` içerikleri (DB'ye dokunmuyor, grep ile doğrulandı; iş kuralı render katmanında — K-BLG).
