# K3a — Transaction & Kilit Envanteri (A: shipping / workorder / subcontractor / order / inventory)

Aşama ① KEŞİF — haritalama. **Yargı yok, bulgu yok**; yalnız olgu + ② için HOTSPOT işaretleri.
Dal `adnansahin`, HEAD ce8681d1 (KUNYE.md). Tüm yollar repo köküne göre; satır numaraları çalışma ağacından (2026-08-28).

## 0. Yöntem ve kapsam

- Hedef 11 dosyadaki **65 `$transaction(` sitesi** (`grep -c`: shipping 24 · workorder 9 · subcontractor 7 · order 7 · inventory 7 · return 2 · sub-mgmt 2 · link 2 · split 3 · manual-move 1 · batch-drop 1 = 65 ✓) tek tek okundu — her callback gövdesi **ve** onu saran fonksiyonun tx-öncesi/sonrası kısmı (check-then-act kararı için gerekli).
- Callback sınırları brace-eşleştirmeli tarayıcıyla (`scratchpad/txscan.mjs`) çıkarıldı, ardından gövdeler elle okundu (grep yetmez kuralı — ör. `this.generateWorkOrderNumber()` gibi dolaylı `prisma.` kullanımı yalnız gövde okumasıyla görülür).
- Tx-helper'lar okundu: `shipment-locks`, `duplicate-guard`, `code-unique`, `fason-open-dispatch`, `tambur-plan-gate`, `workorder-target-color`, `workorder-locks`, `roll-disposition`, `roll-finalize`, `roll-step`, `roll-step-scope`, `roll-barcode`, `roll-variance`, `roll-cancel-restore`, `order-status`, `order-line-scope`, `sack-invariants`, `traveler-card-dirty`, `traveler-card-fanout`, `workorder-clone`, `workorder-rolls`, `kursun-bypass-guard`, `kursun-bypass-eligibility` (§assertKursunTabletMayWrite), `subcontractor-cancel`, `quality-grade`, `guarded-hard-remove` (kapsam dışı route factory — yalnız envanter), `batch.service` (`generateBatchNumberTx`/`createBatchTx`/`isBatchLockedTx`/`assertBatchInWorkOrder`/`deleteIfEmptyAndTraceless`), `printed-document.service` (`freezeForSource`/`voidForSource`/`reissueForSourceTx`), `traveler-card.service.createForWorkOrder`, `system-setting.service` okuyucuları, `utils/barcode-retry.ts`, `utils/code-format.nextDailySeq`.
- DB kanıtı yalnız `audit/tools/sql-saha.sh` (prod kopyası `tekserp_saha_0825`, 190/195 migration) ile: unique/partial-unique index envanteri, DEFERRABLE FK'lar, `roll_barcode_counters` varlığı, günlük sayaç hacimleri.

### Sözlük (tablo kısaltmaları)

| Kısaltma | Anlam |
|---|---|
| **int** | interaktif `$transaction(async (tx) => …)` |
| **batch** | dizi formu `$transaction([...])` |
| **wBR** | `withBarcodeRetry(() => prisma.$transaction(...))` sarmalı (`src/utils/barcode-retry.ts:15` — P2002'de en çok 5 deneme, 5-30 ms jitter; `isRetryable` verilmezse **her** P2002 retry edilir) |
| **opsiyon** | tx seçeneği. Verilmemişse **global `maxWait 5000 / timeout 20000`** (`src/lib/prisma.ts:89-95`, O3-1); DB `statement_timeout=50s` |
| **W** | `WorkOrder` satır kilidi — `touchWorkOrderTx` (`helpers/workorder-locks.helper.ts:50-58`, koşulsuz `updateMany where {id}`) ya da statü-koşullu claim |
| **S** | `Sack` satır kilidi — `touchWarehouseSackTx` (`helpers/shipment-locks.helper.ts:65-78`, `where {id, shipmentId:null}` + `count===0→409`) ya da sevkiyat-atama claim'i |
| **H** | `Shipment` satır kilidi — `touchShipmentPlannedTx` (`shipment-locks.helper.ts:84-97`, `{id, status:PLANNED}`) ya da statü claim'i |
| **L** | `OrderLine` satır kilitleri — `touchOrderLinesTx` (`helpers/order-status.helper.ts:32-40`, **id ASC sıralı**, id-başına döngü) |
| **O** | `Order` satır kilidi (claim / `recomputeOrderStatus` `order.update` :196) |
| **R** | `Roll` satır kilitleri (claim / update) |
| **K** | `roll_barcode_counters` satır kilidi — `reserveRollBarcodes` `INSERT … ON CONFLICT DO UPDATE` (`helpers/roll-barcode.helper.ts:83-85`; kilit **commit'e kadar** tutulur) |
| **A21/A22/A23** | `pg_advisory_xact_lock(ns,key)` — 8021 KK1 mükerrer (`duplicate-guard.helper.ts:28`), 8022 parti no (`batch.service.ts:96-99`, tek anahtar=1 → **sistem geneli**), 8023 sevkiyat kapsamı (`shipment-locks.helper.ts:28`, `hashtext(shipmentId)`) |
| **Q** | "günün max'ı + 1" sayaç okuması — **kilitsiz**, çakışma DB `@unique` + P2002 → wBR ile çözülür (SVK/CV/FS/FK/DSK/IE) |
| **T** | `TravelerCard` satır (`markTravelerCardDirtyTx` `traveler-card-dirty.helper.ts:38`, `setWorkOrderCardStatuses` `traveler-card-fanout.helper.ts:26`) |
| **claim** | `updateMany where {kimlik + beklenen durum}` + `count` kontrolü (beceri §3.2 sınıfı parantezde) |
| **CTA** | check-then-act: tx DIŞINDA okunan değerle tx İÇİNDE karar; tx içinde taze okunup okunmadığı belirtilir |

## 1. Global olgular

| Olgu | Kanıt |
|---|---|
| Tx opsiyonu **hiçbir sitede** verilmemiş; tek istisna `shipping.service.ts:2610` (`RepeatableRead`, salt-okuma batch). Global `maxWait 5s / timeout 20s` | `src/lib/prisma.ts:89-95`; `grep isolationLevel\|maxWait\|timeout:` (§KUNYE) |
| Advisory uzayları (tümü 2-argümanlı): 8021 KK1 · 8022 parti no · 8023 sevkiyat · 8024 oturum · 8025 izin · 8026 kod tekilliği · 8027 merge | `duplicate-guard.helper.ts:28`, `batch.service.ts:96`, `shipment-locks.helper.ts:28`, `session-registry.service.ts:32`, `permission-management.service.ts:21`, `code-unique.helper.ts:57`, `master-data-merge.service.ts:50` |
| Bu kapsamda alınan advisory'ler: **8021** (`inventory.service.ts:844`), **8022** (`batch.service.ts:126` → `createBatchTx` üzerinden 8 çağrı yeri), **8023** (`shipping.service.ts:2159`, `return.service.ts:490`). **8026 bu kapsamda YOK** — `lockCodeScopeTx` yalnız `item.service.ts:237`'den çağrılıyor; `code-unique.helper.ts` başlığı kapsam listesinde `Subcontractor`'ı sayıyor ama `subcontractor-management.service.ts` kilit almıyor (bkz. §6 #16) | `grep -rn 'lockCodeScopeTx(' src` → tek çağrı |
| `FOR UPDATE`: tek yer `order.service.ts:2434` (`toDelete` satırları) | grep |
| `Promise.all` tx client'ında: **0** (tüm hedef tx'ler sıralı `await`; ESLint kuralı da var). Havuz client'ıyla `Promise.all` tx DIŞINDA meşru (ör. `shipping.service.ts:2068`, `inventory.service.ts:3423`) | tarayıcı + gövde okuması |
| Dış I/O (fetch/child_process/fs/print/sleep) tx içinde: **0**. `printedDocumentService.freezeForSource(tx,…)` DB yazımıdır, I/O değil; belge builder'ları `db` parametresiyle çalışır (`shipping.service.ts:3370-3698`, `subcontractor.service.ts:6366-6818`, `return.service.ts:1023-1142` aralıklarında `prisma.` **0**) | grep + awk |
| Audit (`AuditService.log/logMany`) **her sitede tx DIŞINDA, commit'ten SONRA**; best-effort — `audit.service.ts` `log()` gövdesi `try/catch` + `recordAuditFailure` (istisna: `order.service.ts:516-528` ayrıca `.catch(() => undefined)`). Tek tx-içi audit yok. `tambur-plan-gate.helper.ts:137` audit'i pre-tx yolda (K3b) | okuma |
| P2034 (deadlock/serialization) **hiçbir yerde retry edilmez**; `error.middleware.ts:517-523` 409 + "tekrar deneyin" döner; `withBarcodeRetry` yalnız P2002 | grep `P2034` → 3 vuruş (middleware + yorum) |
| DEFERRABLE composite FK'lar: `rolls_sackId_shipmentId_consistency_fkey`, `swatches_…` (INITIALLY DEFERRED) — sevkiyat tx'lerinde çuval↔top `shipmentId` yazım sırası bu sayede serbest | sql-saha `pg_constraint` |
| Claim'lere DB seddi (saha kopyası): `roll_movements_one_open_per_roll_step_uq` (partial), `kursun_bypass_one_pending_per_step_uq` (partial), `printed_documents_docType_sourceId_version_key`, `roll_operations_rollId_workOrderStepId_operationType_key`, `sacks_shipmentId_seq_key`, `sacks_id_shipmentId_key`, `sack_allocations_sackId_orderLineId_key`, `subcontractor_direct_ship_allocations_dsId_orderLineId_key`, `traveler_cards_workOrderId_key`, `work_order_to_order_lines_pkey(workOrderId, orderLineId)`; `clientToken` unique: rolls/work_orders/orders **partial** (`WHERE clientToken IS NOT NULL`), sacks/shipments/subcontractor_receipts **düz** unique (PG'de çoklu NULL zaten serbest — davranış eşdeğer); belge no'ları `@unique` (SVK/CV/FS/FK/DSK/IE) | sql-saha `pg_indexes` |
| Sayaç hacmi (saha, gün başına max): shipment 12 · dispatch 21 · receipt 20 · workorder 21 · sack 12 → `Q` sınıfı çakışma pratikte nadir ama mümkün | sql-saha |

## 2. Site tablosu

Sütunlar: **#** · **site** · **fonksiyon** · **tür/opsiyon** · **kilitler (satır) — alınma SIRASI** · **yazılan tablolar** · **tx içi global `prisma.`** · **dış I/O** · **tx `Promise.all`** · **audit** · **claim sınıfı** · **CTA (tx-dışı okunan → tx-içi taze mi?)**

### 2.1 `Teks-Erp/src/services/shipping.service.ts` (24 site)

| # | site | fonksiyon | tür/opsiyon | kilitler (satır) — sıra | yazılan | tx içi `prisma.` | I/O | P.all | audit | claim sınıfı | CTA |
|---|---|---|---|---|---|---|---|---|---|---|---|
| S1 | :251 | `openSack` | int, **wBR** (predicate :275-287: clientToken/manuel sackNo P2002 retry edilmez), opsiyon yok | **Q** `nextSackNo(tx)` :252 (kilitsiz max+1) → `sack.create` :253 | sacks | yok | yok | yok | sonra :297 | create; idempotency `sacks_clientToken_key` + pre-check :213 + P2002 catch :291 replay | müşteri/şube aktifliği :222-244 pre-tx, tx içinde tekrar okunmaz (master-data) |
| S2 | :372 | `reassignSackCustomer` | int | **S** claim :374 `{id, shipmentId:null}` count!==1→409 | sacks, rolls(labelDirty), sacks(labelDirty) | yok | yok | yok (:448 sıralı) | sonra :389 | tekil claim (durum geçişi) | müşteri/şube :338-361 pre-tx; `sack.customerId` :384 karşılaştırması pre-tx değerle |
| S3 | :515 | `scanIntoSack` (top: başka depo çuvalından TAŞI) | int | **S(hedef)** `touchWarehouseSackTx` :516 → **R** claim :517 `{id, sackId:from, shipmentId:null}` count!==1→409 → S(from,hedef) `markSackContentChangedTx` :522 | rolls, sacks | yok | yok | yok | sonra :524 | tekil claim | **kaynak çuval kilitlenmez** (:511 `from.shipmentId` pre-tx); koruma `roll.shipmentId:null` (createShipment aynı tx'te roll.shipmentId yazar :1420) — bkz. §6 #18 |
| S4 | :534 | `scanIntoSack` (serbest top ekle) | int | **S** :535 → **R** claim :536 `{id, shipmentId:null, sackId:null, status notIn NON_SACKABLE}` count===0→409 → :541 | rolls, sacks | yok | yok | yok | sonra :543 | tekil claim | pre-tx :528-533 statü/shipment; tx içinde claim WHERE'i kapsar ✓ |
| S5 | :563 | `scanIntoSack` (kartela taşı) | int | **S(hedef)** :564 → swatch claim :565 → :567 | swatches, sacks | yok | yok | yok | sonra :569 | tekil claim | S3 ile aynı asimetri |
| S6 | :573 | `scanIntoSack` (kartela ekle) | int | **S** :574 → swatch claim :575 `{id, shipmentId:null, sackId:null, cancelledAt:null}` → :577 | swatches, sacks | yok | yok | yok | sonra :579 | tekil claim | ✓ |
| S7 | :598 | `addKartelaToSack` | int | **S** :599 → `swatch.findMany take N` :600 → swatch **çoklu claim** :610 count!==N→409 (hepsi-ya-hiç, yazılı) → :612 | swatches, sacks | yok | yok | yok | sonra :615 | çoklu claim ✓ | seçim+claim aynı tx; FIFO adaylar kilit altında değil ama claim WHERE'i aynı koşullar → kaybeden 409 ✓ |
| S8 | :634 | `removeRollFromSack` | int | **S** :635 → **R** claim :636 `{id, sackId, shipmentId:null}` count!==1 → :638 | rolls, sacks | yok | yok | yok | sonra :640 | tekil claim | ✓ |
| S9 | :659 | `removeSwatchFromSack` | int | **S** :660 → swatch claim :661 → :663 | swatches, sacks | yok | yok | yok | sonra :665 | tekil claim | ✓ |
| S10 | :690 | `moveRollToSack` | int | **S(from)** :691 → **S(to)** :692 → **R** claim :693 → :695 | rolls, sacks | yok | yok | yok | sonra :697 | tekil claim | ⚠️ iki çuval kilidi **id sırasız** (from→to) — §3 ABBA-2 |
| S11 | :726 | `distributeSackContents` | int | **S** :727 → roll/swatch `updateMany` toplu :732/:738 (where sackId+shipmentId:null) → :740 | rolls, swatches, sacks | yok | yok | yok | sonra :742 | toplu güncelleme (count raporlanır) | ✓ |
| S12 | :769 | `moveRollsToSack` | int | **S(src)** :770 → **S(target)** :771 → **R** `updateMany` toplu :772 `{id in, sackId:src, shipmentId:null}` → :776 | rolls, sacks | yok | yok | yok | sonra :778 | **çoklu claim, count kontrolü YOK** — `moved` sayısı döner, atlananlar listelenmez (§3.2c "sessiz kısmi" adayı) | ABBA-2 (S10 ile aynı) |
| S13 | :831 | `splitSack` | int, **wBR** | **S(src)** :832 → taze sayımlar :834-835 → **Q** `nextSackNo(tx)` :844 → `sack.create` :842 → **R** `updateMany` :853 → :860 | sacks, rolls | yok | yok | yok | sonra :864 | çoklu claim; `moved` ile `claimable` karşılaştırılmaz | kaynak çuval kilitli ama S3/S5 yolları kaynağı kilitlemediği için `moved < claimable` sessiz mümkün — §6 #18 |
| S14 | :964 | `weighSack` | int | **S** :965 → `sack.update` :966 | sacks | yok | yok | yok | sonra :978 | claim = touch | simüle-kantar/bayrak :919-947 pre-tx ✓ (okuma) |
| S15 | :1001 | `removeSack` | int | **S** :1006 → taze sayım :1010-1011 → roll/swatch toplu :1015-1016 → `sack.delete` :1017 (**fiziksel DELETE** — belgeli istisna) | rolls, swatches, sacks | yok | yok | yok | sonra :1019 | toplu | tx-dışı `_count` yalnız mesaj için ✓ |
| S16 | :1402 | `createShipment` | int, **wBR** (predicate :1432: clientToken hariç tüm P2002 retry) | **Q** `nextShipmentNo(tx)` :1403 → `shipment.create` :1404 → **S** claim döngüsü :1412-1418 (`{id, shipmentId:null}` count!==1→409; **sıra = istemcinin `sackIds` sırası**) → roll/swatch toplu :1420-1421 → shipmentOrder del/create :1423 → sackAllocation del/create :1424 → [onay kapalıysa `performDispatchTx` :1427: **H** claim :1812 → hayalet guard :1834 → shipmentOrder :1849 → **R** groupBy+updateMany :1856-1867 → **L** sıralı :1877-1878 → **O** recompute :1879 → printed_documents :1881] | shipments, sacks, rolls, swatches, shipment_orders, sack_allocations, order_lines, orders, printed_documents | yok | yok | yok | sonra :1447 | çoklu claim (döngü, hepsi-ya-hiç ✓); idempotency `shipments_clientToken_key` + :1384/:1441 | hayalet guard :1244 pre-tx **ve** :1834 tx-içi ✓; `confirmationEnabled` :1397 pre-tx (rejim değişimi tx'e yansımaz); çuval sırası istemciden → §3 ABBA-3 |
| S17 | :1547 | `addSacksToShipment` | int | **H** `touchShipmentPlannedTx` :1548 → `sack.findFirst maxSeq` :1549 → **S** claim döngüsü :1552 (istemci sırası) → roll/swatch toplu → alloc rewrite :1559 | sacks, rolls, swatches, sack_allocations | yok | yok | yok | sonra :1561 | çoklu claim ✓ | `loadSacksForShipment` hayalet guard :1544 pre-tx (tx-içi tekrar yok; dispatch'te tekrar var); seq `sacks_shipmentId_seq_key` seddi + H kilidi → seq yarışı serileşir ✓; ABBA-3 |
| S18 | :1572 | `removeSackFromShipment` | int | **H** :1573 → **S** claim :1574 `{id, shipmentId}` count!==1 → roll/swatch :1576-1577 → alloc :1579 | sacks, rolls, swatches, sack_allocations | yok | yok | yok | sonra :1581 | tekil claim | ✓ |
| S19 | :1587 | `setDestination` | int | **H** :1588 → `shipment.update` | shipments | yok | yok | yok | sonra :1591 | claim = touch | ✓ |
| S20 | :1598 | `setProcedureCode` | int | **H** :1599 → update | shipments | yok | yok | yok | sonra :1602 | claim = touch | ✓ |
| S21 | :1905 | `dispatchShipment` → `performDispatchTx` :1806 | int (arrow, non-async) | **H** claim :1812 `{id, PLANNED}`→DISPATCHED count===0→409 → hayalet guard :1834 → shipmentOrder :1849 → **R** groupBy :1856 + updateMany döngü :1862 → **L** sıralı :1877-1878 → recompute (**O** :196 helper) :1879 → printed_documents :1881 (`freezeForSource`: `findFirst version desc` :332 → `create` :337) | shipments, shipment_orders, rolls, order_lines, orders, printed_documents | yok | yok | yok (:1855 sıralı) | sonra :1906 | tekil claim ✓ | pre-tx :1895-1903 (boş sevkiyat, ihracat tartı) **tx içinde tekrarlanmaz** — §6 #17 |
| S22 | :1995 | `cancelShipment` → `cancelPlannedShipmentTx` :1961 | int | **H** claim :1966 `{id, status: expected}`→CANCELLED → shipmentOrder oku → **L** sıralı :1975 → sackAllocation deleteMany :1976 → shipmentOrder :1977 → **R**/swatch/**S** toplu :1979-1981 → recompute **O** :1982 | shipments, order_lines, sack_allocations, shipment_orders, rolls, swatches, sacks, orders | yok | yok | yok | sonra :1998 | tekil claim (beklenen statü pre-tx :1990'dan) ✓ | ✓ |
| S23 | :2152 | `undoDispatch` | int | **A23** :2159 (**tx'in ilk ifadesi**, `rollReturn.count` :2167'den ÖNCE ✓) → shipment taze :2162 → **H** claim :2179 DISPATCHED→PLANNED → shipmentOrder :2186 → **R** groupBy+updateMany :2191-2202 → **L** sıralı :2210 → recompute **O** :2211 → printed_documents void :2214 → [releaseSacks: `sack.count` :2226 → `cancelPlannedShipmentTx` :2227 (H claim PLANNED→CANCELLED, L tekrar, alloc, R/swatch/S, O)] | shipments, shipment_orders, rolls, order_lines, orders, printed_documents, (+sacks, swatches, sack_allocations) | yok | yok | yok (:2190 sıralı) | sonra :2232 | tekil claim ✓ | engel kontrolü tx içinde taze ✓; `sameDayOnly` :2149 pre-tx bayrak |
| S24 | :2592 | `listShipments.attachTotals` | **batch** `[groupBy×3]`, `isolationLevel: RepeatableRead` | yok (salt-okuma) | — | (dizi elemanları `prisma.*` — batch formunun doğru kullanımı, sızıntı değil) | yok | (batch, tek bağlantı) | — | — | — |

Tx-dışı claim'ler (tx sitesi değil, envanter için): `setDispatchNote` :1610 `updateMany {id}`; `setShipmentInvoice` :1627+ `updateMany {id, status}` (okunmadı — K3b/K2); `setSackNotes` :1704 (guard'sız, belgeli istisna).

### 2.2 `Teks-Erp/src/services/workorder.service.ts` (9 site)

| # | site | fonksiyon | tür/opsiyon | kilitler (satır) — sıra | yazılan | tx içi `prisma.` | I/O | P.all | audit | claim sınıfı | CTA |
|---|---|---|---|---|---|---|---|---|---|---|---|
| W1 | :988 | `create` | int, **wBR** (predicate :1084-1094) | kilit yok; **Q** (IE no) `this.generateWorkOrderNumber()` :989 → `orderLine.count` :994 → `resolvePlanDates` :1002 → `workOrder.create` (nested steps/orderLinks/targetProperties) :1007 → `travelerCardService.createForWorkOrder(tx)` :1081 (`traveler-card.service.ts:175-201`, `buildSnapshot(client)` :189) | work_orders, work_order_steps, work_order_to_order_lines, work_order_target_properties, traveler_cards | **EVET ×2**: (a) :989 `generateWorkOrderNumber()` → `prisma.workOrder.findMany` :581 + `findUnique` :592 (havuz); (b) :1002 `resolvePlanDates` → `readWorkOrderDefaultPlanDurationDays()` argümansız (`system-setting.service.ts:2925`, `tx?` → `prisma`) | yok | yok | sonra :1110 (replay yolunda audit yok :1099) | create; clientToken P2002 → `resolveCreateTokenReplay` :1146 (tx dışı, kimlik-kilit kontrolü :1181; `isActive:false` → 409 :1172) | rota/ürün/renk/özellik/istasyon-yetenek doğrulamaları :686-971 pre-tx, tx'te tekrarlanmaz; manuel WO no :984 pre-check + DB `work_orders_workOrderNumber_key` seddi → P2002 → 409 :1091 ✓ |
| W2 | :3376 | `softDelete` (WO iptali) | int | **W** claim **ilk** :3387 `{id, status notIn terminal}`→CANCELLED count===0→409 → fason in-flight count :3423 (kilit altında) → in-flight `roll.findMany` :3471 → `applyRollDispositionsTx` :3491 (`roll-disposition.helper.ts:151`: `collectRollStepScopeTx` :195 → `closeOpenMovementsTx` `$executeRaw` :378 → **R** çoklu claim :239 `{id in, IN_PRODUCTION, shipmentId:null, sackId:null}` count!==len→409 → preCancelStatus :275 → `recomputeStepStatus` döngüsü :325) → **R** blanket `updateMany` :3513 → `$executeRaw` movement kapama :3531 → step SKIPPED :3547 → bypass void :3560 → **T** VOIDED :3563 → findUnique :3565 | work_orders, rolls, roll_movements, work_order_steps, kursun_bypass_assignments, traveler_cards | yok | yok | yok | sonra :3569 + top başına :3585 | tekil claim ✓ + çoklu claim (hepsi-ya-hiç ✓) | ⚠️ **tx-ÖNCESİ YAZMA**: `prepareFasonCancelDecision` :3367 → `cancelBulk` :3253 (ayrı tx'ler, dönen `failed[]` **kullanılmıyor**) + `prisma.roll.updateMany` :3281 (AT_SUB/RETURNED → IN_PRODUCTION, havuz, koşulsuz) — tx düşerse geri sarılmaz → §6 #1; `stepIds` :3322 pre-tx (`existing.steps`) → §6 #5; `resolveReasonCode` :3333 pre-tx okuma ✓ |
| W3 | :3862 | `completeWorkOrder` | int, **wBR** (predicate yok) | **W** claim **ilk** :3866 IN_PROGRESS→COMPLETED → in-flight :3888 → guard'lar (:3906, :3917 count, :3935 kapsam, :3946) → qualityGrade :3962 → [TRANSFER `transferRollsToNewWorkOrderTx` :4151: `cloneWorkOrderTx` (`workorder-clone.helper.ts:67`: **Q** IE no :116 → `workOrder.create` → `createForWorkOrder(tx)` :187) → **R** tekil claim döngüsü :4196 `{id, IN_PRODUCTION, currentStepId, shipmentId:null, sackId:null}` → `repointRollsTx` :4213 → bypass repoint :4228 → `createBatchTx` :4239 (**A22** `batch.service.ts:126` → batch.create :233 → roll.updateMany batchId → **T** dirty :254) → `deleteIfEmptyAndTraceless` :4246 → recompute :4249] → [dispozisyon `applyRollDispositionsTx` :4019: **R** çoklu claim → **K** `reserveRollBarcodes` :296 → roll.update döngüsü] → `$executeRaw` :4038 → step SKIPPED :4058 → bypass void force :4068 → **T** COMPLETED :4071 | work_orders(+yeni), work_order_steps(+yeni), traveler_cards(+yeni), rolls, roll_movements, roll_operations, roll_errors, kursun_bypass_assignments, batches, roll_barcode_counters | yok | yok | yok | sonra :4086 + top başına :4103 | tekil claim ✓ + çoklu claim (birebir kapsam guard'ı :3935 hepsi-ya-hiç ✓) | `stepIds`/`stepSeqById` :3854-3855 pre-tx → §6 #5; `resolveReasonCode` pre-tx ✓. **Kilit sırası**: W → R → (A22 → K) — §3 |
| W4 | :4290 | `hardDelete` (arşiv) | int | **W** claim-kilit **ilk** :4296 `{id, isActive:true, status≠IN_PROGRESS}` (updatedAt) count===0→409 → **R** blanket :4317 → `$executeRaw` :4330 → step SKIPPED :4344 → **T** VOIDED :4353 → `workOrder.update` :4355 (isActive=false, clientToken=null) | work_orders, rolls, roll_movements, work_order_steps, traveler_cards | yok | yok | yok | sonra :4364 | claim-as-lock ✓ | `stepIds` :4288 pre-tx → §6 #5 |
| W5 | :4444 | `attachRolls` | int, **wBR** (predicate yok; içeride @unique üretimi yok — `roll_movements_one_open_per_roll_step_uq` P2002'si retry'a girer) | **kilit yok başta**; `roll.findMany` :4451 → **R** çoklu claim `updateManyAndReturn` :4494 `{id in, status in [STOCK,WAREHOUSE,A1_STOCK], sackId:null, shipmentId:null}` (kısmi → per-top `errorMessages` ✓ politika yazılı) → `rollMovement.createMany` :4530 → `createBatchTx` :4553 (**A22** → batches → roll batchId → **T** dirty) → `recomputeStepStatus` :4565 → `ensureWorkOrderInProgress` :4566 (**W** `updateMany {id, PLANNED}` `roll-step.helper.ts:180` — **en sonda**) | rolls, roll_movements, batches, traveler_cards, work_order_steps, work_orders | yok | yok | yok | sonra :4574 logMany + :4593 | çoklu claim, kısmi (b) ✓ | WO statüsü :4412 pre-tx, tx içinde **tekrarlanmaz**; WO satırı tx sonunda kilitlenir → §3 ABBA-1, §6 #3 |
| W6 | :4773 | `update` | int | **W** `touchWorkOrderTx` **ilk** :4778 → `computeWorkOrderLocks(tx)` :4779 (taze) → `assertTargetColorChange(tx)` :4794 (taze) → **W** claim :4806 `{id, status notIn terminal}` count===0→409 → **T** dirty :4840 | work_orders, traveler_cards | yok | yok | yok | sonra :4850 | tekil claim ✓ (F58 deseni: pre-tx :4675/:4697 havuz + tx-içi taze) | ürün/renk aktifliği :4717-4733 pre-tx (master-data) |
| W7 | :5246 | `replace` | int | **W** claim-kilit **ilk** :5252 (updatedAt, status notIn terminal) → taze locks :5274 → orderLine/links okuma :5323/:5339 → step diff (delete :5411 / update :5474 / create :5486) → links+targetProps deleteMany :5502-5503 → `resolvePlanDates` :5505 → `workOrder.update` (nested create) :5510 → **T** dirty :5562 | work_orders, work_order_steps, work_order_to_order_lines, work_order_target_properties, traveler_cards | **EVET**: :5505 `resolvePlanDates` → `readWorkOrderDefaultPlanDurationDays()` argümansız → havuz (salt-okuma) | yok | yok | sonra :5567 | claim-as-lock ✓ | pre-tx `computeWorkOrderLocks(prisma)` :4912 + tx-içi taze ✓; `attachedRolls` :5208 yalnız uyarı için |
| W8 | :5672 | `updateTargetProperties` | int | **kilit YOK, claim YOK**: targetProps deleteMany/createMany :5674-5678 → `roll.findMany` :5685 → rollProperty deleteMany(FLAG)/createMany skipDuplicates :5700-5711 → **T** dirty :5716 | work_order_target_properties, roll_properties, traveler_cards | yok | yok | yok | sonra :5721 | — (toplu) | ⚠️ `computeWorkOrderLocks(prisma)` :5627 **yalnız pre-tx**; WO statüsü (COMPLETED/CANCELLED) **hiç** kontrol edilmiyor (`update`/`replace`'ten farklı) → §6 #2 |
| W9 | :5948 | `detachRolls` | int | `roll.findMany` :5950 → `loadQualityTargetMaps(tx)` :5989 → **R** gruplu çoklu claim :6000 `{id in, currentStepId in stepIds, status in DETACHABLE}` toplam count!==len→409 (hepsi-ya-hiç ✓) → producedInStepId :6023 → `$executeRaw` :6032 → `recomputeStepStatus` döngüsü :6053 (→ **W** `ensureWorkOrderInProgress` yalnız ACTIVE'e geçişte) | rolls, roll_movements, work_order_steps, (work_orders) | yok | yok | yok | sonra :6063 logMany | çoklu claim ✓ | WO statüsü :5920 pre-tx; `stepIds` :5930 pre-tx → §6 #4/#5 |

Tx-dışı claim'ler: `lockWorkOrder` :6100 `updateMany {id, PLANNED}` ✓ (tx'siz meşru). `generateWorkOrderNumber` :571-597 havuz okumalı 5 denemeli döngü (W1'de tx içinden çağrılıyor — §5).

`quickStart` :1209 orkestrasyonu (tx-dışı zincir): `create()` (kendi tx'i) → `attachRolls()` (kendi tx'i) → 0 bağlanırsa `hardDelete()` telafi (:1379/:1384, hata loglanır :1366) → opsiyonel `dispatch()` (kendi tx'i, hata `dispatchWarning` :1442). Belgeli kısmi-başarı tasarımı (§7).

### 2.3 `Teks-Erp/src/services/subcontractor.service.ts` (7 site)

| # | site | fonksiyon | tür/opsiyon | kilitler (satır) — sıra | yazılan | tx içi `prisma.` | I/O | P.all | audit | claim sınıfı | CTA |
|---|---|---|---|---|---|---|---|---|---|---|---|
| F1 | :1036 | `dispatch` | int, **wBR** (predicate yok) | **W** `touchWorkOrderTx` **ilk** :1041 → WO/step taze :1046/:1064 → **R** autoAttach claim :1084 `{id in, STOCK, currentStepId:null, sackId:null, shipmentId:null}` count!==size→409 → step ACTIVE :1108 → **W** `ensureWorkOrderInProgress` :1118 → parti: [`batch.findMany` taze :1167 + `roll.updateMany` labelDirty/batchId :1187/:1192 + `splitRemainder` → `createBatchTx` (**A22**) :1140 + `deleteIfEmptyAndTraceless` :1199] ya da [`assertBatchInWorkOrder` :1205 + taze merged check :1209 + `roll.updateMany` :1221 + `splitRemainder`] ya da [`createBatchTx` (**A22**) :1231] → **Q** FS no `nextPrefixedSequence(tx)` :1241 → `subcontractorDispatch.create`(+items) :1244 → printed_documents `freezeForSource(tx)` :1283 → **T** dirty :1294 → **R** çoklu claim :1319 `{id in, status in [IN_PRODUCTION,STOCK], currentStepId, sackId:null, shipmentId:null}` count!==len→409 → movement findMany/createMany :1337/:1353 → rollOperation createMany skipDuplicates :1359 → travelerCardScan :1376 | work_orders, work_order_steps, rolls, batches, traveler_cards, subcontractor_dispatches, subcontractor_dispatch_items, printed_documents, roll_movements, roll_operations, traveler_card_scans | yok | yok | yok | sonra :1392 (+parti :1414) | çoklu claim ×2 (hepsi-ya-hiç ✓) | idempotency guard :742 (`OPEN_OUTSTANDING` aynı küme) **pre-tx** — eşzamanlı ikiz replay ikinci sevki roll claim'de 409'a düşürür ✓; item/route/committed guard'ları :809-947 pre-tx; cross-WO parti guard tx-içinde tekrar ✓. **Sıra**: W → R → (batch) → A22 → D → R — §3 |
| F2 | :2013 | `cancel` | int | **W** **ilk** :2016 → dispatch claim :2020 `{id, cancelledAt:null}` count===0→409 → **T** dirty :2033 → printed void :2037 → **R** çoklu claim :2052 `{id in, AT_SUB, currentStepId}` count!==len→409 → `$executeRaw` movement :2071 → rollOperation deleteMany :2086 → adım yeniden değerlendirme (count'lar :2106/:2109/:2113 kilit altında) → step update :2118/:2128 → `completeWorkOrderIfStepsDone` :2125 (**W** `updateMany notIn terminal` `roll-step.helper.ts:204` + **T** :212) → `logTravelerScan` :2137 | subcontractor_dispatches, traveler_cards, printed_documents, rolls, roll_movements, roll_operations, work_order_steps, work_orders, traveler_card_scans | yok | yok | yok | sonra :2148 | tekil + çoklu claim ✓ | engel yüklemi :1991 (`acceptedReceiptItem` :1963, `movedRolls` :1977) **pre-tx**; `dispatch.step.status` :2099 pre-tx değer → §6 #6 (kısmi kabul penceresi) |
| F3 | :2678 | `receive` | int, **wBR** (**predicate yok** → clientToken P2002'si de retry edilir) | **W** **ilk** :2681 → hedef renk taze :2693 (+ `TARGET_COLOR_CHANGED` 409 :2703) → `freshReturns` :2723 `{id in, AT_SUB, currentStepId}` uzunluk kontrolü → takip-teslimat count :2776 → **Q** FK no :2784 → `subcontractorReceipt.create` :2787 (clientToken) → receiptProperty :2805 → `$executeRaw` movement (fullIds) :2828 → **R** tam-kabul claim :2857 `{id in, AT_SUB}` count!==len→409 → **R** kısmi decrement claim döngüsü :2879 `{id, AT_SUB, currentQty gt}` count!==1→409 → dispatchItem findMany :2905 → receiptItem createMany :2915 → rollOperation createMany skipDuplicates :2929 → stillAtSub count :2952 → step COMPLETED :2959 → `completeWorkOrderIfStepsDone` :2968 → [newRolls: `roll.findFirst/findMany` :2989/:3003 → `createBatchTx` (**A22**) :3014 (takip teslimatı) → **K** `reserveRollBarcodes` :3046 (yalnız son adımda) → `roll.createMany` :3055 → rollProperty :3093 → `recordPlanDeviationTx` döngüsü :3120 → movement createMany :3144 + recompute :3154 → `recordVarianceTx` döngüsü :3191] → `logTravelerScan` :3212 → findUnique :3224 | work_orders, subcontractor_receipts, subcontractor_receipt_properties, roll_movements, rolls(+yeni), subcontractor_receipt_items, roll_operations, work_order_steps, traveler_cards, batches, roll_barcode_counters, roll_properties, roll_plan_deviations, roll_variances, traveler_card_scans | yok (`resolveRollLabels(prisma)` :2608 pre-tx hata yolu) | yok | yok | sonra :3235; **ardından tx-DIŞI best-effort mutasyonlar** `changeWidth` :3270 ve `changeTargetColor` :3297 (ayrı claim'ler, try/catch) | çoklu claim ✓ + kısmi decrement claim ✓ | idempotency: clientToken :2350 + küme-eşitliği :2390 **pre-tx**; eşzamanlı aynı-token → `subcontractor_receipts_clientToken_key` P2002 → **wBR 5× retry → yanıltıcı 409** (KK1'deki catch-replay burada YOK) → §6 #9; `step.status===ACTIVE` :2492 ve `nextStep` :2664 pre-tx → §6 #8 |
| F4 | :3399 | `closeRemainder` | int | **W** **ilk** :3401 → roll taze :3404 (statü+adım) → **R** claim :3420 `{id, AT_SUB}` count!==1→409 → `recordVarianceTx` :3432 → dispatchItem `updateMany` toplu :3444 → `$executeRaw` movement :3455 → stillAtSub :3470 → step COMPLETED :3474 → `completeWorkOrderIfStepsDone` :3485 → `logTravelerScan` :3490 | work_orders, rolls, roll_variances, subcontractor_dispatch_items, roll_movements, work_order_steps, traveler_card_scans | yok | yok | yok | sonra :3501 | tekil claim (claim WHERE'inde `currentStepId` yok; taze okuma :3404'te var) | `validateVarianceReason` :3393 pre-tx + helper içinde tekrar ✓; `step.workOrder.steps` (nextStep) :3478 pre-tx |
| F5 | :4804 | `cancelReceipt` | int | **W** **ilk** :4807 → bornRolls taze :4813 + guard :4827 → rollMovement **deleteMany** :4835 (fiziksel DELETE) → rollProperty deleteMany :4842 → **R** `updateMany` :4846 bornRolls→CANCELLED (**statü koşulsuz**; W kilidi altında) → recompute :4862 → receipt claim :4868 `{id, cancelledAt:null}` count===0→409 (**mutasyonlardan SONRA**, aynı tx → rollback ile tutarlı) → movement `updateMany` yeniden aç :4882 → `computeReceiptBatchMismatches(tx)` :4909 → **R** tam claim :4930 `{id in, SUBCONTRACTOR_CONSUMED}` count!==len→409 → **R** kısmi increment claim :4948 `{id, AT_SUB, currentStepId}` → rollOperation deleteMany :4964 → receiptProperty deleteMany :4973 → rollVariance `updateMany` reversedAt :4986 → recompute :4996 → `logTravelerScan` :4999 | work_orders, roll_movements, roll_properties, rolls, work_order_steps, subcontractor_receipts, subcontractor_receipt_properties, roll_variances, traveler_card_scans | yok | yok | yok (:4903 sıralı) | sonra :5010 | tekil + çoklu claim ✓ | **LIFO guard :4757 pre-tx, tx içinde tekrarlanmaz** → §6 #7; WO COMPLETED :4744 pre-tx |
| F6 | :5273 | `undoTransfer` | int | **W** **ilk** :5275 → freshBorn :5280 + guard :5297-5321 → strayBorn count :5330 → dispatch claim :5344 `{id, cancelledAt:null}` → **T** dirty :5356 → printed void :5357 → rollOperation/rollMovement **deleteMany** :5366/:5373 → rollProperty deleteMany :5376 → **R** born claim :5377 `{id in, AT_SUB, currentStepId, batchId}` count!==len→409 → kaynak makbuz döngüsü: `findUnique` taze :5397 + claim :5415 `{id, cancelledAt:null}` → movement yeniden aç :5428 → **R** orig claim :5442 `{id in, SUBCONTRACTOR_CONSUMED}` count → rollOperation deleteMany :5452 → receiptProperty :5459 → scan :5461 → recompute :5475/:5477 → scan :5480 | work_orders, subcontractor_dispatches, traveler_cards, printed_documents, roll_operations, roll_movements, roll_properties, rolls, subcontractor_receipts, subcontractor_receipt_properties, work_order_steps, traveler_card_scans | yok | yok | yok | sonra :5491 | tekil + çoklu claim ✓ | `directShippedAt` :5254 pre-tx (dispatch claim WHERE'inde yok; born roll claim AT_SUB şartı pencereyi kapatır — kısmi direct-ship hariç) |
| F7 | :6019 | `executeDirectShip` | int, **wBR** (predicate yok) | **W** **ilk** :6022 → otherInFlight count :6030 → dispatchStillAtSub :6049 → `applyDirectShipSplits(tx)` :6057 (`createFasonShipChild` :496: **K** `generateRollBarcode` :516 → roll.create → rollProperty/rollOperation createMany → rollMovement.create → **R** parent decrement claim :588 `{id, AT_SUB, currentQty gte}` count!==1→409) → dispatch claim :6078 `{id, directShippedAt:null, cancelledAt:null}` (yalnız tam sevk) → `$executeRaw` movement :6093 → **R** consumed claim :6114 `{id in, AT_SUB}` count!==len→409 → roll.findMany :6126 → **Q** DSK no :6136 → `directShipment.create` :6134 → roll.updateMany directShipmentId :6147 → rollOperation createMany :6153 → stillAtSub :6171 → step COMPLETED :6175 → [complete: `$executeRaw` :6185 → step SKIPPED :6199 → `completeWorkOrderIfStepsDone` :6209] → [alloc: orderLine okuma :6224/:6229 → **L** sıralı TAM küme :6233 → taze satırlar :6234 → cap kontrolü :6280 → `subcontractorDirectShipAllocation.create` döngüsü :6288 → recompute **O** :6298] → printed freeze :6305 → scan :6313 | work_orders, rolls(+çocuk), roll_properties, roll_operations, roll_movements, roll_barcode_counters, subcontractor_dispatches, direct_shipments, work_order_steps, order_lines, orders, subcontractor_direct_ship_allocations, printed_documents, traveler_card_scans | yok | yok | yok | sonra :6325 | tekil + çoklu claim ✓; over-cover guard tx-içi taze ✓ | `acceptedReceiptItem` :5906 / `movedRolls` :5931 pre-tx — roll claim AT_SUB şartı kapsar ✓. **Sıra**: W → R → K → D → … → L(sıralı) → O — §3 |

Tx-dışı orkestrasyonlar: `bulkDispatchStep` :1458 (`SEPARATE` dalı :1539-1554 parti başına ayrı `dispatch()` tx'i, **`failed[]` yok** — ilk hata fırlar, önceki sevkler commit'li kalır ve yanıtta görünmez) ; `transferToNextFason` :1591 (firma başına `receive()` :1709 ayrı tx → `bulkDispatchStep` :1738 ayrı tx; belgeli kısmi-başarı :1589) → §7. `cancelBulk` :2183 sıralı + `failed[]` ✓ (belgeli parçalı).

### 2.4 `Teks-Erp/src/services/order.service.ts` (7 site)

| # | site | fonksiyon | tür/opsiyon | kilitler (satır) — sıra | yazılan | tx içi `prisma.` | I/O | P.all | audit | claim sınıfı | CTA |
|---|---|---|---|---|---|---|---|---|---|---|---|
| O1 | :475 | `cancelOrderLine` | int | **L(tek)** claim **ilk** :478 `{id, cancelledAt:null}` count===0→409 → WO döngüsü: link deleteMany :493 → count :498 → **W** `updateMany {id, type:ORDER}` :500 (statü/targetItem guard'sız) → **T** dirty :505 → `orderLine.findMany` :510 → **L(hepsi, sıralı)** :512 → recompute **O** :513 | order_lines, work_order_to_order_lines, work_orders, traveler_cards, orders | yok | yok | yok | sonra :516 (`.catch`) | tekil claim ✓ | preview (`getLineCancelPreview` :458) **pre-tx**: `blockers` (`blockedNoTargetItem` :396) tx'te tekrarlanmaz; `affectedWoIds` pre-tx → §6 #9. **Sıra**: L(x) → W → L(hepsi sıralı) → O — §3 ABBA-4 |
| O2 | :2148 | `quickOrderFromRolls` | int | **R** çoklu claim :2149 `{id in, STOCK, shipmentId:null, currentStepId:null}` count!==len→409 (hepsi-ya-hiç ✓; hata yolunda `findMany` :2154 mesaj için) | rolls | yok | yok | yok | sonra :2181 | çoklu claim ✓ | ⚠️ `create()` :2173 **tx DIŞINDA, claim'den SONRA** — create düşerse toplar WAREHOUSE'da kalır (yorum :2134-2140 kabul ediyor) → §7; claim WHERE'inde `sackId` yok (pre-check :2107'de var) |
| O3 | :2386 | `update` | int | [lines: `workOrderToOrderLine.findFirst` taze :2391 → **L** `FOR UPDATE` :2434 (yalnız `toDelete`, `id = ANY(array)` — sıra belirsiz) → link taze :2435 → deleteMany :2447 → `orderLine.update/create` döngüsü (payload sırası) :2473/:2498 + requiredProperty del/create] → **O** claim :2521 `{id, status: current.status}` count===0→409 → [lines: recompute :2537 → **L(hepsi)** `orderLine.update` :130 + **O** :196] → findUnique | order_lines, order_line_required_properties, orders | yok | yok | yok | sonra :2548; ardından tx-dışı `promoteCustomerAliases` :2566 | tekil claim (pre-tx gözlenen statüye pin ✓) | WO-bağı guard'ı :2361 pre-tx + :2391/:2435 tx-içi taze ✓; `getActiveShipmentLinks` bu yolda **hiç** yok; recompute öncesi **tam-küme `touchOrderLinesTx` YOK** → §6 #23 / §3 ABBA-4 |
| O4 | :2757 | `softDelete` (sipariş iptali) | int | **O** claim **ilk** :2760 `{id, status notIn [CANCELLED,COMPLETED]}` → links taze :2781 → **W(sıralı)** `touchWorkOrderTx` :2786 → WO taze :2787 + blocking :2792 → link deleteMany :2814 → WO döngüsü (sıralı): **T** dirty :2827 → count :2828 → **W** `updateMany {id, type:ORDER, status notIn, targetItemId not null}` :2833 | orders, work_order_to_order_lines, traveler_cards, work_orders | yok | yok | yok | sonra :2849 | tekil claim ✓ | `getActiveShipmentLinks` :2726 **pre-tx yalnız** → §6 #10 |
| O5 | :3138 | `cancelWithActions` | int | **O** claim **ilk** :3144 → **W(sıralı)** :3172 → WO taze :3173 + izinli-aksiyon taze :3190 → yeni-bağ kontrolü :3197 → WO döngüsü: CONVERT `updateMany {id, status notIn}` :3210 → link deleteMany :3219 → **T** dirty :3231 → UNLINK count :3240 → **W** type flip (targetItemId guard'lı) :3242 | orders, work_orders, work_order_to_order_lines, traveler_cards | yok | yok | yok | sonra :3257 | tekil claim ✓ | ⚠️ `CANCEL_WO` aksiyonları **tx-ÖNCESİ** `woService.softDelete` :3133 (her biri ayrı tx) — sipariş tx'i düşerse WO'lar iptal kalır → §7; `getActiveShipmentLinks` :3081 pre-tx yalnız → §6 #10 |
| O6 | :3318 | `manualComplete` | int | **O** claim :3319 `{id, status notIn [COMPLETED,CANCELLED,PENDING]}` count===0→409 | orders | yok | yok | yok | sonra :3342 | tekil claim ✓ | `assertNoActiveShipments` :3300 pre-tx yalnız |
| O7 | :3392 | `reopen` | int | **O** claim :3397 `{id, manualClosedById not null}` → recompute :3411 (**L(hepsi)** update :130, **O** :196) **`touchOrderLinesTx` OLMADAN** | orders, order_lines | yok | yok | yok | sonra :3415 | tekil claim ✓ | ⚠️ helper protokolü (`order-status.helper.ts:205-209`: "ÇAĞIRAN … TAM satır kümesini `touchOrderLinesTx` ile … kilitlemeli") burada uygulanmıyor → §6 #23 |

Tx-dışı claim'ler: `changeLineColor` :2694 `updateMany {id, orderId, colorId: eski}` ✓ (tx'siz meşru).

### 2.5 `Teks-Erp/src/services/inventory.service.ts` (7 site)

| # | site | fonksiyon | tür/opsiyon | kilitler (satır) — sıra | yazılan | tx içi `prisma.` | I/O | P.all | audit | claim sınıfı | CTA |
|---|---|---|---|---|---|---|---|---|---|---|---|
| I1 | :818 | `createInitialEntry` (KK1) | int (wBR YOK — sayaç atomik) | [guard aktifse **A21** :844 → `roll.findFirst` ikiz :848 (kilit **önce** ✓, sayaçtan **önce** ✓)] → **K** `generateRollBarcode(tx)` :905 → `roll.create` :906 → rollProperty createMany :949 | rolls, roll_properties, roll_barcode_counters | yok (`readKk1DuplicateGuardEnabled()` :808 **pre-tx**, gerekçesi :802-805) | yok | yok | sonra :1009 (replay yolunda yok) | create; clientToken P2002 → catch **tx dışı** :955 → `findUnique` :963 + kimlik-kilit :976-979 → replay ✓ / `CLIENT_TOKEN_COLLISION` 409 | ⚠️ replay dalı `existing.status`'a **bakmıyor** (:971-985) — CANCELLED/SCRAP top için `success:true` döner → §6 #13 (beceri §8 "en pahalı hata" sınıfı, ② doğrulasın); ürün/renk/özellik/kalite :694-774 pre-tx ✓ |
| I2 | :3175 | `softDelete` (top iptal/fire) | int | `collectRollStepScopeTx` :3195 (okuma) → **W(sıralı, id ASC)** :3201 → movement findMany :3204 + update döngüsü :3234 → **R** claim :3253 `{id, status: existing.status, shipmentId: existing.shipmentId}` count===0→409 → findUniqueOrThrow → recompute döngüsü :3309 | work_orders, roll_movements, rolls, work_order_steps | yok | yok | yok | sonra :3332 | tekil claim (gözlenen statüye pin ✓) | pre-tx guard'lar :3015-3083 (fason/açık sevk/PLANNED sevkiyat/çuval/aktif istasyon) — claim `status+shipmentId` pin'i çoğunu kapsar; `sackId` pin'lenmez (claim `sackId:null` yazar); `resolveReasonCode` :3150 pre-tx ✓ |
| I3 | :3549 | `hardDelete` (arşiv) | int | `collectRollStepScopeTx` :3558 → **W(sıralı)** :3559 → **R** claim :3561 `{id, STOCK}` → movement update döngüsü :3586 → recompute :3612 | work_orders, rolls, roll_movements, work_order_steps | yok | yok | yok | sonra :3625 | tekil claim ✓ | ✓ |
| I4 | :3916 | `applyManualProperties` | int | roll taze :3921 (shipmentId/sackId) → [**S** `touchWarehouseSackTx` :3927 çuvaldaysa] → **R** claim :3932 `{id, shipmentId:null, sackId: cur.sackId}` count===0→409 → rollProperty deleteMany(FLAG)/createMany :3947-3953 | rolls, roll_properties, (sacks) | yok | yok | yok | sonra :3961 | tekil claim (üyelik pin'li) | ⚠️ **statü** (FREE_STOCK/süpervizör kapsamı :3742) ve **`rollWhole`** (:3783, `initialQty==currentQty`) pre-tx; claim `status`/`currentQty` pin'lemez → §6 #12 |
| I5 | :4122 | `createOpenFabric` | int | **W** **ilk** :4128 → receipt/WO taze :4130 → step taze :4147 → `assertKursunTabletMayWrite(tx)` :4164 → `roll.create` :4166 (clientToken) → rollProperty :4191 → `rollMovement.create` :4201 → recompute :4212 → **W** ensure :4213 | work_orders, rolls, roll_properties, roll_movements, work_order_steps | yok | yok | yok | sonra :4236 (replay yolunda yok) | create; clientToken P2002 catch tx-dışı :4221 → replay (statü kontrolsüz — yeni top) | pre-tx :4086-4116 + tx-içi taze ✓ (F115) |
| I6 | :4372 | `rescueStuckRoll` | int | movement okuma :4374 → step okuma :4390 → **W** :4396 → movement update döngüsü :4407 → **R** claim :4422 `{id, IN_PRODUCTION, shipmentId:null, sackId:null}` → [**K** `generateRollBarcode` :4439 + `roll.update` :4440] → recompute :4444 → `completeWorkOrderIfStepsDone` :4445 | work_orders, roll_movements, rolls, roll_barcode_counters, work_order_steps, traveler_cards | yok | yok | yok | sonra :4450 | tekil claim ✓ | `openDispatch` :4364 pre-tx (dispatch W kilidi + claim AT_SUB → kapsanır ✓); `roll.currentQty/weightKg/barcode/currentStepId` :4345 pre-tx değerlerle yazılıyor |
| I7 | :4667 | `kursunFinish` | int | **W** **ilk** :4670 → `assertKursunTabletMayWrite(tx)` :4680 → `roll.update` :4683 (koşulsuz metraj) → rollError createMany :4693 → rollOperation createMany skipDuplicates :4732 → `copyStationCapabilitiesToRoll(tx)` :4736 → `roll.update` renk :4750 → movement claim :4760 `{rollId, stepId, exitedAt:null}` count===0 → throw (raceLost → idempotent 200 :4807) → [`openMovementForNextStep` :4782 (movement.create + roll.update + recompute) | `finalizeRollsAtLastStep` :4795 (**K** `reserveRollBarcodesInOrder` `roll-finalize.helper.ts:172`)] → recompute :4798 → ensure :4799 → `completeWorkOrderIfStepsDone` :4803 | work_orders, rolls, roll_errors, roll_operations, roll_properties, roll_movements, roll_barcode_counters, work_order_steps, traveler_cards | yok (`hasBypassClosureOnProcessQcTx(prisma)` :4580 pre-tx okuma; `loadStationPropertyCaps(prisma)` :4654 pre-tx) | yok | yok | sonra :4821 | koşullu kapanış claim (`exitedAt:null`) ✓ — kaybeden tx rollback + idempotent yanıt | `nextStep` :4640 ve `roll.currentStep` :4520 pre-tx; W kilidi altında movement claim yarışı kapatır ✓ |

Tx-dışı claim'ler: `restoreCancelledRoll` :3452 `updateMany {id, CANCELLED}` (tx yok — belgeli; sinyaller :3422 pre-tx `Promise.all` havuz ✓); `prepareRawForSale` :4033 `updateMany {id, STOCK, shipmentId:null, currentStepId:null}` ✓.

### 2.6 `Teks-Erp/src/services/return.service.ts` (2 site)

| # | site | fonksiyon | tür/opsiyon | kilitler (satır) — sıra | yazılan | tx içi `prisma.` | I/O | P.all | audit | claim sınıfı | CTA |
|---|---|---|---|---|---|---|---|---|---|---|---|
| R1 | :481 | `createReturn` | int | **A23** :490 (**ilk ifade** ✓, `undoDispatch` ile aynı anahtar) → **R** tekil claim döngüsü :501 `{id, SHIPPED}` count===0→409 (grup tamamı rollback ✓; **sıra = istemci `rollIds`**) → `rollReturn.create` :521 → `rollReturn.updateMany` grup :551 → printed_documents freeze :564 | rolls, roll_returns, printed_documents | yok | yok | yok (:494 sıralı) | sonra :576 (satır başına) | tekil claim ×N (hepsi-ya-hiç ✓) | tek-sevkiyat kuralı :401 pre-tx; A23 aynı sevkiyat üzerinde serileştirir → istemci sıralı R kilitleri ABBA üretmez ✓ |
| R2 | :879 | `cancelReturn` | int | **R** claim :882 `{id, status: expected, shipmentId:null, sackId:null}` count===0→409 → `rollReturn.update` :901 (koşulsuz) → `rollReturn.count` :913 → printed void :920 / `reissueForSourceTx` :927 (claim :761 `{id, ACTIVE}`) | rolls, roll_returns, printed_documents | yok | yok | yok | sonra :937 | tekil claim (roll üzerinde; `rollReturn.update` claim değil ama roll claim'i ikinci iptali 409'a düşürür) | ⚠️ **A23 YOK** ve sevkiyat statüsü doğrulanmıyor (:846 yalnız `fromShipmentId` varlığı) — `undoDispatch` (A23 + H claim) ile paralel: top `SHIPPED` + `shipmentId=from` yazılırken sevkiyat PLANNED/CANCELLED olabilir → §6 #15; `restoreSackId` :870 pre-tx (FK P2003 riski) |

### 2.7 `Teks-Erp/src/services/subcontractor-management.service.ts` (2 site)

| # | site | fonksiyon | tür/opsiyon | kilitler (satır) — sıra | yazılan | tx içi `prisma.` | I/O | P.all | audit | claim sınıfı | CTA |
|---|---|---|---|---|---|---|---|---|---|---|---|
| M1 | :528 | `create` | int | [reaktivasyon: `subcontractor.updateMany` claim :533 `{id, isActive:false}` count===0→409 → category deleteMany :540] / [`subcontractor.create` :546] → `assertActiveCategories(tx)` :553 → category createMany :555 → findUnique :562 | subcontractors, subcontractor_to_categories | yok | yok | yok | sonra :568 | tekil claim ✓ (reaktivasyon) | ⚠️ kod-katlama (`decideCodeUniqueness` :512 ← `loadSubCodeCandidates(prisma)` :514), ad (`assertSubNameAvailable` :518, havuz) ve VKN (`assertSubTaxAvailable` :525, havuz) kontrolleri **pre-tx, kilitsiz**; DB seddi: `subcontractors_code_key` (TAM eşleşme), `subcontractors_nameFold_key` (partial, yumuşak kapı — saha kopyasında **var**), VKN için **yok** (bilinçli), **katlanmış kod için yok** → `sefa`/`SEFA` yarışı → §6 #16 (item.service'te `lockCodeScopeTx` 8026 var, burada yok) |
| M2 | :665 | `update` | int | `subcontractor.update` :666 (koşulsuz) → [category deleteMany :669 → `assertActiveCategories(tx)` :671 → createMany :673] → findUnique | subcontractors, subcontractor_to_categories | yok | yok | yok | sonra :688 | — | ad/kod/VKN kontrolleri :635-663 pre-tx (yalnız değişince) — M1 ile aynı sınıf |

### 2.8 `Teks-Erp/src/services/workorder-link.service.ts` (2 site)

| # | site | fonksiyon | tür/opsiyon | kilitler (satır) — sıra | yazılan | tx içi `prisma.` | I/O | P.all | audit | claim sınıfı | CTA |
|---|---|---|---|---|---|---|---|---|---|---|---|
| K1 | :375 | `linkOrderLines` | int | link createMany skipDuplicates :376 (PK seddi) → **W** `updateMany {id, type:STOCK}` :381 (statü guard'sız) → **T** dirty :388 | work_order_to_order_lines, work_orders, traveler_cards | yok | yok | yok | sonra :392 | idempotent (skipDuplicates) + koşullu flip | `assertPlanEditable(wo)` :279 ve kumaş/renk/iptal-kalem doğrulamaları :281-342 **pre-tx**, tx'te tekrarlanmaz → §6 #19 |
| K2 | :453 | `unlinkOrderLine` | int | `workOrderToOrderLine.delete` :454 (PK; yoksa P2025) → count taze :461 → **W** `updateMany {id, type:ORDER}` :463 (**`targetItemId not null` guard'ı YOK** — `order.softDelete` :2838 / `cancelWithActions` :3248'de var) → **T** dirty :470 | work_order_to_order_lines, work_orders, traveler_cards | yok | yok | yok | sonra :473 | koşullu flip | `lastLinkOfOrderWo && !wo.targetItemId` :445 pre-tx yalnız → §6 #20 |

Tx-dışı yazımlar: `changeTargetColor` :564 claim `{id, status notIn, targetColorId: eski}` ✓ + `markTravelerCardDirtyTx(prisma, …)` :577 (**havuz client'ıyla çift-mod çağrı**, claim ile ayrı ifade); `changeWidth` :632 claim + :640 aynı desen; `applyAttributeToRolls` :784 döngü, her top ayrı tx (`applyManualProperties`), `failed[]` ✓; `linkOrderLineWithOverride` :863 üç ayrı işlem, belgeli non-atomik (:852-857).

### 2.9 `Teks-Erp/src/services/workorder-split.service.ts` (3 site)

| # | site | fonksiyon | tür/opsiyon | kilitler (satır) — sıra | yazılan | tx içi `prisma.` | I/O | P.all | audit | claim sınıfı | CTA |
|---|---|---|---|---|---|---|---|---|---|---|---|
| P1 | :336 | `redyeSameColor` | int, **wBR** | **R** çoklu claim **ilk** :339 `{id in, batchId, status in REDYE, sackId:null, shipmentId:null}` count!==len→409 → `createBatchTx` :359 (**A22** → batches → roll batchId → **T** dirty) → movement updateMany/createMany :367/:371 → rollError updateMany :384 → recompute döngüsü :391 (→ **W** ensure) → **W** ensure :392 → **W** `updateMany {id, COMPLETED}`→IN_PROGRESS :394 → **T** :399 → `deleteIfEmptyAndTraceless` :403 | rolls, batches, traveler_cards, roll_movements, roll_errors, work_order_steps, work_orders | yok | yok | yok | sonra :409 | çoklu claim ✓ | `loadSplitContext(prisma)` :231 (`allowedModes`, `colorStep`, WO statüsü) pre-tx, tx'te tekrarlanmaz → §6 #21. **Sıra**: R → A22 → W — §3 ABBA-1 ailesi |
| P2 | :454 | `newColorRedye` | int, **wBR** | **R** claim **ilk** :456 → `cloneWorkOrderTx` :471 (**Q** IE no :116 → yeni WO/steps/links/props → **T** yeni kart :187) → `repointRollsTx` :481 (roll/movement/operation/error updateMany ×6 per adım) → movement :484/:488 → roll.updateMany :498 → `createBatchTx` :504 (**A22**) → rollError :512 → recompute (kaynak adımlar → **W(kaynak)** ensure) :518 → bypass void :528 → `deleteIfEmptyAndTraceless` :531 → `supersedeEmptiedSourceWorkOrderTx` :534 (count → **W(kaynak)** `updateMany {id, status in [PLANNED,IN_PROGRESS]}` :283 → **T** VOIDED :288 → bypass void :299) | rolls, work_orders(+yeni), work_order_steps(+yeni), work_order_target_properties, work_order_to_order_lines, traveler_cards, roll_movements, roll_operations, roll_errors, batches, kursun_bypass_assignments | yok | yok | yok | sonra :540/:556 | çoklu claim ✓ | ctx pre-tx (P1 ile aynı); yeni renk aktifliği :445 pre-tx. **Sıra**: R → W(yeni) → A22 → W(kaynak) — §3 |
| P3 | :615 | `undyedMove` | int, **wBR** | **W(kaynak)** `touchWorkOrderTx` **ilk** :621 → `subcontractorDispatch.findFirst` taze :626 → `cloneWorkOrderTx` :639 → **R** claim :649 `{id in, batchId, AT_SUB, currentStepId}` count → `repointRollsTx` :663 → `batch.update` :666 (koşulsuz workOrderId) → `subcontractorDispatch.update` :667 (koşulsuz; taze okuma W altında) → recompute :673/:674 → bypass void :680 → supersede :683 | work_orders(+yeni), …, rolls, batches, subcontractor_dispatches, … | yok | yok | yok | sonra :689/:704 | çoklu claim ✓ | ctx pre-tx; W kilidi ilk ✓ |

### 2.10 `Teks-Erp/src/services/workorder-manual-move.service.ts` (1 site)

| # | site | fonksiyon | tür/opsiyon | kilitler (satır) — sıra | yazılan | tx içi `prisma.` | I/O | P.all | audit | claim sınıfı | CTA |
|---|---|---|---|---|---|---|---|---|---|---|---|
| V1 | :596 | `manualMove` | int, **wBR** | **R** çoklu claim **ilk** :599 `{id in, status in MOVABLE, sackId:null, shipmentId:null}` count!==len→409 → renk sentezi updateMany :619 → `cutBlockedRollIds(tx)` taze :632 → movement deleteMany (**fiziksel**, belgeli B4) :643 / updateMany :647 / createMany :651 → rollOperation deleteMany :667 + roll qualityGrade null :675 → parti: [`createBatchTx` :699 (**A22**)] / [`batch.findUnique` taze :716 + `isBatchLockedTx(tx)` :733 + roll.updateMany :736] → `deleteIfEmptyAndTraceless` döngüsü :748 → backflush count'lar :767 → step SKIPPED :785 → step SKIPPED→PENDING :801 → recompute döngüsü :813 (→ **W** ensure) → **W** ensure :814 → **W** `updateMany {id, COMPLETED}`→IN_PROGRESS :816 → **T** :821 → bypass void :834 | rolls, roll_movements, roll_operations, batches, traveler_cards, work_order_steps, work_orders, kursun_bypass_assignments | yok | yok | yok | sonra :853 | çoklu claim ✓; `join` hedefi tx-içi taze ✓ (B2) | `loadContext(prisma)` :538 (adımlar, `colorStep`, `woStatus` :543 terminal kontrolü) pre-tx → §6 #22. **Sıra**: R → A22 → P → W — §3 ABBA-1 ailesi |

### 2.11 `Teks-Erp/src/services/workorder-batch-drop.service.ts` (1 site)

| # | site | fonksiyon | tür/opsiyon | kilitler (satır) — sıra | yazılan | tx içi `prisma.` | I/O | P.all | audit | claim sınıfı | CTA |
|---|---|---|---|---|---|---|---|---|---|---|---|
| B1 | :275 | `dropBatch` | int, **wBR** | **W** `touchWorkOrderTx` **ilk ifade** :281 → WO taze :284 → `assertBatchInWorkOrder(tx)` :291 → batch/dispatch/live taze :292-316 → engel yüklemi :318 (kilit altında) → `applyRollDispositionsTx` :360 (**R** çoklu claim + `$executeRaw` + recompute) → residual gruplu çoklu claim :394 `{id in, IN_PRODUCTION, sackId:null, shipmentId:null}` toplam count!==len→409 → **K** `reserveRollBarcodes` :418 + roll.update döngüsü → producedInStepId :429 → `$executeRaw` :439 → **T** dirty :457 → recompute döngüsü :461 → `deleteIfEmptyAndTraceless` :467 → count/groupBy okumaları :472-495 | work_orders, rolls, roll_movements, work_order_steps, roll_barcode_counters, traveler_cards, batches | yok | yok | yok (:459 sıralı) | sonra :522 + logMany :540 | çoklu claim ✓ (hepsi-ya-hiç, yazılı) | **model desen**: tüm okumalar kilit altında; yalnız `resolveReasonCode` :251 pre-tx (okuma). **Sıra**: W → batch → R → K → P → T |

## 3. KİLİT SIRASI HARİTASI (ABBA adayları)

Kilit sınıfları §0 sözlüğündeki harflerle. Yalnız **birden çok kilit sınıfı** alan tx'ler listelendi; sıra = kodda alınma sırası (ilk alınan solda).

### 3.1 Tx başına sıra

| tx | sıra |
|---|---|
| F1 `dispatch` | W → R(autoAttach) → batch satırları → **A22** (createBatchTx) → D(create) → printed → T → R(claim) → M → roll_ops → scan |
| F3 `receive` | W → R → D(receipt) → M → R → D-items → roll_ops → P → W(complete) → T → [**A22**] → [**K**] → R(create) → P → variance |
| F2 `cancel` / F4 `closeRemainder` / F5 `cancelReceipt` / F6 `undoTransfer` | W → D → T → R → M → … → P → W(complete) |
| F7 `executeDirectShip` | W → R → **K** (split) → D → M → R → direct_shipments → P → **L(sıralı, tam küme)** → O → printed |
| W2 `softDelete` / W4 `hardDelete` / W6 `update` / W7 `replace` | W(claim) → R → M → P → T (W6/W7: W → W → T) |
| W3 `completeWorkOrder` | W(claim) → R → [W(yeni) → T(yeni) → R → bypass → **A22** → batch] → [M → R → **K**] → M → P → bypass → T |
| **W5 `attachRolls`** | **R → M → A22 → batch → T → P → W** (W **en sonda**, `ensureWorkOrderInProgress` :4566) |
| **V1 `manualMove`** | **R → M → roll_ops → A22/batch → P → W** (:814/:816) |
| **P1 `redyeSameColor`** | **R → A22 → batch → T → M → roll_errors → P → W** (:392/:394) |
| **P2 `newColorRedye`** | **R → W(yeni, create) → T(yeni) → M/ops/errors → A22 → batch → P → W(kaynak)** (:518 recompute→ensure, :534 supersede) |
| P3 `undyedMove` | W(kaynak) → W(yeni) → R → batch → D → P → W(supersede) |
| B1 `dropBatch` | W → batch → R → **K** → P → T |
| I1 `createInitialEntry` | **A21** → **K** (belgeli sıra, :820-830) |
| I2/I3 | W(sıralı, id ASC) → M → R → P |
| I5 `createOpenFabric` | W → R(create) → M → P → W |
| I6 `rescueStuckRoll` | M(oku) → W → M → R → **K** → P → W |
| I7 `kursunFinish` | W → R → roll_errors → roll_ops → M(claim) → M/R (next) veya **K** → P → W |
| S16 `createShipment` | **Q** → H(create) → **S(istemci sırası)** → R → swatch → shipment_orders → sack_allocations → [H(claim) → R → **L(sıralı)** → O → printed] |
| S17 `addSacksToShipment` | H → **S(istemci sırası)** → R → swatch → alloc |
| S21 `dispatchShipment` | H(claim) → R → **L(sıralı)** → O → printed |
| S22 `cancelShipment` | H(claim) → **L(sıralı)** → alloc → R → swatch → S → O |
| S23 `undoDispatch` | **A23** → H → shipment_orders → R → **L(sıralı)** → O → printed → [S/R/swatch/O tekrar] |
| R1 `createReturn` | **A23** → R(istemci sırası) → roll_returns → printed |
| R2 `cancelReturn` | R → roll_returns → printed (**A23 yok**) |
| S10 `moveRollToSack` / S12 `moveRollsToSack` | **S(from) → S(to)** (id sırasız) → R |
| S3/S5 `scanIntoSack` taşı | S(hedef **yalnız**) → R |
| S13 `splitSack` | S(kaynak) → S(yeni) → R |
| O1 `cancelOrderLine` | **L(tek claim) → W → T → L(hepsi, sıralı) → O** |
| O3 `update` (sipariş) | **L(FOR UPDATE, `= ANY(array)` sırası) → L(payload sırası) → O(claim) → L(hepsi, `order.lines` sırası) → O** |
| O4/O5 sipariş iptali | O(claim) → W(sıralı) → links → T → W |
| O7 `reopen` | O(claim) → L(hepsi, kilitsiz recompute) → O |

### 3.2 Ters sıralama çiftleri (ABBA adayları — ② için)

| # | A akışı | B akışı | Ortak kaynaklar | Somut senaryo (② doğrulasın) |
|---|---|---|---|---|
| ABBA-1 | **"roll-first" ailesi**: W5 `attachRolls` (R → A22 → W), V1 `manualMove` (R → A22 → W), P1/P2 split (R → A22 → W) | **"WO-first" ailesi**: F1 `dispatch` (W → R → A22), W3 `completeWorkOrder` TRANSFER (W → R → A22), F3 `receive` (W → R → A22), B1, I2/I3, I5-I7, W2, W4, W6, W7, O4/O5 | W satırı + R satırları + **A22 (tek anahtar, sistem geneli)** | Aynı WO'da Hızlı İş Emri `attachRolls` (STOCK top X'i claim eder :4494 → A22 :4553 alır → W'yi sonda ister :4566) ∥ mobil `dispatch` autoAttach aynı STOCK top X (W :1041'i tutar → R X'i bekler :1084). attach W'yi, dispatch R'yi bekler → PG deadlock → 40P01 → P2034 → 409 (`error.middleware.ts:517`). Ayrıca A22 tek anahtar olduğundan **birbiriyle ilgisiz iki WO** bile A22↔W çaprazında buluşabilir: attach(WO1) A22'yi tutup W1'i beklerken, complete-TRANSFER(WO1) W1'i tutup A22'yi bekler. Proje notu (`kursun-bypass.routes.ts:166`, `subcontractor.service.ts:2220`) deadlock bilincini "WO-first" ailesi için taşıyor; roll-first ailesi belgesiz. |
| ABBA-2 | S10 `moveRollToSack(from=A,to=B)` :691-692 | S10 ters yön (from=B,to=A); S12 `moveRollsToSack` aynı | iki Sack satırı, **id sırasız** | İki operatör aynı anda A→B ve B→A taşıma yaparsa `touchWarehouseSackTx` UPDATE'leri çapraz bekler → deadlock → 409. Sıralı (id ASC) ya da tek `updateMany` ile alınmıyor. |
| ABBA-3 | S16 `createShipment` sack claim döngüsü :1412 (**istemci `sackIds` sırası**) | S17 `addSacksToShipment` :1551 (aynı) ya da ikinci `createShipment` çakışan küme ters sırayla | Sack satırları | İki sevkiyat kurulumu aynı çuval kümesini farklı sırayla seçerse (nadir ama mümkün: iki panel aynı havuz listesinden) → çapraz UPDATE bekleme → deadlock → wBR P2034'ü retry etmez → 409. |
| ABBA-4 | O1 `cancelOrderLine`: L(x) claim :478 → … → L(hepsi sıralı) :512 (**iki-parti edinim**) | S21/S16 `performDispatchTx` :1878, S22 :1975, S23 :2210, F7 :6233 (**tam küme sıralı, tek parti**) | OrderLine satırları | `order-status.helper.ts:205-209` tam da bunu yasaklıyor ("alt-küme kilidi + tam-küme yazımı = iki-parti edinim → deadlock riski"). cancelOrderLine x satırını tutup A'yı beklerken dispatch A'yı tutup x'i bekler (x > A sıralamada). Aynı sınıf: O3 `update` (FOR UPDATE `= ANY(array)` sırası + payload sırası + recompute). |
| ABBA-5 [VARSAYIM] | Set-bazlı `updateMany` ile alınan R kilitleri (S16 :1420 `where sackId in`, S22 :1979 `where shipmentId`, W2 :3513 blanket, F1 :1319 `id in`) | Aynı satırlara tekil sırayla dokunan tx'ler | Roll satırları | PG set-bazlı UPDATE'te kilit sırası tarama sırasıdır; ters sıralı tekil kilitlerle deadlock teorik. Somut senaryo bulunamadı — ② ölçmeden bulgu yazmasın. |

### 3.3 Kilit-önce-okuma sıra kontrolü (beceri §4.2)

| Kilit | Korunan okuma | Sıra | Kanıt |
|---|---|---|---|
| A21 `inventory.service.ts:844` | ikiz `findFirst` :848; sayaç :905 | ✓ kilit önce (KK1 dersinden) | :820-830 yorumu + kod |
| A22 `batch.service.ts:126` | `readLastShortBatchSeqTx` :127 / `batch.findMany` :132 | ✓ ilk ifade | kod |
| A23 `shipping.service.ts:2159` | `rollReturn.count` :2167 | ✓ ilk ifade | kod |
| A23 `return.service.ts:490` | roll claim :501 | ✓ ilk ifade (tekil `if (lockShipmentId)` — sevkiyatsız top zaten :394'te reddedilir) | kod |
| W `touchWorkOrderTx` (F1 :1041, F3 :2681, F2 :2016, F4 :3401, F5 :4807, F6 :5275, F7 :6022, B1 :281, P3 :621, I5 :4128, I7 :4670, W6 :4778) | "tüm toplar döndü" sayımları, taze WO/step statüleri | ✓ ilk ifade | kod |
| W I2 :3201 / I3 :3559 | `collectRollStepScopeTx` :3195/:3558 **kilitten ÖNCE** koşuyor (kapsam çözümü okuması); movement/claim sonra | ⚠️ kapsam okuması kilitsiz — kapsam yalnız WO id kümesi (değişmez ilişki) olduğu için pratik etkisi yok [VARSAYIM] | kod |
| W I6 :4396 | movement okumaları :4374 kilitten önce; kapanış :4407 kilitten sonra | ⚠️ `openMoves`/`affectedStepIds` kilitsiz okundu, `openForClose` :4402 kilit altında tekrar okunuyor ✓ | kod |
| W-claim W2 :3387 / W3 :3866 | in-flight sayımları :3423/:3888 | ✓ claim önce (F57) | yorum :3377-3386 |
| K (sayaç) | — (satır kilidi, commit'e kadar) | belgeli olarak tx sonlarına yakın ve TEK rezervasyon (F-CORE-VER-001); kilit süresi = kalan tx gövdesi | `roll-barcode.helper.ts:47-70` |

## 4. ÇİFT-MOD HELPER'LAR (yer, geçilen client)

Helper'ın kendisi bulgu değildir (beceri §2); aşağıda her çağrı yeri ve geçilen client. **Havuz client'ıyla, atomik olması gereken bir yerde** çağrılanlar ⚠️ ile işaretli.

| Helper (imza) | Çağrı yeri | Geçilen client | Not |
|---|---|---|---|
| `computeWorkOrderLocks(db)` `workorder-locks.helper.ts:90` | `workorder.service.ts:4675` (update, pre) | prisma | UX ön-kontrol |
| | :4779 (update, tx) | tx ✓ | taze |
| | :4912 (replace, pre) | prisma | UX |
| | :5274 (replace, tx) | tx ✓ | taze |
| | :5627 (updateTargetProperties) | **prisma — tek çağrı, tx-içi ikizi YOK** ⚠️ | §6 #2 |
| `assertTargetColorChange(db)` `workorder-target-color.helper.ts:109` | `workorder.service.ts:4697` (pre) / :4794 (tx) | prisma / tx ✓ | F58 çifti |
| | `workorder-link.service.ts:525` (changeTargetColor) | **prisma, tx yok** — sonra claim :564 `targetColorId: wo.targetColorId` pin'i ile kapatılıyor | mal-plan uyumu (`mismatchRows` :99+) claim'de pin'lenmez → bekçi ile yazım arası boyanan top kaçabilir [VARSAYIM, ② ölçsün] |
| `assertKursunTabletMayWrite(db)` `kursun-bypass-eligibility.helper.ts:256` | `inventory.service.ts:4164`, :4680 | tx ✓ | |
| | `kursun-qc.service.ts:613`, :812 | **prisma (tx yok)** | kapsam dışı dosya — K3b'ye not |
| | `kursun-qc.service.ts:448`, :869 | tx ✓ | K3b |
| `hasBypassClosureOnProcessQcTx(db)` `kursun-bypass-guard.helper.ts:105` | `inventory.service.ts:4580` | prisma (pre-tx idempotency okuması) | okuma, meşru |
| `readXxx` sistem ayarı okuyucuları (`tx?` opsiyonel → `prisma`) `system-setting.service.ts` | `readWorkOrderDefaultPlanDurationDays()` ← `resolvePlanDates` `workorder.service.ts:395` ← **tx içinden** :1002 ve :5505 | **prisma (argümansız) — tx AÇIKKEN havuzdan ikinci bağlantı** ⚠️ | §5 |
| | `readShippingToleranceMeters(tx)` ← `recomputeOrderStatus` :165 / `…ForOrders` :218 | tx ✓ | |
| | `readBatchShortNumberEnabled(tx)` ← `generateBatchNumberTx` `batch.service.ts:128` | tx ✓ | |
| | `readKursunBypassEnabled(db)` ← `assertKursunTabletMayWrite` :266 | db (tx) ✓ | |
| | `readKk1DuplicateGuardEnabled()` `inventory.service.ts:808`, `readShipmentConfirmationEnabled()` `shipping.service.ts:1397`, `readShipmentUndoSameDayOnly()` :2149, `readSimulatedWeightEnabled()` :941, `readReturnGradingEnabled()` `return.service.ts:460`, `readKk1WeightEntryEnabled()` `inventory.service.ts:686` | prisma, **tx dışı** ✓ (belgeli gerekçe :802-805) | |
| `resolveQualityGradeIdStrict(code, client=prisma)` `quality-grade.helper.ts:35` | `inventory.service.ts:773`, :3877 | prisma, pre-tx | okuma |
| `loadQualityTargetMaps(tx)` `roll-finalize.helper.ts:22` | `workorder.service.ts:5989`, `workorder-batch-drop.service.ts:380`, `roll-finalize.helper.ts:153` | tx ✓ | |
| `markTravelerCardDirtyTx(tx: TransactionClient)` `traveler-card-dirty.helper.ts:34` (imza tx ister; `PrismaClient` yapısal olarak uyar) | 20+ tx-içi çağrı (shipping yok; workorder :4840/:5562/:5716, link :388/:470, order :505/:2827/:3231, subcontractor :1294/:2033/:5356, batch-drop :457, `createBatchTx` :254) | tx ✓ | |
| | `workorder-link.service.ts:577` (changeTargetColor), :640 (changeWidth) | **prisma (havuz), claim'den AYRI ifade** ⚠️ | claim commit olur, dirty işareti düşerse kart bayat kalır (küçük, "fazla işaretlemek güvenli" yönü) |
| `computeReceiptBatchMismatches(client)` `subcontractor.service.ts:415` | :4909 (cancelReceipt) | tx ✓ | |
| | `getCancelPreview` (:4581+) | prisma | önizleme, okuma |
| `resolveRollLabels(client)` :366 | :2608 | prisma (pre-tx hata yolu) | okuma |
| `cutBlockedRollIds(db)` `workorder-manual-move.service.ts:126` | :632 (tx) | tx ✓ | |
| | `getManualMovePreview` | prisma | önizleme |
| `generateRollBarcode` / `reserveRollBarcodes(db: TransactionClient)` `roll-barcode.helper.ts:39/:74` | `inventory.service.ts:905`, :4439; `subcontractor.service.ts:516`, :3046; `roll-disposition.helper.ts:296`; `roll-finalize.helper.ts:172`; `workorder-batch-drop.service.ts:418` | tx ✓ (hepsi) | helper docstring'i "tx DIŞINDA çağrılmak için" der (:47-70) ama bu kapsamdaki tüm çağrılar tx içinde — her birinin gerekçesi yorumda (küme claim'den sonra çözülüyor) |
| `printedDocumentService.freezeForSource/voidForSource/reissueForSourceTx(tx)` `printed-document.service.ts:313/:722/:747` | shipping :1881/:2214, subcontractor :1283/:2037/:5357/:6305, return :564/:920/:927 | tx ✓ | builder'lar `db` parametreli (doğrulandı: hedef aralıklarda `prisma.` 0); `getCurrent` :357 lazy-init kendi tx'ini açar (K3b) |
| `travelerCardService.createForWorkOrder(tx)` `traveler-card.service.ts:175` | `workorder.service.ts:1081`, `workorder-clone.helper.ts:187` | tx ✓ (`buildSnapshot(client)` :189 → `buildPlan(client)`, `resolveForPrint(null, client)` — gövdeleri okunmadı [VARSAYIM: client'ı sonuna kadar taşırlar], K3b) | |
| `assertRollMatchesPlan` `tambur-plan-gate.helper.ts:65` (global `prisma`, audit içerir) | Tambur yolları (K3b) — bu kapsamda çağrı yok; `recordPlanDeviationTx(tx)` :168 ← `subcontractor.service.ts:3120` tx ✓ | | |
| `AuditService.log/logMany` (global prisma, best-effort) | tümü tx sonrası | prisma ✓ | tx içinde çağrı yok |

## 5. TX SIZINTISI ADAYLARI (tx callback'i içinde havuz client'ı)

| # | Yer | Ne oluyor | Etki sınıfı (② karar versin) |
|---|---|---|---|
| L1 | `workorder.service.ts:989` (`create` tx :988 içinde `this.generateWorkOrderNumber()`) → :581 `prisma.workOrder.findMany` + :592 `prisma.workOrder.findUnique` (5 denemeli döngü) | IE numarası tx AÇIKKEN **ikinci havuz bağlantısından** okunuyor; kendi tx'inin commit'siz satırını göremez (tek WO/tx olduğu için işlevsel etkisi yok); eşzamanlı iki create aynı numarayı okur → P2002 → wBR retry. Aynı dosyada `shipping.service.ts:133-151` yorumu bu deseni tam olarak "havuz kendi kendini bekleme riski" diye reddedip `nextShipmentNo(tx)`'e çevirmiş; `workorder-clone.helper.ts:31` tx-alan ikizi (`generateWorkOrderNumberTx`) zaten var ve kullanılmıyor | havuz 30; `roll-barcode.helper.ts:56-64` ölçümü ("havuz client'ını tx açıkken kullanma: 30 eşzamanlı işlemin 3'ü tamamlandı") aynı sınıf |
| L2 | `workorder.service.ts:1002` ve `:5505` `resolvePlanDates` → :395 `readWorkOrderDefaultPlanDurationDays()` argümansız → `system-setting.service.ts:2925` `tx ?? prisma` | salt-okuma, tek sorgu; yine ikinci bağlantı. Yalnız `plannedEndDate` verilmediğinde koşar | düşük |
| L3 | `guarded-hard-remove.ts:74` (`makeGuardedHardRemove`) — guard sayımları tx DIŞINDA `prisma.*` (:62-72), silme tx içinde | kapsam dışı (route factory, K3b/K5) — check-then-act sınıfı, sızıntı değil | — |
| — | `tambur-plan-gate.assertRollMatchesPlan` (`prisma.color.findUnique` :77-79, `rollPlanDeviation.findFirst` :87, audit :137) | tasarım gereği **pre-tx** (yorum :56); Tambur çağıranları K3b | — |
| — | Belge builder'ları, `buildSnapshotEnvelope(tx)` :182 (`db.documentProfile.findUnique`), `traveler-card.buildSnapshot(client)` | sızıntı **yok** (doğrulandı) | — |

## 6. CHECK-THEN-ACT ADAYLARI (kilitsiz / claim'siz karar) — yargı değil, ② için hotspot

| # | Yer | Tx dışında okunan → tx içinde karar | Tx içinde taze mi? / DB seddi | Not |
|---|---|---|---|---|
| 1 | `workorder.service.ts:3271-3285` (`prepareFasonCancelDecision`, `softDelete` :3367'den) | `residual` AT_SUB/RETURNED toplar → **havuz `updateMany` ile IN_PRODUCTION'a çekiliyor, tx DIŞINDA**; ayrıca `cancelBulk` :3253 sonucu (`failed[]`) **okunmuyor** | Sonraki iptal tx'i (:3376) düşerse (claim 409, kapsam 400) bu yazım **geri sarılmaz** → sevki olmayan IN_PRODUCTION toplar | ⚠️ HOTSPOT |
| 2 | `workorder.service.ts:5602-5719` (`updateTargetProperties`) | `computeWorkOrderLocks(prisma)` :5627 (COMPLETED adımın kilitlediği özellik) → tx'te tekrar yok; **WO statüsü hiç okunmuyor** (`update`/`replace`'teki terminal guard'ı burada yok) | Sed yok; W kilidi yok | tamamlanmış/iptal WO'nun hedef özellikleri ve bağlı topların FLAG özellikleri yeniden yazılabilir |
| 3 | `workorder.service.ts:4412` (`attachRolls`) | WO statüsü (COMPLETED/CANCELLED/SUPERSEDED) pre-tx; tx içinde W yalnız sonda (:4566, sadece PLANNED'ı hedefler) | roll claim WO'ya bakmaz | eşzamanlı `completeWorkOrder`/`softDelete` (W-first) commit ederse toplar terminal WO'nun adımına IN_PRODUCTION bağlanır; parti doğar |
| 4 | `workorder.service.ts:5920` (`detachRolls`) | WO statüsü pre-tx | claim `currentStepId in stepIds` | terminal WO'dan detach (düşük) |
| 5 | `workorder.service.ts:3322`, :3854, :4288, :5930 | `stepIds` = pre-tx `existing.steps` | W claim adımı değil WO satırını kilitler; `replace` (:5486 step create) W claim'i ile serileşir ama pre-tx okuma kilit öncesi | claim'den önce commit eden `replace` yeni adım eklediyse o adımın topları sweep dışı kalır (dar pencere) |
| 6 | `subcontractor.service.ts:1963-1995` (`cancel`) | `acceptedReceiptItem` + `movedRolls` pre-tx; tx içinde roll claim `{AT_SUB, currentStepId}` | **kısmi kabul** (F3 :2879) topu AT_SUB bırakır → pencerede kabul edilen sevk iptal edilebilir; `dispatch.step.status` :2099 pre-tx | dar pencere; `receive` de W alır ama pre-check kilitten önce |
| 7 | `subcontractor.service.ts:4757-4775` (`cancelReceipt` LIFO guard) | "daha yeni aktif makbuz var mı" pre-tx; tx içinde tekrar yok | W kilidi altında yeni makbuz doğamaz, ama pre-check ile kilit arası pencere | kısmi teslimat metrajının çift geri konması (:4948 increment) senaryosu |
| 8 | `subcontractor.service.ts:2492`, :2664 (`receive`) | `step.status===ACTIVE`, `nextStep` pre-tx | W altında step tekrar okunmuyor (hedef renk tekrar okunuyor :2693) | `replace`/`manualMove` ile rota değişirse born toplar bayat `nextStep`'e doğar (dar) |
| 9 | `subcontractor.service.ts:2677-2678` (`receive` wBR predicate'siz) + :2350 clientToken pre-check | eşzamanlı aynı-token iki kabul: kaybeden `subcontractor_receipts_clientToken_key` P2002 → wBR 5 kez tx'i baştan koşturur (guard'lar tx dışı, koşmaz — :2852-2855 yorumu bunu roll claim için söylüyor) → sonunda "Barkod üretimi 5 denemede başarısız" 409 | `inventory.createInitialEntry` :955-1005 ve `shipping.openSack` :275-287 aynı durumu catch+replay ile çözüyor | idempotency replay kalitesi (beceri §8) |
| 9b | `order.service.ts:458-505` (`cancelOrderLine`) | preview `blockers` (özellikle `blockedNoTargetItem` :396) ve `affectedWoIds` pre-tx; tx'te WO type flip :500 **`targetItemId not null` guard'sız** (kardeş yollar :2838/:3248 guard'lı) | W kilidi yok (updateMany koşullu) | STOK değişmezi ("hedef kumaşsız STOK olamaz") bayat preview ile aşılabilir |
| 10 | `order.service.ts:2726`, :3081, :3300 (`getActiveShipmentLinks`) | PLANNED sevkiyat bağı pre-tx; tx'te tekrar yok | `createShipment` sipariş satırını kilitlemez (`shipmentOrder.createMany` :1292) | iptal edilmiş/kapatılmış siparişe PLANNED sevkiyat bağlı kalabilir → dispatch'te recompute CANCELLED siparişe shippedQty yazar (`recomputeOrderStatus` terminalde statüye dokunmaz :160-165) |
| 11 | `order.service.ts:2148-2176` (`quickOrderFromRolls`) | claim tx commit → `create()` ayrı; create düşerse toplar WAREHOUSE'da yetim (yorum :2134-2140 kabul) | — | kısmi durum (§7) |
| 12 | `inventory.service.ts:3742`, :3783 (`applyManualProperties`) | statü kapsamı (FREE_STOCK vs `roll:manual-adjust`) ve `rollWhole` pre-tx; claim :3932 yalnız `shipmentId/sackId` pin'ler | `attachRolls`/`dispatch` claim'leri statüyü değiştirir; Tambur kesimi `currentQty` düşürür (K3b) | (a) STOCK okunup IN_PRODUCTION'a geçmiş topa süpervizör yetkisi olmadan yazım; (b) `currentQty/initialQty` :3866-3868 kesilmiş topa yazılır → kesim metrajı ezilir |
| 13 | `inventory.service.ts:963-985` (`createInitialEntry` replay) | `existing` statüsü kontrol edilmiyor | — | iptal edilmiş/fire topun token replay'i `success:true` + o top (CLAUDE.md "iptal edilen clientToken replay EDİLEMEZ" kuralı `tambur-manual` için yazılmış; burada yok) |
| 14 | `inventory.service.ts:3422-3446` (`restoreCancelledRoll`) | sinyaller pre-tx, claim `{id, CANCELLED}` | CANCELLED top hareket kazanamaz → düşük | belgeli (:3382-3395) |
| 15 | `return.service.ts:879` (`cancelReturn`) | sevkiyat statüsü **hiç** okunmuyor; A23 alınmıyor | `undoDispatch` A23 + H claim alır; iade edilmiş top (WAREHOUSE) undo'nun R updateMany'sine girmez → çakışma yok → **iki tx de commit** | sonuç: `Roll.status=SHIPPED, shipmentId=X` iken `Shipment X` PLANNED/CANCELLED (releaseSacks) — ⚠️ HOTSPOT |
| 16 | `subcontractor-management.service.ts:512-526`, :635-663 | katlanmış kod / ad / VKN tekilliği havuzdan pre-tx, kilitsiz | DB: `subcontractors_code_key` (tam eşleşme ✓), `subcontractors_nameFold_key` (partial, yumuşak kapı; sahada mevcut ✓), VKN yok (bilinçli), **katlanmış kod yok** | `code-unique.helper.ts:63-69` bu yarışı `lockCodeScopeTx` (8026) ile kapatmayı tarif ediyor; kilit yalnız `item.service.ts:237`'de |
| 17 | `shipping.service.ts:1895-1903` (`dispatchShipment`) | boş sevkiyat + ihracat tartı pre-tx | H claim :1812 sonrası tekrar yok; `removeSackFromShipment` (H touch) araya girebilir (pre-check ile claim arası) | boş DISPATCHED sevkiyat (dar) |
| 18 | `shipping.service.ts:515/:563` (`scanIntoSack` taşı) | kaynak çuval kilitlenmez; `splitSack` :834-835 ve `removeSack` :1010 "kilit altında taze sayım"a güveniyor | roll claim `sackId:from` ✓ (tutarlılık bozulmaz) | `splitSack.moved < claimable` sessiz; `moveRollsToSack` `moved` sayısı |
| 19 | `workorder-link.service.ts:279-342` (`linkOrderLines`) | `assertPlanEditable`, kumaş/renk uyumu, iptal kalem pre-tx; tx'te type flip statü guard'sız | link PK seddi ✓ | CANCELLED WO'ya bağ + `type` flip (WO iptali W claim'i alır ama bu yol W almaz) |
| 20 | `workorder-link.service.ts:445` (`unlinkOrderLine`) | `!wo.targetItemId` pre-tx; flip :463 guard'sız | — | STOK değişmezi (9b ile aynı sınıf) |
| 21 | `workorder-split.service.ts:231` (`loadSplitContext(prisma)`) | `allowedModes`, `colorStep`, `redyeEligible`, WO statüsü pre-tx; P1/P2 W almaz | roll claim `batchId+status+sack/shipment` ✓ | CANCELLED/SUPERSEDED kaynak WO'da tebdil (W2 W claim'i ile yarış) |
| 22 | `workorder-manual-move.service.ts:538-583` | `loadContext(prisma)`: adımlar, `colorStep`, `woStatus` (terminal — dirilmez, güvenli), `isWholeParty` pre-tx | roll claim ✓; `join` tx-içi taze ✓ | rota değişimi (`replace`) ile `laterStepIds`/backflush kümesi bayat olabilir (dar) |
| 23 | `order.service.ts:3411` (`reopen`), :2537 (`update`) | `recomputeOrderStatus` **tam-küme `touchOrderLinesTx` olmadan** (helper protokolü :203-211) | READ COMMITTED; eşzamanlı dispatch recompute'u ile lost-update penceresi | `shippedQty` denormu bayat yazılabilir (S21 ∥ O7) |

## 7. Tx-DIŞI ÇOK ADIMLI ORKESTRASYONLAR (kısmi durum sınıfı — belgeli/belgesiz)

| Yer | Zincir | Telafi / rapor | Belgeli mi |
|---|---|---|---|
| `workorder.service.ts:1209` `quickStart` | create tx → attach tx → (0 ise hardDelete tx) → dispatch tx | hardDelete telafi + `console.error` :1366; dispatch hatası `dispatchWarning` | ✓ (:1204-1207, :1390-1393) |
| `workorder.service.ts:3367` `softDelete` → `prepareFasonCancelDecision` | cancelBulk (N tx) + `prisma.roll.updateMany` :3281 → iptal tx | **yok**; `failed[]` okunmuyor | ✗ → §6 #1 |
| `order.service.ts:3129-3135` `cancelWithActions` | WO softDelete (N tx) → sipariş tx | **yok** | ✗ (yorum yalnız "dynamic import" gerekçesi) |
| `order.service.ts:2147-2176` `quickOrderFromRolls` | claim tx → `create()` tx | yok; yorum kabul ediyor | ✓ (:2134-2140) |
| `subcontractor.service.ts:1591` `transferToNextFason` | receive (firma başına tx) → bulkDispatchStep → dispatch tx | yok; "planlamacı Sevk Et ile tamamlar" | ✓ (:1589-1590, :1736) |
| `subcontractor.service.ts:1539-1554` `bulkDispatchStep` SEPARATE | parti başına `dispatch()` tx döngüsü | **ilk hata fırlar**, önceki sevkler commit'li ve yanıtta yok (`failed[]` yok — `cancelBulk` :2183'ün aksine) | ✗ |
| `subcontractor.service.ts:3268-3310` `receive` sonrası | `changeWidth` / `changeTargetColor` best-effort | try/catch + `warnings` | ✓ |
| `workorder-link.service.ts:863` `linkOrderLineWithOverride` | changeTargetColor → changeWidth → applyAttributeToRolls (N tx) → linkOrderLines | `rollsFailed[]` | ✓ (:852-857) |
| `workorder-link.service.ts:564-577`, :632-640 | claim (havuz) → `markTravelerCardDirtyTx(prisma)` | yok (yön: fazla işaretlemek güvenli) | ✗ (küçük) |

## HOTSPOTLAR

② denetçilerinin öncelikle bakması gereken yerler (öncelik sırasıyla; hepsi §2-§7'de kanıtlı):

1. **`Teks-Erp/src/services/workorder.service.ts:3253-3285` (`prepareFasonCancelDecision`)** — iptal tx'inden ÖNCE havuz client'ıyla `roll.updateMany` (AT_SUB/RETURNED → IN_PRODUCTION) + `cancelBulk` sonucu yutuluyor; ardından gelen tx :3376 düşerse (claim 409 / kapsam 400 / `FASON_DECISION_REQUIRED` 409) yazım kalıcı. failure_mode adayı: "fason kararı verildi, WO iptali 409 aldı; toplar sevksiz IN_PRODUCTION kaldı, fason ekranından kayboldu".
2. **`workorder.service.ts:4444-4570` (`attachRolls`) ∥ `subcontractor.service.ts:1036-1099` (`dispatch`)** — ABBA-1: roll-first (R → A22 → W) ile WO-first (W → R → A22) aileleri; A22 tek anahtarlı sistem geneli kilit. Aynı WO + aynı STOCK top senaryosu (Hızlı İş Emri + mobil sevk) → deadlock → P2034 → 409 (retry yok). Aynı aile: `workorder-manual-move.service.ts:596`, `workorder-split.service.ts:336/:454`.
3. **`order.service.ts:475-514` (`cancelOrderLine`)** — `order-status.helper.ts:205-209` protokolünün ihlali (iki-parti OrderLine edinimi: tek claim :478 → tam küme :512) ∥ `shipping.service.ts:1878` / `:1975` / `:2210` / `subcontractor.service.ts:6233` (tam küme sıralı). Ek: :500 type flip `targetItemId` guard'sız.
4. **`return.service.ts:879-935` (`cancelReturn`)** — A23 alınmıyor, sevkiyat statüsü doğrulanmıyor; `undoDispatch` (:2152) ile eşzamanlı çalışınca `SHIPPED` top ↔ `PLANNED/CANCELLED` sevkiyat tutarsızlığı (iki tx de commit eder; satır çakışması yok).
5. **`order.service.ts:3392-3413` (`reopen`) ve :2537 (`update`)** — `recomputeOrderStatus` çağrısı tam-küme `touchOrderLinesTx` olmadan → `shippedQty` lost-update penceresi (helper docstring'inin açık yükümlülüğü).
6. **`workorder.service.ts:5602-5719` (`updateTargetProperties`)** — kilit yok, claim yok, WO terminal-statü guard'ı yok; `computeWorkOrderLocks(prisma)` tek sefer pre-tx.
7. **`inventory.service.ts:3916-3956` (`applyManualProperties`)** — claim statü ve `currentQty` pin'lemiyor; `currentQty/initialQty` :3866-3868 yazımı kesim (K3b: Tambur) ile yarışırsa kesim metrajı ezilir; statü kapsamı (süpervizör yetkisi) bayat statüyle çözülüyor.
8. **`inventory.service.ts:963-985` (`createInitialEntry` clientToken replay)** — `existing.status` kontrolsüz; CANCELLED/SCRAP topun token replay'i `success:true` döner (beceri §8 "en pahalı hata" sınıfı; mobil retry penceresi kısa, Electron iptali ile kesişim ② ölçsün).
9. **`subcontractor.service.ts:2677-2678` (`receive` wBR predicate'siz)** — eşzamanlı aynı-clientToken → P2002 5× retry → yanıltıcı "Barkod üretimi başarısız" 409; kardeş yollar (`inventory.service.ts:955`, `shipping.service.ts:275`) predicate/catch ile ayrışmış.
10. **`subcontractor-management.service.ts:512-526`** — katlanmış kod tekilliği kilitsiz (8026 `lockCodeScopeTx` yalnız `item.service.ts:237`); DB seddi tam eşleşme (`subcontractors_code_key`); `code-unique.helper.ts:63-69` yarışı adıyla tarif ediyor.
11. **`workorder.service.ts:989` + :1002/:5505** — tx içinde havuz client'ı (`generateWorkOrderNumber`, `readWorkOrderDefaultPlanDurationDays`); `workorder-clone.helper.ts:31` tx-alan ikizi mevcut ve kullanılmıyor. Havuz doygunluğu ölçümü emsali `roll-barcode.helper.ts:56-64`.
12. **`shipping.service.ts:690-696` / :769-777** — iki çuval kilidi id-sırasız (ABBA-2); **:1412-1418 / :1551-1555** — çuval claim'leri istemci sırasıyla (ABBA-3).
13. **`subcontractor.service.ts:1539-1554` (`bulkDispatchStep` SEPARATE)** — döngü ortasında hata: önceki sevkler commit'li, yanıtta yok (`cancelBulk` :2183 `failed[]` deseninin aksine) — "sessiz kısmi" (beceri §2).
14. **`subcontractor.service.ts:4757-4775` (`cancelReceipt` LIFO)** — guard tx dışı; kısmi-teslimat increment'i (:4948) çift geri koyma senaryosu (dar pencere).
15. **`shipping.service.ts:515-523`, :563-568 (`scanIntoSack` taşı)** — kaynak çuval kilitlenmiyor; `splitSack` :834-857 "kilit altında taze sayım" varsayımı ve `moveRollsToSack` :772 `moved` sayısı (count kontrolü yok) bununla kesişiyor.
16. **`order.service.ts:2726`, :3081** — `getActiveShipmentLinks` pre-tx; `createShipment`/`addSacksToShipment` sipariş satırını kilitlemez → iptal edilmiş siparişe PLANNED sevkiyat + dispatch'te recompute.
17. **`shipping.service.ts:1895-1903` (`dispatchShipment`)** — boş sevkiyat / ihracat tartı guard'ı yalnız pre-tx (`removeSackFromShipment` ile pencere).
18. **`workorder-link.service.ts:375-389`, :453-471** — W kilidi yok; `type` flip'leri statü/`targetItemId` guard'sız (kardeş yollar `order.service.ts:2833-2841`, :3242-3251 guard'lı) — "STOK'un değişmezi" iki kapıdan biri açık.
19. **`workorder.service.ts:3322 / :3854 / :4288 / :5930`** — `stepIds` pre-tx; `replace` :5486 ile dar pencere (② ölçüp gerekirse `bilgi` yazsın).

## SINIR ÖTESİ NOTLAR

- **K3b (Tambur/kursun-qc/kartela/batch tx envanteri):** `kursun-qc.service.ts:613`, :812 `assertKursunTabletMayWrite(prisma, …)` tx-DIŞI çağrı (aynı helper inventory'de tx ile). `batch.service.ts:373` (`moveRolls`), :869 (`mergeBatches`) wBR+tx, `touchWorkOrderTx` alıyor (:384/:880) — sıra ailesi "WO-first"; `performDispatchSurgeryTx` (`batch-dispatch-surgery.helper.ts:116`) okunmadı. Tambur `cutOpenFabric`/`finalize` yollarının **W kilidi alıp almadığı** bu kapsamdaki üç hotspot'u (#7 `applyManualProperties` metraj yarışı, F5 `cancelReceipt` born-roll koşulsuz updateMany :4846, `createFasonShipChild`) doğrudan etkiler — K3b ölçsün. `printed-document.service.ts:357-420` (`getCurrent` lazy-init, `findFirst` :364 + `create` :387 + P2002 `winner` :417) tx envanterine girmeli. `traveler-card.service.ts:965` `buildSnapshot(prisma, …)` çağrısı (havuz) ve `print()` :208-215 kendi tx'i.
- **K2 (durum makinesi / claim sınıflandırma):** `RollStatus` geçişlerinin claim'leri bu haritada satır satır işaretli; `Roll.status` yazan yabancı servisler (order.quickOrderFromRolls :2149 STOCK→WAREHOUSE, return :501 SHIPPED→raf, shipping dispatch :1862 →SHIPPED) beceri §7.2 "bounded context" sorusu için liste burada.
- **K5 (route/controller katmanı):** `guarded-hard-remove.ts` route-factory'si tx açıyor (:74) ve `AuditService.log` :78 — servis katmanı dışında tx; `express` handler içinde. `machineHardRemove` :256 deleteTx :261-268 `peripheralDevice.updateMany` + `workSession.deleteMany` + `machine.delete`.
- **K4 (idempotency/istemci sözleşmesi):** `receive` clientToken (hotspot #9), `createInitialEntry` replay statüsü (#8), `openSack` :275-295 ve `createShipment` :1432-1444 predicate+catch çifti (doğru desen), `dispatch` :742 küme-eşitliği guard'ı (token yok), `createOpenFabric` :4221 replay.
- **K6 (raw SQL):** bu kapsamda `$executeRaw` movement kapatma şablonları (`workorder.service.ts:3531/:4038/:4330/:6032`, `subcontractor.service.ts:2071/:2828/:3455/:6093/:6185`, `roll-disposition.helper.ts:378`, `workorder-batch-drop.service.ts:439`) — 10 yazma noktasında `now() AT TIME ZONE 'UTC'` (CLAUDE.md O-11 "kırılgan taraf"), batch-drop ve disposition `-- tz-ok` çıplak `now()`. `order.service.ts:2434` `FOR UPDATE` `= ANY(array)`.
- **K1/K2 veri ölçümü:** sahada `roll_barcode_counters` 38 satır; günlük belge sayaçları max 12-21 → Q-sınıfı P2002 yarışı olasılığı düşük ama sıfır değil. `test_consistency` §13/§18/§20 kırmızıları (CLAUDE.md) bu haritadaki #5/#12 pencereleriyle ilişkilendirilebilir.
- **Bekçi sorusu (② için, beceri §4.2 bekçi kuralı):** A21/A22/A23 için "N paralel istek → tam 1 başarı" tipi bekçi var mı (`test_kk1_duplicate_guard`, `test_batch_number_format` "kilit silinince 4 kırmızı", `test_shipment_undo_dispatch`) — CLAUDE.md notları var diyor; ABBA-1/-4 için hiçbir bekçi adı geçmiyor.

## KAPSANMAYAN / ERİŞİLEMEYEN

- **Canlı prod'a erişim yok**; DB kanıtları 2026-08-25 kopyasından (190/195 migration). Deadlock/lock-wait istatistiği (`pg_stat_database.deadlocks`) kopyada anlamlı değil — ölçülmedi. `lib/prisma.ts:26` yorumu "624k transaction'da 0 deadlock" diyor (tarihsiz, kaynağı doğrulanamadı).
- **Zamanlama ölçümü yapılmadı** (kilit süresi, tx süresi, havuz doygunluğu): salt-okunur keşif; `roll-barcode.helper.ts:56-64`'teki ölçüm alıntılandı, tekrar edilmedi.
- **Bekçi scriptleri okunmadı** (`scripts/test_*.ts`): kilit **sırasının** bekçili olup olmadığı (beceri §4.2) ölçülmedi — ② veya bekçi-denetim ajanına devredildi.
- **Okunmayan gövdeler (kapsam dışı ama tx kapsamını etkiler):** `traveler-card.service.buildPlan/resolveForPrint` (client aktarımı [VARSAYIM]), `batch.service.moveRolls/mergeBatches/splitBatch` (K3b), `batch-dispatch-surgery.helper`, `station-capability-transfer.copyStationCapabilitiesToRoll` (tx alır, gövde okunmadı), `coverage.helper`, `kursun-bypass-guard.assertStepNotBypassAssigned/findPendingBypassAssignmentTx` gövdeleri, `printed-document.service` builder kayıt defteri (`requireBuilder` :171), `subcontractor.service.getCancelPreview/getUndoTransferPreview/previewDirectShip` (salt-okuma), `shipping.service.setShipmentInvoice/setDirectShipmentInvoice/setSackNotes` (tx'siz claim'ler — okunmadı), `order.service.create` (tx yok, BaseService — `orderNumber` üretimi `base.service.ts:1031` wBR; kapsam K3b/K5), `inventory.service.getRelabelContext/getRollHistory` (okuma).
- **`Promise.all` tx-içi ihlali için ESLint kuralı** (`eslint.config.mjs`) okunmadı; gövde okumasında 0 ihlal bulundu, kuralın kapsamı doğrulanmadı.
- **Prisma `updateManyAndReturn` / driver adapter davranışı** (tek bağlantıda `Promise.all` "hard error" iddiası) çalıştırılarak doğrulanmadı; beceri referansına dayanıldı.
- Satır numaraları çalışma ağacından; `git status` bu dosyalarda değişiklik göstermiyor (değişen dosyalar Electron/mobil/deploy) — HEAD ile aynı [VARSAYIM: `Teks-Erp/src` staged/unstaged farkı yok, `git status` çıktısında listelenmedi].
