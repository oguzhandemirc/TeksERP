# Tur 1 çürütücülerinin YENİ BULGU önerileri (17) — sonraki turların denetçileri için tohum

Bunlar bulgu DEĞİL, çürütme sırasında görülen ve ayrı kök nedeni olan adaylardır; ilgili tur denetçisi kanıtlayıp bulguya çevirir ya da eler.

## Kaynak: BULGU-T1-007 (topoloji merceği)

Kanıt hijyeni (kapsam dışı, A/K alanına): prod kopyasında 2026-08-25 13:23-13:54 arası ad-mükerrer bekçi fixture'larının audit izi var (system_logs son satır 13:54:59, son GERÇEK top 2026-08-24 22:11) ve `machines` tablosunda 1 adet 'TEST…' satırı temizlenmeden kalmış. Bu satırlar, ardışık-yaratım ölçümü yapan HER denetçiyi yanıltır (benim ilk ölçümümde 4 ms'lik 'gerçek eşzamanlılık' gibi göründü).

## Kaynak: BULGU-T1-013 (kod merceği)

AYRI ve daha ağır bir bulgu ortaya çıktı: `admin:users` tek başına KENDİNE `admin:*` verebiliyor — self-target guard YOK. Kanıt: `Teks-Erp/src/routes/admin.routes.ts:406-410` (`PUT /users/:id/permissions`, tek guard `requirePermission("admin:users")`) ve `Teks-Erp/src/services/permission-management.service.ts:248-330` — `setUserPermissions` yalnız kullanıcının varlığını ve izin id'lerinin geçerliliğini doğruluyor; `actorUserId === userId` kontrolü hiç yok, son-admin koruması (`:299-307`, `acquireAdminGuardLock` + `assertAdminCoverageAfterChange`) sadece `removesAdmin` dalında koşuyor. Aynı desen `POST /users/:id/apply-template` (`admin.routes.ts:584-592`) için de geçerli. failure_mode: sahadaki En*** hesabı (`admin:users` + `admin:settings`, `admin:*` YOK — saha kopyasında ölçüldü) kendi kullanıcı id'siyle bu ucu çağırıp `admin:*` alır; yetki modelinin tanımladığı "sınırlı yönetici" kademesi fiilen mevcut değildir ve bu, hem BULGU-T1-013'ün hem BULGU-T1-014'ün etki hesabını yukarı çeker. Şiddet önerisi: S1 (K2 — saha verisiyle böyle bir hesabın varlığı doğrulandı).

## Kaynak: BULGU-T1-017 (kod merceği)

Yok — yeni ve daha kötü bir şey bulunmadı; aksine kapsam bir miktar daha iyi çıktı (sevkiyat tarafında test_shipment_scope_lock.ts:148 gerçek bir 8023 sondası mevcut, yani 'sevkiyatta hiç sonda yok' cümlesi tam doğru değil: eksik olan createShipment/dispatchShipment sondaları).

## Kaynak: BULGU-T1-020 (kod merceği)

Panel offsite YEREL hedefi hiçbir zaman etkili olmuyor (sessiz): src/routes/admin.routes.ts:1420-1424 `PATCH /api/admin/backups/offsite` `body.localDir`'i `SETTING_KEYS.BACKUP_OFFSITE_DIR` (system_settings `backup.offsiteDir`) olarak yazar ve :1428 `readOffsiteDir()` ile geri okuyup ekrana ayarlanmış gösterir; system-setting.service.ts:3226-3228 SystemSetting→env önceliğini uygular. Ancak yedeği alan kod bu ayarı HİÇ okumaz: backup.service.ts:46 `const OFFSITE_DIR = process.env.BACKUP_OFFSITE_DIR;` (modül yükleme anında) ve :324 `if (OFFSITE_DIR)`. failure_mode: admin panelden "Offsite yerel hedef = \\NAS\yedek" kaydeder, panel değeri onaylar; her yedek yine de "OFFSITE YEDEK AYARLANMADI" uyarısıyla biter ve ikinci kopya HİÇ oluşmaz — kullanıcı korumanın açık olduğunu sanır. (readOffsiteRemote/rclone tarafı SystemSetting'i okur; asimetri yalnız yerel dizinde.)

## Kaynak: BULGU-T1-020 (topoloji merceği)

YENİ (aynı komşuluk, ayrı kök): Panelden girilen YEREL offsite hedefi hiçbir yedeğe uygulanmıyor. `routes/admin.routes.ts:1419-1425` `PATCH /api/admin/backups/offsite` `localDir`'i `SystemSetting backup.offsiteDir`'e yazar, `:1370` `GET` aynı değeri `readOffsiteDir()` ile geri okuyup panelde 'yapılandırıldı' diye gösterir; ama yedeği fiilen kopyalayan yol `services/backup.service.ts:46` `const OFFSITE_DIR = process.env.BACKUP_OFFSITE_DIR` (MODÜL YÜKÜNDE, `readOffsiteDir()` HİÇ çağrılmıyor — `grep -rn 'readOffsiteDir' Teks-Erp/src` yalnız `admin.routes.ts:1370,1428` ve tanım satırını veriyor). failure_mode: admin panelden `E:/yedek` girer, ekran onu gösterir, `backup.service.ts:324` `if (OFFSITE_DIR)` FALSE kalır → ikinci kopya HİÇ alınmaz; tek iz yedek mesajının sonuna eklenen uyarı metnidir ve panelin offsite kartı onu göstermez. Aynı uçtaki `remote` alanı DOĞRU çalışıyor (`sweepOffsiteBackups` `readOffsiteRemote()` çağırır, `offsite-backup.helper.ts:162`) — yani iki kardeş alan aynı uçtan yazılıp FARKLI kaynaklardan okunuyor. Şiddet önerisi S2 (sessiz, yanlış güvence üretir).

## Kaynak: BULGU-T1-021 (db merceği)

YENİ (aynı modül, ayrı kök neden): elle tetiklenen yedeğin İZİ KAYBOLABİLİYOR — saha'da 9 `BACKUP_TRIGGER {"started":true}` kaydına karşılık yalnız 7 manual `BACKUP_COMPLETED` var; 2026-07-16 13:24:03 ve 2026-07-31 21:19:15 tetiklemelerinin NE COMPLETED NE FAILED terminal kaydı var (log penceresi bu tarihleri kapsıyor: 07-16 12:50→08-25 13:54, arşiv 0 satır). Mekanizma: backup.service.ts:395-413 `triggerManualBackup` fire-and-forget (`void runBackupJob("manual")`, HTTP 202) + :190-196 audit'in best-effort (`void AuditService.logEvent`) olması → pm2 restart / süreç ölümü / audit yazım hatası durumunda kullanıcı 'Yedek başlatıldı' mesajını alır, panelde 'son yedek' güncellenmez ve HİÇBİR yerde başarısızlık izi kalmaz. failure_mode: yönetici migration öncesi elle yedek alır, 202 görür, işleme devam eder; dump hiç oluşmamıştır ve bunu söyleyen tek şey 'son yedek' tarihinin değişmemesidir. Şiddet önerisi S3 (gözlemlenebilirlik), kanıt K2 (2/9 tetikleme izsiz).

## Kaynak: BULGU-T1-021 (topoloji merceği)

[YENİ, DAHA KÖTÜ] Bir sonraki paketli kurulum, sahadaki gece yedeğini ve makine-dışı kopyayı SESSİZCE KAPATIR. `deploy/paketle.ps1:129` repo'daki `Teks-Erp/ecosystem.config.js`'i paket sahnesine kopyalar; `deploy/kur.ps1:132` onu ZORUNLU paket içeriği sayar, `:190` `pm2 delete` ile uygulamayı siler ('ecosystem env blogu degismis olabilir') ve `:303` paketten gelen ecosystem ile yeniden başlatır. Deploy'da KORUNAN tek dosya `.env`'dir (`kur.ps1:161-168` — yalnız `.env` kenara alınıp geri konur), `ecosystem.config.js` DEĞİL. Repo'nun her iki dalında da (`git show adnansahin:Teks-Erp/ecosystem.config.js` → `:107 BACKUP_SCHEDULE_ENABLED:"false"`, `:124 BACKUP_OFFSITE_DIR:""`; main'de `:92`/`:109` aynı) bu iki değer kapalıdır, oysa prod 2026-08-25 gecesi ikisi de AÇIK koşmuştur (yukarıdaki `trigger=nightly` + offsite uyarısının yokluğu) — yani sunucudaki dosya elle düzenlenmiş ve repo ile AYRIŞMIŞTIR. failure_mode: bir sonraki `kur.ps1` koşumundan sonra `startBackupScheduler()` `BACKUP_SCHEDULE_ENABLED==="false"` dalına düşüp (`jobs/backup-scheduler.ts:122-129`) yalnız bir konsol satırı basar, `OFFSITE_DIR` (`backup.service.ts:46`, modül yüklemesinde okunur) boşalır; gece yedeği ve makine dışı kopya durur, `BACKUP_FAILED` audit'i DOĞMAZ (iş hiç başlamaz), `backup.lastNightlyAt` donar ve tek belirti panelin bayatlık rozetidir. `.env`'e yazarak telafi de EDİLEMEZ: pm2 `env` bloğu process env'i doldurur, `dotenv.config()` (`lib/prisma.ts:14`) mevcut değişkeni ezmez. Dosya:satır kanıtı: `deploy/kur.ps1:132,161-168,190,303` · `deploy/paketle.ps1:129` · `Teks-Erp/ecosystem.config.js:107,124` · `Teks-Erp/src/jobs/backup-scheduler.ts:122-129` · `Teks-Erp/src/services/backup.service.ts:46,334-338`. Önerilen şiddet S1 (sessiz + alarmsız + tek koruma katmanının tamamını kaldırıyor).

## Kaynak: BULGU-T1-029 (kod merceği)

`Teks-Erp/src/services/workorder.service.ts:5700-5713` — `updateTargetProperties` topların FLAG `RollProperty` kümesini koşulsuz siler ve yalnız WO hedef listesinden yeniden yazar; oysa istasyonlar (`station-capability-transfer.helper.ts:186-205`, AUTO/operatör seçimi) hedef listesinde olmayan FLAG özellikleri de topa yazıyor. failure_mode: planlamacı bir iş emrinin hedef özelliklerine dokunur (listeyi hiç değiştirmese bile) → o iş emrinin TARGET_PROP_MUTABLE_STATUSES'taki tüm toplarından istasyon kaynaklı FLAG satırları silinir; top etikette/raporda o özelliği taşımaz ve `OrderLineRequiredProperty` eşleşmesinden düşer, sipariş için sevk edilemez sayılır. Kanıt: prod kopyasında 1149 FLAG roll_properties satırı bu tanıma uyuyor (2256 satırın yarısı). Kod yorumu (:5698-5703) 'FLAG evreni = WO hedeflerinin kopyası' varsayımını yazılı olarak savunuyor — ama veri o varsayımı çürütüyor; aynı yorumun CHOICE için verdiği karar (silmeme) tam olarak bu sınıfın daha önce bir kez ısırdığını gösteriyor.

## Kaynak: BULGU-T1-032 (topoloji merceği)

Ayrı kusur (bu bulgunun kapsamı değil): scripts/backfill_roll_production_timestamps.ts sahada hiç koşturulmamış — saha kopyasında WAREHOUSE 3/250 + SCRAP 1/1 + A1_STOCK toplarının finalizedAt'i NULL ve bu toplar HİÇBİR dönemin Kalite/Fire Karnesi'nde görünmüyor. failure_mode: 2026-08-08'de fire edilen 300 m'lik bc74b77d hiçbir dönemde raporlanmıyor; yalnız Kalite Karnesi'nin 'çıpasız top' bandında sayı olarak beliriyor, Fire Karnesi'nde o band bile yok (scrap-scorecard.report.service.ts'de unanchored sorgusu yok).

## Kaynak: BULGU-T1-036 (topoloji merceği)

YENİ (bağımsız tetikleyicisi olan, canlıda ÖLÇÜLEN kusur): `pickDefaultAction` CANCELLED WO durumunu ele almıyor → Teks-Erp/src/services/order.service.ts:118-124 (allowedActions ile ayrışan defaultAction), yüzeyi Electron/src/pages/Operations/Orders/OrderCancelDialog.tsx:350-361 (tek-aksiyonlu satırda seçim kontrolü çizilmiyor). failure_mode: açık bir siparişe bağlı TEK-sipariş WO'su herhangi bir yoldan (WO ekranından doğrudan iptal dahil, sipariş iptali kısmi düşmesi ŞART DEĞİL) CANCELLED olduğunda, o siparişin iptal diyaloğu preview'daki defaultAction=CONVERT_TO_STOCK'u gönderir, sunucu 400 verir ve kullanıcının seçimi değiştirmesi için arayüzde kontrol YOKTUR → sipariş kalıcı olarak iptal edilemez. K2 ölçümü (sql-saha.sh, prod kopyası 2026-08-25): SIP2408260016 (IE2408260015, IE2408260016) ve SIP0108260012 (IE0108260002) — üç çiftin de bağlı sipariş sayısı 1, yani BUGÜN iki canlı sipariş bu durumda. Önerilen şiddet S2.

## Kaynak: BULGU-T1-037 (db merceği)

K11 çoklu-parti stratejisinin TAMAMI API'den ölü ve 409 MULTI_BATCH bir ÇIKMAZ. Ne `dispatchSchema` (controllers/subcontractor.controller.ts:10-24) ne `bulkDispatchSchema` (:27-38) `multiBatchStrategy` alanını taşıyor; Electron/mobil'de `MULTI_BATCH` ve `multiBatchStrategy` grep'i 0. Buna karşılık subcontractor.service.ts:1011-1020 seçim 2+ partiye yayılınca 409 `MULTI_BATCH` fırlatıyor ve :950'deki yorum 'Electron birleştir/ayır seçtirir' diyerek var olmayan bir arayüzü tarif ediyor → operatör 2+ partili seçimde ne MERGE ne SEPARATE gönderebilir, hatadan çıkış yolu yok (tek çare topları tek tek/parti parti elle ayıklamak). failure_mode: iki partiden top içeren bir fason adımında 'Sevk Et' → 409 'MULTI_BATCH' + parti seçici verisi, hiçbir istemci bu kodu tanımıyor → sevk yapılamaz. Ölçüm (saha, kendi sorgum): SUBCONTRACTOR istasyonlu adımlarda 2+ farklı batchId taşıyan adım sayısı bekleyen 0, tarihsel 0; 2+ partili WO sayısı 1 → bugün sahada tetiklenmemiş, bu yüzden S3 (ölü özellik + çıkmaz hata yolu), S2 değil. Kanıt: controller:10-38, service:950/1011-1020/1525-1560, grep 0 (Electron/src, mobil/src).

## Kaynak: BULGU-T1-043 (db merceği)

Bulgunun 'tek savunma hattı audit ve o da kurcalanabilir' ayağı SAHA KOPYASINDAN DOĞRULANAMIYOR (çürütme değil, kanıt boşluğu): `pg_db_role_setting` listesinde restore edilmiş `tekserp_saha_0825` için hiç setconfig satırı yok ve `current_setting('teks.audit_guard',true)` boş dönüyor — trigger'lar mevcut ve etkin (tgenabled='O') ama prod DB'sinde `ALTER DATABASE … SET teks.audit_guard='on'` yapılıp yapılmadığı bu kopyadan ölçülemez. Bulguyu yazarken 'varsayılan KAPALI' kod-doğru, 'prod'da kapalı' ise [VARSAYIM] olarak işaretlenmeli.

## Kaynak: BULGU-T1-054 (topoloji merceği)

`Teks-Erp/src/routes/admin.routes.ts:406-409` — `PUT /api/admin/users/:id/permissions` tek guard'ı `requirePermission("admin:users")`; `admin:users` taşıyan bir kullanıcı KENDİNE `admin:*` dahil her izni yazabiliyor (kendi-kaydı kısıtı yok; son-admin kilidi yalnız SON admin'i düşürmeyi engelliyor, yükseltmeyi değil). failure_mode: En*** (admin:users+admin:settings, admin:* YOK) tek istekle kendi izin listesine `admin:*` ekler ve tüm sistemi devralır — audit satırı (USER_PERMISSION_SET) yazılır ama işlem hiçbir ikinci onaydan geçmez. Bu, BULGU-T1-043 ve BULGU-T1-013'ün ortak kök nedeni: `admin:users` fiilen sınırsız yetkidir, bu yüzden o iznin bulunduğu hesaplarda SoD/kimlik gizliliği tartışması anlamsızlaşır.

## Kaynak: BULGU-T1-072 (kod merceği)

Aynı dosyada AYRI ve gerçek bir kusur var (bulgunun anlattığı yön TERSİ): `quality-scorecard.report.service.ts:176` ve `scrap-scorecard.report.service.ts:199-205` metrajı `SUM(r."currentQty")` ile, yani topun BUGÜNKÜ metrajıyla okuyor; damga ise finalize ANINI gösteriyor. Bu yüzden kapanmış bir dönemin rakamı sonradan DÜŞEBİLİR: (i) Temmuz'da 445 m 1. kalite damgalanan top fasona gidip kabul edilince orijinal top `SUBCONTRACTOR_CONSUMED` (K18_DEAD, `batch.service.ts:55-60`) olur → Temmuz'un üretimi sessizce 445 m azalır, mal ise yeni barkodla Ağustos'a yazılır; (ii) depoda kesilen/metrajı düzeltilen top (`inventory.service.ts:3865-3869` `rollData.currentQty = m; rollData.initialQty = m;`) kapanmış ayın metrajını geriye dönük değiştirir. failure_mode: Temmuz karnesi ay sonunda 55.275 m basar, üç hafta sonra aynı aralıkla açıldığında 54.830 m basar; fark hiçbir yerde loglanmaz ve hiçbir bekçi dönem-kararlılığını ölçmez. Bu, BULGU-T1-032'nin "statü değişmeyen yazımda trigger hiç koşmaz" boşluğuyla aynı sınıftan ama ayrı bir tezahürdür.

## Kaynak: BULGU-T1-074 (db merceği)

DB SEDDİ SETİ DEV ↔ PROD ARASINDA İKİ YÖNDE AYRIŞIYOR — bekçi hangi kısıta karşı koştuğunu bilmiyor. `prisma/migrations/20260821150000_name_fold_unique_live/migration.sql:58-74` (ve renk ikizi `20260825120000_color_name_unique_live/migration.sql:73-74`) YUMUŞAK KAPI: mükerrer varsa index NOTICE ile ATLANIR. Canlı ölçüm (pg_indexes): SAHA'da `items_nameFold_key` YOK (items'ta 1 mükerrer nameFold grubu duruyor → sed hiç kurulmadı) ama `colors_nameFoldColor_key` VAR; DEV'de tam TERSİ — `items_nameFold_key` VAR, `colors_nameFoldColor_key` YOK (dev colors'ta 1 mükerrer tr_fold_color grubu). failure_mode: mükerrer ad seddine dayanan her bekçi/servis yolu (ad çakışması yarışı, MasterDataMergeService, içe aktarım) dev'de kumaş için DB tarafından korunur ve renk için korunmaz; prod'da tam tersi. Yani 'dev'de yeşil' bir yarış bekçisi prod'da fiilen korumasız bir kod yolunu ölçmüş olur ve bunu hiçbir yerde söylemez. Kanıt: audit/tools/sql-saha.sh + sql-dev.sh pg_indexes karşılaştırması (yukarıdaki dört index adı) + saha items mükerrer grup=1, dev colors mükerrer grup=1.

## Kaynak: BULGU-T1-106 (kod merceği)

Aynı dosyada, çürütme sırasında BAŞKA ve gerçek bir kusur çıktı (KYY-3-15 tarafından kısmen işaret edilmiş ama failure_mode'u yazılmamış): `recordPrintEvent` kartı KOŞULSUZ günceller ve okuma tx dışındadır — traveler-card.service.ts:350-363 (`findUnique` + `resolvePrintPlan`, ikisi de havuz) → :369-378 `tx.travelerCard.update({ where: { id: cardId }, data: { ..., version: plan.version } })`; WHERE'de beklenen sürüm YOK, atomik claim yok. Failure_mode: IE2108260004 kartı v2'de; planlamacı önce rengi, hemen ardından eni değiştiriyor; iki farklı istemci (Electron + tablet) kartı basıp ~aynı anda print-event gönderiyor. İkisi de `card.version=2` okur, ikisi de `revised=true` hesaplar → ikisi de `plan.version=3` yazar; `archivePrintedVersionTx` (:443-462) aynı v3 anahtarına upsert eder ve İKİNCİSİ BİRİNCİNİN İÇERİĞİNİ EZER (burada planKey eşitliği yok, gerçek içerik farkı var). Sonuç: sahaya iki farklı içerikte kâğıt gitmiş olur, defterde tek bir v3 durur, bir revizyon ve o kâğıdın kaydı sessizce kaybolur; sürüm sayacı da 4 yerine 3'te kalır. Kodun :340-347'deki 'BİLİNEN SINIR' yorumu YALNIZ 'HTML çekme ile basıldı deme ayrı isteklerdir' penceresini kabul ediyor, iki eşzamanlı print-event'in sürüm kaybını KAPSAMIYOR.

## Kaynak: BULGU-T1-165 (kod merceği)

`Teks-Erp/src/services/permission-management.service.ts:529,532` — `createUser` içinde `AuthService.setQuickPin(...).catch(() => undefined)` ve `AuthService.rotateCardToken(...).catch(() => undefined)`: mobil kimlik üretimi sessizce düşebilir. failure_mode: yönetici `generateMobileCredentials` ile kullanıcı açar, `quickPin` P2002 retry'si tükenirse ya da yazım düşerse kullanıcı PIN'siz/kartsız doğar, yanıt yine `success` ve oluşan kullanıcı satırı döner; operatör tablette giriş yapamaz ve panelde hiçbir uyarı yoktur (audit'te de yalnız users CREATE satırı var, kimlik üretimi ayrı iz bırakmıyor). Kasıtlı tasarımın (create'i düşürmemek) sessizlik bedeli ödenmemiş.

