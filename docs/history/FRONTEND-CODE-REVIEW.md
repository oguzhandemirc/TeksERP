# FRONTEND KOD İNCELEMESİ (Electron + Mobil) — 2026-06-13

**Yöntem:** 10 paralel kapsam ajanı (Electron: React Query, yıkıcı akışlar, yazdırma, sekme/modal, API, izinler · Mobil: offline, tarama akışları, API, UI) + her yüksek/orta bulguya 2 bağımsız şüpheci doğrulayıcı (ikisi de çürütemezse rapora girer). 84 ajan, ~5M token, ~1300 araç çağrısı. Backend kampanyalarıyla aynı disiplin: her bulgu kod kanıtlı (dosya:satır).

**Sonuç: 29 doğrulanmış bulgu** (12 yüksek, 17 orta) · 8 iddia doğrulamada çürütüldü · 26 düşük öncelikli not.

Durum işaretleri: ⬜ bekliyor · ✅ düzeltildi · ⏭️ bilinçli kabul

## DOĞRULANMIŞ BULGULAR

| # | Şiddet | Kapsam | Bulgu | Konum | Durum |
|---|---|---|---|---|---|
| F-1 | yüksek | el-query | Manuel top ekleme ölü queryKey'i invalidate ediyor — liste hiç tazelenmiyor | `Electron/src/pages/Operations/Rolls/ManualEntryDialog.tsx:84` | ✅ |
| F-2 | yüksek | el-query | rolls:<tab> string-suffix queryKey deseni — tüm top tabloları cross-invalidation'dan kopuk | `Electron/src/pages/Operations/Rolls/RollsTable.tsx:157` | ✅ |
| F-3 | yüksek | el-print | window.print() çıktısı layout zincirine hapsolmuş — uzun belge tek sayfaya kırpılır, dar/kaymış basılır | `Electron/src/index.css:424` | ✅ |
| F-4 | yüksek | el-print | Revize edilmiş (SUPERSEDED) eski versiyon kâğıt üzerinde güncel resmi belgeyle birebir aynı basılıyor | `Electron/src/pages/Operations/Shipments/ShipmentFrozenSheet.tsx:58` | ✅ |
| F-5 | yüksek | el-tabs | Overlay tıklaması BAŞKA sekmedeki/iç içe açık modalları da sessizce kapatıyor (form verisi kaybı) | `Electron/src/components/ui/dialog.tsx:69` | ✅ |
| F-6 | yüksek | el-perm | Frontend 'isAdmin' kısayolu backend wildcard semantiğiyle uyuşmuyor — kısmi admin tüm UI'ı görür, her tıklama 403 | `Electron/src/types/auth.ts:29` | ✅ |
| F-7 | yüksek | el-perm | Renkler modülü yanlış permission kodu kontrol ediyor (item:* — backend property:* istiyor) | `Electron/src/pages/Colors/ColorsPage.tsx:46` | ✅ |
| F-8 | yüksek | mb-offline | KartelaSevk: paused sevk app restart'ında undefined payload ile replay → sevk kaybı | `mobil/src/screens/Modules/KartelaSevk/KartelaSevkScreen.tsx:156` | ✅ |
| F-9 | yüksek | mb-offline | KartelaKabul: paused kabul restart'ında undefined payload ile replay → sayım/ölçüm kaybı | `mobil/src/screens/Modules/KartelaKabul/KartelaKabulScreen.tsx:353` | ✅ |
| F-10 | yüksek | mb-scan | KK2: çift basış otomatik ilerleyen seçimle SONRAKİ topu işaretliyor / adımı kapatıyor | `mobil/src/screens/Modules/KursunQc/KursunQcScreen.tsx:1404` | ✅ |
| F-11 | yüksek | mb-scan | Tambur: otomatik bitiş (remaining<0.1) yanıt anındaki seçili topu finalize ediyor — kesilen topu değil | `mobil/src/screens/Modules/Tambur/TamburScreen.tsx:597` | ✅ |
| F-12 | yüksek | mb-api | 401'de sessiz logout — offline kuyruktaki istasyon kayıtları kalıcı kaybolur | `mobil/src/services/api.ts:45` | ✅ |
| F-13 | orta | el-query | Çuval Depo sevk/geri alma — sevkiyat listesi ve sipariş durumu invalidate edilmiyor | `Electron/src/pages/Operations/SackStore/SackStorePage.tsx:101` | ✅ |
| F-14 | orta | el-destructive | Sipariş iptal preview'ındaki activeShipments/canCancel frontend'de tamamen yok sayılıyor | `Electron/src/pages/Operations/Orders/OrderCancelDialog.tsx:200` | ✅ |
| F-15 | orta | el-print | Arka plan sekmesinde açık kalan irsaliye dialoğu baskıda aktif belgenin ÜZERİNE biner | `Electron/src/index.css:428` | ✅ |
| F-16 | orta | el-print | Fason irsaliyesi: canlı overlay (renk + boyahane notu) sorgusu HATA verirse fiş renksiz/notsuz basılıyor | `Electron/src/pages/Operations/WorkOrders/FasonSevkPrintDialog.tsx:75` | ✅ |
| F-17 | orta | el-tabs | Arka plan sekmesinde açık dialog varken aktif sekmede Esc tamamen ölü kalıyor | `Electron/src/components/ui/dialog.tsx:58` | ✅ |
| F-18 | orta | el-tabs | window.print() arka plan sekmesinde açık bırakılan irsaliye önizlemesini de basıyor (üst üste binmiş resmi belge) | `Electron/src/index.css:424` | ✅ |
| F-19 | orta | el-api | Global 15 sn timeout: büyük yedek indirme/ağır rapor kesilir ve sunucu yanlışlıkla 'offline' işaretlenir | `Electron/src/services/apiClient.ts:23` | ✅ |
| F-20 | orta | el-perm | Dashboard widget'ları izin kontrolsüz — dar yetkili kullanıcı login olur olmaz 403 toast fırtınası | `Electron/src/pages/Dashboard/KpiCards.tsx:88` | ✅ |
| F-21 | orta | el-perm | İzin değişikliği oturumdaki kullanıcıya hiçbir şekilde yansımıyor; UI'da 'yeniden giriş gerekli' uyarısı da yok | `Electron/src/pages/Access/Users/PermissionsTab.tsx:73` | ✅ |
| F-22 | orta | el-perm | İş emri formu picker'ları cross-module read izinleri istiyor — workorder:write tek başına formu kullanılamaz kılar | `Electron/src/pages/Operations/WorkOrders/LinkedOrderLinesField.tsx:84` | ✅ |
| F-23 | orta | mb-offline | Kartela ekranlarında offline geri bildirim yok: SyncStatusChip + onMutate toast eksik, Gönder sonsuz spinner | `mobil/src/screens/Modules/KartelaSevk/KartelaSevkScreen.tsx:338` | ✅ |
| F-24 | orta | mb-offline | KartelaKabul uzun formu (per-top sayım + ölçümler) draft yedeksiz — LMK kill = sayım baştan | `mobil/src/screens/Modules/KartelaKabul/KartelaKabulScreen.tsx:214` | ✅ |
| F-25 | orta | mb-scan | Paketleme: sürekli kamera modunda scanBusy sırasında gelen okuma sessizce düşüyor (yeşil ✓ verilmesine rağmen) | `mobil/src/screens/Modules/TartiPaket/PaketlemeScreen.tsx:341` | ✅ |
| F-26 | orta | mb-api | warehouse-scope 'preparing' kovası mobilde yok — PREPARING sevkiyattaki toplar Depo'da görünmez | `mobil/src/services/roll.service.ts:158` | ✅ |
| F-27 | orta | mb-ui | Tambur 'Top Kesme' yalnız kamera ile açılıyor — Listeden Seç / manuel giriş yok | `mobil/src/screens/Modules/Tambur/TamburScreen.tsx:1027` | ✅ |
| F-28 | orta | mb-ui | Sabit pageSize'lı master-data picker'ları kayıt sayısı tavanı aşınca sessizce kırpıyor | `mobil/src/screens/Modules/KK1/KK1Screen.tsx:261` | ✅ |
| F-29 | orta | mb-ui | Tartı/Paket açık sipariş listesi: 300 karta kadar ScrollView+map, virtualizasyon ve arama yok | `mobil/src/screens/Modules/TartiPaket/TartiPaketScreen.tsx:278` | ✅ |

## BULGU DETAYLARI

### F-1 — Manuel top ekleme ölü queryKey'i invalidate ediyor — liste hiç tazelenmiyor (yüksek)
**Konum:** `Electron/src/pages/Operations/Rolls/ManualEntryDialog.tsx:84`

onSuccess `qc.invalidateQueries({ queryKey: ["rolls:STOCK"] })` çağırıyor, ama "STOCK" sekmesi artık yok — Rolls/service.ts'deki yorum 'Eski STOCK sekmesi iki ayrı sekmeye bölündü (RAW_STOCK/FINISHED_STOCK)' diyor ve STATUS_GROUPS'ta STOCK anahtarı yok; aktif tablo key'i `rolls:RAW_STOCK` (RollsTable.tsx:157). 'Manuel Top Ekle' butonu yalnız RAW_STOCK sekmesinde gösterildiğinden (RollsPage.tsx:89) operatör topu ekler, başarı toast'ı barkodu gösterir ama altındaki Ham Stok listesi yeni topu GÖSTERMEZ — refetchOnWindowFocus:false (App.tsx:21) + sekmeler mount kaldığı için (TabHost.tsx:11) manuel Yenile'ye kadar bayat kalır; operatör 'girilmedi sanıp' aynı topu ikinci kez girebilir (çift stok).

**Önerilen düzeltme:** `["rolls:RAW_STOCK"]` invalidate et (mevcut şemada), ya da kalıcı çözüm olarak rolls tab key'lerini array formuna geçirip `["rolls"]` prefix'iyle hepsini yakala. Stats query'si aynı prefix'i paylaştığı için birlikte tazelenir.

<details><summary>Doğrulayıcı kararları</summary>

- Bulgu doğru: ManualEntryDialog.tsx:84 ["rolls:STOCK"] invalidate ediyor ama bu key hiçbir yerde kayıtlı değil (grep tek eşleşme: invalidate satırının kendisi); aktif liste key'i ["rolls:RAW_STOCK", baseKey] (RollsTable.tsx:157 + useDataTable.ts:139) ve TanStack prefix eşleşmesi element bazlı olduğundan match etmez. Başka koruyucu katman yok: refetchOnWindowFocus:false (App.tsx:21), TabHost tüm sekmeleri mount tutar (TabHost.tsx:11-13), dialog kapanışı RollsTable'ı remount etmez — liste manuel Yenile'ye veya alt-sekme değişimine kadar bayat kalır. Hafifletici: başarı toast'ı yeni barkodu açıkça gösterir ve Yenile butonu doğru çalışır, bu yüzden çift giriş riski var ama orta düzeyde.
- Bulgu doğru: ManualEntryDialog.tsx:84 ["rolls:STOCK"] invalidate ediyor ama hiçbir query bu key'i kullanmıyor — tablo key'i ["rolls:RAW_STOCK", baseKey] (RollsTable.tsx:157 + useDataTable.ts:139), STATUS_GROUPS'ta STOCK yok (service.ts:35-41, eski sekme bölünmüş). Buton yalnız RAW_STOCK sekmesinde (RollsPage.tsx:89) ve renksiz manuel top backend'de tam RAW_STOCK scope'una düşüyor (inventory.service.ts:308-309 status=STOCK + 530-535 currentStepId:null); refetchOnWindowFocus:false (App.tsx:21) ve sayfa mount kaldığı için liste manuel Yenile'ye/sekme değiştirip dönmeye kadar yeni topu göstermiyor — çift giriş yeni otomatik barkodla engelsiz geçer.
</details>

### F-2 — rolls:<tab> string-suffix queryKey deseni — tüm top tabloları cross-invalidation'dan kopuk (yüksek)
**Konum:** `Electron/src/pages/Operations/Rolls/RollsTable.tsx:157`

Tablo key'i `queryKey: \`rolls:${tab}\`` (ve stats için 178) — proje kuralı (1)'in birebir ihlali: varyant string suffix'te olduğu için `invalidateQueries({queryKey:["rolls"]})` array-eleman prefix eşleşmesinde bu tablolara ASLA çarpmaz (yalnız RollsKanban'ın ["rolls","kanban",*] key'ine çarpar). Somut zarar: ReturnEntryDialog.tsx:118 ve ReturnsDetailSheet.tsx:74 iade sonrası `["rolls"]` invalidate ediyor — toast 'top Hazır Depo'ya eklendi' der ama topun düştüğü Bitmiş Depo tablosu (`rolls:FINISHED_STOCK`) bayat kalır. RollsPage.tsx:83-87'deki yorum uyumsuzluğun farkında, ama düzeltmek yerine RefreshButton'da workaround yapılmış; her yeni mutation bu tuzağa düşmeye devam edecek.

**Önerilen düzeltme:** `useDataTable.queryKey`'i string yerine array kabul edecek şekilde genişletip rolls tablolarını `["rolls", tab]` yap; RefreshButton'a `["rolls", tab]` (KANBAN'da ["rolls"]) ver. Sonrasında roll mutate eden her yer tek `["rolls"]` invalidate ile çalışır.

<details><summary>Doğrulayıcı kararları</summary>

- Doğrulandı: useDataTable.ts:139 key'i [`rolls:${tab}`, baseKey] kurar; ["rolls"] invalidate (ReturnEntryDialog.tsx:118, ReturnsDetailSheet.tsx:74) array-eleman eşleşmesinde bunlara asla çarpmaz, yalnız RollsKanban'a çarpar. refetchOnMount:"always"+staleTime:0 aynı-sekme navigasyonunu kurtarır AMA TabHost.tsx:11-12 tüm açık sekmeleri mount tutar — arka plan sekmesindeki Bitmiş Depo tablosu iade sonrası bayat kalır; üstelik ManualEntryDialog.tsx:84 ölü key ["rolls:STOCK"] invalidate ediyor (STOCK sekmesi RAW_STOCK/FINISHED_STOCK'a bölünmüş) → aynı sayfadaki tablo manuel top eklendikten sonra bile tazelenmiyor, yani tuzak ikinci kurbanını çoktan almış.
- Doğrulandı: RollsTable.tsx:157/177 `rolls:${tab}` string-suffix key üretiyor (useDataTable.ts:139 → ["rolls:FINISHED_STOCK", {...}]), ReturnEntryDialog.tsx:118 ve ReturnsDetailSheet.tsx:74'ün ["rolls"] invalidate'i array-eleman eşleşmesinde bunlara asla çarpmıyor (yalnız RollsKanban.tsx:54 ["rolls","kanban",*] yakalanır); RollsPage.tsx:83-87 yorumu sorunu bilip RefreshButton workaround'u yapıyor ve tuzak gerçekten tekrar ısırmış — ManualEntryDialog.tsx:84 artık var olmayan ["rolls:STOCK"] key'ini invalidate ediyor (service.ts:29-31: STOCK sekmesi RAW_STOCK/FINISHED_STOCK'a bölünmüş), yani manuel top girişi hiçbir tabloyu tazelemiyor. Hafifletici katman: useDataTable refetchOnMount:"always"+staleTime:0 sayfa/alt-sekme geçişinde tazeler; ancak TabHost.tsx:11-13 açık Electron sekmelerini mount tutar (keep-alive), arka plandaki Bitmiş Depo sekmesi iade sonrası manuel yenilemeye dek bayat kalır — zarar gerçek ama görüntü bayatlığıyla sınırlı, veri bozulmaz.
</details>

### F-3 — window.print() çıktısı layout zincirine hapsolmuş — uzun belge tek sayfaya kırpılır, dar/kaymış basılır (yüksek)
**Konum:** `Electron/src/index.css:424`

Print CSS yalnız visibility sıfırlıyor ve `.print-area`'yı `position:absolute; top:0; left:0` yapıyor; ama absolute konum en yakın positioned ataya (DialogContent — dialog.tsx:76-79'da `left-1/2 top-1/2 -translate-…` + taban sınıfta `overflow-y-auto max-h-[90vh]`, sekme içinde `absolute`) göredir, sayfa köküne değil. DialogContent'in containing-block zinciri TabPane (`absolute inset-0`, TabHost.tsx:54) → `main.relative.overflow-hidden` (AppShell.tsx:50) → kök `h-screen overflow-hidden` (AppShell.tsx:43) olduğundan, baskı yeniden-layout'unda (viewport=kâğıt) çıktı (a) DialogContent'in overflow-y-auto + h-[90vh] kutusunda ve kökün h-screen overflow-hidden'ında KIRPILIR → ~40+ top satırlı fason irsaliyesi/çok çuvallı sevk irsaliyesinde ikinci sayfa hiç basılmaz; (b) sekme paneli genişliği = kâğıt genişliği − sidebar olduğundan belge dar ve sağa kaymış basılır. Üç resmi belge dialoğu da (ShipmentDispatchNote.tsx:127, FasonSevkPrintDialog.tsx:149, KartelaCekiPrintDialog.tsx:107) hep sekme içinde açıldığı için her resmi baskı bu yoldan geçer. (Refakat kartının gizli-iframe+PDF yoluna taşınmış olması bu desenin sorunlu olduğunun iç kanıtı.)

**Önerilen düzeltme:** Resmi belgeleri ekran DOM'undan basma: ya refakat kartındaki gibi içeriği ayrı bir gizli iframe'e/penceye yazıp `contentWindow.print()` çağır, ya da `@media print`te `.print-area`'yı `position:fixed` + tüm atalarda `overflow:visible !important; height:auto !important; transform:none !important` ile kökten kurtar (en güvenlisi iframe).

<details><summary>Doğrulayıcı kararları</summary>

- Bulgu kodla birebir doğrulandı: print CSS katmansız olduğundan .print-area'nın absolute'u Tailwind relative'ini ezer ve containing block = DialogContent olur; üç resmi belge dialoğu da (ShipmentDispatchNote.tsx:77, FasonSevkPrintDialog.tsx:88, KartelaCekiPrintDialog.tsx:62) taban overflow-y-auto'yu (dialog.tsx:76) override etmeyen h-[90vh] scroll konteyneri içinde — kâğıtta kaydırma olmadığından ilk sayfa sonrası içerik (FasonSevkSheet.tsx:148 top başına satır basar) hiç basılmaz, üstte main.overflow-hidden + h-screen (AppShell.tsx:50,43) zaten tek sayfaya kilitler ve visibility:hidden sidebar'ın genişliğini koruduğundan çıktı dar/sağa kaymış olur. Telafi eden hiçbir katman yok (beforeprint yok, print: variant yok, main-process müdahalesi yok); yalnız refakat kartı iframe+PDF yoluna taşınmış (TravelerCardPrintDialog.tsx:105-124) — iddianın iç-kanıt tespiti de doğru.
- Bulgu kodla ve deneyle doğrulandı: index.css:424'teki unlayered print CSS .print-area'yı absolute yapar ama containing block DialogContent'tir (dialog.tsx:76-79 transform+overflow-y-auto; üç irsaliye dialoğu h-[90vh] geçer ve yerinde window.print() çağırır — ShipmentDispatchNote.tsx:77/127, FasonSevkPrintDialog.tsx:88/149, KartelaCekiPrintDialog.tsx:62/107); hiçbir print: Tailwind override yok. Aynı zincirin birebir headless-Chromium reprosu: 80 satırlık belge zincir içinde 1 sayfaya kırpıldı (38-80. satırlar kayıp) ve dar/sağa kaymış basıldı; zincirsiz kontrol aynı içerikle 2 tam sayfa üretti (/tmp/print-repro). Refakat kartının PDF+gizli-iframe'e taşınmış olması (TravelerCardPrintDialog.tsx:105-116) iddiadaki iç kanıtı da doğruluyor.
</details>

### F-4 — Revize edilmiş (SUPERSEDED) eski versiyon kâğıt üzerinde güncel resmi belgeyle birebir aynı basılıyor (yüksek)
**Konum:** `Electron/src/pages/Operations/Shipments/ShipmentFrozenSheet.tsx:58`

Üç frozen sheet de baskıya yalnız `voided` durumunda İPTAL filigranı koyuyor (ShipmentFrozenSheet.tsx:58, FasonSevkSheet.tsx:59, KartelaCekiSheet.tsx:78); SUPERSEDED için hiçbir işaret yok. Versiyon/durum bilgisini taşıyan DocVersionBar ise bilerek `.print-area` DIŞINDA (DocVersionBar.tsx:29) ve sheet'ler `PrintedDocument.documentNo`/`version`'ı hiç basmıyor (grep doğrulandı: üç sheet dosyasında documentNo/version referansı yok) — kâğıtta sadece snapshot'taki shipmentNo/dispatchNo var ve bu numara revizyonda değişmez. Senaryo: plaka yanlış girildiği için belge revize edildi; operatör 'Geçmiş'ten Rev.1'i açıp Yazdır'a basarsa (buton eski versiyonda da aktif) yanlış içerikli eski belge, güncel resmi belgeden ayırt edilemez şekilde basılır ve araca/karşı tarafa gidebilir.

**Önerilen düzeltme:** Frozen sheet'lere `version`/`status` prop'u geçir: SUPERSEDED'da 'REVİZE EDİLDİ' filigranı (DocWatermark zaten var) + her baskıda köşeye 'Belge No / Rev.N' künyesi bas; istenirse eski versiyonda Yazdır'ı onaylı yap.

<details><summary>Doğrulayıcı kararları</summary>

- Bulgu kod düzeyinde doğru: üç frozen sheet de filigranı yalnız voided'da basıyor (ShipmentFrozenSheet.tsx:58, FasonSevkSheet.tsx:59, KartelaCekiSheet.tsx:78; çağıranlar voided={status==="VOIDED"} geçiyor) ve DocWatermark sadece "draft"|"void" tonlarını destekliyor — SUPERSEDED için hiçbir işaret yok. Versiyon/durum bilgisi taşıyan DocVersionBar .print-area dışında ve index.css:424-442 @media print her şeyi gizleyip yalnız .print-area basıyor; sheet'lerde documentNo/version referansı hiç yok (grep 0 eşleşme), basılan shipmentNo/dispatchNo revizyonlar arasında aynı. Yazdır butonu eski versiyon görüntülenirken de aktif (ShipmentDispatchNote.tsx:126 disabled={!shown && !draft}). Başka koruyucu katman yok.
- Kod iddiayı birebir doğruluyor: üç sheet de filigranı yalnız VOIDED'da basıyor (ShipmentFrozenSheet.tsx:58, FasonSevkSheet.tsx:59, KartelaCekiSheet.tsx:78; çağıranlar voided={status==="VOIDED"}), DocVersionBar bilerek .print-area dışında (DocVersionBar.tsx:29 + index.css:424-442 yalnız .print-area basar), sheet'lerde documentNo/version/frozenAt hiç render edilmiyor (grep boş) ve documentNo zaten revizyonda değişmeyen shipmentNo/dispatchNo (shipping.service.ts:2436, kartela.service.ts:1210). Yazdır butonu eski versiyonda da aktif (ShipmentDispatchNote.tsx:126, FasonSevkPrintDialog.tsx:148, KartelaCekiPrintDialog.tsx:107) ve window.print() client-side olduğundan başka katman korumuyor; SUPERSEDED Rev.1 çıktısı güncel resmi belgeden kâğıt üzerinde ayırt edilemez. Hafifletici tek etken ekrandaki "Revize edildi" rozeti + dialogun varsayılan ACTIVE açılması (eski versiyon ancak bilinçli Geçmiş seçimiyle basılır) — bu yüzden olasılık düşük ama sonuç (yanlış içerikli resmi irsaliyenin araca gitmesi) gerçek.
</details>

### F-5 — Overlay tıklaması BAŞKA sekmedeki/iç içe açık modalları da sessizce kapatıyor (form verisi kaybı) (yüksek)
**Konum:** `Electron/src/components/ui/dialog.tsx:69`

onInteractOutside guard'ı `target.closest("[data-ui-overlay]")` ile 'herhangi bir karartmaya tıklandı mı' diye bakıyor, overlay'in HANGİ dialoga ait olduğunu kontrol etmiyor (dialog.tsx:61-70, aynı kod sheet.tsx:66-73). Radix non-modal DismissableLayer document'a pointerdown dinler ve modal layer yokken TÜM layer'larda tetiklenir (dismissable-layer dist/index.mjs:43-49: preventDefault edilmezse onDismiss). Senaryo A (cross-tab): Sekme A'da sipariş formu açıkken kullanıcı tasarımın ana akışıyla Sekme B'ye referansa bakar, B'de herhangi bir dialog/sheet'i kendi karartmasına tıklayarak kapatır → A'daki form da arka planda sessizce kapanır; OrderFormDialog yeniden açılışta form.reset yapar (OrderFormDialog.tsx:115-119) → girilen tüm kalemler gider. Senaryo B (aynı sekme, iç içe): OrderFormDialog içindeki ColorPickerModal'ın (OrderLineColorPicker → ColorPickerModal.tsx:120) karartmasına tıklayınca hem picker hem dış sipariş formu birlikte kapanır — iç overlay dış layer için de 'data-ui-overlay' eşleşmesidir.

**Önerilen düzeltme:** Karartma div'ine ref/benzersiz işaret koy ve onInteractOutside'da yalnız KENDİ overlay'in tıklandığında kapan (örn. `target === ownOverlayRef.current`); ek emniyet olarak scoped + !isTabActive iken interactOutside'ı da (Esc gibi) koşulsuz preventDefault et.

<details><summary>Doğrulayıcı kararları</summary>

- Cross-tab senaryo kodla doğrulandı: pasif sekme mount kalır (TabHost.tsx:51-59), non-modal'da Radix tüm layer'larda pointerdown-outside tetikler (dismissable-layer 1.1.11 dist/index.mjs:38-50, isPointerEventsEnabled=index>=-1 hep true; listener document'ta, gizli pane'in pointer-events-none'ı etkisiz), başka sekmenin overlay'i ayrı React ağacında olduğundan capture-koruması devreye girmez ve dialog.tsx:67-69 guard'ı 'herhangi bir [data-ui-overlay]' eşleşmesiyle kapanmaya izin verir → A sekmesindeki form sessizce kapanır, OrderFormDialog.tsx:115-119 yeniden açılışta form.reset yapar (veri kaybı); Esc için isTabActive guard'ı var (dialog.tsx:56-58) ama interact-outside'da yok. ANCAK iç içe senaryo (B) YANLIŞ: ColorPickerModal portal'ı dış DialogContent'in React-ağacı çocuğudur, pointerdown dış layer'ın onPointerDownCapture'ını tetikleyip isPointerInsideReactTreeRef=true yapar (dist/index.mjs:142,174-177) → dış form KAPANMAZ; bulgunun yarısı geçersiz, kapsam yalnız sekmeler-arası.
- Senaryo A (cross-tab) kod zinciriyle tam doğrulandı: TabHost.tsx:51-70 pasif pane'i mount tutar; dialog.tsx:14-17 sekmede modal=false; Radix dismissable-layer (index.mjs:42-49,166) modal layer yokken TÜM açık non-modal layer'lara document pointerdown'da onInteractOutside/onDismiss verir; dialog.tsx:69 guard'ı overlay sahipliğine bakmadan HERHANGİ bir [data-ui-overlay]'de preventDefault'u atlar → Sekme B'de overlay-tıkla-kapat, Sekme A'daki formu da kapatır; OrderFormDialog.tsx:115-119 yeniden açılışta form.reset → veri kaybı gerçek. Senaryo B ise yanlış: ColorPickerModal dış DialogContent'in React çocuğu olduğundan Radix'in React-tree capture flag'i (index.mjs:174-177) dış formu korur — ama aynı bug React-KARDEŞ render edilen CustomerFormDialog'da (OrderFormDialog.tsx:290) aynı sekmede de tetiklenir. Ana bulgu ayakta; yalnız iç-içe örneği yanlış komponente atfedilmiş.
</details>

### F-6 — Frontend 'isAdmin' kısayolu backend wildcard semantiğiyle uyuşmuyor — kısmi admin tüm UI'ı görür, her tıklama 403 (yüksek)
**Konum:** `Electron/src/types/auth.ts:29`

hasAdminAccess() admin:users, admin:settings veya admin:* İZİNLERİNDEN HERHANGİ BİRİ varsa true döner; useRoleAccess.ts:13-15 ve PermissionGate.tsx:15 isAdmin=true olunca TÜM permission kontrollerini bypass eder (hasPermission her kod için true). Backend ise (Teks-Erp/src/middlewares/rbac.middleware.ts:23-35) sadece exact kod + tek-domain wildcard tanır: admin:* yalnız admin:... kodlarını karşılar, admin:users/admin:settings yalnız kendini. Sonuç: sadece 'admin:users' verilen bir kullanıcı (örn. kullanıcı hesaplarını yönetsin diye) Electron'da TÜM sidebar/hub/yazma butonlarını görür, Siparişler/İş Emirleri/Toplar dahil her veri çekişi ve her aksiyon backend'den 403 döner. Ayrıca admin:settings'li kullanıcı ProtectedRoute requirePermission="admin:users" kapısını isAdmin sayesinde geçer (ProtectedRoute.tsx:27) ama /api/admin/users 403 verir — Yetkilendirme sayfası boş+toast'lı açılır. (Karşılaştırma: mobil taraf bunu DOĞRU yapıyor — mobil/src/hooks/usePermission.ts:16-21 wildcard'ı yalnız kendi domain'ine genişletir.)

**Önerilen düzeltme:** useRoleAccess/PermissionGate'teki isAdmin bypass'ını kaldırıp backend'deki matchesPermission ile aynı domain-wildcard mantığını uygula (admin:* → sadece admin:..., exact kod eşleşmesi); isAdmin yalnız adminOnly nav görünürlüğünde kullanılacaksa hasAdminAccess'i 'admin:users || admin:*' gibi gerçek kapsamına daralt.

<details><summary>Doğrulayıcı kararları</summary>

- Tüm iddialar kodla doğrulandı: auth.ts:29-31 hasAdminAccess üç admin kodundan herhangi biriyle true; useRoleAccess.ts:13-15 + PermissionGate.tsx:15 + ProtectedRoute.tsx:27 isAdmin'de TÜM kontrolleri bypass eder; backend rbac.middleware.ts:23-35 ise sadece exact+tek-domain wildcard tanır (admin:users hiçbir order/workorder iznini karşılamaz). Senaryo kurgusal değil — backend permission-management.service.ts:317-340 'admin:users-only kullanıcı-yöneticisi' personasını açıkça modelliyor ve PermissionsTab tek tek izin atamaya izin veriyor; böyle bir kullanıcı tüm hub kartlarını/yazma butonlarını görür (3 hub da `isAdmin ||` filtreli) ve her veri çekişi 403 toast'u olur; admin:settings'li kullanıcı da /access'i isAdmin'le geçip /api/admin/users'tan 403 alır. Tek hafifletici: backend tam koruyor (güvenlik/veri zararı yok, salt UX kırılması) ve seed/şablonlar kısmi admin üretmiyor — bu yüzden şiddet 'orta' önerilir.
- Bulgu kod düzeyinde doğru: auth.ts:29 hasAdminAccess herhangi bir admin:* izniyle true döner, useRoleAccess.ts:13-15 + PermissionGate.tsx:15 + ProtectedRoute.tsx:27 + Sidebar.tsx:43-44 isAdmin ile TÜM kontrolleri bypass eder; backend rbac.middleware.ts:23-35 ise sadece exact+tek-domain wildcard tanır ve hiçbir data route'unda admin fallback yok (grep doğrulandı), JWT'de wildcard genişletme de yok (auth.service.ts:127-142). Lone admin:users senaryosu gerçekçi — permission-management.service.ts:317 last-admin guard'ı 'tek admin:users kullanıcısı'nı açıkça varsayıyor ve atama UI'sı keyfi subset'e izin veriyor; koruyucu başka katman bulunamadı. Hafifletici tek nokta: güvenlik açığı değil (backend 403 ile korur), yalnızca kısmi-admin atandığında tetiklenen UX/konfigürasyon tuzağı — bu yüzden şiddet 'orta' uygun.
</details>

### F-7 — Renkler modülü yanlış permission kodu kontrol ediyor (item:* — backend property:* istiyor) (yüksek)
**Konum:** `Electron/src/pages/Colors/ColorsPage.tsx:46`

ColorsPage writePermission="item:write" (satır 46) ve Tanımlar hub tile'ı permission:"item:read" (pages/Definitions/tile-config.ts:61) kullanıyor; ayrıca content-routes.tsx:103 rotası tamamen guard'sız. Backend ise GET /api/colors için property:read (Teks-Erp/src/routes/color.routes.ts:51), create/update/delete için property:write (98/129/147) ister. İki yönlü kırık: item:read+item:write'lı kullanıcı (ürün sorumlusu) Renkler kartını ve Yeni/Düzenle/Sil butonlarını görür ama daha liste sorgusunda 403 toast yer; property:read/property:write'lı kullanıcı (renk-özellik sorumlusu) ise backend izin verdiği halde kartı hiç göremez (komut paletinde de aynı tile permission'ı kullanılır, command-entries.ts:60).

**Önerilen düzeltme:** tile-config.ts'de colors permission'ını property:read'e, ColorsPage writePermission'ını property:write'a çevir; rota için de ProtectedRoute requirePermission="property:read" ekle.

<details><summary>Doğrulayıcı kararları</summary>

- Bulgu doğru: ColorsPage.tsx:46 writePermission="item:write" ve tile-config.ts:61 "item:read" kullanırken backend color.routes.ts 51/69/98/129/147 property:read/property:write ister; hasPermission düz includes() (alias/wildcard yok) ve seed.ts:60-65'te item:* ile property:* ayrı atanabilir kodlar. item:* kullanıcısı kartı+butonları görüp 403 yer, property:* kullanıcısı backend izinli olduğu halde kartı/komut paletini hiç göremez — kardeş FabricPropertiesPage.tsx:47 doğru şekilde property:write kullanıyor, yani colors istisnai bir hata. Tek nüans: guard'sız route (content-routes.tsx:103) diğer tanım sayfalarında da aynı (tasarım), asıl kusur iki yönlü izin kodu uyuşmazlığı.
- Bulgu kod düzeyinde doğru: ColorsPage.tsx:46 writePermission="item:write" ve tile-config.ts colors tile permission:"item:read" iken backend color.routes.ts GET/POST/PATCH/DELETE property:read|write ister; ne frontend hasPermission (exact match, useRoleAccess.ts) ne backend matchesPermission (sadece aynı-domain wildcard) item↔property çevirisi yapar, seed.ts:60-65 ikisini ayrı modül izinleri olarak tanımlar. Kardeş FabricPropertiesPage property:write kullanır ve colors/service.ts yorumu bile "property:read ile çalışır" der — iki yönlü kilitlenme (item'lı kullanıcı 403 toast, property'li kullanıcı tile/palette göremez) gerçek. Backend doğru enforce ettiği için veri riski yok; bu bir frontend erişim/UX hatası.
</details>

### F-8 — KartelaSevk: paused sevk app restart'ında undefined payload ile replay → sevk kaybı (yüksek)
**Konum:** `mobil/src/screens/Modules/KartelaSevk/KartelaSevkScreen.tsx:156`

dispatchMutation registry anahtarını (STATION_MUT.KARTELA_SEVK_DISPATCH) kullanıyor ama KK1/FasonSevk/FasonKabul deseninin aksine KENDİ closure mutationFn'ini tanımlıyor ve `dispatchMutation.mutate()` (satır 331) variables'sız çağrılıyor — payload firmId/rolls/plateNumber state'inden okunuyor. Persist katmanı yalnız mutationKey+variables saklar (mutations.ts:3-6 yorumu tam bunu söylüyor); offline'da pause olmuş sevk, uygulama Android LMK/restart ile öldüğünde registry'deki default fn `kartelaService.dispatch(undefined)` olarak resume edilir → boş body POST → backend Zod 400 → 3 retry sonrası sessizce düşer. Operatör malı fiziksel olarak göndermişken sevk kaydı kaybolur — persist altyapısının var olma sebebi olan senaryoda.

**Önerilen düzeltme:** Local mutationFn'i kaldır; KartelaDispatchRequest'i FasonSevk gibi `mutate(vars)` ile geçir (registry default fn zaten vars bekliyor, mutations.ts:145-148).

<details><summary>Doğrulayıcı kararları</summary>

- Bulgu kodla birebir doğrulandı: KartelaSevkScreen.tsx:156-167 kendi closure mutationFn'ini tanımlayıp 331'de mutate()'i variables'sız çağırıyor; registry default networkMode:'online' (mutations.ts:56-60,145-148) offline'da pause + persist ettiriyor; App.tsx:80-82 resumePausedMutations restart sonrası registry fn'ini variables=undefined ile çalıştırır → kartelaService.dispatch(undefined) boş body POST → kartela.controller.ts:107 dispatchSchema.parse 400 → observer'sız mutation sessizce düşer, roll listesi component state'inde olduğundan kurtarılamaz. Kardeş ekranlar (FasonSevk:791, FasonKabul:814, KK1:628) payload'ı variables olarak geçiyor; KartelaKabulScreen.tsx:354-358 aynı hataya sahip. Hiçbir koruyucu katman (dehydrate filtresi, offline guard) yok.
- Bulgu kodla doğrulandı: KartelaSevkScreen.tsx:156-167 registry anahtarını kullanıp KENDİ closure mutationFn'ini tanımlıyor ve satır 331'de mutate() variables'sız çağrılıyor; registry default'u networkMode:'online' verdiği için offline'da pause olur, persist katmanı yalnız key+state saklar (mutations.ts:3-6), restart sonrası hydration registry fn'ini (mutations.ts:146) variables=undefined ile çalıştırır → POST /kartela/dispatch boş body → Zod 400 → 3 retry sonrası sessiz kayıp (onError closure restore edilmez, ekranda FasonSevk'in aksine draft yedeği de yok). Kanonik kardeşler (FasonSevk:791, KK1:628) vars geçiyor — sapma kazara; KartelaKabul:354'te aynı hata var.
</details>

### F-9 — KartelaKabul: paused kabul restart'ında undefined payload ile replay → sayım/ölçüm kaybı (yüksek)
**Konum:** `mobil/src/screens/Modules/KartelaKabul/KartelaKabulScreen.tsx:353`

receiveMutation aynı desen sapmasını taşıyor: `useMutation<.., void>({ mutationKey: STATION_MUT.KARTELA_KABUL_RECEIVE, mutationFn: async () => kartelaService.receive({ subcontractorId: selectedJob!.firm.id, ... returns: selectedReturns }) })` — variables void, payload closure'dan. Offline kabul pause olur; app restart sonrası registry fn `kartelaService.receive(undefined)` ile çalışır → 400 → kabul (per-top kartela sayımı + cm/kg ölçümleri) kalıcı kaybolur. In-session resume çalıştığı için sorun yalnız kill/restart'ta görünür — yani en sinsi anda.

**Önerilen düzeltme:** KartelaReceiveRequest'i mutate(vars) ile geçir, local mutationFn'i sil; onMutate'te vars'tan toast üret.

<details><summary>Doğrulayıcı kararları</summary>

- Zincir kodla doğrulandı: KartelaKabulScreen.tsx:353/569 void variables + closure mutationFn + mutate() argümansız; registry default networkMode:'online' (mutations.ts:151-154) → offline'da pause, default shouldDehydrateMutation=isPaused ile persist (App.tsx:73-82); restart'ta hydration registry fn'ini kullanır (mutationCache.ts:114) ve continue() execute(state.variables!)=undefined ile kartelaService.receive(undefined) → POST undefined body → 400, 3 retry sonrası mutation sessizce düşer (hydrated mutation'a onError bağlı değil). FasonKabulScreen.tsx:387-398 doğru deseni (vars=ReceiveRequest, inline fn yok) kanıtlar; KartelaSevk de aynı kusuru taşır. Hafifletici: backend tutarsızlığı yok, iş bekleyen listede kalır ve ekran sahte başarı toast'ı göstermez (operatör spinner görür) — kayıp yalnız girilen sayım/ölçüm formu.
- Bulgu doğru: KartelaKabulScreen.tsx:353 void-variables + closure mutationFn kullanıyor (mutate() satır 569 argümansız), registry default'undan networkMode:'online' miras aldığı için offline'da pause+persist oluyor (App.tsx:27/81 registerStationMutationDefaults + resumePausedMutations aktif); restart'ta closure kaybolur, mutations.ts:151-154 registry fn'i kartelaService.receive(undefined) ile çalışır → 400, üstelik onError toast'u da closure'da olduğundan kayıp SESSİZ. Doğru pattern FasonKabulScreen.tsx:393'te mevcut (local mutationFn yok, vars geçiliyor); aynı kusur KartelaSevkScreen.tsx:157'de de var. Hafifletici: backend kabul hiç commit olmadığı için sevk açık kalır, operatör ölçümleri yeniden girerek kurtarabilir — kalıcı kayıt bozulması değil, girilen sayım/ölçüm + bildirimsiz başarısızlık.
</details>

### F-10 — KK2: çift basış otomatik ilerleyen seçimle SONRAKİ topu işaretliyor / adımı kapatıyor (yüksek)
**Konum:** `mobil/src/screens/Modules/KursunQc/KursunQcScreen.tsx:1404`

"KK2 Tamamla" ve "Kumaşı Bitir" butonlarında bilinçli olarak isPending/disabled bağı yok (satır 1353-1356, 1372-1375 yorumları offline gerekçesi veriyor) ve completeQc2Mutation.onMutate (satır 460-471) ile kursunFinishMutation.onMutate (satır 687-700) seçimi anında SONRAKİ topa ilerletiyor. Fabrika tabletinde kazara çift basışta ikinci basış aynı ekran konumundaki aynı butona düşer ama artık YENİ seçili topu hedefler: operatör hiç incelemediği topu KK2-tamam işaretler veya açık kumaşı Tambur'a yollar. Daha kötüsü: son topta ilk basış allQc2Done'u true yapar ve footer aynı konumda "Adımı Kapat — Toplar Tambur'a" butonuna dönüşür (satır 1349-1367) → ikinci basış finishStep'i tetikleyip TÜM kartı Tambur'a taşır. Koddaki "Optimistic update rulayı listeden düşürdüğü için double-press riski yok" varsayımı yanlış — buton aynı yerde kalıp hedef değiştiriyor.

**Önerilen düzeltme:** Optimistic ilerleme korunarak butona kısa (ör. 700ms) press-throttle/cooldown ekle (ref tabanlı, isPending'e bağlanmadan offline-uyumlu); özellikle footer'ın "KK2 Tamamla"→"Adımı Kapat" dönüşümüne küçük bir gecikme/onay koy.

<details><summary>Doğrulayıcı kararları</summary>

- Çekirdek mekanizma kodla doğrulandı: buton disabled bağsız (1353-56/1372-75/1399-1400, StationActionButton'a loading/disabled geçilmiyor, hiçbir debounce yok) ve onMutate seçimi anında sonraki topa ilerletiyor (460-470, 687-700) → çift basış incelenmemiş topu KK2-tamam/KURSUN_APPLIED işaretler veya sonraki açık kumaşı Tambur'a yollar; per-roll geri al da kaldırılmış. Ancak iddianın en ağır senaryosu YANLIŞ: son topta next=undefined → selectedRollId=null (satır 470) → footer tamamen unmount olur ('Top seçilmedi' boş durumu, 1176-1183; refetchActiveJob 242-250 selectedRollId'yi geri doldurmaz), yani çift basış 'Adımı Kapat'ı tetikleyemez; o buton ancak zaten her şey bitmişken (Kumaşı Bitir son açık kumaş yolu) görünür ki o anda adımı kapatmak doğal sonraki aksiyondur. Tambur per-roll insan kontrol noktası + reopen kurtarması olduğundan şiddet orta.
- Çekirdek mekanik kod kanıtlı doğru: KursunQcScreen.tsx 1401-1411/1376-1383 butonları disabled/debounce'suz, completeQc2Mutation.onMutate (460-471) ve kursunFinishMutation.onMutate (687-700) seçimi senkron sonraki topa ilerletiyor, useState re-render bir frame içinde gelir → çift basışın ikincisi aynı konumdaki butonla hiç incelenmemiş topu KK2-tamam işaretler/Tambur'a yollar; yorumdaki "çift basış riski yok" varsayımı yanlış. ANCAK en ağır senaryo hatalı: son topta selectedRollId null olur (satır 470) ve footer yalnız selectedRoll non-null dalında render edildiğinden (satır 1177 "Top seçilmedi" boş durumu) "Adımı Kapat" ikinci basışta ASLA görünmez — kartın topluca Tambur'a gitmesi mümkün değil; ayrıca yanlış işaret kart re-scan→onaylı reopen ile telafi edilebilir.
</details>

### F-11 — Tambur: otomatik bitiş (remaining<0.1) yanıt anındaki seçili topu finalize ediyor — kesilen topu değil (yüksek)
**Konum:** `mobil/src/screens/Modules/Tambur/TamburScreen.tsx:597`

cutOpenFabricMutation.onSuccess'te `if (remaining < 0.1 && selectedRoll) finalizeOpenFabricMutation.mutate({ rollId: selectedRoll.rollId, remainingAction: 'discard' ... })` (satır 596-603) kesim YANITI geldiği andaki `selectedRoll`'u kullanıyor; oysa kesilen top `variables.rollId`'de mevcut. "Kalanı Kes" (boş input) her zaman remaining<0.1 bırakıp bu yolu tetikliyor (satır 934-939: floor ile kesim). Kes butonu pending'de kilitli ama sağ kolondaki top listesi (BranchGroupedRollList, onSelect=selectRoll satır 1386-1390) kilitli DEĞİL: operatör kesim isteği uçuştayken sıradaki açık kumaşa tıklarsa, yanıt gelince finalize YENİ seçilen kumaşa gider — o kumaşın tüm kalan metrajı 'discard' ile sessizce yok edilir, optimistic update listeden düşürür ve "Tambur tamamlandı" başarı toast'ı gösterilir.

**Önerilen düzeltme:** onSuccess imzasındaki `variables.rollId`'yi kullan (selectedRoll yerine); aynı deseni finalizeMutation.onSuccess'teki activeJob/selectedRollId kullanımı için de gözden geçir.

<details><summary>Doğrulayıcı kararları</summary>

- Doğrulandı: TamburScreen.tsx:597-603 auto-finalize, onSuccess imzasında mevcut variables.rollId yerine render-güncel selectedRoll.rollId kullanıyor; react-query 5.100.14 mutation options'ı her render'da güncellediğinden (mutationObserver.ts:92 → mutation.ts:246) uçuş sırasında listeden (BranchGroupedRollList, selectRoll 470-480 — pending kilidi YOK) başka açık kumaş seçilirse finalize yeni topa gider. Backend de korumaz: tambur.service.ts:2235-2291 guard'ları (barkod null + TAMBUR step + IN_PRODUCTION) yanlış topu da geçirir ve action='discard' kalan metrajın tamamını eşiksiz tüketir; optimistic onMutate başarı toast'ıyla sessiz veri kaybı. Tek hafifletici dar zaman penceresi (tek LAN isteği), ama 'Kalanı Kes' tam da operatörün sıradaki topa geçtiği an.
- Bulgu kodla birebir doğrulandı: TamburScreen.tsx:597-603 finalize'ı `variables.rollId` yerine yanıt-anı `selectedRoll`'una atıyor; TanStack Query 5.100.14 pending mutation'a son render closure'ını basıyor (mutationObserver.ts:91-93 → mutation.ts:246), selectRoll (satır 470) ve sağ kolon top listesi (1386-1390) kesim pending'inde kilitsiz, "Kalanı Kes" floor'u (934-939) her seferinde remaining<0.1 bırakıyor ve backend finalizeOpenFabric (tambur.service.ts:2202-2291) hiç kesilmemiş açık kumaşı 'discard' ile guard'sız tüketiyor — yanlış kumaşın tüm metrajı "Tambur tamamlandı" toast'ıyla sessizce yok olur. Tek hafifletici: pencere tek HTTP isteği süresi (LAN'da genelde <1sn), ama seri kesimde sıradaki topa dokunmak operatörün doğal refleksi.
</details>

### F-12 — 401'de sessiz logout — offline kuyruktaki istasyon kayıtları kalıcı kaybolur (yüksek)
**Konum:** `mobil/src/services/api.ts:45`

api.ts response interceptor'ı login dışı HER 401'de onUnauthorized → clearAuth çağırır (api.ts:45-47, RootNavigator.tsx:51-56) — token+user storage'dan silinir, operatör hiçbir 'oturum süresi doldu' mesajı görmeden Login'e düşer (clearAuth'ta toast yok, authStore.ts:27-31). Asıl yakıcı senaryo offline kuyruk: OFFLINE_AWARE mutation'lar (mutations.ts:56-60, networkMode:'online' + retry:3) 401'i de retry eder; ilk 401 token'ı sildiği için kalan denemeler de 401 alır → mutation 'error' durumuna düşer ve TanStack persist yalnız isPaused mutation'ları sakladığından (queryClient.ts:8-9) kuyruktaki KK1 girişleri / QC2 tamamlama / fason-kartela sevk kayıtları SESSİZCE kaybolur (mutationCache-level onError handler yok, ekran logout'la unmount olduğu için hook onError'ları da çalışmaz). 8 saatlik vardiyada (auth.sessionDurationHours=8 default) token vardiya ortasında dolarsa veya admin cihazı/kullanıcıyı pasifleştirirse: online'da o anki aksiyon kaybolur + sessiz logout; offline çalışıp online'a dönen operatörün TÜM bekleyen kuyruğu kaybolur — operatör kayıtların girildiğini sanır.

**Önerilen düzeltme:** 401'de logout öncesi 'Oturum süresi doldu, tekrar giriş yapın' toast'ı; offline-aware mutation'lar için 401'i retry'dan ayır (retry fonksiyonunda status===401 → false) ve mutationCache'te 401'le düşen station mutation'larını paused'a geri alıp re-login sonrası resumePausedMutations ile tekrar dene; ek olarak JWT exp decode edip dolmadan uyarı göster.

<details><summary>Doğrulayıcı kararları</summary>

- Bulgu kodla birebir doğrulandı: api.ts:45-47 login dışı her 401'de onUnauthorized→clearAuth (RootNavigator.tsx:51-56, authStore.ts:27-31 — hiçbir mesaj yok); OFFLINE_AWARE retry:3 401'i de yeniden dener (mutations.ts:56-60), ilk 401 token'ı sildiği için mutation 'error'a düşer ve persist yalnız isPaused saklayıp (mutations.ts:8-9 kendi yorumu) errored kaydı AsyncStorage'dan düşürür; mutationCache/defaults onError yok, logout Main'i unmount eder. Backend 8h JWT (auth.service.ts:62-65,73-78) + DEVICE_INACTIVE 401 (device.middleware.ts:52,67) gerçek tetikleyiciler; App.tsx:80-82 + onlineManager resume auth-kontrolsüz çalışır — fason sevk gibi fiziken yapılmış işlemlerin kaydı (mutations.ts:135-138 senaryosu) sessizce ve kalıcı kaybolur, koruyucu başka katman bulunamadı.
- Bulgu kod düzeyinde doğru, zincirin her halkası teyit edildi: (1) login-dışı her 401 onUnauthorized→clearAuth (api.ts:45-47, RootNavigator.tsx:51-56; toast yok, authStore.ts:27-31 sadece storage siler) ve loadStoredAuth (authStore.ts:33-43) süre kontrolü yapmadığından operatör süresi dolmuş token'la offline çalışmaya devam edebilir; backend süresi dolan JWT'ye 401 döner (auth.service.ts:73-77), pasif cihaz da 401 DEVICE_INACTIVE (device.middleware.ts:52,67). (2) OFFLINE_AWARE retry:3 düz sayı (mutations.ts:56-60) → 401 de retry edilir; ilk 401 clearAuth ile token'ı sildiğinden kalan denemeler "Token bulunamadı" 401 alır (api.ts:24-25) ve mutation terminal 'error' durumuna düşer (~7sn retry penceresi, re-login yetişemez). (3) Persister default shouldDehydrateMutation yalnız isPaused saklar (mutations.ts:8-9, queryClient.ts:34-38) → error'a düşen kuyruk AsyncStorage'dan ~1sn'de silinir, restart bile kurtarmaz; mutationCache-level onError YOK (queryClient.ts:17-32), ekran callback'leri user=null unmount'uyla ölür, SyncStatusChip sadece status:'pending' gösterir (hooks.ts:28), login/logout'ta hiçbir resume/kurtarma yolu yok. Ağırlaştırıcı: KK1Screen.tsx:396-402 onMutate'te optimistik "Top kaydedildi / Çevrimdışı — sync bekliyor" toast'ı basar — operatöre kayıt açıkça "kaydedildi" denmiş olur. auth.sessionDurationHours=8 default'u vardiya uzunluğuyla çakıştığından senaryo gerçekçi.
</details>

### F-13 — Çuval Depo sevk/geri alma — sevkiyat listesi ve sipariş durumu invalidate edilmiyor (orta)
**Konum:** `Electron/src/pages/Operations/SackStore/SackStorePage.tsx:101`

dispatch/unready/move-to-door/pull-back mutation'ı onSuccess'te yalnız `["sack-store"]` + `["sack-contents"]` invalidate ediyor. Oysa backend'de dispatch Shipment'ı DISPATCHED + topları SHIPPED yapar; unready ise reverseCommitTx→recomputeOrderStatusForOrders çalıştırır (Teks-Erp/src/services/shipping.service.ts:1676) yani sipariş durumu/karşılanması değişir. `["shipments"]` (ShipmentsPage durum kolonu, columns.tsx:36), `["shipment-detail", id]` ve `["orders"]` hiç invalidate edilmediğinden: depocu 'Sevk Et' yapar, açık Sevkiyat sekmesi sevki hâlâ AT_DOOR gösterir; 'Hazırlığa Geri Al' sonrası satışçının Sipariş listesi siparişi hâlâ karşılanmış statüde gösterir → müşteriye yanlış bilgi.

**Önerilen düzeltme:** onSuccess'e `["shipments"]` ve `["shipment-detail", action.shipment.id]` ekle; unready (ve dispatch) için `["orders"]` da invalidate et. Roll tabloları için 2. bulgudaki key düzeltmesi sonrası `["rolls"]` eklemek yeterli.

<details><summary>Doğrulayıcı kararları</summary>

- Mekanik doğru ve kodla kanıtlı: SackStorePage.tsx:101-102 yalnız ["sack-store"]+["sack-contents"] invalidate eder; backend dispatch DISPATCHED+SHIPPED (shipping.service.ts:1903/1928), unready reverseCommitTx→recomputeOrderStatusForOrders (1846→1676) ile sipariş durumunu değiştirir; TabHost tüm sekmeleri mount tutup (TabHost.tsx:11-12) refetchOnWindowFocus:false (App.tsx:21) olduğundan arka planda açık Sevkiyat sekmesi gerçekten bayat AT_DOOR gösterir — kardeş kod (OrderCancelDialog:102-103) çapraz invalidate konvansiyonunu zaten uyguluyor. Ancak etki abartılı: useDataTable staleTime:0 + refetchOnMount:"always" (useDataTable.ts:156-157) sayesinde her taze navigasyon doğru veriyi çeker (sorun SADECE aynı istemcide önceden açık arka-plan sekmesi); "satışçının sipariş listesi" senaryosu farklı istemci olduğundan invalidation ile zaten düzelmez (poll'suz LAN uygulamasının genel özelliği); backend guard'ları (cancel DISPATCHED reddi :2058) bayat UI'dan veri bozulmasını engeller, kurtarma mevcut RefreshButton.
- Bulgu kod düzeyinde doğru: SackStorePage.tsx:101-102 yalnız ["sack-store"]+["sack-contents"] invalidate ediyor, backend dispatch DISPATCHED+SHIPPED (shipping.service.ts:1875-1928) ve unready recomputeOrderStatusForOrders (1676) çalıştırıyor; TabHost tüm sekmeleri mount tuttuğundan (TabHost.tsx:11-13) açık Sevkiyatlar/Siparişler sekmesi refetchOnMount:"always"a rağmen ASLA kendiliğinden tazelenmez (refetchOnWindowFocus:false, App.tsx:21) ve OrderCancelDialog.tsx:102-103'teki çapraz-domain invalidate konvansiyonu burada kırılmış. Ancak şiddet abartılı: "satışçı başka makinede yanlış görür" senaryosu invalidation ile düzelmez (per-client cache, satışçı sekmeyi açınca zaten taze çeker) ve backend atomik claim'ler (unmarkReady 1834-1846, dispatch ~1897) bayat UI'dan yanlış YAZMA yapılmasını 409 ile engelliyor — kalan risk aynı istemcide iki sekme açıkken salt-görüntü bayatlığı, RefreshButton kaçışı mevcut.
</details>

### F-14 — Sipariş iptal preview'ındaki activeShipments/canCancel frontend'de tamamen yok sayılıyor (orta)
**Konum:** `Electron/src/pages/Operations/Orders/OrderCancelDialog.tsx:200`

Backend cancel-preview, somut engel listesi olarak `activeShipments` + `canCancel` döner (Teks-Erp/src/services/order.service.ts:1461-1474) ve cancelWithActions aktif sevkiyat varken her koşulda 409 atar (order.service.ts:1493 → assertNoActiveShipments). Ancak Electron tarafında `OrderCancelPreview` tipi (Orders/service.ts:30-34) bu iki alanı hiç içermiyor ve dialog'un onay butonu `disabled={isLoading || hasError || cancelMut.isPending}` (satır 200) — canCancel'a bakmıyor. Senaryo: sipariş aktif bir sevkiyata bağlı + bağlı WO yokken dialog 'Bu siparişe bağlı iş emri yok. Onayladığında sadece sipariş İptal durumuna geçer.' (satır 150-154) diye AKTİF OLARAK YANLIŞ bilgi verir; operatör onaylar, backend'den 409 toast'ı yer, dialog'da hiçbir engel gösterilmez. Backend'in 'somut listele' diye hazırladığı veri çöpe gidiyor.

**Önerilen düzeltme:** OrderCancelPreview tipine activeShipments+canCancel ekle; canCancel=false iken onay butonunu disable et ve WorkOrderCancelDialog'daki blockReason deseni gibi engelleyen sevkiyatları (shipmentNo+status) inline listele.

<details><summary>Doğrulayıcı kararları</summary>

- Tüm iddialar kodla doğrulandı: backend preview activeShipments+canCancel döner (order.service.ts:1463-1473, yorum "preview operatöre nedeni gösterir"), Electron tipi bu alanları içermez (Orders/service.ts:30-34), onay butonu canCancel'a bakmaz (OrderCancelDialog.tsx:200) ve WO'suz+aktif sevkiyatlı siparişte dialog "sadece sipariş İptal durumuna geçer" diye yanlış metin gösterir (satır 150-154); başka gating katmanı da yok (OrdersPage.tsx:272-279 sadece COMPLETED/CANCELLED gizler). Ancak şiddet düşürülmeli: backend 409 guard'ı (order.service.ts:1493→assertNoActiveShipments:1243-1245) yanlış iptali imkansız kılar ve 409 mesajı somut sevkiyat no/durum listesi + çözüm talimatı içerir, apiClient.ts:85-87 bunu toast olarak operatöre gösterir — yani "hiçbir engel gösterilmez" kısmı abartı, etki veri riski değil yanıltıcı dialog metni + boşa onay denemesi düzeyinde UX sorunu.
- Tüm iddialar kodla doğrulandı: backend preview activeShipments/canCancel döner (order.service.ts:1463-1473, yorum 'preview operatöre nedeni gösterir' frontend gösterimini amaçladığını kanıtlıyor), Electron tipi (Orders/service.ts:30-34) bu alanları içermiyor, buton (OrderCancelDialog.tsx:200) canCancel'a bakmıyor ve WO'suz dalda (150-154) yanlış 'sadece sipariş İptal olur' metni gösteriliyor; upstream gating de yok (OrdersPage.tsx:310). Ancak şiddet abartılı: assertNoActiveShipments cancelWithActions'ın İLK satırı (order.service.ts:1493) — 409 her yan etkiden önce atılır (veri riski sıfır) ve 409 mesajının kendisi somut sevkiyat listesi+çözüm içerir, apiClient interceptor (apiClient.ts:85-87) bunu toast'lar; operatör somut engeli onay-sonrası da olsa görür. Net etki = yanıltıcı bir cümle + boşa giden bir tık, yıkıcı risk değil UX/konvansiyon boşluğu.
</details>

### F-15 — Arka plan sekmesinde açık kalan irsaliye dialoğu baskıda aktif belgenin ÜZERİNE biner (orta)
**Konum:** `Electron/src/index.css:428`

Pasif sekmeler `invisible` (visibility:hidden) ile gizlenir ama mount kalır ve içindeki açık modal de yaşar (TabHost.tsx:49-59 — bu, 'modal açıkken başka sekmeye bakma' özelliğinin bilinçli tasarımı). Print CSS `.print-area, .print-area * { visibility: visible }` kuralı elemana DOĞRUDAN uygulandığı için atadan miras alınan hidden'ı ezer → arka plan sekmesindeki açık irsaliyenin print-area'sı da baskıda görünür olur ve ikisi de `absolute top:0 left:0` ile aynı koordinata oturur. Senaryo (özelliğin teşvik ettiği akış): sekme 1'de Sevk İrsaliyesi açık, referans için sekme 2'ye geçip Fason Sevk İrsaliyesi açılır, Yazdır → iki resmi belge üst üste binmiş tek çıktı.

**Önerilen düzeltme:** Print kuralını aktif sekmeyle sınırla (`.tab-pane:not(.invisible) .print-area` veya pasif pane'e `print:hidden`/`display:none` @media print) — ya da bulgu 1'deki iframe çözümü bunu da kökten giderir.

<details><summary>Doğrulayıcı kararları</summary>

- Bulgu doğru ve mekanizma kendini kanıtlıyor: print hilesi zaten `body * {visibility:hidden}` ile .print-area'nın TÜM atalarını gizlerken belgeyi basabiliyor (index.css:425-431) — yani alt elemandaki açık `visibility:visible` ata hidden'ını eziyor; pasif sekmenin `invisible` sınıfı (TabHost.tsx:59) aynı türden bir ata-hidden olduğundan arka plan sekmesindeki açık irsaliyenin print-area'sını bastıramaz. Dialog sekme pane'ine portallanır (dialog.tsx:44), 4 resmi belge sheet'inde `print-area` statik className (ShipmentDispatchNote.tsx:157, ShipmentFrozenSheet.tsx:57, FasonSevkSheet.tsx:58, KartelaCekiSheet.tsx:77), hiçbir beforeprint/print: guard veya main-process print yakalama yok; iki pane kardeş `absolute inset-0` (TabHost.tsx:34-37) olduğundan iki print-area aynı koordinata (top:0 left:0) basılır.
- Bulgu doğru: pasif sekme yalnız visibility:hidden ile gizlenir (TabHost.tsx:59 "invisible", mount kalır) ve index.css:428 ".print-area, .print-area *{visibility:visible}" elemana doğrudan uygulandığından atadan miras hidden'ı ezer — aktif dialoğu basılabilir kılan mekanizmanın TA KENDİSİ arka plandakini de açar. .print-area sınıfı sheet kökünde statiktir (FasonSevkSheet.tsx:58, ShipmentFrozenSheet.tsx:57, KartelaCekiSheet.tsx:77), baskı window.print() ile aynı pencerede (FasonSevkPrintDialog.tsx:149), beforeprint/print:hidden gibi başka koruyucu katman yok; iki .print-area da absolute top:0'da çakışır. Tek hafifletici: çıktı bariz karman olur, operatör fark edip yeniden basar — bu yüzden şiddet orta.
</details>

### F-16 — Fason irsaliyesi: canlı overlay (renk + boyahane notu) sorgusu HATA verirse fiş renksiz/notsuz basılıyor (orta)
**Konum:** `Electron/src/pages/Operations/WorkOrders/FasonSevkPrintDialog.tsx:75`

`loading` yalnız `overlayQuery.isLoading`'i bekliyor (satır 69-72); sorgu retry'lar sonrası error'a düşerse `overlay` undefined kalır ve `sheetData` yine kurulur: `requestedColor: overlay?.requestedColor ?? null`, `dyehouseNote: null` (satır 75-84). Sheet bu durumda kutuya 'İş emrinde hedef renk belirtilmemiş.' basar (FasonSevkSheet.tsx:108-119) ve boyahane notu kutusu hiç çıkmaz; Yazdır butonu `disabled={!sheetData}` olduğundan aktiftir. Senaryo: geçici ağ/500 hatasında operatör boyahaneye 'renk belirtilmemiş' yazan talimat fişini basıp malla gönderir — yanlış/eksik boya talimatı.

**Önerilen düzeltme:** `overlayQuery.isError`'da sheet render'ını ve Yazdır'ı blokla (hata bandı + 'Yeniden dene'); 'renk belirtilmemiş' metni yalnız overlay BAŞARILI ve requestedColor gerçekten null iken basılmalı.

<details><summary>Doğrulayıcı kararları</summary>

- Kod iddiayı doğruluyor: FasonSevkPrintDialog.tsx:69-84'te loading yalnız isLoading'e bakar (v5'te error'a düşünce false), overlayQuery.isError hiçbir yerde kontrol edilmez, sheetData requestedColor:null/dyehouseNote:null ile kurulur ve Yazdır (satır 148) aktif kalır; FasonSevkSheet.tsx:108-119 bu durumda 'İş emrinde hedef renk belirtilmemiş.' basar, not kutusu (satır 123) hiç çıkmaz. Tek koruma apiClient'ın geçici 'Sunucu hatası' toast'ı (apiClient.ts:88-91) — baskıyı engellemez ve fallback metin gerçek 'renksiz WO' durumundan ayırt edilemez. Ancak pencere iddiadakinden dar: tam ağ kesintisinde docQuery de düşer (print disabled); kötü baskı için doc'un cache'den/başarılı gelip overlay'in İLK fetch'inin hata vermesi gerekir (overlay daha önce yüklendiyse RQ bayat veriyi korur) — bu yüzden şiddeti orta öneriyorum.
- Kod iddiayı doğruluyor: FasonSevkPrintDialog.tsx:69-72'de loading yalnız isLoading'leri kapsar (error'da false), overlayQuery.isError hiçbir yerde işlenmez; donmuş FasonDispatchDoc (service.ts:259-287) requestedColor/dyehouseNote içermediğinden overlay hatasında sheetData null renk/null notla kurulur (satır 78-84), FasonSevkSheet.tsx:108-117 'İş emrinde hedef renk belirtilmemiş.' basar ve Yazdır aktif kalır (satır 148, disabled={!sheetData}). Tek koruma apiClient'ın geçici 5xx toast'ı (apiClient.ts:88-91) — baskıyı bloklamaz; ayrıca tam ağ kesintisinde docQuery de düşüp fişi kapattığından pencere yalnız dye-overlay endpoint'inin spesifik hatasına daralır.
</details>

### F-17 — Arka plan sekmesinde açık dialog varken aktif sekmede Esc tamamen ölü kalıyor (orta)
**Konum:** `Electron/src/components/ui/dialog.tsx:58`

Radix Esc'i yalnız EN ÜST layer'a verir (`isHighestLayer = index === context.layers.size - 1`, dismissable-layer dist/index.mjs:60-67); alt layer'lar hiç görmez. dialog.tsx:56-59 pasif sekmedeki dialog'da Esc'i preventDefault eder (doğru — kapanmamalı) ama event aktif sekmenin dialog'una asla ulaşmaz. Senaryo: Sekme A'da dialog aç → Sekme B'ye geç, dialog aç → A'ya dön (B'ninki açık/mount, daha sonra açıldığı için 'highest') → A'daki dialog Esc ile kapanmaz, operatör için Esc sebepsizce ölüdür; B'deki dialog kapatılana kadar sürer.

**Önerilen düzeltme:** Esc dismiss'ini Radix'e bırakma: tüm scoped dialoglarda onEscapeKeyDown'u preventDefault edip AppShell/TabHost'ta tek global Esc handler'ı ile yalnız AKTİF sekmenin en üstteki açık dialog'unu programatik kapat.

<details><summary>Doğrulayıcı kararları</summary>

- Bulgu kodla doğrulandı: @radix-ui/react-dismissable-layer@1.1.11 dist/index.mjs:59-67 Esc'i yalnız en son mount edilen layer'a verir (isHighestLayer, layers Set mount sırası; listener document-capture olduğu için sekme görünürlüğü etkisiz); TabHost.tsx:49-59 pasif sekmeyi sadece invisible yapar (dialog mount kalır) ve dialog.tsx:56-59 pasif sekmedeki (highest) dialog'da preventDefault eder — aktif sekmedeki dialog event'i hiç almaz, src'de telafi eden global Escape handler yok (grep: yalnız CopyContextMenu kendi menüsü için). Ancak operatör kilitlenmez: X butonu, iptal butonları ve karartmaya tıklama (pointer-down-outside highest-layer kapısından geçmiyor) çalışmaya devam eder; senaryo ayrıca arka plan dialog'unun daha sonra açılmış olmasını gerektirir.
- Bulgu kod düzeyinde doğru: tüm sekmeler mount kalır (TabHost.tsx:51-70), tüm dialog'lar Radix'in modül-düzeyi varsayılan DismissableLayerContext'indeki TEK layers Set'ini paylaşır (dismissable-layer dist/index.mjs:16-20,77) ve Esc yalnız en üst layer'a gider (dist:59-67 `isHighestLayer`); arka plan sekmesinin dialog'u en üstteyse dialog.tsx:58 preventDefault eder, kendi dismiss'i de atlanır (dist:63), aktif sekmenin daha eski dialog'una event hiç ulaşmaz — Esc her yerde no-op olur, telafi eden global Esc handler yok. Ancak operatör kilitli kalmaz: X butonu (dialog.tsx:85-88) ve overlay tıklaması (pointerDownOutside yolunda highest-layer kısıtı YOK, dist:43-50) dialog'u kapatır; senaryo da spesifik sıra gerektirir (A'da dialog aç → B'de dialog aç ve açık bırak → A'ya dön). Gerçek ama yalnız klavye-konfor kaybı → düşük şiddet.
</details>

### F-18 — window.print() arka plan sekmesinde açık bırakılan irsaliye önizlemesini de basıyor (üst üste binmiş resmi belge) (orta)
**Konum:** `Electron/src/index.css:424`

@media print kuralı `body * {visibility:hidden}` + `.print-area, .print-area * {visibility:visible}` ve `.print-area {position:absolute; left:0; top:0}` (index.css:424-438) — explicit `visibility:visible`, pasif sekmenin `invisible` (visibility:hidden) sınıfını da ezer. Dört belge sheet'i (.print-area: FasonSevkSheet.tsx:58, KartelaCekiSheet.tsx:77, ShipmentDispatchNote.tsx:157, ShipmentFrozenSheet.tsx:57) tab-scoped, mount-kalan dialoglarda render edilir ve `window.print()` kullanır (örn. FasonSevkPrintDialog.tsx:149). Senaryo: operatör Sekme A'da sevk irsaliyesi önizlemesini açık bırakıp (tasarımın desteklediği akış) Sekme B'de fason irsaliyesi açıp Yazdır'a basar → iki .print-area da görünür ve aynı koordinata absolute konumlandığı için resmi belge çıktısı üst üste binmiş/bozuk basılır; sebebi teşhis etmesi çok zordur.

**Önerilen düzeltme:** Yazdır butonunda print öncesi yalnız o dialog'un print-area'sına geçici bir işaret (örn. data-printing) koyup CSS'i `.print-area[data-printing]`e daraltın; ya da TravelerCardPrintDialog'daki gibi izole iframe/PDF yoluna geçin.

<details><summary>Doğrulayıcı kararları</summary>

- Çürütülemedi — tüm zincir kodla doğrulandı: TabHost.tsx:59 pasif sekmeyi `invisible` (visibility:hidden) ile gizler ve açık dialog MOUNT kalır (dosyanın kendi yorumu da söylüyor); dialog.tsx:61-70 başka sekmeye tıklamanın modalı kapatmamasını bilerek garanti eder; index.css:428-430 `.print-area * {visibility:visible}` CSS spec gereği atadaki visibility:hidden'ı ezer (display:none olsaydı ezemezdi); 4 sheet `.print-area` taşır ve 3 çağrı yeri çıplak window.print() kullanır; hiçbir beforeprint/afterprint koruması yok (grep boş). İki sekmede iki irsaliye önizlemesi açıkken yazdırma, iki .print-area'yı aynı koordinata basar.
- Bulgu doğrulandı: pasif sekme `invisible` (visibility:hidden, TabHost.tsx:59) ile gizlenir ve dialog sekme pane'ine portallanıp mount kalır (dialog.tsx:44); `.print-area * {visibility:visible}` (index.css:428-431) ata visibility:hidden'ı CSS gereği ezer — headless Chromium print-to-PDF ile aynı yapıyı kurup İKİ print-area'nın üst üste bastığını fiilen reprodüke ettim (glifler görünür şekilde binişik). Hiçbir katman korumuyor (beforeprint/afterprint/tab-active guard yok); ek olarak DocumentPreview.tsx:21-25 mock verili sheet'i koşulsuz print-area ile inline render eder — Belge Şablonları sayfası arka sekmede açıkken bile her baskıyı bozar.
</details>

### F-19 — Global 15 sn timeout: büyük yedek indirme/ağır rapor kesilir ve sunucu yanlışlıkla 'offline' işaretlenir (orta)
**Konum:** `Electron/src/services/apiClient.ts:23`

`timeout: 15_000` tüm isteklere uygulanıyor ve hiçbir çağrı override etmiyor (tek istisna ApiEndpointDialog'un ayrı axios'u). `downloadBackup` (pages/System/Backups/service.ts:31, responseType:'blob') DB büyüdükçe 15 sn'yi aşan dosyada her seferinde kesilir — off-site yedek kopyası alınamaz hale gelir; backend DB tarafında 50 sn statement_timeout'a izin verirken geniş tarihli raporlar da istemcide 15 sn'de ölür. Üstelik timeout'ta `error.response` yok → apiClient.ts:60 `markUnreachable()` çağrılır: sunucu ayaktayken sidebar kırmızı 'offline' olur ve operatöre 'Sunucuya ulaşılamıyor.' (apiClient.ts:91) denir — yanlış teşhis.

**Önerilen düzeltme:** Uzun transferlere per-request timeout override ver (download/rapor için 0 veya 120s); interceptor'da `error.code === 'ECONNABORTED'` (timeout) durumunu markUnreachable'dan ayır — timeout ≠ sunucu kapalı.

<details><summary>Doğrulayıcı kararları</summary>

- Mekanizma kod-kanıtlı ve korumasız: apiClient.ts:23 global 15sn timeout'u renderer'da hiçbir çağrı override etmiyor (grep doğrulandı), downloadBackup (Backups/service.ts:31, blob) XHR adapter'da tüm gövde transferine uygulanır ve backend res.download (admin.routes.ts:834) pg_dump -Fc dosyasını saf transferle akıtır; timeout'ta error.response yok → apiClient.ts:60 markUnreachable + 'Sunucuya ulaşılamıyor' toast'ı gerçekten atılıyor. Ancak etki abartılı: kopma ancak dosya ~180MB (100Mbps) / ~1.5GB (gigabit) eşiğini aşınca tetiklenir (bugünkü test verisinde çalışır), sahte 'offline' useServerClock.ts heartbeat'iyle ≤30sn'de kendini düzeltir, yedekler sunucu diskinde durur (veri kaybı yok, sadece off-site kopya yolu), 'raporlar da ölür' kısmı somut endpoint'siz spekülatif.
- Tüm mekanik iddialar kodla doğrulandı: apiClient.ts:23 global 15s timeout, grep ile hiçbir çağrı override etmiyor (tek istisna gerçekten ApiEndpointDialog'un ayrı axios'u), Backups/service.ts:31 blob indirme override'sız (XHR timeout tüm gövdeyi kapsar → 15s'i aşan transfer her denemede kesilir, alternatif katman yok), backend statement_timeout=50s gerçek, ve timeout'ta error.response yok → apiClient.ts:60 markUnreachable() tek hatada sidebar'ı offline yapıp ts:91 'Sunucuya ulaşılamıyor.' der. Tek hafifletici: offline işareti useServerClock.ts heartbeat'i ile ≤30s'de kendini düzeltir ve bugünkü test-verisi yedekleri LAN'da 15s eşiğinin (gigabit ~1.8GB, 100Mbps ~180MB) muhtemelen çok altında — yani bulgu gerçek ama bugün yakıcı değil, DB büyüdükçe deterministik patlayan latent kusur.
</details>

### F-20 — Dashboard widget'ları izin kontrolsüz — dar yetkili kullanıcı login olur olmaz 403 toast fırtınası (orta)
**Konum:** `Electron/src/pages/Dashboard/KpiCards.tsx:88`

Dashboard index rotası herkese açık (content-routes.tsx:89) ve canEnterApp 'en az 1 permission' (types/auth.ts:33-35) — yani sadece mobile:kk1 veya shipping:read gibi tek izinli kullanıcı da girer. KpiCards 5 sorguyu (order:read, workorder:read, roll:read×3 — dashboardService.ts), StationLoad /api/dashboard/stations/live-state'i (backend station:read, dashboard.routes.ts:61), 3 panel de order/workorder sorgularını hiçbir enabled/permission kapısı olmadan ateşler. apiClient her 403'te toast basar (apiClient.ts:80-82) ve retry:1 (App.tsx:20) her başarısız sorguyu iki kez koşturur → depo/sevkiyat/mobil profilli operatör her açılışta 8-16 'Bu işlem için yetkiniz bulunmuyor' toast'ı yer; KPI kartları hata ikonu ile kalır.

**Önerilen düzeltme:** Her widget'a useRoleAccess ile enabled kapısı koy (izin yoksa sorguyu hiç atma, kartı gizle ya da 'yetki yok' placeholder'ı göster); dashboard sorgularına suppressErrorToast ekle.

<details><summary>Doğrulayıcı kararları</summary>

- Tüm zincir kodla doğrulandı: content-routes.tsx:89 index rotası gate'siz, KpiCards.tsx:88 + 4 panel useQuery'si enabled/permission kapısız ve suppressErrorToast'sız; backend order:read (order.routes.ts:249), station:read (dashboard.routes.ts:58) gibi katı izinler 403 döner; apiClient.ts:80-83 her 403'te dedupe'suz toast basar ve App.tsx:20 retry:1 403'ü de tekrar dener → shipping:read-only kullanıcı 9 sorgu × 2 = 18, mobile:depo kullanıcı 12 toast yer. Tek minör hata: KPI kartı hata ikonu değil '—' gösterir (KpiCards.tsx:146).
- Bulgu kodla doğrulandı: index rotası gate'siz (content-routes.tsx:89, TabHost.tsx:22-27 ilk sekme "/"), canEnterApp=permissions.length>0 (auth.ts:33-35), Dashboard klasöründe hiçbir permission/enabled kapısı yok (grep boş), backend 4 endpoint gerçekten 403 döner (order.routes.ts:249 order:read; workorder.routes.ts:47; inventory.routes.ts:129; dashboard.routes.ts:58-61 station:read), apiClient.ts:80-82 her 403'te toast basar (dashboard sorgularında suppressErrorToast yok) ve App.tsx:20 retry:1 403'ü de tekrarlar → shipping:read-only kullanıcıda 9 sorgu × 2 = 18'e kadar toast. Tek hafifletme: sonner default 3 toast gösterir (kuyruklanır) ve KPI'lar "—"/panel "Veri alınamadı" ile zarifçe bozulur — yani fırtına görsel olarak kuyruk, veri hasarı yok.
</details>

### F-21 — İzin değişikliği oturumdaki kullanıcıya hiçbir şekilde yansımıyor; UI'da 'yeniden giriş gerekli' uyarısı da yok (orta)
**Konum:** `Electron/src/pages/Access/Users/PermissionsTab.tsx:73`

Permissions yalnız login'de JWT'den decode edilir (App.tsx:31-41 AuthHydrator); authService.getMe tanımlı ama hiçbir yerden çağrılmıyor (tek referans authService.ts:9). Backend de izinleri token'dan okur (auth.middleware.ts:32, verifyToken → JWT payload; DB'ye bakılmaz). PUT /admin/users/:id/permissions sonrası hedef kullanıcının HEM UI'ı HEM backend yetkisi oturum süresi boyunca (default 8h) eski kalır: iptal edilen yetki kullanılmaya devam eder, yeni verilen yetki görünmez. PermissionsTab kaydetmede sadece 'Yetkiler güncellendi.' der (satır 73) — admin operatörün masasına gidip 'hâlâ göremiyorum' sürprizi yaşar.

**Önerilen düzeltme:** En azından PermissionsTab onSuccess toast'ına 'kullanıcı yeniden giriş yapana kadar etkili olmaz' notu ekle; kalıcı çözüm için periyodik/odakta getMe ile permissions tazeleme veya ertelenen User.tokenVersion mekanizması.

<details><summary>Doğrulayıcı kararları</summary>

- Bulgu kod düzeyinde doğru: backend izinleri her istekte JWT payload'ından okur (auth.service.ts:73-79 salt jwt.verify, rbac.middleware.ts:47 req.user.permissions; repo genelinde tokenVersion=0 hit), setUserPermissions (permission-management.service.ts:122-175) yalnız UserPermission satırlarını değiştirir, Electron AuthHydrator (App.tsx:31-41) token'ı bir kez decode eder ve getMe hiçbir yerden çağrılmaz; PermissionsTab.tsx:73 ve tüm Access sayfalarında 'yeniden giriş' uyarısı yok. Ancak proje belleği bu eksiği (token iptali) LAN-only duruş kapsamında bilinçli ertelenmiş/acil-değil olarak kaydetmiş; pencere oturum süresiyle (default 8h) sınırlı ve tipik akış (izinsiz başlayan kullanıcıya login ÖNCESİ izin atama) etkilenmiyor — kalan yeni aksiyon, kaydetme toast'ına 'değişiklik bir sonraki girişte geçerli' notu eklemek.
- Tüm iddialar kodla doğrulandı: izinler yalnız login'de JWT'ye gömülür (auth.service.ts:53-65), backend her istekte token payload'ından okur (auth.middleware.ts:32 + rbac.middleware.ts:47, DB yok, tokenVersion backend'de hiç yok), setUserPermissions (permission-management.service.ts:122) hiçbir oturum geçersizleştirmez, Electron getMe (authService.ts:9) hiçbir yerden çağrılmıyor ve PermissionsTab.tsx:73 toast'ı yeniden-giriş uyarısı içermiyor — yani değişiklik aktif oturuma 8h boyunca iki yönde de yansımaz. Ancak proje bunu LAN-only duruşunda bilinçli ertelenmiş bir kalem olarak kayda almış (tokenVersion 'anlamlı ama acil değil'), ortam ~10 güvenilir kullanıcılı fabrika LAN'ı, workaround basit (yeniden giriş) ve süre 8h token expiry ile sınırlı; asıl eyleme dönük kısım toast'a 'yeniden girişte geçerli olur' notu eklemek.
</details>

### F-22 — İş emri formu picker'ları cross-module read izinleri istiyor — workorder:write tek başına formu kullanılamaz kılar (orta)
**Konum:** `Electron/src/pages/Operations/WorkOrders/LinkedOrderLinesField.tsx:84`

WO oluştur/düzenle sayfası yalnız workorder:write ile açılır (content-routes.tsx:312-326) ama içindeki picker'lar gating'siz başka modüllerin read iznini ister: OrderPickerDialog → GET /api/orders/wo-picker sadece order:read kabul eder (order.routes.ts:278-281; oysa kardeşi /order-lines/coverage bilinçli olarak workorder:read/write de kabul ediyor, satır 370), hedef ürün → item:read, renk → property:read, istasyon/rota/şablon → station:read, fason firma → subcontractor:read. Desktop için seed'de izin template'i yok (mobil 'Hızlı İş Emri' template'i bu read'leri bilinçli bundle'lıyor, seed.ts:136-150) — admin'in elle 'iş emri yazma' verip master-data read'lerini atlaması kolay; planlamacı formu açar, her picker açıklamasız 403 toast'a döner.

**Önerilen düzeltme:** Kısa vade: wo-picker'ı coverage gibi requireAnyPermission(order:read, workorder:read, workorder:write) yap + picker'ları hasPermission ile kapatıp 'X izni gerekli' mesajı göster; orta vade: desktop roller için seed'e master-data read'leri bundle'layan template ekle.

<details><summary>Doğrulayıcı kararları</summary>

- Tüm iddialar kodla doğrulandı: WO form route'ları yalnız workorder:write ister (content-routes.tsx:312-326); wo-picker sadece order:read kabul eder (order.routes.ts:278-281) oysa kardeş coverage endpoint'i workorder:read/write de kabul ediyor (satır 370); item/color/station/route/recipe/subcontractor liste endpoint'lerinin hiçbiri workorder izni kabul etmiyor (item.routes.ts:71, color.routes.ts:51, station.routes.ts:72, route.routes.ts:66, product-recipe.routes.ts:54, subcontractor-management.routes.ts:37); rbac.middleware'de write→read iması yok, form bileşenlerinde PermissionGate yok, seed'de desktop template yok ama mobil Hızlı İş Emri template'i (seed.ts:136-150) tam bu read'leri bundle'lıyor; üstelik apiClient.ts:80-82 403'te backend'in hangi iznin eksik olduğunu söyleyen mesajını atıp jenerik toast gösteriyor — admin eksik izni 5 modül arasında deneme-yanılmayla bulmak zorunda.
- Tüm iddialar kodla doğrulandı: form route'u yalnız workorder:write ister (content-routes.tsx:313-326), ama wo-picker sadece order:read kabul eder (order.routes.ts:278; kardeşi coverage workorder:* da kabul eder, satır 370), items=item:read|mobile:kk1, stations/rota=station:read, fason=subcontractor:read|mobil — hiçbiri workorder:* kabul etmez; rbac.middleware'de write→read iması yok, WO form bileşenlerinde picker gating'i yok, EntityPickerModal'da error state hiç yok ve apiClient 403'ü jenerik 'Bu işlem için yetkiniz bulunmuyor.' olarak gösterir (apiClient.ts:80-81) — eksik izin adı görünmez. Seed'de desktop template yok, mobil Hızlı İş Emri template'i bu read'leri bilinçli bundle'lıyor (seed.ts ~136-150) ve seed yorumu kullanıcıların yetkisiz başlayıp admin'in kademeli atayacağını söylüyor — senaryo tasarlanan iş akışının kendisi.
</details>

### F-23 — Kartela ekranlarında offline geri bildirim yok: SyncStatusChip + onMutate toast eksik, Gönder sonsuz spinner (orta)
**Konum:** `mobil/src/screens/Modules/KartelaSevk/KartelaSevkScreen.tsx:338`

SyncStatusChip yalnız 5 eski istasyon ekranında render ediliyor (FasonSevk:910, KK1:672, KursunQc:1100, FasonKabul:868, Tambur:1401) — KartelaSevk/KartelaKabul registry'de offline-aware olmasına rağmen chip'i de onMutate optimistic toast'unu da (KK1Screen:393, FasonSevkScreen:611 deseni) almamış. Offline'da Gönder'e basınca mutation pause olur, isPending=true kalır → buton disabled + ActivityIndicator sonsuz döner (satır 338-339), hiçbir 'çevrimdışı · sync bekliyor' izi yok. Operatör kaydın sıraya girdiğini bilemez; uygulamayı kapatıp açarsa bir önceki iki bulgu tetiklenir.

**Önerilen düzeltme:** İki Kartela ekranına SyncStatusChip ekle + onMutate'te 'Çevrimdışı — sync bekliyor' toast'u ve form resetini diğer istasyon ekranlarındaki gibi uygula.

<details><summary>Doğrulayıcı kararları</summary>

- Doğrulandı: KartelaSevkScreen.tsx:157 mutationKey'i mutations.ts:145-148'deki OFFLINE_AWARE (networkMode:'online') default'una bağlı → offline'da mutation pause olur, isPending=true kalır ve satır 338-339'daki ActivityIndicator süresiz döner; ekranda SyncStatusChip yok (ScreenChrome kendisi render etmiyor, sadece headerExtras slotu var) ve onMutate de yok (sadece onSuccess/onError, 168-180), global offline banner/MutationCache callback'i de bulunmuyor. Karşılaştırma deseni doğru: FasonSevkScreen.tsx:611-619 onMutate'te açık 'Çevrimdışı — irsaliyeyi ELLE yaz' toast'u + :910 SyncStatusChip gösteriyor, KK1 aynı (393/672); KartelaKabulScreen.tsx:354/386/586 aynı boşluğa sahip. Ek ağırlaştırıcı: KartelaSevk mutate()'i değişkensiz çağırıyor (mutationFn state'i closure'lıyor, 156-167) — pause'dayken app restart olursa registry default mutationFn undefined vars ile resume olur.
- Doğrulandı: KartelaSevkScreen:157 mutationKey ile mutations.ts:145-148'deki networkMode:'online' default'unu miras alır → offline'da pause, isPending=true → satır 338-339 spinner sonsuz döner + canDispatch (183) butonu kilitler; ekranda SyncStatusChip yok (grep: chip yalnız 5 istasyon ekranında), onMutate toast yok (KK1:393 / FasonSevk:611 desenine karşılık), ScreenChrome/queryClient'ta global offline göstergesi de yok — KartelaKabul (354/586) aynı durumda. Ek ağırlaştırıcı: mutate() void-variables + closure mutationFn olduğundan app restart'ta resume registry fn'ine undefined payload verir.
</details>

### F-24 — KartelaKabul uzun formu (per-top sayım + ölçümler) draft yedeksiz — LMK kill = sayım baştan (orta)
**Konum:** `mobil/src/screens/Modules/KartelaKabul/KartelaKabulScreen.tsx:214`

rows state'i (Record<rollId, RollEntry>: count + defCm/defKg + index bazlı istisna ölçümleri) + manifestNo/notes tamamen bellekte; FasonKabul'daki DRAFT_KEY/savedAt/8h-TTL deseni (FasonKabulScreen:87-280) ve İade'deki muadili buraya taşınmamış. Operatör onlarca kartelayı sayıp ölçerken Android LMK uygulamayı öldürürse her şey baştan. KartelaSevk'in okutulmuş top listesi (KartelaSevkScreen:49) ve HizliIsEmri'nin scanned listesi (NewWorkOrderView:70) da aynı şekilde yedeksiz — üçü de 'sonradan eklenen ekran draft kuralını almamış' sınıfında.

**Önerilen düzeltme:** FasonKabul'daki AsyncStorage draft desenini (debounce + savedAt + TTL + restore guard) KartelaKabul rows/manifestNo'ya, KartelaSevk top listesine ve HizliIsEmri scanned listesine uygula.

<details><summary>Doğrulayıcı kararları</summary>

- Doğrulandı: KartelaKabulScreen.tsx:214-217'de rows/manifestNo/notes salt useState, modülde hiç AsyncStorage/DRAFT_KEY yok; FasonKabulScreen.tsx:87-88+238-280 ve IadeGirisiScreen.tsx:33+101-131'deki 8h-TTL draft deseni buraya taşınmamış, offline kuyruk (offline/mutations.ts) yalnız submit-sonrası paused mutation'ı persist eder — form-öncesi state'i hiçbir katman korumuyor. KartelaSevkScreen.tsx:49 (rolls + plaka/şoför/not, üstelik BarcodeScannerModal kameralı) ve NewWorkOrderView.tsx:70 (scanned) de aynı şekilde yedeksiz. Tek hafifletici: KartelaKabul kamerasız olduğundan LMK baskısı FasonKabul'den düşük (ölüm için arka plana alınma gerekir) — bulgu doğru ama 'yüksek' yerine 'orta' şiddet uygun.
- Doğrulandı: KartelaKabulScreen.tsx:214-217'de rows/manifestNo/notes salt useState, dosyada AsyncStorage hiç yok; FasonKabul (DRAFT_KEY:87-88 + restore:238 + save:279, 8h TTL/savedAt) ve IadeGirisi (:33-34) aynı sınıf ekranlarda draft desenine sahip. Offline katmanı (queryClient persister + KARTELA_KABUL_RECEIVE registry) yalnız submit SONRASI paused mutation'ı korur, yazım halindeki form state'ini korumaz; KartelaSevk (in-memory rolls + kamera) ve HizliIsEmri de draft'sız — iddianın tüm noktaları kodla örtüşüyor. Tek hafifletici: KartelaKabul akışında kamera yok (LMK baskısı FasonSevk'ten düşük), bu yüzden şiddet orta uygun.
</details>

### F-25 — Paketleme: sürekli kamera modunda scanBusy sırasında gelen okuma sessizce düşüyor (yeşil ✓ verilmesine rağmen) (orta)
**Konum:** `mobil/src/screens/Modules/TartiPaket/PaketlemeScreen.tsx:341`

handleScan `if (!code || scanBusy.current) return;` ile meşgulken gelen barkodu hiçbir geri bildirim olmadan atıyor (satır 339-362). scanBusy süresi = scan isteği RTT'si + sabit 600ms (satır 358-360), ilk okutmada ensureShipment+ensureActiveSack zincirini de içerir; BarcodeScannerView ise REARM_MS=1400ms sonra yeniden silahlanıp yakalama anında yeşil çerçeve + "Okundu" + başarı haptiği veriyor (BarcodeScannerView.tsx:67,127-145, captureHaptic default true). Yavaş Wi-Fi noktasında işlem >800ms sürdüğünde operatörün 1.4sn sonra okuttuğu ikinci top "okundu" hissi vermesine rağmen çuvala HİÇ eklenmez — top fiziksel çuvala girer, sistemde görünmez, eksik sevk/karşılama farkı ancak sayaçtan fark edilebilir.

**Önerilen düzeltme:** Meşgulken gelen kodu düşürme: kuyruğa alıp sırayla işle, ya da en azından uyarı toast'u + hata haptiği ver; alternatif olarak scanner'ın yeniden silahlanmasını scanBusy ile senkronla.

<details><summary>Doğrulayıcı kararları</summary>

- Kod iddiayı birebir doğruluyor: PaketlemeScreen.tsx:341 scanBusy'deyken okumayı sessizce düşürür (haptik/toast/kuyruk yok), scanner ise continuous+captureHaptic=true default ile yakalama ANINDA yeşil çerçeve+"Okundu"+başarı haptiği verip sabit 1400ms sonra yeniden silahlanır (BarcodeScannerView.tsx:67,127-145,207,253-261) — istek bitmesini beklemez. İlk okutma 3 ardışık POST (createShipment+addSack+scan) olduğundan T>800ms eşiği gerçekçi; üstelik düşen topu hemen tekrar okutmak lastScanRef guard'ına (BarcodeScannerView.tsx:130) takılıp yine sessiz kalır ve canReady (PaketlemeScreen:374-380) kapsama kontrolü yapmadığından eksik top sevke kadar gider.
- Kod iddiayı birebir doğruluyor: PaketlemeScreen.tsx:341 `if (!code || scanBusy.current) return;` sessiz düşürür (toast/haptik/kuyruk yok) ve scanBusy ancak istek zinciri (ilk okutmada createShipment+addSack+scan = 3 ardışık network çağrısı) bittikten 600ms sonra serbest kalır (satır 358-360); BarcodeScannerView ise continuous modda REARM_MS=1400 ile ekranın işleme durumundan BAĞIMSIZ yeniden silahlanır ve yakalama ANINDA yeşil çerçeve+"Okundu"+başarı haptiği verir (BarcodeScannerView.tsx:67, 127-145, 260; PaketlemeScreen 845-853 captureHaptic'i false geçmiyor, lastScanRef satır 130 sadece AYNI barkodu engeller). İşleme >~620ms sürdüğünde (1400−180−600) farklı bir top tam başarı geri bildirimiyle yakalanıp hiç eklenmeden düşer; başka hiçbir katman (modal pass-through, spinner yok, backend'e istek hiç gitmiyor) korumuyor — tek iz modal sayacının artmaması.
</details>

### F-26 — warehouse-scope 'preparing' kovası mobilde yok — PREPARING sevkiyattaki toplar Depo'da görünmez (orta)
**Konum:** `mobil/src/services/roll.service.ts:158`

Backend bugün GET /rolls/warehouse-scope cevabına 'preparing' kovasını ekledi (Teks-Erp/src/services/inventory.service.ts:803-833 — yorum aynen 'PREPARING'deki toplar eskiden HİÇBİR kovada sayılmıyordu' diyor). Mobil tip hâlâ free/sackStore/atDoor (roll.service.ts:158-164); TS kırılmaz (ek alan tolere edilir, runtime hata yok) ama DepoScreen aynı tutarsızlığı UI'da sürdürüyor: liste shipmentScope:'free' ile PREPARING topları zaten dışlıyor (DepoScreen.tsx:128) ve 'Çuvalda' sayacı committedCount = sackStore + atDoor (DepoScreen.tsx:177-178, render 332) preparing'i saymıyor. Senaryo: TartıPaket'te PREPARING sevkiyata okutulan toplar mobil Depo'da ne serbest listede ne herhangi bir sayaçta görünür — depo sorumlusu fiziksel sayımla ekranı karşılaştırınca topları 'kayıp' sanır.

**Önerilen düzeltme:** RollService getWarehouseScope tipine preparing:{count,qty} ekle; DepoScreen'de ya ayrı 'Hazırlanıyor' StatBox göster ya da committedCount'a preparing'i dahil et.

<details><summary>Doğrulayıcı kararları</summary>

- Bulgu doğru: backend warehouse-scope artık 'preparing' kovası döner (inventory.service.ts:803-833, yorum aynen gap'i teyit eder) ama tek tüketici olan mobil DepoScreen onu hiç göstermez — liste shipmentScope:'free' (DepoScreen.tsx:128 → backend where.shipmentId=null, inventory.service.ts:590-591) ve stats aynı filtreyi paylaşır, 'Çuvalda' sayacı sadece sackStore+atDoor (DepoScreen.tsx:177-178); scanIntoShipment PREPARING'de shipmentId'yi setlediğinden (shipping.service.ts:885-886) bu toplar markReady'ye kadar Depo ekranında hiçbir liste/sayaçta görünmez; Electron warehouse-scope'u hiç çağırmıyor (grep boş). Şiddet düşük: toplar TartıPaket/Sevkiyat modülünde ve top detayında görünür kalır, veri/proses bozulması yok; backend ekibi de sunucu tarafı eşleniğini 'L (düşük bulgu)' olarak sınıflamış.
- Bulgu kod düzeyinde doğru: backend warehouse-scope artık preparing döner (inventory.service.ts:806/815/828) ama tek tüketicisi olan mobil DepoScreen onu hiçbir yerde göstermez — liste shipmentScope:'free'=shipmentId null ile PREPARING toplarını dışlar (DepoScreen.tsx:128 + inventory.service.ts:590-591; scanIntoShipment shipmentId'yi PREPARING'de hemen yazar, shipping.service.ts:885-886) ve 'Çuvalda' sayacı sadece sackStore+atDoor toplar (DepoScreen.tsx:177-178, 332). Ancak 'kayıp sanır' senaryosu abartılı: Depo'da barkod okutma filtresiz getByBarcode ile topu bulur ve detayda 'Çuvalda · Çuvallanıyor' gösterir (DepoScreen.tsx:57, 770-783), PREPARING sevkler top sayılarıyla TartıPaket'te görünür (TartiPaketScreen.tsx:236), liste dışlaması da yorumla belgelenmiş bilinçli tasarım (DepoScreen.tsx:126-127); backend'in kendi yorumu da aynı boşluğu 'L (düşük bulgu)' olarak sınıflar — kalan etki yalnız sayaç mutabakat eksiği.
</details>

### F-27 — Tambur 'Top Kesme' yalnız kamera ile açılıyor — Listeden Seç / manuel giriş yok (orta)
**Konum:** `mobil/src/screens/Modules/Tambur/TamburScreen.tsx:1027`

`const openRecut = () => drawerQueue.run(() => setRecutScannerOpen(true));` (1027) — Top Kesme akışına giden TÜM yollar (tablet header, telefon drawer, manuel-mod buton satırı ~1301) doğrudan BarcodeScannerModal'ı açıyor (2235-2249). BarcodeScannerView'da metin girişi fallback'i yok (BarcodeScannerView.tsx:210-265, sadece kamera + izin ekranı); ekranın manualBarcodeEntry modunda bile yalnız refakat kartı için ScannerEntryBar var (1274-1285), top kesme için yok ve TamburScreen'de RollPickerModal hiç import edilmemiş. Proje kuralı 'top okutulan her ekranda kamera çalışsa bile Listeden Seç olmalı' (memory: project_mobile_roll_list_picker); kamera arızalı/izinsiz cihazda veya etiketi yıpranmış/okunmayan depo topunda operatör kesim işlemini HİÇ yapamaz (HID okuyucu da kamera modalında karşılık bulmaz — odaklı input yok).

**Önerilen düzeltme:** openRecut akışına Kartela Sevk/Paketleme'deki gibi paylaşılan RollPickerModal alternatifi ekle (filters: WAREHOUSE + renksiz ham STOCK, handleRecutScanned'a barkod ver) ve/veya manuel modda ScannerEntryBar ile elle barkod girişi.

<details><summary>Doğrulayıcı kararları</summary>

- Bulgu doğru: openRecut (TamburScreen.tsx:1027) tüm 3 giriş noktasından (1301/1339/1421) yalnız BarcodeScannerModal'ı (2236) açıyor; BarcodeScannerView.tsx:210-266'da TextInput yok (HID girişi karşılıksız), manuel-mod ScannerEntryBar'ı sadece refakat kartı çözüyor (resolveCard→getByCardBarcode, 396-450), handleRecutScanned'in tek çağıranı kamera modalı (2248), RollPickerModal import edilmemiş ve cut-warehouse endpoint'inin Electron'da hiç çağıranı yok — alternatif katman yok. Kamera arızalı/izinsiz cihazda veya okunamayan etikette Top Kesme yapılamaz.
- Bulgu doğru: TamburScreen.tsx'te recut formuna giden tek yol kamera taraması (openRecut:1027 → BarcodeScannerModal:2236-2249 → handleRecutScanned:1035, recutRollMeta'nın başka setter'ı yok); BarcodeScannerView kamera-only (CameraView:229, TextInput hiç yok, izin reddinde sadece 'İzin Ver'), manuel moddaki ScannerEntryBar (1276-1285) yalnız refakat kartı çözer ve RollPickerModal import edilmemiş. Electron/src'de recut/cutWarehouse UI'sı da yok → kamera çalışmayan cihazda veya etiketi okunmayan topta Top Kesme hiçbir katmandan yapılamaz; proje kuralı (project_mobile_roll_list_picker) bu ekranda uygulanmamış.
</details>

### F-28 — Sabit pageSize'lı master-data picker'ları kayıt sayısı tavanı aşınca sessizce kırpıyor (orta)
**Konum:** `mobil/src/screens/Modules/KK1/KK1Screen.tsx:261`

KK1 ürün picker'ı tek seferde `page:1, pageSize:500` çekiyor (KK1Screen.tsx:256-265); aynı kalıp Tambur 'Kime' müşteri picker'ı 500 (TamburScreen.tsx:1158-1162), Hızlı İş Emri renk picker'ı 300 (WorkOrderHeaderFields.tsx:47-50), Sevkiyat Geçmişi müşteri filtresi 200 (SevkiyatGecmisiScreen.tsx:62-64). PickerModal client modu yalnız çekilen options içinde arar; tavanı aşan kayıt listede ve aramada HİÇ görünmez — hata da yok. Backend MAX_PAGE_SIZE=500 olduğundan (query-parser.ts:20,67-69, aşan istek 400) tavan yükseltilemez: 501. kumaş/müşteri eklendiğinde KK1'de o ürünle giriş yapılamaz veya operatör fark etmeden benzer kaydı seçer. Proje 'uzun vadeli doğruluk için optimize et' diyor; backend bu yüzden cursor'a geçmişken bu picker'lar snapshot kalmış.

**Önerilen düzeltme:** Bu picker'ları PickerModal'ın mevcut `paginated`+onEndReached (cursor/infinite) moduna veya server-side aramalı yapıya geçir; kısa vadede en azından data.pagination.total > pageSize ise uyarı göster.

<details><summary>Doğrulayıcı kararları</summary>

- Mekanizma kodla doğrulandı: KK1Screen.tsx:261 (pageSize:500) → PickerModal.tsx:176-185 client modu yalnız çekilen options içinde arar, tavan aşımı sessiz; query-parser.ts:67-69 pageSize>500'ü clamp etmeyip 400 attığından tavan yükseltilemez; Tambur:1160 (500), WorkOrderHeaderFields.tsx:49 (300), SevkiyatGecmisiScreen.tsx:64 (200) aynı kalıp ve Electron aynı renk picker'ını cursor+server aramaya taşımışken (useColorPickerData.ts:70) mobil snapshot kalmış. Ancak güncel DB'de 4 kumaş/4 müşteri/6 renk var ve mobil CLAUDE.md master-data için snapshot'ı bilinçli kabul ediyor — bugün kimseyi yakmayan, yıllar ölçeğinde gelecek-hacim eşiği.
- Tüm iddialar kodla doğrulandı: KK1Screen.tsx:256-265 (pageSize:500) + satır 1026'da paginated'sız client-mode PickerModal, TamburScreen.tsx:1158-1163 (500), WorkOrderHeaderFields.tsx:49+204 (300), SevkiyatGecmisiScreen.tsx:63-64 (200); query-parser.ts:20+67-72 pageSize>500'ü clamp değil 400 ile reddediyor (tavan yükseltilemez) ve PickerModal client modu (101-105,176-185) yalnız bellekteki options'ta arıyor — hiçbir çağıran totalPages/hasMore kontrol etmiyor, koruyan katman yok. Backend'in kendi yorumu (query-parser.ts:58-62) 'sessiz kırpma yanıltıcıdır' diyerek aynı hatayı sunucuda bilinçle reddetmişken mobil picker'lar bunu istemcide yapıyor; ancak canlı DB'de aktif FABRIC item=4, müşteri=4, renk=6 (tavanların ~%1'i) ve mobil CLAUDE.md master-data için offset'i bilinçli kabul ettiğinden tetiklenme çok yıllık ufukta — bulgu doğru ama acil değil, şiddet düşük.
</details>

### F-29 — Tartı/Paket açık sipariş listesi: 300 karta kadar ScrollView+map, virtualizasyon ve arama yok (orta)
**Konum:** `mobil/src/screens/Modules/TartiPaket/TartiPaketScreen.tsx:278`

`displayOrders.map(renderOrderCard)` (267-279) tüm açık siparişleri düz ScrollView içinde tek seferde render ediyor; her kart tüm satırlarını da (o.lines.map, 163-181) basıyor. Backend listOpenOrdersWithCoverage `take: 300` döner (shipping.service.ts:2145-2146) → kötü durumda ~300 kart × satırlar = binlerce View/Text tek frame'de mount olur; ucuz Android tablette günlük giriş ekranı gözle görülür donar. Arama/müşteri filtresi de yok — operatör müşteriyi kaydırarak arar. Proje kuralı sınırsız/büyük listelerde FlashList (mobil CLAUDE.md liste bölümü); kardeş ekran SevkiyatScreen aynı problemi 'HAFİF board: FlashList + cursor' diye açıkça çözmüş (SevkiyatScreen.tsx:33-36). Ayrıca 300 üstü açık sipariş sessizce görünmez (deadline sıralı olduğu için en yakın terminliler kalır — kısmi teselli).

**Önerilen düzeltme:** Listeyi FlashList'e taşı (kart = renderItem; 'Devam Eden' bölümü ListHeaderComponent) ve müşteri/sipariş arama kutusu ekle; backend zaten customerId filtresi alıyor.

<details><summary>Doğrulayıcı kararları</summary>

- Tüm iddialar kodla doğrulandı: TartiPaketScreen.tsx:209/278 düz ScrollView+map (satırlar dahil, 163-181), ekranda arama/filtre yok (packing.service.ts:270 customerId paramı var ama hiç geçilmiyor), backend take:300 (shipping.service.ts:2147) ve hasMore sinyali yok; kardeş SevkiyatScreen.tsx:32-37 aynı problemi 'yüzlerce sevk birikse de hepsini çek/render YOK' diyerek FlashList+cursor+sunucu aramasıyla bilinçli çözmüş, ayrıca her seçim dokunuşu tüm kartları yeniden render ediyor (memoizasyonsuz renderOrderCard). Ancak canlı DB'de sadece 7 açık sipariş var ve liste kendini-boşaltan, 300 ile tavanlanmış bir kuyruk — bugün kimseyi yakmıyor, ölçek büyüyünce (100+ açık sipariş) gerçekleşecek bir risk; bu yüzden geçerli ama şiddeti düşük.
- Tüm iddialar kodla doğrulandı: TartiPaketScreen.tsx:209/278 düz ScrollView+map (satır başına o.lines.map 163-181, memo yok — her seçim dokunuşu tüm kartları yeniden render eder), arama/filtre yok (listOpenOrders parametresiz çağrılıyor, backend customerId destekliyor ama kullanılmıyor), shipping.service.ts:2147 take:300 + deadline-sort ile 300 üstü sessizce kırpılır. Proje emsali bulguyu destekliyor: SevkiyatScreen aynı profili FlashList+cursor+arama ile çözmüş (33-36 yorumu), hatta fetch-all yapılan FasonKabul PendingPane bile FlashList+arama kullanıyor; aynı ekranda PREPARING_CAP=5 ile liste şişmesi düşünülmüş ama açık siparişler kapsam dışı bırakılmış.
</details>

## ÇÜRÜTÜLEN İDDİALAR (doğrulamada elendi)

- **(el-query) İş emri iptali topları stoğa çekiyor ama hiçbir roll query'si invalidate edilmiyor**
  - Dialog gerçekten roll query'si invalidate etmiyor (WorkOrderCancelDialog.tsx:47-49) ama iddia edilen etki abartılı: 'Üretimde'/'Ham Stok' sekmelerini besleyen useDataTable refetchOnMount:"always" + staleTime:0 kullanır (hooks/useDataTable.ts:156-157) — Toplar sayfasına her giriş ve her sekme geçişi (queryKey `rolls:${tab}` değişir → yeni observer mount) sunucudan taze çeker, yani iptal sonrası sayfaya giden depo operatörü doğru veriyi görür. Bayatlık yalnız arka planda zaten mount duran Electron sekmesinde olur (TabHost.tsx:11-12) ki bu tüm sayfa/mutasyon çiftlerini etkileyen bilinçli tasarım takası ve sayfadaki RefreshButton (RollsPage.tsx:86-87) ile karşılanır; üstelik önerilen ["rolls"] invalidation'ı string anahtarlı tabloları (rolls:STOCK/rolls:IN_PRODUCTION, RollsTable.tsx:157) zaten yakalamazdı.
  - Teknik gözlem doğru (WorkOrderCancelDialog.tsx:47-50 roll key invalidate etmiyor) ama zarar iddiası başka bir katmanca karşılanıyor: bulgunun işaret ettiği 'Ham Stok'/'Üretimde' sekmeleri useDataTable kullanır ve useDataTable.ts:156-157'de refetchOnMount:"always" + staleTime:0 sabittir — sayfaya her geliş/alt-sekme değişimi taze veri çeker, stoğa dönen topu arayan operatör bulur. Ayrıca önerilen ["rolls"] invalidation tabloları zaten yakalamazdı: tablo key'leri string-prefix'li ["rolls:RAW_STOCK",...] (RollsPage.tsx:83-85'teki yorum bunu açıkça belgeliyor) — yani tasarım bilinçli olarak mount-refetch'e dayanıyor, mutation-side invalidation'a değil. Kalan tek boşluk arka planda mount kalan sekmedeki (TabHost.tsx:11-12) kanban önizlemesi/aynı alt-sekme görünümü olup RefreshButton ile kapanır; farklı kullanıcıdaki depo operatörü senaryosunu ise hiçbir client-side invalidation çözemez (realtime sync yok).

- **(el-destructive) Cihaz 'Eşleşmeyi Kaldır' ve 'Pasife Al' tek tıkla, onaysız çalışıyor**
  - Kod iddiaları doğru: DevicesPage.tsx:181-182 unpair/deactivate confirm'siz mutate, dropdown (310) + sağ-tık (333-335) anında tetikler; aynı dosyada hardDelete ConfirmDialog'lu, üstelik 'Pasife Al' danger:true işaretli ama onaysız. Eşleştirme zorunlu modda (sayfanın tek çalıştığı mod, satır 143-145) middleware unpair sonrası tableti login dahil 401'ler — vardiya ortası kilitlenme gerçek ve başka katman korumuyor. ANCAK şiddet abartılı: deactivate'te machineId korunur (device.service.ts:201-223) ve mobil pairing'i silmez (api.ts:13) → aynı menüden tek tık 'Aktifleştir' ile tam geri dönüş, fiziksel erişim/yeni kod GEREKMEZ (bulgu bunu yanlış anlatıyor); sadece unpair yeni kod + tablete erişim ister, o da dakikalarla sınırlı, veri kaybı yok, audit'li, admin-only ve menü arkasında.
  - Onay yokluğu kod düzeyinde doğru ama etki iddiası yanlış/abartılı: 'Pasife Al' machineId'yi korur ve mobil 401'de yerel eşleşmeyi silmez (Teks-Erp/src/services/device.service.ts:202-205 + mobil/src/services/api.ts:13-14) → geri dönüş aynı satırdaki tek tık 'Aktifleştir', fiziksel erişim/yeni kod GEREKMEZ; 'Eşleşmeyi Kaldır' ise veri kaybetmez (audit'te eski machineId, device.service.ts:166-173) ve yalnız devicePairingRequired=true iken etkilidir (default false, middleware:62-75; bayrak kapalıyken sayfa zaten yönlendirir, DevicesPage.tsx:143). Ayrıca her iki aksiyon admin:settings'e kapılı iki adımlı menü (… dropdown / sağ-tık) arkasında, 'tek tık' değil; proje kuralının hedeflediği geri alınamaz işlem (kalıcı sil) zaten ConfirmDialog + backend guard'lı (DevicesPage.tsx:204-221, device.service.ts:234-238). Unpair'e confirm eklemek olsa olsa düşük öncelikli UX iyileştirmesi.

- **(el-destructive) Çuval Depo 'Sevk Et' onayı geri alınamaz işlemde içeriği listelemiyor**
  - Doğrulandı: SackStorePage.tsx:57-62 dispatch onayı yalnız shipmentNo + statik metin; pending.shipment zaten customer/sackCount/rollCount/totalKg/totalQty taşıyor (types.ts:18-29) ve backend'de un-dispatch yok (shipping.service.ts:2059). En güçlü kanıt: aynı işlemin mobil eşdeğeri (SevkiyatScreen.tsx:342-343) onay modalında 'müşteri · N çuval · Xm' özetini ZATEN gösteriyor — Electron sayfası projenin kendi paterninin gerisinde. Hafifletici: buton, müşteri+çuval/kg/metraj gösteren kartın üstünde ve başlıkta sevk no var → şiddet yüksek değil orta.
  - Dialog metni gerçekten minimal (SackStorePage.tsx:57-62) ama abartılı: "Sevk Et" butonu müşteri adı/şube/çuval-kg-metraj-top sayaçlarını zaten gösteren kartın ÜZERİNDE (SackStoreCard.tsx:59-97), dialog başlığındaki benzersiz shipmentNo kartla çapraz kontrol sağlar ve içerik dökümü tek tık uzakta (ShipmentContentsSheet). shipmentConfirmationEnabled açıkken backend dispatch'i AT_DOOR'a kilitler (shipping.service.ts:1878-1883) yani iki ayrı onaylı adım var; proje kuralı da açıkça iptal/sil/scrap kapsamlı, dispatch ileri-yön iş olayı. 2026-06-10'daki çuval depo denetimi bu akışı inceleyip doğru bulmuştu.

- **(el-print) Sevk irsaliyesi: resmi belge sorgu hatası 'belge yok' sayılıp canlı TASLAK gösteriliyor**
  - Kod gözlemi (docQuery.isError kontrolü yok, hata=null'a iniyor) doğru ama senaryo abartılı ve başka katmanlar koruyor: (1) bulgunun adlandırdığı lazy-init/P2002 yarışı backend'de zaten yakalanıyor (Teks-Erp/src/services/printed-document.service.ts getCurrent catch → P2002'de kazananı okuyup success döner, hata üretmez; DISPATCHED sevkte belge zaten var, getCurrent tek findFirst); (2) 5xx/ağ hatası apiClient interceptor'da her zaman toast basar (apiClient.ts:88-91, suppressErrorToast yok) ve docQuery'yi düşüren DB/ağ arızası 1sn sonraki getDetail'i de büyük olasılıkla düşürür → draft undefined → 'bulunamadı' placeholder + Yazdır disabled (ShipmentDispatchNote.tsx:112-116,126); (3) kalan dar pencerede bile basılan kağıt tam sayfa 'TASLAK' filigranlı (satır 158), yani 'donmuş içerikten sapmış satırlar fark edilmeden dolaşıma girer' iddiası filigranın kendisiyle çelişiyor.
  - Kod iddiayı doğruluyor: ShipmentDispatchNote.tsx:46 `docQuery.data?.data ?? null` hata durumunu (data undefined) meşru 'belge yok' ile aynı null'a indirger; satır 61 enabled koşulu RQ v5'te hata sonrası (isLoading=false, retry:1) canlı detayı çekip satır 83/110'da 'henüz sevk edilmedi' + TASLAK DraftSheet basar, dosyada hiç isError yok; backend getCurrent (printed-document.service.ts:185) P2002 dışı lazy-init hatasını 500 olarak rethrow eder = legacy DISPATCHED sevkte kalıcı tetiklenebilir. Ancak şiddet abartılı: çıktıda büyük 'TASLAK' filigranı (satır 158) + apiClient 5xx/ağ toast'ı resmi belge sahteciliğini engeller ve tehlikeli render yalnız printed-documents endpoint'inin seçici (getDetail çalışırken) hatasında oluşur.

- **(el-api) Sunucu saati offset'i hiç senkronlanamıyor — Date header CORS'ta expose edilmiyor**
  - Bulgu DOĞRU ve ampirik olarak kanıtlandı: backend cors() exposedHeaders set etmiyor (app.ts:84; canlı curl'de Access-Control-Expose-Headers yok, Date var), renderer her zaman cross-origin (baseURL mutlak http://, prod file://, webSecurity:true, main'de header-rewrite yok) ve headless Chromium ile file:// origin'den yapılan XHR testinde getAllResponseHeaders() yalnız content-length+content-type döndü — date filtrelendi. Dolayısıyla readDateHeader hep undefined, offsetMs 0'da kalır, serverStatus.ts:49-54 yine synced:true yazar; sidebar 'sunucu saati' aslında istemci saatidir. Tek sync yolu bu header (heartbeat de aynı interceptor'a düşüyor), başka koruyucu katman yok.
  - Empirik test (projenin kendi Electron v42 binary'si, main.ts:50-56 ile birebir webPreferences: webSecurity:true+sandbox:true, file:// sayfa → bare cors()'lu Express): hem fetch hem XHR `date` header'ını OKUYABİLDİ — Electron file:// origin'lerde CORS expose-headers filtrelemesi uygulamıyor, prod build `loadFile` (main.ts:33) ile file://'dan yüklendiği için offset senkronu fabrikada çalışıyor. İddia yalnız dev modunda (http://localhost:5174 origin'inde header gerçekten filtreleniyor, test: fetchDate=null) doğru; bu da geliştirici makinesinde kozmetik bir saat gösterimi olup operatörü etkilemez.

- **(mb-offline) Default mutation retry:1 — idempotency anchor'sız create/geçişlerde timeout sonrası ikinci istek**
  - Mekanik doğru (queryClient.ts:28 retry:1, api.ts:9 timeout 10s) ama zarar iddiaları abartılı/başka katman koruyor: (1) "başarılı işlemi hata görme" retry'dan bağımsız — yanıt kaybolan her timeout'ta onSuccess zaten çalışmaz, manuel tekrar da aynı 409'u üretir; backend atomik claim'ler (shipping dispatch/markReady, return.service.ts:296-301 koşullu flip "çift RollReturn yazılmaz") veri bozulmasını zaten engelliyor. (2) quickStart çift-WO iddiası kodu yanlış okuyor: attach "per-roll yarışı" değil, tek tx içinde TEK bulk updateMany (workorder.service.ts:2591, status=STOCK guard) + zero-attach'te WO hardDelete telafisi (819-824); retry ancak ilk istek reddedildikten sonra (t≈10.8s) ateşlenir, örtüşme için LAN sunucuda ilk isteğin 10.8s+ sürmesi VE iki özdeş tek-statement update'in satır-kilit sırasının ayrışması gerekir — pratikte ikinci istek 400 (toplar IN_PRODUCTION) veya 0-attach→409+telafi alır.
  - Mekanizma (retry:1, queryClient.ts:28 + 10s timeout, api.ts:9) doğru ama zarar iddiaları kodla çürüyor: (1) 'başarılı işlem hata görünür' sonucu retry'a özgü değil — retry'sız timeout'ta da onSuccess çalışmaz, manuel tekrar aynı 409'u verir; tüm anılan uçlar atomik korumalı (shipping.service.ts:1696 claim, return.service.ts:300 koşullu flip 'çift iade' için kasten tasarlanmış). (2) En ağır iddia (quickStart çift WO) abartılı: attachRolls per-roll yarış DEĞİL tek bulk updateMany claim'dir (workorder.service.ts:2591) + zero-attach'te WO hardDelete telafisi var (815-824); gerçekçi retry senaryosu sıralıdır ve ön-doğrulama IN_PRODUCTION görüp 400 atar (766-772) — ikinci WO hiç oluşmaz; örtüşme LAN'da >10.8s süren tek istek gerektirir (patolojik). Kalan tek etki, zaten-anormal bir senaryoda yanıltıcı toast — proje belleğindeki bilinçli atomic-claim/409 duruşuyla tutarlı.

- **(mb-scan) FasonSevk/FasonKabul: onMutate'te koşulsuz başarı toast'ı — online reddi (409/400) 'kaydedildi'den SONRA geliyor, Kabul'de form da geri gelmiyor**
  - Mekanik doğru ama bulgu bilinçli/belgeli offline-first tasarımı bug olarak sunuyor: mutation'lar networkMode:'online'+persist ile offline'da PAUSE olur, onSuccess o oturumda hiç gelmeyebilir — toast onMutate'te olmak ZORUNDA ve metin offline'da "irsaliyeyi ELLE yaz" uyarısı basar (FasonSevkScreen.tsx:611-620, mutations.ts:56-64). FasonSevk formu online redde ayakta kalır (reset yalnız onSuccess'te, satır 602-604/621-636; ITEM_MISMATCH modalı var) ve resmi irsaliye zaten backend başarısı olmadan basılamaz (PrintedDocument create'te) — "toast'a bakıp aracı yollama" senaryosu online'da gerçekleşemez. FasonKabul form temizliği koddaki yorumla belgeli tradeoff (387: "rollback kompleks... operatör yeniden giriş yapar"), parti yeniden seçilince satırlar sevkten otomatik prefill olur (539-560) ve pending listesi modal açılışında tazelenir (362); backend idempotency çift-kaydı ayrıca korur. Kalan tek nüans (online'dayken toast'ı onSuccess'e erteleme) operatörü yakan bir hata değil, düşük öncelikli UX cilası.
  - Optimistic onMutate toast bilinçli offline-first tasarım: mutation'lar offline persist registry'de (mutations.ts:131-142, networkMode 'online' → offline'da pause, onSuccess bu oturumda hiç gelmeyebilir) ve toast onlineManager.isOnline() ile dallanıp offline'da "irsaliyeyi ELLE yaz" talimatı veriyor — 'irsaliye yazıp aracı yollama' senaryosu bug değil, dokümante iş gereksinimi (FasonSevkScreen.tsx:599-604, mutations.ts:135-137). Online redde FasonSevk formu AYAKTA kalır (reset sadece onSuccess'te, 602-604 yorumu + 621-636) ve LAN'da hata toast'ı başarıyı sub-second sürede değiştirir; mobil ekran irsaliye basmaz (print kodu yok, donmuş belge backend başarısında oluşur). FasonKabul rollback'sizliği açıkça dokümante tradeoff (385-387) ve applyParty (536-559) parti yeniden seçilince formu prefill eder — kabulde ölçüm yok, kayıp veri minimal; onError toast+haptic+submitArmed reset var. Kalan çekirdek (ITEM_MISMATCH retry'da ikinci toast) kozmetik ve okutma-anı pre-check (685-698) server mismatch'ini zaten nadirleştiriyor.

- **(mb-ui) RollPickerModal + Tambur/FasonSevk top aramalarında min-3 karakter kapısı yok (Roll tablosu)**
  - Min-3 kapısının yokluğu doğru ama maliyet iddiası abartılı: DepoScreen'deki kapının gerekçesi search-keyed LIMIT'siz stats aggregate taramalarıdır (DepoScreen.tsx:160 ve :197 — groupBy tüm filtreli set), üç picker yolunun hiçbirinde stats/aggregate yok; hepsi limit:30 cursor sayfası ve RollPickerModal/FasonSevk withTotal bile geçmez (COUNT yok). buildRollWhere'de (inventory.service.ts:420-424) barkod kolu exact-match index seek, contains yalnız küçük items master tablosuna değer; 1-2 harflik seçici-olmayan predicate LIMIT-30 keyset taramasında ~ilk 30 satırda durur — maliyeti modalın zaten açılışta attığı filtresiz ilk sayfayla aynı düzeyde. Tambur modalındaki withTotal COUNT'u boş aramada da her açılışta koşuyor; ~10 kullanıcılı LAN + statement_timeout=50s arka katman olarak mevcut.
  - Min-3 kapısının yokluğu doğru ama maliyet iddiası abartılı: DepoScreen'in gate gerekçesi 'ağır stats taraması'dır (search'le birlikte getRollStats unbounded aggregate çalışır), picker'lar ise sadece take:31 cursor sorgusu atar (inventory.service.ts:688-696) — LIMIT'li sorguda 1-2 harflik GENİŞ eşleşme createdAt index yürüyüşünde 31 satırda erken durur, yani ucuz; pahalı olan seçici aramadır ve gate onu zaten engellemez. Tambur'daki tek unbounded parça (withTotal→roll.count, tambur.service.ts:1116-1118) zaten her modal açılışında BOŞ aramayla tüm TAMBUR_SPLIT kümesini sayıyor (tasarım gereği), 1-2 harflik aramanın marjinal maliyeti baseline'dan farksız. 300ms debounce + ~10 LAN kullanıcı + statement_timeout=50s ile operatörü/veriyi yakacak bir senaryo yok.

## DÜŞÜK ÖNCELİKLİ NOTLAR (26 — doğrulanmadı, uygularken kodu teyit et)

- **(el-query) Kartela detay sheet'i 5 dk default staleTime'da + sayfa Yenile'si kapsamıyor (extraKeys eksik)** — `Electron/src/pages/Operations/Kartela/KartelaDetailSheet.tsx:68`
  DispatchDetail/ReceiptDetail query'leri (`["kartela","dispatch",id]` :68, `["kartela","receipt",id]` :166) staleTime vermiyor → App.tsx:22'deki global 5 dakikalık default geçerli. KartelaPage'in RefreshButton'ı sekmeye göre yalnız "kartela-dispatches"/"kartela-receipts" liste köklerini invalidate eder (KartelaPage.tsx:88-96) — açık/yeniden açılan detay sheet'i kapsamaz (kural 2 ihlali). Mobil tarafta kartela iptal edilirse operatör 5 dakikaya kadar bayat 'aktif' detay görür; Yenile butonu listede iptali gösterirken açık sheet eski kalır (aynı ekranda çelişen iki görünüm).
  *Fix:* Sheet query'lerine `staleTime: 0` (veya 30sn) ver; ayrıca RefreshButton'a `extraKeys={[["kartela"]]}` ekleyerek açık detayın da tek tuşta tazelenmesini sağla.

- **(el-query) Log arşivleme yalnız istatistiği tazeliyor — Aktivite ve Arşiv listeleri bayat** — `Electron/src/pages/System/Archive/ActivityArchivePage.tsx:49`
  Arşiv mutation'ı binlerce SystemLog satırını arşiv tablosuna taşıdıktan sonra yalnız `["system-log-stats"]` invalidate ediyor. Taşınan kayıtların kaybolacağı "system-logs" listesi (ActivityPage.tsx:21) ve yeni kayıtların görüneceği "system-logs-archive" (ArchiveSearchPage.tsx:21) invalidate edilmiyor; bu sekmeler açıksa (sekmeler mount kalır, staleTime 30sn ama focus-refetch kapalı) admin arşivlenen kayıtları hâlâ canlı listede görür, arşiv aramasında bulamaz — manuel Yenile'ye kadar.
  *Fix:* onSuccess'e `["system-logs"]` ve `["system-logs-archive"]` invalidate ekle (filtre varyantları array elemanı olduğundan prefix match yakalar).

- **(el-destructive) İş emri iptal dialog'u impact yüklenemeyince boş gövde gösteriyor** — `Electron/src/pages/Operations/WorkOrders/WorkOrderCancelDialog.tsx:188`
  Gövde render'ı `impactQ.isLoading ? Skeleton : impact ? (...) : null` (satır 75-188) — cancel-impact isteği hata verirse (timeout/500) kullanıcı başlık+iki butonlu BOŞ bir dialog görür; isError dalı yok, retry yolu yok. Onay butonu `!impact?.canCancel` ile disabled kaldığı için veri zarar görmez ama operatör neden iptal edemediğini anlamaz (interceptor toast'ı geçicidir). Aynı ekibin OrderCancelDialog'unda inline hasError kutusu var (OrderCancelDialog.tsx:145-149) — desen tutarsız.
  *Fix:* impactQ.isError için OrderCancelDialog'daki gibi inline hata kutusu + 'tekrar dene' (impactQ.refetch) ekle.

- **(el-destructive) Çuval Depo geçişlerinde 409 sonrası liste tazelenmiyor, bayat kart ekranda kalıyor** — `Electron/src/pages/Operations/SackStore/SackStorePage.tsx:105`
  Mutation'da bilinçli olarak onError yok (satır 105 yorumu); backend atomik claim 409 mesajları ('Sevkiyat durumu değişti — yenileyip tekrar deneyin', shipping.service.ts:1753/1798/1844/1912) toast olarak DÜZGÜN gösteriliyor. Ancak hata sonrası `[QUERY_KEY]` invalidate edilmediği ve `setPending(null)` çağrılmadığı için confirm dialog açık, kart eski durumuyla ekranda kalır; iki operatörlü senaryoda kullanıcı aynı eyleme tekrar basıp tekrar 409 yer, elle Yenile'ye basana kadar liste gerçeği yansıtmaz.
  *Fix:* mutation'a onError ekle: 409'da (veya tüm hatalarda) qc.invalidateQueries([QUERY_KEY]) + setPending(null) — toast'ı interceptor'a bırakmaya devam et.

- **(el-destructive) ConfirmDialog'da isPending opsiyonel — bazı yıkıcı onaylar çift-tık korumasız** — `Electron/src/components/forms/ConfirmDialog.tsx:13`
  ConfirmDialog yalnız caller isPending geçerse butonu kilitler (satır 38-44); kendi iç pending state'i yok. isPending geçmeyen yıkıcı onaylar: LabelTemplatesPage DeleteConfirm (LabelTemplatesPage.tsx:218-228, mut.isPending bağlanmamış), CustomerItemAliasesPanel (CustomerItemAliasesPanel.tsx:104-114) ve CustomerColorAliasesPanel (106 civarı) alias silmeleri. Mevcut çağrılar idempotent soft-delete olduğundan çift istek bugün veri bozmaz (çift toast/404 olasılığı), ama desen yeni ve daha tehlikeli bir yıkıcı akışa kopyalandığında koruma sessizce yok olur.
  *Fix:* ConfirmDialog onConfirm'i içeride await edip kendi busy state'iyle butonu kilitlesin (isPending prop'u override olarak kalsın) — tüm caller'lar otomatik korunur.

- **(el-print) Eski snapshot'lar için savunmacı render yok — schemaVersion hiç kontrol edilmiyor** — `Electron/src/services/printedDocumentService.ts:22`
  Snapshot zarfı `schemaVersion: 1` taşıyor ama client'ta hiçbir yerde okunmuyor; frozen sheet'ler `shown.snapshot.company.name`, `doc.lines.reduce`, `doc.sacks.length`, `doc.summary.totalKg` gibi alanlara guard'sız erişiyor (ShipmentFrozenSheet.tsx:54,94,101; KartelaCekiSheet.tsx:73-74). Bugün backend zarfı her yolda (freeze + lazy reconstruct, printed-document.service.ts:85-102) tam kurduğu için kırılma yok; ama belgeler tanım gereği SONSUZA DEK donmuş kaldığından ileride doc şeması değişip schemaVersion artarsa eski belge açılışında render exception olur ve dialog değil tüm sekme route-error fallback'ine düşer (dialog seviyesinde ErrorBoundary yok).
  *Fix:* Sheet render'ından önce `snapshot.schemaVersion === 1` kontrolü + bilinmeyen versiyonda 'Bu belge eski/yeni bir şemayla donduruldu' mesajı; dialog gövdesine küçük bir ErrorBoundary.

- **(el-print) İptal (void) sonrası belge cache'i 5 dakikaya kadar İPTAL filigranı olmadan basılabiliyor** — `Electron/src/pages/Operations/WorkOrders/FasonSevkPrintDialog.tsx:43`
  Fason ve Kartela current-doc sorguları `staleTime: 5*60_000` (FasonSevkPrintDialog.tsx:43, KartelaCekiPrintDialog.tsx:44) ve `['printed-doc', …]` anahtarını DocVersionBar'daki reissue dışında hiçbir akış invalidate etmiyor (grep doğrulandı). Senaryo: dialog bir kez açılıp kapandıktan sonra sevk başka kanaldan (ör. mobil FasonKabul'den) iptal edilir → belge backend'de VOIDED olur; planlamacı 5 dk içinde dialoğu tekrar açarsa cache'ten ACTIVE görünür, İPTAL filigransız basar. Dialogda manuel yenile butonu da yok.
  *Fix:* Current-doc sorgusunda staleTime'ı düşür/sıfırla veya `refetchOnMount:'always'` yap (belge zaten hafif tek satır + snapshot); Electron'daki sevk-iptal mutasyonlarına `['printed-doc']` invalidate ekle.

- **(el-print) Taslak sevk irsaliyesi canlı belge ayarını (documents.config) okumuyor — taslak/resmi çıktı tutarsız** — `Electron/src/pages/Operations/Shipments/ShipmentDispatchNote.tsx:140`
  DraftSheet `resolveDocConfig(undefined, "shipmentDispatch")` ile çağrılıyor — canlı feature-flag'lerdeki documentsConfig hiç verilmediği için admin'in başlık override'ı ve bölüm aç/kapa ayarları taslakta yok sayılır; ayrıca DraftSheet letterhead/imza kutuları/footer notu hiç basmaz (satır 156-205, frozen sheet'in aksine). Senaryo: admin 'Çuval dökümü' bölümünü kapattı ve başlığı özelleştirdi; operatör sevk öncesi TASLAK basıp kontrol eder, sevkte donan resmi belge bambaşka görünür — 'önizleme ile çıktı farklı' şikâyeti ve yanlış kontrol.
  *Fix:* Taslakta da canlı flag'lerden documentsConfig + firma künyesini çekip (`useFeatureFlags`) frozen sheet'le aynı `resolveDocConfig` girdisini kullan; mümkünse DraftSheet yerine ShipmentFrozenSheet'i canlı veriden kurulan doc + TASLAK filigranıyla render et (tek render yolu).

- **(el-tabs) Sunucu Durumu sekmesi arka planda süresiz 5 sn'de bir /health polling + gizli grafik re-render** — `Electron/src/pages/System/ServerStatus/serverHealth.ts:62`
  useServerHealth `refetchInterval: 5_000` ile yazılmış ve yorumu 'YALNIZ sayfa açıkken — yük bindirmez' diyor (serverHealth.ts:49-62); ama sekme sistemi pasif sekmeyi mount tuttuğu (TabHost.tsx:51-70) ve sekme listesi zustand persist ile kalıcı olduğu (store/tabs.ts:165-170) için bir kez açılıp unutulan Sunucu Durumu sekmesi panel kapatılana dek her 5 sn'de /health çeker (backend'de DB kilit sorgusu dahil) ve her örnekte setHistory ile gizli recharts panelini yeniden render eder. usePulseSync'in aksine (usePulseSync.ts:46-54) burada useIsTabActive gating'i yok.
  *Fix:* useServerHealth'e useIsTabActive ekleyip `refetchInterval: active ? 5000 : false` (ve history eklemeyi de active'e bağla) — usePulseSync'teki mevcut desenle aynı.

- **(el-tabs) Idle logout açık modal/formda uyarısız tetikleniyor; PDF önizleme (iframe) etkileşimi aktivite sayılmıyor** — `Electron/src/hooks/useIdleLogout.ts:37`
  Süre dolunca doLogout hiç uyarı/geri sayım vermeden token'ı silip #/login'e atar (useIdleLogout.ts:37-47) → AppShell ve tüm sekme panelleri unmount olur, açık dialoglardaki kaydedilmemiş form girişi (sekme state'iyle birlikte) kaybolur. Ayrıca aktivite yalnız window event'lerinden okunur (mousemove/keydown..., satır 7-14); TravelerCardPrintDialog'daki PDFViewer iframe'i (TravelerCardPrintDialog.tsx:189-204) içindeki scroll/tıklamalar parent window'a bubble etmez → refakat kartını dakikalarca inceleyen kullanıcı 'idle' sayılıp oturumdan atılabilir. Ayar default 0/kapalı olduğu için etkisi sınırlı.
  *Fix:* Logout'tan ~1 dk önce 'oturum kapanacak' toast'u + herhangi bir tuşla uzatma ekleyin; iframe etkileşimi için window 'blur' + document.activeElement iframe kontrolünü aktivite olarak sayın.

- **(el-api) Oturum düşmesinde toast yağmuru: 401 toast'ı tekilleştirilmemiş + retry:1 4xx'leri de yeniden deniyor** — `Electron/src/services/apiClient.ts:76`
  Token süresi dolduğunda sayfadaki her paralel istek ayrı 'Oturum süreniz doldu' toast'ı üretir (apiClient.ts:76, toast id yok); QueryClient `retry: 1` (App.tsx:20) status ayrımı yapmadığı için her 401/403 query bir kez daha denenir — ilk 401 token'ı sildiği için retry Authorization'sız gider, yine 401 → toast sayısı ikiye katlanır. Aynı mekanizma 403'te de aynı sayfada çift 'Bu işlem için yetkiniz bulunmuyor' (apiClient.ts:81) gösterir. Davranış bozulmaz (login'e düşer) ama 4-8 üst üste toast operatörü şaşırtır ve gereksiz istek üretir.
  *Fix:* 401/403 toast'larına sabit `{ id: "session-expired" }` / `{ id: "forbidden" }` ver; QueryClient retry'ını fonksiyon yap: `(count, err) => 4xx ise false, değilse count < 1`.

- **(el-api) AuthHydrator süresi dolmuş token'ı exp kontrolü yapmadan kabul edip uygulamayı açıyor** — `Electron/src/App.tsx:35`
  Açılışta `decodeJwt(token)` sadece payload'ı parse eder (lib/jwt.ts:3-13), `exp` alanına hiç bakılmaz ve /auth/me doğrulaması yapılmaz; decode başarılıysa `setUser(decoded)` ile AppShell açılır. Sabah uygulama açıldığında (8 saatlik oturum gece dolmuş) tüm dashboard query'leri aynı anda 401 alır → yukarıdaki toast yağmuru + ekran bir an dolu görünüp login'e düşer. Interceptor sayesinde kendini toparlar ama her gün ilk açılışta yaşanan çirkin bir akıştır.
  *Fix:* AuthHydrator'da `decoded.exp * 1000 < Date.now()` ise token'ı silip direkt login'e düş (JwtPayload'a exp ekle); istenirse arkada /auth/me ile doğrula.

- **(el-api) Logout/login'de React Query cache temizlenmiyor — kullanıcı değişiminde 5 dk'lık bayat veri ağa çıkmadan gösterilir** — `Electron/src/store/auth.ts:18`
  `logout()` yalnız token+user temizler (auth.ts:18-21); module-level QueryClient (App.tsx:17, staleTime 5 dk) yaşamaya devam eder ve LoginPage yeni girişte de cache'e dokunmaz (LoginPage.tsx:52). Fabrikada ortak PC'de kullanıcı değişiminde yeni kullanıcı, önceki kullanıcının 5 dakikadan taze query sonuçlarını (sipariş/sevkiyat durumları, admin listeleri) refetch olmadan aynen görür — araya giren durum değişiklikleri görünmez. Preferences bilinçli olarak `["preferences", userId]` ile kullanıcıya keyed (PreferencesProvider.tsx:51) yani sızıntı düşünülmüş ama genel veri cache'i unutulmuş.
  *Fix:* `logout()` ve apiClient'ın 401 dalında (veya login başarısında) `queryClient.clear()` çağır — QueryClient'ı export edip auth akışına bağla.

- **(el-perm) Müşteri düzenleme modalının alias sekmeleri customer-alias:read kontrolü yapmıyor** — `Electron/src/pages/Customers/CustomerFormDialog.tsx:144`
  CustomerFormDialog'da 'Ürün İsimleri' ve 'Renk İsimleri' sekmeleri koşulsuz render edilir (satır 144-148); panel sorguları GET /api/customer-alias/... çağırır ve backend customer-alias:read ister (customer-alias.routes.ts, 3 endpoint). Yazma butonları customer-alias:write ile gate'li (CustomerColorAliasesPanel.tsx:64) ama OKUMA sorgusu gate'siz (satır 28-30, enabled yok) — customer:read+customer:write'lı ama alias izni olmayan kullanıcı müşteriyi düzenlerken sekmeye tıklayınca 403 toast + boş panel görür. Aynı projede 'customer-alias:read yoksa sessiz kapat' deseni zaten var (OrderDetailSheet.tsx:100 aliasEnabled) — burada uygulanmamış.
  *Fix:* OrderDetailSheet'teki desenle hizala: hasPermission("customer-alias:read") yoksa alias sekmelerini TabsList'ten gizle veya sorguyu enabled:false yapıp bilgilendirici boş durum göster.

- **(el-perm) 9 tanım rotası route-level guard'sız — koruma deseni tutarsız** — `Electron/src/routes/content-routes.tsx:95`
  definitions/items, customers, stations, machines, routes, product-recipes, defect-types, quality-grades, colors rotaları ProtectedRoute'suz (satır 95-103) — aynı dosyada return-reasons'tan itibaren tüm kardeşleri guard'lı (107+). Bugün erişim vektörleri (hub tile, komut paleti, favoriler) aynı permission'la filtrelendiği için pratik etki sınırlı; ama Electron'da savunma tek katmana iniyor ve tile permission'ı yanlış olduğu an (colors vakasında olduğu gibi) ya da yeni bir derin link eklendiğinde kullanıcı doğrudan 403'lü sayfaya düşer. CLAUDE.md kontrol listesi de ('Route requirePermission ile eklendi') bu sayfalarda uygulanmamış.
  *Fix:* Dokuz rotaya tile-config'tekiyle aynı read permission'lı ProtectedRoute sar (items→item:read, customers→customer:read, stations/machines/routes/product-recipes→station:read, defect-types/quality-grades→quality:read, colors→property:read).

- **(mb-offline) OFFLINE_AWARE retry:3 deterministik 4xx hatalarını da yeniden dener — hata toast'ı ~8sn gecikir** — `mobil/src/offline/mutations.ts:58`
  OFFLINE_AWARE { retry: 3, retryDelay: 1s/2s/4s } hata tipine bakmaz: backend'in anında döndürdüğü validation/conflict (Zod 400, 409) hatasında da 4 deneme yapılır — örn. KK1 girişinde silinmiş ürün reddi veya KartelaSevk'te çuvala rezerve top conflict'i, operatöre ancak ~7-8sn sonra toast olur. api.ts wrapper'ı hataya .status ekliyor (api.ts:57-58) ama retry kararında kullanılmıyor.
  *Fix:* retry'ı fonksiyona çevir: `(failureCount, err) => (err.status && err.status < 500 ? false : failureCount < 3)` — 4xx'te anında pes et, ağ/5xx'te mevcut davranış.

- **(mb-offline) Offline/timeout hatalarında ham İngilizce axios mesajı operatöre sızıyor** — `mobil/src/services/api.ts:49`
  Yanıt alınamayan hatalarda message fallback'i error.message: offline'da 'Network Error', yavaş sunucuda 'timeout of 10000ms exceeded' — bunlar toast'larda aynen gösterilir (örn. PaketlemeScreen handleScan 'Eklenemedi: Network Error', SevkiyatScreen 'Sevk edilemedi: timeout of...'). networkMode 'always' mutation'lı tüm yeni ekranlar (TartıPaket/Sevkiyat/İade/Kartela geçmiş iptalleri) sahada tam bağlantı koptuğu anda İngilizce teknik mesaj gösteriyor; 'Validation hataları Türkçe' kuralının en kritik anında deliği.
  *Fix:* api.ts interceptor'ında response'suz hataları kodla eşle: ECONNABORTED → 'Sunucu yanıt vermedi — bağlantıyı kontrol et', network error → 'Çevrimdışı — sunucuya ulaşılamıyor'.

- **(mb-offline) Paketleme ensureShipment timeout'unda hayalet sevkiyat kilidi — ekrandan kurtarma yolu yok** — `mobil/src/screens/Modules/TartiPaket/PaketlemeScreen.tsx:147`
  ensureShipment ham promise (react-query dışı): createShipment timeout olursa sunucu sevkiyatı yaratmış olabilir ama setShipmentId çalışmaz; ekran taslak modunda kalır ve sonraki HER okutma yeniden create dener → backend sipariş-zaten-açık-sevkiyatta guard'ı (shipping.service.ts:298-304) 'Sipariş zaten başka bir sevkiyatta (SVK-x)' conflict döndürür. Ekranda mevcut sevkiyata bağlanma yolu yok; operatör geri çıkıp TartıPaket listesinden (preparingQ) sevkiyatı bulmak zorunda — toast bu yönlendirmeyi de yapmıyor.
  *Fix:* Conflict hatasının details'ından mevcut shipmentId'yi alıp setShipmentId ile otomatik bağlan (veya toast'ta 'mevcut sevkiyat açılıyor' diyerek Paketleme'yi shipmentId ile yeniden aç).

- **(mb-scan) Hızlı İş Emri: resolvingRef meşgulken okutulan top sessizce yok sayılıyor** — `mobil/src/screens/Modules/HizliIsEmri/NewWorkOrderView.tsx:311`
  handleScan `if (resolvingRef.current) return;` ile önceki getByBarcode uçuştayken gelen yeni barkodu toast'sız/haptiksiz düşürüyor (satır 307-330); kamera modalı yeşil "Okundu" çerçevesini yine gösteriyor (captureHaptic=false ama görsel onay duruyor, BarcodeScannerView busy state). LAN'da pencere dar (tek istek RTT'si) ama yavaş anda okutulan top listeye girmez ve iş emri eksik topla başlatılabilir — Paketleme'deki desenin hafif hali.
  *Fix:* Meşgulken gelen barkodu küçük bir kuyrukta biriktirip sırayla resolve et veya "bekle" uyarısı göster.

- **(mb-scan) Sevk Çıkışı: durum geçişi hatasında (409 dahil) board tazelenmiyor — bayat kartla aynı hata tekrarlanır** — `mobil/src/screens/Modules/Sevkiyat/SevkiyatScreen.tsx:116`
  dispatchMut/moveToDoorMut/pullBackMut/unreadyMut onError'ları yalnız toast gösteriyor (satır 116, 126, 136, 148), refresh()/invalidate çağrılmıyor. İki cihazlı senaryoda (backend unready atomik claim 409 döner, diğer geçişler check-then-act) kart listede eski durumuyla kalır; operatör aynı butona tekrar basıp tekrar hata alır, gerçek durumu ancak manuel Yenile ile görür.
  *Fix:* Tüm bu mutation'ların onError'ına da refresh() (sack-store invalidate) ekle — onSuccess ile aynı tazeleme.

- **(mb-scan) KK2/Tambur: kart çözümlemede in-flight guard yok — çift tetikte aynı kart iki sekme açılıyor** — `mobil/src/screens/Modules/KursunQc/KursunQcScreen.tsx:271`
  resolveCard 'zaten açık mı' kontrolünü (satır 271-281) await'lerden ÖNCE yapıyor ve fonksiyon girişinde resolvingCard kontrolü yok; Açık Kartlar modalındaki satıra çift dokunuş (handleCameraSelect satır 392-395, satır TouchableRipple'ı disabled değil) aynı barkod için iki eşzamanlı çözümleme başlatır — ikisi de existing kontrolünü geçer ve openResolvedStep iki kez append eder (satır 343): aynı cardId'li mükerrer sekme (duplicate React key), closeJob ikisini birden kapatır. Aynı desen TamburScreen.tsx resolveCard'da (satır 396-430, append 430).
  *Fix:* resolveCard girişinde `if (resolvingCard) return;` + openResolvedStep/append içinde functional updater'da cardBarcode dedup yap.

- **(mb-scan) FasonSevk: aynı barkodun eşzamanlı çözümlenmesinde sevk listesine çift satır girebiliyor** — `mobil/src/screens/Modules/FasonSevk/FasonSevkScreen.tsx:699`
  addRollToList mükerrer kontrolünü render closure'ındaki scannedRolls üzerinden yapıyor (satır 676) ama `setScannedRolls((prev) => [{...}, ...prev])` functional updater'ında dedup YOK (satır 699-712). Sürekli kamera + manuel "Ekle"/picker kombinasyonunda veya A→B→A dizisinde A'nın ilk getByBarcode'u hâlâ uçuştayken ikinci A çözümlenirse liste aynı topu iki kez içerir; dispatch rollIds'e aynı id iki kez gider. HizliIsEmri aynı riski bilerek updater içi dedup ile kapatmış (NewWorkOrderView.tsx:288-291) — FasonSevk'te eksik. Etki sınırlı: satır görünür ve handleRemoveRoll id ile ikisini birden siler.
  *Fix:* HizliIsEmri'deki gibi functional updater içinde Set ile tekrar dedup yap.

- **(mb-scan) Tambur: over-cut onayı bayat client metrajıyla atlanabilir** — `mobil/src/screens/Modules/Tambur/TamburScreen.tsx:946`
  exceedsRemaining kontrolü yalnız client state'e bakıyor: açık kumaşta `length > selectedRoll.currentQty + 0.001` (satır 946), recut'ta `cut > recutRollMeta.currentQty` (satır 1099). Client metrajı backend'den geriyse (refetch hatası, uzun süre açık ekranda başka cihaz/işlem aynı topu küçülttüyse) onay diyaloğu HİÇ çıkmadan istek gider; overQuantityEnabled default açık olduğundan backend parent'ı tamamen tüketir ve overCutConfirm'in koruduğu "kaynak kumaş tamamen kapanacak" kararı operatöre sorulmadan gerçekleşir.
  *Fix:* Backend cut yanıtına 'overConsumed' bayrağı ekleyip aşım client onayı olmadan geldiyse 409 dönmesini sağla (istek payload'ına confirmOver flag'i) — onay sunucu tarafında zorunlu olsun.

- **(mb-api) Ölü sevkiyat-öncesi servis dosyaları backend'de hiç mount edilmemiş endpoint'lere işaret ediyor** — `mobil/src/services/packaging.service.ts:1`
  packaging.service.ts, packagingQueue.service.ts ve shippingQueue.service.ts hiçbir ekran/hook tarafından import edilmiyor (grep: sıfır importer; canlı ekranlar packing.service kullanıyor) ve gittikleri /api/packaging, /api/packaging-queue, /api/shipping-queue backend app.ts:344-381 mount listesinde YOK — wire edilen ilk çağrı 404 yer. Eski silinen sevkiyat modülünün kalıntıları; üstelik order.service.ts:7 yorumu packagingQueue.service'i canlıymış gibi referans veriyor ('TartıPaket artık... kuyruktan çeker — packagingQueue.service'). Ayrıca constants/api.ts:22-36'daki ENDPOINTS sabiti ('/production' ve '/packaging' dahil) projede hiçbir yerde kullanılmıyor — bugün silinen /api/production'a işaret eden tek kalıntı bu ölü sabit.
  *Fix:* Üç ölü servis dosyasını ve constants/api.ts ENDPOINTS sabitini sil; order.service.ts baş yorumunu gerçek akışa (packing.service) göre güncelle.
  ✅ **KAPANDI → ölçüldü 2026-09-15 (ağaç `dc34c4e3`):** 2026-09-14'te DARALMIŞTI (çapa `packaging.service.ts` + `packagingQueue.service.ts` silinmiş, `order.service.ts` yorumu düzeltilmiş); ayakta kalan iki kalıntı bu commit'te gitti — `mobil/src/services/shippingQueue.service.ts` (içe aktaran 0, `/shipping-queue` backend'de mount DEĞİL) SİLİNDİ · `mobil/src/constants/api.ts` `ENDPOINTS` sabiti (kullanan 0) KALDIRILDI. Ölçüm: `mobil/`de `shippingQueue`/`ENDPOINTS` eşleşmesi 0, mobil tsc + lint + jest yeşil; ölü-kod kapısı (knip benzeri) projede YOK — bulgu elle ölçümle kapandı.

- **(mb-api) orderService.getReadyOrders backend'de var olmayan /shipping/ready-orders'a gidiyor** — `mobil/src/services/order.service.ts:57`
  getReadyOrders GET /shipping/ready-orders çağırıyor ama bu path backend'in hiçbir yerinde yok (Teks-Erp/src grep: 0 sonuç; shipping.routes.ts'te ready-orders yok) — çağrılırsa 404. Şu an mobilde hiçbir çağıranı yok (importer'lar yalnız getAvailableOrderLines kullanıyor) ama dosya başı yorum (order.service.ts:8 'sevkiyat ekranının ihtiyacı olan ready-orders'ı tutar') bunu canlı sözleşme gibi tanıtıyor; ReadyOrderLine.allocatedRolls tipi de 2026-05-25'te silinen allocation domain'inin kalıntısı. Gelecekte sevkiyat ekranına dokunulduğunda bu wrapper'a güvenen geliştirme anında 404 ile patlar.
  *Fix:* getReadyOrders metodu + ReadyOrder/ReadyOrderLine tipleri silinsin; gerçek açık-sipariş ihtiyacı packing.service.listOpenOrders (/shipping/open-orders) üzerinden karşılanıyor.
  ✅ **KAPANDI → ölçüldü 2026-09-14 (ağaç `08e20215`):** `getReadyOrders` + `ReadyOrder`/`ReadyOrderLine` kaldırılmış; `mobil/src`te kalan tek `ReadyOrder` geçişi `order.service.ts:7`deki kaldırıldı notudur.

- **(mb-ui) FasonKabul iptal onay modalı AppModal değil — el yapımı overlay, header/geri tuşu açık kalıyor** — `mobil/src/screens/Modules/FasonKabul/FasonKabulScreen.tsx:1676`
  CancelReceiptModal (yıkıcı 'Mal Kabulü İptal' onayı) AppModal yerine ScreenChrome içeriğine absoluteFill overlay olarak render ediliyor (1671-1690, cancelStyles.overlay 1814-1822); gerekçe yorumu 'iki RNModal çakışması' diyor ama bu react-native-modal dönemine ait — host HistoryReceiptsModal artık Portal tabanlı RemoteListSheet/AppModal (2136) ve AppModal'lar üst üste sorunsuz açılır. Sonuç: overlay yalnız content alanını kapladığı için üstteki Appbar (ana sayfa/yenile/kullanıcı menüsü) yıkıcı onay açıkken tıklanabilir kalır ve Android donanım geri tuşu modalı kapatmak yerine ekrandan çıkar (AppModal'daki BackHandler güvencesi yok, AppModal.tsx:123-130 ile kıyasla).
  *Fix:* historyModalOpen değilken üst seviyedeki kullanımı normal AppModal'a çevir (Portal stack'i yeterli); history içi varyant kalacaksa en azından BackHandler aboneliği ekle.

## KAPSAM NOTLARI (neyin sağlam bulunduğu)

- **el-query:** Kapsam: Electron/src altındaki tüm useQuery/useInfiniteQuery (126 dosya), useMutation (45 dosya) ve 71 invalidateQueries çağrısı tek tek tarandı; kritik akışlar (Rolls/Returns/SackStore/WorkOrders/Shipments/Kartela/Access/GeneralSettings/Reports/Dashboard) dosya bazında okundu, iddialar backend'e karşı doğrulandı (örn. 'WO aç/iptal order status'a dokunmuyor' yorumu — workorder.service.ts:100 — sayesinde 'WO create sonrası orders bayat' şeklindeki spekülatif bulgu ELENDİ; unready'nin recomputeOrderStatusForOrders çağırdığı shipping.service.ts:1676'da doğrulandı). SAĞLAM bulunanlar: WO detay RefreshButton'ı extraKeys ile dalları doğru kapsıyor; dashboard/reports/feature-flags/CRUD master-data invalidation disiplini tutarlı (array-prefix kökler, picker/lookup key'leri CRUD köküyle prefix paylaşıyor); rapor tarih parametreleri URL'den geldiği için object-identity kaynaklı sonsuz refetch riski YOK (refetchInterval yalnız NotificationBell ve ServerStatus'ta, bilinçli); DocVersionBar/printed-doc zinciri eksiksiz. Bakılamayanlar: mobil taraf kapsam dışıydı; bulgular statik kod okumasıyla kanıtlandı, çalışan uygulamada repro denenmedi; renk/route picker'larındaki 30-60sn'lik kısa bayatlık pencereleri (color-picker kökü colors CRUD'unca invalidate edilmiyor ama modal her açılışta remount-refetch ile kendini topluyor) bilinçli olarak bulgu yapılmadı.

- **el-destructive:** SAĞLAM bulunanlar: WO iptali cancel-impact preview + per-top liste + canCancel kilidi + isPending guard ile (WorkOrderCancelDialog), sipariş iptali WO-bazlı aksiyon seçimi ve özetle (OrderCancelDialog — yukarıdaki activeShipments boşluğu hariç), parti ayırma split-preview + taşınacak top listesiyle (SplitBranchModal), iade iptali somut kayıt + zorunlu sebep + pending kilidiyle (ReturnsDetailSheet); genel CRUD soft-delete'ler CrudPage'de confirm + 'Geri al' undo + isPending ile, belge revizyonu (DocVersionBar) zorunlu gerekçe + pending kilidiyle, cihaz kalıcı silme ve arşivleme/yedek onaylı. apiClient interceptor'ı tüm 4xx/5xx'i merkezi toast'ladığı için sessiz başarısızlık ve yanıltıcı başarı toast'ı deseni bulunamadı (mutationFn içinde hata yutan kod yok); backend 409 mesajları zaten 'yenileyin' yönlendirmesi içeriyor. Bakılamayanlar: roll cancel-preview, kartela/fason kabul iptali ve kursun-qc reopen-preview akışlarının Electron'da hiç UI'ı yok (mobil akışlar — kapsam dışı), bu yüzden o preview'ların tüketimi bu denetimde doğrulanamadı; mobil uygulama ve backend'in kendisi yalnız kontrat doğrulamak için nokta okundu.

- **el-print:** SAĞLAM bulunanlar: (1) donmuş-vs-canlı ayrımı üç belgede de doğru kurulmuş — frozen sheet'ler içeriği/config'i/künyeyi yalnız snapshot'tan okuyor, canlı veri sadece kasıtlı fason renk+boya-notu overlay'i ve TASLAK yolunda; yanlışlıkla canlı veri basan resmi yol bulunamadı. (2) DocVersionBar'ın reissue→invalidate→güncele-dön akışı ve versiyon seçim durumu tutarlı; gerekçe zorunluluğu ve VOIDED'da revize kilidi doğru. (3) Belge Şablonları önizlemesi (DocumentPreview) gerçek sheet bileşenlerini preview override'ıyla yeniden kullanıyor — kopya render kayması yok. (4) Refakat kartı kart snapshot'ından basıyor, eksik snapshot'ta canlı WO'ya düşen savunmacı fallback'leri var ve gizli-iframe+PDF yolu .print-area desenindeki sorunlardan muaf. (5) Backend snapshot zarfı (printed-document.service.ts) freeze ve lazy-reconstruct yollarında hep tam kuruluyor. Bakılamayanlar: fiziksel yazıcı çıktısı görsel olarak doğrulanmadı (bulgu 1 ve 3 CSS semantiği + DOM yapısından kanıtlandı; bir kez gerçek baskı testi önerilir), mobil taraftaki sevk irsaliyesi render'ı ve backend builder'ların alan-bazlı içerik doğruluğu bu kapsamın dışında kaldı.

- **el-tabs:** Kapsamda SAĞLAM bulunanlar: (a) focus-trap çakışması yok — scoped dialoglar Radix non-modal yoldan trapFocus:false/disableOutsidePointerEvents:false alıyor (react-dialog dist:174-176), pasif pane `invisible pointer-events-none` (TabHost.tsx:59) tıklamayı zaten engelliyor; (b) sekme kapatma temizliği doğru — closeTab/closeOthers disposeTabRouter çağırıyor (store/tabs.ts:114,159; tab-routers.tsx:48-51), sayfalarda kaçak global key/interval listener'ı bulunamadı (tek window listener RouteEditor resize, temizlenmeli şekilde useEffect içinde); (c) usePulseSync pasif sekmede aboneliği kesip tek paylaşılan interval'ı sıfır-abonede kapatıyor; (d) OrdersPage body-portal'lı detay panelini isTabActive ile bilinçli gating'lemiş; (e) login/komut paleti/ShortcutsDialog tab-portal dışı kaldığından klasik bloklayan modal davranışı korunuyor ve modal katman açıkken Radix alt layer'ların outside-dismiss'ini zaten kapatıyor. Bakılamayanlar: bulgular salt statik kod okuma + Radix 1.1.x kaynak analiziyle kanıtlandı, çalışan uygulamada runtime doğrulaması yapılmadı; ~80 sayfa modülünün dialog kullanımı tek tek taranmadı (OrderForm/ColorPicker/print dialogları temsilî olarak okundu) — bulgu 1 ve 3 paylaşılan primitiflerden geldiği için tüm kullanıcılarına aynı şekilde uygulanır.

- **el-api:** SAĞLAM bulunanlar: (1) Kaldırılan endpoint taraması temiz — Electron'daki TÜM /api çağrıları envanterlendi (literal + template suffix'ler) ve backend route'larıyla tek tek eşleştirildi; /api/production/*, undo-qc2, rolls/:id/identity, available-for-attach, attach/detach-rolls, auth/register hiçbir yerde çağrılmıyor (backend route yorumları da 'hiçbir frontend çağırmıyordu' diyor; /api/production-balance ve /api/reports/production/* farklı prefix'ler ve backend'de yaşıyor). (6) Cursor token her yerde opak kullanılıyor (crudService→query-builder sadece sp.set('cursor',...), picker hook'ları nextCursor'u aynen geri verir; tek atob lib/jwt.ts'te ve meşru JWT decode). (4) baseURL runtime override'ı kaçıran hardcoded URL yok — tek axios instance apiClient, ApiEndpointDialog'daki ham axios `${target}/health` bilinçli aday-adres testi, main process HTTP yapmıyor. (5) Decimal=number varsayımı tutarlı; Number() sarmaları no-op, string varsayımı yok. (2) 401/403/409 tek interceptor'dan tutarlı akıyor, sayfalarda onError toast duplikasyonu yok (sessiz boş ekran üreten suppress kullanımı da bulunmadı) — kalan pürüzler bulgu 3/4'te. 'preparing' kovası: Electron warehouse-scope endpoint'ini hiç tüketmiyor → bu değişiklik Electron'u etkilemiyor (tüketici mobil tarafta, bu kapsamda bakılmadı). Bakılamayanlar: mobil istemci tamamen kapsam dışı; Electron main process yalnız URL/CORS/secure-store açısından okundu; sayfa-içi UI mantığı API tüketimi dışında denetlenmedi.

- **el-perm:** SAĞLAM bulunanlar: 12 CrudPage'in writePermission kodları backend route'larıyla birebir (tek istisna Renkler); 7 rapor rotası + tile'ları backend report:* kodlarıyla birebir; system/access/devices/label-template rotaları admin:settings | admin:users | label-template:* ile doğru kilitli; donmuş belge 'Revize Et' butonları (DocVersionBar canReissue) printed-document.routes.ts'deki docType→izin eşlemesiyle birebir (shipping:write/workorder:write/kartela:write); Orders/Returns/Rolls/SackStore/WO detay aksiyon butonları PermissionGate'li ve kodlar backend'le uyumlu; sidebar+komut paleti+hub tile'ları tek permission kaynağından filtreleniyor; mobil uygulamanın usePermission'ı mobile:*/admin:* wildcard'ını backend'le aynı tek-domain mantığıyla DOĞRU genişletiyor (Electron'un aksine). Bakılamayanlar: çalışan uygulama üzerinde runtime doğrulama yapılmadı (salt statik kod incelemesi); mobil ekranların izin denetimi kapsam dışı bırakıldı (yalnız wildcard karşılaştırması için bakıldı); backend'in tüm ~38 route dosyası endpoint-endpoint değil, Electron'un dokunduğu yüzeyler önceliğiyle tarandı. Not: izin değişikliğinin oturuma yansımaması (bulgu 4) kısmen bilinçli LAN-only ertelemesi (tokenVersion) — bulgu bu yüzden backend mekanizmasından çok UI mesajsızlığına odaklı.

- **mb-offline:** SAĞLAM bulunanlar: offline çekirdek mimarisi tutarlı — queryClient persist+buster+resumePausedMutations (App.tsx:73-83), KK1 clientBarcode / QC2 clientErrorId idempotency anchor'ları ve registry yorumlarındaki backend idempotency sözleşmeleri (mutations.ts) kodla uyumlu; FasonSevk/FasonKabul/İade AsyncStorage draft'ları savedAt+TTL+restore-guard ile doğru; useManualRefresh (onlineManager guard + 12s timeout) tüm yeni ekranlarda (TartıPaket/Sevkiyat/Paketleme) ve PickerModal içinde benimsenmiş — FasonSevk:1271'deki refreshing={woQuery.isFetching} deprecated prop olup PickerModal içeride hook kullandığından zararsız; backend markReady/dispatchShipment atomik claim'leri ve return create koşullu flip'i doğrulandı (retry hasarını veri bozulmasından UX karışıklığına indiriyor). Bakılamayanlar: 2-5k satırlık Tambur/KursunQc/KK1/FasonSevk ekranlarının offline-dışı tam derinliği (yalnız offline ilgili kısımları örneklendi), BLE/etiket yazdırma yollarının offline davranışı, Login/auth akışı ve LMK/restart senaryosunun gerçek cihazda runtime doğrulaması (bulgular statik kod analizine dayanır).

- **mb-scan:** Sağlam bulunanlar: IadeGirisi (submit isPending kilidi + online-only mutation + tek-okuma kamera) temiz; KK1 scrap onayı preview yüklenirken confirm'i kilitliyor (confirmDisabled, KK1Screen.tsx:1439) ve backend confirmActive guard'ı bypass'ı kapatıyor; KK2 reopen onayı kartı çözen TÜM yollardan (input/kamera/liste) geçiyor, atlanabilir yol bulunamadı; Paketleme'nin scanBusy in-flight kilidi, çuval silme/iptal preview'ları ve ensureShipment/ensureSack tekil-promise desenleri doğru kurgulanmış; CutActionBar/StationActionButton loading kilitleri (Tambur Kes, recut) yerinde. Bakılamayanlar: backend idempotency iddiaları (dispatch cached-return, receive step-check, P2002 barkod cache) yalnız mobil koddaki yorumlardan okundu, backend kodu bu kapsamda doğrulanmadı; offline paused-mutation FIFO resume davranışı runtime'da test edilmedi; KartelaSevk/KartelaKabul/Depo ekranları kapsam listesinde olmadığı için incelenmedi. Tüm bulgular dosya:satır düzeyinde kod okunarak kanıtlandı; TanStack v5 mutation callback'lerinin son render closure'ını kullandığı (observer.setOptions) bulgu 2'nin temel varsayımı olarak ayrıca doğrulandı.

- **mb-api:** SAĞLAM bulunanlar: bugün silinen endpoint'lere (production/*, /kursun-qc/undo-qc2, /rolls/:id/identity, WO attach/detach/available-for-attach, /auth/register) mobilde sıfır canlı referans — tek kalıntı kullanılmayan ENDPOINTS sabiti; npx tsc --noEmit temiz. Mobil servislerden çıkarılan ~100 endpoint path'i backend route dosyalarıyla tek tek eşleştirildi (shipping/kartela/subcontractor/tambur/kursun-qc/returns/labels/label-templates/printed-documents/work-orders/devices/feature-flags) ve packing.service'in tüm sevkiyat akışı (scan/sacks/weigh/ready/unready/move-to-door/dispatch) backend'de mevcut. reportError sözleşmesi (defectTypeId zorunlu) hem KK2 hem Tambur'da mobilde zaten uyumlu (kursunQc.service.ts:23, models.ts:709 vs backend zod şemaları). Cursor sayfalama tipi (nextCursor/hasMore/totalEstimate) ve RollPickerModal filter anahtarları (status/statusIn/shipmentScope/rollScope/rollKind/dispatchableForStepId/markedForKartela/includeFire) backend buildRollWhere'de birebir karşılanıyor; fason receive (newRolls min(1)) ve preferences/feature-flags sözleşmeleri uyumlu; hardcoded IP/URL kalıntısı yok (dev host türetme + Settings'ten runtime URL override + SettingsScreen'deki 192.168 metinleri sadece placeholder). Cihaz pairing hata yolları (yanlış kod inline+toast, DEVICE_INACTIVE mesajı login ekranında gösteriliyor, Settings→DevicePairing'ten re-pair) tasarlandığı gibi. Bakılamayanlar: davranışlar statik kod okumayla doğrulandı (cihaz üstünde koşturulmadı); Electron frontend'i ve backend iç mantığı kapsam dışı bırakıldı (yalnız sözleşme karşılaştırması için okundu).

- **mb-ui:** Kapsam mobil/src; tüm ekranlar gezildi, iddialar dosya:satır okunarak doğrulandı. SAĞLAM bulunanlar: Pressable/TouchableOpacity yasağına eylem yollarında uyum tam (kalan Pressable'lar yalnız Keyboard.dismiss sarmalayıcıları ve AppModal/RemoteListSheet backdrop'ları — bilinçli); react-native-modal importu yok, tüm modallar AppModal/Portal üstünde (tek istisna 5. bulgu); sınırsız listelerin tamamı FlashList+cursor (geçmiş ekranları dahil), Card+onPress ihlali yok; useManualRefresh standardı her yenile butonunda (FasonSevk'teki deprecated refreshing prop'u PickerModal içinde yok sayılıyor, zararsız); Reanimated worklet'leri temiz (runOnJS doğru, AnimatedCounter'da Fabric crash fix'i belgeli, animasyonlar useReducedMotion'lı); package.json overrides expo-font ~14.0.11 yerinde ve npm ls tek kopya 14.0.12 gösteriyor; RollPickerModal İade/KartelaSevk/Paketleme/HızlıİşEmri'nde mevcut. Bakılamayanlar: uygulama cihazda çalıştırılmadı (statik inceleme — TartiPaket render maliyeti ve tablet/telefon yerleşimleri kod düzeyinde değerlendirildi), BLE/yazıcı donanım yolları ve Electron tarafı kapsam dışı.

## DÜZELTME GÜNLÜĞÜ — 2026-06-13 (yüksek bulgular, 12/12 ✅)

**Electron:**
- F-1/F-2 (rolls queryKey): `useDataTable`'a `queryKeyParts` eklendi — tablolar `["rolls", tab]` array key'ine geçti (tercih anahtarı string olarak korundu, sütun düzeni kaybolmaz); ManualEntryDialog ölü `["rolls:STOCK"]` yerine `["rolls"]` invalidate ediyor; RefreshButton hedefli `["rolls", tab]`. Artık roll mutate eden her yerin `["rolls"]` invalidate'i tüm sekme tablolarına + stats'a çarpıyor.
- F-5/F-9 (overlay sahipliği): dialog.tsx + sheet.tsx — scoped modal yalnız KENDİ karartmasına tıklanınca kapanır (`ownOverlayRef` eşitliği); pasif sekmedeki modal dış tıklamayla hiç kapanmaz (Esc guard'ının simetriği). Çapraz-sekme form veri kaybı kapandı.
- F-3 (yazdırma): yeni `lib/print.ts` — `.print-area` gizli iframe'e klonlanıp iframe basılıyor (canvas/QR pikselleri kopyalanır, görseller beklenir). Layout zinciri hapsi (tek sayfa kırpılması) ve arka plan sekmesindeki açık irsaliyenin baskıya binmesi birlikte çözüldü. 3 print dialog'u geçirildi. **Kullanıcı doğrulaması önerilir: 3 belge tipinde birer test baskısı.**
- F-4 (SUPERSEDED): üç frozen sheet'e `superseded`/`docNo`/`docVersion` prop'ları — revize edilmiş eski versiyon kâğıtta amber "REVİZE EDİLDİ" filigranı + köşede "Belge No · Rev.N" künyesi taşıyor.
- F-6 (izin semantiği): frontend'e backend'le birebir `matchesPermission` (domain wildcard); `useRoleAccess`/`PermissionGate`/OperationsHub'daki "isAdmin ise her şey serbest" bypass'ları kaldırıldı. isAdmin yalnız adminOnly nav görünürlüğünde.
- F-7 (Renkler izni): tile `property:read`, sayfa `property:write`, rota `ProtectedRoute requirePermission="property:read"` — backend color.routes ile hizalandı.

**Mobil:**
- F-8/F-9... (Kartela Sevk/Kabul replay): closure mutationFn'ler kaldırıldı — payload artık `mutate(vars)` ile registry default fn'ine gidiyor; offline pause + app restart sonrası replay gerçek payload'la çalışıyor (sevk/sayım kaybı kapandı). Toast'lar vars'tan üretiliyor.
- F-10 (KK2 çift basış): footer aksiyonlarına ref-tabanlı 700ms throttle (isPending'e bağlanmadan — offline kilitlenme yok); çift basışın ikinci vuruşu sonraki topu işaretleyemiyor/adımı kapatamıyor.
- F-11 (Tambur otomatik bitiş): finalize hedefi yanıt anındaki seçimden değil `variables.rollId`'den — uçuşta seçim değişirse ilgisiz top finalize edilmiyor.
- F-12 (401): "Oturum süresi doldu" toast'ı (5sn tekilleştirmeli) + OFFLINE_AWARE retry artık 401'i yeniden denemiyor. NOT: 401 ile düşen kuyruk kaydının login sonrası otomatik replay'i ayrı iş — şimdilik operatör kaydı yeniden gönderir (toast bunu söylüyor).

Doğrulama: Electron `tsc` ✅ · mobil `tsc` ✅. Orta (17) ve düşük (26) bulgular ⬜ — ayrı parti olarak ele alınacak.

## DÜZELTME GÜNLÜĞÜ — 2026-06-13 (orta bulgular 17/17 ✅ + düşük triyajı)

**Electron (orta):**
- O1 Çuval Depo geçişleri artık `["shipments"]` + `["shipment-detail", id]`'yi, unready/dispatch `["orders"]`'ı, dispatch `["rolls"]`'u da invalidate ediyor — açık Sevkiyat/Sipariş sekmeleri bayat kalmıyor.
- O2 Sipariş iptal dialog'u `activeShipments`/`canCancel`'ı okuyor: aktif sevkiyat varken onay butonu kapalı + engelleyen sevkiyatlar (no + durum) somut listeleniyor.
- O3/O6 (yazdırma bindirmesi) Y3 iframe-izole baskı ile birlikte çözüldü — iframe'e yalnız İLGİLİ dialog'un print-area'sı klonlanır.
- O4 Fason irsaliyesi: canlı overlay (renk + boyahane notu) sorgusu hata verirse fiş RENDER edilmez + Yazdır kilitli + "Yeniden Dene" — boyahaneye eksik talimat gitmez.
- O5 Esc artık `escape-stack` ile yönetiliyor: scoped dialoglarda Radix Esc'i pasif; capture-phase tek dinleyici AKTİF sekmedeki en üst dialog'u kapatır (arka plan sekmesindeki dialog aktif sekmenin Esc'ini öldürüyordu); global modallar `data-global-modal` ile Radix'e bırakılır.
- O7 İstemci timeout'u (ECONNABORTED) artık "sunucu kapalı" sayılmıyor; yedek indirme 5dk timeout'la.
- O8 Dashboard: 5 KPI kartı + 4 widget izin kapılı (`enabled` + gizleme, hook-sonrası return) — dar yetkili kullanıcıda login anındaki 403 toast fırtınası bitti.
- O9 Yetki kaydetme toast'ı artık "değişiklik bir sonraki girişte etkili olur" diyor (JWT tabanlı yetki gerçeği; tokenVersion mekanizması bilinçli ertelenmiş).
- O10 Backend `/orders/wo-picker` artık kardeşi coverage gibi `workorder:read/write`'ı da kabul ediyor — yalnız workorder:write'lı planlamacının WO formu kilitlenmiyor. (Master-data read izinleri için öneri: seed'e "masaüstü planlamacı" izin template'i — ayrı iş.)

**Mobil (orta):**
- O11 Kartela Sevk/Kabul: SyncStatusChip + offline'da "sıraya alındı" toast'u (FasonSevk deseni) — sonsuz spinner belirsizliği bitti.
- O12 KartelaKabul (rows+manifest+notes), KartelaSevk (toplar+firma+plaka) AsyncStorage draft yedeğine kavuştu (debounce + savedAt + 8h TTL + başarıda temizlik). Hızlı İş Emri okutma listesi ⏭️ (kısa süreli akış — düşük etki, kalıp hazır).
- O13 Paketleme okutmaları FIFO kuyruğunda — meşgulken gelen okuma artık SESSİZCE düşmüyor; aynı barkodun ardışık kareleri yutulur.
- O14 warehouse-scope tipine `preparing` kovası eklendi; Depo "Çuvalda" sayacı PREPARING'i de sayıyor.
- O15 BarcodeScannerModal'a `onPickFromList` slotu + Tambur Top Kesme'ye RollPickerModal (WAREHOUSE+STOCK filtre, seçimde aynı doğrulama) — kamera tek yol değil.
- O16 `useTruncationWarning` hook'u: 4 sabit-pageSize picker (KK1 kumaş, Tambur müşteri, Hızlı İş Emri renk, Sevkiyat Geçmişi müşteri) tavan aşımında artık operatörü uyarıyor (kalıcı çözüm: infinite mod — uyarı tetiklenirse).
- O17 Tartı/Paket açık siparişler FlashList'e taşındı (header bölümleri ListHeaderComponent) + müşteri/sipariş no/şube arama kutusu.

**Düşük triyajı (26 → 11 düzeltildi, 15 gerekçeli ⏭️):**
- ✅ Kartela detay sheet staleTime:0 + sayfa Yenile extraKeys `[["kartela"]]`; arşivleme `system-logs` + `system-logs-archive` invalidate; Çuval Depo 409'da liste tazeleme; Electron 401 toast 5sn tekilleştirme; Electron query retry deterministik 4xx'te kapalı; AuthHydrator süresi dolmuş token'ı login'e düşürüyor (exp kontrolü); kullanıcı değişiminde React Query cache.clear (CacheUserGuard); mobil OFFLINE_AWARE retry tüm 4xx'te fail-fast; Sevk Çıkışı geçiş hatalarında board refresh; mobil ölü `getReadyOrders`/`ReadyOrder` kaldırıldı (backend'de hiç olmayan /shipping/ready-orders).
- ⏭️ Bilinçli açık (gerekçe): WO iptal boş-gövde (preview hatasında buton zaten kilitli); ConfirmDialog isPending audit'i (geniş tarama); snapshot schemaVersion savunması + taslak doc-config tutarlılığı (tasarım işi); void-sonrası 5dk cache filigranı (staleTime:0 Kartela'da çözüldü, diğerleri nadir); Sunucu Durumu arka plan polling'i (yalnız sekme açık bırakılırsa); idle-logout modal uyarısı (tasarım kararı); müşteri alias sekme izni + 9 tanım rotası guard'ı (backend 403 koruyor, kozmetik tutarsızlık); mobil ham axios mesajları (kozmetik); Paketleme hayalet sevkiyat kilidi (domain kararı gerek); HizliIsEmri/KK2/Tambur/FasonSevk çift-çözümleme in-flight guard'ları (düşük frekans; O13 deseni hazır); Tambur over-cut bayat metraj (backend flag guard'lı); FasonKabul el yapımı iptal overlay'i (çalışıyor; AppModal taşıması ayrı iş).

**Doğrulama:** Electron `tsc` ✅ · mobil `tsc` ✅ · backend `tsc` ✅ (wo-picker izin değişikliği).
**Kullanıcı doğrulaması bekleyen:** 3 belge tipinde test baskısı (Y3 iframe geçişi) + Electron'da hızlı bir duman testi (dialog Esc/overlay davranışı yapısal değişti).

## AÇIK KALANLAR TURU — 2026-06-13 (kullanıcı kararları: oto-kurtar + uyarı/uzat + A grubu tamamı)

| # | İş | Durum |
|---|---|---|
| K-A1 | ConfirmDialog çift-tık taraması (isPending'siz yıkıcı onaylar) | ✅ |
| K-A2 | Void sonrası filigransız baskı — void/reissue mutation'ları belge cache'ini invalidate etsin | ✅ |
| K-A3 | Mobil ham İngilizce axios mesajları → Türkçe (api.ts eşleme) | ✅ |
| K-A4 | Hızlı İş Emri meşgulken yutulan okutma → FIFO kuyruk (O13 deseni) | ✅ |
| K-A5 | KK2/Tambur kart çözümleme + FasonSevk addRoll in-flight guard'ları | ✅ |
| K-A6 | WO iptal dialogu: impact yüklenemeyince hata bandı + yeniden dene | ✅ |
| K-A7 | 9 tanım rotasına ProtectedRoute (tile-config izinleriyle) | ✅ |
| K-A8 | Sunucu Durumu: pasif sekmede 5sn polling durdur (isTabActive gate) | ✅ |
| K-B1 | Paketleme hayalet sevkiyat OTOMATİK kurtarma (açılışta + hata sonrası PREPARING eşleştir-bağlan) | ✅ |
| K-B2 | Idle logout: T-60sn "oturum kapanmak üzere" uyarısı + aktiviteyle uzatma | ✅ |

### Açık kalanlar turu — günlük (2026-06-13, 10/10 ✅)

- **K-A1** ConfirmDialog'a MERKEZÎ çift-tık kilidi: onay tıklanır tıklanmaz buton kilitlenir (2.5sn emniyet penceresi + dialog yeniden açılınca sıfır) — `isPending` vermeyen 10 kullanım tek noktadan korundu.
- **K-A2** Belge dialog'larının "güncel belge" sorguları `staleTime: 0` — başka istemciden void/revize edilen belge artık 5dk'ya kadar filigransız basılamıyor (Electron'da void tetikleyen akış yok; cross-client durumda invalidate işlemez, taze çekim tek doğru çözüm).
- **K-A3** Mobil ham axios İngilizcesi Türkçeleştirildi (timeout/ağ hatası ayrımıyla).
- **K-A4** Hızlı İş Emri okutmaları FIFO kuyruğunda (Paketleme deseni) — meşgulken okutulan top artık kaybolmuyor.
- **K-A5** KK2 + Tambur kart çözümlemede in-flight guard (çift okutma = çift sekme bitti); FasonSevk'te eşzamanlı aynı-barkod çözümlemesi `resolvingBarcodesRef` set'iyle engellendi (çift satır bitti).
- **K-A6** WO iptal dialogu: etki önizlemesi yüklenemezse hata bandı + "Yeniden Dene" (boş gövde yerine).
- **K-A7** 8 tanım rotasına daha ProtectedRoute (tile-config izinleriyle birebir) — URL ile direkt gidişte net "yetki yok".
- **K-A8** Sunucu Durumu polling'i pasif sekmede duruyor (`isTabActive ? 5sn : kapalı`).
- **K-B1 (kullanıcı kararı: oto-kurtar)** Paketleme: createShipment transport hatasında (timeout/ağ — istek sunucuya ulaşmış olabilir) PREPARING sevkiyatlar taranıp sipariş kümesini içeren bulunursa OTOMATİK bağlanılıyor + bilgi toast'u; backend reddi (4xx) normal hata akışında.
- **K-B2 (kullanıcı kararı: uyarı+uzat)** Idle logout: kapanmadan 60sn önce kalıcı "Oturum kapanmak üzere" uyarısı; herhangi bir aktivite süreyi uzatıp uyarıyı kapatır (uyarı görünürken throttle beklenmez). Çok kısa timeout'larda uyarı süreye oranlanır.

Doğrulama: Electron `tsc` ✅ · mobil `tsc` ✅.
