
## backend-servis  keys=['conventions', 'gaps', 'docVsCode', 'sizeProposal', 'verdict']
### conventions (24)
- [insan|dev=77] Yeni servis = düz fonksiyon modülü (`export async function`) ya da `export const XService = {}` nesnesi. Class + singleton devralınan kalıptır, yeni dosyada açılmaz. ⟶ ex: Teks-Erp/src/services/settings-password.service.ts:73 · sack-tag.service.ts:108
- [insan|dev=0] Static-only class YALNIZ altyapı servisidir (audit, auth, device, oturum, izin). Domain servisi static class kullanmaz. ⟶ ex: Teks-Erp/src/services/audit.service.ts:44
- [insan|dev=143] `ApiResponse<T>` zarfını SERVİS kurar (`{ success:true, data, message? }`); controller yalnız `res.json(await XService.m(...))` yapar. ⟶ ex: Teks-Erp/src/services/sack-tag.service.ts:113-122
- [insan|dev=0] `helpers/` altındaki hiçbir dosya HTTP zarfı kurmaz — helper saf değer/where/guard döner. ⟶ ex: Teks-Erp/src/services/helpers/quality-station.helper.ts:88
- [insan|dev=2] Zod servis katmanına GİRMEZ. Doğrulama controller (21 dosya) / route (37 dosya) katmanında yapılır; servis tiplenmiş girdi alır. ⟶ ex: Teks-Erp/src/controllers/shipping.controller.ts:295
- [eslint|dev=0] `prisma.$transaction` YALNIZ servis katmanında açılır ve orada kapanır. ⟶ ex: Teks-Erp/src/services/sack-tag.service.ts:296
- [eslint|dev=0] Transaction closure parametresi DAİMA `tx` adlanır; tx içinde `Promise.all/allSettled` YASAK, sıralı `await` kullanılır. ⟶ ex: Teks-Erp/eslint.config.mjs (TX_PROMISE_ALL)
- [bekçi|dev=0] `AuditService.log` transaction'ın DIŞINDA, yazma başarıyla bittikten sonra çağrılır; best-effort olduğu için isteği düşürmez. ⟶ ex: Teks-Erp/src/services/sack-tag.service.ts:296-311
- [insan|dev=0] Transaction içinde dış I/O (fetch/axios/fs/exec/setTimeout) YOK; tx kısa tutulur, bütçe global (`maxWait 5s / timeout 20s`). ⟶ ex: Teks-Erp/src/lib/prisma.ts:87-92
- [insan|dev=65] Audit `await` edilir. `void AuditService.log(...)` ateşle-unut yalnız finans paketinin bilinçli istisnasıdır. ⟶ ex: Teks-Erp/src/services/payment-allocation.service.ts:399
- [insan|dev=17] Kullanıcıya dönen her hata `AppError.<factory>(mesaj, details?)` ve mesaj TÜRKÇE. `throw new Error` yalnız iç sentinel / programcı hatası için. ⟶ ex: Teks-Erp/src/utils/app-error.ts:31-59
- [insan|dev=0] `details.code` TALEP GÜDÜMLÜDÜR: yalnız istemcinin dallanması gereken hataya yazılır, her hataya değil. ⟶ ex: Teks-Erp/src/services/inventory.service.ts:3726
- [insan|dev=117] Her Prisma okuması AÇIK projeksiyon taşır ve `select` tercih edilir; `include` yalnız gerçekten tüm ilişkiyi taşımak gerektiğinde. ⟶ ex: Teks-Erp/src/services/sack-tag.service.ts:39-47
- [tsc|dev=211] Projeksiyon dosya başında sabitlenir: `const X_SELECT = {...} satisfies Prisma.XSelect` ve dönüş için adlandırılmış arayüz/DTO tanımlanır. ⟶ ex: Teks-Erp/src/services/sack-tag.service.ts:39-56
- [insan|dev=0] Decimal kolonda JS float aritmetiği yok: `new Prisma.Decimal` + `.plus()/.minus()` ya da DB-side `increment/decrement`. ⟶ ex: Teks-Erp/src/services/inventory.service.ts:1863
- [insan|dev=15] Çağıranın transaction'ında koşmak ZORUNDA olan fonksiyonun adı `*Tx` biter ve ilk parametresi `tx: Prisma.TransactionClient`'tir. ⟶ ex: Teks-Erp/src/services/sack-tag.service.ts:499 clearSackTagsOnDispatchTx
- [insan|dev=18] Ortak parça `services/helpers/<konu>.helper.ts` dosyasına çıkar; helper küçük ve tek konuludur. ⟶ ex: Teks-Erp/src/services/helpers/sack-tag.helper.ts
- [insan|dev=0] Yardımcı fiil sözlüğü sabit: `assertX` (void + throw guard), `resolveX` (tek değer türet), `buildX`, `loadX`, `collectX`, `normalizeX`, `computeX`. ⟶ ex: Teks-Erp/src/services/settings-password.service.ts:107 assertSettingsPasswordUsable
- [insan|dev=49] Aktör kimliği metodun SON parametresidir ve opsiyoneldir: `userId?: string`. ⟶ ex: Teks-Erp/src/services/sack-tag.service.ts:123
- [insan|dev=18] Dosya `// ====` banner'ı ile açılır: ne olduğu + NEDEN'i 3-10 satırda. ⟶ ex: Teks-Erp/src/services/sack-tag.service.ts:1-18
- [tsc|dev=1] TS hijyeni sert: `any` ve `@ts-ignore` yok, tipler açık. ⟶ ex: Teks-Erp/src/services/search.service.ts:134
- [insan|dev=793] Yorum 1-3 satır, koruduğu satırın hemen üstünde ve yalnız NEDEN/değişmezi söyler; tarih ve ölçüm anlatısı arşive gider. ⟶ ex: docs/KOD-KURALLARI.md § Yorum politikası
- [insan|dev=5] REÇETE 1-6 · yeni servis metodu: (1) yeni yüzey → yeni `<konu>.service.ts`, düz fonksiyon; (2) banner + NEDEN; (3) `X_SELECT satisfies Prisma.XSelect` + adlandırılmış Dto, `include` yok; (4) imza `(input|id, …, userId?)`… ⟶ ex: Teks-Erp/src/services/sack-tag.service.ts
- [bekçi|dev=0] REÇETE 7-12: (7) yazma tek `$transaction(async (tx)…)`, tx içinde Promise.all/dış I/O/audit YOK; (8) durum geçişi → atomik claim `updateMany WHERE {id,durum}` + `count===0 → 409`, benzersiz no `withBarcodeRetry` + sequen… ⟶ ex: Teks-Erp/scripts/test_audit_depth.ts:19-23
### gaps (8)
- [yüksek|büyük] Servis dönüş tipleri tiplenmemiş: `ApiResponse<unknown>` fiilen 'tip yok' demek ve istemci sözleşmesi derleyicide hiç ölçülmüyor. ⟶ 429 `Promise<ApiResponse<…>>` imzasının 211'i `<unknown>`/`<any>`, 17'si `<Record<string,unknown>>`; tüm serviste yalnız 4 `export interface XDto` ve 7 `ApiResp…
- [orta|küçük] Boyut kapısı hiç yok: ne ESLint ne 453 bekçiden biri dosya/fonksiyon uzunluğu ölçüyor. ⟶ grep 'MAX_LINES|lineCount|satir' scripts/test_*.ts → 0. Sonuç: subcontractor.service.ts 7142 satır / 24 metod (~297 satır/metod), workorder.service.ts 6846 / 34…
- [orta|orta] HTTP zarfının yeri ayrışmış — aynı sistemde iki sözleşme var: kimi servis `ApiResponse` döner, kimi ham nesne; route/controller da zarf kuruyor. ⟶ 113 servis dosyasının 62'si ApiResponse kullanıyor, 51'i kullanmıyor. `success: true`: services 597, routes 107, controllers 36. En yeni 5 servisin 4'ü (client-…
- [orta|orta] `*Tx` ad sözleşmesi %69'da kalmış; sonekli olmayan bir fonksiyonun tx zorunluluğu imzadan başka hiçbir yerde görünmüyor. ⟶ Export edilen tx-alan 48 fonksiyonun 15'i soneksiz (helpers/finance.helper.ts:38 nextInvoiceNo, :210 resolveExchangeRate, helpers/order-status.helper.ts:96 reco…
- [orta|orta] Yorum politikası (2026-09-05) ile fiilî kod ters yönde ve fark EN YENİ dosyalarda en büyük — yeni kural bugün yazılmış olsa da örnek alınacak dosyalar onu ihlal ediyor. ⟶ services %25 yorum (34.126/131.907), 793 tarihli yorum satırı, 1613 ⚠️. En yorum-yoğun 8 dosyanın 8'i de son 6 haftada eklenen helper: roll-entry-station %86, s…
- [orta|büyük] `include:` kullanımı DB kuralı 7 ile çelişiyor ve devralınan dosyalarda yoğun; snapshot/ilişki yükünü listeye taşıma riski kapısız. ⟶ 269 `include:` satırı: subcontractor.service.ts 60, workorder.service.ts 50, tambur 19, inventory 19, item 13. Ayrıca 117 okuma çağrısı hiç projeksiyon taşımıyo…
- [orta|büyük] Servis içi yardımcıya çıkarma eşiği tanımsız: 5 büyük serviste 12-24 helper import'u var ama metodlar hâlâ 200-420 satır. ⟶ subcontractor 19 helper import / 24 metod / 7142 satır; workorder 17 / 34 / 6846; inventory 24 / 24 / 5420. En uzun 6 fonksiyon: accounting-export:126 (549), wo…
- [düşük|orta] Zorlama dengesizliği: ESLint'te yalnız 6 kural var ve hiçbiri servis biçimini (class/fonksiyon, zarf yeri, Tx soneki, DTO) ölçmüyor; standardın çoğu 'insan' kategorisinde. ⟶ eslint.config.mjs 6 selector (tx+Promise.all, notIn:[], orderBy batchNumber, requireModule, Europe/Istanbul, route/controller prisma import). Bu raporda ölçülen…
### docVsCode (12)
- [uyumlu] Servis: transaction + `AuditService.log()` (tx dışında) ⟶ 323 audit çağrısının 0'ı tx bloğu içinde (brace-derinliği taraması; aynı tarama tx içinde 804 `tx.` satırı buldu → dedektör sağlam).
- [uyumlu] Route/controller'da `prisma` import YASAK (ESLint no-restricted-imports) ⟶ routes/ + controllers/ altında `lib/prisma` importu 0; iki isabet de kuralı ANLATAN yorum satırı (admin.routes.ts:187, tambur.controller.ts:45).
- [uyumlu] `$transaction` ve iş mantığı servis katmanındadır ⟶ `$transaction`: services 196, controllers 0, routes 0. Zod: services 2/269 (ikisi de paylaşılan şema helper'ı), controllers 21, routes 37.
- [uyumlu] Tx kısa, dış I/O tx içinde yok ⟶ 196 tx bloğu tarandı; fetch/axios/fs/exec/setTimeout gerçek isabeti 0 (tek şüphe inventory.service.ts:4696'da `const fs = await tx.workOrderStep.findUnique` yanlış pozitifi).
- [uyumlu] Decimal kolonda JS float yok — DB-side increment/decrement ya da `Prisma.Decimal` ⟶ 229 `new Prisma.Decimal`, 339 `.plus/.minus`, 34 `increment:/decrement:`; metraj/kg/tutar kolonlarında ham aritmetik 0.
- [uyumlu] `any` yok ⟶ `as any` 1, `@ts-ignore` 0; `: any` 12 isabetin 11'i yanlış pozitif. Tek gerçek: search.service.ts:134 dinamik Prisma delegate erişimi.
- [uyumlu] Durum geçişi → atomik claim (`updateMany WHERE {id,durum}` + `count===0 → 409`); `findUnique→if→update` YASAK ⟶ 100 `count === 0` kontrolü + 26 `pg_advisory_xact_lock` + 99 `withBarcodeRetry`. Kalıp yerleşik. Kapsam tamlığı ölçülmedi (BELİRSİZ).
- [kod ihlal] `select`, `include` değil (DB kuralı 7) ⟶ select 2601 / include 269 (~10:1) ama include hâlâ 269 yerde ve devralınan dosyalarda yoğun (subcontractor 60, workorder 50). Ayrıca 117 okuma hiç projeksiyon taşımıyor.
- [belge yanlış] AppError.details.code 'opsiyonel ama kullanılması önerilir — message string match'inden sağlam dedektör için' ⟶ 409'ların %10'u (54/534), 400'lerin %4'ü (44/976) kod taşıyor. İstemci yalnız 41 yerde okuyor (Electron 18, mobil 23). Fiilî kural 'öneri' değil, 'istemci dallanacaksa'.
- [kod ihlal] Koddaki yorum 1-3 satırdır, yalnız NEDEN'i söyler; tarih ve ölçüm anlatısı yoruma GİRMEZ ⟶ services %25 yorum satırı (34.126/131.907), 793 tarihli yorum, 1613 ⚠️; en yorum-yoğun 8 dosyanın hepsi son 6 haftada eklenen helper (%71-86).
- [uyumlu] Master data CRUD için `BaseController` + `BaseService` ⟶ base.service.ts var ve `buildListWhere`/cursor/künye/ad-normalizasyonu orada toplanmış; ancak yeni servislerin hiçbiri (sack-tag, settings-password, client-registry, boss/overview) BaseService'i geniş…
- [BELİRSİZ] Bekçi çıktısı `check(label, ok)` + '=== Sonuç: N geçti, M başarısız ===' + `$disconnect()`+`pool.end()` ⟶ scripts/test_audit_depth.ts:19-23 tam bu biçimde. 453 test dosyasında kapsam tamlığı bu turda ölçülmedi.
### sizeProposal
Kaynak: boyut-dagilimi.json backend-services (dosya p50 236 / p90 1001 / max 7143; fn p50 14 / p90 59 / p99 160) + kendi ölçümüm (üst-seviye fn n=1325: p50 10 / p90 72 / p95 112; helper dosya p50 135 / p90 306). ÖNERİ — yalnız YENİ kod için, devralınan sınır dışı: (1) fonksiyon UYARI 60, SERT 80 satır → bugünkü ihlal 80 üstü 269 fn (boyut dosyası) / 109 üst-seviye fn; 200 üstü 28. (2) yeni `.helper.ts` ≤ 300 satır → ihlal 11/106 (>400: 3). (3) yeni `<konu>.service.ts` ≤ 400 satır → devralınanda ihlal 78/163 (>600: 49); p75=463 olduğu için 400 gerçekçi tavan, 500 gevşek. (4) sert kural: 1000 satırı geçen YENİ servis dosyası açılmaz (bugün 26 dosya). Zorlama: bugün hiçbir kapı yok (ESLint 6 kuralın hiçbiri boyut ölçmüyor, 453 bekçide 'satir/MAX_LINES' araması 0) → tek satırlık `scripts/test_dosya_boyutu.ts` bekçisi + git-yeni-dosya süzgeci ile kurulabilir (fixSize: küçük).
### verdict
Fiilî kalıp NET ve son 6 haftada tek yöne oturmuş: yeni servis = düz `export async function` modülü (son 30 dosyanın 27'si), Zod dışarıda (2/269), `$transaction` yalnız serviste (196/0/0), closure daima `tx`, audit tx DIŞINDA (0/323 ihlal), tx içinde dış I/O yok (0), hata `AppError` (1922 vs 17), okumada açık projeksiyon (%93), Decimal'de float yok (0). Bunlar "yeni kodda ZORUNLU" yazılabilir; class+singleton (77 dosya), `include:` (269), ApiResponse<unknown> (211), 6800+ satırlık servisler devralınan ve sınır dışı. Üç gerçek boşluk: dönüş tipi tiplenmemiş (211/429), boyut kapısı hiç yok, zarfın yeri ayrışmış (62/51 dosya). Bir de belge-kod çelişkisi: 2026-09-05 yorum politikası 1-3 satır diyor, en yeni helper'lar %80 yorum. Sonuç JSON: scratchpad/standart/results/backend-servis.json (12 adımlık reçete dahil).

## backend-katman  keys=['conventions', 'gaps', 'docVsCode', 'sizeProposal', 'verdict']
### conventions (12)
- [eslint|dev=0] Katman sırası Routes → Controllers → Services → Prisma; route/controller'da `lib/prisma` import YASAK ⟶ ex: src/routes/workorder.routes.ts → controllers/workorder.controller.ts → services/workorder.…
- [insan|dev=36] Controller İNCEDİR: Zod parse + servis çağrısı + res.status(N).json(result); iş mantığı/transaction yok ⟶ ex: src/controllers/base.controller.ts:67-75 findAll
- [insan|dev=127] Zarfı SERVİS kurar (ApiResponse<T>/PaginatedResponse<T>), controller yalnız statü kodunu seçip aynen basar ⟶ ex: src/types/api.types.ts:6 + src/controllers/base.controller.ts:71
- [bekçi|dev=53] Kapı sırası: dosya başında `router.use(verifyToken, requireXEnabled)`, sonra handler'da requirePermission/requireAnyPermission ⟶ ex: src/routes/workorder.routes.ts:21 router.use(verifyToken, requireProductionEnabled)
- [bekçi|dev=1] Modül kapısı ADLANDIRILMIŞ middleware; jenerik requireModule("x") yasak ⟶ ex: src/middlewares/module.middleware.ts:79-82
- [insan|dev=17] Zod şeması modül düzeyinde `const xSchema = z.object({...})` ve `.parse(req.body ?? {})`; ayrı schema dosyası yok ⟶ ex: src/controllers/shipping.controller.ts:60 weighSackSchema
- [insan|dev=4] Yazma ucunun gövdesi Zod'suz okunmaz ⟶ ex: src/routes/order.routes.ts:291 cancelBodySchema.parse(req.body ?? {})
- [insan|dev=1857] Hata = AppError + isteğe bağlı details.code; error.middleware tek çeviri noktası, app.ts'te EN SON mount ⟶ ex: src/utils/app-error.ts:30 + src/middlewares/error.middleware.ts:417-434
- [tsc|dev=2] Master data CRUD kod yazmadan: `new BaseController(xService)` + `class XService extends BaseService` ⟶ ex: src/routes/item.routes.ts:31 + src/services/item.service.ts
- [bekçi|dev=60] Her uç Swagger JSDoc bloğu (@openapi) taşır; spec routes/** + controllers/**'tan üretilir ⟶ ex: src/routes/workorder.routes.ts:26-40
- [bekçi|dev=0] app.ts mount düzeni: uzaklık → helmet/cors/compression/morgan → latency/clientInfo → body/static → resolveDevice/requestContext → rate limit → Access JWT → 72 API mount → /api 404 → SPA → errorHandler ⟶ ex: src/app.ts:810 app.use('/api', verifyAccessJwt(remoteAccess))
- [insan|dev=10] Audit servis katmanında ve tx dışında ⟶ ex: src/services/workorder.service.ts
### gaps (10)
- [yüksek|küçük] 'Zarfı kim kurar' hiçbir kural kaynağında yazmıyor — fiilen ÜÇ üretici var ⟶ services 429 tipli ApiResponse dönüşü + 106 inline route handler kendi 'success:true'sunu kuruyor + reportEnvelope (services/reports/_shared.ts:157) 21 çağrı; A…
- [yüksek|küçük] Controller'sız route artık ÇOĞUNLUK ama hâlâ 'bilinçli istisna' diye anlatılıyor ve ölçütü yazılı değil ⟶ 45/80 route dosyası controller import etmiyor; 323/672 handler inline. Gerekçeyi yazan tek dosya record-info.routes.ts:4. Teks-Erp/CLAUDE.md 'Yeni endpoint kont…
- [orta|orta] Swagger kapsamının mekanik bekçisi yok ⟶ test_swagger_spec.ts:27 PATH_COUNT_FLOOR=120 iken gerçek 502 yol — zemin hiç ısırmıyor; uç↔blok eşlemesi ölçülmüyor, sonuç ~60 belgesiz uç (612/672).
- [orta|orta] details.code sözleşmesi tanımsız: ne zaman zorunlu, hangi dallar taşır ⟶ 120/1977 AppError çağrısı code taşıyor (%6); error.middleware'in Prisma (9 dal) ve ZodError dalları HİÇ code üretmez ve ZodError gövdesi `errors[]` ile ayrı şek…
- [orta|küçük] requireFinanceEnabled, kardeş dört kapının MODULE_DISABLED sözleşmesini taşımıyor ⟶ finance.middleware.ts:22-27 düz AppError.forbidden(mesaj); module.middleware.ts:81 `{ code: 'MODULE_DISABLED', modul }`. 30 mount etkileniyor — en çok kullanıla…
- [orta|büyük] verifyToken'ın handler başına yazıldığı fail-open desen hâlâ baskın ⟶ 53/80 dosya router.use kapısı kullanmıyor, 244 handler argümanında verifyToken var. test_route_auth_coverage.ts:5-11 bu deseni bizzat 'FAIL-OPEN' diye tanımlıyo…
- [orta|küçük] `.strict()` ne zaman zorunlu — kural yok, kullanım %24 ⟶ 50/208 adlandırılmış z.object'te .strict(); shipping.controller.ts:71-77 'LOAD-BEARING' gerekçesini (opsiyonel alan + `?? null` → sessiz silme) yazıyor ama gene…
- [orta|orta] Servis katmanında Express Request/Response sızıntısı için sınır çizilmemiş ⟶ 20 imza / 11 servis dosyası `req: Request` alıyor (base.service.ts:390 findAll dahil); services/helpers/guarded-hard-remove.ts 5 route-handler fabrikası üretiyo…
- [düşük|orta] Route dosyası için boyut sınırı yok ⟶ admin.routes.ts 1982, workorder 1128, inventory 1016, order 930, tambur 852 satır; 22 route dosyası >300 (boyut-dagilimi.json backend-routes). Buna karşılık inl…
- [düşük|küçük] Audit'in route katmanından yazılabildiği hiçbir yerde kabul/red edilmemiş ⟶ 10 çağrı / 3 dosya (admin.routes.ts ×8, db-copy.routes.ts:191, feature-flag.routes.ts:101). Kural 'Service: AuditService.log()' diyor.
### docVsCode (12)
- [belge yanlış] 'Routes → Controllers → Services → Prisma, alt katman atlanmaz. Bilinçli istisna: ince read/ayar uçları controller'sız' ⟶ 45/80 route dosyası controller'sız, 323/672 handler inline — istisna ÇOĞUNLUK. Ayrıca 15 dosya kendi controller'ı yerine generic `new BaseController(service)` kuruyor; yalnız 20 dosyada özel controlle…
- [uyumlu] 'route/controller'da prisma import YASAK — mekanik: ESLint no-restricted-imports' ⟶ Doğru: 0 gerçek ihlal; eslint.config.mjs:100-121 kuralı src/routes/** ve src/controllers/**'a bağlı.
- [belge yanlış] Yeni uç kontrol listesi: 'Controller: Zod + servis' ⟶ Zod route'ta da yaşıyor (83 z.object / 66 adlandırılmış); zarfı controller değil SERVİS kuruyor (429 ApiResponse dönüşü); controller res.status(N).json(result) ile sınırlı.
- [kod ihlal] Yeni uç kontrol listesi: 'Route: verifyToken + requirePermission(kod) + Swagger JSDoc' ⟶ verifyToken/izin kısmı tutuyor (377 requirePermission + 247 requireAnyPermission, kapı sırası ihlali 0); Swagger kısmı ~60 uçta eksik (612/672) ve 6 dosyada hiç yok.
- [belge yanlış] 'Modüle aitse ADLANDIRILMIŞ kapı (requireProductionEnabled / requireTicaretEnabled / requireIplikEnabled / requireDepoMultiEnabled, module.m… ⟶ Liste EKSİK: en çok kullanılan kapı `requireFinanceEnabled` (30 mount, finance.middleware.ts:21) listede yok. Jenerik fabrika yasağına uyum tam (0 ihlal).
- [kod ihlal] 'Hata kodu details.code; kapalı modül 403 MODULE_DISABLED' ⟶ details.code yalnız %6 çağrıda (120/1977) ve Prisma/Zod dalları hiç üretmiyor; MODULE_DISABLED'ı module.middleware'in 4 kapısı yazıyor, requireFinanceEnabled YAZMIYOR.
- [uyumlu] 'Master data CRUD için BaseController + BaseService; liste/cursor/özet tek where buildListWhere' ⟶ Doğru ama kalıp beklenenden farklı: `extends BaseController` 0 — routes doğrudan `new BaseController(service)` kuruyor (18 örnek/15 dosya); `extends BaseService` 14.
- [kod ihlal] 'Service: transaction + AuditService.log() (tx dışında)' ⟶ Ağırlıkla doğru (services 301 audit, 196 $transaction; routes/controllers 0 $transaction) ama route katmanında 10 audit çağrısı var (admin ×8, db-copy, feature-flag).
- [uyumlu] 'app.ts'e app.use("/api/...", routes)' ⟶ 72 mount; önek çakışmaları dar-önce sıralı; verifyAccessJwt tüm /api mount'larından önce (app.ts:810); errorHandler en sonda (987). Bekçi: test_middleware_order.ts + test_route_mount_reachability.ts.
- [uyumlu] 'any yok' ⟶ routes 0, controllers 0, middlewares 0, services 2.
- [uyumlu] KARARLAR-TASLAK K4: 'Route: ... İş mantığı YOK' ⟶ Fiilen tutuyor: 323 inline handler'ın 256'sı ≤25 satır, 33'ü 26-50, yalnız 2'si 51-100, 0'ı >100; 60+ satırlık TEK handler admin.routes.ts:179 ve gerekçesi yorumda. Sorun handler değil dosya boyutu (a…
- [kod ihlal] KARARLAR-TASLAK K4: 'Helper: ... DB'ye kendi başına gitmez (tx parametresi alır)' ⟶ services/helpers/guarded-hard-remove.ts bu tarifin dışında: Express route handler'ı üretiyor, res.status().json() yazıyor ve 5 route dosyasından doğrudan mount ediliyor (station×2, route, product-reci…
### sizeProposal
Fiilî dağılıma dayalı (boyut-dagilimi.json + inline handler ölçümü): **Route handler ≤ 40 satır** (inline 323 handler'ın %79'u ≤25, %89'u ≤50, yalnız 2'si >50) · **Controller metodu ≤ 40 satır** (p50 14, p90 29) · **Route dosyası ≤ 500 satır** (bugün 22 dosya >300, 12 dosya >500; en büyüğü admin.routes 1982) · **Controller dosyası ≤ 600** (bugün 8 dosya >300, max 1099). Hepsi "yeni ve dokunulan kodda"; devralınan 5 büyük route + 5 büyük controller sınır dışı, baseline sayacı ARTMAZ kapısıyla. Zorlama: eslint max-lines/max-lines-per-function warn + CI baseline tavanı.
### verdict
Katman sözleşmesi KOD TARAFINDA sağlam, BELGE tarafında bayat. Mekanik olarak zorlanan tek kural (route/controller'da prisma yasağı) %100 tutuyor: 0 ihlal, ESLint kapısı yeşil. İş mantığı gerçekten serviste — routes/controllers'ta 0 `$transaction`, 0 `any`, controller fonksiyon p50 14 satır, 323 inline route handler'ının yalnız 1'i 60 satırı geçiyor (o da gerekçeli). Kapı sırası da temiz: `router.use(verifyToken, requireXEnabled)` satırından ÖNCE tanımlanmış tek bir uç yok. Buna karşılık belgenin üç cümlesi ölçümle çürüdü: ① "controller'sız uç bilinçli istisnadır" — 45/80 dosya ve 323/672 handler controller'sız, yani ÇOĞUNLUK; ② "Controller: Zod + servis + ApiResponse zarfı" — zarfı SERVİS kuruyor (429 `Promise<ApiResponse<>>`, ApiResponse'u hiçbir route/controller import etmiyor), controller yalnız `res.status(N).json(result)`; ③ modül kapısı listesi en çok kullanılan kapıyı (requireFinanceEnabled, 30 mount) atlıyor ve o kapı kardeşlerinin `details.code:'MODULE_DISABLED'` sözleşmesini taşımıyor. Sayılabilir sapmalar dar ve adreslenebilir: Zod'suz gövde 4 nokta, route'ta audit 10 çağrı/3 dosya, serviste Express Request/Response 20 imza/11 dosya (+guarded-hard-remove'un 5 route-handler fabrikası), Swagger'da ~60 belgesiz uç. Standart yazarken en yüksek getirili üç madde: (a) "zarfı servis kurar, controller/route zarf ÜRETMEZ" kuralını yazmak ve ölçmek — bugün üç üretici var; (b) controller'sız ucun ÖLÇÜTÜNÜ yazmak (bugün 45 dosyanın 1'i gerekçesini yazıyor); (c) Swagger kapsam…

## veritabani-model  keys=['conventions', 'gaps', 'docVsCode', 'sizeProposal', 'verdict']
### conventions (29)
- [BELİRSİZ|dev=1] PK: `id String @id @default(uuid()) @db.Uuid` ⟶ ex: prisma/schema.prisma:7661 (StockCount)
- [tsc|dev=1] Tablo adı `@@map("snake_case_çoğul")`; kolon `@map` kullanılmaz ⟶ ex: prisma/schema.prisma:4612 @@map("sack_tags")
- [bekçi|dev=4] Her `DateTime` alanı `@db.Timestamptz`; tek muafiyet takvim-günü kolonları (`@db.Date`) ⟶ ex: prisma/schema.prisma:4607
- [BELİRSİZ|dev=4] createdAt `@default(now()) @db.Timestamptz`; mutasyona uğrayan modelde ayrıca `updatedAt @updatedAt @db.Timestamptz`. Append-only defter/pivot yalnız createdAt ⟶ ex: prisma/schema.prisma:3284 (RollPlanDeviation — yalnız createdAt)
- [BELİRSİZ|dev=?] Her UUID kolonu `@db.Uuid`; metin kolonları `@db.VarChar(n)` ile sınırlı ⟶ ex: prisma/schema.prisma:3268 field String @db.VarChar(16)
- [BELİRSİZ|dev=?] Katalog/master-data modeli: değişmez `code` + `name`/`label` + `sortOrder Int @default(0)` + `isActive Boolean @default(true)`; silme yok, isActive:false ⟶ ex: prisma/schema.prisma:4585-4600 (SackTag)
- [BELİRSİZ|dev=1] Künye: master-data'da `createdById`/`updatedById String? @db.Uuid`; defter tablosunda yalnız aktör alanı ⟶ ex: prisma/schema.prisma:4604-4605
- [insan|dev=0] KÜNYE FK'SI INDEX ALMAZ — 'kim yaptı' FK'ları kural gereği index'siz ⟶ ex: prisma/schema.prisma:530-532 (User.createdById/updatedById — index yok)
- [BELİRSİZ|dev=5] Her DOMAIN FK'ya `@@index([fk])`; composite unique ÖN EKİ index sayılır, 4. ağaç yazılmaz ⟶ ex: prisma/schema.prisma:7742-7745 (StockCountLine — FK index'i composite unique ön ekinden, y…
- [insan|dev=0] Composite index sırası: eşitlik kolonları önce, tarih/range kolonu sonra ⟶ ex: prisma/schema.prisma:3298 @@index([field, createdAt])
- [bekçi|dev=0] Partial index ham SQL migration'da (DROP + CREATE ... WHERE); şemada düz `@@index` BIRAKILIR. Partial UNIQUE için şemada `@@unique` (Prisma predicate farkını drift saymaz, index↔unique farkını SAYAR) ⟶ ex: schema.prisma:2035 `@@index([sackId])` ↔ DB'de rolls_sackId_idx WHERE ("sackId" IS NOT NUL…
- [bekçi|dev=4] Şema-dışı her DB nesnesi test_db_invariants envanterine yazılır — DOKUZ liste, iki yönlü kapı (envanter dışı nesne de kırmızı) ⟶ ex: scripts/test_db_invariants.ts:501-553 (TRIGGERS, timing dizisi PG kanonik sırasında olmak …
- [tsc|dev=1] Enum adı PascalCase, değerleri SCREAMING_SNAKE ⟶ ex: prisma/schema.prisma:StockCountStatus
- [insan|dev=?] Değer kümesi büyümeye açıksa pg enum yerine `String @db.VarChar(n)` — ALTER TYPE geri alınamaz ⟶ ex: prisma/schema.prisma:3264-3268 ("pg enum DEĞİL: ALTER TYPE + client churn istemiyoruz")
- [tsc|dev=0] İlişki adı `<Model><Rol>` PascalCase; aynı hedefe ikinci ilişkide ad ZORUNLU ⟶ ex: prisma/schema.prisma:3287-3291 (RollPlanDeviationRoll / RollPlanDeviationChild)
- [insan|dev=?] `onDelete` yalnız karar verildiğinde yazılır ve gerekçesi `///` ile satırın üstünde durur ⟶ ex: prisma/schema.prisma:4636-4642 (CASCADE ZORUNLU / Restrict gerekçeleri yazılı)
- [bekçi|dev=0] İdempotency gereken yaratma modelinde `clientToken String? @unique @db.Uuid` ⟶ ex: prisma/schema.prisma:5814 (ImportRun)
- [BELİRSİZ|dev=1] Json yalnız snapshot / config / audit yükü için; Prisma JSON filtresi kullanılmaz; listede `omit: { snapshot: true }` ⟶ ex: src/services/traveler-card.service.ts:751 omit: { snapshot: true }
- [insan|dev=95] Para/metraj `Decimal` + açık `@db.Decimal(p,s)`; JS float yok — `Prisma.Decimal` ya da DB-side increment/decrement ⟶ ex: prisma/schema.prisma:3276 qtyM Decimal @db.Decimal(12, 3)
- [insan|dev=1] Sıra dışı HER karar satırın üstünde `///` ile gerekçelenir (FK yok, index yok, enum yok, CASCADE, nullable seçimi) ⟶ ex: prisma/schema.prisma:4624-4634 (soft-clear + 'terslemenin adresi' gerekçesi)
- [bekçi|dev=0] Migration dizini `<14 hane>_<snake_slug>/migration.sql` ⟶ ex: prisma/migrations/20260904120442_cuval_izleri_etiket/
- [insan|dev=?] Migration SQL ELLE yazılır; `migrate dev` yerine `--create-only` + DropForeignKey satırlarını sil ⟶ ex: prisma/migrations/20260904120442_cuval_izleri_etiket/migration.sql:6-16
- [insan|dev=0] Büyük tabloya DDL yazan migration EN BAŞA `SET statement_timeout = 0;` koyar (DB'de 50s aktif) ⟶ ex: prisma/migrations/20260819060000_search_fold/migration.sql:41; DB ayarı Teks-Erp/docker-co…
- [bekçi|dev=0] Canlı veriye tekillik eklerken YUMUŞAK KAPI: DO $etiket$ + mükerrer say → varsa RAISE NOTICE + ATLA, yoksa CREATE UNIQUE INDEX IF NOT EXISTS (idempotent, yeniden koşulabilir) ⟶ ex: prisma/migrations/20260821150000_name_fold_unique_live/migration.sql:52-73
- [insan|dev=0] `CREATE INDEX CONCURRENTLY` migration'da YAZILMAZ (Prisma tx'i içinde çalışmaz) ⟶ ex: prisma/migrations/20260830090000_items_ad_kod_seddi/migration.sql:14-15
- [bekçi|dev=0] İfade index'inin fonksiyonu IMMUTABLE (+STRICT); karakter sınıfı AÇIK yazılır (`\s` yasak — locale bağımlı) ⟶ ex: prisma/migrations/20260825120000_color_name_unique_live/migration.sql:46-47
- [bekçi|dev=?] Elle migration uygulama sırası tek komut: git add → psql -v ON_ERROR_STOP=1 → migrate resolve --applied → bağımsız doğrulama ⟶ ex: scripts/apply-migration.ts:69-112
- [insan|dev=276] Prisma sorgusunda `select`, `include` DEĞİL ⟶ ex: Teks-Erp/CLAUDE.md:43 madde 7
- [eslint|dev=0] `tx.*` ile `Promise.all/allSettled` YASAK ⟶ ex: Teks-Erp/eslint.config.mjs:29
### gaps (8)
- [yüksek|küçük] 'Yeni Prisma modeli' reçetesi hiçbir belgede yok ⟶ docs/RECETELER.md başlıkları (satır 6,40,78,108,149,188,220,257,299): route+izin · feature flag · enum'a değer · migration · bekçi · Electron sayfası · mobil ek…
- [orta|küçük] 5 domain FK index'siz — kural 1'in künye muafiyetine girmiyor ⟶ schema.prisma:3702 SubcontractorDirectShipAllocation.dispatchId · :3747 DirectShipment.branchId · :4529 Sack.branchId · :5983 WarehouseMovement.shipmentId · :59…
- [orta|küçük] Sorgulanan JSON kolonunda GIN yok (kural 6) ⟶ src/services/inventory.service.ts:2509 `WHERE "newData" ->> 'event' LIKE 'TAMBUR_UNDO%'`; migration'larda jsonb GIN index sayısı 0 (11 GIN'in hepsi gin_trgm_ops…
- [orta|küçük] Teks-Erp/CLAUDE.md:43 madde 4 şema-dışı nesne envanterini EKSİK sayıyor ⟶ Belge 5 liste anıyor; scripts/test_db_invariants.ts'de 9 liste var — TRIGGERS:501, REQUIRED_EXTENSIONS:544, EXPECTED_FUNCTIONS:554, EXPECTED_COLLATIONS:576 belg…
- [düşük|orta] Kapı asimetrisi: 14 DB kuralının yalnız 4'ünde mekanik kapı var ⟶ Kapılı: 3+4 (test_db_invariants), 5 (query-parser.ts:385), 11 (eslint.config.mjs:24-29). Kapısız: 1 (FK index), 6 (JSON GIN), 7 (select/include, 276 sapma), 10 …
- [düşük|küçük] CI hâlâ emekli `adnansahin` dalına bağlı ⟶ .github/workflows/ci.yml:15-17 `branches: [main, adnansahin]`; kök CLAUDE.md 2026-09-02 satırı 'adnansahin ve feature/depo-mal-kabul emekli edildi'
- [düşük|küçük] İki adlandırma istisnası hiçbir kural dosyasında anılmıyor ⟶ schema.prisma:333-337 PermissionCategory { web mobile admin } — 68 enum'un tek küçük-harf değer kümesi; @@map'lerin 123'ü 's' ile biterken endpoint_latency_dail…
- [orta|küçük] 'Elle migration SQL'i idempotent yazılır' kuralı hiçbir .md'de yok ⟶ scripts/apply-migration.ts:80-85,102-107 defterde satır varsa resolve'u atlar ama SQL'i YİNE koşar; docs/RECETELER.md:108 blokunun kendi 'belgesiz kalan adımlar…
### docVsCode (18)
- [kod ihlal] Her @relation kolonuna @@index (düşük trafik 'kim yaptı' FK'ları hariç) ⟶ 368 FK alanının 272'si ön-ek index/unique/pk kapsamında. 96 kapsamsızın 91'i *ById/userId — kuralın muafı, tam tutuyor. 5 gerçek boşluk: schema.prisma:3702, :3747, :4529, :5983, :5984
- [uyumlu] Composite sıra: eşitlik önce, range/order sonra ⟶ 106 composite @@index; 0'ı tarih kolonuyla başlıyor, 68'i tarih kolonuyla bitiyor
- [uyumlu] Partial index ham SQL migration ile; şemada @@index bırak; partial unique için @@unique ⟶ schema.prisma:2035 düz `@@index([sackId])` dururken DB'de rolls_sackId_idx PARTIAL (test_db_invariants:110). 68 partial index envanterde (37 unique)
- [belge yanlış] Şema-dışı nesneler (partial index, CHECK, DEFERRABLE FK, statistics, EXPRESSION_UNIQUES) test_db_invariants envanterinde ⟶ Envanterde DOKUZ liste: PARTIAL 68 · CHECK 59 · DEFERRABLE 2 · EXT_STATS 1 · EXPRESSION_UNIQUES 4 · TRIGGERS 3 · REQUIRED_EXTENSIONS 2 · EXPECTED_FUNCTIONS 2 · EXPECTED_COLLATIONS 1. Belge dördünü hiç…
- [BELİRSİZ] Yüksek hacim tablolar cursor pagination; MAX_OFFSET=10000 ⟶ src/utils/query-parser.ts:23 `const MAX_OFFSET = 10000`, :385 skip>MAX → 400. 'Cursor ZORUNLU' tarafının bekçisi yok (docs/KOD-KURALLARI.md:10 zaten 'test yok' diyor)
- [kod ihlal] JSON alan sorgulanacaksa GIN önce ⟶ jsonb GIN index sayısı 0 (11 GIN'in hepsi gin_trgm_ops metin). Prisma JSON path filtresi 0; tek JSON sorgusu ham SQL: src/services/inventory.service.ts:2509 (system_logs.newData) ve index'siz
- [uyumlu] select, include değil ⟶ src/ içinde `select:` 2651 vuruş, `include:` 276 vuruş (%90,6 select). Mekanik kapı yok
- [uyumlu] Karmaşık aggregation $queryRaw ⟶ src/ içinde $queryRaw 104 vuruş
- [uyumlu] createMany ⟶ src/ içinde createMany 100 vuruş
- [BELİRSİZ] Tx kısa, dış I/O tx içinde yok (idle_in_transaction_session_timeout=5min) ⟶ Ayar doğrulandı: Teks-Erp/docker-compose.yml:28 idle_in_transaction_session_timeout=300000. 196 $transaction, 4'ünde açık timeout opsiyonu. 'Dış I/O yok' için mekanik kapı bulunamadı
- [uyumlu] tx.* + Promise.all YASAK (ESLint) ⟶ Teks-Erp/eslint.config.mjs:24-29 ORTAK_YASAKLAR — hem Promise.all hem allSettled, isim tabanlı guard; :88 ve :97'de no-restricted-syntax olarak bağlanıyor
- [BELİRSİZ] Yeni uç büyük tabloya değiyorsa EXPLAIN ANALYZE ⟶ Mekanik kapı yok; 'EXPLAIN ANALYZE' dizesi yalnız 4 dosyada (query-parser.ts, order.routes.ts, backup-impact.service.ts, scripts/scale_report.ts) ve hiçbiri kapı değil
- [uyumlu] Snapshot JSON'ları listede çekme ⟶ Tek emsal src/services/traveler-card.service.ts:751 `omit: { snapshot: true }`. Kalan 4 `snapshot: true` seçimi tekil okuma (workorder.service.ts:6835, traveler-card.service.ts:358/938/1024). Kapı yok
- [BELİRSİZ] Canlıda index migration vardiya dışında; büyük tabloda SET statement_timeout = 0 (DB statement_timeout=50s) ⟶ 42/232 migration'da `SET statement_timeout = 0`; DB ayarı Teks-Erp/docker-compose.yml:26 statement_timeout=50s. 'Vardiya dışında' bir süreç kuralı, koddan ölçülemez
- [uyumlu] (Ek) Her DateTime @db.Timestamptz; bekçi test_timestamptz_contract, TEK bekçi ⟶ 315 DateTime alanının 311'i Timestamptz; 4 muaf hepsi @db.Date (schema.prisma:5724, 6684, 6810, 7161). Bekçi scripts/test_timestamptz_contract.ts:146-194 iki yönlü (bayat muaf da kırmızı). Belgedeki '…
- [BELİRSİZ] (Ek) Decimal kolonda JS float yok — DB-side increment/decrement ya da Prisma.Decimal ⟶ 97 @db.Decimal kolon; Prisma.Decimal 662 vuruş, increment/decrement 34 vuruş. Karşı sinyal: `Number(<qty|amount|price|total|weight>…)` 95 vuruş — çoğu sunum katmanı olabilir, tek tek doğrulanmadı
- [uyumlu] (Ek) migrate dev her diff'te iki DEFERRABLE composite FK'yı DROP etmek ister → --create-only + DropForeignKey sil ⟶ Gerçek DEFERRABLE DDL satırı 2 (20260708120000_faz4_db_constraint_hardening/migration.sql:81,85); envanterde DEFERRABLE_FKS 2 kayıt (test_db_invariants.ts:444-447). En yeni migration başlığı elle sili…
- [BELİRSİZ] (Ek) Yeni izin kodu için migration YAZMA — katalog koda, atama panele ⟶ Doğrulanmadı (bu görevin kapsamı dışında; permission-catalog.job.ts okunmadı)
### sizeProposal
Şemada DOSYA tavanı anlamsız (schema.prisma 7749 satır, bilinçli tek dosya). Ölçülen fiili sınırlar: model gövdesi p50=34 / p90=93 satır, tek aşkın Roll=554 ve User=187. Migration p50=20 / p90=91 / p99=431, yalnız 6 dosya >200 satır. ÖNERİ — yeni model ≤120 satır (yorumlar dahil; şemanın %43,6'sı yorum olduğu için bu ≈60 alan satırı demek), aşarsa alt-tablo/pivot ayrımı sorulur. Yeni migration ≤200 satır; aşan 6 dosyanın hepsi çok-nesneli sertleştirme turu (20260708 faz4, 20260819 search_fold) → 'çok nesneli tur' bilinçli istisna ve gerekçesi dosya başlığında yazılır. Devralınan Roll/User sınır dışı.
### verdict
Şema ve migration disiplini bu repoda ÖLÇÜLEBİLİR şekilde tutarlı — standardı uydurmaya gerek yok, koddan birebir çıkıyor: id kalıbı 116/117, @@map 124/124, Timestamptz 311/315 (4 muaf @db.Date, iki yönlü bekçili), clientToken 15/15 birebir aynı yazım, migration adlandırması 232/232, composite sıra 106/106 doğru. Sağlıklı model şablonu = uuid PK + @db.Uuid + Timestamptz damgalar + VarChar sınırlı metin + katalogsa code/sortOrder/isActive + defterse yalnız createdAt + domain FK'ya index (künye FK'sına DEĞİL) + sıra dışı her karara /// gerekçe. Standarda MUTLAKA yazılması gereken üç şey: (a) künye FK'sının index ALMAMASI bir KURALDIR — 91 emsal; yazılmazsa yeni gelen 'eksik' sanıp ekler; (b) şema-dışı nesne → test_db_invariants envanteri İKİ YÖNLÜ kapıdır ve DOKUZ liste taşır (belge 5 sayıyor, düzeltilmeli); (c) 'yeni migration' reçetesi var ama 23 adım ve ağır (docs/RECETELER.md:108) — yeni kodda zorunlu çekirdek 13 adıma iner; 'yeni model' reçetesi HİÇ YOK, en büyük boşluk bu. Yeni kodda zorunlu / devralınan sınır dışı ayrımı: sınır dışı = Roll (554 satır), 5 index'siz domain FK, PermissionCategory küçük-harf enum, endpoint_latency_daily tekil tablo adı, system_logs JSON sorgusunun GIN'sizliği — bunlar 'yeni kodda tekrarlanmaz' notuyla kayda geçer, geriye dönük düzeltilmez (canlı DB kuralı). Kapı asimetrisi de standarda yazılmalı: 14 DB kuralının yalnız 4'ünde mekanik kapı var; kapısız kural bir NİYET beyanıdır, uyum ölçülmemiştir.

## eszamanlilik  keys=['conventions', 'gaps', 'docVsCode', 'verdict', 'sizeProposal']
### conventions (13)
- [bekçi|dev=6] Durum geçişi ATOMİK CLAIM ile yazılır: updateMany WHERE {id, beklenen-durum} + count===0 → AppError.conflict (409). findUnique→if→update yasak. ⟶ ex: Teks-Erp/src/services/printed-document.service.ts:748 — claim + count===0 → 409, ardından …
- [bekçi|dev=2] Henüz OLMAYAN satırlara karşı koruma (sayaç, dönem, kod, mükerrer tuzağı) pg_advisory_xact_lock(UZAY, hashtext(anahtar)) ile ve tx'in İLK ifadesi olarak alınır; 1-argümanlı form kullanılmaz. ⟶ ex: Teks-Erp/src/services/batch.service.ts:126 — generateBatchNumberTx'in İLK ifadesi
- [insan|dev=8] Kayıt-yaratan uç istemci clientToken'ı (@unique, partial index) alır; P2002'de retry EDİLMEZ, tx-dışı catch → findUnique → CACHED yanıt (şekil normal yanıtla birebir). ⟶ ex: Teks-Erp/src/services/cash-transaction.service.ts:320-330 — predicate + cached dönüş; util…
- [BELİRSİZ|dev=13] clientToken replay'inde DÖRDÜNCÜ durum ele alınır: kayıt yazıldı ama SONRADAN iptal edildiyse cached başarı DÖNMEZ, 409 (ENTRY_CANCELLED / ORDER_CANCELLED). ⟶ ex: Teks-Erp/src/services/helpers/token-replay.helper.ts:52
- [insan|dev=0] Sıra-numarası (@unique barkod/belge no) çakışması withBarcodeRetry ile çözülür: 5 deneme + 5-30ms jitter; iş-anahtarı P2002'leri predicate ile dışarı bırakılır. ⟶ ex: Teks-Erp/src/services/subcontractor.service.ts:2751-2762
- [insan|dev=3] Çok satırlı yazım DAİMA tek $transaction içinde; interaktif fn biçimi kullanılır, tx dışında çok-model yazma yapılmaz. ⟶ ex: Teks-Erp/src/services/traveler-card.service.ts:367 — 'kart satırı ve belge defteri AYNI tr…
- [insan|dev=0] İzolasyon READ COMMITTED (varsayılan); serileştirme RepeatableRead ile değil kilit + koşullu WHERE ile kurulur. ⟶ ex: Teks-Erp/src/services/shipping.service.ts:3620-3644
- [bekçi|dev=0] Sayısal tavan/bakiye kontrolünde SELECT … FOR UPDATE derinlik savunmasıdır; asıl serileştirici advisory kilit ya da koşullu UPDATE'tir. ⟶ ex: Teks-Erp/src/services/helpers/cash-balance-guard.helper.ts:30-81
- [bekçi|dev=0] Durum↔sayaç çifti olan modelde İKİ yazar da karşı tarafın koşulunu kendi atomik WHERE'ine koyar + DB CHECK ikinci hat; count===0 tanısı ayrı bir explain*BumpZeroTx ile verilir. ⟶ ex: Teks-Erp/src/services/payment-allocation.service.ts:284-298
- [bekçi|dev=0] Şema-dışı DB seddi (partial UNIQUE / CHECK) envantere yazılır ve envanter İKİ YÖNLÜ doğrulanır (kayıp DA fazla DA kırmızı). ⟶ ex: Teks-Erp/scripts/test_db_invariants.ts:56-80 (checkNoExtras gerekçesi)
- [insan|dev=0] Cari deftere yazan HER yol, satır yazmadan önce dönem guard'ından geçer (advisory 8026/8028 + kapalı dönem 409). ⟶ ex: Teks-Erp/src/services/helpers/period-guard.helper.ts:88-97
- [insan|dev=0] PG sınıf-40 (deadlock 40P01 / serialization 40001) ve P2034 kullanıcıya 500 değil 409 'tekrar deneyin' olarak döner; tespit açık 'Code: `40P01`' kalıbından yapılır, genel regex yasak. ⟶ ex: Teks-Erp/src/middlewares/error.middleware.ts:322-342
- [insan|dev=0] İş emri kapsamlı sayımlar tx başında touchWorkOrderTx ile WO satırı write-kilitlenerek serileştirilir. ⟶ ex: Teks-Erp/src/services/subcontractor.service.ts:1057
### gaps (8)
- [orta|küçük] Refakat kartı sürüm numarası check-then-act ile artıyor — eşzamanlı iki baskı aynı sürümü yazar, biri sessizce kaybolur ⟶ traveler-card.service.ts:284 `version: existing.version + 1` + `update where:{id}` (version guard'ı YOK); recordPrintEvent kartı tx DIŞINDA okur (:350) ve plan.…
- [orta|küçük] İki advisory kilit uzayında ÇİFT SAHİP var; envanter yorumu bunu bilmiyor ve tam küme eşsizliğini ölçen bekçi yok ⟶ 8026: period-guard.helper.ts:73 PERIOD_CLOSE_LOCK_NS=8026 (hashtext(cariId|currency)) ↔ code-unique.helper.ts:57 CODE_UNIQUE_LOCK_NS=8026 (hashtext(scope|kod)).…
- [orta|orta] Tablet üzerinden belge yaratan dört uçta clientToken yok — zaman aşımı sonrası retry cached yanıt yerine 409 alır ⟶ schema.prisma: SubcontractorDispatch / KartelaDispatch / KartelaReceipt / StockCount modellerinde clientToken alanı YOK (16 modelde var). Mobil ölçüm: 60 .post …
- [düşük|küçük] SackTag adı check-then-act ile tekilleştiriliyor, DB seddi yok ⟶ sack-tag.service.ts:132 `findFirst({where:{name}}) → conflict → create`, :164 aynısı update'te. schema.prisma model SackTag: yalnız `code String @unique`; name/…
- [düşük|orta] Yönetim/master-data yüzeylerinde 36 tx-dışı findUnique→if→update sitesi (devralınan kod) ⟶ Ölçüm: prisma.X.find→durum dalı→prisma.X.update deseni, tx yok = 36 site. 15'i tek tek okundu: device.service.ts:322/345/401/416/429/476, auth.service.ts:191/25…
- [düşük|küçük] Boot job'unda tx dışı çok-model yazma (repo genelindeki TEK örnek) ⟶ jobs/role-template-catalog.job.ts:57 reconcileRoleTemplates() → permissionTemplate + permissionTemplateItem, $transaction yok. Tarama: services+controllers+rout…
- [düşük|küçük] Fason talimatı güncellemesi tx dışı check-then-act ⟶ subcontractor.service.ts:4753 findUnique (cancelledAt + kabul var mı) → :4784 prisma.subcontractorDispatch.update, arada tx yok. Eşzamanlı kabul araya girerse '…
- [orta|orta] 'Ölü replay' yüklemi (token'lı kayıt sonradan iptal edildi) yalnız 3 uçta; token'lı 16 modelin 13'ünde tek kaynak yok ⟶ helpers/token-replay.helper.ts iki yüklem tanımlıyor (assertRollReplayAlive, assertOrderReplayAlive); import edenler: inventory.service.ts:19, order.service.ts:…
### docVsCode (10)
- [belge yanlış] Advisory kilit uzay envanteri helpers/period-guard.helper.ts başlığındadır; her uzayın tek sahibi vardır ('aynı uzayı paylaşan iki alt siste… ⟶ Envanter 7 uzay sayıyor (8021,8022,8024-8028) ve 8023'ü numaralı listeye almamış. 8026'nın İKİ sahibi var (period-guard.helper.ts:73 + code-unique.helper.ts:57), 8027'nin İKİ sahibi var (purchase-orde…
- [belge yanlış] 'İdempotency (2026-07-14): kayıt-yaratan uçlar clientToken taşır (Roll, Order, WorkOrder)' ⟶ 16 model taşıyor: Roll, Order, WorkOrder, SubcontractorReceipt, Swatch, SwatchStockReduction, Sack, Shipment, ImportRun, WarehouseTransfer, GoodsReceipt, Invoice, Payment, CashTransaction, Cheque, Pur…
- [uyumlu] 'Durum geçişini findUnique→if→update ile yazma; atomik claim kullan (updateMany WHERE {id, beklenen-durum}, count===0 → 409)' ⟶ Kural fiilen uygulanıyor: 80 claim sitesi, 74'ü count kontrollü, 130 count kontrolü 409'a bağlı. Kontrolsüz 6 site bilinçli idempotent no-op. Sapmalar (36 tx-dışı check-then-act) yalnız yönetim/master…
- [uyumlu] 'Çift yüklem kuralı: iki yazar da karşı tarafın koşulunu kendi atomik WHERE'ine koyar + DB CHECK seddi' ⟶ payment-allocation.service.ts:248-318'de her iki yönde de koşullu UPDATE + DB CHECK cheques_terminal_not_allocated (migration 20260814110200:96) mevcut; error.middleware.ts:291 constraint adını mesaja…
- [belge yanlış] 'Kilit sırası kuralı: uzaylar arası = uzay numarası artan; bugün çapraz-uzay çifti YOK (ölçüldü)' ⟶ Çapraz-uzay çifti ARTIK VAR: inventory.service.ts:988 (8021, KK1 tuzağı) ve :1060 (lockAgainstMergeTx → 8027 shared) aynı createInitialEntry akışında. Sıra 8021→8027 = ARTAN, yani kural İHLAL EDİLMEMİ…
- [belge yanlış] 'İzolasyon: tüm tx'ler READ COMMITTED (repoda isolationLevel kullanılmıyor)' ⟶ 196 $transaction'ın 195'i seviyesiz; tek istisna shipping.service.ts:3643 RepeatableRead ve o SALT OKUMA (üç groupBy). Beyanın 'kullanılmıyor' kısmı artık kelimesi kelimesine doğru değil ama ruhu koru…
- [uyumlu] 'clientToken P2002 RETRY EDİLMEZ; predicate ile propagate → dış catch CACHED yanıt; cached yanıtın şekli normal yanıtla BİREBİR' ⟶ 18 isClientTokenP2002 kullanımı, 7 serviste predicate + tx-dışı catch deseni birebir uygulanmış (workorder, inventory, invoice, order, kartela, cash-transaction, payment, cheque, purchase-order). Sapm…
- [kod ihlal] 'Yeni bir clientToken replay yolu yazan herkes token-replay.helper'dan geçmeli' (dördüncü durum: yazıldı sonra iptal) ⟶ Yalnız 3 dosya import ediyor (inventory, order, tambur-manual). Token'lı diğer 13 modelde eşdeğer yüklem tek kaynakta değil; shipping.service.ts:1663 helper'a yalnız yorumda atıf yapıyor.
- [uyumlu] 'Cari deftere yazan HER yol guard'ı çağırır; guard'ı ATLAYAN bir yazar kilidin tamamını sessizce delik yapar' (5+1 çağıran listesi) ⟶ cariTransaction.create yalnız 4 dosyada (cari ×2, cheque ×1, invoice ×2, payment ×2) ve dördü de guard çağırıyor (cari ×6, cheque ×4, invoice ×3, payment ×2). Liste bugün TAM.
- [uyumlu] 'Şema-dışı DB nesneleri envanterde ve envanter iki yönlü doğrulanır' ⟶ test_db_invariants.ts'te 68 partial index (37 unique) + 60 CHECK + 7 fonksiyon + 2 collation envanterli; checkNoExtras() beş bölümde koşuyor ve envanter-dışı nesne KIRMIZI. Migration'larda ölçülen 14 …
### verdict
FİİLİ KALIP GÜÇLÜ, BOŞLUKLAR DAR VE ADRESLENEBİLİR. Eşzamanlılık disiplini bu repoda gerçekten uygulanıyor: 80 atomik claim sitesinin 74'ü count kontrollü ve kontrollerin %92'si (130/141) 409'a bağlanıyor; 16 advisory kilit sitesi tx'in ilk ifadesi olma kuralına uyuyor; 196 $transaction'ın tamamı READ COMMITTED'da kilit+koşullu WHERE ile serileşiyor (tek isolationLevel salt-okuma); tx dışında çok-model yazan TEK fonksiyon var (~4600 fonksiyon içinde); 60 CHECK + 68 partial index iki yönlü bekçili. Yani standart "uydurulmayacak", ölçülen kalıp yazıya geçirilecek. ASIL BOŞLUKLAR ÜÇ TANE ve üçü de mekanizmanın yokluğu değil, KAPSAMIN eksikliği: ① traveler-card sürüm artırımı, aynı repoda printed-document'ın doğru yaptığı işi claim'siz yapıyor (orta risk, küçük düzeltme); ② advisory uzay envanteri koddan geride — 8026 ve 8027'nin ikişer sahibi var ve tam küme eşsizliğini ölçen bekçi yok (bugün zarar yok, yarın deadlock); ③ token-replay'in "sonradan iptal edildi" yüklemi 16 token'lı modelin 3'ünde. Standart yazımı için ayrım net: YENİ KODDA ZORUNLU = claim + count + 409, kayıt-yaratan uçta clientToken + cached yanıt, advisory uzay envantere yazılır ve bekçiye eklenir, çok satırlı yazım tx içinde. DEVRALINAN/SINIR DIŞI = 36 tx-dışı findUnique→if→update sitesi (hepsi yönetim/master-data, defter değil), 6 bilinçli count-kontrolsüz idempotent no-op, kur/deploy job'ları. Standardın en kritik cümlesi ölçümden doğuyor: "count kontrolü yoksa bu bir claim değil, dürtmedir — hangisi olduğun…
### sizeProposal
Ölçüm (boyut-dagilimi.json): backend-services dosya p50=236, p90=1001, max=7143 (subcontractor.service.ts), 269 dosyanın 103'ü >300 satır; fonksiyon p50=14, p90=59, p99=160, 269 fonksiyon >80 satır, 28'i >200. ÖNERİ — yeni kodda: fonksiyon ≤80 satır (p90'ın hemen üstü, bugünkü kütlenin %94'ü zaten uyuyor), dosya ≤400 satır (p50'nin ~1.7 katı; >300 olanların çoğu devralınan mega-servis). ⚠️ Eşiği tek başına dayatma: bu repoda uzunluğun büyük kısmı GEREKÇE YORUMU (period-guard.helper.ts'in ilk 55 satırı saf karar kaydı) — sınırı "kod satırı" olarak say, yorumu sayma, yoksa standart en değerli kalıbı (kararın kod yanında yaşaması) cezalandırır. Devralınan 5 mega-servis (subcontractor 7143, workorder 6847, inventory 5421, system-setting 5022, shipping 4861) sınır dışı; oralara EKLEME yaparken kural "yeni yüzeyi helper'a çıkar" olsun — nitekim fiili kalıp da bu (services/helpers/ altında period-guard, cash-period-guard, code-unique, shipment-locks, token-replay, quality-station, workorder-locks…).

## kutuphane  keys=['conventions', 'gaps', 'docVsCode', 'sizeProposal', 'verdict']
### conventions (9)
- [bekçi|dev=3] Her ihtiyaç için TEK kütüphane; ikinci paket eklenmez, mevcut olan DEĞİŞTİRİLİR. Electron ikon=lucide-react, toast=sonner, xlsx=exceljs, http=axios; mobil toast=react-native-toast-message, ikon=@expo/vector-icons, liste=… ⟶ ex: Electron/src/lib/xlsx-export.ts:39 (exceljs tek xlsx yolu, dinamik import)
- [bekçi|dev=1] ESM-only/native ABI riski taşıyan paket TAM SABİT sürümle yazılır (^ / ~ yok) + gerekçe yorumda + bekçi. ⟶ ex: Electron/src/test/discovery-ipc-contract.test.ts:132
- [BELİRSİZ|dev=0] Backend CommonJS'tir; her dependency require ile çözülebilmeli. ESM-only paket backend'e girmez, major yükseltme sözleşme değişikliğidir. ⟶ ex: Teks-Erp/src/jobs/mdns-advertiser.job.ts:91 (lazy require kalıbı)
- [bekçi|dev=0] Çalışma zamanında gereken paket dependencies'te durur — devDependencies'e düşerse kurulu üründe MODULE_NOT_FOUND. Aynı kural pakete girmesi gereken CLI için de geçerli. ⟶ ex: deploy/paketle.ps1:245 (PRISMA_CLI_BINARY_TARGETS=windows) + kur.ps1:48
- [insan|dev=0] Native / config-plugin paket mobilde APK gerektirir, OTA yetmez; Electron'da native modül createRequire ile TEMBEL + try/catch yüklenir ve yüklenemezse yol fail-open kapanır. ⟶ ex: Electron/electron/ipc/printer.ipc.ts:13-42
- [insan|dev=0] Büyük paket (>200KB) açılışa yüklenmez — dinamik import ile ayrı chunk'a alınır. ⟶ ex: Electron/src/lib/xlsx-export.ts:5
- [insan|dev=0] Elle yazmak meşru bir alternatiftir: küçük ihtiyaç için paket eklenmez. ⟶ ex: docs/kurallar/filtre-liste.md:84
- [BELİRSİZ|dev=0] Lisans permissive olmalı (MIT/ISC/BSD/Apache-2.0/OFL); copyleft girmez. ⟶ ex: Electron/node_modules/@fontsource/plus-jakarta-sans/package.json (OFL-1.1)
- [insan|dev=0] HTTP çağrısı: oturum/interceptor taşıyan istekler axios ile, kimliksiz sonda/manifest okuması fetch ile. ⟶ ex: mobil/src/offline/serverReachability.ts (fetch sonda) ↔ mobil/src/services/api.ts (axios)
### gaps (12)
- [orta|küçük] Yeni bağımlılık eklemenin yazılı reçetesi YOK — tek genel kural yerine bir özel yasak var. ⟶ grep 'kütüphane|paket ekle|bağımlılık|npm install' → docs/KOD-KURALLARI.md + docs/kurallar/*.md içinde kütüphane politikası olarak YALNIZ filtre-liste.md:84 (mo…
- [orta|orta] Ölü bağımlılık bekçisi yok; 13 doğrulanmış ölü paket birikmiş. ⟶ package.json denetleyen TEK test Electron/src/test/discovery-ipc-contract.test.ts (yalnız bonjour-service). Ölüler: @react-pdf/renderer, electron-window-state, …
- [orta|küçük] mobil'de ÇİFT Bluetooth kütüphanesi; ölü olan APK'ya native kod + izin taşıyor. ⟶ react-native-ble-plx yalnız src/services/bluetooth.service.ts'de (27 satır); o dosyayı import eden yok (grep 'bluetooth.service|BleManager|bluetoothService' → 3…
- [düşük|küçük] mobil'de ÇİFT ikon paketi — react-native-vector-icons hiçbir yerde ve Paper'ın peer'ı DEĞİL. ⟶ grep src+app.json+babel.config → 0 eşleşme. react-native-paper@5 peerDependencies ölçüldü: {react, react-native, react-native-safe-area-context} — vector-icons …
- [düşük|küçük] react-native-qrcode-svg + react-native-svg birlikte ölü; svg'yi isteyen tek paket qrcode-svg. ⟶ qrcode-svg: yalnız package.json:57. node_modules tam taraması — react-native-svg'yi peer/dep isteyen TEK paket react-native-qrcode-svg (>=14.0.0). react-native-…
- [düşük|küçük] Backend'de bonjour-service pin bekçisi yok (Electron'da var). ⟶ Teks-Erp/package.json:36 '1.4.4' sabit; scripts/test_discovery_advertiser.ts package.json'ı hiç okumuyor (grep '1.4.4|package.json' → yalnız yorum satırı:7). El…
- [orta|küçük] Backend CJS invariantını koruyan mekanizma yok — ESM-only bir major yükseltme sessizce girebilir. ⟶ Teks-Erp package.json'da `type` yok = commonjs; 18/19 dependency require.resolve ile çözülüyor (bu tarama elle yapıldı, hiçbir bekçi koşmuyor). ESLint kuralları…
- [düşük|orta] Araç zinciri drift: üç TypeScript ana sürümü, iki ESLint ana sürümü. ⟶ typescript: Teks-Erp ^6.0.2 / Electron ~5.6.2 / mobil ~5.9.2. eslint: ^10.2.1 / ^9.39.0 / ^9.0.0 (envanterden, package.json'larla doğrulandı). Zod hizalı (her i…
- [düşük|küçük] Electron'da PDF paketi kurulu ama PDF yolu yok — 'ihtiyaç kayboldu, paket kaldı' sınıfı. ⟶ @react-pdf/renderer ^4.5.1 yalnız package.json:57 + package-lock'ta. Fiili baskı yolu HTML + sandbox'lı iframe; dışa aktarım exceljs. Büyük paket, kurulum boyut…
- [düşük|küçük] mobil'de iki id/HTTP-benzeri ikili karar yazılı değil: axios↔fetch ayrımı ve backend'de uuid↔crypto.randomUUID ikiliği. ⟶ Backend: uuid ^13 yalnız 2 src dosyasında (subcontractor.service.ts:25 v4, order.service.ts:35 validate) — aynı repoda crypto.randomUUID 20 yerde. `validate` iç…
- [orta|küçük] 'importFiles: 0' sinyali tek başına yanıltıcı — 13 canlı paket sıfır görünüyor. ⟶ Doğrulanan yanlış pozitifler: exceljs (dinamik import, xlsx-export.ts:39), serialport/node-hid (createRequire, printer.ipc.ts:42 / scanner.ipc.ts:70), bonjour-s…
- [düşük|küçük] react-native-web + react-dom: uygulamada sıfır kullanım ama `npm run web` script'i onlara dayanıyor — karar gerekli, tek başına silinmez. ⟶ mobil app.json'da `platforms` ve `web` alanı YOK (ölçüldü); package.json:10 "web": "expo start --web" var; react-native-web ^0.21.0 ve react-dom 19.1.0 src'de 0…
### docVsCode (6)
- [uyumlu] Mobil tarih seçici için YENİ PAKET EKLENMEZ — takvim ızgarası elle yazıldı, sıfır bağımlılık (expo-audio peer'ı bir kez expo-asset'i köke çe… ⟶ mobil/package.json'da hiçbir tarih seçici paketi yok (doğrulandı). expo-asset bugün app.json plugins[4]'te açıkça kayıtlı — o kazanın kalıcı izi. Electron'da react-day-picker var ama kural yalnız mobi…
- [uyumlu] bonjour-service dependencies içinde olmalı (devDependencies'te DEĞİL) ve sürüm SABİT — ^/~ yok, çünkü sonraki ana sürüm ESM-only olabilir. ⟶ İki projede de '1.4.4' sabit yazılı (Teks-Erp/package.json:36, Electron/package.json). Bekçi yalnız Electron'da koşuyor; backend'de aynı invariant korumasız.
- [uyumlu] Paket deploy/ ve scripts/'i ALMAZ; `npm ci --omit=dev` tsx'i eler — paketin taşımadığı yola bağımlı kod yazılmaz. ⟶ Bu kuralın bağımlılık tarafındaki karşılığı `prisma` CLI'ın dependencies'te tutulması: kur.ps1 [7/9] `prisma migrate deploy` koşar, devDep olsaydı --omit=dev elerdi. require.resolve('prisma') MODULE_N…
- [uyumlu] Prisma'nın şema motoru NATIVE'dir; paketleme dört kapıyla Windows motorunu zorlar (PRISMA_CLI_BINARY_TARGETS + varlık + MZ imzası + kur.ps1 … ⟶ deploy/paketle.ps1:245-276'da dört kapının hepsi mevcut ve yabancı platform motorunu paketten atıyor (satır 273-275). Kural kod tarafında karşılığını buluyor.
- [belge yanlış] Mekanik olarak zorlananlar listesi (ESLint): tx.*+Promise.all, notIn:[], orderBy:{batchNumber}, requireModule("x"), 'Europe/Istanbul', route… ⟶ Liste tamamen domain kuralları; kütüphane/bağımlılık sınıfından TEK kural içermiyor. Yani bağımlılık politikasının bugün hiçbir mekanik ayağı yok — tek istisna bir vitest testi (bonjour-service).
- [kod ihlal] Soft delete / bilinçli istisna kayıt kültürü: her istisna gerekçesiyle yazılır ('Z KULLANILMAZ çünkü…'). ⟶ Domain tarafında güçlü uygulanıyor (docs/kurallar/top-duzeltme.md:13,109). Kütüphane tarafında UYGULANMIYOR: 13 ölü paketin hiçbirinin kaldırma/tutma kararı hiçbir yerde yazılı değil; @react-pdf/rende…
### sizeProposal
Kütüphane politikası tek dosyaya sığar: docs/kurallar/kutuphane.md ~60-80 satır. İçerik: (1) katman×ihtiyaç tablosu — üç proje, satır başına "X için Y; Z KULLANILMAZ çünkü…" (ölçülen 30 kadar satır); (2) 10 adımlı ekleme reçetesi; (3) çıkarma reçetesi (üç yanlış-pozitif kanalı: dinamik import / createRequire / config referansı); (4) sabit sürüm gerekçe listesi (bugün 8 paket). Bu ayrı bir "standart" bölümü değil, mevcut docs/kurallar/*.md kalıbının 28. dosyası olmalı — aynı [ÇEKİRDEK]/[PROFİL] etiketi ve · bekçi: satırıyla. Politikanın kendisi ÇEKİRDEK (üç projede aynı), tablo satırları PROFİL değil ama ürün kararı: yeni kurulum aynı tabloyu miras alır.
### verdict
FİİLİ KALIP SAĞLAM, KAYIT YOK. Üç projede de "her ihtiyaç için tek kütüphane" disiplini gerçekten tutuyor (Electron ikon lucide-react 490 dosya / toast sonner 219 / http axios 14+fetch 0; mobil toast 64, paper 132) ve native/lazy yükleme kalıbı dört Electron IPC dosyasında birebir aynı. Lisans temiz: üç projenin tüm top-level bağımlılıkları tarandı, copyleft SIFIR. Ama bu disiplinin YAZILI karşılığı yok: kütüphane politikası olarak docs'ta tek bir özel yasak var (mobil tarih seçici, filtre-liste.md:84) ve package.json'ı denetleyen TEK bekçi bonjour-service pinini koruyor (Electron'da; backend'de aynı pin korumasız).  Bedeli ölçüldü: 13 doğrulanmış ölü paket (@react-pdf/renderer, electron-window-state, 3 Radix primitifi, @fontsource/roboto, @faker-js/faker, react-native-{ble-plx, vector-icons, qrcode-svg, svg}, expo-sharing, @react-navigation/bottom-tabs) ve bunların ikisi gerçek ÇİFT: mobilde ölü BLE kütüphanesi app.json plugins[1]'de duruyor, yani APK'ya native kod ve izin taşımaya devam ediyor; ölü ikon paketi Paper'ın peer'ı bile değil (peerDependencies ölçüldü).  Standart için iki ayrım: (a) "importFiles: 0" tek başına kanıt değil — 13 canlı paket sıfır görünüyor (dinamik import, createRequire, app.json plugin, jest preset, CSS import, peer, CLI); (b) devralınan kod sınır dışı ≠ ölü paket sınır dışı: eski kodun kütüphane seçimi tartışılmaz ama HİÇBİR kodun kullanmadığı paket kaldırılabilir, çünkü orada devralınacak bir davranış yok.  Politika yasak listesi değil kayıtlı k…

## electron-sayfa  keys=['conventions', 'gaps', 'docVsCode', 'sizeProposal', 'verdict']
### conventions (15)
- [insan|dev=8] Sayfa iskeleti: <PageShell> + <PageHeader> + <PageBody> (ya da CrudPage / ReportPageLayout sarmalayıcısı) ⟶ ex: Electron/src/pages/System/Clients/ClientsPage.tsx:45-60
- [insan|dev=0] Her içerik route'u <ProtectedRoute requirePermission=...> ile sarılır ⟶ ex: Electron/src/routes/content-routes.tsx:241-247
- [insan|dev=1] Tüm HTTP tek geçitten: `services/apiClient.ts` (ham fetch/axios yok) ⟶ ex: Electron/src/services/crudService.ts:24-44
- [bekçi|dev=0] Servis yolu TAM yazılır (`/api/...`); baseURL /api içermez ⟶ ex: Electron/src/services/api-path-prefix.test.ts:23-27
- [insan|dev=0] query-key ilk elemanı kebab-case string literal ⟶ ex: Electron/src/pages/Operations/Shipments/ShipmentsPage.tsx:37
- [insan|dev=0] Master-data servisi `createCrudService<T>(basePath)` ⟶ ex: Electron/src/pages/DefectTypes/service.ts:1-4
- [eslint|dev=1] `any` yasak ⟶ ex: Electron/src/components/forms/EntityFormDialog.tsx:42-43
- [insan|dev=1] Named export (default export yok) ⟶ ex: Electron/src/pages/System/Clients/ClientsPage.tsx:40
- [insan|dev=17] Mutation `onError`'da ikinci toast YOK — apiClient interceptor 4xx/5xx'i zaten basar; istisna isteğin `suppressErrorToast: true` taşımasıdır ⟶ ex: Electron/src/pages/Operations/Rolls/RollQtyAdjustDialog.tsx:77
- [insan|dev=1] Panel zod `max(n)` = Prisma `@db.VarChar(n)` ⟶ ex: Electron/src/pages/Warehouses/schema.ts:4-6
- [eslint|dev=0] Renderer'da `electron`/`fs`/`path`/`child_process`/`os` import edilmez ⟶ ex: Electron/eslint.config.mjs:32-43
- [insan|dev=0] Sonsuz kaydırma otomatik; 'Daha Fazla Yükle' butonu yok ⟶ ex: Electron/src/components/data-table/AutoLoadMore.tsx:22
- [insan|dev=0] Sidebar'a satır eklenmez — keşif hub kartı / komut paleti üzerinden ⟶ ex: Electron/src/components/layout/nav-config.ts
- [insan|dev=14] Basit master-data formu `EntityFormDialog` sarmalayıcısı ⟶ ex: Electron/src/pages/DefectTypes/DefectTypeFormDialog.tsx
- [insan|dev=3] Paylaşılan hook `hooks/`e, sayfaya özel hook sayfa dizinine ⟶ ex: Electron/src/hooks/useCrudMutations.ts:11
### gaps (8)
- [orta|küçük] StationCapabilities ekranında yazma butonu izin kapısız — route salt-okuma izniyle açılıyor ⟶ content-routes.tsx:486 `requirePermission="station:read"`; StationCapabilitiesPage.tsx:280-287 'Yetkinlikleri Düzenle' butonu PermissionGate'siz; CapabilitiesEd…
- [düşük|küçük] Çift hata toast'ı: 17 mutation `onError`'da elle toast basıyor, isteği `suppressErrorToast` taşımıyor ⟶ grep `onError: (e|err…) => toast.error` → 17 satır / 10 dosya (SackTagsPage.tsx:73,98,110 · SackTagDialog.tsx:68 · ReasonPresetsPage.tsx:76,82 · DuplicatesPage.…
- [orta|küçük] Panel zod ↔ Prisma uzunluk sapması (Customers.code) ⟶ Electron/src/pages/Customers/schema.ts:21 `max(50)` ↔ Teks-Erp/prisma/schema.prisma Customer `code String @unique @db.VarChar(32)`. Backend'de bu uçta Zod yok (…
- [orta|orta] Dosya boyutu kuralının bekçisi YOK — en yeni sayfalar da ihlal ediyor ⟶ Electron/CLAUDE.md 'tek dosya ≤300, Page ≤200 ZORUNLU' diyor; `grep -rl 'maxLines|300 satır'` bir bekçi bulmuyor. 2026-09-03/04'te eklenen sayfalar: ModuleProfi…
- [düşük|büyük] FilterBar.tsx tek gerçek monolit bileşen ⟶ components/data-table/FilterBar.tsx 1049 satır, 19 hook çağrısı, yorum oranı %11. Diğer en büyük 4 dosya katalog/route tablosu (0 hook, %19-31 yorum). >300 satı…
- [düşük|küçük] 8 rapor hub sayfası ortak iskeleti kullanmıyor ⟶ pages/Reports/_components/ReportHubGrid.tsx:21-22 `<div className="flex h-full flex-col">` + `<div className="p-6">` — PageShell/PageBody yok. Kardeş hub'lar (S…
- [düşük|küçük] Kullanılmayan Radix bağımlılıkları ⟶ bagimlilik-envanteri.json Electron: @radix-ui/react-avatar ve @radix-ui/react-scroll-area importFiles=0; `grep -rn 'react-avatar|react-scroll-area' src/` → 0 so…
- [düşük|küçük] 3 `service.ts` React hook'u export ediyor (katman karışması) ⟶ pages/System/Clients/service.ts, System/DbRestore/service.ts, System/Backups/service.ts `export function use…`. Kalan 49 service.ts saf veri erişimi.
### docVsCode (10)
- [uyumlu] Electron/CLAUDE.md: 'Yeni master data sayfası 5 dosya: types/service/schema/columns/FormDialog + Page' ⟶ 131 Page.tsx'in 14'ü tam TSZCF setine sahip (Colors, Customers, DefectTypes, FabricProperties, Items, Machines, Orders, PeripheralDevices, ProductRecipes, ReturnReasons, Routes, SubcontractorCategorie…
- [uyumlu] Electron/CLAUDE.md: 'devralınan 134/1385 ve 55/131 ihlal bu sınırın dışında, bekçi yok' ⟶ Ölçüldü: >300 satır 134 dosya / toplam 1385 ts-tsx (src+electron+shared); Page.tsx >200 satır 55 / 131. Bekçi araması sonuçsuz. Rakamlar birebir doğru.
- [uyumlu] Electron/CLAUDE.md: 'servis yolları TAM (/api/...)' ⟶ Mekanik bekçi var ve yeşil: `vitest run src/services/api-path-prefix.test.ts` → 3 passed; 0 ihlal.
- [kod ihlal] Electron/CLAUDE.md: '5xx generic → mutation onError'da ikinci toast YOK' + skill: 'onError toast EKLEME' ⟶ 17 koşulsuz `onError → toast.error` var (10 dosya), hiçbiri suppressErrorToast taşımıyor. En yeni sayfa (SackTags, 2026-09-04) 4 tanesini içeriyor.
- [kod ihlal] Electron/CLAUDE.md: 'tek dosya ≤300, Page ≤200 satır ZORUNLU (yeni ve dokunduğun dosyada)' ⟶ 2026-09-03/04'te eklenen sayfalar: ModuleProfilePage 442, SettingsSurfacePage 304, SackTagsPage 257. Kural metni 2026-09-05'te yazıldı (dosya başlığı: 'yeniden yazım 2026-09-05'), yani sapmalar kurald…
- [uyumlu] Electron/CLAUDE.md: 'Sidebar'a satır EKLENMEZ' ⟶ components/layout/nav-config.ts 8 `to:` girdisi ↔ 131 sayfa. Kural tutuyor.
- [uyumlu] Skill electron-admin-page: 'any yasak' ⟶ eslint no-explicit-any error ve koşum 0 error veriyor. 9 `as any` var; 8'i eslint-disable + gerekçe ile (zodResolver tip sürtünmesi), 1'i (OrderFormDialog.tsx:297) gerekçesiz ama disable de taşımıyor …
- [kod ihlal] Electron/CLAUDE.md: 'schema.ts (zod, Partial<T> backend'le uyumlu)' ⟶ Backend master-data CRUD'da Zod YOK — BaseController.create (Teks-Erp/src/controllers/base.controller.ts:95-102) req.body'yi servise doğrudan geçirir. 'Uyum' fiilen Prisma VarChar uzunluğuna hizadır v…
- [uyumlu] Electron/CLAUDE.md: 'Sonsuz kaydırma otomatik; Daha Fazla Yükle YOK' ⟶ Metin yalnız DataTable.tsx:154'teki yorumda geçiyor; buton yok.
- [uyumlu] Electron/CLAUDE.md: 'renderer electron/fs/path/child_process/os import etmez' ⟶ eslint no-restricted-imports error olarak kurulu, koşumda 0 ihlal.
### sizeProposal
YENİ KODDA ZORUNLU: tek dosya ≤300 satır · `*Page.tsx` ≤200 · `*FormDialog.tsx` ≤200 (aşarsa alan bölümlerine ayır) · bir fonksiyon/bileşen gövdesi ≤80 satır (fiili p90=98, p50=17). DEVRALINAN SINIR DIŞI, ama DONMUŞ BASELINE ile: bugün >300 olan 124 test-dışı dosya (+10 test) ve >200 olan 55 Page.tsx bir allowlist'e yazılır; kural "listede olmayan dosya sınırı aşamaz + listedeki dosya BÜYÜYEMEZ" (satır sayısı baseline'a çivilenir). Bekçi zaten kanıtlanmış kalıpla yazılır: `src/services/api-path-prefix.test.ts` gibi dosya taramalı bir vitest (muaf listesi bayatlığı da ölçülsün — ölü muaf kırmızı). MUAFİYET GEREKÇESİ ALANLA: yalnız "katalog/registry" dosyaları (0 hook çağrısı, yorum oranı >%15) — en büyük 4 dosya (settings-config 1445, WorkOrders/service 1266, content-routes 1218, documentConfig 1216) bu sınıfta ve bölmek zarar verir. Gerçek borç ölçüsü: >300 olan 124 dosyanın ≥10 hook çağrısı taşıyan 28'i; FilterBar.tsx (1049/19 hook) baştan hedef.
### verdict
Electron paneli ölçülen üç projenin en disiplinli katmanı: `tsc --noEmit` rc=0, eslint 0 error / 37 warn, `: any` 0 (9 `as any`'nin 8'i gerekçeli disable), 0 ham `fetch`, 235 HTTP çağrısının hepsi apiClient'tan, `/api` öneki mekanik bekçili ve yeşil, 569 query-key'in 532'si kebab literal (0 camelCase), 130/131 named export, 106 route bloğunun 100'ü ProtectedRoute. Fiilî iyi kalıp GERÇEK ve tarif edilebilir: `pages/<Modul>/` altında types+service(createCrudService)+schema(zod, Prisma VarChar aynası)+columns+FormDialog+Page, sayfa yalnız `CrudPage`/`PageShell+PageHeader+PageBody`e props verir, saf yardımcılar ayrı dosyada + testli (referans: `pages/System/Clients/`, `pages/DefectTypes/`). "5 dosya" kuralı yalnız 14 master-data sayfasında tam — ama bu sapma değil, kuralın kapsamı zaten master-data. Standart yazarken üç şeye zemin var: (1) çoğu kural fiilen tutuyor, yazıya geçirmek yeterli; (2) üç kural bekçisiz ve tam da o üçü sapıyor — dosya boyutu (yeni sayfalarda 257/304/442), mutation onError çift toast (17 nokta, en yenisi 2026-09-04), zod↔Prisma uzunluk (1 sapma, backend'de Zod olmadığı için TEK kapı); (3) tek gerçek güvenlik açığı StationCapabilities'in izin kapısız yazma butonu. Kural yazılıp ölçülmezse tutmuyor — repo bunu `api-path-prefix.test.ts` yorumunda kendisi söylüyor ve o kural %100 tutuyor.

## mobil-ekran  keys=['conventions', 'docVsCode', 'gaps', 'sizeProposal', 'verdict']
### conventions (16)
- [insan|dev=10] Ekran = ince kabuk + View bileşenleri + ekran-hook + saf mantık modülü ⟶ ex: mobil/src/screens/Modules/HizliIsEmri/ (15 dosya, 4167 satır)
- [insan|dev=1] Katmanlama: ekran → <domain>.service.ts → apiClient (ekran HTTP kurmaz) ⟶ ex: mobil/src/services/tambur.service.ts (581 satır)
- [insan|dev=5] Test edilebilir mantık ekrandan çıkıp <ad>.ts/<ad>.helper.ts kardeş dosyaya alınır, yanında .test.ts durur ⟶ ex: mobil/src/screens/Modules/FasonKabul/receivePayload.helper.ts (351) + .test.ts (468)
- [insan|dev=11] Çevrimdışı kuyruğa girecek yazma mutationKey: STATION_MUT.* ile tanımlanır (mutationFn offline/mutations.ts'te setMutationDefaults ile bağlı) ⟶ ex: mobil/src/screens/Modules/KursunQc/KursunQcScreen.tsx (5 mutasyonun 5'i de kuyruklu)
- [insan|dev=11] Okutma geri bildirimi scanFeedback.signalScan ya da useScanFeedback üzerinden; ekran okutma yolunda Haptics'i doğrudan çağırmaz ⟶ ex: mobil/src/services/scanFeedback.ts (accept|duplicate|reject + scanSoundEnabled)
- [insan|dev=9] Tıklanabilir her alan react-native-paper TouchableRipple/Button/IconButton/List.Item; Card + onPress YASAK ⟶ ex: mobil/src/components/PickerModal.tsx
- [insan|dev=3] Modallar components/AppModal.tsx üzerinden (Portal + Reanimated) ⟶ ex: mobil/src/components/AppModal.tsx
- [insan|dev=0] Liste = FlashList + useInfiniteQuery (onEndReached); pager YOK ⟶ ex: mobil/src/components/RemoteListSheet.tsx
- [insan|dev=0] Oturum kapısı tek registry'den dayatılır ve navigator'da withWorkSession ile sarılır ⟶ ex: mobil/src/constants/stationScreens.ts
- [insan|dev=0] HAL: çevre birimi oturumdan çözülür (useMachinePeripherals), cihaz-yerel seçim yok ⟶ ex: mobil/src/hooks/useMachinePeripherals.ts (74 satır)
- [tsc|dev=0] Tip disiplini: strict TS, kaçış kapısı yok ⟶ ex: mobil/src/types/models.ts (1372 satır)
- [insan|dev=88] Renk/spacing theme/tokens.ts'ten alınır; ham hex yazılmaz ⟶ ex: mobil/src/screens/Modules/Tambur/TamburSendToDyeModal.tsx (350 satır, 0 ham hex)
- [insan|dev=2] Adlandırma: hooks use*, store <ad>Store.ts, servis <domain>.service.ts, bileşen PascalCase.tsx ⟶ ex: mobil/src/hooks/useMachinePeripherals.ts
- [insan|dev=3] Sebep seçimi tek bileşen ReasonPresetPicker (serbest metin ÜSTTE) ⟶ ex: mobil/src/components/reasonPresets/ReasonPresetPicker.tsx (139 satır)
- [insan|dev=38] Ortak bileşen önce aranır (components/ paylaşımlı kabuk) ⟶ ex: mobil/src/components/ScreenChrome.tsx
- [insan|dev=9] Import biçimi: göreli yol (tsconfig alias'ları fiilen ölü) ⟶ ex: mobil/src/screens/Modules/Tambur/TamburSendToDyeModal.tsx:26-29
### docVsCode (12)
- [belge yanlış] mobil/CLAUDE.md UI/UX: 'font ≥16sp, kritik 20sp+' ⟶ 1163 fontSize bildiriminin 970'i (%83) 16'nın ALTINDA; en yoğun değerler 12 (268) ve 13 (268); 9px bile var (8 adet).
- [BELİRSİZ] mobil/CLAUDE.md: 'Dokunma hedefi ≥56dp' ⟶ minHeight bildirimlerinin 41/88'i 56 altında; ama minHeight her zaman dokunma hedefi değil (bant/etiket kutuları da sayılıyor) ve Paper bileşenleri kendi hit alanını taşıyor.
- [uyumlu] mobil/CLAUDE.md: 'Card + onPress KULLANMA' ⟶ <Card kullanımı 0; TouchableRipple 233.
- [kod ihlal] mobil/CLAUDE.md: 'ekranlar Haptics'i doğrudan çağırmaz' (okutma geri bildirimi signalScan'den) ⟶ expo-haptics 44 dosyada, 230 çağrı; 40'ı ekran/bileşen. Okutma yolunda doğrudan çağrı 3 ekranda gövde okunarak kanıtlandı: Depo 296-319, KursunQc 446/477, Paketleme 361-384.
- [kod ihlal] mobil/CLAUDE.md: 'ReasonPresetPicker tek bileşen (üç kopyadan tek bileşene alındı)' ⟶ Bileşen 1 yerde kullanılıyor; 2 ekran kendi seçici UI'sini çiziyor, 1 modal hook'u ayrı tüketiyor.
- [belge yanlış] mobil/CLAUDE.md Paketler listesi (bottom-tabs, expo-sharing, react-native-qrcode-svg dahil) ⟶ Bu üç pakete src/ + App.tsx + jest.config.js içinde SIFIR kod referansı var (yalnız package.json). react-native-vector-icons 'peer artığı' notu doğru; react-native-screens/worklets/web/react-dom RN/Ex…
- [uyumlu] mobil/CLAUDE.md: 'modalda NumpadInput + useNativeKeyboard (modalda NumpadHost yok)' ⟶ NumpadHost 3 JSX kullanım (KursunQc:1624, Tambur:4299, KK1:2290) — üçü de ekran gövdesinde; SackManualWeightSheet:13 ve TamburManualRollModal:277 kuralı yorumla teyit ediyor.
- [uyumlu] mobil/CLAUDE.md: 'NumpadHost autoActivate yalnız tek alanda' ⟶ 6 autoActivate kullanımı; KK1'de iki tane ama karşılıklı dışlayan koşullarla (1737 !manualMode / 1998 manualMode), gerekçe satır içi yazılı (KK1:1993).
- [uyumlu] mobil/CLAUDE.md: 'FlashList onEndReached; Önceki/Sonraki pager YOK' ⟶ <FlatList 0, <FlashList 37 kullanım / 23 dosya.
- [belge yanlış] mobil/CLAUDE.md klasör yapısı: 'test/ (guard testleri)' ⟶ src/test/ yalnız 4 dosya. 84 test dosyasının 80'i kaynağın YANINDA (offline 12, components 13, services 10, hooks 9, utils 8, screens 11, store 5…). Fiili kalıp 'yan yana test'; belge onu söylemiyor.
- [uyumlu] mobil/CLAUDE.md: 'SegmentedButtons flexDirection:row kabının doğrudan çocuğu OLAMAZ (bekçi var)' ⟶ Bekçi gerçek ve AST tabanlı: TS AST ile her <SegmentedButtons> için en yakın sarmalayıcının style'ını çözüp flexDirection:'row' arıyor.
- [kod ihlal] mobil/CLAUDE.md: 'Modallar components/AppModal.tsx' ⟶ 54 dosya AppModal kullanıyor; 3 dosya ham RN <Modal (LabelNamePreview, AppMenu, labels/LabelPreviewSheet).
### gaps (9)
- [yüksek|küçük] Dosya/fonksiyon boyutu için HİÇBİR mekanik kapı yok ⟶ mobil/eslint.config.js (24 satır) yalnız react/no-unescaped-entities kapatıyor; max-lines / max-lines-per-function yok. Sonuç: TamburScreen.tsx 9386 satır, tek …
- [yüksek|orta] signalScan kapısının bekçisi yok; okutma geri bildirimi ekran ekran ayrışıyor ⟶ Kural mobil/CLAUDE.md + scanFeedback.ts:75-85 yorumunda. 3 tüketici vs 11 doğrudan-Haptics dosya. 'duplicate' sonucu ve scanSoundEnabled ayarı o ekranlarda hiç …
- [yüksek|küçük] Kuyruksuz (online-only) istasyon yazması bir KARAR olmalı ama 13'ün 11'i gerekçesiz ⟶ Gerekçe taşıyanlar: TamburScreen.tsx:1706, FasonKabulScreen.tsx:650. Taşımayanlar arasında finalizeMutation(1198), cutOpenFabricMutation(1245), cutWarehouseRoll…
- [orta|büyük] Ekran bileşeni testi yok denecek kadar az ⟶ 84 test dosyasının 2'si ekran/modal render ediyor (EntryConflictModal.test.tsx, UpdateActions.test.tsx). 5 dev ekran (23.862 satır) için 0 bileşen testi; 820 te…
- [orta|orta] Ham hex için bekçi yok; kural yalnız tokens.ts yorumunda yaşıyor ⟶ theme/tokens.ts:7 'Yeni ekran/komponent yazarken ham hex YAZMA' diyor; kodda 1931 ham hex / 88 dosya. Ayrıca ikinci bir yerel palet var: screens/Common/settings…
- [orta|orta] queryKey merkezi kayıt YOK — 238 satır içi anahtar ⟶ grep 'queryKey:' mobil/src → 238 (test hariç); QUERY_KEYS/queryKeys sabiti yalnız offline/persistPolicy.ts'te. invalidateQueries ile liste anahtarının ayrışması…
- [düşük|orta] tsconfig alias'ları fiilen ölü ⟶ mobil/tsconfig.json 9 path alias tanımlı, kodda 2 kullanım; buna karşılık 587 üç-seviye göreli import (../../../). Dosya taşındığında kırılma yüzeyi; yeni kodda…
- [düşük|küçük] Kullanılmayan üç paket, üstelik belgede 'kullanımda' gibi duruyor ⟶ @react-navigation/bottom-tabs, expo-sharing, react-native-qrcode-svg: mobil/src + App.tsx + jest.config.js içinde SIFIR referans; üçü de mobil/CLAUDE.md Paketle…
- [düşük|orta] react-hooks/exhaustive-deps 26 yerde bastırılmış ⟶ grep 'eslint-disable' mobil/src → 45; 26'sı exhaustive-deps (RootNavigator:183, KartelaSevk:88, KartelaKabul:255/790, TartiPaket:179/203, KursunQc:1053 …), 19'u…
### sizeProposal
YENİ KOD SINIRI (ölçülen p90'lara dayalı; screens dışı her katman zaten ≤450): ekran kabuğu ≤250 · görünüm/sheet ≤500 · ekran-hook ≤400 · tek React fonksiyonu ≤300 satır ve ≤12 useState · saf mantık dosyası ≤200 + .test.ts ZORUNLU · servis ≤400 · yeni ekranda ham hex 0. Kapı: eslint max-lines(400)/max-lines-per-function(300), 5 dev ekran muafiyet listesinde (liste kısaldıkça kısalır). DEVRALINAN sınır dışı; dokunulan bölüm çıkarılırken sınıra çekilir. TamburScreen bölme planı (6 adım, satır aralıklarıyla) results/mobil-ekran.json → sizeProposal.tambur_bolme_plani.
### verdict
Mobilde tek bir "fiili iyi kalıp" VAR ve yenidir: ekran = ince kabuk + görünüm bileşenleri + ekran-hook + saf mantık modülü (+ yanında test). Emsal HizliIsEmri (15 dosya/4167 satır, kabuk 140) ve Siparis (kabuk 134). Bu kalıp 2026-06 sonrası açılan her ekranda tutuyor. Sorunun tamamı 2026-05-11 doğumlu 5 dev ekranda (Tambur 9386 / FasonKabul 4679 / KK1 4204 / KursunQc 3192 / FasonSevk 2401 = 23.862 satır); bunlar 21-55 commit'le birikerek büyüdü ve boyutu ölçen hiçbir kapı olmadığı için hiç durdurulmadı. Katman sözleşmesi (ekran→servis→apiClient), oturum kapısı (tek registry + withWorkSession), HAL (useMachinePeripherals 3 tüketici), Paper disiplini (Card+onPress 0, FlatList 0, TouchableRipple 233) ve tip disiplini (tsc temiz, 0 @ts-ignore, 12 any'nin hepsi React tip erozyonu) SAPMASIZ ya da tek-haneli sapmalı. Zayıf üç eksen ölçülebilir: ① okutma geri bildiriminde signalScan yalnız 3 tüketicide, 11 dosya doğrudan Haptics çağırıyor (kanıt: DepoScreen:296-319, KursunQcScreen:446/477, PaketlemeScreen:361-384) — o ekranlarda "mükerrer" sinyali ve ses ayarı hiç çalışmıyor; ② 13 kuyruksuz istasyon mutasyonunun 11'inde "online-only" gerekçesi yok; ③ ham hex 1931/88 dosya, %50'si dev ekranlarda ama yeni ekranlarda 0. Standart bu yüzden "yeni kodda zorunlu / devralınan sınır dışı" ayrımına doğal olarak oturur: kurallar zaten yeni kodda tutuyor, eksik olan tek şey mekanik kapı.

## test-derleme  keys=['conventions', 'gaps', 'docVsCode', 'sizeProposal', 'verdict']
### conventions (10)
- [insan|dev=3] Her bekçi tek dosya, kendi fixture'ını business-key ile yaratıp finally'de temizler; süreç `process.exit(fail>0?1:0)` ile hüküm verir, özet satırı basar ("Sonuç: N geçti, M başarısız"). Framework YOK (jest/vitest yasak, … ⟶ ex: Teks-Erp/scripts/test_station_quality_capability.ts
- [insan|dev=?] Yeni bekçi NEGATİF SONDAYLA kırmızı verdiği doğrulanarak yazılır; commit mesajında kaç kontrol/kaç sonda olduğu yazılır. ⟶ ex: 
- [bekçi|dev=1] Test paketi mock KULLANMAZ; gerçek Postgres'e yazar. Bu yüzden koşucu FAIL-CLOSED üretim-DB kapısı taşır: DATABASE_URL host'u localhost/127.0.0.1/::1/0.0.0.0 değilse durur, NODE_ENV/APP_ENV=production hiç geçmez. ⟶ ex: 
- [tsc|dev=2] Tip kontrolü test paketinin GEÇİDİDİR: `npm test` önce tsconfig.scripts.json (scripts+prisma+src) derler (~28 sn), kırmızıysa paketi HİÇ koşmaz. ⟶ ex: 
- [BELİRSİZ|dev=3] CI üç projeyi ayrı job'da doğrular: lint → tsc → test. Backend job'ı Postgres service + `migrate deploy` + `seed` + `seed:fixtures` (ikisi AYRI ve zorunlu adım) kurar. ⟶ ex: 
- [insan|dev=2] Bekçiler seri koşar: aynı dev DB'sini paylaştıkları için paralel koşum fixture çakışması üretir. ⟶ ex: 
- [bekçi|dev=2] Yıkıcı/ortam bozucu komutlar mekanik kapıda: `pkill -f tsx`, `migrate reset/dev/db push`, `TRUNCATE/DROP`, WHERE'siz DELETE, `git push --force`, ham `npm run build:win`, `gradlew assembleRelease`. ⟶ ex: 
- [insan|dev=5] Kod değişikliği ve bekçisi AYNI commit'te gelir. ⟶ ex: 
- [bekçi|dev=1] Migration/test hijyeni ayrı script'lerde: check-docs (ölü link + CLAUDE.md boyut tavanı → exit 1), check-migrations (GATE 1-5: untracked/modified migration, eksik migration.sql, untracked test_*.ts, package.json'ın andığ… ⟶ ex: 
- [eslint|dev=1] Panel/tablet testleri DB'siz ve hızlı: Electron vitest (jsdom, paralel), mobil jest-expo (--runInBand). ⟶ ex: 
### gaps (12)
- [yüksek|küçük] CI'ın Electron tip kontrolü NO-OP: `npx tsc --noEmit` kök tsconfig'i (files:[] + references, -b yok) kullanıyor ve hiçbir dosyayı derlemiyor. ⟶ Electron/tsconfig.json = {"files":[],"references":[node,web]}; ölçüm: `npx tsc --noEmit --pretty false | grep -c 'error TS'` → 0, `npm run typecheck` → 9 hata. …
- [yüksek|küçük] main'de 9 tip hatası duruyor (1 gündür) — Electron/src/pages/Operations/SackContentEdit/service.ts:69,80 `SackCustomerPage` import edilmemiş (tip types.ts:71'de VAR); SackTags/SackTagsPage.tsx:253; SackEditorView.ship.te… ⟶ git log -1 -- .../service.ts → 20d2f967 (2026-09-04). Backend src tsc 0 hata, mobil tsc 0 hata — yalnız Electron kırmızı.
- [yüksek|küçük] main'de Electron vitest kırmızı: 2 dosya / 7 test. 22bbe617 (2026-09-04) tabs store'a zustand persist ekledi, jsdom'da localStorage kapalı olduğu için mevcut iki bekçi patlıyor. ⟶ npm test → 'Test Files 2 failed | 206 passed'; src/store/tabs.back.test.ts (4), src/components/layout/PageHeader.test.tsx (3). İkisi de Electron/CLAUDE.md:32'de…
- [yüksek|küçük] run-all-tests.ts'te MIGRATION DURUMU KAPISI yok: dev DB 2 migration geride olunca 453 bekçinin 134'ü (%30) kırmızı ve teşhis okuyucuya 'TableDoesNotExist' stack'i olarak bırakılıyor. ⟶ npx prisma migrate status → 20260903020000_station_applies_quality + 20260904120442_cuval_izleri_etiket uygulanmamış. npm test → 319/453, hataların içeriği 'The…
- [yüksek|orta] Yerelde hiçbir git hook yok; tsc kapısı yalnız Claude'un Bash aracındaki PreToolUse hook'unda ve 1 günlük. ⟶ .git/hooks'ta örnek dışı dosya 0; husky/lefthook yok; bash-guard.mjs ilk commit 773f0d07 (2026-09-05). Hatalı Electron commit'i 20d2f967 bir gün ÖNCE atıldı.
- [yüksek|küçük] CLAUDE.md:116'daki 'tam paket npm test SAATLER sürer' cümlesi yanlış ve tam paketin koşulmamasının beyan edilmiş gerekçesi. ⟶ Ölçüldü: 453 dosya, 388 sn kullanıcı zamanı, 6:28 duvar saati (typecheck geçidi dahil). 2026-08-09 kaydı da 280 dosya/239 sn diyor (docs/history/SAHA-NOTLARI-20…
- [orta|küçük] e2e ve load-test job'ları `continue-on-error: true` — sinyal üretiyor, kapı kurmuyor. Stryker (mutation) hiç koşmuyor. ⟶ ci.yml:150 ve :249; ci.yml'de 'stryker|mutation' grep → 0 eşleşme; Electron/stryker.conf.json son dokunuş f46c254e (2026-06-14).
- [düşük|küçük] Lint uyarıları kapı değil: Electron 37 uyarıyla yeşil geçiyor. ⟶ cd Electron && npm run lint → '37 problems (0 errors, 37 warnings)'; backend lint temiz. CI adımı çıkış kodundan hüküm veriyor.
- [orta|orta] 5 HTTP ayaklı bekçi sunucu yoksa bölümlerini SESSİZCE atlıyor — bekçi yeşil kalır, kapsam düşer. ⟶ test_finance_flag_off / test_module_flag_off / test_module_profile / test_settings_password / test_superadmin; portlar 4100/4101/4122/4112/4104 (docs/GELISTIRME…
- [orta|büyük] 242 kural satırının 13'ü bekçisiz beyan ediliyor ('bekçi: yok' / 'test yok'). ⟶ grep -c '^- \*\*' docs/KOD-KURALLARI.md → 242; 'bekçi: `yok' + 'test yok' → 13.
- [orta|orta] Bekçi paketinin paralelleştirilmesi için şema izolasyonu YETMEZ: pg_advisory_xact_lock veritabanı kapsamlıdır, şema kapsamlı değil. ⟶ src'te sabit anahtarlı kilitler: batch.service.ts:126 (parti no), permission-management.service.ts:699, master-data-merge.service.ts:563 (8027). 12 bekçi doğrud…
- [düşük|küçük] Şema adını sabitleyen 5 bekçi paralelleştirmede parametrelenmeli. ⟶ table_schema='public' → test_audit_depth, test_master_data_name_dup, test_record_provenance, test_timestamptz_contract (6 geçiş); nspname='public' → test_fold_c…
### docVsCode (8)
- [belge yanlış] CLAUDE.md:116 — 'tam paket `npm test` saatler sürer, sıralı' ⟶ Ölçüldü: 453 dosya, 6:28 duvar saati (388 sn), typecheck geçidi (28 sn) dahil. Sıralı olduğu doğru, 'saatler' yanlış.
- [kod ihlal] ci.yml Electron job'ı 'Type-check' adımı — tip kontrolü yapılıyor beyanı ⟶ `npx tsc --noEmit` kök tsconfig'i files:[]+references ile kullanıyor; -b olmadan sıfır dosya derleniyor. Proje kendi `typecheck` script'inde iki config'i ayrı koşuyor ve 9 hata veriyor.
- [kod ihlal] Electron/CLAUDE.md:32 — geri-ok davranışının bekçileri 'tabs/history-depth.test.ts, PageHeader.test.tsx, store/tabs.back.test.ts, HubCard.te… ⟶ PageHeader.test.tsx ve tabs.back.test.ts HEAD'de KIRMIZI (7 test). Belge doğru, kod yeşil değil.
- [belge yanlış] kök CLAUDE.md 'Test Kullanıcıları' — 'Admin dışı tüm kullanıcılar test123' ⟶ CLAUDE.md'nin kendi içinde 2026-09-03 düzeltmesi var: seed yalnız admin üretir. İki cümle yan yana duruyor.
- [uyumlu] docs/GELISTIRME-DONGUSU.md:20 — 'Tam paket: npm test = 453 test_*.ts dosyası, SIRALI' ⟶ Doğrulandı: ls scripts/test_*.ts | wc -l → 453; koşucu readdirSync + /^test_.*\.ts$/ ile seri spawnSync.
- [uyumlu] docs/GELISTIRME-DONGUSU.md:21 — 'npm test iki geçitten geçer: productionDbGate + typecheckGate ~28sn' ⟶ Doğrulandı; koşum çıktısı '→ Hedef DB: tekserp_demo @ localhost (yerel) ✅' ile başlıyor. Üçüncü bir geçit (migrate status) YOK ve bugünkü 134 kırmızının sebebi tam da o.
- [uyumlu] scripts/check-migrations.mjs başlığı — 'Bu bekçinin değeri YERELDEDİR' ⟶ Doğru ve sonucu ciddi: CI'da temiz checkout yüzünden GATE 1/2/4 yapısal olarak hep yeşil; yerelde onu otomatik koşan hiçbir mekanizma yok (git hook yok, bash-guard commit'te yalnız tsc koşuyor).
- [kod ihlal] Electron/CLAUDE.md:13 — komut listesi 'test:mutation (stryker) · e2e (playwright)' ⟶ İkisi de var ama kapı değil: stryker CI'da hiç yok, e2e continue-on-error. Stryker config'ine son dokunuş 2026-06-14.
### sizeProposal
Backend bekçi dosyaları p50=217, p90=524, max=1914 satır (153 dosya >300). Öneri: yeni bekçi ≤400 satır, >600 satırda bölümlere değil DOSYALARA bölünür (test_payment_allocation.ts 1914 satır zaten §-numaralı bölümlerle yönetiliyor). Fonksiyon p50=14, p90=69; yeni yardımcı ≤80 satır. Devralınan 153 dosya sınır dışı — yalnız dokunulan bölüm kurala çekilir.
### verdict
FİİLİ İYİ KALIP GERÇEK VE GÜÇLÜ: framework'süz tek-dosya bekçi (453 backend + 208 Electron + 84 mobil = 3.400+ test), business-key fixture, fail-closed üretim-DB kapısı, tip kontrolü geçidi, negatif sonda disiplini, altyapı-flake'i için tek yeniden deneme, 6 job'lık CI. Kod+bekçi birlikteliği ölçülü: src'ye dokunan 18 commit'in 14'ü (%78) aynı commit'te test de değiştiriyor. KIRIK OLAN ŞEY KAPILAR, KÜLTÜR DEĞİL — üçü ölçüldü ve üçü de BUGÜN main'de zarar veriyor: ① CI'ın Electron tip adımı no-op (bare `npx tsc --noEmit` + files:[] tsconfig) → 9 tip hatası 1 gündür main'de; ② Electron vitest main'de kırmızı (7 test) çünkü 22bbe617 yeni bekçi yazıp mevcut ikisini koşmadı, oysa maliyet 33 saniye; ③ run-all-tests migration durumunu ölçmüyor → dev DB 2 migration geride olduğu için 453'ün 134'ü kırmızı ve bu 6,5 dakika sonra anlaşılıyor. Dördüncüsü kültürel ve düzeltmesi tek satır: CLAUDE.md'nin 'npm test saatler sürer' cümlesi YANLIŞ (6:28) ve tam paketi koşmamanın gerekçesi olarak kullanılıyor. STANDART İÇİN ZEMİN: 'yeni kodda zorunlu' = commit anında (a) migrate status, (b) değişen projede `npm run typecheck` (Electron'da bare tsc DEĞİL) + lint, (c) Electron/mobil src değiştiyse o projenin tam paketi (33/29 sn) — toplam 13-45 sn, git pre-commit hook'una taşınmalı; backend tam paketi PR/sürüm kadansında. 'Devralınan sınır dışı' = 153 adet >300 satırlık bekçi, 13 bekçisiz kural, 37 lint uyarısı ve 5 sessizce atlayan HTTP bekçisi — bunlar dokunulan yerde düzeltilir, toplu kampanya …

## eslint-olcum  keys=['conventions', 'gaps', 'verdict', 'sizeProposal', 'docVsCode']
### conventions (9)
- [eslint|dev=0] Backend'de her Promise ya await edilir, ya `void` ile ya `.catch()` ile kapatılır — sahipsiz promise yok. ⟶ ex: Teks-Erp/src/jobs/*.job.ts
- [eslint|dev=14] Tanımlayıcılar ASCII ve camelCase/PascalCase/UPPER_CASE; Türkçe karakter yalnız string/yorumda. ⟶ ex: Teks-Erp/src/services/order.service.ts:150 (`__test__` — bilinçli test kapısı)
- [eslint|dev=0] İş mantığı servis katmanında; route/controller Prisma client'a inmez. ⟶ ex: Teks-Erp/eslint.config.mjs
- [eslint|dev=0] Fabrika saat dilimi, boş `notIn`, parti sıralaması ve modül kapısı tek kaynaktan; literal kopyalanmaz. ⟶ ex: Teks-Erp/src/constants/time.ts (tek kaynak, kendi muafiyet bloğu var)
- [eslint|dev=7] Modül döngüsü yok — import grafiği neredeyse ağaç. ⟶ ex: mobil/src (0 döngü)
- [eslint|dev=0] Renderer native API'ye `window.api` dışından erişmez; token localStorage'a yazılmaz. ⟶ ex: Electron/src/lib/secure-store.ts
- [eslint|dev=12] `any` kullanılmaz; kaçınılmazsa satır bazlı disable + gerekçe. ⟶ ex: Electron/eslint.config.mjs (zaten error)
- [eslint|dev=47] Prisma enum'unu Zod'a `z.enum(PrismaEnum)` ile bağla; string dizisi literali yazma. ⟶ ex: Teks-Erp/src/config/label-kind.schema.ts (`z.enum(LabelKind)`)
- [eslint|dev=148] Log yalnız yapılandırılmış kanaldan — panel/tablet'te console yok. ⟶ ex: Electron/src (3 kullanım)
### gaps (8)
- [yüksek|küçük] Backend lint kapsamı yalnız `src` — scripts/ (222+ test dosyası) ve prisma/ hiç lint edilmiyor. ⟶ Teks-Erp/package.json:22 `"lint": "eslint src"`; eslint.config.mjs files: ["src/**/*.ts"]. Aynı boşluk tsc'de yaşandı (ci.yml yorumu: 87 tip hatası, 2026-08-01)…
- [yüksek|orta] Hiçbir projede TİP BİLGİLİ kural yok; ölçülünce mobil'de 291 sahipsiz promise çıkıyor. ⟶ 3 config'te de parserOptions.project/projectService yok. Ölçtüm: no-floating-promises backend 0, Electron 35, mobil 291 (screens 275).
- [yüksek|orta] Uyarılar hiçbir yerde tavanlanmıyor — CI 'Lint' adımı yalnız exit koduna bakıyor, warning'ler sessizce birikiyor. ⟶ .github/workflows/ci.yml backend/electron/mobile job'larında `npm run lint`. Bugün: mobil 136 warning, Electron 37 warning, ikisi de yeşil.
- [orta|küçük] eslint-disable yorumları sayılmıyor; reportUnusedDisableDirectives her üç projede kapalı/varsayılan. ⟶ Bastırılmış bulgu: Electron 65 (exhaustive-deps 54, no-explicit-any 8), mobil 42 (exhaustive-deps 23, no-require-imports 19), backend 0. Teks-Erp/eslint.config.…
- [orta|büyük] Dosya/fonksiyon boyutunda hiçbir kapı yok; kuyruk aşırı uzun. ⟶ backend p95=1175 satır, 30 dosya >1000, max 7142 (subcontractor.service.ts). mobil max 9386 (TamburScreen.tsx). Fonksiyon >80: backend 391, Electron 505, mobil …
- [düşük|küçük] mobil `expo lint` ile çıplak `eslint .` farklı kapsam görüyor — scripts/ CI'da lint edilmiyor. ⟶ `expo lint` → 0 error/136 warning; `./node_modules/.bin/eslint .` → mobil/scripts/lib/feed.cjs:36 `no-undef '__dirname'` (1 error).
- [orta|orta] Backend'de yapılandırılmış logger yok — 137 console çağrısı fiilen tek log kanalı. ⟶ grep `from ['\"].*logger` Teks-Erp/src = 0; console.log 54 / warn 44 / error 44, 38 dosya.
- [düşük|küçük] 'Yeni kural yalnız sıfır-ihlalliyse girer' ölçütü doğru ama sıfır-ihlalli 5 aday ölçülüp yazılmamış: kind==="PROCESS_QC", !=="mobile", .body.code, applied_steps_count, çıplak DATE_TRUNC. ⟶ Her biri için Teks-Erp/src'de grep = 0 gerçek isabet (yalnız yasağı anlatan yorumlar). Beşi de bedava.
### verdict
Üç config üç ayrı olgunlukta: backend bilinçli minimal ve BUGÜN tertemiz (5 syntax yasağı + 1 katman yasağı, 0 hata/0 bastırılmış); Electron orta (no-explicit-any error, 0 error/37 warning/65 bastırılmış); mobil yalnız expo preset (0 error/136 warning, proje kuralı yok). Ölçüm sonucu üç kova: (1) BEDAVA, hemen error — no-floating-promises SADECE backend'de (0/456; Electron 35, mobil 291), naming-convention (7/3/4, hepsi `__xForTests`), Türkçe tanımlayıcı (0), no-explicit-any (backend/Electron 0, mobil 12), döngüsel import (1/2/0), 5 yeni syntax yasağı (kind==="PROCESS_QC", !=="mobile", .body.code, applied_steps_count, çıplak DATE_TRUNC), Electron+mobil'de no-console (3/8), Card+onPress (0), localStorage-token (0). (2) BASELINE İSTER — max-lines 300 (145/134/70), max-lines-per-function 80 (391/505/188), z.enum literal aynası 47, `now() AT TIME ZONE 'UTC'` 11, mobil floating promise 291. (3) YASAK DAR OKUNMUŞ, KURAL YAZMA — toLocaleUpperCase("tr") 56 (config'te zaten reddedilmiş, doğrulandım), Haptics doğrudan 230 çağrı/44 dosya (kural sadece okutma geri bildirimi için), inline pageSize 22 (çoğu `pageSize:1` sayaç), backend no-console 137 (logger yok), findUnique→update 5 aday ve incelediğim 2'si yanlış pozitif → mekanik kural yazılamaz, `new Date()` rapor kapsamı kodda tanımsız (BELİRSİZ). Baseline mekanizması: yeni paket YOK — kural `warn`, `scripts/check-lint-baseline.mjs` (check-docs.mjs deseni) `eslint -f json` çıktısını kural bazında sayıp `lint-baseline.json` tavanına ba…
### sizeProposal
Ölçülen fiili dağılım — dosya satır p50/p90/p95/max: backend 206/748/1175/7142 (456 dosya), Electron 112/298/380/1445 (1359), mobil 118/427/581/9386 (386). Öneri: YENİ dosya ≤ 300 satır (Electron p90'ı zaten 298 — panel bu sınırda yaşıyor), YENİ fonksiyon ≤ 80 satır (backend servis fonksiyon p90=59). Devralınan sınır dışı: backend 145, Electron 134, mobil 70 dosya baseline'da donar, tavan yalnız düşer. Sert tavan (yeni dosyada asla aşılamaz): 600 satır — bugün backend 68, Electron 15, mobil 20 dosya bunu aşıyor, hepsi devralınan.
### docVsCode (8)
- [belge yanlış] kök CLAUDE.md Yasaklar: `toLocaleUpperCase("tr")` yasak ⟶ Teks-Erp/src'de 56 kullanım; Teks-Erp/eslint.config.mjs başlığı bunu 2026-09-05'te ölçüp (62 demiş) kuralı REDDETMİŞ, çünkü yasak yalnız koşullu etiket elemanı ↔ QualityGrade.code için dar.
- [belge yanlış] mobil/CLAUDE.md:33 — ekranlar `Haptics`i doğrudan çağırmaz, `scanFeedback.signalScan` kullanır ⟶ mobil/src/screens altında 28 dosya expo-haptics import ediyor; toplam 44 dosya / 230 çağrı. signalScan 38 yerde çağrılıyor.
- [uyumlu] Electron/CLAUDE.md:20 — renderer localStorage'a token yazmaz ⟶ 35 localStorage kullanımının hepsi UI tercihi anahtarı; token yolu `window.api.secureStore`. Tek istisna Electron/src/lib/secure-store.ts web build fallback'i ve dosya başlığında gerekçesiyle yazılı.
- [uyumlu] kök CLAUDE.md — `now() AT TIME ZONE 'UTC'` YENİ yazımı yasak ⟶ 11 mevcut kullanım (workorder.service ×4, subcontractor.service ×5, kursun-bypass ×1, kursun-qc ×1), hepsi `SET "exitedAt" = ...`. Yasak zaten 'yeni' diyor.
- [belge yanlış] Electron/CLAUDE.md:34 — inline `pageSize:N` yazma, `loadAllForPicker` kullan ⟶ loadAllForPicker 111 yerde doğru kullanılmış; ama test/yorum dışı 22 inline pageSize var (çoğu `pageSize: 1` sayaç sorgusu, birkaçı dashboard/finans listesi).
- [uyumlu] mobil/CLAUDE.md:43 — `Card` + `onPress` KULLANMA ⟶ mobil/src'de `<Card` JSX'i hiç yok (0 isabet) — ihlal yüzeyi mevcut değil.
- [uyumlu] Teks-Erp/eslint.config.mjs başlığı — 'buraya yalnız sıfır ihlal ölçülen kural girer' ⟶ Doğrulandı: `eslint src` bugün 0 hata, 0 uyarı, 0 bastırılmış (456 dosya). Beş yasağın grep'te görünen 10 isabetinin tamamı yorum satırı.
- [kod ihlal] Teks-Erp/src/config/label-kind.schema.ts — Prisma enum'u z.enum(Enum) ile bağla, elle liste yazma ⟶ Aynı sınıf 47 yerde ihlal ediliyor: `z.enum([...])` literali bir Prisma enum'unun tam aynası (DeviceKind, ShipmentDestination, WorkOrderType, Currency…). Kural yalnız LabelKind için uygulanmış.

## belge-kod-uyumu  keys=['conventions', 'docVsCode', 'gaps', 'sizeProposal', 'verdict']
### conventions (15)
- [insan|dev=89] Hata fırlatma tek sınıftan: AppError statik fabrikaları (badRequest/conflict/notFound/forbidden/unauthorized/tooManyRequests/internal); yapılandırılmış bilgi ikinci argümanda details, kod details.code altında. ⟶ ex: Teks-Erp/src/utils/app-error.ts:5-62 + Teks-Erp/src/middlewares/error.middleware.ts:425-43…
- [insan|dev=1] Durum geçişi atomik claim: updateMany where {id, beklenen-durum} → count===0 ise 409. findUnique→if→update ile durum çevirme yok. ⟶ ex: Teks-Erp/src/services/workorder-link.service.ts:385-388
- [eslint|dev=0] Tx client üzerinde Promise.all/allSettled yok; tx çağrıları sıralı await. ⟶ ex: Teks-Erp/eslint.config.mjs:23-31
- [insan|dev=3] Advisory kilit tx'in ilk DB ifadesidir; öncesinde yalnız saf JS anahtar hesabı ya da kapının kendisi durabilir. Kilit uzayı adlandırılmış sabit (X_LOCK_NS), çıplak sayı değil. ⟶ ex: Teks-Erp/src/services/batch.service.ts:118-126
- [insan|dev=4] Her CUD AuditService.log ile deftere düşer; audit best-effort (catch yutar), tx DIŞINDA ve prisma ile (tx ile değil). ⟶ ex: Teks-Erp/src/services/audit.service.ts:62-122
- [insan|dev=2] Kayıt yaratan uç clientToken String? @unique @db.Uuid taşır; replay dört durumlu (yok / aynı yük / farklı yük 409 / iptal edilmiş). ⟶ ex: Teks-Erp/src/services/helpers/token-replay.helper.ts:1-45
- [insan|dev=0] Aynı soruyu iki farklı biçimde soran koşul TEK dosyada ikiz olarak yaşar: bellek-içi yüklem + Prisma WHERE parçası yan yana tanımlanır, WHERE'e spread ile girer. ⟶ ex: Teks-Erp/src/services/helpers/quality-station.helper.ts:2-37,88,97
- [insan|dev=8] Silme soft'tur (isActive:false / RollStatus.CANCELLED); hard delete yalnız bağımlılık-guard'lı tek noktadan. ⟶ ex: Teks-Erp/src/services/helpers/guarded-hard-remove.ts:183-351
- [bekçi|dev=0] Zaman: her DateTime @db.Timestamptz, takvim günü soran her sorgu AT TIME ZONE 'Europe/Istanbul' — ve o literal yalnız src/constants/time.ts'te yazılır. ⟶ ex: Teks-Erp/src/constants/time.ts:60-90
- [bekçi|dev=0] Durum ↔ sayaç çiftinde İKİ yazar da karşı koşulu kendi atomik WHERE'ine koyar, üstüne DB CHECK seddi konur. ⟶ ex: Teks-Erp/prisma/migrations/20260814110200_saglamlik_paketi/migration.sql:96-97
- [bekçi|dev=0] Kapılar fail-closed: tanınmayan kapsam 400, kapalı modül 403 details.code=MODULE_DISABLED, uzaktan kapalı yol 404 (403 varlığı doğrular). ⟶ ex: Teks-Erp/src/middlewares/remote-access.middleware.ts:175-200
- [insan|dev=0] Önbellekte TTL tazeliktir, geçerlilik değil: dolmuş TTL bayat listeyi düşürmez, arka planda tazeler; hiç dolmamışsa fail-closed kalır. ⟶ ex: Teks-Erp/src/services/reason-preset.service.ts:176-190
- [bekçi|dev=1] İzin kontrolü matchesPermission'dan geçer (anahtar-kapsamlı joker), düz includes zincirine çevrilmez; üç istemcide de aynı fonksiyon. ⟶ ex: Teks-Erp/src/middlewares/rbac.middleware.ts:39
- [insan|dev=197] İstek gövdesi Zod ile doğrulanır ve gövde sözleşmesi iki uçta birlikte değişir; yeni ailelerde şema .strict(). ⟶ ex: Teks-Erp/src/routes/cheque.routes.ts (10 strict şema)
- [insan|dev=4118] Yorum 1-3 satır NEDEN söyler; tarih/ölçüm anlatısı arşive gider, kodda en fazla tek satırlık çapa. ⟶ ex: docs/KOD-KURALLARI.md:136-141
### docVsCode (41)
- [uyumlu] Her DateTime @db.Timestamptz; UUID kolon @db.Uuid; PG_SESSION_OPTIONS '-c timezone=UTC' süs değil. ⟶ Bekçi koşuldu, 13/13 yeşil: 307 timestamptz kolon, 311 DateTime alanı tarandı, tz'siz kolon YOK; 4 bilinçli @db.Date muafı canlı; 6/6 new Pool() PG_SESSION_OPTIONS taşıyor. Şema taramamda @db.Uuid'siz…
- [kod ihlal] Her modelde UUID PK + createdAt/updatedAt (M:N pivot ve append-only log yalnız createdAt). ⟶ 124 model. UUID PK yok: 7 (6'sı composite-PK pivot, 1'i SystemSetting key PK). createdAt yok: 2. updatedAt yok: 36 — bunların 10'u kodda GERÇEKTEN update ediliyor, yani pivot/append-only muafiyetine g…
- [uyumlu] Fabrika günü Europe/Istanbul, tek kaynak src/constants/time.ts (çıplak DATE_TRUNC yasak). ⟶ DATE_TRUNC yalnız time.ts:71 ve :90'da, ikisi de AT TIME ZONE ile; kalan 3 hit yorum. 'Europe/Istanbul' literali ESLint ile yasak, yalnız time.ts muaf; lint exit 0.
- [belge yanlış] Yalnız soft delete; bilinçli istisnalar: guard'lı DELETE /:id/permanent, boş çuval, cihaz unpair, pivot replace, ItemPrice satırı. ⟶ 18 hard .delete({ + 70 .deleteMany({. 18'in 11'i sayılı istisna (guarded-hard-remove 5, base.service:1291, shipping:1147, device:466, 2 pivot replace, item-price:471). Listede OLMAYAN 7: customer-alia…
- [uyumlu] Her CUD → AuditService.log(); tek istisna UserPreference. ⟶ 367 audit çağrısı; CUD yapan 75 servis dosyasının 71'i doğrudan çağırıyor. Çağırmayan 21 dosyanın 16'sı helpers/ (audit'li servisten çağrılır), 1'i audit.service, 1'i user-preference (beyan edilen ist…
- [uyumlu] Audit best-effort ve tx DIŞINDA; sayaç /api/admin/health'te. ⟶ log/logMany/logEvent üçü de try/catch ile yutuyor (audit.service:110,153,193) ve prisma kullanıyor, tx değil. $transaction bloğu içinde AuditService.log çağrısı: 0 (AST taraması). Sayaç app.ts:686 → A…
- [uyumlu] Kalıcı sayaç/rapor SystemLog'tan değil KALICI kolondan okunur (audit 6 ayda arşivlenir). ⟶ src/services/reports/ altında prisma.systemLog kullanımı 0. Tek SystemLog okuyucu audit.report.service.ts — o zaten audit defterinin kendi raporu, kalıcı sayaç değil. Arşiv yolu audit.service:220-261 …
- [uyumlu] Sevk rakamı HER yüzeyde BRÜT; iade ayrı belgeyle kapanır, çıkış belgesi düzeltilmez. ⟶ shipping.service.ts:3650 brütü tek noktada liveMeters+returnedMeters olarak hesaplıyor; :3597 sevk anında donan tartıya dokunulmadığını, :2976 ayrı iade irsaliyesi kesildiğini yazıyor. RollReturn 45 k…
- [uyumlu] Deftere yazan her ileri kaynağın *_CANCEL ters yolu olmalı; ters kayıt daima bugüne yazılır. ⟶ CariTxnSource 11 değer; INVOICE/PAYMENT/ADJUSTMENT birebir *_CANCEL taşıyor. Dört CHEQUE_* ileri kaynağın birebir soneki yok ama hepsinin ters yolu tek CHEQUE_CANCEL (cheque.service:1344,1482). Ad-son…
- [uyumlu] Ölü top kümesi tek kaynak K18_DEAD_STATUSES. ⟶ batch.service.ts:55'te tek tanım, 40 kullanım, 5 servis import ediyor (workorder, traveler-card, workorder-manual-move, tambur, batch). Elle kopyalanmış ölü-statü listesi bulunamadı.
- [uyumlu] finalizedAt/statusChangedAt damgasını DB trigger'ı yazar, elle yazılmaz. ⟶ Trigger var: migrations/20260809090000_roll_production_timestamps/migration.sql:93 'rolls_stamp_production_timestamps' (:16 gerekçe: Roll.status'e yazan 40+ nokta). Kodda finalizedAt: yazımı yalnız ta…
- [uyumlu] Yıkıcı işlemde backend preview ucu döner, arayüz etkilenen HER kaydı listeler ve per-record seçim sunar. ⟶ 9 preview/impact ucu: order:181, order:790, inventory:570, shipping:530, subcontractor:697, kartela:251, workorder:820 cancel-impact, workorder:1087, admin:1778 restore-impact. Arayüz tarafının per-re…
- [uyumlu] Validation mesajları Türkçe; TR-only bilinçli, i18n kurulmaz. ⟶ Üç package.json'da i18n/i18next/react-intl bağımlılığı 0. src/lib/zod-locale.ts:17-20 Zod global locale'ini tr'ye bağlıyor. message: içinde İngilizce kalıntı 0.
- [kod ihlal] Durum geçişi ATOMİK CLAIM: updateMany WHERE {id, beklenen-durum} + count===0 → 409; findUnique→if→update YASAK. ⟶ 303 updateMany, 120 'count === 0 / count !== 1'. findUnique→if→update AST adayı 31; çoğu master-data alan düzenlemesi (durum geçişi değil). Gerçek ihlal: traveler-card.service.ts:269-285 — tx içinde f…
- [uyumlu] tx.* çağrıları Promise.all ile paralelleştirilmez. ⟶ Parantez dengeleyen AST taraması: 124 Promise.all'ın 86'sı prisma.<model> içeriyor, tx.<model>/tx.$ içeren 0. ESLint TX_PROMISE_ALL selector'ü (eslint.config.mjs:23-31) mekanik zorluyor, lint exit 0, …
- [uyumlu] Advisory kilit tx'in İLK ifadesidir (sonra alınan kilit TOCTOU). ⟶ 26 çağrı yeri. $transaction içindekilerin hepsinde kilit ilk DB ifadesi: inventory:988'de öndeki 11 satır saf JS lockKey hesabı + yorum, hiç sorgu yok; goods-receipt:682'de önce yalnız opts.txGate (ka…
- [belge yanlış] Kilit uzayı envanteri period-guard.helper.ts başlığında (8021…8028); master-data merge 8027'yi paylaşıyor ve envanterde yok. ⟶ Kodda 10 NS sabiti var, başlık (period-guard.helper.ts:40-46) yalnız 7'sini listeliyor. Eksik üç uzay: 8023 SHIPMENT_LOCK_NS (shipment-locks.helper:28), 8026 CODE_UNIQUE_LOCK_NS (code-unique.helper:57…
- [uyumlu] PG 40P01/40001 → 409 'tekrar deneyin'. ⟶ error.middleware.ts:330 TRANSIENT_SQLSTATES = {40P01, 40001}; :369 yalnız mesajdaki açık 'Code: `40P01`' kalıbını eşliyor, genel regex :341-342'de gerekçesiyle yasaklanmış ('40001' meşru bir tutarda g…
- [uyumlu] Durum ↔ sayaç çifti olan modelde iki yazar da karşı koşulu kendi atomik WHERE'ine koyar + DB CHECK seddi. ⟶ Emsal birebir kurulu: cheque.service:854 allocatedTotal:0 atomik WHERE'de, payment-allocation.service:284 bumpChequeAllocated karşı süzgeci taşıyor (:147 ikisini bağlıyor), sed migration 2026081411020…
- [belge yanlış] Kayıt yaratan uçlar clientToken @unique taşır (16 model). ⟶ Şemada clientToken @unique taşıyan model 15, 16 değil: Roll, Order, WorkOrder, SubcontractorReceipt, SwatchStockReduction, Sack, Shipment, ImportRun, WarehouseTransfer, GoodsReceipt, Invoice, Payment,…
- [uyumlu] İstemci token'ı MANTIKSAL DENEME başına bir kez üretir; replay dört durumlu (helpers/token-replay.helper.ts). ⟶ token-replay.helper.ts:1-28 dört durumu ve saha ölçümünü yazıyor, REPLAY_DEAD_ROLL_STATUSES (CANCELLED, SCRAP) tanımlıyor; 4 servis import ediyor (inventory, shipping, order, tambur-manual). CLIENT_TO…
- [kod ihlal] Türetilmiş alan / ayrışan yüzey: aynı soruyu cevaplayan koşul TEK helper'da yaşar ve AST bekçisiyle korunur. ⟶ AST tabanlı bekçi 8 tane (test_search_field_config, test_permission_catalog, test_period_close, test_raw_sql_hygiene, test_fason_open_dispatch_single_source, test_route_mount_reachability, lib/regime-…
- [uyumlu] Bellek-içi yüklem ile Prisma parçası boğaz-ikizdir ve birlikte değişir (stepCanApplyQuality ↔ QUALITY_STATION_WHERE); saf yüklem WHERE'e gir… ⟶ quality-station.helper.ts:88 ve :97 ikisini yan yana tanımlıyor; başlık (2-37) ikizliği, 'satisfies ile yaz', 'OR ile aynı seviyeye koyma, AND: ile sar' kurallarını taşıyor. Üç serviste birlikte kulla…
- [BELİRSİZ] Elle okunan her id filtresi readIdCondition/readFilterList'ten geçer; cursor'lu listede süzme SUNUCUDA. ⟶ İkisi de query-parser.ts:149 ve :185'te tek tanım, 61 kullanım. Sunucu tarafı uyumlu. Ancak Electron/src/pages altında 296 .filter( çağrısı var; bunların cursor'lu liste üstünde mi yoksa yerel dizide …
- [uyumlu] Enum'a değer eklemek, bayrak eklemek, route+izin eklemek reçeteli iştir (docs/RECETELER.md). ⟶ Reçetenin arkasında bekçi var: test_permission_catalog + test_route_mount_reachability (ikisi de AST/ts-morph), test_feature_flag_contract, test_module_flags, test_module_flag_off. check-docs.mjs koşu…
- [kod ihlal] İstek gövdesini elle kuran istemci katmanı sessiz bir allowlist'tir; Zod tanımadığı anahtarı sessizce siler — alan iki uçta da sözleşmeye ek… ⟶ Backend 252 z.object, yalnız 55 .strict() (%22). Dağılım kümelenmiş: yeni finans ailesi finance.routes 10 + cheque.routes 10 + finance.report.routes 6 strict; buna karşılık shipping.controller 31 z.ob…
- [uyumlu] FAIL-CLOSED varsayılan: tanınmayan kapsam 400. ⟶ rollScope switch'inin default'u atıyor: inventory.service.ts:1547 AppError.badRequest('Bilinmeyen liste kapsamı: ...'). 1995 AppError çağrısının 1009'u badRequest / 537 conflict / 374 notFound. Sessiz…
- [uyumlu] Önbellekte TTL tazeliktir geçerlilik değil (bayat döner + tazeler; hiç dolmadıysa fail-closed kalır). ⟶ reason-preset.service.ts:19 ve :176-190 kuralı birebir uyguluyor (TTL dolması önbelleği düşürmez, bayat liste döner, arka planda tazelenir). remote-access.middleware.ts:212 aynı dersi ikinci yüzeyde t…
- [uyumlu] Hata kodu details.code altında (body.code hep undefined). ⟶ error.middleware.ts:433 gövdeyi {success, message, ...(details ? {details} : {})} kuruyor — üst düzey code ASLA basılmıyor. 1995 AppError çağrısının 223'ü details taşıyor, 134'ünde code var; 89'unda d…
- [uyumlu] Kapalı modül 403 MODULE_DISABLED; uzakta kapalı yol 404 (403 varlığı doğrular); jenerik requireModule('x') yasak. ⟶ module.middleware.ts:81,122 ve yarn.service.ts:175 details {code:'MODULE_DISABLED'} veriyor. Uzak 404: remote-access.middleware.ts:200 res.status(404), gerekçesi :175-176'da ('403 keşfe davet eder'). …
- [uyumlu] Uzak/LAN ayrımı soketten, clientType güvenlik sınırı değil; tünel dinleyicisi yalnız 127.0.0.1. ⟶ remote-access.middleware.ts:10-11 ayrımın clientType ile YAPILAMAYACAĞINI yazıyor (gövdeden gelir, uydurulabilir). server.ts:214 'TÜNEL DİNLEYİCİSİ — YALNIZ 127.0.0.1', :227 app.listen(remotePort, '12…
- [uyumlu] SoD üçlüsü (shipping:invoice, shipping:undo-dispatch, roll:manual-adjust) yalnız Muhasebe/Süpervizör; süperadmin rol değil (['*']), panelden… ⟶ permission-catalog.ts:57,87,90 üçünü tanımlıyor. role-template-catalog.ts: WEB_ACCOUNTING(:158) shipping:invoice(:165); WEB_PRODUCTION_SUPERVISOR(:292) roll:manual-adjust(:301) + shipping:undo-dispatc…
- [kod ihlal] İzin guard'ı anahtar-kapsamlı ve düz OR'a çevrilmez; matchesPermission üç istemcide. ⟶ Üç istemcide de var: rbac.middleware.ts (backend, 19 dosya kullanıyor), Electron/src/types/auth.ts (+ auth.test.ts:38 negatif sonda), mobil/src/hooks/usePermission.ts. SAPMA: Electron/src/components/l…
- [uyumlu] Rota kapsaması KATEGORİ düzeyinde reddetmez, UYARIR (ApiResponse.warnings); özellik-başına kapsama sert kalır (create 400 / replace 409). ⟶ workorder.service.ts:174 collectRouteCoverageWarnings → Promise<string[]>, atmıyor; :140-172 gerekçesi (2026-08-27, SAP PP emsali, kabul edilen risk). Özellik başına sert kapı: :965 bloğu, :988 AppErr…
- [uyumlu] Top↔sipariş satırı bağı YOKTUR, karşılama SackAllocation ile sevk anında; WorkOrder.type beyan değil bağın aynasıdır. ⟶ Roll modelinde satış OrderLine bağı yok — yalnız purchaseOrderLineId (ALIŞ siparişi, farklı domain). SackAllocation 15 kullanım. workorder-link.service.ts:385-388 bağ yazılırken type'ı ATOMİK CLAIM il…
- [uyumlu] Backend ÖNCE; minVersion yalnız gerçek kırılmada ve sahadakinden BÜYÜK OLAMAZ. ⟶ client-version-policy.ts:90 minVersion: '1.0.0' — en düşük değerde duruyor ve :106 bunu açıkça yazıyor ('Faz 2'de mobil ekseni burada'). :74-77 minVersion'ın ifade EDEMEDİĞİ durum için ayrı alan tanım…
- [uyumlu] Sürüm notu yazılmadan sürüm çıkmaz; paketleme kapısı durdurur (surum-notlari.json). ⟶ node scripts/check-surum-notlari.mjs koşuldu → 13 geçti / 0 kaldı (Electron ve mobil kopyaları kaynakla aynı). Kapı gerçekten paketleme yolunda: deploy/electron-paketle.sh:143 bu betiği --panel=<sürüm…
- [uyumlu] Prisma'nın iki motoru var; Windows paketi PRISMA_CLI_BINARY_TARGETS=windows + MZ kapısı. ⟶ deploy/paketle.ps1:245 env set, :248 temizlik, :261 hata mesajında çözüm satırı; deploy/kur.ps1:281 kurulum tarafında ikinci kapı. Son commit 56ab3f8e 'dort kapi eklendi' diyor ve dördü de dosyalarda …
- [uyumlu] Backend TEK process; pkill -f tsx YASAK (yalnız kendi PID'in); ikinci Node süreci yasak; seri port/donanım polling backend'e girmez. ⟶ server.ts:94 ve :227 AYNI app nesnesini iki kez listen ediyor — tek process, iki dinleyici; :42 invariantı yazıyor. Backend'de serialport/node-hid bağımlılığı 0 (package.json + src). pkill yasağı yaln…
- [uyumlu] quickPin düz metin ve tek başına kimliktir — hiçbir yüzeyden sızdırılmaz; uydurulmuş değer source:'SIMULATED' beyanıyla gider. ⟶ quickPin 24 hit; Prisma select'inde yalnız auth.service.ts:193 ve :295 (kimlik doğrulama). Route/controller'da yalnız yorum + admin.routes.ts:808 quickPinSchema (yazma yolu). db-copy.routes.ts:10 ve a…
- [uyumlu] Koddaki yorum 1–3 satır NEDEN söyler, tarih ve ölçüm anlatısı taşımaz. ⟶ 10.803 yorum bloğu: %61'i (6.685) 1-3 satır, p50 2; ama 4.118 blok >3 satır, 1.442 >10, 182 >30 (max 111) ve 1.008 blok 20xx-xx-xx tarihi taşıyor. AMA docs/KOD-KURALLARI.md:140 kuralın İLERİYE DÖNÜK o…
### gaps (11)
- [orta|orta] updatedAt taşımayan 10 model kodda gerçekten update ediliyor — 'pivot ve append-only log yalnız createdAt' muafiyeti bunları kapsamıyor ⟶ prisma/schema.prisma: updatedAt'siz 36 model; bunlardan update çağrısı olanlar: rollOperation(5), subcontractorDispatchItem(5), rollVariance(4), sackTagAssignme…
- [yüksek|küçük] 8026 advisory kilit uzayı iki farklı amaçla paylaşılıyor ve bu çakışma HİÇBİR belgede yok (8027 çakışması belgede var, bu yok) ⟶ Teks-Erp/src/services/helpers/code-unique.helper.ts:57 CODE_UNIQUE_LOCK_NS = 8026; Teks-Erp/src/services/helpers/period-guard.helper.ts:73 PERIOD_CLOSE_LOCK_NS …
- [orta|küçük] Kilit uzayı envanteri başlığı 10 uzayın yalnız 7'sini listeliyor; 8023, 8026, 8027(merge) eksik — kök CLAUDE.md envanterin tam olduğunu varsayıyor ⟶ Teks-Erp/src/services/helpers/period-guard.helper.ts:40-46 (7 satır) vs kodda 10 NS sabiti: shipment-locks.helper.ts:28 (8023), code-unique.helper.ts:57 (8026),…
- [orta|küçük] traveler-card yeniden basımı atomik claim kullanmıyor: findUnique → status kontrolü → version+1 update; iki eşzamanlı basım aynı sürüm numarasını yazar ⟶ Teks-Erp/src/services/traveler-card.service.ts:269-285 — tx.travelerCard.findUnique → if status !== ACTIVE throw → tx.travelerCard.update({ where:{id}, data:{ v…
- [yüksek|büyük] mobil'de şema doğrulama katmanı hiç yok — 'alan iki uçta da sözleşmeye eklenir' kuralının tablet yarısı kurulmamış; gövdeler elle kuruluyor, sessiz allowlist riski %100 ⟶ mobil/package.json'da zod bağımlılığı yok; mobil/src'de 386 .ts/.tsx dosyada z.object 0, .strict() 0
- [orta|orta] Backend Zod şemalarının %78'i strict() değil; strict yalnız yeni finans ailesinde kümelenmiş, eski controller'larda hiç yok ⟶ 252 z.object / 55 .strict(); finance.routes 10 + cheque.routes 10 + finance.report.routes 6 strict, buna karşılık shipping.controller 31 z.object/3 strict, work…
- [orta|küçük] Soft-delete istisna listesi kod gerçeğinden dar: 7 hard delete beyan edilen 5 istisnadan hiçbirine girmiyor, ayrıca DRAFT fatura entity düzeyinde siliniyor ⟶ customer-alias.service.ts:94,198 · duplicate-review.service.ts:151 · sack-tag.service.ts:218 · permission-management.service.ts:1041 · batch.service.ts:337 · la…
- [düşük|küçük] Electron bildirim çanı izin kontrolünü matchesPermission yerine düz OR zincirine çevirmiş — kuralın adıyla anılan tam ihlali ⟶ Electron/src/components/layout/NotificationBell.tsx:28-31 → permissions.includes('*') || permissions.includes('admin:*') || permissions.includes('admin:settings…
- [düşük|küçük] clientToken model sayısı belgede 16, kodda 15 ⟶ prisma/schema.prisma'da clientToken @unique alanı 15 (satır 1516,2241,2436,3781,4123,4471,4667,5814,6023,6101,6334,6567,6780,6976,7374)
- [orta|orta] En çok anılan 'boğaz ikiz' (stepCanApplyQuality ↔ QUALITY_STATION_WHERE) AST bekçisiyle korunmuyor; kural 'AST bekçisiyle korunur' diyor ama bu çift için yalnız davranış testi var ⟶ AST/ts-morph kullanan 8 bekçi arasında quality-station yok (test_station_capability.ts + test_station_capability_flags.ts davranış testi); quality-station.helpe…
- [düşük|orta] Electron sayfalarında 296 .filter( çağrısı var; hangilerinin cursor'lu liste üstünde istemci süzmesi yaptığı ölçülmedi — 'süzme SUNUCUDA' kuralı bu ajanda doğrulanamadı ⟶ grep '.filter(' Electron/src/pages --include=*.tsx → 296 hit; sunucu tarafı (readIdCondition/readFilterList, query-parser.ts:149,185) 61 kullanımla temiz
### sizeProposal
Ölçülen gerçek: backend-services 269 dosya, p50 236 / p90 1001 / max 7143 satır (103 dosya >300); fonksiyon p50 14 / p90 59 / p99 160, 28 fonksiyon >200. routes p50 208 / p90 749 / max 1983. Electron p50 113 / p90 299; mobil p50 119 / p90 436 / max 9387. Öneri: YENİ dosya ≤400 satır, YENİ fonksiyon ≤60 satır (p90'ın hemen altı — bugünkü iyi yarıyı tarif eder, uydurma değil); devralınan >300 satırlık 103 servis + 5 dev route dosyası sınır dışı, yalnız dokunulan bölümde kural işler. Tavan kırmızı bekçi olarak DEĞİL, inceleme sinyali olarak konur: ölçülen ihlal sıfır olmadığı için ESLint'e giremez (docs/KOD-KURALLARI.md:149 ölçütü).
### verdict
40 çekirdek değişmezin 32'si koda karşı UYUMLU, 5'i KOD İHLALİ, 3'ü BELGE YANLIŞ, 1'i BELİRSİZ (Electron istemci süzmesi ölçülmedi). Değişmezlerin mekanik olarak zorlananları gerçekten sıfır ihlalli: ESLint 6 desen (npx eslint src exit 0, ilgili disable yok), timestamptz bekçisi 13/13, sürüm notu kapısı 13/13, tx.*+Promise.all AST taramasında 0/124, $transaction içinde audit çağrısı 0. En sert bulgu belgede hiç yok: 8026 advisory kilit uzayı iki amaçla paylaşılıyor (code-unique.helper.ts:57 ↔ period-guard.helper.ts:73) ve envanter başlığı 10 uzayın 7'sini listeliyor. İkinci sert bulgu: mobil'de zod HİÇ yok (386 dosya, 0 z.object), yani 'alan iki uçta da sözleşmeye eklenir' kuralının tablet yarısı kurulmamış. Standart için zemin nettir: bu repo zaten 'yeni kodda zorunlu / devralınan sınır dışı' modeliyle çalışıyor ve bunu yazılı ölçütle yapıyor (KOD-KURALLARI.md:149 — kural ancak AST'den kesin yakalanıyorsa VE mevcut ihlal SIFIRSA ESLint'e girer). Ölçülen ihlali sıfır olmayanlar (Zod strict %22, yorum uzunluğu %39, updatedAt eksikleri) tam da ileriye-dönük kural adayıdır; ölçülen ihlali sıfır olanlar (tx+Promise.all, notIn:[], Europe/Istanbul literali, route'ta prisma import) zaten mekanik kapıya alınmış.