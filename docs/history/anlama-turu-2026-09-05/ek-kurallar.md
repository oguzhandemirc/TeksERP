# Ek — Canlı kural kümesi (Faz C, keepIn ile)


## kök-kural (277)

| Küme | Kural | Sınıf | Tür | Kaynak | Bekçi |
|---|---|---|---|---|---|
| belge-etiket-sablon | ACTIVE refakat kartının önizlemesi VE baskısı iş emrinin GÜNCEL planından üretilir; TEK karar noktası resolvePrintPlan (önizleme + baskı + sürüm no). GET yan etkisiz; print-event basılan planı kaydeder, içerik değiştiyse version++ — aynı içeriğin kopyası sürüm artırmaz. | ÇEKİRDEK | değişmez | `R:2026-08-05__refakat-karti-plan-canli-sunum-donmus` | scripts/test_traveler_card_stale.ts §6 + test_tra… |
| belge-etiket-sablon | İÇERİK ≠ SUNUM, ikisi de CANLI: planKey config/template'i DIŞLAR (tasarım değişikliği sürüm artırmaz, kartı bayat işaretlemez); şablon + config + sayfa boyutu HER baskıda canlı — arşiv sürümü (?version=N) ve geçersiz kart dahil, onlarda yalnız İÇERİK donuk. 'Şablon karta donar' KALKTI (2026-08-06). | ÇEKİRDEK | değişmez | `R:2026-08-05__refakat-karti-plan-canli-sunum-donmus`, `R:2026-08-03__refakat-karti-sablonu-uc-kademe-2026`, `R:2026-08-03__refakat-karti-a5-varsayilan-parti-blogu` | test_traveler_template.ts:186-196 E2 (sunum donma… |
| belge-etiket-sablon | Şablon güvenliği İKİ katman, ikisi gerekli: ① sanitizeTemplateHtml HEM kayıtta HEM render'da (script / iframe-object-embed / link-meta / on* / javascript: / data:text/html kesilir; aşırı kesme güvenli yön; dış <img>/<a> BİLEREK serbest); ② değerler escapeHtml (yalnız sunucu QR SVG'si ham). | ÇEKİRDEK | değişmez | `R:2026-08-03__refakat-karti-sablonu-uc-kademe-2026` | scripts/test_traveler_template.ts (sanitize kontr… |
| belge-etiket-sablon | Baskı + önizleme iframe'leri sandbox="allow-same-origin allow-modals" taşır (doc.write + win.print için ikisi load-bearing); allow-scripts YOK — kırpma. Ölçüldü: sandbox'sız iframe'de gömülü script Electron renderer'ında çalışıyordu. | ÇEKİRDEK | yasak | `R:2026-08-03__refakat-karti-sablonu-uc-kademe-2026` | yok |
| belge-etiket-sablon | İÇ VERİ TAŞIYAN kolon OPT-IN doğar: columns[tablo].hidden BLOCKLIST'tir (anahtar yoksa AÇIK → yeni kolon varsayılan GÖRÜNÜR doğar, çuval notu müşteri irsaliyesine sızar). Opt-in kolon = DocCol.defaultHidden + columns[tablo].shown allowlist (hidden orada yok sayılır); sıra columns[tablo].order. | ÇEKİRDEK | değişmez | `R:2026-07-30__2026-07-30-cuval-notu-sack`, `E:2026-07-30__belge-kolonu-ekleme-ic-veri-tasiyorsa`, `R:2026-08-05__2026-08-05-parti-no-k` | scripts/test_sack_note_document.ts |
| belge-etiket-sablon | Belge BÖLÜMLERİNDE aynı üçlü: sections BLOCKLIST (anahtar yoksa AÇIK); iç veri bölümü DocSectionDef.defaultHidden ile ALLOWLIST ve HEM renderer HEM panelde işaretlenir (tek taraf 'panel açık der, belge boş çıkar' yalanı üretir). | ÇEKİRDEK | tuzak | `R:2026-08-05__2026-08-05-parti-no-k` | scripts/test_sack_note_document.ts |
| belge-etiket-sablon | Sevk belgesinde müşterideki ad: AD DONAR (snapshot products[].customerName, cekiRows[].customerDesen/Varyant — sonraki alias düzeltmesi eski irsaliyeyi değiştirmez), REJİM DONMAZ (shipping.docItemNameMode her baskıda canlı, buildRenderExtras → meta.itemNameMode). İkisi karışırsa iki ayrı arıza. | ÇEKİRDEK | değişmez | `R:2026-09-04__2026-09-04-sevk-belgesinde-musterideki` | scripts/test_shipment_doc_customer_name.ts §3 |
| belge-etiket-sablon | Müşteri adı zinciri ETİKETLE AYNI (shipment-customer-name.helper: OrderLine.customerItemName > CustomerItemAlias > yok; renk ikizi customerColorName/CustomerColorAlias — yarı çevrilmiş ad basılmasın). İkinci semantik açmak aynı topun etiketi ile irsaliyesinde farklı ad demektir. | ÇEKİRDEK | değişmez | `R:2026-09-04__2026-09-04-sevk-belgesinde-musterideki` | scripts/test_shipment_doc_customer_name.ts |
| belge-etiket-sablon | Top↔sipariş satırı bağı YOK → override kümesi sevkiyat kapsamında (çuval tahsisi + sipariş bağı) toplanır, (itemId,colorId) ile eşlenir, sıra DETERMİNİSTİK (tahsisli→createdAt→id). Gruplama anahtarı BİZİM adımız; kolon anahtarları ayrı (name↔customerName); iade satırları aynı zincir. | ÇEKİRDEK | tuzak | `R:2026-09-04__2026-09-04-sevk-belgesinde-musterideki` | scripts/test_shipment_doc_customer_name.ts (overr… |
| belge-etiket-sablon | SEVK RAKAMI BRÜT: getDispatchReport (fiş) DONMUŞ snapshot'tan okur (PLANNED taslakta collectShipmentDocContent); snapshot'ı ÜRETEN collectShipmentDocContent de brüt kurar — iptal edilmemiş RollReturn prevSackId ile çuvala geri eklenir — çünkü reissue + lazy-init sevkten SONRA da onu çağırır. | ÇEKİRDEK | değişmez | `R:2026-08-02__2026-08-02-sevk-rakami-brut`, `A:2026-08-05__2026-08-05-belge-yolu-da` | BELİRSİZ — bu turda bekçi adı doğrulanmadı |
| belge-etiket-sablon | Brüt belge üreticisinin üç tuzağı: çuval satırı + ürün özeti + çeki satırları TEK sacksGross kaynağından (ayrı toplamak çift sayar); iade sorgusu ayrı (tx yok) → canlı id kümesiyle DEDUP; totalKg DEĞİŞMEZ (iade Sack.weightKg'a dokunmaz). Yeni sevk-içeriği yüzeyinde soru: 'sevkten SONRA da koşar mı?' | ÇEKİRDEK | tuzak | `A:2026-08-05__2026-08-05-belge-yolu-da`, `A:2026-08-03__2026-08-03-brut-kurali-cuval` | BELİRSİZ — bu turda bekçi adı doğrulanmadı |
| belge-etiket-sablon | İade sevk belgesini geriye dönük DEĞİŞTİRMEZ; AYRI belgeyle kapanır (RETURN_DISPATCH iade ANINDA donar, iptalde VOIDED). Belgedeki 'Güncel' rozeti = son VERSİYON demektir, 'içerik güncel' DEĞİL. | ÇEKİRDEK | değişmez | `R:2026-08-02__2026-08-02-sevk-rakami-brut` | BELİRSİZ |
| belge-etiket-sablon | SACK etiketi: barkod + QR = Sack.sackNo (CV+GGAAYY+NNNN) — Sack'e ayrı barcode kolonu EKLENMEZ (tek kod kuralı). Katalogda ürün/renk/kat alanı YOK (çuval karışık içerikli → tek değer sessizce yanlış). Çuval kodu top alanına okutulursa anlamlı 400; mobil Paketleme kodu tanıyıp çuvalı aktif yapar. | ÇEKİRDEK | değişmez | `R:2026-07-30__2026-07-30-cuval-notu-sack`, `R:2026-08-13__2026-08-13-kesimde-kat-sessizce` | scripts/test_label_context_fit.ts |
| belge-etiket-sablon | SACK baskısı FAIL-CLOSED: şablon çözülemez ya da bağlama UYMAZSA 400, roll/swatch etiketine SAPMAZ. Uyum LabelTemplate.kind'a BAKMAZ (kolon DEPRECATED + nullable — 'tek havuz': şablon türden bağımsız), şablonun BAĞLADIĞI alanlardan türer: kanvas varyantı + sackNo bind ŞART (analyzeContextFit). | ÇEKİRDEK | değişmez | `R:2026-07-30__2026-07-30-cuval-notu-sack` | scripts/test_label_context_fit.ts |
| belge-etiket-sablon | Sack.notes ANNOTATION'dır (her an yazılır, sevk edilmiş çuvala da; donmuş belgeye girmez): setSackNotes touchWarehouseSackTx guard'ını UYGULAMAZ, markSackContentChangedTx nota DOKUNMAZ — guard'ı 'eksik' sanıp EKLEME, eklersen özellik sessizce 409'a düşer. | ÇEKİRDEK | yasak | `R:2026-07-30__2026-07-30-cuval-notu-sack`, `B:undated__depo-cuvali-icerigine-dokunuyorsa-once-where` | scripts/test_sack_notes.ts (8b/9) |
| belge-etiket-sablon | Koşullu etiket elemanı (showIf): koşul ELEMANDA, değeri QualityGrade.code (ad DEĞİL); toLocaleUpperCase("tr") YASAK; kalitesiz topta koşullu eleman BASILMAZ (fail-closed); tek uygulama noktası prepareElements — yeni emitter expandMultilineText'i çağırmaz; koşullu alan bağlam kimliği SAYILMAZ. | ÇEKİRDEK | değişmez | `R:2026-08-02__2026-08-02-kosullu-etiket-elemani` | scripts/test_label_element_condition.ts |
| belge-etiket-sablon | Etikete katalog KODU basılır ('6-KAT'), görünen ad DEĞİL — kod topun kimliğidir, ad değişirse geçmiş baskıyla ayrışır; ada çevirmek her etikete DB okuması ekler. Değeri olmayan alan present:false → şablonda dursa da ATLANIR (eski şablonlar bayt-bayt, sackNote emsali). | ÇEKİRDEK | değişmez | `R:2026-08-13__2026-08-13-kesimde-kat-sessizce` | scripts/test_fold_edit_and_label.ts (16) |
| belge-etiket-sablon | Belge tasarımı ayrı yetkidir: Tanımlar → Çıktılar'ın dört ekranı (Belge Şablonları · Refakat Kartı · Refakat Kartı Şablonları · Serbest Belgeler) document-template:read/write ile korunur; kategori web — hasAdminAccess saymaz, taşıyan Yönetim menüsünü/Sistem hub'ını görmez. | ÇEKİRDEK | değişmez | `R:2026-08-05__belge-tasarimi-ayri-bir-yetkidir-sistem`, `R:2026-08-03__refakat-karti-sablonu-uc-kademe-2026` | scripts/test_document_template_permission.ts (81) |
| belge-etiket-sablon | admin:settings DOCUMENT_DESIGN kümelerinden DÜŞÜRÜLMEZ (dört ekranı açmaya devam eder): boot uzlaştırması yeni izin satırını getirir ama kimseye ATAMAZ — sıkı ayrım deploy anında admin dahil herkesi dışarıda bırakır. READ kümesi WRITE kodunu da içerir (yazabilen okuyabilir). Atama elle. | ÇEKİRDEK | yasak | `R:2026-08-05__belge-tasarimi-ayri-bir-yetkidir-sistem` | scripts/test_document_template_permission.ts (neg… |
| belge-etiket-sablon | Belge tasarım izin kümeleri TEK kaynak constants/document-design.ts (saf string literal — AST bekçisi okur); Electron backend'i import edemez, lib/permissions.ts'te AYNALAR; kart tile-config.permissionAny ↔ route content-routes.requireAnyPermission BİREBİR (ayrışma = görünen kart + /forbidden). | ÇEKİRDEK | değişmez | `R:2026-08-05__belge-tasarimi-ayri-bir-yetkidir-sistem` | scripts/test_document_template_permission.ts (kar… |
| belge-etiket-sablon | PATCH /api/feature-flags guard'ı ANAHTAR-KAPSAMLI, düz OR'a ÇEVRİLMEZ: gövde YALNIZ DOCUMENT_DESIGN_FLAG_KEYS (documentsConfig, travelerCardConfig) taşıyorsa DOCUMENT_DESIGN_WRITE yeter; tek yabancı anahtar / boş gövde → admin:settings (FAIL-CLOSED). Aynı yüklem ayar-şifresi belge muafiyeti. | ÇEKİRDEK | değişmez | `R:2026-08-05__belge-tasarimi-ayri-bir-yetkidir-sistem`, `R:2026-09-03__2026-09-03-ayar-sifresi-p3` | scripts/test_document_template_permission.ts + te… |
| belge-etiket-sablon | DOCUMENT_DESIGN_FLAG_KEYS bilerek DAR: companyName / companyLetterhead / belge logosu firmanın KİMLİĞİDİR, şablon değil — kümeye EKLENMEZ (onları yazan ekran zaten admin:settings arkasında). Yeni anahtar sorusu: 'yanlış girilirse etkisi belge çıktısıyla SINIRLI mı'. | ÇEKİRDEK | yasak | `R:2026-08-05__belge-tasarimi-ayri-bir-yetkidir-sistem` | yok |
| deploy-kurulum | PRODUCTION CANLI ve TEKİL DEĞİL: her canlı kurulumda gerçek veri korunur — migrate reset / reseed / toplu DELETE YASAK; migration geri alınamaz kabul edilir (rollback = yedekten restore). Toplu veri düzeltme script'i dry-run varsayılan, --apply öncesi etkilenen her kaydı somut listeler. | ÇEKİRDEK | yasak | `R:undated__production-canli-gercek-fabrika-gercek-veri`, `R:2026-08-21__2026-08-21-is-emri-tipi` | yok (script başına dry-run deseni; kur.ps1 [7/9] … |
| deploy-kurulum | Şema/migration işinde ölçüt en eski canlı kurulumun verisidir; prova o kurulumun dump'ı üstünde: dump restore → migrate deploy → bekçiler → profil boot. | ÇEKİRDEK | reçete | `R:undated__production-canli-gercek-fabrika-gercek-veri` | yok |
| deploy-kurulum | Commit edilmemiş migration prod'da SESSİZ eksiktir: `migrate deploy` yalnız dizindeki dosyaları uygular, 'başarılı' der, kolonu okuyan her yol P2022 verir. Elle migration sırası git add → db execute → resolve --applied → DOĞRULA; mekanik: `npx tsx scripts/apply-migration.ts <ad> [--apply]`. | ÇEKİRDEK | değişmez | `B:undated__database-performance-rules-her-zaman-uygula` | scripts/check-migrations.mjs (npm run check:migra… |
| deploy-kurulum | Kısıt veri temizlenmeden aynı sürümde gelmez (expand → backfill → contract): sed migration'ı mükerrer varsa RAISE NOTICE ile ATLAR, deploy'u DÜŞÜRMEZ; temizlik sonrası aynı dosya yeniden koşulur (idempotent enforce); o güne dek `test_db_invariants` §1 kırmızı = 'enforce bekliyor' (bilerek). | ÇEKİRDEK | değişmez | `R:2026-08-21__2026-08-21-namefold-db-seddi`, `A:2026-08-22__2026-08-22-sifirlama-rafa-kalkti`, `R:2026-08-25__2026-08-25-prod-un-uc` | Teks-Erp/scripts/test_db_invariants.ts §1 (kırmız… |
| deploy-kurulum | Prisma'nın İKİ motoru var: sorgu WASM (platform-bağımsız), ŞEMA motoru (`migrate deploy`) NATIVE `schema-engine-<platform>`. Windows paketi `PRISMA_CLI_BINARY_TARGETS=windows`; `paketle.ps1` varlık + MZ kapısı, `kur.ps1 [1/9]` eşikten ÖNCE; yabancı motor atılır. Yeşil prova 'paket yeter' DEMEZ. | ÇEKİRDEK | değişmez | `R:2026-09-04__2026-09-04-cekirdek-prisma-nin` | deploy/paketle.ps1 kapıları + deploy/kur.ps1 [1/9… |
| deploy-kurulum | 'Doğru cümlenin yanlış genellemesi' sınıfı: bir bileşenin bir boyutta platformdan bağımsız olması tüm alt bileşenleri için geçerli değildir — 'artık platformdan bağımsız' notu hangi ikilileri kapsadığını ve aynı paketin başka hangi ikilileri olduğunu söyler. | ÇEKİRDEK | değişmez | `R:2026-09-04__2026-09-04-cekirdek-prisma-nin` | yok |
| deploy-kurulum | Fabrika paketi ve demo derlemesi `main`'den üretilir; müşteri dalı YOK (`adnansahin`, `feature/depo-mal-kabul` emekli); `if (musteri === 'X')` fork'un ilk sinyalidir, yasak — müşteri farkı yalnız bayrak profilinde yaşar. | ÇEKİRDEK | yasak | `R:undated__tekserp-monorepo-koku`, `E:undated__adnan-sahin-erp-admin` | yok |
| deploy-kurulum | Deploy sırası pazarlık dışı: BACKEND ÖNCE; sahada bir süre eski istemci koşar. Her paket notu üç kapıyı sayar (migration? izin? APK/Electron?). İstemci sunucunun İSTEDİĞİ yeni alanı göndermek zorundaysa (`confirmMismatch`) üçü BİRLİKTE; eksik alan güvenli varsayılana düşüyorsa backend tek gider. | ÇEKİRDEK | değişmez | `B:undated__istemci-surum-politikasi-panel-tablet-uyumu`, `R:2026-08-06__2026-08-06-filtrelerde-coklu-secim`, `R:2026-08-19__2026-08-19-fason-kismi-kabul` | yok (istemci politikası test_client_policy yalnız… |
| deploy-kurulum | Altı sözleşme tetiği — uç kaldırıldı (404) · yanıt alan adı değişti (sessiz undefined) · tip/birim değişti (sessiz yanlış hesap) · zorunlu parametre (400) · enum'a değer ('altıncı enum' beş kez) · izin zorunlu (403) — 'eski istemci ne yapar' AÇIKÇA cevaplanır; bozulursa minVersion, değilse DOKUNMA. | ÇEKİRDEK | tuzak | `B:undated__tetik-su-degisiklikleri-yaparken-dur-ve` | yok |
| deploy-kurulum | `minVersion` sahadaki sürümden BÜYÜK OLAMAZ (en güncel istemci bile kapıda kalır, indirecek şey yok — kendini kurtaramayan tek arıza) ve yalnız GERÇEK kırılmada yükselir. Sıra: ÖNCE yeni istemci yayınlanır, SONRA minVersion'lı backend. | ÇEKİRDEK | yasak | `B:undated__istemci-surum-politikasi-panel-tablet-uyumu`, `B:undated__tetik-su-degisiklikleri-yaparken-dur-ve` | Teks-Erp/scripts/test_client_policy.ts (kayıt def… |
| deploy-kurulum | HER `DateTime` timestamptz: yeni alan `@db.Timestamptz` ZORUNLU, UUID kolon `@db.Uuid`. `@prisma/adapter-pg` OTURUMUN UTC OLDUĞUNU VARSAYAR → `PG_SESSION_OPTIONS` ('-c timezone=UTC') SÜS DEĞİL, SİLİNMEZ; `new Pool(` kuran her dosya geçirir (yoksa her tarih +3 saat kayar). `@db.Date` artık DÖRT alan. | ÇEKİRDEK | değişmez | `B:2026-08-01__sema-tip-konvansiyonu-o-11-2026` | Teks-Erp/scripts/test_timestamptz_contract.ts (TE… |
| fason | Fason kabulünde TAM kabul topu `SUBCONTRACTOR_CONSUMED` yapar, makbuzdan `parentReceiptId`li yeni açık-kumaş toplar doğar (`entrySource=SUBCONTRACTOR_RETURN`); KISMİ kabulde top TÜKETİLMEZ — `AT_SUBCONTRACTOR` kalır, `currentQty` atomik decrement ile kalana iner. Her teslimat AYRI makbuz. | ÇEKİRDEK | değişmez | `R:2026-08-19__2026-08-19-fason-kismi-kabul`, `A:2026-07-13__2026-07-13-her-rota-final` | scripts/test_fason_partial_receive.ts P1/P5 |
| fason | Fason rotanın SON adımıysa kabulde doğan açık-kumaş toplar FİNAL üründür: `WAREHOUSE` + tx içinde sıralı barkod (tek rezervasyon, Promise.all YASAK) + `form=ACIK`. Ara adımsa `IN_PRODUCTION` + barkodsuz; kesin ölçüm/etiket sonraki İÇ istasyonun FINISH'inde damgalanır. | ÇEKİRDEK | değişmez | `A:2026-07-13__2026-07-13-her-rota-final` | yok |
| fason | Defter `SubcontractorReceiptItem.receivedQty`+`isPartial` (NULL = eski satır, tamamı). Sevk kalemi YALNIZ TAM (`isPartial=false`, iptal edilmemiş) makbuz satırıyla dönmüş sayılır; tüm outstanding filtreleri bu yüklemi okur — atlanırsa kısmen dönmüş sevk kapalı görünür, ikinci teslimat firma çözemez. | ÇEKİRDEK | değişmez | `R:2026-08-19__2026-08-19-fason-kismi-kabul` | scripts/test_fason_partial_receive.ts + scripts/t… |
| fason | Fason 'açık + outstanding sevk' koşulunun TEK KAYNAĞI `helpers/fason-open-dispatch.helper.ts` (`OPEN_OUTSTANDING`·`OUTSTANDING_ITEM`·`outstandingItemOfOpenDispatch`); elle kopya YASAK — eski kopyalar `directShippedAt`/`receipt.cancelledAt` süzgecini taşımıyordu (WO sonsuza dek gizli kalıyordu). | ÇEKİRDEK | yasak | `R:2026-08-21__2026-08-21-aksam-tutarlilik-taramasi` | scripts/test_fason_open_dispatch_single_source.ts… |
| fason | Fason replay kimliği `SubcontractorReceipt.clientToken @unique`; iptal edilmiş makbuzun token'ı 409 `RECEIPT_CANCELLED`. Küme-eşitliği guard'ı ('aynı top kümesi → cached makbuz') yalnız tam-tüketimli makbuzda; kalemi `isPartial` olan makbuz cached DÖNEMEZ — aynı top meşru olarak tekrar gelir. | ÇEKİRDEK | değişmez | `R:2026-08-19__2026-08-19-fason-kismi-kabul` | scripts/test_fason_partial_receive.ts P3 |
| fason | Parti: ilk teslimat giden partiyi SÜRDÜRÜR; aynı sevkin ikinci+ teslimatında doğan toplar YENİ parti alır (`createBatchTx`, `splitFromId` = kaynak parti — K5 kalan-böl kuralının dönüş aynası). Tespit sevk-kapsamlı ve kendi makbuz satırı yazılmadan ÖNCE sayılır (sıra load-bearing). | ÇEKİRDEK | değişmez | `R:2026-08-19__2026-08-19-fason-kismi-kabul` | scripts/test_fason_partial_receive.ts P2 |
| fason | Makbuz iptali LIFO: aynı topa dokunan daha YENİ aktif makbuz varken eski iptal edilemez (409 `RECEIPT_NOT_LATEST`, önizleme `laterReceipts`). Geri sarma iki dallı — TAM kalem statüyü geri çeker, KISMİ kalem metrajı atomik increment ile GERİ KOYAR (kalan-kapama araya girdiyse 409). | ÇEKİRDEK | değişmez | `R:2026-08-19__2026-08-19-fason-kismi-kabul` | scripts/test_fason_partial_receive.ts P5 |
| fason | Çekme hata değil ölçüm: kabulde `bornTotal − consumedTotal` farkı (eşik 0.01 m) RollVariance'a yazılır — source `SUBCONTRACTOR_RETURN`, eksi SCRAP sistem sebebi `FASON_CEKME`, artı OVERAGE sebepsiz; çok toplu kabulde `allocateShrink` orantılı + artık son satıra. Kısmi kabulde de yazılır. | ÇEKİRDEK | değişmez | `R:2026-08-21__2026-08-21-fason-kabulu-cekme` | scripts/test_fason_partial_receive.ts P9 + script… |
| fason | WO MANUEL KAPATMA (`completeWorkOrder`) hard-block'u yalnız FASON: `AT_SUBCONTRACTOR`/`RETURNED_FROM_SUBCONTRACTOR` top ya da açık fason sevkine bağlı IN_PRODUCTION top → 409; fason dönüşü top STOCK'a çekilemez (GUARD 3). İPTAL yolu farklı: sert engel 2026-08-17'de kalktı, karar SORULUR. | ÇEKİRDEK | değişmez | `R:2026-07-30__wo-kapatma-kapanis-dispozisyonu-2026-07` | yok |
| fason | GİRİŞ NOKTASI: iş emrine AŞAĞIDAN katılan top (fason dönüşü çocuğu — fason adımının ÇIKTISI; elle eklenen) yukarıdaki adımları BEKLETEMEZ. Giriş noktası = bu WO'daki EN ERKEN hareketin `stepSequence`'i; küçükse bekleyen sayılmaz; çözülemezse bekleyen say. Bekçi §20 AYNI kuralı söyler. | ÇEKİRDEK | değişmez | `R:2026-08-04__2026-08-04-giris-noktasi-kurali` | scripts/test_consistency.ts §20 + scripts/test_fa… |
| fason | Kartela fason ÜRETİM fasonundan AYRI ve WO'suz: bitmiş top `KartelaDispatch` ile çıkar (`AT_KARTELA`), `KartelaReceipt` ile N `Swatch` döner, parent `KARTELA_CONSUMED`. `KartelaDispatch`'te `clientToken` YOK; idempotency `SwatchStockReduction.clientToken`'da. Önce `docs/design/KARTELA-TASARIM.md`. | ÇEKİRDEK | değişmez | `R:undated__kartela-fason-swatch` | yok |
| fason | Fason DOĞRUDAN SEVK `shipping.orderRequirement` KAPSAMINDADIR (`assertOrderLinkAllowed(resolveOrderRequirement())`); `block` rejiminin kaçışı `orderless` beyanı. Kapı takarken 'bu malın çıktığı BAŞKA yol var mı' sorusu KARDEŞ bayrağın (`invoiceMode`) kapsamına bakılarak sorulur. | ÇEKİRDEK | değişmez | `R:2026-09-03__2026-09-03-dilim-2-davranis` | scripts/test_shipping_flags.ts |
| fason | Rota kapsaması ('hedef var ama rotada veren adım yok') REDDETMEZ, UYARIR — create · replace · 'Rengi Değiştir' tek kural, yanıt `ApiResponse.warnings`. Boyahanesiz rota dışarıdan boyalı mal için DOĞRUDUR. `goods` muafiyeti nitelik topların HEPSİNDE varsa (`some` YASAK). | ÇEKİRDEK | karar | `A:2026-08-27__2026-08-27-siparis-baglarsam-hata`, `R:2026-08-02__ozellik-istasyon-kisiti-kalir-ama-bos` | scripts/test_wo_route_coverage_goods.ts |
| filtre-liste-arama | CSV de bir string'dir: filtreyi ELLE okuyan her servis id filtresini `readIdCondition`/`readFilterList`'ten geçirir (`typeof v==='string'` ham geçirir). Üç arıza modu: uuid kolon→P2007/400 · uuid olmayan string→sessiz 0 satır (foldType: yazımda normalizeFoldType) · ön-süzgeçli (currentStationId regex)→filtre SESSİZCE … | ÇEKİRDEK | değişmez | `R:2026-08-06__2026-08-06-filtrelerde-coklu-secim`, `R:2026-08-12__2026-08-12-giris-izlenebilirligi-kim`, `R:2026-08-04__2026-08-04-roll-foldtype-entryreason` | test_filter_multi_select.ts (§2b createdById/entr… |
| filtre-liste-arama | Cursor'lu (sonsuz kaydırmalı) listede süzme SUNUCUDA yapılır — istemci süzmesi yalnız o anki sayfayı süzer ve operatöre yanlış 'kayıt yok' gösterir (kayıt sonraki sayfadadır). Taşınabilir çekirdek; ekran çifti profil. | ÇEKİRDEK | değişmez | `R:2026-08-12__2026-08-12-top-listesi-filtresi`, `R:2026-08-12__2026-08-12-giris-izlenebilirligi-kim` | test_tambur_recent_output_filter.ts (kumaş filtre… |
| filtre-liste-arama | Liste + cursor + özet şeridi TEK where'den doğar: `BaseService.buildListWhere` (safeFilters İÇERİDE; envanter `buildRollWhere` emsali) — stats ucu ayrı where kurarsa üst satırdaki sayı tabloyla çelişir. Sipariş şeridinde ADET listenin aynası, METRAJ iptalleri HER ZAMAN dışlar (bilinçli iki kapsam). | ÇEKİRDEK | değişmez | `A:2026-08-27__2026-08-27-siparis-gorunurlugu-serit` | test_order_stats.ts (33; şerit ↔ withTotal her fi… |
| filtre-liste-arama | Açık talep ('hangi kalem hâlâ talep') süzgeci TEK KAYNAK `helpers/order-line-scope.helper.ts` (ACTIVE_LINE ↔ isActiveLine boğaz-ikiz); elle `cancelledAt: null` kopyası AST bekçili YASAK; ham SQL muafiyeti `-- aktif-kalem-muaf:`. GELECEK sorusu süzer, GEÇMİŞ (defter/tarihçe) sorusu süzmez. | ÇEKİRDEK | değişmez | `A:2026-08-27__2026-08-27-siparis-gorunurlugu-serit` | test_order_line_scope_single_source.ts (AST + ham… |
| filtre-liste-arama | Topun bir statüye GELİŞ anı `createdAt` DEĞİLDİR (kurtarma/dispozisyon/fason kabulü/finalize eski topu bugün taşır). Giriş sekmeleri (Ham Stok, Yarı Mamul) `createdAt`, diğer sekmeler `updatedAt desc` (`rollTabDefaultSortBy`; index @@index([status, updatedAt])). Yeni 'topu şu statüye çek' yolu sıralama sözleşmesini de… | ÇEKİRDEK | değişmez | `R:2026-07-30__buraya-gelis-olusturma-2026-07-30` | Electron service.test.ts:159-161 (RAW_STOCK/SEMI_… |
| filtre-liste-arama | Filtre/lookup/bayrak sözleşmesi değişince deploy sırası PAZARLIK DIŞI: backend ÖNCE, panel/APK sonra (eski panel CSV göndermez; yeni panel + eski backend = her çoklu lookup'ta 400 P2007, yeni APK + eski backend = lookup 404 + Son Kayıtlar filtresiz). Aynı anda gitmesi gerekenler aynı pencerede. | ÇEKİRDEK | reçete | `R:2026-08-06__2026-08-06-filtrelerde-coklu-secim`, `R:2026-08-12__2026-08-12-giris-izlenebilirligi-kim`, `A:2026-08-27__2026-08-27-siparis-gorunurlugu-serit` | yok |
| filtre-liste-arama | Her modelde UUID PK + `createdAt`/`updatedAt`; M:N pivot ve append-only log tablolarında YALNIZ `createdAt` (SystemLog: 'updatedAt YOK ve OLMAMALI'). Mobil CLAUDE'daki 'her modelde' satırı bu istisnayı düşürür — tek yer kök. | ÇEKİRDEK | değişmez | `R:undated__uuid-primary-key-tum-modellerde-m`, `M:undated__uuid-primary-key-her-modelde` | yok |
| finans-saglamlik | Sevk rakamı HER yüzeyde BRÜT: fiş/irsaliye donmuş snapshot'tan; liste (`attachTotals`), muhasebe Excel'i ve detay canlı + iptal edilmemiş `RollReturn` geri-eklemesi (snapshot OKUNMAZ); kg geri-ekleme İSTEMEZ; iade AYRI belgeyle kapanır, çıkış belgesi düzeltilmez. | ÇEKİRDEK | değişmez | `R:2026-08-02__2026-08-02-brut-kurali-liste`, `A:2026-08-02__2026-08-02-sevk-rakami-brut` | test_shipment_list_gross.ts · test_dispatch_repor… |
| finans-saglamlik | `Shipment/DirectShipment.invoiceNo+invoicedAt+invoicedById` DIŞ muhasebe belgesinin izidir (tutar/KDV yok); yalnız DISPATCHED işaretlenir (atomik claim); `invoiceNo:null` izi VE tarihi birlikte temizler; storno kapısı yalnız `invoiceNo`ya bakar → iç fatura onayı damgayı HER modda yazar. | ÇEKİRDEK | değişmez | `R:2026-08-02__2026-08-02-brut-kurali-liste` | test_shipment_invoice.ts |
| finans-saglamlik | SINIF 1 — İki tarih: yeni mali belge tipi eklerken sor 'kâğıdın tarihi = işlem tarihimiz mi?'; değilse İKİ alan (`Cheque.issueDate` ↔ `postingDate`); defter/kilit/belge-no/kur DÖRDÜ `postingDate`ten. Payment/Invoice tek tarihli MEŞRU — çift tarihi her tabloya yaymak reddedildi. | ÇEKİRDEK | reçete | `R:2026-08-14__bes-saglamlik-sinifi-2026-08-14` | postingDate okuyan bekçiler: test_cheque_portfoli… |
| finans-saglamlik | SINIF 2 — Ters yol: deftere yazan her ileri kaynak tipinin tipli `*_CANCEL` ters yolu olmalı (`reversesTxnId` self-FK @unique); enum'a ileri değer eklerken ters değeri de ekle. Ters kayıt DAİMA bugüne (`txnDate = now`) yazılır — kapanmış dönem fotoğrafı değişmez, dönem guard'ına takılmaz. | ÇEKİRDEK | değişmez | `R:2026-08-14__bes-saglamlik-sinifi-2026-08-14` | test_finance_opening.ts (reversesTxnId) · test_db… |
| finans-saglamlik | SINIF 3 — Kilit sırası: tek tx'te birden çok advisory/satır kilidi DETERMİNİSTİK sırada (`assertPeriodsOpenTx` anahtara göre; tekil guard'ı bir tx'te İKİ KEZ çağırmak YASAK — canlı deadlock); uzaylar arası uzay no ARTAN; envanter `period-guard.helper.ts` başlığı; PG 40P01/40001 → 409. | ÇEKİRDEK | değişmez | `R:2026-08-14__bes-saglamlik-sinifi-2026-08-14` | error.middleware TRANSIENT_SQLSTATES (kod); adlı … |
| finans-saglamlik | SINIF 4 — Çift yüklem: durum ↔ sayaç çifti olan modelde İKİ yazar da karşı tarafın koşulunu kendi atomik WHERE'ine koyar (`claimTx allocatedTotal:0` ↔ `bumpChequeAllocated status NOT IN no-money`) + DB CHECK seddi (`cheques_terminal_not_allocated`). Tek yönlü CAS YASAK — yarışta sessiz tutarsızlık. | ÇEKİRDEK | değişmez | `R:2026-08-14__bes-saglamlik-sinifi-2026-08-14` | test_payment_allocation.ts · test_db_invariants.ts |
| finans-saglamlik | SINIF 5 — Tek kaynak satır: çok-tipli çocukları olan belge satırları TEK assembler'dan verilir (`assembleReceiptLines` FABRIC\|YARN; `_shipped.ts`; `producedOutputWhere`); tüketici tabloya doğrudan gitmez — beşinci tüketici yazıldığı gün sınıf yeniden açılır. | ÇEKİRDEK | değişmez | `R:2026-08-14__bes-saglamlik-sinifi-2026-08-14` | test_goods_receipt.ts (assembleReceiptLines); AST… |
| finans-saglamlik | Olay defterinde kronoloji `createdAt`'tir, `eventDate` DEĞİL — eventDate kullanıcı girdisidir ve geriye tarihlenebilir; 'en son olay' araması `createdAt desc` ile yapılır (cheque cancelCollect: yanlış hesaptan geri çekme canlı ölçüldü). Append-only defterde yazım sırası = gerçek kronoloji. | ÇEKİRDEK | yasak | `R:2026-08-14__bes-saglamlik-sinifi-2026-08-14` | çek bekçisi §19n (cheque.service.ts:1049 yorumu; … |
| finans-saglamlik | TR-only BİLİNÇLİ (2026-08-15 kullanıcı kararı): arayüz ve backend mesajları Türkçe; i18n/sözlük altyapısı KURULMAZ. Yabancı müşteri gerçekten doğarsa yalnız ticaret yüzeyleri (cari/fatura/çek/kasa/mal kabul) sınırlı sözlüğe alınır — o gün planlanır, bugünden hazırlık yapılmaz. | ÇEKİRDEK | yasak | `R:2026-08-15__tr-only-bilinclidir-2026-08-15` | yok |
| finans-saglamlik | Ortak Konvansiyonlar bölümü (UUID/zaman damgası, soft delete, audit, beş sağlamlık sınıfı, yıkıcı işlemde detaylı onay) ÇEKİRDEK'tir — bayrakla açılıp kapanmaz; 'bizde böyle olmasın' talebinin cevabı bayrak değil süreç/eğitimdir (red gerekçesi MODUL-BAYRAK-TASARIM §11). | ÇEKİRDEK | değişmez | `R:undated__ortak-konvansiyonlar` | yok |
| is-emri-siparis-bag | Bir iş emri N sipariş satırına bağlanabilir ya da hiçbirine (STOCK_PRODUCTION). `WorkOrder.type` beyan DEĞİL bağın aynasıdır: ilk bağda STOK→ORDER, son bağ kalkınca ORDER→STOK; create/replace/quickStart da gövdedeki satırdan türetir ("STOK + satır" sunucuda ORDER — istemci disiplinine bırakılmaz). | ÇEKİRDEK | değişmez | `R:undated__is-emri-esnekligi`, `R:2026-08-21__2026-08-21-is-emri-tipi`, `A:2026-08-19__2026-08-19-tambur-paketi-2` | Teks-Erp/scripts/test_workorder_order_link.ts (§2… |
| is-emri-siparis-bag | Bağ işlemlerine (bağla/kaldır/toplara uygula) KAPALI statüler yalnız CANCELLED+SUPERSEDED — COMPLETED iş emrine uyumlu 'Sipariş Bağla' AÇIK. Plan değişikliği (renk/en/uyumsuz-bağ override) `PLAN_CHANGE_FROZEN_STATUSES` ile COMPLETED'da da kapalı (409 WO_PLAN_FROZEN). İki liste ayrıdır, birleştirme. | ÇEKİRDEK | değişmez | `R:2026-08-21__2026-08-21-uretim-rengi-tek`, `R:2026-08-21__2026-08-21-is-emri-tipi` | test_wo_target_color_guard.ts (59) + test_workord… |
| is-emri-siparis-bag | İKİ BAĞ YOLU, İKİ SÖZLEŞME: create/replace/quickStart gövdesindeki sipariş satırından hedef kumaş/renk/özellik TÜRETİLİR (`onlyColorId`); sonradan `POST /:id/order-links` HİÇBİR ŞEY MİRAS ALMAZ — kumaş/renk uyuşmazsa 400, en farkı `warnings`; uyumsuz bağ yalnız süpervizör `order-links/override`. | ÇEKİRDEK | değişmez | `R:2026-08-27__2026-08-27-rota-kapsamasi-hedef`, `R:2026-08-21__2026-08-21-uretim-rengi-tek`, `R:2026-08-21__2026-08-21-is-emri-tipi` | test_workorder_order_link.ts (2026-08-17 'miras a… |
| is-emri-siparis-bag | Rota kapsaması REDDETMEZ, UYARIR: create · replace · 'Rengi Değiştir' tek kural `collectRouteCoverageWarnings` → `ApiResponse.warnings`; uyarı NE eksik + SONUÇ der. Goods muafiyeti `every` (`some` kapıyı sessizce açar); malsız muafiyet yok, hedef renk silinmez. | ÇEKİRDEK | karar | `R:2026-08-27__2026-08-27-rota-kapsamasi-hedef`, `R:2026-08-21__2026-08-21-uretim-rengi-tek` | test_wo_route_coverage_goods.ts (7, iki negatif s… |
| is-emri-siparis-bag | Sipariş: liste/cursor/özet TEK where (`BaseService.buildListWhere`); kalem iptali SOFT, `recomputeOrderStatus` iptal kalemi `shipped` ile sayar; 'açık talep' süzgeci TEK KAYNAK `order-line-scope.helper` (`cancelledAt == null` GEVŞEK); karşılanma raporu `getCoverageForLines` KULLANMAZ (çift sayım). | ÇEKİRDEK | değişmez | `R:2026-08-27__2026-08-27-siparis-gorunurlugu-ozet` | test_order_line_scope_single_source.ts (AST) |
| is-emri-siparis-bag | Yarı mamul ARZDIR: Ürün Dengesi `malzemeAcigi` ham+yariMamul'ü BİRLİKTE düşer, ayrım yalnız gösterimde; sipariş karşılamada `netGap` DEĞİŞMEZ, `freeSemiFinished` ayrı alan (Electron `?? 0`, backend ÖNCE). `STOCK` statüdür, sekmeyi `entrySource` belirler; `RAW_STOCK` kapsamı BİLEREK geniş. | ÇEKİRDEK | değişmez | `R:2026-08-27__2026-08-27-3-tur-yari`, `A:2026-08-26__2026-08-26-yari-mamul-filtre` | test_semi_finished_surfaces.ts (10; §4 toplam kor… |
| is-emri-siparis-bag | GİRİŞ NOKTASI: `recomputeStepStatus`ta top, iş emrine GİRDİĞİ adımdan ÖNCEKİ adımlar için bekleyen DEĞİLDİR (giriş noktası = bu WO'daki en erken hareketin `stepSequence`i); çözülemezse bekleyen say (erken COMPLETED geç olandan kötü). Bekçi §20 SQL ürün kodunun aynasıdır — biri değişince diğeri de. | ÇEKİRDEK | değişmez | `A:2026-08-04__2026-08-04-giris-noktasi-kurali`, `B:undated__3-ayda-bir` | test_consistency §20 + test_fason_wrong_station_g… |
| is-emri-siparis-bag | Depo/ham/yarı mamul top yeni iş emrine YALNIZ `quick-start` ile girer; kabul listesi `STOCK/WAREHOUSE/A1_STOCK` backend tek kaynak (`quickStart.attachable`), mobil `ATTACHABLE_STATUSES` BİREBİR. Mevcut iş emrine top EKLEME YOK (`attach-rolls` 2026-06-12'de kaldırıldı) — rework ayrı emirdir. | ÇEKİRDEK | değişmez | `A:2026-08-25__2026-08-25-bitmis-kumas-tekrar`, `A:2026-08-25__2026-08-25-aksam-mobil-yeniden`, `A:2026-08-26__2026-08-26-yari-mamul-filtre` | scanClassify.test.ts (12) · ReworkRollsDialog.tes… |
| is-emri-siparis-bag | Bir yeteneğin 'VAR' sayılması için üçü birden gerekir: motor + en az bir çıkış yüzeyi + izin ataması. 'Backend destekliyordu ama hiçbir istemci kullanamıyordu' (rework: masaüstünde ekran yok, tablette `status !== 'STOCK'` elemesi) — 2026-08-25 dersi; ölçüm 980 topun 4'ü, dördü ham. | ÇEKİRDEK | karar | `A:2026-08-25__2026-08-25-bitmis-kumas-tekrar`, `A:2026-08-25__2026-08-25-aksam-mobil-yeniden` | yok |
| is-emri-siparis-bag | Refakat kartı iş emriyle DOĞAR (WO = tek kart, `TravelerCard.workOrderId @unique`, karekod = İş Emri No; parti kart üretmez). Kart `type` + sipariş bloğu basar → bağ ekleme, bağ kaldırma ve sipariş iptali `markTravelerCardDirtyTx` ile kartı bayat işaretler. | ÇEKİRDEK | değişmez | `R:2026-07-14__refakat-karti-traveler-card`, `R:2026-08-21__2026-08-21-is-emri-tipi` | yok |
| is-emri-siparis-bag | WO kapsamı yalnız üretim; tartı/paket/sevkiyat ayrı domain (`shipping.service.ts`) ve depoya/çuvala bağlanır. Top→sipariş bağı YOKTUR — sipariş karşılama `SackAllocation` ile sevk ANINDA yazılır, stok yalnız DISPATCH'te `SHIPPED` düşer (PLANNED tahsis sayılmaz). | ÇEKİRDEK | değişmez | `R:undated__wo-kapsami-sadece-uretim`, `R:2026-07__cuval-depo-modeli-2026-07`, `A:undated__not-tarihsiz` | yok |
| is-emri-siparis-bag | Manuel taşıma ('Konumu Düzelt'): CANCELLED/SUPERSEDED iş emrinde 409 (`manualMoveWoBlockReason` tek kaynak, önizlemede `woBlocked`); doğru yol topu yeni iş emrine almak — iptalde toplar STOCK'a çekildiği için `quick-start` alır. Fason adımına taşıma `AT_SUBCONTRACTOR` yapmaz; çıkış Fason Sevk'ten. | ÇEKİRDEK | değişmez | `R:2026-07-30__manuel-tasima-saha-surekliligi-2026-07`, `A:2026-08-25__2026-08-25-bitmis-kumas-tekrar` | yok |
| is-emri-siparis-bag | Şema uzun vadeli doğruluk için tasarlanır; seed satırlarıyla geriye uyum ARANMAZ (seed yalnız ilk kurulumda koşar). Bu canlı veriye dokunma izni DEĞİLDİR — seed fixture'ı serbestçe değişir, fabrikanın topları/siparişleri korunur. | ÇEKİRDEK | karar | `R:undated__sema-tasariminda` | yok |
| is-emri-siparis-bag | Yıkıcı işlemde (iptal/sil/scrap) backend preview ucu döner, arayüz etkilenen HER kaydı (WO, top, sipariş) somut listeler ve per-record seçim sunar; 'X kayıt etkilenecek' soyut sayısı YETMEZ. | ÇEKİRDEK | değişmez | `R:undated__yikici-islemlerde-detayli-onay-zorunlu` | yok |
| kalite-istasyon-yetenek | 'Bu adım kalite kontrol yürütür mü' sorusunun TEK kaynağı `services/helpers/quality-station.helper.ts`; boğaz İKİZ — bellek-içi `stepCanApplyQuality(station)` + Prisma parçası `QUALITY_STATION_WHERE` AYNI kuralı söyler ve BİRLİKTE değişir. Yeni `kind === PROCESS_QC` karşılaştırması YAZILMAZ. | ÇEKİRDEK | değişmez | `R:2026-09-03__2026-09-03-kalite-istasyon-yetenegi`, `R:undated__kursun-qc2-tek-fiziksel-istasyon` | test_station_quality_capability §3 (ikiz eşdeğerl… |
| kalite-istasyon-yetenek | İstasyon yeteneği istasyonun KENDİ alanıdır: `Station.appliesColor` (default false) / `appliesProperty` (default true) / `appliesQuality` (default false); ortak yüklemler `stepCanApplyColor/Property(station, requiredCategory)` ve tek argümanlı `stepCanApplyQuality` — kategoride kalite karşılığı YOK (fason firma kalite… | ÇEKİRDEK | değişmez | `R:2026-08-10__2026-08-10-11-uretim-karakteristigi`, `R:2026-09-03__2026-09-03-kalite-istasyon-yetenegi` | test_route_step_targets · test_station_capability… |
| kalite-istasyon-yetenek | Yetenek bayrağı PLANLAMA sorusunu yanıtlar ('bu adım verebilir mi'); fason KABULDE rengin/özelliğin FİİLEN uygulandığı hâlâ o sevkin `requiredCategory` bayrağından okunur (`subcontractor.service`). İkisi BİRLEŞTİRİLMEZ: istasyon bayrağı 'yapabilir', kategori bayrağı 'yaptı' der. | ÇEKİRDEK | değişmez | `R:2026-08-10__2026-08-10-11-uretim-karakteristigi` | arşiv:319 'bekçi ayrımı kaynak taramasıyla kilitl… |
| kalite-istasyon-yetenek | İkinci KK istasyonu TANIMLANABİLİR (Faz A) ama Kurşun→KK→Tambur üçlü rota KURULMAZ: kurşun bypass uygunluğu hâlâ 'sonraki adım Tambur' TÜRÜNE bakar ve sessizce kapanır (Faz B R3'e kadar). | ÇEKİRDEK | yasak | `R:2026-09-03__2026-09-03-kalite-istasyon-yetenegi`, `A:2026-08-06__2026-08-06-dagitim-artik-isin` | yok (yalnız panel yorumu) |
| kalite-istasyon-yetenek | 'Kurşun ile KK2 aynı istasyonda' (`StationKind.PROCESS_QC`, tek `WorkOrderStep`) adnansahin'in TOPOLOJİSİDİR — sektörde KK ayrı istasyonda ya da başka istasyonla birlikte olabilir (tasarım karar #11, §5.1); yeni yüzey bu birlikteliğe yaslanmaz. | PROFİL | değişmez | `R:undated__kursun-qc2-tek-fiziksel-istasyon` | yok |
| kalite-istasyon-yetenek | Per-roll iz `RollOperation` ile `KURSUN_APPLIED` / `QC2_COMPLETED` olarak tutulur — her top kurşun görmez; kurşun bypass'ta bu satırlar YAZILMAZ ve RollError açılmaz (dijital iş ile kâğıt akışının farkı budur). | ÇEKİRDEK | değişmez | `R:undated__kursun-qc2-tek-fiziksel-istasyon` | test_kursun_bypass / test_qc2_idempotency (bölüm … |
| kalite-istasyon-yetenek | `RollError` Tambur kararıyla kapanır: `isProcessed = true` + `actionTaken = CUT\|NO_CUT`; redye/parti ayırmada Tambur DIŞINDA `NO_CUT` ile idari kapanış olabilir. Kapanma semantiği ÇEKİRDEK'tir. | ÇEKİRDEK | değişmez | `R:undated__hata-yasam-dongusu` | yok |
| kalite-istasyon-yetenek | Her rotanın SON adımı topu finalize eder (`finalizeRollsAtLastStep`) — Tambur özel DEĞİL: `currentStepId=null`, `form=ACIK` (Tambur çocukları TOP), barkodsuz açık kumaşa barkod (satılabilir 'F' / 'H'); `qualityGrade` NULLABLE, yalnız kalite istasyonları belirler; `PRODUCED` limbosu YOK (enum'da da yok); WO depo topunu… | ÇEKİRDEK | değişmez | `R:undated__uretim-akisi`, `R:2026-07-13__2026-07-13-her-rota-final` | test_finalize_last_step |
| kalite-istasyon-yetenek | Tambur = kesim/bölme + kalite kararı istasyonu, 'final ürün kapısı' DEĞİL; depo bir istasyon değil tartı/paket öncesi bekleme statüsü (`RollStatus.WAREHOUSE`); ham (renksiz) top kesiminde operatör parçayı `STOCK` (üretime devam) da seçebilir. | ÇEKİRDEK | değişmez | `R:undated__uretim-akisi` | yok |
| kalite-istasyon-yetenek | Kök 'Üretim Akışı' bloğu adnansahin'in ROTASIDIR (KK1→[fason]→Kurşun+KK2→Tambur→Depo→Çuval→Sevk), sistemin zorunlu akışı değil — her adım istasyon kataloğu + rota şablonundan kurulur; 'kumaş hazır gelir' bu fabrikanın cümlesi. Devere/çözgü/haşıl YENİ MİMARİ İSTEMEZ; dokuma diliminin tek tasarım işi 'top TEZGAHTAN doğa… | PROFİL | değişmez | `R:undated__uretim-akisi` | yok |
| kalite-istasyon-yetenek | Renk istasyon KISITI DEĞİL (reçetedir): rota adımının renk seçicisi TÜM aktif renk kataloğunu gösterir; `StationColor` deprecated — FİLTRE olarak okunmaz (DTO geriye-uyumu için station-capability.service hâlâ okur/yazar); 'renk veren adım var mı' tek yüklem `stepCanApplyColor` — ikinci renk filtresi EKLENMEZ. | ÇEKİRDEK | değişmez | `R:2026-08-02__renk-istasyon-bazli-kisit-degil-2026`, `R:2026-08-10__2026-08-10-11-uretim-karakteristigi` | test_helpers (kilit) + test_station_capability 8b |
| kalite-istasyon-yetenek | `StationProperty` gerçek proses kısıtıdır ve İKİ İŞ yapar (planlama filtresi + `copyStationCapabilitiesToRoll` otomatik uygulama); yeni `FabricProperty` `stationIds` ZORUNLU ve bağ AYNI insert'te (`nestedCreateFields`); update'te `stationIds` verilmezse bağa DOKUNULMAZ, boş dizi iki yolda da 400; create/replace kapsam… | ÇEKİRDEK | değişmez | `R:2026-08-02__ozellik-istasyon-kisiti-kalir-ama-bos` | test_property_station_binding |
| kalite-istasyon-yetenek | Fason dönüş: kabulde orijinal toplar `SUBCONTRACTOR_CONSUMED`; makbuzdan `parentReceiptId`'li YENİ açık-kumaş Roll doğar (`entrySource=SUBCONTRACTOR_RETURN`, barcode null); metraj ZORUNLU, ağırlık opsiyonel; kesin ölçüm sonraki istasyonun FINISH'inde damgalanır. | ÇEKİRDEK | değişmez | `R:undated__fason-donus` | test_fason_partial_receive (bölüm BELİRSİZ) |
| kesif-cihaz-ag | Backend'e seri port / donanım polling GİRMEZ. Gerçek makine verisi geldiğinde de fabrika LAN'ında AYRI bir toplayıcı ajan okur ve API'den basar (tablet gibi bir istemci); backend tek-process kalır. | ÇEKİRDEK | yasak | `R:undated__phase-1` | yok |
| kesif-cihaz-ag | Donanım sürücüsü İSTEMCİ katmanında yaşar (Electron main IPC · mobil HAL) ve simülasyon per-cihaz VERİ bayrağıdır (`PeripheralDevice.simulate`) — kodda gömülü 'gerçek donanım ASLA' kuralı DEĞİL. | ÇEKİRDEK | karar | `M:undated__kural`, `M:undated__donanim-veri`, `R:undated__phase-1` | yok |
| kesif-cihaz-ag | Donanım fail-closed: cihaz yok / bond edilemedi / okunamadı / değer ≤ 0 → NET Türkçe toast ve mutasyon HİÇ ÇAĞRILMAZ. Sessiz sahte değer yok; donanım/cihaz yoksa hata verilir, varsayılana sapılmaz. | ÇEKİRDEK | değişmez | `M:undated__fail-closed`, `M:undated__simulasyon__3` | mobil/src/hooks/useSackWeigh.test.tsx |
| kesif-cihaz-ag | Uydurulmuş değer canlı veriye GİRMEZ: istemci sahte değeri üretir ama `source:'SIMULATED'` olarak BEYAN eder, kararı backend verir. Toast'ta açık 'SİMÜLASYON' ibaresi şart. Elle giriş (`source:'MANUAL'`) korumadan muaftır — kantarsız/arızalı durumun kaçış yolu. | ÇEKİRDEK | değişmez | `M:2026-07-30__cihazda-yazma-2026-07-30` | Teks-Erp/scripts/test_sack_weigh_source.ts + mobi… |
| kesif-cihaz-ag | Keşifte BİR SATIR = BİR SUNUCU: tekilleştirme ölçütü adres değil `installationId` (`groupByInstallation`); diğer adresler grubun içinde durur, atılmaz. Sıra ÖNCE tekilleştir SONRA sırala (yoksa 'tek aday' kapısı çok adresli sunucuda hiç ateşlemez). | ÇEKİRDEK | değişmez | `R:2026-09-04__2026-09-04-kesif-bir-satir` | Electron/src/test/discovery-logic.test.ts + mobil… |
| kesif-cihaz-ag | Keşif kuralı TEK METİN olarak `>>> KEŞİF-İKİZ BAŞLANGIÇ` / `<<< KEŞİF-İKİZ SON` bloğunda yaşar; mobil Electron'u import EDEMEZ, bu yüzden iki bekçi bloğu BİREBİR kıyaslar (her koşucu kendi tarafından bakar, tek CI adımına bağlı kalınmaz). | ÇEKİRDEK | değişmez | `R:2026-09-04__2026-09-04-kesif-bir-satir`, `R:2026-09-04__2026-09-04-kesif-kademeli-port` | Electron/src/test/discovery-logic.test.ts + mobil… |
| kesif-cihaz-ag | Keşif portu KADEMELİ: ① mDNS (portu ilandan) → ② tarama yalnız varsayılan portta → ③ YALNIZ ①+② sıfır aday döndürdüyse yedek portlar sırayla, aday bulan İLK portta DURARAK. Sunucu bulunduysa genişleme HİÇ koşmaz (maliyet-sıfır kuralı). | ÇEKİRDEK | reçete | `R:2026-09-04__2026-09-04-kesif-kademeli-port` | Electron/src/test/discovery-logic.test.ts (44) + … |
| kesif-cihaz-ag | `DISCOVERY_PORTS = [4000, 5000, 3000, 8080]` TEK KAYNAKTIR ve `DISCOVERY_DEFAULT_PORT` ondan TÜRER. İkinci bir gerçek yazma: ayrışırsa mDNS çalışmaya devam eder, yalnız tarama yanlış porta bakar — saha tarifi 'bazen buluyor'. Liste KISA tutulur (maliyet host × port ile doğrusal). | ÇEKİRDEK | tuzak | `R:2026-09-04__2026-09-04-kesif-kademeli-port` | discovery-logic.test.ts negatif sondası ('varsayı… |
| kesif-cihaz-ag | YEDEK PORTTA KİMLİK ZORUNLU (`identityRequiredForPort`): `/health` `{"status":"UP"}` orada aday saymaya YETMEZ (Actuator aynısını basar; 8080'deki rastgele web sunucusu 'bulundu' görünürdü). Varsayılan portta kimliksiz adaya izin BİLİNÇLİDİR — yedek porta genişletme. | ÇEKİRDEK | değişmez | `R:2026-09-04__2026-09-04-kesif-kademeli-port` | discovery-logic.test.ts (yedek portta /health açı… |
| kesif-cihaz-ag | Yeni cihazın doğuş durumu `devicePairingRequired` bayrağından TÜRER: bayrak KAPALIYSA cihaz APPROVED doğar. `announce` koşulsuz PENDING yaratmaz; enum'a üçüncü (otomatik-onaylı) değer EKLENMEZ — ayrım audit olayında (`DEVICE_AUTO_APPROVED`) yaşar. | ÇEKİRDEK | değişmez | `R:2026-09-04__2026-09-04-cihaz-onay-kapisi` | Teks-Erp/scripts/test_device_pairing_flag.ts (18) |
| kesif-cihaz-ag | Bayrak sonradan AÇILIRSA otomatik onaylanmış cihazlar APPROVED KALIR (ileriye dönük kapı; retroaktif düşürme vardiyayı kilitler — gözden geçirme yolu `revoke`). Mevcut PENDING ya da revoke edilmiş cihaz ise TERFİ ETMEZ: public bir uç yönetici kararını geri alamaz. | ÇEKİRDEK | yasak | `R:2026-09-04__2026-09-04-cihaz-onay-kapisi` | test_device_pairing_flag.ts (mevcut PENDING terfi… |
| kesif-cihaz-ag | Onay kapısı kararını SUNUCU verir: `/devices/status` ve `/devices/announce` cevapları `pairingRequired` taşır, koşul `pairingRequired && status !== "APPROVED"`. Mobilde tek yüklem `navigation/pairingGate.ts`; `??` LOAD-BEARING (sunucu kararı bayat uç bayrağını EZER) — `\|\|`/`&&` yazma. | ÇEKİRDEK | tuzak | `R:2026-09-04__2026-09-04-cihaz-onay-kapisi` | mobil/src/navigation/pairingGate.test.ts (9; `??`… |
| kesif-cihaz-ag | Uzak/LAN ayrımı SOKETTEN çözülür (`req.socket.localPort`); `clientType` gövdeden gelir ve GÜVENLİK SINIRI DEĞİLDİR. Tünel dinleyicisi YALNIZ `127.0.0.1`e bağlanır — uzak portu (4001) `0.0.0.0`a açma, ayrım komple çöker. İki dinleyici İKİ PROCESS DEĞİLDİR. | ÇEKİRDEK | değişmez | `R:2026-09-01__2026-09-01-patron-modulu-fabrikaya` | Teks-Erp/scripts/test_remote_access_guard.ts:147-… |
| kesif-cihaz-ag | Uzak `/api` isteklerinde Cloudflare Access JWT ikinci katmandır ve FAIL-CLOSED'dır (politika panelden kalkarsa sessiz açık değil gürültülü arıza). JWKS önbelleğinde TTL TAZELİKTİR, geçerlilik değil: süre dolunca bayat anahtar döner + arka planda tazelenir; önbellek HİÇ dolmadıysa fail-closed KALIR. | ÇEKİRDEK | değişmez | `R:2026-09-01__2026-09-01-patron-modulu-fabrikaya` | Teks-Erp/scripts/test_remote_access_guard.ts |
| kesif-cihaz-ag | Uzakta PIN/kart/cihaz/keşif/swagger uçları 404 döner — 403 VERME, varlığı doğrulayıp keşfe davet eder. `quickPin` 6 hanedir, DÜZ METİN saklanır, sistem genelinde `@unique`tir ve tek başına kimlik sayılır — hiçbir yüzeyden sızdırılmaz. | ÇEKİRDEK | değişmez | `R:2026-09-01__2026-09-01-patron-modulu-fabrikaya` | test_remote_access_guard.ts |
| kesif-cihaz-ag | Yeni paket eklemeden ÖNCE onay alınır (hem Electron hem mobil Allowed Packages tabloları). ⚠️ İki tablo da ölçülebilir biçimde BAYAT — envanteri kanıt saymadan önce `package.json` ile karşılaştır. | ÇEKİRDEK | yasak | `E:undated__allowed-packages`, `M:undated__allowed-packages` | yok |
| kk1-idempotency-kuyruk | Kayıt-yaratan uçlar istemci `clientToken`'ı taşır; kolon `String? @unique @db.Uuid`. Kapsam ÜÇ MODEL DEĞİL 15: Roll/Order/WorkOrder + SubcontractorReceipt, SwatchStockReduction, Sack, Shipment, ImportRun, WarehouseTransfer, GoodsReceipt, Invoice, Payment, CashTransaction, Cheque, PurchaseOrder. | ÇEKİRDEK | değişmez | `R:2026-07-14__idempotency-2026-07-14`, `B:undated__78-model-35-enum-idempotency-katmani` | yok |
| kk1-idempotency-kuyruk | İstemci token'ı MANTIKSAL DENEME başına BİR KEZ üretir ve tekrar denemede AYNI token'ı gönderir; mutate çağrısı başına yeni token üretmek korumayı boşa düşürür. | ÇEKİRDEK | değişmez | `R:2026-07-14__idempotency-2026-07-14` | mobil/src/offline/entryAttempt.test.ts |
| kk1-idempotency-kuyruk | Token yalnız sonucu BELİRSİZ bırakan hatadan sonra YAPIŞIR (ağ hatası / zaman aşımı / 5xx — timeout 'yazılmadı' demek DEĞİLDİR). Kesin 4xx'te YAPIŞMAZ: yapışırsa aynı payload'ı sonsuza dek gönderen bir 'Tekrar Dene' döngüsü kurulur. | ÇEKİRDEK | değişmez | `R:2026-07-14__idempotency-2026-07-14` | mobil/src/offline/entryAttempt.test.ts |
| kk1-idempotency-kuyruk | KK1 mükerrer tuzağı tx İÇİNDE koşar ve tx'in İLK ifadesi `pg_advisory_xact_lock(8021, hashtext(anahtar))`'dır. SIRA LOAD-BEARING: kilit `findFirst`'ten ÖNCE (sonra alınan kilit hiçbir şey kazandırmaz — TOCTOU) ve `generateRollBarcode`'dan da ÖNCE (ters sıra ABBA deadlock). | ÇEKİRDEK | değişmez | `R:2026-08-05__2026-08-05-kk1-mukerrer-korumasi` | Teks-Erp/scripts/test_kk1_duplicate_guard.ts |
| kk1-idempotency-kuyruk | Advisory kilit uzayları AYRI tutulur: 8021 = KK1 mükerrer tuzağı, 8022 = parti no üreteci. Aynı uzayı paylaşan iki alt sistem birbirini sessizce serileştirir; yeni uzay alırken envantere bak. | ÇEKİRDEK | değişmez | `R:2026-08-05__2026-08-05-kk1-mukerrer-korumasi`, `R:2026-07-13__is-emri-no-parti-2026-07` | yok |
| kk1-idempotency-kuyruk | Mükerrer penceresi SUNUCU saatiyle değil operatörün bastığı anla (`Roll.clientEnteredAt`) ölçülür ve İKİ YÖNLÜ kurulur (since/until); damgası olmayan eski kayıt `createdAt`'e düşer. Tek yönlü `gte`, saati ileri kaymış cihazın satırlarını sonsuza dek ikiz gösterir. | ÇEKİRDEK | değişmez | `R:2026-08-05__2026-08-05-kk1-mukerrer-korumasi` | Teks-Erp/scripts/test_kk1_duplicate_guard.ts |
| kucuk-kumeler | Her CUD operasyonu `AuditService.log()` ile `SystemLog`a yazılır; tek istisna `UserPreference` (kişisel UI blob'u). | ÇEKİRDEK | değişmez | `R:undated__her-cud-operasyonu-tablosu-istisna-kisisel` | Teks-Erp/scripts/test_audit_depth.ts (kapsam kısm… |
| kucuk-kumeler | Audit best-effort'tur: yazım hatası isteği DÜŞÜRMEZ ve çağrı transaction DIŞINDA yapılır. Başarısızlık sayacı `/health`te DEĞİL `/api/admin/health`tedir (`auditWriteFailures`) — `/health`in alan kümesi DONDURULMUŞ, yeni operasyonel metrik `buildRichHealth`e eklenir. | ÇEKİRDEK | değişmez | `R:undated__her-cud-operasyonu-tablosu-istisna-kisisel` | yok |
| kucuk-kumeler | Validation hata mesajları Türkçe yazılır. | ÇEKİRDEK | değişmez | `R:undated__validation-hata-mesajlari-turkce` | yok |
| kucuk-kumeler | Uzak/LAN ayrımı SOKETTEN çözülür (`req.socket.localPort` === REMOTE_PORT); `clientType` gövdeden gelir ve GÜVENLİK SINIRI DEĞİLDİR. Soket okunamazsa istek LAN sayılır — fail-closed yönü budur (LAN kuralları zaten dar). | ÇEKİRDEK | değişmez | `R:2026-09-01__2026-09-01-patron-modulu-fabrikaya` | scripts/test_remote_access_guard.ts |
| kucuk-kumeler | Tünel dinleyicisi YALNIZ `127.0.0.1`e bağlanır; uzak port `0.0.0.0`a AÇILMAZ (açılırsa uzak/LAN ayrımı komple çöker, iki yönde de kural seti sessizce yanlış uygulanır) ve `HOST` env'i bu dinleyicide bilerek onurlandırılmaz. | ÇEKİRDEK | yasak | `R:2026-09-01__2026-09-01-patron-modulu-fabrikaya` | scripts/test_remote_access_guard.ts |
| kucuk-kumeler | Uzak `/api` isteğinde Cloudflare Access JWT ikinci katmandır ve FAIL-CLOSED'dır (politika panelden kalkarsa sessiz açık değil gürültülü arıza). JWKS önbelleğinde TTL TAZELİKTİR geçerlilik değil: bayat anahtar döner + arka planda tazelenir; önbellek HİÇ dolmadıysa fail-closed KALIR. | ÇEKİRDEK | değişmez | `R:2026-09-01__2026-09-01-patron-modulu-fabrikaya` | scripts/test_remote_access_guard.ts (Access JWT n… |
| kucuk-kumeler | Uzakta kapalı yollar 404 döner, 403 DEĞİL (403 'burada bir şey var' der, keşfe davet eder): login-card · login-quick-pin · mobile-users + /api/devices, /api/discovery, /api/mobile, /api-docs. Liste YOL bazlıdır; servis katmanında ikinci hat `assertNotRemote` vardır. | ÇEKİRDEK | yasak | `R:2026-09-01__2026-09-01-patron-modulu-fabrikaya` | scripts/test_remote_access_guard.ts |
| kucuk-kumeler | `users.quickPin` 6 hane, DÜZ METİN ve sistem genelinde `@unique` — PIN tek başına kimliği belirler. PIN/kart uçları internete açılmaz; PIN repoya, log'a, sürüm notuna ve audit diff'ine girmez. | ÇEKİRDEK | değişmez | `R:2026-09-01__2026-09-01-patron-modulu-fabrikaya` | scripts/test_remote_access_guard.ts (denylist son… |
| kucuk-kumeler | `CLIENT_IP_HEADER` app-wide OKUNMAZ — yalnız uzak istekte geçerlidir (`clientIpHeaderRemoteOnly`, `REMOTE_PORT`ten türer); `TRUST_PROXY` de app-wide AYARLANMAZ. Aksi hâlde LAN'daki biri `CF-Connecting-IP` uydurup giriş kilidini VE hız sınırını etkisizleştirir (her denemede farklı kova). | ÇEKİRDEK | yasak | `R:2026-09-01__2026-09-01-patron-modulu-fabrikaya` | scripts/test_remote_access_guard.ts (IP başlığı n… |
| kucuk-kumeler | İki dinleyici İKİ PROCESS DEĞİLDİR: presence Map'i, feature-flag cache'i ve zamanlayıcı bayrakları process-local'dir; aynı `app`i ikinci bir sokette dinletmek invariantı bozmaz — İKİNCİ BİR NODE SÜRECİ hâlâ YASAK. | ÇEKİRDEK | değişmez | `R:2026-09-01__2026-09-01-patron-modulu-fabrikaya` | yok |
| kucuk-kumeler | TOTP kurulumunun TEK yolu yöneticinin açtığı 15 dk'lık tek kullanımlık penceredir (TOFU REDDEDİLDİ: parola sızmışsa saldırgan 2FA'yı kendi telefonuna bağlar); ikinci faktör `issueToken`den ÖNCE koşar; üç hata üç ayrı statü (403/409/401) ve giriş kilidi yalnız 401'i sayar. | ÇEKİRDEK | değişmez | `R:2026-09-01__2026-09-01-patron-modulu-fabrikaya` | scripts/test_totp.ts (RFC 4226/6238 dış vektörler… |
| kursun-planlama-bypass | Dağıtımsız kapanışın kapsamı 'her dağıtılmamış adım' DEĞİL: bayrak AÇIK **ve** adım bypass'a UYGUN. Tek kapı `resolveUnassignedTamburClosure`, `assertKursunTabletMayWrite` ile AYNI kaynaktan beslenir — ayrışırsa 'tablet yazamıyor, Tambur da kapatamıyor' çıkmazı döner. Bayrak koşulu load-bearing. | ÇEKİRDEK | değişmez | `R:2026-08-06__2026-08-06-dagitim-on-kosul` | scripts/test_kursun_unassigned_close.ts (bayrak k… |
| kursun-planlama-bypass | Atıf UYDURULMAZ: dağıtımsız kapanışta `RollMovement.machineId` NULL kalır — varsayılan makineye yazmak makine bazlı hacim raporunu sistematik yanlışlar. Boşluk GÖRÜNÜR kılınır: `listDistribution.unassignedClosures` → amber bant ('son 7 günde N iş dağıtılmadan kapandı'). | ÇEKİRDEK | yasak | `R:2026-08-06__2026-08-06-dagitim-on-kosul` | scripts/test_kursun_unassigned_close.ts (atıf uyd… |
| modul-bayraklari-profiller | Her kural yazılırken önce sor: ÇEKİRDEK mi PROFİL mi? Ölçüt ve red gerekçesi MODUL-BAYRAK-TASARIM §0/§11, uygulama kuralları §12 bağlayıcı; Ortak Konvansiyonlar bölümünün tamamı ÇEKİRDEK'tir. | ÇEKİRDEK | değişmez | `R:undated__tekserp-monorepo-koku`, `R:undated__ortak-konvansiyonlar` | yok |
| modul-bayraklari-profiller | ÇEKİRDEK her kurulumda aynıdır, bayrakla açılıp kapanmaz: defter semantiği, brüt sevk, idempotency, kilit sırası, atomik claim, fail-closed kapı, sır hijyeni, veri bütünlüğü, UUID/soft delete/audit/beş sağlamlık sınıfı/yıkıcı onay. 'Bizde olmasın' talebinin cevabı bayrak değil süreç/eğitimdir. | ÇEKİRDEK | yasak | `R:undated__tekserp-monorepo-koku`, `R:undated__ortak-konvansiyonlar` | yok |
| modul-bayraklari-profiller | PROFİL bu fabrikanın seçimidir (rota, istasyon topolojisi, açık modüller, sayısal ayarlar). Kök CLAUDE.md'deki üretim akışı adnansahin'in ROTASIDIR, sistemin kısıtı değil; devere/çözgü/haşıl yeni mimari istemez — istasyon kataloğuna istasyon, rotaya adım eklenir. | PROFİL | değişmez | `R:undated__tekserp-monorepo-koku`, `R:undated__uretim-akisi` | yok |
| modul-bayraklari-profiller | PRODUCTION CANLI = HER canlı kurulum: `migrate reset`/reseed/toplu DELETE yasak; migration geri alınamaz (rollback = yedekten restore); toplu düzeltme script'i dry-run varsayılan, `--apply` öncesi kayıtları listeler; şema provası en eski canlı dump'ta (restore → deploy → bekçiler → profil boot). | ÇEKİRDEK | yasak | `R:undated__production-canli-gercek-fabrika-gercek-veri` | yok |
| modul-bayraklari-profiller | Modül anahtarını yalnız süperadmin yazar: `flagWriteGuard` modül dalı EN ÖNDE ve `.some` (belge dalı `.every`), SENKRON, `MODULE_FLAG_SUPERADMIN_ONLY`. Supap: hesap YOKSA `admin:settings` yeter + audit; bilinmiyor = VAR (fail-closed); supap dalı tembel DB tazeleme (`resolveSystemAccountLock`). | ÇEKİRDEK | değişmez | `R:2026-09-03__2026-09-03-superadmin-p2-satici`, `R:2026-09-02__2026-09-02-03-modul-anahtarlari` | test_superadmin §A-§M (7 anahtar × 4 senaryo, sup… |
| modul-bayraklari-profiller | Özellik Anahtarları'nda KAPALI modülün bayrak satırı fabrika yöneticisine HİÇ çizilmez (satış sınırı görünür — kullanıcı kararı); satır kalmazsa sekme düşer; satıcıda (supap dahil) hiçbir satır gizlenmez, kilit bandı satıcıya kalır. P5'in 'kilit ≠ gizleme'sinin BİLİNÇLİ tersi. | ÇEKİRDEK | karar | `R:2026-09-04__2026-09-04-cekirdek-kapali-modulun`, `R:2026-09-03__2026-09-03-panel-modul-kapilari` | flag-modules.test.ts (12) + SettingsSurfacePage.m… |
| modul-bayraklari-profiller | Yeni davranış bayrağının varsayılanı = BUGÜNKÜ davranış ve hiçbir profilde açık doğmaz; 'varsayılan = bugün' cümlesi ÖLÇÜLMEDEN yazılmaz (warn rejimi bayrak konmadan önce onarıldı: uyarı yok, `warnings` gösterilmiyor, `orderless` ölüydü). | ÇEKİRDEK | değişmez | `R:2026-09-03__2026-09-03-dilim-2-davranis` | test_shipping_flags + test_quality_batch_flags |
| modul-bayraklari-profiller | Çıkışsız kapı üreten bayrak (`block`, `manualWeightRestricted`) yazılır ama AÇILMAZ; önkoşul PANEL METNİNDE ve bekçi cümleyi metinden ölçer (ölü şerh koruması). Bayrak adı yaptığı işi söyler: `batch.requiredEnabled` reddedildi → `batch.autoCreateEnabled` (parti yaratan uç yok). | ÇEKİRDEK | reçete | `R:2026-09-03__2026-09-03-dilim-2-davranis` | test_shipping_flags (metin ölçümü) |
| modul-bayraklari-profiller | Kapı takarken 'malın çıktığı BAŞKA yol var mı' KARDEŞ bayrağın kapsamına bakarak sor: fason DOĞRUDAN SEVK `orderRequirement` kapsamında (`orderless` kaçışı). `gradeRequiredEnabled` DAR kapsam — fason kabulü, son-adım finalize, cutOpenFabric, attachRolls DIŞARIDA; genişletme = sıfır-fark ihlali. | ÇEKİRDEK | yasak | `R:2026-09-03__2026-09-03-dilim-2-davranis` | test_shipping_flags §2.12 + test_quality_batch_fl… |
| mukerrer-namefold | Ad mükerrerinin son hattı DB seddidir: `<tablo>_nameFold_key` UNIQUE. Ana veri BİRLEŞTİRMESİNE katılan tabloda partial (`WHERE "mergedIntoId" IS NULL` — mezar taşı aynı adı meşruen taşır), katılmayanda DÜZ UNIQUE. Sed uygulama guard'ının AYNASIDIR, ondan gevşek olamaz. | ÇEKİRDEK | değişmez | `R:2026-08-21__2026-08-21-namefold-db-seddi` | test_db_invariants PARTIAL_INDEXES + test_master_… |
| mukerrer-namefold | Renge DÜZ `nameFold` seddi KOYMA: renk mükerreri ayraç ve sayı-sırası bağımsızdır (`foldColorNameForCompare`), düz kısıt uygulama kuralından zayıf kalıp yanlış güven verir. Sed `tr_fold_color(name)` İFADESİ üzerinde partial UNIQUE'tir (`colors_nameFoldColor_key`). | ÇEKİRDEK | yasak | `R:2026-08-25__2026-08-25-prod-un-uc` | test_db_invariants EXPRESSION_UNIQUES (test_db_in… |
| mukerrer-namefold | Uygulama bekçisi `assertNameNotDuplicate` KALDIRILMAZ — Türkçe, kod bilgili 409'u ('zaten var' ↔ 'PASİF, aktifleştirin') o verir. Ama KİLİTSİZ check-then-act'tir (`create()` tx bile açmaz): yarışı, `duplicateNameField` unutulmasını, içe aktarım fail-open'ını ve elle SQL'i yalnız DB seddi kapatır. | ÇEKİRDEK | değişmez | `R:2026-08-21__2026-08-21-namefold-db-seddi` | test_master_data_name_dup §9/§10 |
| mukerrer-namefold | Enforce bekleyen kurulumda `test_db_invariants` §1 / EXPRESSION_UNIQUES satırının KIRMIZI kalması BİLİNÇLİDİR ('enforce bekliyor' sinyali, unutulmasın diye) — bekçiyi bu yüzden daraltma; ad mükerrerini temizlemek mükerrer panelinin ve iş kararının işidir, toplu UPDATE'in değil. | ÇEKİRDEK | karar | `R:2026-08-21__2026-08-21-namefold-db-seddi`, `R:2026-08-22__2026-08-22-mutabakat-kapisinin-kirmizisi` | test_db_invariants §1 |
| mukerrer-namefold | Kimlik alanına (VKN vb.) DB seddi BİLİNÇLİ KONULMAZ: aynı tüzel kişiye ikinci cari kart meşru bir iş kararı olabilir; kimlik alanı sektörde EŞLEŞTİRME SİNYALİDİR, tekillik kısıtı değil — aday kuyruğa düşer, yazma engellenmez. | ÇEKİRDEK | karar | `R:2026-08-22__2026-08-22-mukerrer-paneli-v2` | yok |
| mukerrer-namefold | Survivorship'te (P2) seçim DEĞER değil KAYIT üzerindendir (`fieldPicks[alan]=kayıtId`) — serbest metin uçu sınırsız alan düzenleme API'sine çevirirdi; `code` ve kimlik alanları seçilemez. ⚠️ SIRA LOAD-BEARING: survivor alan yazımı ATOMİK CLAIM'DEN SONRA, yoksa ad taşıma P2002 verir. | ÇEKİRDEK | değişmez | `R:2026-08-22__2026-08-22-mukerrer-paneli-v2` | test_merge_field_picks |
| mukerrer-namefold | Mükerrer TOP birleştirilmez, İPTAL edilir (`MUKERRER`): top işlem kaydıdır, birleştirmek metrajı toplamak olurdu. İptal topun KENDİ ucundan yapılır (etiket/çuval/sevk guard'ları atlanmasın), toplu iptal ucu BİLİNÇLİ YOK; 'asıl' = etiketi BASILAN top; tespit `duplicate-rolls.service`te. | ÇEKİRDEK | değişmez | `R:2026-08-22__2026-08-22-mukerrer-paneli-v2` | test_duplicate_rolls |
| parti-no-batch | Partinin KİMLİĞİ yalnız `Batch.id`'dir — hiçbir yerde `batchNumber` ile lookup YAPMA (findUnique/findFirst dahil). Numara benzersiz değildir; aynı numarayı yıllar içinde onlarca parti alır. | ÇEKİRDEK | yasak | `R:2026-07-13__is-emri-no-parti-2026-07` | yok (kimlik tarafı); kısıtın kalktığını scripts/t… |
| parti-no-batch | `batches.batchNumber` üzerinde `@unique` YOKTUR ve GERİ KONMAZ (migration 20260805120000_batch_short_number). Kısıt geri gelirse sarma ilk tekrarda 500 verir ve üretim durur. | ÇEKİRDEK | yasak | `R:2026-07-13__is-emri-no-parti-2026-07` | scripts/test_batch_number_format.ts §6 (pg_indexe… |
| parti-no-batch | `generateBatchNumberTx`'in İLK ifadesi `pg_advisory_xact_lock(8022, 1)` olmalı — okumalardan önce. Sonraya alınırsa TOCTOU açılır ve aynı gün doğan iki parti sessizce aynı kodu alır (hata yok, log yok). | ÇEKİRDEK | değişmez | `R:2026-07-13__is-emri-no-parti-2026-07` | scripts/test_batch_number_format.ts §2 ('Kilit: a… |
| parti-no-batch | Advisory uzay 8022 parti no üretecine aittir ve KK1 mükerrer guard'ının 8021'inden AYRI tutulur; iki alt sistem birbirini sessizce serileştirmemeli. Uzay envanteri `helpers/period-guard.helper.ts` başlığında. | ÇEKİRDEK | değişmez | `R:2026-07-13__is-emri-no-parti-2026-07` | scripts/test_batch_number_format.ts §2 (BATCH_NUM… |
| parti-no-batch | Parti no biçimi PROFİL kararıdır: `batch.shortNumberEnabled` AÇIK (varsayılan; kayıt yoksa true) → `P01…P99`, P99'dan sonra KÖRLEMESİNE `P01`'e sarar (numara canlı mı diye BAKILMAZ); KAPALI → `P+GGAAYY+günlük sıra`. | PROFİL | karar | `R:2026-07-13__is-emri-no-parti-2026-07` | scripts/test_batch_number_format.ts §1/§3 (iki re… |
| raporlar | Roll.finalizedAt/statusChangedAt damgalarını DB TRIGGER'ı yazar (roll_stamp_production_timestamps, BEFORE INSERT OR UPDATE) — uygulama koduna güvenilmez: Roll.status'e yazan 40+ nokta var, biri atlanırsa rapor SESSİZCE eksilir (hata yok, log yok). Damgayı elle yazma. | ÇEKİRDEK | değişmez | `R:2026-08-09__2026-08-09-rapor-temeli-finalizedat` | scripts/test_db_invariants.ts §6 + scripts/test_q… |
| raporlar | Damganın KAYNAK STATÜ LİSTESİ load-bearing: yalnız IN_PRODUCTION\|STOCK\|AT_SUBCONTRACTOR\|RETURNED_FROM_SUBCONTRACTOR → WAREHOUSE\|A1_STOCK\|SCRAP geçişi finalizedAt yazar. SHIPPED/CANCELLED BİLEREK dışarıda: storno aylar önceki topu bugünün karnesine sokar. | ÇEKİRDEK | yasak | `R:2026-08-09__2026-08-09-rapor-temeli-finalizedat` | scripts/test_quality_scorecard.ts (trigger'a SHIP… |
| raporlar | finalizedAt WRITE-ONCE DEĞİLDİR, üzerine yazılır: depo topu yeni iş emrine girip tekrar finalize olursa tazelenir. Invariant: finalizedAt DAİMA mevcut qualityGradeId ile AYNI olaydan gelir; sabitlenirse top ESKİ tarihle YENİ kaliteyi taşır. | ÇEKİRDEK | değişmez | `R:2026-08-09__2026-08-09-rapor-temeli-finalizedat` | yok |
| raporlar | 'Dönemde sevk edilen metraj' TEK tanım reports/_shipped.ts (brüt: canlı sevk + RollReturn geri-eklemesi + DirectShipment). İkinci tanım yazma — biri doğrudan sevkleri, diğeri iade geri-eklemesini unutur. Bugün 3 tüketici; beşincisinde sınıf yeniden açılır. | ÇEKİRDEK | değişmez | `R:2026-08-09__2026-08-09-rapor-temeli-finalizedat` | yok (kök Ortak Konvansiyonlar 'Tek kaynak satır k… |
| raporlar | Parti numarası BENZERSİZ DEĞİLDİR (P01…P99 döner) → parti araması DAİMA aday listesi döndürür ve izleme batchId ile yapılır; tek sonuç varsaymak aynı numaralı BAŞKA partinin müşterilerini göstermek olur. | ÇEKİRDEK | tuzak | `R:2026-08-09__2026-08-09-rapor-temeli-finalizedat` | scripts/test_batch_trace.ts (15 kontrol) |
| raporlar | Fabrika günü = Europe/Istanbul takvim günü, TEK KAYNAK src/constants/time.ts: literali hiçbir yere kopyalama — FACTORY_TIMEZONE + factoryDaySql/factoryDayStart/factoryDayKeyUtcMidnight/factoryYmd kullan. Çıplak DATE_TRUNC günü OTURUM (UTC) diliminde keser. | ÇEKİRDEK | yasak | `B:2026-08-01__fabrika-gunu-takvim-gunu-tek-kaynak` | scripts/test_report_day_boundary.ts (4. cephe: sr… |
| raporlar | İKİ SORUYU AYIR: TAKVİM GÜNÜ (günlük sayaç/grafik, GGAAYY) saat dilimine BAĞLIDIR → açık yaz; MUTLAK PENCERE (son 72 saat, yaşlandırma, termin, daysOpen/daysLate) iki an farkıdır, dilimden BAĞIMSIZ → dokunma, yalnız '-- tz-ok:' ile belirt. | ÇEKİRDEK | karar | `B:2026-08-01__fabrika-gunu-takvim-gunu-tek-kaynak` | scripts/test_report_day_boundary.ts |
| refakat-karti | Refakat kartı iş emri AÇILIŞINDA doğar; bir WO = TEK kart (`TravelerCard.workOrderId @unique`); `cardNumber = barcode = workOrderNumber` ve karekod versiyonlar arası SABİT; Parti (Batch) kart ÜRETMEZ. Kart okutulunca istasyon süreçleri tetiklenir. | ÇEKİRDEK | değişmez | `R:2026-07-14__refakat-karti-traveler-card` | DB seddi: schema @unique (cardNumber, barcode, wo… |
| refakat-karti | ACTIVE kartın önizlemesi+baskısı iş emrinin GÜNCEL hâlinden üretilir (plan CANLI); SUNUM (şablon/config/sayfa) HER kartta HER baskıda canlı — karta DONDURULMAZ; geçersiz kartın (VOIDED/COMPLETED/REPRINTED) yalnız İÇERİĞİ donuk kalır, hiç revize edilmez; iş emri okunamazsa eldeki snapshot'a düşülür. | ÇEKİRDEK | değişmez | `R:2026-08-05__refakat-karti-plan-canli-sunum-donmus`, `R:2026-08-03__refakat-karti-sablonu-uc-kademe-2026`, `R:2026-08-03__refakat-karti-a5-varsayilan-parti-blogu` | scripts/test_traveler_card_stale.ts §6 + scripts/… |
| refakat-karti | Önizleme, baskı ve sürüm numarası TEK karar noktası `resolvePrintPlan`'dan beslenir; önizlemedeki `v` BU BASKININ alacağı sürümdür, GET yan etkisizdir. Generic printed-document freeze/reissue/lazy-init TRAVELER_CARD için KAPALI (`SELF_MANAGED_DOC_TYPES` → 400) — ikinci sürüm üretici olurdu. | ÇEKİRDEK | değişmez | `R:2026-08-05__refakat-karti-plan-canli-sunum-donmus` | scripts/test_traveler_card_stale.ts §6 |
| refakat-karti | `print-event` = 'kart fiziksel olarak basıldı': istemci baskı BAŞARIYLA dönünce çağırır (yalnız KÂĞIT; PDF ve `GET /html` bildirmez); sunucu basılan planı snapshot'a yazar, `contentDirty` temizler, içerik değiştiyse `version++` (PRINT_REVISION) — aynı içerik sürüm şişirmez; bildirim hatası yutulur. | ÇEKİRDEK | değişmez | `R:2026-08-05__refakat-karti-plan-canli-sunum-donmus`, `R:2026-08-05__2026-08-05-parti-no-k`, `E:undated__yalniz-k-git-baskisinda` | scripts/test_traveler_card_stale.ts (30 check) |
| refakat-karti | `TravelerCard.contentDirty` tek yazıcı `markTravelerCardDirtyTx` (ACTIVE + contentDirty:false CAS; fazla işaretlemek GÜVENLİ, eksik HATA); K18 'ilk parti ataması bayraklanmaz' karta KOPYALANMAZ (kart parti doğmadan basılır); şablon düzenlemesi bayat İŞARETLEMEZ; `GET /html` TEMİZLEMEZ. | ÇEKİRDEK | değişmez | `R:2026-08-05__2026-08-05-parti-no-k`, `R:2026-08-05__refakat-karti-plan-canli-sunum-donmus` | scripts/test_traveler_card_stale.ts |
| refakat-karti | Partiler snapshot'a YAZILMAZ, baskıda `resolveLiveBatches` canlı çözer: `mergedIntoId:null`, `K18_DEAD_STATUSES` dışı, sevk `cancelledAt:null` (en yeni + `(+N)`), sıra `createdAt` — `orderBy batchNumber` YASAK (P01…P99 sarar). 'Sevk rakamı brüt/donmuş belge' kuralı karta UZANMAZ. | ÇEKİRDEK | değişmez | `R:2026-08-03__refakat-karti-a5-varsayilan-parti-blogu`, `R:2026-07-13__is-emri-no-parti-2026-07` | scripts/test_traveler_card_a5_batches.ts (36 chec… |
| refakat-karti | FAIL-CLOSED şablon çözümü: `templateId` verilmiş ama silinmiş/pasifse baskı 404 — yerleşiğe SAPMAZ (çuval etiketi emsali). İstisna DEĞİL: gövdesi boş RAW_HTML yerleşiğe düşer (şablon ÇÖZÜLMÜŞTÜR; alternatif boş kâğıt basmaktı). Bilinmeyen `{{anahtar}}` boş basar, baskıyı DURDURMAZ. | ÇEKİRDEK | değişmez | `R:2026-08-03__refakat-karti-sablonu-uc-kademe-2026` | scripts/test_traveler_template.ts |
| refakat-karti | Şablon güvenliği İKİ katman: ① `sanitizeTemplateHtml` (script/iframe/object/embed/link/meta/on*/javascript:/data:text/html) HEM kayıtta HEM render'da; aşırı kesme güvenli; dış `<img>`/`<a>` BİLEREK serbest ② değerler kaçırılır, yalnız QR SVG ham. Baskı iframe sandbox: `allow-scripts` YOK. | ÇEKİRDEK | değişmez | `R:2026-08-03__refakat-karti-sablonu-uc-kademe-2026` | scripts/test_traveler_template.ts |
| refakat-karti | `PATCH /api/feature-flags` guard'ı ANAHTAR-KAPSAMLI, FAIL-CLOSED: gövde YALNIZ `documentsConfig`/`travelerCardConfig` taşıyorsa dar izin yeter; yabancı anahtar ya da boş gövde → admin:settings. Düz OR'a ÇEVİRME; `companyName`/letterhead/logo kümeye EKLENMEZ (firma kimliği). | ÇEKİRDEK | yasak | `R:2026-08-05__belge-tasarimi-ayri-bir-yetkidir-sistem` | scripts/test_document_template_permission.ts (düz… |
| refakat-karti | İş emri belgeleri TEK uçtan `GET /api/work-orders/:id/documents` (kart + 3 PrintedDocType); istemci liste KURMAZ; liste izni ↔ baskı izni (`DOC_PERMISSIONS`) HİZALI — ayrışma = görünen satır + sessiz 403; iptal belge `cancelled:true` ile listede KALIR; sıra sunucudan (kart önce, tarih DESC). | ÇEKİRDEK | değişmez | `R:2026-08-05__2026-08-05-is-emri-belgeleri` | scripts/test_workorder_documents.ts (liste×izin +… |
| rota-renk-ozellik-kapsama | Yeni tarihli not CLAUDE.md'ye değil ARŞİVE (`docs/history/CLAUDE-NOT-ARSIVI.md`) yazılır; köke yalnız `[ÇEKİRDEK]`/`[PROFİL]` etiketli özet satırı eklenir; alana dokunmadan önce arşivdeki TAM not okunur. Ölçüt: MODUL-BAYRAK-TASARIM §11 'bayraklanmayacaklar' = ÇEKİRDEK. | ÇEKİRDEK | reçete | `R:undated__karar-notlari-dizini` | yok |
| rota-renk-ozellik-kapsama | 'Adım renk/özellik verebilir mi' TEK YÜKLEM `stepCanApplyColor`/`stepCanApplyProperty` (istasyon bayrağı VEYA adımın fason hizmeti). Rota hedefi, WO kapsama, renk kilidi, taşıma, split, kursunFinish, send-to-dye ONU kullanır; tekil `requiredCategory.appliesColor` / `kind===` yazımı YASAK. | ÇEKİRDEK | değişmez | `R:2026-08-10__2026-08-10-11-uretim-karakteristigi` | test_station_capability_flags.ts (kaynak taraması… |
| rota-renk-ozellik-kapsama | Özellik (`StationProperty`) GERÇEK proses kısıtıdır ve BOŞ DOĞAMAZ: `FabricProperty` create'te `stationIds` ZORUNLU, bağ aynı insert'te (`nestedCreateFields: stationCapabilities`); update'te `stationIds` verilirse replace, verilmezse DOKUNMA (ad düzenlemesi bağı silmesin); boş dizi iki yolda da 400. | ÇEKİRDEK | değişmez | `R:2026-08-02__ozellik-istasyon-kisiti-kalir-ama-bos` | test_property_station_binding.ts (§1-§9) |
| rota-renk-ozellik-kapsama | `StationProperty` İKİ İŞ yapar (planlama filtresi + `copyStationCapabilitiesToRoll` otomatik uygulama). Satırın VARLIĞI 'verebilir' der; `mode` AUTO\|OPTIONAL\|REQUIRED (varsayılan OPTIONAL) yalnız teyit biçimi — OPTIONAL kapsam dışı sayılmaz; kurşun bypass AUTO arar; SEÇİM özelliği AUTO olamaz. | ÇEKİRDEK | değişmez | `R:2026-08-10__2026-08-10-11-uretim-karakteristigi` | test_station_property_mode.ts (19) |
| rota-renk-ozellik-kapsama | SEÇİM (CHOICE) tipli özellik HEDEF OLAMAZ — iki katman: aktif seçim yapan uçlar `assertTargetablePropertyIds` (400, adıyla); mevcut listeyi geri yollayan echo uçları `partitionTargetableIds` (sessiz ayırma). İstemci `isTargetableProperty` yalnız konfordur; sözleşmeyi SUNUCU korur. | ÇEKİRDEK | yasak | `R:2026-08-10__2026-08-10-11-uretim-karakteristigi` | test_property_targetable.ts (22) |
| rota-renk-ozellik-kapsama | Hedef/Düzelt replace'leri yalnız FLAG satırlarına dokunur (`property: { valueType: "FLAG" }`); CHOICE satırı operatör verisidir, `propertyIds=[]` bile silmez. Değerli seçim `RollProperty.valueId` ile saklanır; kesim/finalize/undo/fason çocukları valueId'yi TAŞIR. | ÇEKİRDEK | değişmez | `R:2026-08-10__2026-08-10-11-uretim-karakteristigi` | test_property_value_selection.ts (27) |
| rota-renk-ozellik-kapsama | Rota şablonu adım başına HEDEF saklar (`RouteStep.plannedColorId` + `RouteStepProperty`); hedef ÖNERİDİR, kilit değil (`WorkOrder.targetColorId` ile karıştırma); adım tüketilirken kopyalanmaz — tek kopyalayan istemcinin 'rotayı uygula' adımı (`seedFromRoute`/`applyRouteTarget`/`chooseRoute`). | ÇEKİRDEK | değişmez | `R:2026-08-06__2026-08-06-rota-sablonu-hedef` | test_route_step_targets.ts (20) |
| rota-renk-ozellik-kapsama | Sipariş bağlı iş emrinde renk SİPARİŞİN şartıdır: hedef renk/özellik sipariş satırından TÜRER (`resolvedTargetColorId = onlyColorId`, `orderLineRequiredProperty` birleşimi); rota şablonu hedefi orada renge DOKUNMAZ. Sipariş rengi plan beyanı değil müşterinin isteğidir. | ÇEKİRDEK | değişmez | `R:2026-08-27__2026-08-27-rota-kapsamasi-hedef` | test_wo_route_coverage_goods.ts |
| rota-renk-ozellik-kapsama | Rota kapsaması (kategori düzeyi: 'renk/özellik VEREBİLEN adım var mı') REDDETMEZ, UYARIR — create · replace · 'Rengi Değiştir' tek kural (`collectRouteCoverageWarnings` / helper §5), yanıt `ApiResponse.warnings`; `quickStart` create uyarılarını yanıta taşır; uyarı metni NE eksik + SONUÇ. | ÇEKİRDEK | karar | `R:2026-08-27__2026-08-27-rota-kapsamasi-hedef` | test_wo_route_coverage_goods.ts (7) + test_wo_tar… |
| rota-renk-ozellik-kapsama | Özellik-BAŞINA kapsama ('ŞU özelliği veren istasyon var mı') HÂLÂ SERT: create 400, replace/updateTargetProperties 409 (`locks.applicablePropertyIds`); `goods` muafiyeti bu yola UYGULANMAZ. 2026-08-27 'uyarı' kararı yalnız kategori düzeyi — 'hepsi uyarır' diye OKUMA. | BELİRSİZ | tuzak | `R:2026-08-02__ozellik-istasyon-kisiti-kalir-ama-bos`, `R:2026-08-27__2026-08-27-rota-kapsamasi-hedef` | yok — test_wo_route_coverage_goods.ts bu yolu ölç… |
| rota-renk-ozellik-kapsama | Hedef renk değişikliğinin iki kapısı (Düzenle · Rengi Değiştir) TEK bekçi `assertTargetColorChange`: terminal statü (`PLAN_CHANGE_FROZEN_STATUSES` COMPLETED/CANCELLED/SUPERSEDED → 409 `WO_PLAN_FROZEN`) → renk aktif → `Item.allowedColors` (boş = sınırsız) → mal–plan uyumu → kapsama UYARISI. | ÇEKİRDEK | değişmez | `R:2026-08-21__2026-08-21-uretim-rengi-tek` | test_wo_target_color_guard.ts (59) |
| rota-renk-ozellik-kapsama | Renk kilidi MALA bakar, adım statüsüne değil: mismatch=0 SERBEST · mismatch>0 & pending>0 → 409 `COLOR_PARTIAL_CONFIRM` (`confirmPartial`) · pending=0 → 409 `COLOR_DYED_BLOCKED` (Tebdil/yeni WO/tümünü düzelt). `computeWorkOrderLocks.targetColor` yalnız Düzenle formunu pasifleştirir; COLOR_LOCKED YOK | ÇEKİRDEK | değişmez | `R:2026-08-21__2026-08-21-uretim-rengi-tek` | test_wo_target_color_guard.ts §2/§5 |
| rota-renk-ozellik-kapsama | Fason kabulde hedef renk KİLİT ALTINDA taze; `expectedTargetColorId` farklıysa 409 `TARGET_COLOR_CHANGED` (alan yoksa kontrol yok, eski APK fail-open). Plandan farklı renkte TEK soru `planColorAction`: APPLY_TO_PLAN (plan da döner) · ROLLS_ONLY (`RollPlanDeviation`; Tambur kapısı tekrar sormaz). | ÇEKİRDEK | değişmez | `R:2026-08-21__2026-08-21-uretim-rengi-tek` | test_wo_target_color_guard.ts §6/§9 |
| rota-renk-ozellik-kapsama | Tambur plan-sapma kapısı: renk/en (eşik sabiti 10 cm, eşit fark sapma DEĞİL) sapan top depoya ONAYLA iner — 409 `PLAN_MISMATCH` → aynı istek `confirmMismatch`, top başına BİR soru, audit imzalı; üç depo-indiriş yolu TEK helper `tambur-plan-gate.helper`; scrap/discard + renk-hedefsiz WO kapı DIŞI. | ÇEKİRDEK | değişmez | `R:2026-08-19__2026-08-19-tambur-plan-sapma` | test_tambur_plan_gate.ts (28→48) + mobil mutation… |
| rota-renk-ozellik-kapsama | Kalite = istasyon YETENEĞİ, boğaz İKİZ: `stepCanApplyQuality(station)` (bellek içi) + `QUALITY_STATION_WHERE` (Prisma, `satisfies`; spread'e ikinci `OR` eklenmez); saf yüklem WHERE'e GİREMEZ (atomik claim bozulur); ikisi birlikte değişir; yeni `kind===PROCESS_QC` YAZMA. | ÇEKİRDEK | değişmez | `A:2026-09-03__2026-09-03-kalite-istasyon-yetenegi` | test_station_quality_capability.ts (ikizin canlı … |
| sebep-kataloglari | Hazır sebep listeleri (fire · kayıt düzeltmesi · elle top ekleme · top iptali · sipariş iptali · yeniden üretim) DB'de yaşar (`ReasonPreset`) ve fabrika kendi diliyle düzenler; `code` doğuşta yazılır ve ASLA düzenlenmez (rapor anahtarı), `label` serbesttir. | ÇEKİRDEK | değişmez | `R:2026-08-19__2026-08-19-hazir-sebep-kataloglari` | test_reason_presets §5 |
| sebep-kataloglari | Sebep satırı SİLİNMEZ, yalnız gizlenir (`isActive=false`); son aktif satırın gizlenmesi 400 ile reddedilir — fire/düzeltme kararında sebep zorunlu olduğu için boş katalog operatörü kilitler. | ÇEKİRDEK | yasak | `R:2026-08-19__2026-08-19-hazir-sebep-kataloglari` | test_reason_presets |
| sebep-kataloglari | Fason adımına manuel taşıma malı `AT_SUBCONTRACTOR` YAPMAZ; çıkış ayrıca Fason Sevk ekranından yapılır (taşınan top orada kendiliğinden görünür). | ÇEKİRDEK | değişmez | `M:2026-08-19__tambur-boyahaneye-geri-gonder-2026-08` | yok |
| sevkiyat-brut-storno-cuval | Çuval bir DEPO nesnesidir: `Sack.customerId` opsiyonel (açılışta ya da sevkte atanır), `shipmentId` NULL = havuzda; sevkiyat depodan çuval seçilerek kurulur (`createShipment({sackIds, customerId, orderIds?})`), top↔sipariş satırı bağı YOKTUR. | ÇEKİRDEK | değişmez | `R:2026-07__cuval-depo-modeli-2026-07`, `A:undated__not-tarihsiz`, `R:undated__uretim-akisi` | yok |
| sevkiyat-brut-storno-cuval | `SackAllocation` sevk ANINDA yazılır (spec+şube FIFO, `distributeSacksToLines`); stok yalnız DISPATCH'te `SHIPPED` düşer ve tahsis o anda `shippedQty`ye terfi eder; PLANNED tahsis SAYILMAZ; `shippedQty` defterden TÜRER, elle yazılmaz. | ÇEKİRDEK | değişmez | `R:2026-07__cuval-depo-modeli-2026-07`, `A:undated__not-tarihsiz`, `R:2026-08-05__2026-08-05-sevk-storno-iade` | scripts/test_shipment_undo_dispatch.ts |
| sevkiyat-brut-storno-cuval | Rezervasyon gelirse (`shipping.reservationEnabled`, MODUL-BAYRAK karar #6 + §9) KENDİ append-only defteriyle kurulur; `SackAllocation`a DOKUNULMAZ — o sevk muhasebesidir. Bayrak bugün kodda YOK; 'rezerv YOK' profil seçimidir, kalıcı domain kuralı değil. | ÇEKİRDEK | yasak | `R:2026-07__cuval-depo-modeli-2026-07`, `R:undated__uretim-akisi` | yok |
| sevkiyat-brut-storno-cuval | `WorkOrder` yalnız üretimi yönetir (istasyonlar, kurşun/QC2, tambur); tartı/paket/sevkiyat AYRI domaindir (`shipping.service.ts`) ve WO'ya değil depoya/çuvala bağlanır. | ÇEKİRDEK | değişmez | `R:undated__wo-kapsami-sadece-uretim` | yok |
| sevkiyat-brut-storno-cuval | SACK etiketinin barkodu/QR'ı `Sack.sackNo`dur — Sack'e ayrı barcode kolonu EKLENMEZ; baskı FAIL-CLOSED: SACK şablonu çözülemez ya da bağlama uymazsa (çuvala ROLL şablonu atanmış) Türkçe 400, roll/swatch etiketine SAPMAZ. | ÇEKİRDEK | değişmez | `R:2026-07-30__2026-07-30-cuval-notu-sack`, `A:undated__not-tarihsiz` | scripts/test_label_context_fit.ts, scripts/test_s… |
| sevkiyat-brut-storno-cuval | Sevk rakamı BRÜT: sevk edilmiş sevkiyatın metrajı/adedi canlı çuval sorgusundan ÜRETİLMEZ; iade çıkış belgesini geriye dönük DEĞİŞTİRMEZ, ayrı belgeyle kapanır (`RETURN_DISPATCH` iade ANINDA tx içinde donar, iptalde VOIDED). Yanıttaki `returns {count, meters}` düşmek için değil DİPNOT içindir. | ÇEKİRDEK | değişmez | `R:2026-08-02__2026-08-02-sevk-rakami-brut`, `A:2026-08-02__2026-08-02-brut-kurali-liste`, `R:undated__tekserp-monorepo-koku` | scripts/test_dispatch_report_gross.ts |
| sevkiyat-brut-storno-cuval | Brütün TEK KAYNAĞI `RollReturn` (iptal edilmemiş; `prevSackId` geri-ekleme), snapshot DEĞİL — beş tüketici: `attachTotals` · `accounting-export` · `getShipmentById` · `collectShipmentDocContent` · `_shipped.ts`; altıncı kaynak altıncı rakam. Yalnız `getDispatchReport` donmuş snapshot okur. | ÇEKİRDEK | değişmez | `R:2026-08-02__2026-08-02-sevk-rakami-brut`, `A:2026-08-02__2026-08-02-brut-kurali-liste`, `R:2026-08-03__2026-08-03-brut-kurali-sevkiyat` | scripts/test_shipment_list_gross.ts · test_shipme… |
| sevkiyat-brut-storno-cuval | Yeni sevk-içeriği yüzeyi eklerken sor: 'sevkten SONRA da koşar mı?' — freeze tek koruma DEĞİLDİR: `reissue` ve lazy-init aynı üreticiyi sevkten sonra çağırır. Sevk tx'i içindeki freeze ve PLANNED taslak önizlemesi NO-OP (iade `roll.status=SHIPPED` istediği için orada RollReturn doğamaz). | ÇEKİRDEK | reçete | `R:2026-08-05__2026-08-05-belge-yolu-da`, `R:2026-08-03__2026-08-03-brut-kurali-sevkiyat` | scripts/test_dispatch_report_gross.ts §2b |
| sevkiyat-brut-storno-cuval | 'Dönemde sevk edilen metraj' TEK tanım `reports/_shipped.ts` (RollReturn geri-eklemeli, DirectShipment dahil, iade satırı kendi spec snapshot'ını taşır); ikinci tanım yazma. `finalizedAt` trigger kaynak listesinde SHIPPED/CANCELLED BİLEREK YOK — storno eski topu bugünün karnesine sokar. | ÇEKİRDEK | değişmez | `R:2026-08-09__2026-08-09-rapor-temeli-finalizedat` | scripts/test_quality_scorecard.ts, test_db_invari… |
| sevkiyat-brut-storno-cuval | Storno ≠ iade (SAP VL09): İade = mal müşteriye ULAŞTI → çıkış belgesi düzeltilmez, iade irsaliyesi + iade defteri (`RollReturn`, sebep zorunlu). Storno = mal HİÇ ÇIKMADI → çıkış belgesi İPTAL (VOIDED), stok rafına döner, iade defterine GİRMEZ; iade yazmak kalite verisini kirletir. | ÇEKİRDEK | değişmez | `R:2026-08-05__2026-08-05-sevk-storno-iade` | scripts/test_shipment_undo_dispatch.ts |
| sevkiyat-brut-storno-cuval | Düz storno (DISPATCHED→PLANNED) tahsisi SİLMEZ: `ShipmentOrder.isActive=true` + recompute; plaka/şoför KORUNUR. `releaseSacks` ile storno aynı tx'te `cancelPlannedShipmentTx` → CANCELLED: tahsis silinir, çuval havuza, toplar ÇUVALDA kalır (`sackId` korunur), irsaliyeler VOIDED, iade defteri YOK. | ÇEKİRDEK | değişmez | `R:2026-08-05__2026-08-05-sevk-storno-iade`, `R:2026-08-22__2026-08-22-sevk-kapisi-bayragin`, `A:undated__not-tarihsiz` | scripts/test_shipment_undo_dispatch.ts §10 (50) |
| sevkiyat-brut-storno-cuval | İzin ayrımı (SoD): `shipping:undo-dispatch` (storno + `releaseSacks` kapanışı; `shipping:write` KAPSAMAZ, aranmaz) · `shipping:invoice` (faturalandı işareti) · `shipping:write` (çuval/irsaliye/sevk). Üçlü yalnız Muhasebe/Süpervizör rol şablonlarında; boot uzlaştırması izni DB'ye getirir, atama ELLE. | ÇEKİRDEK | değişmez | `R:2026-08-05__2026-08-05-sevk-storno-iade`, `R:2026-08-22__2026-08-22-sevk-kapisi-bayragin`, `A:2026-08-02__2026-08-02-brut-kurali-liste` | scripts/test_shipment_undo_dispatch.ts, test_ship… |
| sevkiyat-brut-storno-cuval | Toplu iade: defter SATIR bazlı kalır (N top = N `RollReturn`; brüt kuralı ve `prevSackId` geri-ekleme buna dayanır), belge GRUP bazlı: tek irsaliye, `returnGroupId` = grup liderinin id'si; `POST /api/returns` `rollIds[]` (+ `rollId` aynen, APK değişmedi), `GET /api/returns/lookup-sack?sackCode=`. | ÇEKİRDEK | karar | `R:2026-08-05__2026-08-05-sevk-storno-iade` | scripts/test_return_bulk_group.ts (29) |
| sevkiyat-brut-storno-cuval | İade belgesi ÜYE id'siyle ÇÖZÜLMEZ (`buildReturnDispatchDoc` null; aksi hâlde lazy-init grubun İKİNCİ resmi kopyasını dondurur) → istemci `documentSourceId` (`returnGroupId ?? id`) kullanır, alan `select`ten düşürülmez. Grup iptali VOID DEĞİL REVİZE: `reissueForSourceTx` v+1; son kalem → VOIDED. | ÇEKİRDEK | yasak | `R:2026-08-05__2026-08-05-sevk-storno-iade` | scripts/test_return_bulk_group.ts (üye-id kapısı … |
| sevkiyat-brut-storno-cuval | Uydurulmuş kg canlı veriye GİRMEZ: istemci `source:'SIMULATED'` BEYAN eder (toast'ta 'SİMÜLASYON'), kararı backend verir — `shipping.simulatedWeightEnabled` KAPALIYKEN (varsayılan) 400. Kg müşteri belgesine basıldığı için SCALE simüle DOĞMAZ, METER doğar. `source:'MANUAL'` bu kapıdan MUAF. | ÇEKİRDEK | değişmez | `M:2026-07-30__cihazda-yazma-2026-07-30` | scripts/test_sack_weigh_source.ts + mobil useSack… |
| superadmin-gizlilik-ayar-sifresi | Süperadmin = `User.isSystemAccount` (additive kolon). Kimlik JWT'ye GİRMEZ: `verifyToken` taze okur → `req.isSystemAccount`; sahte claim 403. Tam yetki `getEffectivePermissions`ın İLK ifadesi `["*"]` — grant satırı doğmaz, süperadmin ROL değil, katalogda `code:"*"` yok. | ÇEKİRDEK | değişmez | `R:2026-09-03__2026-09-03-superadmin-p2-satici`, `R:2026-09-04__2026-09-04-en-yetkili-hesap` | test_superadmin (§: ["*"] + grant 0 · katalogda *… |
| superadmin-gizlilik-ayar-sifresi | `isSystemAccount` panelden/API'den ATANAMAZ ve mevcut kullanıcı YÜKSELTİLMEZ: hiçbir Zod şemasına/yazma yoluna girmez; tek yazar `scripts/superadmin-olustur.ts` (`src/` DIŞINDA, sunucu paketine girmez). | ÇEKİRDEK | yasak | `R:2026-09-04__2026-09-04-en-yetkili-hesap`, `R:2026-09-03__2026-09-03-superadmin-dogusu-p8` | test_superadmin_visible ('Alan panelden ATANAMAZ'… |
| superadmin-gizlilik-ayar-sifresi | En yetkili hesap GİZLENMEZ (2026-09-04): kullanıcı listesi (masaüstü+tablet), audit (canlı+arşiv), karneler, cihaz detayı DB'deki adıyla basar; VISIBLE_*/visibleUserWhere/maskSystemActor/SQL_ACTOR_*/blockSystemAccountTarget geri getirilmez; `isSystemAccount` yanıtta kalır → 'Tüm yetkiler' rozeti. | ÇEKİRDEK | değişmez | `R:2026-09-04__2026-09-04-en-yetkili-hesap` | test_superadmin_visible (30 — gizleme geri gelmed… |
| superadmin-gizlilik-ayar-sifresi | GÖRÜNÜRLÜK ≠ KİMLİK TESLİMİ: `GET /users/:id/credentials` (düz PIN) en yetkili hesap için başkasına 403; ayrıca (686c7212) `/users/:id*` altındaki HER YAZMA ucu `protectSystemAccountTarget` ile 403 + audit `SYSTEM_ACCOUNT_WRITE_BLOCKED` — GET serbest, hesabın kendisi muaf. | ÇEKİRDEK | değişmez | `R:2026-09-04__2026-09-04-en-yetkili-hesap` | test_superadmin_visible §5 + :180-200 (tanım · GE… |
| superadmin-gizlilik-ayar-sifresi | `flagWriteGuard`: modül dalı EN ÖNDE ve `.some` (belge dalı `.every`, yönler ters), SENKRON, hata `MODULE_FLAG_SUPERADMIN_ONLY`. Supap: sistem hesabı YOKSA `admin:settings` yazar + audit `SUPERADMIN_ABSENT_MODULE_WRITE`; bilinmiyor=VAR (fail-closed); supap dalı tembel DB tazeler (tek async yol). | ÇEKİRDEK | değişmez | `R:2026-09-03__2026-09-03-superadmin-p2-satici` | test_superadmin §A-§D (7 anahtar × 4 senaryo, kar… |
| superadmin-gizlilik-ayar-sifresi | SIR HİJYENİ: süperadmin parolası/PIN/TOTP ve ayar şifresi repoya, log'a, sürüm notuna, audit yüküne GİRMEZ; log satırında sır da kullanıcı adı da yok. `.env`de kalan `SUPERADMIN_*` uyarısı YALNIZ ANAHTAR ADI basar; job tam anahtar literali taşımaz (`Object.keys(process.env)` + ön ek). | ÇEKİRDEK | yasak | `R:undated__test-kullanicilari`, `R:2026-09-03__2026-09-03-superadmin-p2-satici`, `R:2026-09-03__2026-09-03-superadmin-dogusu-p8` | test_superadmin_provision §6 (literal/process.env… |
| superadmin-gizlilik-ayar-sifresi | Süperadmin hesabının TEK doğuş/rotasyon yolu sunucuda elle koşulan `npm run superadmin:kur` (`--rotate`: parola+PIN+TOTP, tokenVersion++). `.env` tohumlaması KALDIRILDI (iki yol = iki sır yüzeyi). Boot job hesap YARATMAZ, `SUPERADMIN_*` OKUMAZ: supap defterini tazeler + yaşam döngüsü audit'i. | ÇEKİRDEK | değişmez | `R:2026-09-03__2026-09-03-superadmin-dogusu-p8`, `R:2026-09-03__2026-09-03-superadmin-dogusu-p8__2` | test_superadmin_provision (§6 job'da .env yolu ge… |
| superadmin-gizlilik-ayar-sifresi | Modül profili uygulama için YAZMA UCU YOK: `PATCH /feature-flags` tek kapı (süperadmin guard + ayar şifresi zinciri ikinci kez KURULMAZ — §12.5 kapı çoğaltma yasağı); `GET /api/admin/module-profile` salt okuma, fark SUNUCUDA hesaplanır. | ÇEKİRDEK | yasak | `A:2026-09-03__2026-09-03-tamlik-bekcisi-kurulum` | BELİRSİZ — notta adlı bekçi yok |
| superadmin-gizlilik-ayar-sifresi | Dev/test girişi `admin` / `123123` — seed YALNIZ admin üretir; 'admin dışı tüm kullanıcılar test123' cümlesi 2026-07-03'te kalkan eski kullanıcılara aittir (sil). Yeni kullanıcı panelden (`POST /api/admin/users`); 0-izinli testler geçici kullanıcı üretir. Bölüme yalnız seed/dev kimlikleri yazılır. | ÇEKİRDEK | değişmez | `R:undated__test-kullanicilari` | yok |
| surum-yayin | Sürüm notu yazılmadan sürüm ÇIKMAZ: taslağı Claude yazar, kullanıcı ONAYLAR, onaysız paketlemeye geçilmez. Tek kaynak `surum-notlari.json` (operatör dili; kapsam panel\|tablet\|her-ikisi, 'sunucu' bilerek yok); panele+tablete gömülür. Kapı beklenen sürümü ARGÜMANDAN alır, not dosyası onu üretmez. | ÇEKİRDEK | değişmez | `R:undated__surum-yayinlama-sahaya-guncelleme-cikarma` | scripts/check-surum-notlari.mjs (paketleme kapısı) |
| surum-yayin | Sürüm numarası artmazsa hiçbir istemci güncellenmez (panel `package.json>version`↔`latest.yml`; tablet paket sürümü + APK `versionCode`). Yama hanesi OTOMATİK: taban git etiketi `panel-v*`/`tablet-v*`, yayın sunucusu DOĞRULAR (eşit/geride → DUR; okunamazsa sessiz geç). Küçük/büyük hane ELLE = karar. | ÇEKİRDEK | değişmez | `R:undated__surum-yayinlama-sahaya-guncelleme-cikarma`, `E:2026-08-26__otomatik-guncelleme-2026-08-26` | scripts/test_surum.mjs |
| surum-yayin | Yükleme SIRASI pazarlık dışı: paket dosyaları ÖNCE, manifest/`latest.yml`/APK künyesi EN SON (yayını AÇAN adım); elle `scp` YOK. Sonra dışarıdan HTTPS doğrulama: temiz URL ↔ `?cb=` kıyası 'yüklenmemiş' ile 'CF'de kalmış 404'ü ayırır (ikincisi yalnız Purge by URL); boyut/sha512 yarım yüklemeyi bulur. | ÇEKİRDEK | değişmez | `R:undated__surum-yayinlama-sahaya-guncelleme-cikarma`, `M:undated__guncelleme-dagitma-once-bu-bolum`, `R:2026-08-26__2026-08-26-electron-dagitimi-guncelleyici` | deploy/electron-yayinla.sh dogrula(); deploy/mobi… |
| surum-yayin | Cloudflare proxy (turuncu bulut) AÇIK kalmalı: yayın sunucusunun sertifikası Origin CA, ona yalnız CF Edge güvenir; DNS-only'ye çevrilirse panel/tablet güncellemesi SESSİZCE durur. | ÇEKİRDEK | değişmez | `R:undated__surum-yayinlama-sahaya-guncelleme-cikarma`, `R:2026-08-26__2026-08-26-electron-dagitimi-guncelleyici` | yok |
| surum-yayin | Yayın adresi pakete DERLEME ANINDA gömülür (panel `app-update.yml`, tablet AndroidManifest `EXPO_UPDATE_URL`); yanlış müşteri kodu BAŞKA fabrikanın güncellemesini kurar, hata SESSİZDİR. Müşteri kodu tek kaynak `Electron/shared/musteri.json` · `mobil/musteri.json`; adres ondan türer. | ÇEKİRDEK | değişmez | `R:undated__surum-yayinlama-sahaya-guncelleme-cikarma`, `R:2026-08-26__2026-08-26-electron-dagitimi-guncelleyici`, `R:2026-08-26__2026-08-26-mobil-uzaktan-guncelleme` | Electron/src/test/update-feed-url.test.ts (5 it) … |
| surum-yayin | OTA turunda `android.versionCode`a DOKUNULMAZ (tablet onu kurulu APK sürümü sanar, gerçek APK'yı bir daha teklif etmez). `versionCode` YALNIZ yayınlanacak APK için artar; `app.json` ↔ yayındaki `apk/surum.json` ayrışırsa OTA durur → 'native değişti mi?': hayırsa GERİ AL, evetse ÖNCE APK. | ÇEKİRDEK | yasak | `R:undated__surum-yayinlama-sahaya-guncelleme-cikarma` | yayinla-ota.mjs versionCode kapısı |
| surum-yayin | ERP adresi (`EXPO_PUBLIC_API_URL`) her tablet yayınında AÇIKÇA verilir — koda gömülü sabit varsayılan YOK (çözülemezse dur). Çözüm TEK KAYNAK `scripts/lib/adres.mjs`: `--api-url` → ortam → `.env*`; localhost ve `/api`siz adres RED. ERP kanalı ↔ güncelleme kanalı AYRI, biri diğerinden TÜRETİLMEZ. | ÇEKİRDEK | değişmez | `R:undated__surum-yayinlama-sahaya-guncelleme-cikarma`, `M:undated__apk-derleme-sahaya-kurulacak-paket-tek`, `R:2026-08-26__2026-08-26-mobil-uzaktan-guncelleme` | yayinla-ota/build-apk adres kapıları; sonrasında … |
| surum-yayin | Manifest yayın anında DONAR ve imzalanır — üreten tek yer `mobil/scripts/lib/manifest.mjs` (imza HAM baytlar üzerinden). Sunucu (VPS nginx ve LAN ikizi `/api/mobile/updates/*`) yalnız bayt servis eder; render eden kod YAZMA. Uçlar PUBLIC (koruma kod imzalama). LAN deposu `app\` DIŞINDA. | ÇEKİRDEK | değişmez | `B:undated__mobil-guncelleme-istemci-surum-politikasi`, `R:2026-08-26__2026-08-26-mobil-uzaktan-guncelleme` | Teks-Erp/scripts/test_mobile_update.ts (37 check;… |
| surum-yayin | Kod imzalama AÇIK: OTA anahtarı ve APK mührü `mobil/keystore/` altında, git DIŞI, VPS'e GİTMEZ; imza geçersizse tablet paketi reddeder; sertifika APK'ya gömülü değilse istemci imzayı HİÇ kontrol etmez (build-apk durdurur). Anahtar kaybı = her tablette sil+kur. `codesigning:configure` tuzağı bekçili. | ÇEKİRDEK | değişmez | `M:undated__guncelleme-dagitma-once-bu-bolum`, `M:undated__uzaktan-guncelleme-ota-apk-sahaya-nasil`, `R:2026-08-26__2026-08-26-mobil-uzaktan-guncelleme` | mobil/src/test/update-feed-url.test.ts:71 (update… |
| surum-yayin | İstemci sürüm politikası KODDA sabit (`client-version-policy.ts` kayıt defteri; yeni istemci = satır), uç `GET /api/client-policy/:istemci` PUBLIC, istemci FAIL-OPEN, tanımsız istemci 404. `minVersion` YALNIZ gerçek sözleşme kırılmasında yükselir, sahadakinden BÜYÜK olamaz; önce istemci yayınlanır. | ÇEKİRDEK | değişmez | `R:undated__surum-yayinlama-sahaya-guncelleme-cikarma`, `B:undated__mobil-guncelleme-istemci-surum-politikasi` | Teks-Erp/scripts/test_client_policy.ts (§3 minVer… |
| surum-yayin | Deploy sırası: backend ÖNCE, panel + tablet (OTA/APK) SONRA — yeni uç/anahtar/tablo eski istemciyi bozmaz; sözleşme kıran değişiklikte (uç kaldırma, alan adı/tipi, zorunlu parametre, enum, izin) 'eski istemci ne yapar' sorusu AÇIKÇA cevaplanır, gerekirse minVersion yükselir. | ÇEKİRDEK | reçete | `B:undated__mobil-guncelleme-istemci-surum-politikasi`, `R:2026-08-19__2026-08-19-2-paket-sektor`, `R:2026-08-27__2026-08-27-siparis-gorunurlugu-ozet` | yok |
| tambur-finalize-kesim | Her rotanın SON adımı topu finalize eder (`finalizeRollsAtLastStep`) — Tambur özel değil; top⟺Tambur, açık kumaş⟺diğer istasyonlar; `Roll.form` otomatik; `qualityGrade` NULLABLE (yalnız kalite istasyonları belirler, `finalizedAt` kaliteyle aynı olaydan). | ÇEKİRDEK | değişmez | `R:2026-07-13__2026-07-13-her-rota-final`, `A:2026-08-09__2026-08-09-rapor-temeli-topun` | scripts/test_finalize_last_step.ts |
| tambur-finalize-kesim | Finalize, iş emrini topun `currentStep`'inden çözer — köken `producedInStep`'ten DEĞİL (Top Kesme çocuğu `producedInStepId=null` doğar). | ÇEKİRDEK | değişmez | `R:2026-07-27__tambur-finalize-wo-disiplini-2026-07` | scripts/test_tambur_finalize_wo_guard.ts §4 |
| tambur-finalize-kesim | İş emri kapaması YALNIZ terminal-guard'lı `completeWorkOrderIfStepsDone` ile (COMPLETED/CANCELLED/SUPERSEDED notIn); CANCELLED/SUPERSEDED asla COMPLETED'a dirilmez; iptal/devredilmiş WO adımındaki topun finalize/kesimi reddedilir (pre-tx + kilit altı taze guard). | ÇEKİRDEK | değişmez | `R:2026-07-27__tambur-finalize-wo-disiplini-2026-07` | scripts/test_wo_terminal_guard.ts + test_tambur_f… |
| tambur-finalize-kesim | Ölü top kümesi TEK kaynak `K18_DEAD_STATUSES` (SUBCONTRACTOR_CONSUMED, TAMBUR_CONSUMED, KARTELA_CONSUMED, CANCELLED); `SCRAP` BİLEREK YOK — fire gerçek karardır, partiyi KAPATMAZ ve karneden düşmez; liste/lane/rapor filtresinde elle statü listesi kopyalanmaz. | ÇEKİRDEK | yasak | `R:2026-07-27__tambur-finalize-wo-disiplini-2026-07`, `R:2026-08-04__2026-08-04-roll-foldtype-entryreason`, `A:2026-08-04__2026-08-04-topun-kalici-alanlari` | scripts/test_tambur_manual_batch.ts (K18↔SCRAP ay… |
| tambur-finalize-kesim | Movement kapanışında `qtyOut = qtyIn` (istasyona giren işlenmiş metraj); finalize öncesi kesimler hacimden düşmez; WIP raporu qtyIn/qtyOut farkını 'kayıp' diye BASMAZ. | ÇEKİRDEK | değişmez | `R:2026-07-27__tambur-finalize-wo-disiplini-2026-07`, `A:2026-08-09__2026-08-09-rapor-temeli-topun` | yok (wip-scorecard.report.service.ts:6 yorumu; te… |
| tambur-finalize-kesim | Split çocuğu `parentRollId` + YENİ barkod + `entrySource=TAMBUR_SPLIT`; parti/depo parent'tan MİRAS, giriş istasyonu/kat/`producedInStepId` MİRAS DEĞİL. Beş yol: Tambur finalize · cutOpenFabric · finalizeOpenFabric · Top Kesme (cutWarehouseRoll/finalizeWarehouseCut) · fason kısmi sevk. | ÇEKİRDEK | değişmez | `R:undated__roll-split`, `R:2026-07-27__tambur-finalize-wo-disiplini-2026-07`, `A:2026-08-04__2026-08-04-topun-kalici-alanlari` | scripts/test_tambur_finalize_wo_guard.ts §3 + tes… |
| tambur-finalize-kesim | `producedInStepId` damgası: adıma bağlı kesimlerde (finalize · cutOpenFabric · finalizeOpenFabric) işlemin YAPILDIĞI Tambur adımı (parent kalıtımı değil); depo topu kesimi (Top Kesme) ve fason kısmi sevk çocuğunda NULL — o yüzden WO çözümü currentStep'ten. | ÇEKİRDEK | değişmez | `R:undated__roll-split`, `R:2026-07-27__tambur-finalize-wo-disiplini-2026-07` | scripts/test_tambur_finalize_wo_guard.ts §3-§4 |
| tambur-finalize-kesim | `RollError` PROCESS_QC'de (kursun-qc + kurşun bypass `kursunFinish`) ve Tambur'da açılır; Tambur kararıyla kapanır (`isProcessed=true`, `actionTaken=CUT\|NO_CUT`); Tambur dışı `NO_CUT` idari kapanış olabilir. Kapanış semantiği ÇEKİRDEK, hangi istasyonda açıldığı PROFİL (Faz B). | ÇEKİRDEK | değişmez | `R:undated__hata-yasam-dongusu`, `R:2026-08-12__2026-08-12-gece-single-restore` | yok |
| tambur-finalize-kesim | CANCELLED/SUPERSEDED iş emrinde manuel taşıma REDDEDİLİR (409 + önizlemede `woBlocked`); tek kaynak `workorder-manual-move.service.manualMoveWoBlockReason` (Tambur manual da onu import eder); doğru yol topu yeni iş emrine bağlamak. | ÇEKİRDEK | yasak | `R:2026-07-30__manuel-tasima-saha-surekliligi-2026-07` | scripts/test_manual_move_field_continuity.ts |
| tambur-finalize-kesim | Fason adımına manuel taşıma `AT_SUBCONTRACTOR` YAPMAZ — mal içeride bekler, çıkış Fason Sevk ile; fason dönüşü top (`entrySource=SUBCONTRACTOR_RETURN`) geri boyahaneye alınıp yeniden sevk+kabul edilebilir, ek engel konmaz. | ÇEKİRDEK | değişmez | `R:2026-07-30__manuel-tasima-saha-surekliligi-2026-07` | scripts/test_manual_move.ts (AT_SUBCONTRACTOR dal… |
| tambur-finalize-kesim | Kat `Roll.foldType` KALICI kolondur (JSON'da değil); MİRAS ALINMAZ — kesimde SEÇİLEN yazılır; alan hiç gönderilmezse parent→plan fallback'i sözleşme boşluğudur, miras DEĞİL; kanoniklik ZORUNLU (`resolveFoldTypeForWrite`/`normalizeFoldType` — ham değer filtrede sessiz 0 satır). | ÇEKİRDEK | değişmez | `R:2026-08-04__2026-08-04-roll-foldtype-entryreason`, `A:2026-08-04__2026-08-04-topun-kalici-alanlari`, `A:2026-08-13__2026-08-13-kesimde-kat-sessizce` | scripts/test_roll_fold_and_reason.ts + test_fold_… |
| tambur-finalize-kesim | Giriş sebebi `Roll.entryReason` KOLONUNDAN okunur — audit tek kaynak olamaz (`archive-scheduler` 6 ayda arşive taşır); audit yazımı kalır, kolon boşsa eski kayıt için audit'e düşülür; geri doldurma script'i dry-run varsayılan. | ÇEKİRDEK | değişmez | `R:2026-08-04__2026-08-04-roll-foldtype-entryreason`, `A:2026-08-04__2026-08-04-topun-kalici-alanlari` | scripts/test_roll_fold_and_reason.ts |
| tambur-finalize-kesim | Elle eklenen top PARTİSİZ doğmaz: tek açık parti → sormadan bağla · birden çok → 400 `BATCH_REQUIRED` + liste (reddeden taraftan) · hiç yok → NULL, `batch.autoCreateEnabled` AÇIKSA sunucu partiyi FAZ 2 tx'inde açar; 'açık' VERİYE dayanır (K18); karar yanıtta + audit `batchSource`. | ÇEKİRDEK | değişmez | `R:2026-08-04__2026-08-04-roll-foldtype-entryreason`, `A:2026-08-04__2026-08-04-topun-kalici-alanlari`, `A:2026-09-03__2026-09-03-dilim-2-davranis` | scripts/test_tambur_manual_batch.ts + test_qualit… |
| tambur-finalize-kesim | Aşım kesimi 0'da tıkanmaz: aşım dallarının claim WHERE'ine `currentQty > 0` KONMAZ (cutOpenFabric + cutWarehouseRoll — çifte-harcama koruması 0'ın altında anlamsız); normal dal `gte` durur; her sıfır-üstü kesim çocuk + sapma satırı üretir. | ÇEKİRDEK | yasak | `R:2026-08-12__2026-08-12-top-listesi-filtresi` | scripts/test_tambur_over_quantity.ts (13) |
| tambur-finalize-kesim | Metraj geri koyma İKİ DALDA FARKLI: üretim akışı (cutOpenFabric) yalnız `currentQty` + AŞIM KORUMASI (restore > initialQty ise initialQty yukarı + deftere OVERAGE/TAMBUR_UNDO_RESTORE); depo kesimi currentQty ve initialQty BİRLİKTE — tek taraf yazılırsa sahte aşım/sessiz sapma. | ÇEKİRDEK | değişmez | `R:2026-08-12__2026-08-12-gece-single-restore`, `R:2026-08-22__2026-08-22-mutabakat-kapisinin-kirmizisi` | scripts/test_tambur_undo.ts §5 + §11 |
| tambur-finalize-kesim | SENKRON SÖZLEŞMESİ: restore-toplamına giren HER kaynak `applyFull` 5b terslemesine de eklenir (yoksa metraj geri konur, sapma satırı canlı kalır, dönem raporu aynı metrajı İKİ KEZ görür); `computeRestoredQty` çocuk-kapsamlı `TAMBUR_UNDO_SINGLE` düzeltmelerini de sayar. | ÇEKİRDEK | değişmez | `R:2026-08-12__2026-08-12-gece-single-restore` | scripts/test_tambur_undo.ts |
| tambur-finalize-kesim | Mutabakat kırmızısı ÜÇE ayrılır (kod hatası · iş kararı bekleyen veri · sorgunun kör noktası); `test_consistency` salt-okunur ve CANLI DB'ye karşı koşulur; eski sapma satırları toplu UPDATE ile düzeltilmez, §13 DARALTILMAZ; adım durumu tarihsel olgudur (§20), veriye dokunulmaz. | ÇEKİRDEK | karar | `R:2026-08-22__2026-08-22-mutabakat-kapisinin-kirmizisi` | scripts/test_consistency.ts (salt-okunur kapı) |
| tambur-finalize-kesim | Renk/en hedeften sapan top depoya ONAYLA iner: 409 `PLAN_MISMATCH` → `confirmMismatch`; top başına BİR soru; audit + `RollPlanDeviation` çağıran tx'inde; üç yol TEK helper `tambur-plan-gate.helper`; eşik ±10 cm (PROFİL); scrap/discard + renk-hedefsiz WO kapı DIŞI; fason onayı tekrar SORULMAZ. | ÇEKİRDEK | değişmez | `R:2026-08-19__2026-08-19-tambur-plan-sapma`, `R:2026-08-21__2026-08-21-uretim-rengi-tek` | scripts/test_tambur_plan_gate.ts |
| top-duzeltme-iptal-fire-geri-alma | Silme yalnız soft'tur: isActive:false ya da RollStatus.CANCELLED; fiziksel DELETE ASLA. Bilinçli istisnalar: bağımlılık-guard'lı master-data DELETE /:id/permanent, boş çuval, cihaz unpair, pivot replace, ItemPrice satırı (isActive EKLENMEZ). | ÇEKİRDEK | değişmez | `R:undated__sadece-soft-delete-veya-gercek-fire`, `M:undated__soft-delete-veya-gercek-fire-karari` | yok |
| top-duzeltme-iptal-fire-geri-alma | İptal ≠ Fire (defter semantiği): İptal=CANCELLED 'hiç olmamalıydı' — stok DÜŞMEZ, açık hareket qtyOut=0 (storno), fire raporuna girmez. Fire=SCRAP 'vardı, gitti' — stok DÜŞER, qtyOut=qtyIn, fire oranına girer; uç POST /rolls/:id/scrap, izin roll:manual-adjust (roll:write yetmez). | ÇEKİRDEK | değişmez | `R:2026-08-25__2026-08-25-saha-deploy-sonrasi`, `R:undated__sadece-soft-delete-veya-gercek-fire`, `R:2026-08-05__2026-08-05-manuel-top-geri` | test_roll_cancel_undo §7 · test_manual_roll_undo … |
| top-duzeltme-iptal-fire-geri-alma | İKİNCİ BİR İPTAL MOTORU YAZILMAZ: tüm iptal/fire yolları InventoryService.softDelete(mode CANCEL\|SCRAP)'ten geçer; TamburUndoService.MANUAL da onu çağırır. Hareket notu EZİLMEZ: 'CANCELLED (<eski not>)' / 'SCRAPPED (…)'. | ÇEKİRDEK | yasak | `R:2026-08-05__2026-08-05-manuel-top-geri`, `R:2026-08-25__2026-08-25-saha-deploy-sonrasi` | test_manual_roll_undo (negatif sonda: MANUAL dalı… |
| top-duzeltme-iptal-fire-geri-alma | İptal edilmiş topun clientToken'ı REPLAY EDİLEMEZ → 409 ENTRY_CANCELLED (assertRollReplayAlive); 'success:true + iptal edilmiş barkod' sessizce yanlış cevaptır. Mantıksal deneme iptalle kapanır, yeni top yeni token ister. | ÇEKİRDEK | değişmez | `R:2026-08-05__2026-08-05-manuel-top-geri` | test_manual_roll_undo (ENTRY_CANCELLED guard'ı ka… |
| top-duzeltme-iptal-fire-geri-alma | İptali geri alma TEK yüklem resolveRollRestoreBlockReason (ekran+uç aynı fonksiyon, sebep hep söylenir). Sinyaller: CANCELLED · hareket yok · istasyon işlemi yok · çocuk yok · adıma bağlı değil · çuval/sevk yok · fason/kartela sevki yok · Tambur geri almasının ürünü değil. batchId ENGEL DEĞİL. | ÇEKİRDEK | değişmez | `R:2026-08-25__2026-08-25-saha-deploy-sonrasi`, `kod:94b937c0 (2026-08-29, BULGU-T1-011 — NOTU YOK)` | test_roll_cancel_undo (TAMBUR_UNDO_CANCEL_CODE :5… |
| top-duzeltme-iptal-fire-geri-alma | Yıkıcı işlemde (iptal/sil/fire) backend cancel-preview döner, arayüz etkilenen HER kaydı adıyla listeler ve kayıt bazlı seçim sunar; 'X kayıt etkilenecek' yetmez. Engelli top denenmez, engel backend'in kendi cümlesiyle basılır; izinsiz şık (fire) ÇİZİLMEZ (kart↔route hizası). | ÇEKİRDEK | değişmez | `R:undated__yikici-islemlerde-detayli-onay-zorunlu`, `R:2026-08-25__2026-08-25-saha-deploy-sonrasi`, `R:2026-08-12__2026-08-12-gece-single-restore` | BulkCancelRollsDialog.test.tsx (5) |
| top-duzeltme-iptal-fire-geri-alma | Giriş/iptal sebebi KOLONDAN okunur (Roll.entryReason/entryReasonCode, cancelReason/cancelReasonCode — metin GÖRÜNEN kayıt, kod RAPOR ANAHTARI); audit 6 ayda arşivlenir, tek kaynak olamaz. Audit yazımı KALIR; okuma kolonu tercih eder, boşsa eski kayıt için audit'e düşer. | ÇEKİRDEK | değişmez | `R:2026-08-04__2026-08-04-roll-foldtype-entryreason`, `kök CLAUDE.md:78 (2026-08-21 nameFold + sebep KODU)`, `R:2026-08-05__2026-08-05-manuel-top-geri` | yok |
| top-duzeltme-iptal-fire-geri-alma | Metraj geri koyma İKİ DALDA FARKLI: üretim (cutOpenFabric) yalnız currentQty, depo (cutWarehouseRoll) currentQty+initialQty birlikte (tek taraf → sahte AŞIM). ÜRETİM dalı da aşımı korur: initialQty bump + OVERAGE/TAMBUR_UNDO_RESTORE — canlı dal ↔ arşiv ikizi ayrışamaz (08-22'ye dek ayrışmıştı). | ÇEKİRDEK | değişmez | `R:2026-08-12__2026-08-12-gece-single-restore`, `R:2026-08-22__2026-08-22-mutabakat-kapisinin-kirmizisi` | test_tambur_undo §5 + §11 (§11 üretim dalını ölçe… |
| top-duzeltme-iptal-fire-geri-alma | SENKRON SÖZLEŞMESİ: restore-toplamına giren HER kaynak (computeRestoredQty; çocuk-kapsamlı TAMBUR_UNDO_SINGLE düzeltmeleri dahil, satırlar ÇOCUĞUN rollId'sinde) applyFull 5b terslemesine de eklenir — yoksa dönem raporu aynı metrajı İKİ KEZ görür. Sıfır-çocuk + restored>0 → FULL çalışır (F0402). | ÇEKİRDEK | değişmez | `R:2026-08-12__2026-08-12-gece-single-restore` | test_tambur_undo §10 (F0402; tersleme koparılınca… |
| top-duzeltme-iptal-fire-geri-alma | WO kapatma = kapanış dispozisyonu: 'işlemde top var' diye REDDETMEZ; kalan her top için altı karar — STOCK ('Stoğa geri') · WAREHOUSE · A1_STOCK · SCRAP (mal vardı) · CANCELLED (mal yoktu) · TRANSFER (yeni WO klonu; kaynak COMPLETED kalır, SUPERSEDED OLMAZ). Roll.form'a dokunulmaz. | ÇEKİRDEK | değişmez | `R:2026-07-30__wo-kapatma-kapanis-dispozisyonu-2026-07`, `R:2026-08-27__2026-08-27-3-tur-yari` | yok |
| top-duzeltme-iptal-fire-geri-alma | Kapanış kapıları: hard-block yalnız FASON (AT_SUBCONTRACTOR/RETURNED_FROM_SUBCONTRACTOR ya da açık fason sevki; fason dönüşü STOCK'a çekilemez); kapsam BİREBİR (tx içinde taze in-flight küme ≠ gönderilen liste → 400); dispozisyon varsa workorder:write + roll:manual-adjust. | ÇEKİRDEK | değişmez | `R:2026-07-30__wo-kapatma-kapanis-dispozisyonu-2026-07` | yok (fazA: test dosyası bulunamadı) |
| top-duzeltme-iptal-fire-geri-alma | Kapanışta kalite yalnız WAREHOUSE/A1_STOCK dispozisyonunda sorulur; VARSAYILAN opsiyonel, quality.gradeRequiredEnabled açıkken kalitesiz top 400 GRADE_REQUIRED (dar kapsam). Satılabilir statüye çekilen barkodsuz top barkod alır. | ÇEKİRDEK | değişmez | `R:2026-07-30__wo-kapatma-kapanis-dispozisyonu-2026-07`, `kök CLAUDE.md:106 (2026-09-03 Dilim 2 davranış bayrakları)` | yok |
| top-duzeltme-iptal-fire-geri-alma | Top düzeltme TEK sözleşme, kapsam topun DURUMUNDAN: ALWAYS_BLOCKED (fason/kartela/emekli/sevk) her yolla red · FREE_STOCK (STOCK/WAREHOUSE/A1_STOCK) sebep opsiyonel, ek yetki yok · gerisi sebep ZORUNLU + roll:manual-adjust. Kapsam Boolean(reason) ile GENİŞLETİLMEZ. | ÇEKİRDEK | değişmez | `R:2026-07-30__top-duzeltme-tek-sozlesme-2026-07` | test_roll_edit_unified |
| top-duzeltme-iptal-fire-geri-alma | Fason adımına manuel taşıma AT_SUBCONTRACTOR YAPMAZ — mal içeride bekler, çıkış Fason Sevk ile (kabul doğrudan yapılamaz; önizleme uyarır). Fason dönüşü top (entrySource=SUBCONTRACTOR_RETURN) geri boyahaneye alınıp yeniden sevk+kabul edilebilir — ek engel yok. | ÇEKİRDEK | değişmez | `R:2026-07-30__manuel-tasima-saha-surekliligi-2026-07` | test_manual_move.ts |
| top-duzeltme-iptal-fire-geri-alma | CANCELLED/SUPERSEDED iş emrinde manuel taşıma REDDEDİLİR (409 + önizlemede woBlocked): kartlar VOIDED, ensureWorkOrderInProgress yalnız PLANNED'ı diriltir → top 'canlı ama okutulamaz' çıkmaza düşerdi. Doğru yol topu yeni iş emrine bağlamak. Tek kaynak manualMoveWoBlockReason. | ÇEKİRDEK | yasak | `R:2026-07-30__manuel-tasima-saha-surekliligi-2026-07` | test_manual_move.ts |
| top-duzeltme-iptal-fire-geri-alma | Kat (Roll.foldType) KALICI kolondur ve MİRAS ALINMAZ: kesimde SEÇİLEN yazılır; alan hiç gönderilmezse parent→plan fallback'i sözleşme boşluğudur, miras değil. Kanoniklik ZORUNLU (normalizeFoldType — ham değer filtrede sessizce 0 satır). Beş yazma yolu aynı kural. | ÇEKİRDEK | değişmez | `R:2026-08-04__2026-08-04-roll-foldtype-entryreason` | yok |
| top-duzeltme-iptal-fire-geri-alma | Toplu veri düzeltmesi yapan script DRY-RUN varsayılandır ve --apply öncesi etkilenecek her kaydı somut listeler (backfill_roll_fold_and_reason · fix_tambur_undo_cancel_marker emsalleri); prod canlı, geri alma yolu raporda basılır. | ÇEKİRDEK | reçete | `R:undated__production-canli-gercek-fabrika-gercek-veri`, `R:2026-08-04__2026-08-04-roll-foldtype-entryreason`, `kod:94b937c0 (2026-08-29, BULGU-T1-011 — NOTU YOK)` | yok |
| yari-mamul | `rollScope=RAW_STOCK` ham + yarı mamulün BİRLEŞİMİDİR ve DARALTILMAZ: mobil Hızlı İş Emri top seçicisinin tek kaynağıdır, tablette üçüncü sekme yoktur — daraltmak o topların üretime alınmasının sahadaki tek yolunu kapatır. | ÇEKİRDEK | yasak | `R:2026-08-26__2026-08-26-yari-mamul-kendi`, `R:2026-08-27__2026-08-27-2-tur-yari` | Teks-Erp/scripts/test_semi_finished_entry.ts §5 (… |
| yari-mamul | `rollScope` FAIL-CLOSED: tanınmayan kapsam 400 atar. `delete where.rollScope` koşulsuz koştuğu ve istemci `status:'ALL'` gönderdiği için sessiz kalmak CANCELLED/SHIPPED/SCRAP dahil TÜM tabloyu döndürür — hata yok, log yok. | ÇEKİRDEK | değişmez | `R:2026-08-26__2026-08-26-yari-mamul-kendi` | yok |
| yari-mamul | Yarı mamul rafta duran, üretime sokulabilir maldır: ARZDAN DÜŞÜLMEZ, ayrı GÖSTERİLİR. `malzemeAcigi = max(0, uretilecek − (ham + yariMamul))` — ikisi birden düşülür. Arzdan düşmek olmayan bir kumaş açığı uydurur (negatif sonda: 400 m talepte 200 m sahte açık). | ÇEKİRDEK | değişmez | `R:2026-08-27__2026-08-27-3-tur-yari` | Teks-Erp/scripts/test_semi_finished_surfaces.ts §… |
| yari-mamul | Aynı stok sorusuna bakan üç yüzey TEK rakam basar: Stok Karnesi ↔ Envanter rozeti ↔ Kanban kolonu. Kanban stok kolonu bu yüzden `currentStepId: null` koşulunu taşır ve kapsam tanımları Envanter `rollScope` zinciriyle BİREBİRDİR; ayrışırlarsa aynı soruya iki rakam doğar. | ÇEKİRDEK | değişmez | `R:2026-08-27__2026-08-27-2-tur-yari` | Teks-Erp/scripts/test_production_flow_columns.ts … |
| yari-mamul | Envanter ekranında bir kova ayrıldıysa RAPOR da aynı turda ayrılır (Stok Karnesi `semiQty`/`semiCount`, aynı `rollScope` ayrımıyla) — yoksa 'ekran 800 / rapor 950' çelişkisini biz üretiriz. Yaş kovaları, ölü stok ve `byItem` yalnız `finished` üzerinde çalıştığı için dokunulmaz. | ÇEKİRDEK | değişmez | `R:2026-08-26__2026-08-26-yari-mamul-kendi` | yok |
| yari-mamul | `STOCK` bir STATÜdür; topun hangi envanter sekmesinde görüneceğini `entrySource` belirler. Kullanıcı-görünür metni statüden türetme: kapanış dispozisyonu etiketi 'Ham stok' değil 'Stoğa geri', iptal geri alma mesajı yarı mamul topu için 'yarı mamul stoğunda' der. | ÇEKİRDEK | değişmez | `R:2026-08-27__2026-08-27-3-tur-yari` | Electron .../WorkOrderCompleteDialog.test.tsx:151… |
| yari-mamul | Fason kabulünde orijinal rulolar `SUBCONTRACTOR_CONSUMED` ile emekliye ayrılır; makbuz üzerinden `parentReceiptId`'li YENİ açık-kumaş Roll'lar doğar (`entrySource=SUBCONTRACTOR_RETURN`, barcode null). Kabulde metraj ZORUNLU, ağırlık opsiyonel; kesin ölçüm sonraki istasyonun `FINISH`'inde damgalanır. | ÇEKİRDEK | değişmez | `R:undated__fason-donus` | yok |
| yetki-izin-rol | İzin doğrudan kullanıcıya bağlanır (`UserPermission`), rol modeli YOK (`PermissionTemplate` yalnız paket; `hasRole` yok). Eşleşmenin TEK yüklemi `matchesPermission` (global `*` + `<domain>:*`) — backend/Electron/mobil ikiz; düz `includes("admin:*")` YASAK (`["*"]` süperadmini tanımaz). | ÇEKİRDEK | değişmez | `B:undated__rbac-permission-kodlari`, `E:undated__rbac`, `A:2026-09-03__2026-09-03-superadmin-p2-gizli` | scripts/test_superadmin_visible.ts (`*` körlüğü k… |
| yetki-izin-rol | Yeni izin eklemenin TEK adımı `permission-catalog.ts` → `PERMISSION_CATALOG`'a bir satır (`category` Prisma enum'u — yazım hatası derlemede düşer). Boot uzlaştırması her açılışta EKSİK satırı yazar: kodu deploy etmek = katalogu getirmek. Sayı dokümana sabitlenmez (bugün 86). | ÇEKİRDEK | değişmez | `B:2026-08-01__yeni-izin-eklemek-tek-dosya-2026`, `B:undated__rbac-permission-kodlari`, `B:undated__route-swagger-jsdoc-kod-src-constants` | scripts/test_permission_catalog.ts (TS AST; katal… |
| yetki-izin-rol | Katalog/uzlaştırma ATAMA İÇERMEZ: izin ve rol DB'ye gelir, KİMSEYE verilmez (katalog koda, atama panele ya da `scripts/sync-*-permissions.ts`). Atanmamış iznin tek görünürlüğü Yetki Kataloğu'ndaki 'N yetki hiçbir kullanıcıda yok' bandı (`userCount`/`templateCount`) — yeni izinde bandı kontrol et. | ÇEKİRDEK | değişmez | `B:undated__katalog-yine-atama-icermez`, `R:2026-08-06__2026-08-06-rol-sablonlari-koda`, `R:2026-08-05__belge-tasarimi-ayri-bir-yetkidir-sistem` | yok (panel bandı görsel; arşiv ölçümü: 7 izin hiç… |
| yetki-izin-rol | Rol şablonları tek kaynak `constants/role-template-catalog.ts` (29 rol; sayı sabitlenmez) + boot uzlaştırma (izinlerden SONRA, aynı zincir — FK) + bekçi `test_role_template_catalog`. İzin kodunun DB'ye gelmesi yetmez; onu kullanıcıya götüren PAKET (şablon) de kodda gelir. | ÇEKİRDEK | değişmez | `R:2026-08-06__2026-08-06-rol-sablonlari-koda`, `B:2026-08-06__rol-yetki-sablonu-katalogu-ayni-uc` | scripts/test_role_template_catalog.ts (§1 tutarlı… |
| yetki-izin-rol | Mobil ekranın dokunduğu her uç `requireAnyPermission('<web-izni>', ...MOBILE_X)`; yalnız `requirePermission` saha kullanıcısını 403'ler. Yeni saha özelliğinde 'yeni izin kodu' refleksi YOK — mevcut çift yeter (`roll:manual-adjust` ∨ `mobile:tambur-duzelt`; rework: `workorder:write`). | ÇEKİRDEK | değişmez | `B:undated__mobil-ekranin-dokunacagi-endpoint-sadece-r`, `R:2026-08-19__2026-08-19-hazir-sebep-kataloglari`, `R:2026-08-26__2026-08-26-aksam-sebep-adimi` | scripts/test_permission_catalog.ts (kod katalogda… |
| yetki-izin-rol | Kart ile route AYNI izin listesini taşır (`tile-config.permissionAny` ↔ `content-routes.requireAnyPermission`); ayrışırsa kullanıcı kartı görür, tıklar, `/forbidden`a düşer. Etiketler kartı 2026-08-14'ten beri `[station:read, label-template:read]` — eski 'hiza sorunu' KAPANDI. | ÇEKİRDEK | değişmez | `R:2026-08-05__belge-tasarimi-ayri-bir-yetkidir-sistem`, `E:undated__rbac` | scripts/test_document_template_permission.ts (kar… |
| yetki-izin-rol | Dar izin çiftinde READ kümesi WRITE kodunu DA içerir (yazabilen okuyabilir); `label-template:read/write` bu tuzağı hâlâ taşır — yalnız write işaretlenen kullanıcı ekranı hiç açamaz, sebep yazmaz. Kümeler saf string literal dizi kalır (AST bekçisi okur). | ÇEKİRDEK | değişmez | `R:2026-08-05__belge-tasarimi-ayri-bir-yetkidir-sistem`, `E:undated__rbac` | scripts/test_document_template_permission.ts (yol… |
| yetki-izin-rol | `admin:settings` DOCUMENT_DESIGN_READ/WRITE kümelerinden DÜŞÜRÜLMEZ: uzlaştırma yeni izni getirir ama atamaz, sıkı ayrım deploy anında admin dahil herkesi dışarıda bırakır — o kod geriye uyum değil, kilitlenmeye karşı tek emniyet supabıdır. | ÇEKİRDEK | yasak | `R:2026-08-05__belge-tasarimi-ayri-bir-yetkidir-sistem`, `E:undated__rbac` | scripts/test_document_template_permission.ts (adm… |
| yetki-izin-rol | `PATCH /api/feature-flags` guard'ı ANAHTAR-KAPSAMLI ÜÇ DAL, FAIL-CLOSED: ① bir modül anahtarı bile varsa (`.some`, EN ÖNDE, senkron) → sistem hesabı (supaplı) ② YALNIZ belge anahtarları (`.every`) → `document-template:write` ③ gerisi/boş → `admin:settings`. Düz OR YASAK; ayar şifresi bundan SONRA. | ÇEKİRDEK | değişmez | `R:2026-08-05__belge-tasarimi-ayri-bir-yetkidir-sistem`, `A:2026-09-03__2026-09-03-superadmin-p2-gizli`, `A:2026-09-03__2026-09-03-ayar-sifresi-p3` | scripts/test_document_template_permission.ts (gua… |
| yetki-izin-rol | `settings:workstation` ve `document-template:read/write` kategorisi WEB: `hasAdminAccess` saymaz, `admin:*` VERMEZ; taşıyan Yönetim menüsünü görmez. `/system/settings` ve `/system/update` GENİŞ kapı DAR içerik (`admin:settings` ∨ `settings:workstation`; `visibleSettingsCategories` süzer). | ÇEKİRDEK | değişmez | `B:undated__rbac-permission-kodlari`, `E:undated__rbac`, `R:2026-08-05__belge-tasarimi-ayri-bir-yetkidir-sistem` | scripts/test_workstation_permission.ts + Electron… |
| yetki-izin-rol | Masaüstüne kabul `canEnterApp`: kullanıcının en az bir MOBİL-OLMAYAN izni olmalı; yalnız `mobile:*` izinli hesap panele giremez — backend `login` `isDesktopClient` (electron VE web) iken 403 döner, token bile üretmez; `!== "mobile"` YAZILMAZ. | ÇEKİRDEK | değişmez | `E:undated__rbac`, `E:undated__test-kullanicilari` | yok (Electron auth.test.ts kapsamı doğrulanmadı —… |
| yetki-izin-rol | Süperadmin ROL DEĞİL: `getEffectivePermissions` ilk ifadesi `isSystemAccount → ["*"]` (grant satırı DOĞMAZ; katalogda `code:"*"` YOK → panelden atanamaz). `isSystemAccount` panelden ATANAMAZ (tek yazar `superadmin-olustur.ts`). Kimlik JWT'ye GİRMEZ — `req.isSystemAccount` istek başına taze okunur. | ÇEKİRDEK | değişmez | `A:2026-09-03__2026-09-03-superadmin-p2-gizli`, `R:2026-09-03__2026-09-03-superadmin-dogusu-p8` | scripts/test_superadmin.ts (['*'] + grant 0, kata… |
| yetki-izin-rol | En yetkili hesap GÖRÜNÜR (2026-09-04): kullanıcı listesi/audit/tablet gerçek ad, 'Tüm yetkiler' rozeti (hesabın hiç `UserPermission` satırı yok, sayaç 0 gösterirdi); gizleme süzgeci, takma ad ve `/users/:id*` 404 kapısı geri GELMEZ. | ÇEKİRDEK | değişmez | `A:2026-09-03__2026-09-03-superadmin-p2-gizli` | scripts/test_superadmin_visible.ts (30 kontrol) +… |
| yetki-izin-rol | GÖRÜNÜRLÜK ≠ KİMLİK TESLİMİ: `/users/:id/credentials` düz PIN döner ve `login-quick-pin` PIN'i tek başına kimlik sayar → o ucun üstünde dar 403 kapısı; `/users/:id` öneki KALIR (`protectSystemAccountTarget`: GET serbest, yazma 403 + audit, kendisi muaf) ve önek izni alt rotaların BİRLEŞİMİ. | ÇEKİRDEK | değişmez | `A:2026-09-03__2026-09-03-superadmin-p2-gizli` | scripts/test_superadmin_visible.ts §5/§5b + test_… |
| yetki-izin-rol | Modül anahtarı kilidi + emniyet supabı: sistem hesabı VARSA modül anahtarlarını yalnız o yazar (403 `details.code=MODULE_FLAG_SUPERADMIN_ONLY`); hesap HİÇ doğmamışsa `admin:settings` yeter + audit `SUPERADMIN_ABSENT_MODULE_WRITE`; bilinmiyor = VAR (fail-closed); defter TEK YÖNDE tazelenir. | ÇEKİRDEK | değişmez | `A:2026-09-03__2026-09-03-superadmin-p2-gizli`, `R:2026-09-02__2026-09-02-03-modul-anahtarlari` | scripts/test_superadmin.ts (supap 4 durum, senkro… |
| yetki-izin-rol | Ayar şifresi bir NİYET kapısıdır (yetki değil; izin zincirinin ARDINA takılır, asenkron): süperadmin MUAF, belge-only gövde MUAF (`.every`), hash yoksa kapı UYUR (sıfır fark), kilit `bcrypt.compare`den ÖNCE; başlık yok → 403 REQUIRED (audit yok), yanlış → 403 INVALID + audit, doğru → USED audit. | ÇEKİRDEK | değişmez | `A:2026-09-03__2026-09-03-ayar-sifresi-p3` | scripts/test_settings_password.ts §A–§L |
| yetki-izin-rol | Şifre YALNIZ `X-Settings-Password` başlığında — gövde/query/cookie fallback YASAK (ölçülmüş sızıntı: erişim logu URL'i, audit yükü yolu basar). ASCII `^[\x21-\x7E]+$`, 8–72 (HTTP başlığı non-ASCII taşımaz; bcrypt 72 baytta sessiz kırpar); `.trim()` yerine RED; backend↔Electron ayna bekçili. | ÇEKİRDEK | yasak | `A:2026-09-03__2026-09-03-ayar-sifresi-p3` | scripts/test_settings_password.ts §C (gövde/query… |
| yetki-izin-rol | Süperadmin TEK doğuş/rotasyon yolu `npm run superadmin:kur` (`--rotate`, sunucuda interaktif): İDEMPOTENT (hesap varsa DOKUNMAZ), MEVCUT kullanıcı YÜKSELTİLMEZ, TTY yoksa FAIL-LOUD, saf `provisionSuperadmin` yazdırmaz. `.env` tohumlaması YASAK; kalan `SUPERADMIN_*` için yalnız ANAHTAR ADI. | ÇEKİRDEK | değişmez | `R:2026-09-03__2026-09-03-superadmin-dogusu-p8`, `A:2026-09-03__2026-09-03-superadmin-p2-gizli` | scripts/test_superadmin_provision.ts (§7 non-TTY … |
| yetki-izin-rol | Dev/test kimliği yalnız seed `admin / 123123` (katalogdaki TÜM izinler; sayı sabitlenmez — '~55' bayat, bugün 86). 'Admin dışı tüm kullanıcılar test123' ÖLÜ (2026-07-03). HTTP testleri kendi kullanıcısını `ensureTestAdmin()` ile üretip temizler; 'admin/123123 için hâlâ AÇIK' listesi bayat. | ÇEKİRDEK | değişmez | `R:undated__test-kullanicilari`, `E:undated__test-kullanicilari`, `B:undated__fixture-seed-master-data-si-business` | yok |

## kök-dizin (62)

| Küme | Kural | Sınıf | Tür | Kaynak | Bekçi |
|---|---|---|---|---|---|
| belge-etiket-sablon | [PROFİL] Kart varsayılan A5 (DEFAULT_TRAVELER_CARD_CONFIG.pageSize); A4 panelden ya da baskı diyaloğundan — sayfa boyutu kurulum ayarıdır, kod kararı değil (daha yoğun kartı olan kurulum A4 seçer). | PROFİL | karar | `R:2026-08-03__refakat-karti-a5-varsayilan-parti-blogu` | yok |
| belge-etiket-sablon | [PROFİL] docItemNameMode: bizdeki (VARSAYILAN — belgeye tek bayt eklenmez, ölçülü) \| musterideki (FAIL-OPEN: karşılığı yoksa BİZİM adımız; boş ad taşıyan irsaliye hukuken sakat) \| ikisi. Dört kapı + test_feature_flag_contract §16 aEnum + panel enumFlags + Electron değer aynası. | PROFİL | karar | `R:2026-09-04__2026-09-04-sevk-belgesinde-musterideki` | test_shipment_doc_customer_name §1 + test_feature… |
| belge-etiket-sablon | [PROFİL] docItemNameMode kapsamı yalnız sevk irsaliyesi ailesi (SHIPMENT_DISPATCH + aynı snapshot'ın muhasebe fişi). Fasondan DOĞRUDAN sevk irsaliyesi (SUBCONTRACTOR_DIRECT_SHIP, ayrı payload/renderer) ve muhasebe Excel dışa aktarımı BİLİNÇLİ dışarıda — panel metninde yazılı. | PROFİL | karar | `R:2026-09-04__2026-09-04-sevk-belgesinde-musterideki` | yok |
| belge-etiket-sablon | Çek teslim bordrosu ANLIK çıktıdır: bordro grubunu sahiplenen kaynak model yok → donmuş PrintedDocument üretilmez, belge no/sürüm yok (kâğıtta yazılı); bordro basmak çekin DURUMUNU değiştirmez. Donmuş sürüm istenirse ÖNCE ChequeBatch benzeri kaynak modeli (J kararı). | ÇEKİRDEK | karar | `R:2026-08-14__bes-saglamlik-sinifi-2026-08-14` | yok |
| belge-etiket-sablon | [PROFİL] Fason çekisi yerleşimi panelden: placements.batchInfo (sol/sağ), sections.accountNo OPT-IN (varsayılan KAPALI — eski donmuş çekiler de artık basmaz, bilinçli), sections.fabricHeader OPT-IN, gridGroups 3/4/5 × gridRows 1-40 = sayfa başına top (varsayılan 5×10=50, kullanıcı kararı 08-06). | PROFİL | karar | `R:2026-08-05__2026-08-05-belge-yerlesimi-a5` | scripts/test_fason_ceki_html.ts §18 |
| belge-etiket-sablon | label-template:read/write çiftinde READ WRITE'ı KAPSAMAZ (okuma uçları salt requirePermission('label-template:read')): yalnız 'düzenleme' işaretlenen kullanıcı Etiketler ekranını HİÇ açamaz, sebep hiçbir yerde yazmaz — yeni çiftte tekrarlama. Etiketler ekranı document-template kapsamı DIŞINDA. | ÇEKİRDEK | tuzak | `R:2026-08-05__belge-tasarimi-ayri-bir-yetkidir-sistem` | yok |
| deploy-kurulum | Prod'dan gelen 'dev'de yapılacaklar' notunun TEŞHİSİ tutar; 'repoda şu var / şuraya yaz / şu bekçi yeter' cümleleri VARSAYIMDIR — uygulamadan önce her biri ölçülür (2026-08-25: 6'sı ölçümle düzeltildi). | ÇEKİRDEK | değişmez | `R:2026-08-25__2026-08-25-prod-un-uc` | yok |
| fason | Çekme UYARISI sunum katmanındadır: `fason.shrinkWarnEnabled` (varsayılan AÇIK) + `fason.shrinkTolerancePct` (varsayılan %10); uyarı/onay yalnız eşik aşımında. Backend ENFORCE ETMEZ — eşiğin altındaki fark da deftere aynen yazılır. Fark 'ÇEKME' adıyla ve NÖTR renkle basılır. | PROFİL | karar | `R:2026-08-21__2026-08-21-fason-kabulu-cekme` | yok |
| fason | Fasondaki/kartelalık/emekli/sevk edilmiş top (`ALWAYS_BLOCKED`: AT_SUBCONTRACTOR, AT_KARTELA, *_CONSUMED, SHIPPED…) HİÇBİR düzeltme yoluyla (Düzelt / Etiket / dahili çağrı) değiştirilemez; kapsam topun DURUMUNDAN çözülür, `Boolean(reason)` ile genişlemez. | ÇEKİRDEK | yasak | `R:2026-07-30__top-duzeltme-tek-sozlesme-2026-07` | scripts/test_roll_edit_unified.ts |
| fason | Manuel taşıma fason adımına AT_SUBCONTRACTOR YAPMAZ — mal içeride bekler, çıkış FASON SEVK ile; fason dönüşü top yeniden sevk+kabul edilebilir. Konumu Düzelt'in inline kabulü (`FasonQuickReceivePanel`) HEP TAM kabul gönderir (`receivedQty` yok); kısmi kabul yalnız `FasonReceiveDialog`/tabletten. | ÇEKİRDEK | değişmez | `R:2026-08-19__2026-08-19-fason-kismi-kabul` | yok |
| fason | Fason kabulü haftalar önce yaratılmış topu BUGÜN depoya alır → 'buraya geliş' ≠ `createdAt`; Ham Stok dışı sekmeler `updatedAt desc` (`rollTabDefaultSortBy`, index `[status, updatedAt]`). Yeni 'topu şu statüye çek' yolu eklerken sıralama sözleşmesini hatırla — operatör topu BULABİLMELİ. | ÇEKİRDEK | tuzak | `R:2026-07-30__buraya-gelis-olusturma-2026-07-30` | Electron Rolls/service.test.ts:159 |
| fason | Fason kabulünde renk: hedef renk `wo.targetColorId`'den (renk istasyon kısıtı DEĞİL, `StationColor` deprecated); renk UYGULAMAYAN fason adımı (zımpara vb.) rengi SİLMEZ (2026-09-01). Fason kategorisinin `appliesColor`'ı 'renk veren adım' sorusunun tek kaynağıdır. | ÇEKİRDEK | değişmez | `R:2026-08-02__renk-istasyon-bazli-kisit-degil-2026` | scripts/test_helpers.ts (kilit) + scripts/test_st… |
| filtre-liste-arama | Karşılanma raporu `order.service.getCoverageForLines` KULLANMAZ — o motor fungible havuzu HER satıra tam yazar (tek sipariş ekranında doğru, raporda ÇİFT SAYIM). Rapor motoru production-balance; havuz termin ASC, terminsiz EN SONA. | ÇEKİRDEK | yasak | `A:2026-08-27__2026-08-27-siparis-gorunurlugu-serit` | yok |
| filtre-liste-arama | Aşım kesimi dalının claim WHERE'ine `currentQty > 0` KONMAZ — 0'a inmiş topta ikinci aşım kesimi meşrudur (mal fiziksel elde; çifte-harcama koruması 0'ın altında anlamsız); şart cutOpenFabric + cutWarehouseRoll aşım dallarından kaldırıldı, normal dalın `gte` guard'ına dokunulmadı. | ÇEKİRDEK | yasak | `R:2026-08-12__2026-08-12-top-listesi-filtresi` | test_tambur_over_quantity.ts (10→13; gt:0 geri ko… |
| filtre-liste-arama | Fatura izi yalnız `invoiceNo` (ERP fatura KESMEZ); izin `shipping:invoice`, `shipping:write` VERMEDEN verilir (muhasebe sevkiyat düzenleyemesin). | ÇEKİRDEK | reçete | `R:2026-08-02__2026-08-02-brut-kurali-liste` | yok |
| filtre-liste-arama | Sayfa içi arama (Ctrl+F) YOK — kayıt aramak için listenin SUNUCU araması (tüm kayıtlara bakar; DOM araması kısmi cevap verir). Yeniden denenirse: Electron `findInPage` seçeneğindeki `findNext` 'sonraki eşleşme' DEĞİL 'YENİ OTURUM BAŞLAT' (true=initial) — ters yazım hata vermez, `found-in-page` doğmaz, çubuk 0/0 der. | PROFİL | karar | `R:2026-08-06__2026-08-06-sayfa-ici-arama` | yok |
| finans-saglamlik | Fatura rejimi `shipping.invoiceMode` (varsayılan `dis` = bugünkü elle iz; `ic` = elle iz 400 INVOICE_TRACE_DISABLED, numara yalnız iç onaydan; `ikisi` = serbest + amber uyarı). İz KALDIRMA her modda açık. Kapı SERVİSTE (`assertInvoiceTraceAllowed`), route'ta değil. | PROFİL | karar | `R:2026-08-02__2026-08-02-brut-kurali-liste` | BELİRSİZ (Dilim 2 bekçisi test_feature_flag_contr… |
| finans-saglamlik | `shipping:invoice` ayrı izin kodudur; muhasebeciye `shipping:write` VERİLMEZ (sevkiyat iptal edebilir yapardı). Route `requireAnyPermission('shipping:invoice','shipping:write')` — yazma izni işareti de kapsar. | ÇEKİRDEK | karar | `R:2026-08-02__2026-08-02-brut-kurali-liste` | test_shipment_invoice.ts |
| finans-saglamlik | Çek teslim bordrosu ANLIK çıktıdır: grubu sahiplenen kaynak model yok → donmuş `PrintedDocument` üretilmez, belge no/sürüm yok (kâğıtta yazılı); bordro basmak çekin DURUMUNU değiştirmez. Donmuş sürüm istenirse önce `ChequeBatch` benzeri kaynak modeli (J kararı). | ÇEKİRDEK | karar | `R:2026-08-14__bes-saglamlik-sinifi-2026-08-14` | yok |
| finans-saglamlik | Ticaret bayrakları: `finance.blockNegativeCashEnabled` varsayılan KAPALI (satır yoksa false) — açıkken kasa eksiye düşürecek 4 İLERİ yol 409, BANKA ve TERS/storno yolları MUAF; `finance.defaultVatRate` varsayılan 20, panel `emptyLine` + mal-kabulden alış taslağı TEK kaynaktan. | PROFİL | karar | `R:2026-08-14__bes-saglamlik-sinifi-2026-08-14` | test_cash_negative_guard.ts (muafiyet asıl negati… |
| is-emri-siparis-bag | Rota şablonunun hedefi ÖNERİDİR, kilit değil; sipariş bağlı iş emrinde şablon rengi SİPARİŞİN ŞARTINI EZEMEZ — Electron `applyRouteTarget` `pickedLines.length>0` ise dokunmaz, mobil `chooseRoute` `orderLinked` ise rengi düşürmez. | ÇEKİRDEK | değişmez | `A:2026-08-06__2026-08-06-rota-sablonu-artik` | yok |
| is-emri-siparis-bag | Ölü etiket uyarısı yalnız DAR koşulda: etiketli top VE ilk adım fason (fason kabulünde top `SUBCONTRACTOR_CONSUMED`, mal YENİ barkodla döner → geçersizleşme KESİN). Engel değil bilgi; genel iptal etiket-onayı aynı gün KALDIRILDI, ikisini karıştırma. İki istemcide de aynı koşul. | ÇEKİRDEK | karar | `A:2026-08-25__2026-08-25-bitmis-kumas-tekrar`, `A:2026-08-25__2026-08-25-aksam-mobil-yeniden` | ReworkRollsDialog.test.tsx (dar uyarı koşulu) |
| kalite-istasyon-yetenek | Faz B AÇIK — türe bağlı kalan yerler: `assertWoAtStepKind(PROCESS_QC)` (R1), bypass 'sonraki adım TAMBUR' şartı (R3), mobil ekran seçimi kind→ekran (R4, APK), RollError açılış yeri (R5, partial unique 409), dashboard ham SQL (R6), `QC2_COMPLETED` semantiği (R9), roll-finalize son-adım dalı (R10). İlk farklı-topolojili… | ÇEKİRDEK | karar | `R:2026-09-03__2026-09-03-kalite-istasyon-yetenegi`, `R:undated__kursun-qc2-tek-fiziksel-istasyon`, `R:undated__hata-yasam-dongusu` | yok (plan dokümanı; regresyon yüzeyi: test_e2e_fu… |
| kalite-istasyon-yetenek | Hatanın hangi istasyonda AÇILDIĞI profildir: bugün üç yazar (kursun-qc addError :643, tambur :1870, kursunFinish :5264) hep PROCESS_QC/TAMBUR bağlamı; `roll_errors_roll_meter_defect_uq` partial unique çok-istasyonda 409 riski taşır (Faz B R5) — açıldığı istasyona yaslanan yeni yüzey yazmadan önce Faz B listesine bak. | PROFİL | tuzak | `R:undated__hata-yasam-dongusu` | yok |
| kalite-istasyon-yetenek | Seed kalite hedefleri: 1.KALİTE ve A1 → WAREHOUSE (ikisi de SATILABİLİR), FİRE → SCRAP (2026-08-20 'fire çöpe gider', migration 20260820030000). Kalite kodu koda GÖMÜLMEZ (`code === 'FIRE'` reddedildi) — kova `targetStatus`/katalogdan çözülür. | PROFİL | karar | `R:undated__uretim-akisi` | yok |
| kalite-istasyon-yetenek | Rota şablonu HEDEF saklar (`RouteStep.plannedColorId` + `RouteStepProperty`) ve hedef ÖNERİDİR, kilit değil; rotadan WO'ya: SON renk veren adım kazanır, özellikler BİRLEŞİR; renk yeteneği sorgusunda adımın KENDİ kategorisi de okunur (yoksa istasyonun varsayılan kategorisi); istemci sözleşmesi DÜZ ID dizisi; istasyon d… | ÇEKİRDEK | değişmez | `R:2026-08-06__2026-08-06-rota-sablonu-hedef` | test_route_step_targets |
| kalite-istasyon-yetenek | Koşullu etiket elemanı (`showIf`): koşul ELEMANDA yaşar, değer `QualityGrade.code` (ad DEĞİL); kalitesiz topta koşullu eleman BASILMAZ (fail-closed); tek uygulama noktası `prepareElements` — yeni emitter `expandMultilineText`'i doğrudan ÇAĞIRMAZ; koşullu alan bağlam kimliği sayılmaz. | ÇEKİRDEK | değişmez | `R:2026-08-02__2026-08-02-kosullu-etiket-elemani` | test_label_element_condition |
| kk1-idempotency-kuyruk | Yeni tarihli notu kök CLAUDE.md'ye YAZMA — tam metin `docs/history/CLAUDE-NOT-ARSIVI.md`'ye, köke yalnız `[ÇEKİRDEK]`/`[PROFİL]` etiketiyle BAŞLAYAN özet satırı. Bir alana dokunmadan önce arşivdeki TAM notu oku; kökteki satır 'dur ve arşive bak' tetiğidir. | ÇEKİRDEK | reçete | `R:undated__karar-notlari-dizini` | yok |
| modul-bayraklari-profiller | Süperadmin tam yetkisi `getEffectivePermissions` ilk ifadesi `["*"]` (rol değil); `*` körlüğü yasağı (mobil `has('*')`, Electron `hasAdminAccess`, backend `admin:*`); `isSystemAccount` panelden ATANAMAZ (tek yazar `superadmin-olustur.ts`); doğuş yalnız `npm run superadmin:kur`; kurtarma kodu yok. | ÇEKİRDEK | değişmez | `R:2026-09-03__2026-09-03-superadmin-p2-satici`, `A:2026-09-03__2026-09-03-superadmin-dogusu-p8`, `R:2026-09-04__2026-09-04-en-yetkili-hesap` | test_superadmin + test_superadmin_provision (§7 n… |
| modul-bayraklari-profiller | En yetkili hesap GÖRÜNÜR (gizleme süzgeçleri geri getirilmez); görünürlük ≠ kimlik teslimi: `/users/:id/credentials` düz PIN döner → sistem hesabını hedefleyen dar 403 kapısı DURUR; `/users/:id` önek kapısı (alt rotaların BİRLEŞİMİ) KALIR; ayar şifresi yönetim uçları 403, 404 değil. | ÇEKİRDEK | karar | `R:2026-09-04__2026-09-04-en-yetkili-hesap`, `R:2026-09-03__2026-09-03-ayar-sifresi-p3` | test_superadmin_visible (30) + test_superadmin §M |
| refakat-karti | Bayat rozeti YÜZEYLERİ yok (2026-08-06 kullanıcı kararı; Electron diyalog/önizleme bandı, mobil detay bandı/rozet); backend kolonu, işaretleyici, print-event temizliği, audit `wasDirty` DURUYOR, `contentDirty` yanıtta döner. Geri istenirse bant DEĞİL gerçek karşılaştırma (planKey + parti izi). | PROFİL | karar | `R:2026-08-05__2026-08-05-parti-no-k`, `R:2026-08-05__2026-08-05-is-emri-belgeleri` | yok |
| refakat-karti | Kart varsayılan A5 (`DEFAULT_TRAVELER_CARD_CONFIG.pageSize`); A4 panelden ya da baskı başına `?pageSize=A4\|A5` (tek seferlik: yazılmaz, versiyon doğurmaz, geçersiz değer SESSİZCE yok sayılır — 400 sahayı kâğıtsız bırakır). Sayfa boyutu KURULUM AYARIDIR: yoğun kartlı kurulum A4 seçer, kod değişmez. | PROFİL | karar | `R:2026-08-03__refakat-karti-a5-varsayilan-parti-blogu` | yok |
| refakat-karti | Refakat Kartı + Şablon Stüdyosu (ve diğer iki çıktı ekranı) izni `document-template:read/write`; `admin:settings` iki kümede KALIR (boot izni getirir, ATAMAZ); READ ⊇ WRITE; tek kaynak `document-design.ts` + Electron `permissions.ts` aynası; kart ↔ route listesi BİREBİR; salt-okumada yazma çizilmez. | ÇEKİRDEK | değişmez | `R:2026-08-05__belge-tasarimi-ayri-bir-yetkidir-sistem`, `E:undated__rbac`, `R:2026-08-03__refakat-karti-sablonu-uc-kademe-2026` | scripts/test_document_template_permission.ts (43 … |
| rota-renk-ozellik-kapsama | Kat değeri KOLONDA (`Roll/WorkOrder/ProductRecipe.foldType`), pivotta değil; biçim (`normalizeFoldType`) ↔ geçerlilik (`resolveFoldTypeForWrite`) ayrı fonksiyon; katalog boşsa FAIL-OPEN, pasif değer yazmada kabul; metre cihazı kat rolüyle BİREBİR eşleşir, yoksa `null` (başka cihaza SAPMAZ). | ÇEKİRDEK | değişmez | `R:2026-08-10__2026-08-10-11-uretim-karakteristigi` | test_fold_catalog.ts + mobil useMachinePeripheral… |
| rota-renk-ozellik-kapsama | Rota↔WO çeviri: rotadan WO'ya SON renk veren adım kazanır, özellikler BİRLEŞİR; WO'dan rotaya (`deriveStepTargets`) istasyon yeteneğiyle süzerek dağıtır; adımın istasyonu değişince hedefi SIFIRLANIR; adım güncellemesi deleteMany+create → pivot CASCADE şart. | ÇEKİRDEK | değişmez | `R:2026-08-06__2026-08-06-rota-sablonu-hedef` | test_route_step_targets.ts |
| rota-renk-ozellik-kapsama | Plan sapması KALICI deftere (`RollPlanDeviation`) yazılır, audit'e değil (audit 6 ayda arşivlenir, rapor arşivi okumaz); granülerlik onaylı geçiş × sapan alan, `confirmationId` çift-sayım kilidi (onay = COUNT DISTINCT, metraj imza başına TEK); `field` string, pg enum değil. | ÇEKİRDEK | değişmez | `R:2026-08-19__2026-08-19-2-paket-sektor` | yok |
| sebep-kataloglari | Mevcut iş emrine sonradan top EKLEME yolu YOK (`attach-rolls` 2026-06-12'de kaldırıldı): yeniden üretim her iki istemcide de YENİ iş emri açar (SAP'ta da rework ayrı emirdir). | ÇEKİRDEK | karar | `R:2026-08-25__2026-08-25-aksam-mobil-hizli` | yok |
| sevkiyat-brut-storno-cuval | [PROFİL] Bu kurulumda mühür ve rezerv YOK; `shipping.confirmationEnabled` varsayılan KAPALI → sevkiyat doğrudan DISPATCHED (yanıt dispatched=true); AÇIK → PLANNED kalır, çıkış `dispatchShipment` ile, kapı önü ara adımı YOK (PLANNED→DISPATCHED). | PROFİL | karar | `R:2026-07__cuval-depo-modeli-2026-07`, `A:undated__not-tarihsiz`, `R:undated__uretim-akisi` | yok |
| sevkiyat-brut-storno-cuval | Fatura izi `invoiceNo`+`invoicedAt`+`invoicedById` DIŞ muhasebe belgesinin izidir (tutar/KDV yok): yalnız DISPATCHED (atomik claim), `null` tarihi de temizler; izin `shipping:invoice`. finance.enabled AÇIKKEN ERP taslak fatura doğurur; çakışma `shipping.invoiceMode`. | ÇEKİRDEK | değişmez | `A:2026-08-02__2026-08-02-brut-kurali-liste`, `R:2026-08-02__2026-08-02-sevk-rakami-brut`, `R:2026-09-03__2026-09-03-dilim-2-davranis` | scripts/test_shipment_invoice.ts |
| sevkiyat-brut-storno-cuval | Brüt/donmuş-belge kuralı refakat kartına UZANMAZ — o kural para/irsaliye yüzeyine aittir; kart malla gezen operasyon kâğıdıdır: parti bloğu baskı anında canlı (`resolveLiveBatches`), sevk yalnız iptal edilmemişlerden (en yenisi + `(+N)`). | ÇEKİRDEK | yasak | `R:2026-08-03__refakat-karti-a5-varsayilan-parti-blogu` | scripts/test_traveler_card_a5_batches.ts |
| sevkiyat-brut-storno-cuval | `shipping.orderRequirement` off\|warn\|block (varsayılan `warn` = bugünkü: sevkiyat kurulur, `warnings[]` döner); `block` BUGÜN AÇILAMAZ — tablet `orderIds` göndermiyor. Kapsam FASON DOĞRUDAN SEVK'i de içerir (`orderless` kaçışı); kapı takarken 'BAŞKA çıkış yolu var mı' kardeş bayrağa bak. | ÇEKİRDEK | değişmez | `R:2026-09-03__2026-09-03-dilim-2-davranis` | scripts/test_shipping_flags.ts |
| sevkiyat-brut-storno-cuval | Sevk belgesinde müşterideki ürün adı: ad DONAR (snapshot `products[].customerName`, `cekiRows[].customerDesen/Varyant`), REJİM DONMAZ (`shipping.docItemNameMode` her baskıda canlı); zincir ETİKETLE AYNI (customerItemName > CustomerItemAlias > yok); `(itemId,colorId)` DETERMİNİSTİK; gruplama BİZİM ad | ÇEKİRDEK | değişmez | `R:2026-09-04__2026-09-04-sevk-belgesinde-musterideki` | scripts/test_shipment_doc_customer_name.ts (37, y… |
| sevkiyat-brut-storno-cuval | [PROFİL] `docItemNameMode`: `bizdeki` (varsayılan, bayt-bayt bugünkü çıktı) \| `musterideki` (FAIL-OPEN — karşılığı yoksa bizimki) \| `ikisi`; `columns[tablo].labels` kullanıcı girdisi → `applyColumnCfg` tek noktada kaçırır, boş dize = varsayılan; fasondan DOĞRUDAN sevk + Excel KAPSAM DIŞI. | PROFİL | karar | `R:2026-09-04__2026-09-04-sevk-belgesinde-musterideki` | scripts/test_shipment_doc_customer_name.ts |
| sevkiyat-brut-storno-cuval | Bitmiş top (WAREHOUSE/A1_STOCK) yeniden üretime alınabilir: `attachRolls`/`quickStart` kabul listesi STOCK/WAREHOUSE/A1_STOCK; masaüstü Bitmiş Depo 'Yeniden Üretime Al' (`ReworkRollsDialog` → `quick-start`), tablet `scanClassify` — statü kontrolü ÇUVALDAN ÖNCE (SHIPPED topa 'çuvaldan çıkarın' deme). | ÇEKİRDEK | değişmez | `R:2026-08-25__2026-08-25-bitmis-topu-yeniden`, `A:2026-08-25__2026-08-25-bitmis-kumas-tekrar` | mobil scanClassify.test.ts (12), Electron ReworkR… |
| superadmin-gizlilik-ayar-sifresi | Süperadmin için kurtarma kodu BİLİNÇLİ YOK (az yüzey > konfor; cihaz kaybı → `--rotate`); giriş kilidi süperadmini de kapsar (muafiyet = parolaya sınırsız deneme). Yeni bir 'kurtarma' yüzeyi açmadan önce P2 arşiv Q2 gerekçesini çürüt. | ÇEKİRDEK | karar | `R:2026-09-03__2026-09-03-superadmin-p2-satici` | yok |
| tambur-finalize-kesim | İş emri depo topunu da tüketebilir: `attachRolls`/`quickStart` STOCK/WAREHOUSE/A1_STOCK kabul eder (`acceptedRollStatuses`); atomik `updateManyAndReturn` claim ile. | ÇEKİRDEK | değişmez | `R:2026-07-13__2026-07-13-her-rota-final` | yok (2026-08-25 rework notunun bekçileri ayrı küm… |
| tambur-finalize-kesim | 'Durum Düzelt' / `recover-to-production` KALDIRILDI; IN_PRODUCTION'da takılı top için tek yol 'Kurtar' (`rescueStuckRoll`, `roll:manual-adjust`) — WAREHOUSE'a çeker. | ÇEKİRDEK | karar | `R:2026-07-13__2026-07-13-her-rota-final` | yok |
| tambur-finalize-kesim | Etikete katalog KODU basılır (ad değil); kat girilmemiş topta `present:false` → şablonda dursa da atlanır; ÇUVAL etiketinde kat YOK; PPLB beklentisi emitter'ın kendi dönüşümüyle (`cleanCtlCp1254`) kurulur; 'katalogda yok' sondası kataloğa eklenebilecek GERÇEK kod kullanamaz. | ÇEKİRDEK | reçete | `A:2026-08-13__2026-08-13-kesimde-kat-sessizce` | scripts/test_fold_edit_and_label.ts (16) |
| tambur-finalize-kesim | Elle top ekleme sebebi hazır katalogdan seçilir (artık DB `ReasonPreset`; `manualReasons.ts` yalnız APK zemini); serbest metin 'Diğer'/üstte kalır; sahada sürekli 'Diğer' seçiliyorsa katalog yanlıştır, liste büyütülmez. | PROFİL | reçete | `R:2026-08-04__2026-08-04-roll-foldtype-entryreason`, `A:2026-08-04__2026-08-04-topun-kalici-alanlari` | scripts/test_reason_presets.ts (sebep kümesi) |
| tambur-finalize-kesim | Tambur 'Sipariş Bağla' `workorder:write`; uyumsuz satır yalnız süpervizörde `order-links/override` zinciri (workorder:write AND roll:manual-adjust; plan+toplar+bağ); kumaş (cins) farkı HER ZAMAN red; zincir tek tx DEĞİL — bilinçli. | ÇEKİRDEK | karar | `R:2026-08-19__2026-08-19-tambur-plan-sapma` | yok (is-emri-siparis-bag kümesinde) |
| tambur-finalize-kesim | Hedef renk değişikliği (Düzenle + Rengi Değiştir) TEK bekçi `assertTargetColorChange`; kilit ADIMA değil MALA bakar (COLOR_PARTIAL_CONFIRM / COLOR_DYED_BLOCKED); rota kapsaması UYARIR (`ApiResponse.warnings`); 'renk veren adım' tek yüklem `stepCanApplyColor`. | ÇEKİRDEK | değişmez | `R:2026-08-21__2026-08-21-uretim-rengi-tek` | scripts/test_wo_target_color_guard.ts (59) — rota… |
| tambur-finalize-kesim | Kurşun dağıtımı ön koşul DEĞİL: Tambur okutması dağıtılmamış kurşunu da kapatır (bayrak AÇIK **ve** adım bypass'a uygun — `assertKursunTabletMayWrite` ile AYNI kaynak); makine atfı UYDURULMAZ (`machineId=null` + amber bant); marker `KURSUN_BYPASS_FINISHED:UNASSIGNED:` BASE ön ekiyle. | ÇEKİRDEK | değişmez | `R:2026-08-06__2026-08-06-dagitim-on-kosul` | scripts/test_kursun_unassigned_close.ts |
| tambur-finalize-kesim | Rapor çıpası `Roll.finalizedAt` (üretim çıkışı) ve `statusChangedAt` (yaşlandırma) — TRIGGER yazar (40+ çağrı noktası, uygulama koduna güvenilmez); kaynak statü listesinde SHIPPED/CANCELLED BİLEREK YOK (storno bugünün karnesine sokar); damga üzerine yazılır (kaliteyle aynı olay). | ÇEKİRDEK | değişmez | `A:2026-08-09__2026-08-09-rapor-temeli-topun` | scripts/test_quality_scorecard.ts (30) + test_db_… |
| tambur-finalize-kesim | `quality.gradeRequiredEnabled` DAR kapsam (KK1 · Tambur kalan-kuyruk · depo kesimi · WO kapanışının satılabilir dalları); fason kabulü · son-adım finalize · `cutOpenFabric` · `attachRolls` DIŞARIDA (negatif sondayla çivili) — genişletmek 'sıfır fark' ihlali, bayrak AÇILDIĞI gün patlar. | PROFİL | değişmez | `A:2026-09-03__2026-09-03-dilim-2-davranis` | scripts/test_quality_batch_flags.ts |
| top-duzeltme-iptal-fire-geri-alma | Tambur geri alması iptal ettiği parçanın SATIRINA iz yazar: cancelReasonCode=TAMBUR_GERI_ALMA (sistem kodu: seçicide çıkmaz, doğrulama kabul eder) + currentQty=0 (initialQty korunur). 08-29 öncesi kayıtlar audit kapısıyla tanınır; audit 6 ayda arşivlenince pencere KAPANIR. | ÇEKİRDEK | karar | `kod:94b937c0 (2026-08-29, BULGU-T1-011 — NOTU YOK)` | test_roll_cancel_undo :286,:430,:505 |
| top-duzeltme-iptal-fire-geri-alma | fix_tambur_undo_cancel_marker.ts (dry-run varsayılan) eski parçalara izi satıra taşır — 'pencere kapanmadan' seçeneği; KULLANICI KARARIYLA BEKLİYOR, metrajı sıfırlamaz (kaynak topun iadesi doğrulanamaz). Zorunlu değil, kalıcılık tercihi. | ÇEKİRDEK | karar | `kod:94b937c0 (2026-08-29, BULGU-T1-011 — NOTU YOK)` | yok |
| top-duzeltme-iptal-fire-geri-alma | Ölü etiket onayı KAPI DEĞİL (2026-08-25): confirmLabelPrinted kabul edilir ama hiçbir yolu açmaz; labelPrintedAt baskıda otomatik yazılır (kaldırılan şey SORU). Geri koyarken yalnız 409'u koymak bayrağı göndermeyen istemcileri kilitler. Yeniden-üretime-al'daki ölü etiket UYARISI ayrıdır. | ÇEKİRDEK | karar | `R:2026-08-25__2026-08-25-saha-deploy-sonrasi` | test_roll_cancel_undo §2/§2b (tersine çevrildi) |
| top-duzeltme-iptal-fire-geri-alma | Tambur geri alma DÖRT mod: SINGLE (tek parça) · SINGLE_RESTORE 'İş Emrine Geri Al' (tek topun metrajıyla dirilme; kardeşlere dokunulmaz; kapanış sapmaları TERSLENMEZ; RollError açılmaz; TAMBUR_PROCESSED silinir; ek izin/sebep yok; aynı-gün ayarı yok) · FULL (roll:manual-adjust + sebep) · MANUAL. | ÇEKİRDEK | değişmez | `R:2026-08-12__2026-08-12-gece-single-restore`, `R:2026-08-05__2026-08-05-manuel-top-geri` | test_tambur_undo §9 |
| top-duzeltme-iptal-fire-geri-alma | Kapanış izi: movement notes='WO_CLOSE_<ACTION>' + audit event='WO_CLOSE_DISPOSITION'; RollOperationType'a yeni değer EKLENMEZ. 'Kurtar' (rescueStuckRoll, roll:manual-adjust): IN_PRODUCTION-stuck topu WAREHOUSE'a çeker (qtyOut=qtyIn, barkod üretir). 'Durum Düzelt'/recover-to-production KALKTI. | ÇEKİRDEK | değişmez | `R:2026-07-30__wo-kapatma-kapanis-dispozisyonu-2026-07`, `R:2026-07-13__2026-07-13-her-rota-final` | test_rescue_stuck (1a: rescue → WAREHOUSE) |
| top-duzeltme-iptal-fire-geri-alma | Manuel taşıma ('Konumu Düzelt') = saha sürekliliği: top IN_PRODUCTION + currentStepId=hedef, hedef adım ACTIVE, COMPLETED WO IN_PROGRESS'e dirilir, kart yeniden aktif. Geri taşımada hedef-sonrası kalite/kurşun VOID (grade → Belirsiz), hayalet movement silinir, SKIPPED→PENDING. | ÇEKİRDEK | değişmez | `R:2026-07-30__manuel-tasima-saha-surekliligi-2026-07` | test_manual_move.ts |
| top-duzeltme-iptal-fire-geri-alma | Mutabakat kırmızısı ÜÇ ayrı şey olabilir — kod hatası (§13) · iş kararı bekleyen veri (§18) · sorgunun kör noktası (§20: adımın tek topu sonradan iptal → COMPLETED adım PENDING'e çöker; adım durumu TARİHSEL, düzeltme SORGUYA süzgeç). Eski sapma toplu UPDATE ile düzeltilmez, §13 DARALTILMAZ. | ÇEKİRDEK | karar | `R:2026-08-22__2026-08-22-mutabakat-kapisinin-kirmizisi`, `R:undated__production-canli-gercek-fabrika-gercek-veri` | test_consistency §13/§20 (§20 süzgeci EKLENMEDİ —… |
| yari-mamul | Yarı mamul için ayrı DEPO açılmaz — değişen yalnız GÖRÜNÜMdür (ayrı sekme/kolon); statü, `entrySource` ve stok mekaniği ham stokla aynı kalır. Fabrikada ayrı deponun fiziksel karşılığı yok. | PROFİL | karar | `R:2026-08-26__2026-08-26-yari-mamul-kendi` | yok |

## kod-kurallari (113)

| Küme | Kural | Sınıf | Tür | Kaynak | Bekçi |
|---|---|---|---|---|---|
| belge-etiket-sablon | buildPlan dizileri deterministik sıralanır (targetProperties, orderLinks → orderBy); orderBy'sız Prisma ilişkisi sıra garanti etmez, oynayan sıra planKey'i 'içerik değişti' yapar (değişmemiş kart her baskıda sürüm atlar). planKey anahtar sırasından bağımsız — düz JSON.stringify YASAK. | ÇEKİRDEK | tuzak | `R:2026-08-05__refakat-karti-plan-canli-sunum-donmus` | yok (dolaylı: test_traveler_card_stale §6) |
| belge-etiket-sablon | Alan bazlı belge CSS'inde (DocumentConfig.fields) calc()/var() YASAK — scaleDocCss yazı ölçeğini font-size:\s*([\d.]+)px regex'iyle uygular, calc() takılmaz, ölçek elle ayarlı alanda SESSİZCE ölür. Override yoksa TEK BAYT CSS basılmaz; her seçici '.sheet ' önekli. | ÇEKİRDEK | yasak | `R:2026-08-05__2026-08-05-belge-yerlesimi-a5` | scripts/test_doc_density_fields.ts |
| belge-etiket-sablon | columns[tablo].labels (kolon başlığı override'ı) KULLANICI GİRDİSİDİR, yerleşik label'ların aksine HTML olarak güvenli değil → applyColumnCfg TEK noktada kaçırır; yeni tablo motoru bu kaçırmayı taşımak zorunda. Boş dize = varsayılana dön (başlıksız kolon üretilmez), 40 karakterde kırpılır. | ÇEKİRDEK | değişmez | `R:2026-09-04__2026-09-04-sevk-belgesinde-musterideki` | scripts/test_document_customization.ts + test_shi… |
| belge-etiket-sablon | Etiket alan→değer eşlemesi label-field-values.ts TEK noktadır (HTML/PPLA/PPLB/ZPL ortak) — yeni emitter oradan geçer; Etiket Stüdyosu paleti backend katalogundan (label-fields.ts) beslenir, alan eklemek Electron'da kod istemez. | ÇEKİRDEK | reçete | `R:2026-08-13__2026-08-13-kesimde-kat-sessizce` | scripts/test_label_canvas_equivalence.ts |
| belge-etiket-sablon | 'Bu değer katalogda yok' diyen bekçi sondası kataloğa eklenebilecek GERÇEK bir kod kullanamaz (${PROBE}-KAT) — fabrika 6-KAT'ı ekleyince test_fold_catalog P2002 ile çöktü. Teşhis: şablonu pasifleştirip testi tekrar koş; geçiyorsa hata VERİDEN doğuyor. | ÇEKİRDEK | tuzak | `R:2026-08-13__2026-08-13-kesimde-kat-sessizce` | scripts/test_fold_catalog.ts (kendisi) |
| belge-etiket-sablon | PPLB beklentisi emitter'ın KENDİ dönüşümüyle (cleanCtlCp1254; EPL2 I8,E → gerçek Türkçe, 'Şube'→'Þube') kurulur, asciiFold ile DEĞİL — yoksa Türkçe içeren her yeni alan sahte kırmızı üretir (branchName/sackNote'ta yaşandı). | ÇEKİRDEK | tuzak | `R:2026-08-13__2026-08-13-kesimde-kat-sessizce` | scripts/test_label_canvas_equivalence.ts (kendisi) |
| belge-etiket-sablon | İstek gövdesini ELLE kuran her katman (mutationFn, offline mutations) tıpkı z.object gibi SESSİZ allowlist'tir — gövdeye alan eklerken kaynak nesnedeki her alanın geçtiğini kontrol et (tanı: aynı isteği servis/HTTP/tablet üçlüsünden atıp farkı bul). [Çapraz küme: kat/kesim] | ÇEKİRDEK | tuzak | `R:2026-08-13__2026-08-13-kesimde-kat-sessizce` | yok |
| belge-etiket-sablon | Kat (foldType) yazımı üçlü sözleşme: undefined DOKUNMA · null TEMİZLE · kod YAZ; kanoniklik daima resolveFoldTypeForWrite (ham '6 kat' kaydedilirse kat filtresi topu bulamaz, sessiz); kat değişimi labelDirty işaretler; satırı YENİDEN ADLANDIRMAK yeni değer değildir — kod KİMLİKTİR. | ÇEKİRDEK | reçete | `R:2026-08-13__2026-08-13-kesimde-kat-sessizce` | scripts/test_fold_edit_and_label.ts |
| deploy-kurulum | Partial index drift-free: şemada `@@index([col])` BIRAK, migration'da DROP + CREATE … WHERE — Prisma 7 predicate farkını drift saymaz, index↔unique farkını SAYAR → partial UNIQUE için şemada `@@unique`. 'items/customers partial'ı henüz YOK' cümlesi BAYAT (2026-08-21'den beri var). | ÇEKİRDEK | reçete | `B:undated__database-performance-rules-her-zaman-uygula`, `R:2026-08-21__2026-08-21-namefold-db-seddi` | Teks-Erp/scripts/test_db_invariants.ts |
| deploy-kurulum | Grandfathering / davranış-koruyan veri migration'ı KOŞULLU yazılır (`WHERE EXISTS (SELECT 1 FROM rolls LIMIT 1)`) ve değer = DÜNKÜ DAVRANIŞ; sabit `false` demo/boş kurulumda yüzeyleri 403'e düşürür. | ÇEKİRDEK | reçete | `R:2026-09-02__2026-09-02-03-modul-anahtarlari` | yok |
| deploy-kurulum | Seed, migration'lardan SONRA boş tabloda koşar → yeni kolonun backfill'ine seed'de GÜVENİLMEZ, değer seed'e açıkça yazılır (2026-08-10 appliesColor / 2026-09-03 appliesQuality tuzağı). | ÇEKİRDEK | tuzak | `R:2026-09-03__2026-09-03-kalite-istasyon-yetenegi` | yok |
| deploy-kurulum | Paketin TAŞIMADIĞI yola bağımlı kod yazılmaz: paket `deploy/` ve `scripts/`yi ALMAZ, `npm ci --omit=dev` tsx'i eler. Sunucu araçları `scripts/build-araclar.mjs` ile `dist/tools/*.cjs`'e derlenir (`npm run build` = tsc + araçlar); profiller TS sabitinde, `deploy/profiller` yok. | ÇEKİRDEK | değişmez | `R:2026-09-03__2026-09-03-tamlik-bekcisi-profiller`, `B:undated__commands`, `R:2026-09-03__2026-09-03-superadmin-p2-satici` | deploy/paketle.ps1:148 (dist/tools kapısı) + kur.… |
| deploy-kurulum | Transaction kısa tutulur; external I/O (HTTP, dosya) tx İÇİNDE YAPILMAZ (idle_in_transaction_session_timeout=5min). `tx.*` ile `Promise.all/allSettled` YASAK — pg adapter tek connection seri çalıştırır; ESLint kuralı yakalar. | ÇEKİRDEK | yasak | `B:undated__database-performance-rules-her-zaman-uygula` | Teks-Erp/eslint.config.mjs no-restricted-syntax |
| deploy-kurulum | Decimal kolonda JS float aritmetiği yok — DB-side `increment`/`decrement` ya da `Prisma.Decimal` (`.plus()/.minus()`). | ÇEKİRDEK | yasak | `B:undated__decimal-kolonda-js-float-aritmetigi-yok` | yok |
| deploy-kurulum | Ham SQL'de çıplak `now()` GÜVENLİ ve TERCİH EDİLEN yazım; `now() AT TIME ZONE 'UTC'` turu artık kırılgan taraf (10 yazma noktası kaldı). `test_raw_sql_hygiene` çıplak zaman fonksiyonunu yalnız gerekçeli `-- tz-ok: <neden>` ile geçirir; şablon içi açıklamada BACKTICK yok. | ÇEKİRDEK | reçete | `B:2026-08-01__sema-tip-konvansiyonu-o-11-2026` | Teks-Erp/scripts/test_raw_sql_hygiene.ts |
| deploy-kurulum | Fabrika günü = `Europe/Istanbul`, TEK KAYNAK `src/constants/time.ts` (`FACTORY_TIMEZONE`, `factoryDaySql/Start/…`); literal kopyalanmaz. TAKVİM GÜNÜ tz'ye bağlı (açık yaz) ↔ MUTLAK PENCERE bağımsız (`-- tz-ok:`). `factoryDaySql` metni değişirse `sl_day_exact` istatistiği için YENİ migration. | ÇEKİRDEK | yasak | `B:2026-08-01__fabrika-gunu-takvim-gunu-tek-kaynak` | Teks-Erp/scripts/test_report_day_boundary.ts (beş… |
| deploy-kurulum | Bekçi yazım dersleri: (1) aynı invariant'a İKİNCİ bekçi yazılmaz (muaf listeleri ayrışır) · (2) KÖRLÜK ZEMİNİ — sayım kontrolüne alt sınır, 'ihlal yok' ile 'bakılmadı' aynı yeşile çıkmasın · (3) muaflar KEŞFEDİLİR, bayatlığa karşı denetlenir · (4) negatif sonda ZORUNLU. | ÇEKİRDEK | reçete | `B:2026-08-01__sema-tip-konvansiyonu-o-11-2026`, `R:2026-09-03__2026-09-03-kalite-istasyon-yetenegi`, `R:2026-08-25__2026-08-25-prod-un-uc` | yok (meta kural) |
| fason | Çekme/fazla sapmasının TERSLEME ADRESİ `sourceRefId = receipt.id`; makbuz iptalinde sapmayı `rollId` ile SİLME — kısmi teslimatta önceki meşru sapmayı da süpürür. Fason kabulü yapan HER test cleanup'ı `rollVariance.deleteMany` içermeli (RESTRICT FK; yoksa temizlik 23001 ile yarıda kalır). | ÇEKİRDEK | yasak | `R:2026-08-21__2026-08-21-fason-kabulu-cekme` | yok |
| fason | Tolerans okumasında `<= 0 → null` kalıbı YASAK: `0` geçerli değerdir ve 'tolerans yok, her fark uyarır' demektir; `null` = 'varsayılana dön'. Susturmanın yolu eşik değil BAYRAKtır. | ÇEKİRDEK | yasak | `R:2026-08-21__2026-08-21-fason-kabulu-cekme` | yok |
| filtre-liste-arama | Liste sayfalamada VARSAYILAN cursor (keyset) + sonsuz kaydırma; offset yalnız açıkça istenirse ya da kalıcı küçük master-data'da. Yüksek hacimli tablolar (Roll, RollMovement, RollOperation, SystemLog, TravelerCardScan) HER ZAMAN cursor; skip>MAX_OFFSET(10000) → 400. | ÇEKİRDEK | değişmez | `M:undated__liste-sayfalama-default-cursor-infinite-scroll`, `E:undated__sonsuz-kaydirma-otomatik` | runtime: query-parser.ts:385 (skip>MAX_OFFSET → 4… |
| filtre-liste-arama | Cursor sözleşmesi `mode=cursor&limit&cursor&withTotal`; toplam yalnız İLK sayfada ve YAKLAŞIK (`totalEstimate`). BaseService sıralamaya HER ZAMAN `id` tie-breaker ekler (offset+keyset) → yeni sıralama index'i `(kolon, id)` BİLEŞİK olmalı; tekil kolon Incremental Sort'ta kalır. | ÇEKİRDEK | reçete | `M:undated__liste-sayfalama-default-cursor-infinite-scroll`, `A:2026-08-27__2026-08-27-siparis-gorunurlugu-serit` | yok |
| filtre-liste-arama | 'Daha Fazla Yükle' butonu ve 'Önceki/Sonraki · Sayfa X/Y' pager'ı YOK — dibe gelince otomatik yükleme. Electron: useDataTable+DataTable hazır; özel useInfiniteQuery listelerinde useInfiniteScroll (rootRef→PageBody, sentinelRef→AutoLoadMore). Mobil: FlashList onEndReached / RemoteListSheet. | ÇEKİRDEK | yasak | `E:undated__sonsuz-kaydirma-otomatik`, `M:undated__liste-sayfalama-default-cursor-infinite-scroll` | yok |
| filtre-liste-arama | Generic `buildWhereClause` CSV'yi `{in:[…]}`e otomatik çevirir — `createdById`/`entryStationId` gibi yeni id filtreleri bu yoldan geçer, `buildRollWhere`'de elle okumaya başlanırsa readIdCondition ŞART (bekçi §2b kilitler). | ÇEKİRDEK | tuzak | `R:2026-08-12__2026-08-12-giris-izlenebilirligi-kim`, `R:2026-08-06__2026-08-06-filtrelerde-coklu-secim` | test_filter_multi_select.ts §2b |
| filtre-liste-arama | Tarih aralığı: `dateField` gönderilmez ya da whitelist dışıysa `applyDateRange` aralığı SESSİZCE yok sayar (filtre seçili görünür, liste süzülmez); rolls whitelist'i yalnız `createdAt` (ROLL_DATE_FIELDS); dateField kolonunda index ŞART. `last7` BUGÜNÜ içerir, özel aralık kısa yolu EZER, query anahtarı itemLabel/now'da… | ÇEKİRDEK | tuzak | `R:2026-08-12__2026-08-12-top-listesi-filtresi`, `R:2026-08-12__2026-08-12-giris-izlenebilirligi-kim` | mobil rollHistoryFilter.test.ts (16); Electron ta… |
| filtre-liste-arama | `resolveSortBy` whitelist dışı sortBy'ı SESSİZCE fallback'e (createdAt) düşürür: `SortableHeader` basılan HER kolon `ROLL_SORTABLE_FIELDS`'te olmalı, yoksa tıklama sıralamaz VE sekmenin updatedAt varsayılanı createdAt'e kayar (hata yok, log yok). İlişki üzerinden sıralama desteklenmez — 'İstasyon' kolonu SortableHeade… | ÇEKİRDEK | tuzak | `A:2026-08-05__2026-08-05-elle-eklenen-topu`, `R:2026-07-30__buraya-gelis-olusturma-2026-07-30` | yok |
| filtre-liste-arama | `cancelledAt` yüklemi çağıranın `select`ine BAĞIMLIDIR: alan seçilmezse `undefined` gelir ve katı `=== null`/`!== null` TÜM kalemleri iptal sayar (sessiz felç — sipariş sevk yokken CANCELLED'a düşer). Ya gevşek `== null` yazılır ya da alan seçimi LOAD-BEARING yorumla korunur. | ÇEKİRDEK | tuzak | `A:2026-08-27__2026-08-27-siparis-gorunurlugu-serit` | yok (test_helpers sahte tx'i 2026-08-27'de dört s… |
| filtre-liste-arama | Kapsam zorlaması SAF katmanda (`rollHistoryFilter.forcedCreatorFilter`), ekrandaki bir `if`te DEĞİL (tersine çevrilmesi hiçbir testi kırmazdı); KK1 bunu filters'a EN SON yayar (çipten sızan createdById'yi ezer); kimlik yüklenmemişse filtre üretilmez (boş string listeyi sessizce boşaltır). | ÇEKİRDEK | değişmez | `R:2026-08-12__2026-08-12-giris-izlenebilirligi-kim` | mobil rollHistoryFilter.test.ts |
| finans-saglamlik | `attachTotals` üç sayımı TEK anlık görüntüde alır: batch `$transaction` + RepeatableRead (READ COMMITTED'da her ifade ayrı görüntü → iade çift sayılır ya da kaybolur). `Promise.all` bunu sağlamaz. | ÇEKİRDEK | tuzak | `R:2026-08-02__2026-08-02-brut-kurali-liste` | yok (F-SEV-ESZ-002 denetim bulgusu; adlı bekçi do… |
| finans-saglamlik | Atomik `updateMany` claim'i 0 dönünce sebep tx İÇİNDE taze okumayla söylenir ve tanı sayaç kaynaklarında SİMETRİK (`explainCheque/Invoice/PaymentBumpZeroTx`); tek kaynağa yazıp diğerlerini bırakmak yarışın kaybedenine 'tavan aşıldı' YALANI bastırır. | ÇEKİRDEK | değişmez | `R:2026-08-14__bes-saglamlik-sinifi-2026-08-14` | test_payment_allocation.ts |
| finans-saglamlik | Satır doğuran her tx'in İLK işi kap-belgesini status şartıyla claim'lemek (`createInitialEntry.txGate` F221 deseni; GR addLines ‖ cancel); iptalin top kümesi claim'den SONRA tx İÇİNDE taze okunur. | ÇEKİRDEK | değişmez | `R:2026-08-14__bes-saglamlik-sinifi-2026-08-14` | test_goods_receipt_invoice.ts §10/§11 |
| finans-saglamlik | Eşzamanlı aynı-`clientToken` çift gönderim PO deseniyle kapanır: `withBarcodeRetry`'a `!isClientTokenP2002` yüklemi + tx-dışı catch → findUnique → CACHED yanıt; cached yanıtın şekli normal yanıtla BİREBİR (ayrışırsa replay istemciden ayırt edilir). 5 finans ucunda uygulandı. | ÇEKİRDEK | reçete | `R:2026-08-14__bes-saglamlik-sinifi-2026-08-14` | BELİRSİZ (adlı bekçi doğrulanmadı) |
| finans-saglamlik | Yarış bekçisinde pencere ELLE AÇIK TUTULAN tx ile kurulur (`Promise.allSettled` pencereyi ıskalar, sahte-yeşil); sonda kilidi bilinçli `FOR NO KEY UPDATE` (`FOR UPDATE` FK KEY SHARE'i bloklar, sonda kendini kilitler); 'bekledi' süre ölçümü yalnız pozitif yönde anlamlı, kırmızı SONUÇ kontrolüdür. | ÇEKİRDEK | reçete | `R:2026-08-14__bes-saglamlik-sinifi-2026-08-14` | emsal: test_goods_receipt_invoice.ts §10/§11 |
| finans-saglamlik | Bekçide henüz await edilmemiş gate-tx promise'ine no-op `.catch` konur (`void tx.catch(() => undefined)`); yoksa erken red sahipsiz rejection olur ve süreç Sonuç satırı basılmadan ölür — en sessiz kırmızı. | ÇEKİRDEK | tuzak | `R:2026-08-14__bes-saglamlik-sinifi-2026-08-14` | yok (bekçi yazım kuralı) |
| is-emri-siparis-bag | Tip çevrimi ATOMİK: `updateMany WHERE {id, type: eski}` + tx içinde TAZE bağ sayımı (`count===0` ise dokunma); `findUnique→if→update` yasak. Değişim sessiz olmaz: yanıt `typeChanged`, mesajda söylenir, audit `oldData.type/newData.type` (künyede 'Ne değişti' satırı). | ÇEKİRDEK | değişmez | `R:2026-08-21__2026-08-21-is-emri-tipi` | test_workorder_order_link.ts (typeChanged/mesaj/a… |
| kalite-istasyon-yetenek | Saf yüklem Prisma WHERE'inin yerine KONMAZ — sorgu DB'de koşar; 'önce oku, yüklemle kontrol et, sonra update' biçimi `setQueueUrgent`in F167 atomik claim'ini check-then-act yarışına geri döndürür. | ÇEKİRDEK | yasak | `R:2026-09-03__2026-09-03-kalite-istasyon-yetenegi` | test_station_quality_capability §3 |
| kalite-istasyon-yetenek | `QUALITY_STATION_WHERE` hijyeni: `satisfies` ile yazılır (`as const` DEĞİL — readonly literal `OR:` konumunda Prisma input tipine oturmaz); nesne MUTATE EDİLMEZ, spread ile kopyalanır; spread'in bulunduğu kapsama ikinci `OR:` YAZILMAZ (biri sessizce kaybolur) — gerekirse `AND: [{...WHERE}, {OR:[…]}]`. | ÇEKİRDEK | yasak | `R:2026-09-03__2026-09-03-kalite-istasyon-yetenegi` | test_station_quality_capability §6 |
| kalite-istasyon-yetenek | Yüklemi çağıran her Prisma select `STEP_QUALITY_SELECT`'i spread eder (alan atlamasın); `STEP_CAPABILITY_SELECT` kaliteyle GENİŞLETİLMEZ — dar select kuralı: yalnız `appliesColor` okuyan çağıran `appliesProperty` seçmeye zorlanmaz. | ÇEKİRDEK | değişmez | `R:2026-09-03__2026-09-03-kalite-istasyon-yetenegi`, `R:2026-08-10__2026-08-10-11-uretim-karakteristigi` | test_station_quality_capability §5 (dosya başına … |
| kalite-istasyon-yetenek | Bekçide muaf/taban kontrolleri DOSYA bazlı değil SAYIM bazlı yazılır: dosya bazlı muaf o dosyaya eklenen yeni tür kontrolünü sessizce kapsar; 'dosyada X geçiyor mu' ikinci select'in alan atlamasını gizler. 'kind seçen her select yüklem içindir' varsayımı yanlış — görüntüleme select'leri (currentStepKind) de kind okur. | ÇEKİRDEK | tuzak | `R:2026-09-03__2026-09-03-kalite-istasyon-yetenegi` | test_station_quality_capability (S3/S4 negatif so… |
| kalite-istasyon-yetenek | `setCapabilities` renk tarafına yalnız `colorIds` AÇIKÇA gönderilirse dokunur: `[]` hepsini siler, alan hiç yokken mevcut satırlara dokunulmaz. | ÇEKİRDEK | tuzak | `R:2026-08-02__renk-istasyon-bazli-kisit-degil-2026` | test_station_capability (8b — bölüm BELİRSİZ) |
| kesif-cihaz-ag | Grup içi adres sırası: adres tercihi → `tieBreak` → gecikme (rttMs) → ad. Son basamak (ad) DETERMİNİZM içindir — atlanırsa eşit adaylarda sıra turdan tura oynar ve 'otomatik seçilen adres' de oynar. | ÇEKİRDEK | reçete | `R:2026-09-04__2026-09-04-kesif-bir-satir` | Electron/src/test/discovery-logic.test.ts |
| kesif-cihaz-ag | Cihaz satırı tavanı REJİMDEN BAĞIMSIZ olmalı (`MAX_DEVICE_ROWS`): `MAX_PENDING_DEVICES` kapalı rejimde PENDING sayacı hiç artmadığı için sessizce etkisizleşir. Genel kural — bir kapıyı bayrağa bağlarken sor: bu bayrak KOMŞU kapının saydığı şeyi sıfırlıyor mu? | ÇEKİRDEK | tuzak | `R:2026-09-04__2026-09-04-cihaz-onay-kapisi` | test_device_pairing_flag.ts (toplam tavan silinin… |
| kesif-cihaz-ag | Derlenmiş paket üzerinden yapılan istemci çıkarımı o paketin YAŞINI ölçmez: mobil `usePermission` global joker (`["*"]`) kestirmesi KALDIRILMAZ — 2026-09-04'te 'kaldırın' önerisi ölçümle çürütüldü, tablette ESKİ BUNDLE koşuyordu. | ÇEKİRDEK | tuzak | `R:2026-09-04__2026-09-04-cihaz-onay-kapisi` | mobil/src/hooks/usePermission.test.ts |
| kesif-cihaz-ag | 'Yazmaz' kontrolü, kayıt zorlanan değerle AYNI başlatılırsa VAKUMDUR — sızan yazım mevcut değeri yazar ve bekçi fark etmez. Sonda kurarken başlangıç değerini bilerek TERS seç. | ÇEKİRDEK | tuzak | `M:2026-08-19__yon-tercihi-cihazda-2026-08-19` | yok |
| kesif-cihaz-ag | Mobil kendi izin union'ını TAŞIR (`mobil/src/types/permissions.ts`): ayrı proje, backend katalogunu import edemez — mobil ekran izni eklerken oradaki liste ELLE güncellenir ve bekçi orayı TARAMAZ. Aynı sınıf: mobil `Electron/shared/discovery.ts`i de import edemez (→ KEŞİF-İKİZ). | ÇEKİRDEK | tuzak | `B:undated__katalog-yine-atama-icermez`, `R:2026-09-04__2026-09-04-kesif-bir-satir` | discovery için var (contract.test), izin union'ı … |
| kk1-idempotency-kuyruk | Durum geçişleri ATOMİK CLAIM ile yazılır: `updateMany WHERE {id, beklenen-durum}` + `count===0 → 409`. `findUnique → if → update` (check-then-act) YASAK. | ÇEKİRDEK | yasak | `R:2026-07-14__idempotency-2026-07-14` | yok |
| kk1-idempotency-kuyruk | Token replay'i DÖRT durumludur ve tek kaynağı `helpers/token-replay.helper.ts`: yazılmadı → yaz · aynı yük → cached dön · BAŞKA yük → 409 `CLIENT_TOKEN_COLLISION` · kayıt İPTAL/ölü → 409 `ENTRY_CANCELLED`. 'success:true + iptal edilmiş barkod' sessizce yanlış cevaptır. | ÇEKİRDEK | değişmez | `R:2026-07-14__idempotency-2026-07-14`, `R:2026-08-05__2026-08-05-manuel-top-geri` | yok |
| kucuk-kumeler | Kapalı modül HER ZAMAN 403 döner ve kod `details.code = "MODULE_DISABLED"` altındadır (`body.code` hep undefined okur — sahte yeşil). 404 dönmek destek ekibini 'uç deploy edilmemiş' diye yanlış yöne gönderir; uzakta bile 403 kalır. | ÇEKİRDEK | değişmez | `B:undated__uc-bir-module-aitse-router-adlandirilmis`, `R:2026-09-01__2026-09-01-patron-modulu-fabrikaya` | test_module_flag_off §1d/§4 |
| kursun-planlama-bypass | Toplu uçları (`assign-bulk`/`cancel-bulk`, ≤100 satır) hepsi-ya-hiç tek transaction'a ÇEVİRME: her satır kendi tx'inde koşar, atlanan her satır somut sebebiyle `failed[]` içinde döner ve arayüz onu gösterir. '42 atandı' deyip 8'inin sebebini yutmak en kötü davranıştır. | ÇEKİRDEK | değişmez | `R:2026-08-05__2026-08-05-kursun-planlama-birlesik` | scripts/test_kursun_bulk.ts (28 kontrol; hepsi-ya… |
| kursun-planlama-bypass | Kalıcı sayaç/rapor `SystemLog`'tan değil KALICI İZDEN okunur (burada movement marker'ı) — `archive-scheduler` audit'i 6 ayda taşır ve rapor sessizce sıfırlanır. Emsal: `Roll.entryReason` kolonu. | ÇEKİRDEK | değişmez | `R:2026-08-06__2026-08-06-dagitim-on-kosul` | yok |
| kursun-planlama-bypass | `completeUnassignedFromTambur`'un tx İÇİ tazelemesi (`resolveUnassignedTamburClosure(tx)`) bir DERİNLİK SAVUNMASIDIR ve bekçi ona erişemez — tek iş parçacıklı testte tx dışı ön kontrol her zaman önce reddeder, körleştirilse test yeşil kalır. Silmeden önce yerine ne koyduğunu bil. | ÇEKİRDEK | tuzak | `R:2026-08-06__2026-08-06-dagitim-on-kosul` | yok (bilinçli — arşivde yazılı) |
| kursun-planlama-bypass | Hata mesajı zenginleştirmesi YALNIZ gerçekten fırlatılacak dalda koşar: `explainTamburScanBlock` sebep sorgusu bypass rejiminin EN SIK yolunda (bypassPending dolu, hata yutuluyor) boşa koşmamalı. | ÇEKİRDEK | reçete | `R:2026-08-06__2026-08-06-dagitim-on-kosul` | yok |
| modul-bayraklari-profiller | Modüle ait router ADLANDIRILMIŞ kapı taşır: `require<Alan>Enabled` (`module.middleware.ts`, argümansız `readXEnabled()`, cache'siz); jenerik `requireModule("x")` YASAK — bekçi adı AST/metin arar. Kapısız uç gerekçesiyle başlıkta/`PANEL_EXEMPT`te yazılı; yer tutucu modüle middleware/panel eklenmez. | ÇEKİRDEK | yasak | `R:2026-09-02__2026-09-02-03-modul-anahtarlari`, `B:undated__uc-bir-module-aitse-router-adlandirilmis` | test_{ticaret,iplik,depo_multi,production}_regime… |
| modul-bayraklari-profiller | Kapalı modül 403 + `details.code="MODULE_DISABLED"` + `details.modul` (bağımlıda `dependent`); kod TOP-LEVEL değil — `body.code` hep undefined. Tünel ayrımı YOK: uzaktaki 404 yalnız REMOTE_DENIED listesidir, modül kapısı orada değil. | ÇEKİRDEK | değişmez | `R:2026-09-02__2026-09-02-03-modul-anahtarlari`, `B:undated__uc-bir-module-aitse-router-adlandirilmis` | test_module_flag_off §1h/§4 (AST details.code + d… |
| modul-bayraklari-profiller | Kapı, tek yazarın İÇİNDE olmalı: `applyYarnMovementTx`'in ilk ifadesi `readIplikEnabled(tx)` (route kapısı yetmedi — ticaret açık + iplik kapalı kurulumda kg defteri doluyordu); çağıranlara ikinci kopya kontrol EKLENMEZ. Köprü bayrağı tek resolver `resolveYarnOutOnInvoiceEnabled`. | ÇEKİRDEK | tuzak | `R:2026-09-02__2026-09-02-03-modul-anahtarlari` | test_iplik_regime_gate |
| modul-bayraklari-profiller | 'Bekçi yeşil ≠ kapı doğru': kapı kapsamı elle sayılmaz, AST ile ölçülür; her bekçi negatif sondayla kırmızı verdiği görülerek sayılır (HTTP ayağı çalışan sürecin kodunu ölçer, kaynak bozulunca yeşil kalır); kapı taşıma ile bekçi güncellemesi AYNI commit'te. | ÇEKİRDEK | tuzak | `R:2026-09-02__2026-09-02-03-modul-anahtarlari`, `R:2026-09-03__2026-09-03-dilim-2-davranis` | yok |
| modul-bayraklari-profiller | §3.6 tek resolver (`resolveXEnabled = modülAçık && altBayrak`) YALNIZ ebeveyni (modül anahtarı) OLAN 7 bayrağa; çekirdek/ebeveynsiz bayrağa (kk1.*, sevkiyat, ana veri) resolver YAZILMAZ — ölü sabit doğurur. Motor çekirdek, sunum modülde. | ÇEKİRDEK | yasak | `R:2026-09-03__2026-09-03-dilim-2-davranis` | yok (helper başlık yorumları) |
| modul-bayraklari-profiller | Feature-flag sözleşmesi ÜÇ AYAK `aBool`/`aNum`/`aEnum`: her anahtar A(api Zod)+B(`SETTING_KEYS`)+C(`setFeatureFlags`)+panel; sayısal alan `FlagDef.numberField` iç adı `numberKey:` ZORUNLU (`key:` regex'e sızar); enum şeması KAPALI küme + panel `enumKey` + çöp-değer sigortası. | ÇEKİRDEK | reçete | `B:2026-08-19__sayisal-feature-flag-eklerken-2026-08`, `R:2026-09-03__2026-09-03-dilim-2-davranis`, `R:2026-08-19__2026-08-19-2-paket-sektor` | test_feature_flag_contract (§16 dahil) |
| modul-bayraklari-profiller | Sayısal ayarda 'değeri temizle' 0 yazılarak kurulur (`set()` null/JsonNull almaz); `<=0 → null` okuma kalıbı ALAN BAZLIDIR: kısa-kesim eşiğinde 0 = girilmemiş, fason toleransında 0 = 'tolerans yok' (kalıp orada YASAK). Yeni sayısal anahtarda 0'ın anlamı kararlaştırılır. | ÇEKİRDEK | tuzak | `B:2026-08-19__sayisal-feature-flag-eklerken-2026-08` | yok |
| mukerrer-namefold | Kapsamlı ad kuralı olan tabloda sed BİLEŞİK olur: `machines` seddi `(stationId, nameFold)`dir — düz `nameFold` seddi 'Makine 1' adını fabrikada tek bir istasyona hapsederdi. | ÇEKİRDEK | tuzak | `KOD:20260831120000_namefold_sed_kalan_tablolar` | test_master_data_name_dup §10d |
| mukerrer-namefold | Sabit adla master-data yaratan test fixture'larını damgala (rastgele sonek): sed kurulduktan sonra kalan ad ikinci koşumu P2002'ye düşürür. Fold bekçisi fixture'ında sonek HARFE yapıştırılır (`X${suffix}`) — renk kuralında rakam blokları kendi sırasını korur, salt rakamlı sonek sondayı çürütür. | ÇEKİRDEK | tuzak | `R:2026-08-21__2026-08-21-namefold-db-seddi`, `R:2026-08-25__2026-08-25-prod-un-uc` | yok |
| mukerrer-namefold | Tombstone'a dokunan her yüzey onu DIŞLAR: `ColorService.assertNameAvailable` (aksi 'PASİF var, aktifleştirin' deyip birleşmişi diriltmeye davet eder), `import-name-guard` önizlemesi, `seed-fixtures` (diriltmez) ve `test_consistency` §18 customers sorgusu (yoksa her birleştirme kırmızı yapar). | ÇEKİRDEK | tuzak | `R:2026-08-21__2026-08-21-namefold-db-seddi`, `R:2026-08-25__2026-08-25-prod-un-uc` | test_consistency §18, test_master_data_name_dup §9 |
| mukerrer-namefold | Bulanık ad eşleştirmede birim KELİMEdir, karakter DEĞİL: Jaro-Winkler/trigram isim alanlarında yanıltıcıdır (ön ek bonusu 'BOYER EMRE'↔'BOYER'i 0.900 yapıyordu). Skor sıralı kelime hizalaması + profil: FIRM = gürültüsüz çift ORTALAMASI, PRODUCT = kelime/numerik eşitliği + EN DÜŞÜK çift. | ÇEKİRDEK | değişmez | `R:2026-08-22__2026-08-22-mukerrer-paneli-v2` | test_duplicate_detection |
| mukerrer-namefold | Kimlik kuralı 'aynı değer = aynı tüzel kişi' varsayar ve bu yalnız değer AZ kayıtta geçerken güçlüdür: bir değer `IDENTITY_MAX_GROUP` (=4) kayıttan fazlasında geçiyorsa YER TUTUCU sayılır ve çift ÜRETMEZ. Bastırma SESSİZ OLAMAZ — yanıtta `suppressedIdentities` ile raporlanır. | ÇEKİRDEK | değişmez | `KOD:bba838ce (2026-09-01)` | yok |
| mukerrer-namefold | Bulanık ad kuralı KAYIT AİLESİNİN YAPISINA göre profillenir; tek genel skor ya gürültü ya körlük üretir. Eşik okunabilir ölçek olmalı (%100 yazım · %90 neredeyse aynı · %80 tek harf) ve 'fazladan anlamlı kelime' hiçbir eşikte aday olmamalı; ayar dört kapıdan geçer. | ÇEKİRDEK | karar | `R:2026-08-22__2026-08-22-mukerrer-paneli-v2` | test_duplicate_detection + test_feature_flag_cont… |
| parti-no-batch | Kısa parti no İKİ HANE DOLGULUDUR (`P01`), günlük parti no DOLGUSUZDUR (hane serbest, 9999/gün tavanı yok); diğer tüm günlük kodlar (SIP/İE/CV/RK/FS) 4 hane dolgulu KALIR — onlar okutulur, parti no okutulmaz. | ÇEKİRDEK | değişmez | `R:2026-07-13__is-emri-no-parti-2026-07` | scripts/test_batch_number_format.ts §3/§4 (dolgu … |
| raporlar | Toplu geçmiş düzeltmesi (backfill) HAM SQL yazar: Prisma update updatedAt'i tazeler ve envanter sekmelerinin 'Son İşlem' sıralaması o kolondan çözülür → script sahadaki HER listeyi yeniden sıralar. Backfill kuralı trigger ile BİREBİR aynı olmalı. | ÇEKİRDEK | reçete | `R:2026-08-09__2026-08-09-rapor-temeli-finalizedat` | yok |
| raporlar | Katalog değeri koda gömülmez: 'en üst kalite' quality_grades.sortOrder'dan çözülür, code === '1.KALITE' araması kaliteyi yeniden adlandıran fabrikada SESSİZCE boş karne üretir. Aynı kalıp her katalog anahtarı için geçerli. | ÇEKİRDEK | yasak | `R:2026-08-09__2026-08-09-rapor-temeli-finalizedat` | test_quality_scorecard.ts (kaynak taramasıyla mek… |
| raporlar | tsc ÇIKTISI BORUDA DA RENKLİDİR: `typecheck \| grep 'error TS'` HİÇ eşleşmez (ANSI kodu araya girer; 6 hata 'temiz' raporlandı). Temiz/kirli ölçüm birimi ÇIKIŞ KODUDUR (`npm run typecheck; echo $?`); metin süzeceksen typecheck:plain. | ÇEKİRDEK | ölçüm | `B:2026-08-13__tsc-ciktisi-boruda-da-renklidir-temiz` | yok |
| raporlar | Casing hataları (AppError ↔ app-error.ts) için EK ÖNLEM GEREKMEZ — forceConsistentCasingInFileNames açık ve tsc TS2307 ile yakalar; macOS'ta 'derlendi sanma'nın tek sebebi yukarıdaki grep tuzağıydı. | ÇEKİRDEK | karar | `B:2026-08-13__tsc-ciktisi-boruda-da-renklidir-temiz` | tsc TS2307 |
| raporlar | Script çıkışı: `$disconnect()` TEK BAŞINA YETMEZ — havuz idleTimeoutMillis 600_000, idle handle event loop'u 10 dk açık tutar, koşucu 180 sn'de SIGTERM'ler (ZAMAN AŞIMI). Ya process.exit(fail>0?1:0) ya `$disconnect()+pool.end()` (uzun çıktıda ikincisi). | ÇEKİRDEK | tuzak | `B:undated__cikis-tek-basina-yetmez` | yok |
| refakat-karti | Bayt-bayt korunan belge çıktısında kapalı blok TEK BAYT basmaz (tablo da CSS de koşullu emit) ve `${…}` kendi satırına konmaz — boş satır A4/eski-belge parmak izini bozar; opsiyonel yeni alan eski donmuş snapshot'ta satır/kolon üretmez, geriye dönük doldurma YAPILMAZ (tazeleme yalnız `reissue`). | ÇEKİRDEK | tuzak | `R:2026-08-03__refakat-karti-a5-varsayilan-parti-blogu`, `R:2026-08-05__2026-08-05-parti-no-k` | scripts/test_traveler_card_a5_batches.ts · test_f… |
| rota-renk-ozellik-kapsama | Rota adımı istemci sözleşmesi DÜZ ID DİZİSİ (`plannedPropertyIds`); `RouteService.ALLOWED_STEP_KEYS` nested write'ı (`plannedProperties`/connect/deleteMany) REDDEDER — F209 seddi; çeviriyi servis yapar. | ÇEKİRDEK | yasak | `R:2026-08-06__2026-08-06-rota-sablonu-hedef` | test_route_step_targets.ts (allowlist gevşeyince … |
| rota-renk-ozellik-kapsama | Kapı alanı eklerken ÜÇ Zod şeması (`finalizeSchema`/`cutOpenFabricSchema`/`finalizeOpenFabricSchema`) VE mobil offline `mutations.ts` gövdesi birlikte güncellenir — Zod tanımadığı anahtarı sessizce siler, kuyruk gövdeyi elle kurar; idempotent-retry erken dönüşleri kapıdan ÖNCE koşar. | ÇEKİRDEK | reçete | `R:2026-08-19__2026-08-19-tambur-plan-sapma` | mobil mutations.test.ts (sözleşme sondası) |
| sebep-kataloglari | Sebep kodu çözümü tx DIŞINDA yapılır: `applyRollDispositionsTx` gibi tx içi motorlar katalog OKUMAZ, hazır kodu çağıran geçirir (senkron/async ayrımı bozulmasın). | ÇEKİRDEK | reçete | `R:2026-08-21__2026-08-21-namefold-db-seddi` | yok |
| sebep-kataloglari | Senkron katalog okumasında TTL yalnız TAZELİĞİ ölçer, GEÇERLİLİĞİ değil: bayat liste olduğu gibi döner ve bayatlık okumayı düşürmek yerine arka planda bir tazeleme TETİKLER (çağıran beklemez). | ÇEKİRDEK | değişmez | `R:2026-08-26__2026-08-26-fabrikanin-ekledigi-sebep` | test_reason_presets §3b |
| sebep-kataloglari | Bayatlık ≠ boşluk: önbellek HİÇ dolmadıysa (ya da kind boşsa) kod kataloğuna düşülür ve fabrika kodu reddedilir — elde doğrulanacak bir şey yokken FAIL-CLOSED doğrudur. | ÇEKİRDEK | değişmez | `R:2026-08-26__2026-08-26-fabrikanin-ekledigi-sebep` | test_reason_presets §3d |
| sebep-kataloglari | Tek-uçuş (dedup) YALNIZ arka plan tazelemesine aittir; `refreshReasonPresetCache` o kapıdan geçirilmez — create/update/duplicate/reorder kendi yazdığını GÖRMEK zorundadır, uçuştaki eski okumaya iliştirilemez. | ÇEKİRDEK | yasak | `R:2026-08-26__2026-08-26-fabrikanin-ekledigi-sebep` | yok |
| sebep-kataloglari | Arka plan tazelemesi DB'ye ulaşamazsa ELDEKİ liste korunur ve kısa bir geri çekilme uygulanır (`refreshBlockedUntil`/`REFRESH_RETRY_MS`) — DB kapalıyken her doğrulama yeni sorgu açmasın. Async metin-kind yolu (`rowsForTextKind`) da aynı dayanıklılığı taşır. | ÇEKİRDEK | reçete | `R:2026-08-26__2026-08-26-fabrikanin-ekledigi-sebep` | yok |
| sebep-kataloglari | Geçersiz sebep kodu/eksik metin bir İSTEMCİ hatasıdır: `AppError.badRequest` + `details.code` (`REASON_CODE_INVALID` / `REASON_TEXT_REQUIRED`). Düz `Error` ATILMAZ — middleware onu 500'e çevirir, operatör sebebi göremez ve mobil kuyruk 5xx'i geçici sanıp üç kez daha dener. | ÇEKİRDEK | yasak | `R:2026-08-26__2026-08-26-fabrikanin-ekledigi-sebep` | test_reason_presets §3c |
| sebep-kataloglari | Doğrulama kuralı kapının KENDİSİNDE yaşar; çağıran taraftaki elle try/catch sarmalayıcıları kaldırılır — sarmalayıcı `details.code`'u yutar ve hata sınıfını kaybettirir. | ÇEKİRDEK | karar | `R:2026-08-26__2026-08-26-fabrikanin-ekledigi-sebep` | yok |
| sebep-kataloglari | `expireReasonPresetCacheForTest` (satırları KORUR, yalnız bayatlatır) ile `invalidateReasonPresetCache` (satırları da düşürür) KARIŞTIRILMAZ; bekçi ikisini ayırmazsa düzeltmeyi değil başka bir şeyi ölçer. | ÇEKİRDEK | tuzak | `R:2026-08-26__2026-08-26-fabrikanin-ekledigi-sebep` | test_reason_presets §3b/3d |
| sebep-kataloglari | Önbellek davranışı ölçen bekçi doğrulamadan hemen ÖNCE tazeleme yapmaz (ölçümünü hep taze pencerede yapar, arızayı göremez); 'kendini onarır' ölçümü önbelleğin GÖRMEDİĞİ bir satırla (servisle değil doğrudan prisma ile yazılan) kurulur — 'kod kabul edildi mi' sorusu bayat listeyle de EVET der. | ÇEKİRDEK | tuzak | `R:2026-08-26__2026-08-26-fabrikanin-ekledigi-sebep` | test_reason_presets §3b/c/d (dört negatif sonda) |
| sevkiyat-brut-storno-cuval | Belge kolonu/bölümü İÇ veri taşıyorsa OPT-IN: renderer `DocCol.defaultHidden` + `shown` allowlist, panel `DocSectionDef.defaultHidden`; `hidden`/`sections` BLOCKLIST'tir — naif kolon müşteri irsaliyesinde varsayılan GÖRÜNÜR doğar. İki taraf birlikte işaretlenir. | ÇEKİRDEK | tuzak | `R:2026-07-30__2026-07-30-cuval-notu-sack`, `E:2026-07-30__belge-kolonu-ekleme-ic-veri-tasiyorsa`, `A:2026-08-05__2026-08-05-parti-no-k` | scripts/test_shipment_doc_batch_column.ts |
| sevkiyat-brut-storno-cuval | Brüt geri-ekleme tuzakları: (1) çuval satırı + özet + çeki TEK gross kaynağından (`grossRolls`/`sacksGross`); (2) ana sorgu ile iade sorgusu tx'siz → canlı id kümesiyle DEDUP şart; (3) `totalKg` DEĞİŞMEZ. Sentetik satır `sackId=prevSackId` taşır; `prevSackId`siz eski iade ATLANIR. | ÇEKİRDEK | tuzak | `R:2026-08-03__2026-08-03-brut-kurali-sevkiyat`, `R:2026-08-05__2026-08-05-belge-yolu-da` | scripts/test_shipment_detail_gross.ts (33), test_… |
| sevkiyat-brut-storno-cuval | `dispatchedAt` sıralamasında union'ın iki tarafında alan adı FARKLI (`Shipment.dispatchedAt` nullable ↔ `DirectShipment.shippedAt` NOT NULL): direct dalı için `orderBy` VE cursor `where` AYRI kurulur; nulls-last (`sortNullable`). Muhasebe listesi varsayılanı `dispatchedAt desc`. | ÇEKİRDEK | tuzak | `A:2026-08-02__2026-08-02-brut-kurali-liste`, `R:2026-08-02__2026-08-02-sevk-rakami-brut` | scripts/test_shipment_list_sort.ts |
| sevkiyat-brut-storno-cuval | Sevk topun önceki rafını `Roll.preShipStatus`a yazar (statüye göre gruplu updateMany), storno oradan döner ve NULL'lar; körlemesine WAREHOUSE'a döndürmek 2. kalite (A1_STOCK) topu 1. kalite rafına yazar. NULL (karar öncesi sevk) → WAREHOUSE. Aile: `prevSackId/prevQualityGrade`, `preCancelStatus`. | ÇEKİRDEK | tuzak | `R:2026-08-05__2026-08-05-sevk-storno-iade` | scripts/test_shipment_undo_dispatch.ts (preShipSt… |
| sevkiyat-brut-storno-cuval | `freezeForSource` versiyonu `max+1` (findFirst orderBy version desc → (prev?.version ?? 0)+1); sabit `version:1` bir kaynağın İKİNCİ dondurulmasında (storno sonrası yeniden sevk) `docType_sourceId_version` unique'ine çarpıp tam sevk anında 500 verir. | ÇEKİRDEK | tuzak | `R:2026-08-05__2026-08-05-sevk-storno-iade` | scripts/test_shipment_undo_dispatch.ts (version:1… |
| sevkiyat-brut-storno-cuval | Filtrede iki değerli NOT NULL enum'a çoklu seçim EKLENMEZ: `destination` çoklu seçilince `listShipments` DirectShipment'ları union'dan düşürür; kapsam anahtarları (`shipmentScope`, `invoiced`, `hasReturns`) if/else dalıdır. Elle okunan her id filtresi `readIdCondition`/`readFilterList`'ten geçer. | ÇEKİRDEK | yasak | `R:2026-08-06__2026-08-06-filtrelerde-coklu-secim` | scripts/test_filter_multi_select.ts |
| sevkiyat-brut-storno-cuval | Bekçi ortamdaki veriye BAĞIMLI OLMAZ: `findFirst()` ile 'herhangi bir çuval/top' bulup üzerine test kurma — dev DB dolu olduğu için yerelde geçer, temiz CI DB'sinde düşer; fixture'ı test kendisi yaratır (CI: `npm run seed` + `seed:fixtures`). | ÇEKİRDEK | yasak | `B:undated__ortamdaki-veriye-bagimli-olma` | yok |
| superadmin-gizlilik-ayar-sifresi | `router.use("/users/:id", verifyToken, requireAnyPermission("admin:users","admin:settings"), protectSystemAccountTarget)` ÖNEK kapısı SİLİNMEZ (yeni uç kapısız doğmasın); önek izin kümesi alt rotaların BİRLEŞİMİ, daraltmayı alt rotanın kendi zinciri yapar. | ÇEKİRDEK | değişmez | `R:2026-09-04__2026-09-04-en-yetkili-hesap`, `R:2026-09-03__2026-09-03-superadmin-p2-satici` | test_superadmin §M (AST: önek kümesi ⊇ her alt ro… |
| superadmin-gizlilik-ayar-sifresi | `*` jokerini HER kapı tanır: yüklem `matchesPermission`; backend `includes("admin:*")` YASAK (kısayol `admin:*` korunur); Electron `hasAdminAccess` = ADMIN_PERMISSION_LIST.some(matchesPermission) (isAdmin yetki kısayolu DEĞİL, yalnız adminOnly görünürlük); mobil `has()` ilk satır `set.has('*')`. | ÇEKİRDEK | tuzak | `R:2026-09-03__2026-09-03-superadmin-p2-satici`, `E:undated__rbac`, `R:2026-09-04__2026-09-04-en-yetkili-hesap` | test_superadmin_visible (`*` körlüğü kolu) + Elec… |
| superadmin-gizlilik-ayar-sifresi | Ayar şifresi kapısının KAPSAMI ELLE SAYILMAZ: `src/routes/**` özyineli AST tripwire — `systemSettingService.set(`/`setFeatureFlags(` çağıran HER route zinciri `requireSettingsPassword` taşır (iki yönlü muaf); dolaylı yazıcı ayrı karar kaydı ister. Elle 'üç yüzey' listesi bir sonraki yüzeyde delinir. | ÇEKİRDEK | reçete | `R:2026-09-03__2026-09-03-ayar-sifresi-p3` | test_settings_password tripwire kolu |
| superadmin-gizlilik-ayar-sifresi | `SECRET_FIELDS` (audit-diff.helper.ts) yalnız `changes`i maskeler ve TAM AD eşleşir (`Set.has`; '…Hash' son eki yakalanmaz) — payload/oldData/newData HAM, çağıranın yükü temiz olmak ZORUNDA (`rotated: boolean` emsali). Sır grep'i tam bcrypt gövdesiyle, ön ek yanlış pozitif verir. | ÇEKİRDEK | tuzak | `R:2026-09-03__2026-09-03-superadmin-p2-satici`, `R:2026-09-03__2026-09-03-ayar-sifresi-p3`, `R:2026-09-03__2026-09-03-superadmin-dogusu-p8` | test_superadmin (sır hijyeni AST) + test_settings… |
| superadmin-gizlilik-ayar-sifresi | `SystemLog.userId` NULLABLE — aktör/kullanıcı süzgeçlerini düz eşitlikle yazma; ham SQL'de `IS NOT TRUE` / null-güvenli yazım (fabrikada 641 `userId IS NULL` sistem olayı sessizce düşer). Gizleme süzgeçleri kalktı, tuzak her yeni aktör süzgeci için yaşıyor. | ÇEKİRDEK | tuzak | `R:2026-09-03__2026-09-03-superadmin-p2-satici` | yok (eski test_superadmin_hidden_single_source ha… |
| surum-yayin | DAİRESEL KAPI YASAĞI: beklenen değeri gerçek değerle AYNI kaynaktan alan kapı o kaynağın yanlışını yakalayamaz → `--musteri` HER yayın/derleme komutunda ZORUNLU, beklenen değer argümandan (musteri.json'dan DEĞİL); kontrol beyana değil ARTEFAKTA bakar (manifest varlık URL'leri, app-update.yml). | ÇEKİRDEK | değişmez | `M:undated__guncelleme-dagitma-once-bu-bolum`, `M:undated__uzaktan-guncelleme-ota-apk-sahaya-nasil`, `R:2026-08-26__2026-08-26-electron-dagitimi-guncelleyici` | her script'te argüman yoksa dur(); update-feed-ur… |
| tambur-finalize-kesim | İstek gövdesini ELLE kuran her katman (mobil `mutationFn`, offline `mutations.ts`) sessiz bir allowlist'tir — Zod dersinin istemci ikizi; yeni alan eklerken ekran→tip→gövde üçünü birlikte yaz ve tabletten uçtan uca ölç (servis/HTTP yeşilken yalnız tablet NULL yazdı). | ÇEKİRDEK | tuzak | `A:2026-08-13__2026-08-13-kesimde-kat-sessizce` | yok (mutations.ts gövdesinde 'bu satır unutulursa… |
| tambur-finalize-kesim | SIRA sözleşmesi: payload'ın kendi tutarlılığı (ürün/renk — `ITEM_MISMATCH`/`COLOR_MISMATCH` hard guard) ÖNCE, bağlam çözümü (parti) SONRA; ürün/renk/en operatöre SORULMAZ, iş emrinden gelir; kalite Tambur kararında. | ÇEKİRDEK | değişmez | `R:2026-08-04__2026-08-04-roll-foldtype-entryreason`, `A:2026-08-04__2026-08-04-topun-kalici-alanlari` | scripts/test_tambur_manual_batch.ts |
| tambur-finalize-kesim | Nullable kolon eklemek PG11+'ta metadata-only'dir (dolu `rolls`'ta 6 ms ölçüldü); 'canlı tabloya kolon pahalı' sezgisi DEFAULT'lu kolonlar içindir — kolon yerine JSON seçme gerekçesi olamaz. | ÇEKİRDEK | ölçüm | `A:2026-08-04__2026-08-04-topun-kalici-alanlari` | yok |
| tambur-finalize-kesim | Cursor'lu (sonsuz kaydırmalı) listede süzme SUNUCUDA — istemci süzmesi yalnız o sayfayı süzer, yanlış 'kayıt yok' üretir; tarih aralığıyla `dateField` ŞART (`applyDateRange` alan adı yoksa aralığı SESSİZCE yok sayar); Zod `.uuid()` süzgeçler (ham CSV P2007). | ÇEKİRDEK | tuzak | `R:2026-08-12__2026-08-12-top-listesi-filtresi` | mobil rollHistoryFilter.test.ts + scripts/test_ta… |
| tambur-finalize-kesim | Bir invariantı yalnız TEK dalda ölçen bekçiye güvenme — bekçinin kör noktası hatanın kendisiyle aynı yerde olabilir (§5 aşımı yalnız FULL+DEPO'da ölçüyordu, üretim dalı kırıktı); 'birebir ayna' yorumu ölçüm değildir. | ÇEKİRDEK | tuzak | `R:2026-08-22__2026-08-22-mutabakat-kapisinin-kirmizisi` | yok (ilke) |
| tambur-finalize-kesim | Davranış bayrağı varsayılanı = BUGÜNKÜ davranış ve bu ÖLÇÜLÜR; ÇIKIŞSIZ KAPI tasarım hatasıdır — kapı takmadan önce istemcinin çıkış yolu var mı sor, ad yaptığı işi söylesin (autoCreate ≠ required); kapı takarken 'bu malın çıktığı BAŞKA yol var mı' kardeş bayrağın kapsam listesinden sorulur. | ÇEKİRDEK | karar | `A:2026-09-03__2026-09-03-dilim-2-davranis` | scripts/test_feature_flag_contract.ts §16 (aEnum)… |
| top-duzeltme-iptal-fire-geri-alma | İptal ve fire sebep katalogları AYRI ve iki FARKLI kapıdan geçer: ROLL_CANCEL metin saklar → resolveReasonCode (async, tx DIŞINDA; kodu sunucu türetir, serbest metne kod UYDURULMAZ) · ROLL_SCRAP saklamaz → validateVarianceReason (senkron). TextReasonKind tip düzeyinde zorlar. | ÇEKİRDEK | değişmez | `R:2026-08-25__2026-08-25-saha-deploy-sonrasi`, `R:2026-08-19__2026-08-19-hazir-sebep-kataloglari`, `kök CLAUDE.md:78 (2026-08-21 nameFold + sebep KODU)` | test_reason_presets §5/§6 · test_roll_cancel_undo |
| top-duzeltme-iptal-fire-geri-alma | Alanı eklemek yetmez — hangi YANITTA döndüğünü doğrula (liste satırı ≠ detay ucu): 'Ekleme Nedeni' listeden okunuyordu, manualReason yalnız detayda dönüyordu; özellik commit edildi, kullanıcıya hiç ulaşmadı. | ÇEKİRDEK | tuzak | `R:2026-08-05__2026-08-05-manuel-top-geri` | yok |
| top-duzeltme-iptal-fire-geri-alma | F221 deseni: servis opts.permissions ALMAZSA enforcement atlanır (güvenilen dahili çağrı) — güvenlik sınırı controller'dadır; dış uçtan çağırırken permissions geçirilir. Aynı desen createInitialEntry.duplicateGuard/txGate'de. | ÇEKİRDEK | tuzak | `R:2026-07-30__top-duzeltme-tek-sozlesme-2026-07` | yok |
| top-duzeltme-iptal-fire-geri-alma | Bir invariantı TEK dalda ölçen bekçiye güvenme — kör nokta hatanın kendisiyle aynı yerde olabilir (§5 aşımı yalnız FULL+DEPO'da ölçüyordu). Koddaki 'birebir ayna' yorumu kanıt değildir; ayna kırık olabilir — yorum ≠ kod, iki dalı da sondayla ölç. | ÇEKİRDEK | tuzak | `R:2026-08-22__2026-08-22-mutabakat-kapisinin-kirmizisi`, `R:2026-08-12__2026-08-12-gece-single-restore` | test_tambur_undo §11 (negatif sonda: koruma kapat… |
| yari-mamul | Masaüstü/tablet dar sekmeleri `RAW_STOCK_PURE` (yalnız ham) ve `SEMI_FINISHED` (yalnız yarı mamul) kullanır; ayrım SERVİS katmanında yaşamak zorundadır çünkü negasyon istemciden söylenemez (`buildWhereClause` yalnız eşitlik/CSV-in/boolean üretir). | ÇEKİRDEK | değişmez | `R:2026-08-26__2026-08-26-yari-mamul-kendi`, `R:2026-08-27__2026-08-27-2-tur-yari` | yok |
| yari-mamul | Kolon/kapsam bekçisi ANAHTAR varlığını değil SAYI SEMANTİĞİNİ ölçer: kolon toplamları Envanter kapsamlarıyla karşılaştırılır ve örtüşmezlik kontrol edilir. Yalnız anahtar ölçen bekçi, panonun envanterden sapmasını yıllarca görmedi (negatif sonda: pano=48 ↔ envanter=47). | ÇEKİRDEK | ölçüm | `R:2026-08-27__2026-08-27-2-tur-yari` | test_production_flow_columns.ts |
| yari-mamul | 'Altıncı enum değeri unutuldu' sınıfı: `RollEntrySource` gibi bir enum'a değer eklerken TİP-GÜVENLİ OLMAYAN yüzeyleri elle ara — switch `default` dalı, `Record<string,string>`, dizi/CSV literali, ham SQL, istemcideki string haritaları. Derleyici oralarda sessiz kalır; beş vaka ölçüldü. | ÇEKİRDEK | tuzak | `R:2026-08-26__2026-08-26-yari-mamul-kendi`, `R:2026-08-27__2026-08-27-2-tur-yari` | yok |
| yari-mamul | Bir giriş yüzeyi ('backend hazır, arayüz sonra') çıkışıyla birlikte kurulmazsa sıfır kullanım üretir ve bunu kimse hata olarak raporlamaz — özellik paketi giriş+çıkış+veri (rota/izin) üçlüsüyle kapanır. | ÇEKİRDEK | karar | `R:2026-08-26__2026-08-26-yari-mamul-kendi` | yok |
| yari-mamul | Ürün Dengesi TALEPTEN doğar: sipariş kalemi olmayan spec listede HİÇ görünmez — bekçi fixture'ında önce talep, sonra ölçüm kurulur (ters sırada test 'grup=0' ile düşer). | ÇEKİRDEK | tuzak | `R:2026-08-27__2026-08-27-3-tur-yari` | scripts/test_semi_finished_surfaces.ts |
| yari-mamul | Playwright + `_electron.launch` görsel turunda temiz `--user-data-dir` profili kullan; yoksa önceki turun oturumu geri yüklenir, giriş ekranı çıkmaz, seçiciler tutmaz. Birim testi + typecheck yeşilken bile 'derleyici görmez' hatalarını (string haritası, CSS breakpoint) yalnız ekrana bakmak bulur. | ÇEKİRDEK | tuzak | `R:2026-08-27__2026-08-27-2-tur-yari` | yok |
| yetki-izin-rol | Ayar şifresi kilit kovası GİRİŞTEN AYRI, ayrım KEY'de (`sp:` ön eki): `resolveLoginLockoutKeys` varsayılan `ip` kapsamında kimliği düşürür, 5 yanlış ayar şifresi IP'nin LOGIN'ini kilitlerdi. LOCKED audit kilit başına tek satır (`justLocked`); `Retry-After` tek noktadan; audit yolu `baseUrl+path`. | ÇEKİRDEK | tuzak | `A:2026-09-03__2026-09-03-ayar-sifresi-p3` | scripts/test_settings_password.ts (ayar 429 iken … |
| yetki-izin-rol | Elle sayılmış KAPSAM listeleri ('üç yazma yüzeyi') bir sonraki yüzeyde delinir: kapsamın yüklemi ekran değil YAZMA davranışıdır, `src/routes/**` AST tripwire ile ölçülür; 'bekçi yeşil ≠ kapı doğru' — harness çerçeve semantiğini (Express mount göreliliği, senkron sözleşme) modellemeli. | ÇEKİRDEK | tuzak | `A:2026-09-03__2026-09-03-ayar-sifresi-p3`, `R:2026-09-02__2026-09-02-03-modul-anahtarlari`, `A:2026-09-03__2026-09-03-superadmin-p2-gizli` | scripts/test_settings_password.ts §J + test_docum… |

## recete (45)

| Küme | Kural | Sınıf | Tür | Kaynak | Bekçi |
|---|---|---|---|---|---|
| belge-etiket-sablon | resolveSectionOrder tanınmayan bölüm anahtarını atar, EKSİK bölümü SONA ekler (kaydedilmiş sıra yarınki bölümü yutmasın). Yeni bölüm: traveler-card.sections.ts + Electron TravelerCardStudio/types.ts BİRLİKTE (Electron backend'i import edemez). | ÇEKİRDEK | reçete | `R:2026-08-03__refakat-karti-sablonu-uc-kademe-2026` | yok |
| belge-etiket-sablon | Yeni DocumentConfig alanı = DÖRT KAPI, yoksa ayar SESSİZCE kaybolur: ① system-setting DocumentConfig tipi · ② sanitizeDocumentsConfig kayıt kapısı · ③ printed-document.controller.docConfigSchema (z.object bilinmeyen anahtarı ATAR → önizlemede YOK) · ④ Electron documentConfig.ts aynası. | ÇEKİRDEK | reçete | `R:2026-08-05__2026-08-05-belge-yerlesimi-a5`, `R:2026-09-04__2026-09-04-sevk-belgesinde-musterideki` | scripts/test_fason_ceki_html.ts §17/§18 + test_bl… |
| belge-etiket-sablon | Dört kapının bekçisi iki ayaklı: §17 önizleme şeması + §18 kayıt kapısı round-trip — yeni alan eklerken İKİSİ genişletilir (sanitize'dan alan silinince hiçbir test kırmızı vermiyordu). columns.shown (07-30) ve labels (09-04) bu kapılardan birini atlamıştı. | ÇEKİRDEK | tuzak | `R:2026-08-05__2026-08-05-belge-yerlesimi-a5`, `R:2026-09-04__2026-09-04-sevk-belgesinde-musterideki` | scripts/test_fason_ceki_html.ts §17/§18 |
| belge-etiket-sablon | Çuval içeriğine dokunan yol üç guard: touchWarehouseSackTx (WHERE shipmentId IS NULL); PLANNED küme değişiyorsa touchShipmentPlannedTx; içerik değişiyorsa markSackContentChangedTx (eski adı resetSackWeightsTx; kg + labelDirty AYRI updateMany — kg koşuluna bağlanırsa tartılmamış çuvalda yazılmaz). | ÇEKİRDEK | reçete | `B:undated__depo-cuvali-icerigine-dokunuyorsa-once-where`, `R:2026-08-21__2026-08-21-aksam-tutarlilik-taramasi`, `R:2026-07-30__2026-07-30-cuval-notu-sack` | scripts/test_sack_notes.ts; test_label_dirty_sour… |
| belge-etiket-sablon | LabelKind genişletme: backend Zod z.nativeEnum(LabelKind) OTOMATİK (literal z.enum YOK). ELLE, derleme hatası VERMEZ: Electron labelTemplateService.ts LabelKind + labelKindLabels, CustomerTemplateRoutesPanel KINDS, PrinterSettingsFields ROUTE_KINDS, mobil types/models.ts LabelKind union. | ÇEKİRDEK | reçete | `R:2026-07-30__2026-07-30-cuval-notu-sack` | yok (git grep labelKindLabels scripts → 0) |
| belge-etiket-sablon | Yeni etiket bağlamı için ayrıca label-fields.ts kataloğuna alan listesi ve (bağlamın okunur kimlik alanı varsa) label-context-fit IDENTITY_BIND girdisi eklenir — yoksa yanlış atanmış şablon fail-closed guard'ı arka kapıdan geçer. | ÇEKİRDEK | reçete | `R:2026-07-30__2026-07-30-cuval-notu-sack` | scripts/test_label_context_fit.ts |
| deploy-kurulum | Şema-dışı nesneler (partial index · CHECK · DEFERRABLE FK · ifade-UNIQUE · statistics) `test_db_invariants.ts` envanterinde yaşar; kapı İKİ YÖNLÜ — envanter-dışı nesne de düşürür. Kırmızıya tepki nesneyi silmek değil envantere yazmak (`EXPRESSION_UNIQUES` girdisi predicate ile ZORUNLU). | ÇEKİRDEK | değişmez | `B:undated__database-performance-rules-her-zaman-uygula`, `R:2026-08-25__2026-08-25-prod-un-uc` | Teks-Erp/scripts/test_db_invariants.ts (npm test) |
| deploy-kurulum | Fabrika `git pull → build` ile DEĞİL, PAKETLE kurulur: build klonunda `git pull` → `.\deploy\paketle.ps1 -Cikti …` → sunucuda `kur.ps1 -Paket <zip> -Zorla`. Çalışan `app\` git klonu değildir; `kur.ps1` kaynağı `deploy/kur.ps1`, sunucuya ELLE kopyalanır (kendini güncelleyemez). Eski akış tarihsel. | ÇEKİRDEK | reçete | `R:2026-08-25__2026-08-25-prod-un-uc`, `B:undated__pull-sonrasi-senkronizasyon-yeni-dev-sonrasi` | yok (kur.ps1 [1/9] paket doğrulaması) |
| deploy-kurulum | `kur.ps1` dokuz adım: [1/9] paket doğrula (Windows şema motoru dahil) · [3/9] `premigrate_` dump + `pg_restore --list` · [4/9] pm2 delete · [5/9] eskiyi `app.eski-*`a al · [6/9] npm ci · [7/9] `migrate deploy` GERİ ALINAMAZ EŞİK · [8/9] pm2 start · [9/9] /health. Eşik öncesi otomatik geri alma. | ÇEKİRDEK | reçete | `R:2026-08-25__2026-08-25-prod-un-uc`, `R:2026-09-04__2026-09-04-cekirdek-prisma-nin` | deploy/test/run-harness.sh + kur-gerialma.harness… |
| deploy-kurulum | `kur.ps1` geri alma KAYNAĞA bakar: `app.eski-*` yoksa hiçbir şey silinmez, mevcut `app\` pm2 ile GERİ KALDIRILIR (yalnız komut basmak fabrikayı kapalı bırakır); `Move-Item` `-ErrorAction Stop` (hedef doluysa düşmez, İÇİNE taşır); `KokeDon` `GeriAlOtomatik`'in ilk satırı; kilit sondası bilinçli RED. | ÇEKİRDEK | değişmez | `R:2026-08-25__2026-08-25-prod-un-uc` | deploy/test/kur-gerialma.harness.ps1 (run-harness… |
| deploy-kurulum | [PROFİL] Sunucuda İKİ klon var: paket **D:\tekserp-build\tekserp** (tam klon, `main`) kökünden üretilir; `C:\Etkili-Yazilim\tekserp` sparse klon (`deploy/` diskte yok) kullanılmaz. Çalışan kurulum `C:\Etkili-Yazilim\app`, pm2 adı `tekserp-backend`, önceki sürüm `app.eski-<damga>`. | PROFİL | ölçüm | `R:2026-08-25__2026-08-25-prod-un-uc` | yok |
| deploy-kurulum | Backend sürümü de OTOMATİK artar; taban git etiketi `backend-v*` (`scripts/backend-surum.mjs`, paketle.ps1 hesaplar → uygular → yayın sonrası etiketler). Kök 'Sürüm Yayınlama' bölümü yalnız `panel-v*`/`tablet-v*`ı anlatır — backend ekseni orada YOK. | ÇEKİRDEK | ölçüm | `R:2026-09-04__2026-09-04-cekirdek-prisma-nin`, `R:undated__tekserp-monorepo-koku` | scripts/test_surum.mjs (panel/tablet; backend içi… |
| deploy-kurulum | Yeni izin TEK adım: `PERMISSION_CATALOG`a satır. Boot uzlaştırması her açılışta EKSİĞİ yazar — kodu deploy etmek = katalogu getirmek; yalnız EKLER (silmez, güncellemez, ATAMAZ). Elle INSERT ve izin başına migration YAZILMAZ. Rol şablonu kimliği `code`; `code=null` fabrikanın şablonu. | ÇEKİRDEK | değişmez | `B:2026-08-01__yeni-izin-eklemek-tek-dosya-2026`, `B:undated__kimlik-dur-ad-degil-migration-fabrik` | Teks-Erp/scripts/test_permission_catalog.ts (AST … |
| fason | Fason MODÜLÜ henüz anahtar DEĞİL (`fason.enabled` yok). Fason ekran/bayrak aidiyeti `planlanan:fason` (kartela `planlanan:kartela`) — tasarımda var, kod anahtarı yok, hiza aranmaz, ölü sayılmaz; kapalı-modül gizlemesi UYGULANMAZ. Doğduğu gün `flag-modules.ts` + `screen-catalog.ts` birlikte. | ÇEKİRDEK | karar | `R:2026-09-04__2026-09-04-cekirdek-kapali-modulun`, `A:2026-09-03__2026-09-03-tamlik-bekcisi-kurulum` | Electron flag-modules.test.ts + SettingsSurfacePa… |
| filtre-liste-arama | Yeni özellik bayrağı DÖRT kapıda birlikte: system-setting (SETTING_KEYS+interface+builder+update+read helper) · feature-flag.routes · Electron featureFlagService+settings-config · mobil featureFlag.service+useFeatureFlags (emsal `kk1.historyAllEntriesEnabled`). | ÇEKİRDEK | reçete | `R:2026-08-12__2026-08-12-giris-izlenebilirligi-kim` | test_feature_flag_contract.ts |
| finans-saglamlik | Yeni ayar anahtarı eklerken üç tip de sözleşme bekçisine girer: `test_feature_flag_contract` boolean + SAYISAL (2026-08-14) + ENUM (`aEnum`, 2026-09-03) ayaklarını ölçer — 'bekçi yalnız boolean bakar' varsayımı bayattır. | ÇEKİRDEK | reçete | `R:2026-08-14__bes-saglamlik-sinifi-2026-08-14` | test_feature_flag_contract.ts (kendisi) |
| is-emri-siparis-bag | Rework sebebi İKİ hedefe: metin → 1. adım notu → fason çekisine talimat; kod → `WorkOrder.parameters.rework` (rapor anahtarı, migration yok). Yeni `ReasonPresetKind` BEŞ kapı birlikte (şema+migration · backend katalog · Electron KIND_TABS · mobil union · mobil zemin); mobil zemin GERÇEK kod taşır. | ÇEKİRDEK | reçete | `A:2026-08-25__2026-08-25-aksam-mobil-yeniden` | reworkPayload.test.ts (8) |
| kk1-idempotency-kuyruk | `kk1.duplicateGuardEnabled` bayrağını AÇMADAN ÖNCE sahadaki tabletler 409 `POSSIBLE_DUPLICATE`'i tanıyan APK'ya güncellenmiş olmalı — yoksa ham giriş çıkışsız kalır. | PROFİL | reçete | `R:2026-07-14__idempotency-2026-07-14` | yok |
| kk1-idempotency-kuyruk | Geçmiş mükerrer topları aramak için salt-okunur `scripts/find_duplicate_rolls.ts` kullanılır (yazan bir temizlik script'i yazma). | ÇEKİRDEK | reçete | `R:2026-07-14__idempotency-2026-07-14` | yok |
| kucuk-kumeler | Modüle ait her uç ADLANDIRILMIŞ modül kapısını taşır (requireProductionEnabled / requireTicaretEnabled / requireIplikEnabled / requireDepoMultiEnabled); jenerik requireModule("x") YASAK — bekçiler middleware ADINI arar. Kapı gerekmiyorsa gerekçe muaf listesinde YAZILI. | ÇEKİRDEK | yasak | `B:undated__uc-bir-module-aitse-router-adlandirilmis` | test_module_flag_off.ts + scripts/lib/regime-gate… |
| kucuk-kumeler | `Electron/dist-web` backend paketine girer — SPA↔API sürüm drift'i imkânsız. Eksiklik SESSİZDİR (kök, panelin yerine durum sayfası basar), bu yüzden paketlemede iki kapı vardır: derleme düşerse ve `dist-web\index.html` yoksa paket ÜRETİLMEZ. | ÇEKİRDEK | değişmez | `R:2026-09-01__2026-09-01-patron-modulu-fabrikaya` | deploy/paketle.ps1 kapıları |
| kursun-planlama-bypass | Yeni izin eklemenin TEK adımı: `src/constants/permission-catalog.ts` → `PERMISSION_CATALOG`'a bir satır; başka yere kopyalanmaz. `category` Prisma enum'una bağlı (yazım hatası derlemede düşer), boot uzlaştırması eksiği getirir, AST bekçisi kaçağı yakalar. Elle DB INSERT'i gerekmez. | ÇEKİRDEK | reçete | `B:2026-08-01__yeni-izin-eklemek-tek-dosya-2026` | scripts/test_permission_catalog.ts (TS AST; katal… |
| modul-bayraklari-profiller | Modül/bayrak paketlerinde dağıtım sırası backend ÖNCE (yeni uç/anahtar/tablo eski istemciyi bozmaz); Electron+APK sonra/birlikte; 403 gerekçe metni finance→ticaret değişen dört yüzey sürüm notuna yazılır. | ÇEKİRDEK | reçete | `R:2026-09-02__2026-09-02-03-modul-anahtarlari`, `R:2026-09-03__2026-09-03-superadmin-p2-satici`, `R:2026-09-03__2026-09-03-ayar-sifresi-p3` | yok |
| mukerrer-namefold | Sed migration'ı İKİ desenden biriyle iner: ① mükerrer varsa/bilinmiyorsa YUMUŞAK KAPI — o tablonun index'i ATLANIR + NOTICE, deploy DURMAZ, dosya idempotenttir ve temizlik sonrası yeniden koşunca enforce eder; ② çakışma ölçülüp script'le 0'a indiyse sert index, SIRA (önce temizlik) pazarlık dışı. | ÇEKİRDEK | reçete | `R:2026-08-21__2026-08-21-namefold-db-seddi`, `R:2026-08-22__2026-08-22-mukerrer-paneli-v2` | yok |
| mukerrer-namefold | Yeni bir `nameFold` kolonu eklendiğinde ya sed de eklenir ya da GEREKÇELİ MUAF listesine yazılır — unutmak SESSİZDİR (uygulama guard'ı normal yolda çalışır, hiçbir şey kırmızıya dönmez). Envanter İKİ YÖNLÜ: ölü muaf (sedlenmiş ama muafta) ve hayalet muaf (olmayan tablo) da kırmızı verir. | ÇEKİRDEK | reçete | `KOD:20260831120000_namefold_sed_kalan_tablolar` | test_master_data_name_dup §10 / §10a-c |
| mukerrer-namefold | Sed eklerken: şemaya `map:`siz `@@unique([nameFold])` (index↔unique drift'i önler, ad `<tablo>_nameFold_key` doğar), `error.middleware` UNIQUE_COLUMN_LABELS'a kolon etiketi ('ad'), ifade-UNIQUE ise `EXPRESSION_UNIQUES` girdisi (+predicate) ZORUNLU. Drift allowlist'i GEREKMEZ (ölçüldü). | ÇEKİRDEK | reçete | `R:2026-08-21__2026-08-21-namefold-db-seddi`, `R:2026-08-25__2026-08-25-prod-un-uc` | test_db_invariants EXPRESSION_UNIQUES (iki yönlü)… |
| mukerrer-namefold | Yeni tabloya sed eklemeden ÖNCE `scripts/find_fold_duplicates.ts` koştur: soy bağlı tablolarda tombstone'u süzer, renk grubunu JS renk kuralıyla (düz nameFold ile DEĞİL) kurar ve iki keskin tarama (noktalama-sız ad, harf-duyarsız kod) yapar; temiz çıktıda enforce komutunu kendisi yazdırır. | ÇEKİRDEK | reçete | `R:2026-08-21__2026-08-21-namefold-db-seddi`, `R:2026-08-22__2026-08-22-mukerrer-paneli-v2`, `R:2026-08-25__2026-08-25-prod-un-uc` | yok |
| mukerrer-namefold | Birleştirme kararlarını toplu uygularken İNCELEME SQL'de, UYGULAMA MOTORDA: ham UPDATE 42 kurallık FK haritasını, çakışma politikalarını, etiket/kart bayatlatmasını, advisory kilidi ve audit'i atlar. `apply_merge_decisions.ts` DRY-RUN varsayılan; CSV ayırıcısı `;` (gerekçede `;` yok). | ÇEKİRDEK | yasak | `R:2026-08-22__2026-08-22-mukerrer-paneli-v2`, `R:2026-08-25__2026-08-25-prod-un-uc` | yok |
| raporlar | sl_day_exact ifade istatistiği GRUPLAMA İFADESİNE bağlıdır: factoryDaySql'in ürettiği SQL metnini değiştirirsen YENİ BİR MIGRATION da yaz (CREATE STATISTICS yeniden) — yoksa istatistik sessizce devre dışı kalır ve sorgu ~2× yavaşlar. | ÇEKİRDEK | tuzak | `B:2026-08-01__fabrika-gunu-takvim-gunu-tek-kaynak` | scripts/test_report_day_boundary.ts |
| refakat-karti | `resolveSectionOrder` tanınmayan bölüm anahtarını atar ve EKSİK bölümü SONA ekler (kayıtlı sıra yarınki bölümü yutmasın). Yeni bölüm eklerken `traveler-card.sections.ts` + Electron `TravelerCardStudio/types.ts` BİRLİKTE güncellenir; alan kataloğu için de aynı (Electron backend'i import edemez). | ÇEKİRDEK | reçete | `R:2026-08-03__refakat-karti-sablonu-uc-kademe-2026` | yok |
| rota-renk-ozellik-kapsama | Yeni istasyon yetenek bayrağı eklerken BACKFILL'E GÜVENME: seed migration'dan SONRA boş tabloda koşar → seed'e açık değer + yüklemde köprü dalı (`kind===PROCESS_QC \|\| appliesQuality`). 08-10 `appliesColor` göçünde iç istasyonlara FALSE yazıldı — bayrak DÜRÜST olmalı, true Tambur'a renk yazdırır. | ÇEKİRDEK | reçete | `A:2026-09-03__2026-09-03-kalite-istasyon-yetenegi`, `R:2026-08-10__2026-08-10-11-uretim-karakteristigi` | test_station_quality_capability.ts (Faz A köprüsü… |
| rota-renk-ozellik-kapsama | KÜME DIŞI (backend): Yeni route = `verifyToken` + `requirePermission(kod)` + Swagger JSDoc; kod `src/constants/permission-catalog.ts`'te OLMALI. | ÇEKİRDEK | reçete | `B:undated__route-swagger-jsdoc-kod-src-constants` | scripts/test_permission_catalog.ts |
| sebep-kataloglari | Yeni `ReasonPresetKind` eklerken BEŞ KAPI birlikte güncellenir: şema+migration · backend katalog (`constants/reason-presets.ts`) · Electron `KIND_TABS`+union · mobil union · mobil çevrimdışı zemin. Eksik kapı sessiz kalır (tip hatası vermez). | ÇEKİRDEK | reçete | `R:2026-08-25__2026-08-25-aksam-mobil-hizli` | yok (mobil union ORDER_CANCEL'ı taşımıyor — unres… |
| sevkiyat-brut-storno-cuval | `LabelKind` genişletirken 4 yer ELLE güncellenir (labelTemplateService KINDS · PeripheralDevices tipleri · 4 literal z.enum) — Electron/mobil kendi union'ını taşıdığı için derleme hatası VERMEZ. | ÇEKİRDEK | tuzak | `R:2026-07-30__2026-07-30-cuval-notu-sack` | yok |
| sevkiyat-brut-storno-cuval | Deploy sırası: sevkiyat sözleşmesi değişince backend ÖNCE (eski Electron `releaseSacks` göndermez → false → eski PLANNED davranışı; yeni Electron eski backend'de Zod alanı strip eder). Mobil yanıt şekli değişen turda (brüt detay) backend + Electron + APK aynı pencerede; storno turunda APK gerekmez. | ÇEKİRDEK | reçete | `R:2026-08-22__2026-08-22-sevk-kapisi-bayragin`, `R:2026-08-03__2026-08-03-brut-kurali-sevkiyat`, `R:2026-08-05__2026-08-05-sevk-storno-iade` | yok |
| surum-yayin | Etiketi yayın script'i YAYIN BİTTİKTEN sonra atar (best-effort; var olan etiket taşınmaz). HEAD etiketin üstündeyken numara KORUNUR — tekrar koşum ve aynı turda ikinci müşteri aynı numarayı taşır. OTA ve APK AYNI çizgide (tek `tablet-v*`; `expo.version` hem paket hem versionName). | ÇEKİRDEK | reçete | `R:undated__surum-yayinlama-sahaya-guncelleme-cikarma` | scripts/test_surum.mjs (sonrakiSurumEtiketten HEA… |
| surum-yayin | Yol düzeni `guncelleme.etkiliyazilim.com/<müşteri>/<ürün>/` (electron\|mobil; aynı konteyner, statik nginx, yeni konteyner YOK). Müşteri segmenti ALT ALAN ADI değil (wildcard Origin CA iki seviyeyi kapsamaz). Yeni müşteri = sunucuda `mkdir` + paketle + yayınla; DNS/sertifika/servis GEREKMEZ. | PROFİL | reçete | `R:undated__surum-yayinlama-sahaya-guncelleme-cikarma`, `R:2026-08-26__2026-08-26-electron-dagitimi-guncelleyici`, `R:2026-08-26__2026-08-26-mobil-uzaktan-guncelleme` | yok |
| surum-yayin | Yan etkisiz ön kontroller yayın öncesi koşulur: `npm run yayinla:check`, `npm run build:apk:check` (derleme yok), `build:apk:verify` (mevcut APK'nın gömülü adresi), `electron-yayinla.sh --dogrula`, `mobil-yayinla.mjs --dogrula=<url> --boyut=<bayt>` (yükleme yok). `--check` dosya DEĞİŞTİRMEZ. | ÇEKİRDEK | reçete | `R:undated__surum-yayinlama-sahaya-guncelleme-cikarma`, `M:undated__guncelleme-dagitma-once-bu-bolum`, `M:undated__apk-derleme-sahaya-kurulacak-paket-tek` | yok |
| tambur-finalize-kesim | Kat yazan her yol aynı kuralı uygular (cutOpenFabric · cutWarehouseRoll · yeniden-kesim artığı · finalizeOpenFabric · elle giriş · Düzelt); Düzelt'te üçlü sözleşme (undefined DOKUNMA · null TEMİZLE · kod YAZ), kat değişimi `labelDirty`; depo kesimi mobilde kat SORMAZ (bilinçli, fallback parent). | ÇEKİRDEK | reçete | `A:2026-08-04__2026-08-04-topun-kalici-alanlari`, `A:2026-08-13__2026-08-13-kesimde-kat-sessizce` | scripts/test_fold_edit_and_label.ts |
| yari-mamul | Yeni `RollEntrySource` değerinde güncellenecek yerler: `entryTitle` switch'i (açık case; default 'Ham Giriş' yalanı basar) · mobil KK1 listelerinin CSV allowlist'i · Electron `rollProcessingState` haritası · Giriş Kaynağı filtre seçenekleri · gerekiyorsa `rollScope` dalı. | ÇEKİRDEK | reçete | `R:2026-08-26__2026-08-26-yari-mamul-kendi`, `R:2026-08-27__2026-08-27-2-tur-yari` | yok |
| yari-mamul | Kurulum başına panelden 'Yarı Mamul (Kurşun + Tambur)' ROTASI oluşturulur ve `mobile:kk1-yari-mamul` izni atanır — rota kod değil VERİdir; ikisi eksikken giriş kapısı açık olsa da özellik sıfır kullanım üretir. | PROFİL | reçete | `R:2026-08-26__2026-08-26-yari-mamul-kendi` | yok |
| yari-mamul | 'OTA'ya uygun mu' sorusunu insan değil yayın script'i cevaplar (native parmak izi kıyası; değiştiyse DURUR). Uzaktan güncelleme yalnız yeni APK kurulu tabletlere gider — elle APK turundan ÖNCE yayınlamak işe yaramaz. | ÇEKİRDEK | reçete | `R:2026-08-27__2026-08-27-2-tur-yari` | mobil `npm run yayinla` native parmak izi kontrolü |
| yetki-izin-rol | Yeni ekran canlıda görünmüyorsa sırayla bak: (1) satır DB'de mi (boot log `[permission-catalog]`), (2) kullanıcıya ATANMIŞ mı, (3) kullanıcı yeniden giriş yaptı mı (JWT'deki izin listesi bayat olabilir). | ÇEKİRDEK | reçete | `B:undated__katalog-yine-atama-icermez` | yok |
| yetki-izin-rol | Mobil ayrı union taşır (`mobil/src/types/permissions.ts`), Electron backend'i import edemez → izin listeleri AYNALANIR (`Electron/src/lib/permissions.ts` ↔ `document-design.ts`; `WORKSTATION_PERMISSION`). Mobil ekran izni eklerken union ELLE güncellenir (bekçi taramaz); ayna çiftleri bekçili. | ÇEKİRDEK | tuzak | `B:undated__katalog-yine-atama-icermez`, `E:undated__rbac`, `R:2026-08-05__belge-tasarimi-ayri-bir-yetkidir-sistem` | scripts/test_document_template_permission.ts (5. … |
| yetki-izin-rol | Yeni uç: `verifyToken` + `requirePermission(kod)` + Swagger; kod katalogda; modüle aitse ADLANDIRILMIŞ modül kapısı (`requireXEnabled`, jenerik `requireModule` YASAK); mobil dokunuyorsa `requireAnyPermission`; 403 kodu `details.code`. İzin bir uçta ZORUNLU olunca eski istemci 403 → `minVersion`. | ÇEKİRDEK | reçete | `B:undated__route-swagger-jsdoc-kod-src-constants`, `B:undated__mobil-ekranin-dokunacagi-endpoint-sadece-r`, `B:undated__tetik-su-degisiklikleri-yaparken-dur-ve` | scripts/test_permission_catalog.ts + test_route_a… |

## yasaklar (31)

| Küme | Kural | Sınıf | Tür | Kaynak | Bekçi |
|---|---|---|---|---|---|
| belge-etiket-sablon | Uzman modu (RAW_HTML) ŞABLON DİLİ DEĞİL, metin ikamesidir: {{alan}} + {{#liste}}…{{/liste}} (iç içe YOK). Koşul/ifade/JS EKLENMEZ — ihtiyaç doğarsa katalogda yeni ALAN (config/traveler-card-fields.ts). Bilinmeyen anahtar boş basar, baskıyı DURDURMAZ. (Emsal: handlebars yerine kendi motor.) | ÇEKİRDEK | yasak | `R:2026-08-03__refakat-karti-sablonu-uc-kademe-2026`, `B:undated__allowed-npm-packages` | yok |
| belge-etiket-sablon | Belge şablonu JS template literal'i içinde (CSS yorumlarında bile) BACKTICK KULLANMA — literal ortadan bölünür, dosya derlenmez (aynı işte iki kez ısırdı). | ÇEKİRDEK | yasak | `R:2026-08-05__2026-08-05-belge-yerlesimi-a5` | yok (derleme hatası) |
| fason | `FASON_CEKME` sistem sebebi `SCRAP_REASONS`'a / `RECORD_CORRECTION_REASONS`'a EKLENMEZ: `validateVarianceReason` kabul eder, `reasonsForKind` döndürmez — operatörün Tambur fire listesine sızmasın. | ÇEKİRDEK | yasak | `R:2026-08-21__2026-08-21-fason-kabulu-cekme` | scripts/test_roll_variance.ts |
| fason | `quality.gradeRequiredEnabled` kapsamı DAR: fason kabulü ve son-adım finalize DIŞARIDA; kapsam genişletmek sıfır-fark ihlalidir ve bayrak açıldığı gün patlar. | ÇEKİRDEK | yasak | `R:2026-09-03__2026-09-03-dilim-2-davranis` | scripts/test_quality_batch_flags.ts |
| filtre-liste-arama | Çoklu seçim HER filtreye uymaz: iki değerli NOT NULL enum (`destination`, `WorkOrder.type`) TEKİL — `destination` çoklu seçilince listShipments DirectShipment'ları union'dan DÜŞÜRÜR; nullable alanda (foldType) çoklu anlamlı. Kapsam anahtarlarına (processingStatus, rollScope, shipmentScope, rollKind, scope, status, inv… | ÇEKİRDEK | yasak | `R:2026-08-06__2026-08-06-filtrelerde-coklu-secim`, `R:2026-08-02__2026-08-02-brut-kurali-liste` | yok |
| kalite-istasyon-yetenek | Kalite/özellik KODU bir kimliktir: ASCII yazılır (`TUP`, `TÜP` değil), karşılaştırma `toUpperCase()` ile — `toLocaleUpperCase("tr")` YASAK (i/İ dönüşümü eşleşmeyi sessizce bozar). | ÇEKİRDEK | yasak | `R:2026-08-02__2026-08-02-kosullu-etiket-elemani`, `R:2026-08-10__2026-08-10-11-uretim-karakteristigi` | test_label_element_condition |
| kesif-cihaz-ag | `installationId` NULL olan aday BAŞKA HİÇBİR adayla birleşmez — kendi `addr:<host>:<port>` anahtarında kalır. Kimliksiz iki sunucuyu 'aynı' saymak gerçek ikinci sunucuyu listeden SİLER; eski adres bazlı davranış orada birebir korunur. | ÇEKİRDEK | yasak | `R:2026-09-04__2026-09-04-kesif-bir-satir` | Electron/src/test/discovery-logic.test.ts (33) |
| kesif-cihaz-ag | `addressPreferenceRank` bir SIRALAMADIR, ELEME DEĞİL: 172.16/12 (Hyper-V/Docker), 100.64/10 (Tailscale), loopback ve link-local adaylarını listeden DÜŞÜRME — o adresten gerçekten hizmet veriliyor olabilir. Sıra: LAN → ad → 172.16/12 → 100.64/10 → 127/8 → 169.254/16. | ÇEKİRDEK | yasak | `R:2026-09-04__2026-09-04-kesif-bir-satir` | Electron/src/test/discovery-logic.test.ts + mobil… |
| kesif-cihaz-ag | KEŞİF-İKİZ bloğuna PROJEYE ÖZGÜ hiçbir şey girmez: Electron'un kaynak güvenilirliği sırası (`bySourceRank`, mDNS ↔ tarama) blok DIŞINDA yaşar ve içeri `tieBreak` parametresiyle verilir (mobilde `via` alanı yok). | ÇEKİRDEK | yasak | `R:2026-09-04__2026-09-04-kesif-bir-satir` | discovery.contract.test.ts ikiz metin kilidi |
| kesif-cihaz-ag | `CLIENT_IP_HEADER` app-wide OKUNMAZ; yalnız uzak istekte geçerlidir (`clientIpHeaderRemoteOnly`, `REMOTE_PORT`ten türer) — aksi hâlde LAN'daki biri `CF-Connecting-IP` uydurup giriş kilidini VE hız sınırını etkisizleştirir. | ÇEKİRDEK | yasak | `R:2026-09-01__2026-09-01-patron-modulu-fabrikaya` | test_remote_access_guard.ts:266 |
| kesif-cihaz-ag | `ClientType.WEB` ayrı bir değerdir; istemci ayrımını `!== "mobile"` ile YAZMA — telefon tarayıcısı masaüstü oturumunu düşürür. İkinci faktör (TOTP) `issueToken`den ÖNCE koşar; üç hata kodu üç AYRI HTTP statüsü döner ve giriş kilidi yalnız 401'i sayar. | ÇEKİRDEK | yasak | `R:2026-09-01__2026-09-01-patron-modulu-fabrikaya` | Teks-Erp/scripts/test_remote_access_guard.ts |
| kk1-idempotency-kuyruk | Seri giriş ekranında (KK1) token'ı ref'te SABİTLEME — arka arkaya kuyruğa giren topları tek kayda çakıştırıp sessizce yutar (kopyanın aynadaki ikizi: eksik stok). | ÇEKİRDEK | yasak | `R:2026-07-14__idempotency-2026-07-14` | mobil/src/offline/entryAttempt.test.ts |
| kk1-idempotency-kuyruk | Donanım okuma yolu TEK: HAL. Eski `hardware.service.ts` mock'unu geri getirme. | ÇEKİRDEK | yasak | `M:undated__tum-istasyonlar-tambur-kk1-okuma-icin` | yok |
| kucuk-kumeler | `ClientType.WEB` ayrı bir oturum türüdür; istemci ayrımını `!== "mobile"` ile YAZMA — `clientType` opsiyoneldir ve `undefined` tarihsel olarak MOBİL demektir, negatif yazım alan göndermeyen eski mobil istemcileri masaüstü sayıp 403'e düşürür. | ÇEKİRDEK | yasak | `R:2026-09-01__2026-09-01-patron-modulu-fabrikaya` | yok |
| modul-bayraklari-profiller | Müşteri dalı YOK (`feature/*` → `main`); `if (musteri === 'X')` fork'un ilk sinyalidir, YASAK; 'adnansahin'de yok' = 'bayrağı kapalı'. Hiçbir eski not müşteri koşulu gerekçesi olamaz. Fabrika paketi ve demo derlemesi `main`'den üretilir. | ÇEKİRDEK | yasak | `R:undated__tekserp-monorepo-koku`, `R:2026-09-02__2026-09-02-03-modul-anahtarlari` | yok |
| mukerrer-namefold | SQL fold fonksiyonunda ayırıcı sınıfı AÇIK yazılır, `\s` YASAKTIR: PostgreSQL'de `\s` ASCII dışında veritabanının ctype'ına bağlıdır (dev ICU ↔ saha C locale) ve aynı ad iki kurulumda farklı katlanır. JS ikiziyle BMP genelinde birebirlik bekçiyle ölçülür. | ÇEKİRDEK | yasak | `R:2026-08-25__2026-08-25-prod-un-uc` | test_fold_contract §2b |
| mukerrer-namefold | Uygulama guard'ı OLMAYAN tabloya (customer_branches · label_templates · permission_templates · subcontractor_categories) sed EKLEME: orada mükerrer ad bugün MEŞRUDUR, sed yeni bir KISIT olur — iş kararıdır. `stations` da muaf (tarihsel-mükerrer senaryosu + damgalanmamış fixture'lar). | ÇEKİRDEK | yasak | `KOD:20260831120000_namefold_sed_kalan_tablolar` | test_master_data_name_dup §10 (muaf gerekçeleri) |
| mukerrer-namefold | `token_set_ratio` KULLANILMAZ — alt-küme adları %100 verir ('MODA' ⊂ 'MODA ANKARA'). Renkte `hex` kimlik alanı DEĞİLDİR (canlıda 7 meşru `#ffffff`). | ÇEKİRDEK | yasak | `R:2026-08-22__2026-08-22-mukerrer-paneli-v2` | yok |
| parti-no-batch | `orderBy: { batchNumber }` HER İKİ REJİMDE DE YASAK: günlük kalıpta sözlüksel ≠ sayısal (P05082610 < P0508262), kısa kalıpta numara sardığı için sayısal sıra da yenilik sırası değildir. Parti listeleyen her yer `createdAt` ile sıralar. | ÇEKİRDEK | yasak | `R:2026-07-13__is-emri-no-parti-2026-07` | scripts/test_batch_number_format.ts §5 (src/ rege… |
| parti-no-batch | Sayaç sıfırlama düğmesi EKLENMEZ (bilinçli red): saklanan ikinci bir sayaç gerektirir — repo tüm sıraları veriden türetir — ve P01-P40 canlıyken sıfırlamak, kabul edilen sarma çakışmasından daha kötü bir aynı-hafta çakışma serisi üretir. | PROFİL | yasak | `R:2026-07-13__is-emri-no-parti-2026-07` | yok |
| parti-no-batch | 2026-08-05 öncesi dolgulu parti kayıtları (`P0508260019`) OLDUĞU GİBİ DURUR — geriye dönük düzeltme/migration yapılmaz (canlı veri + o numaralar kâğıda basıldı); `nextDailySeq` parseInt ile okur, aynı gün içinde kaldığı yerden devam eder. | ÇEKİRDEK | yasak | `R:2026-07-13__is-emri-no-parti-2026-07` | scripts/test_batch_number_format.ts §3 |
| refakat-karti | Uzman modu bir ŞABLON DİLİ DEĞİL metin ikamesidir (`{{alan}}` + `{{#liste}}…{{/liste}}`, iç içe YOK); koşul/ifade/JS EKLENMEZ — ihtiyaç doğarsa çözüm `config/traveler-card-fields.ts` kataloğuna yeni ALAN, değer üretimi sunucuda kalır. | ÇEKİRDEK | yasak | `R:2026-08-03__refakat-karti-sablonu-uc-kademe-2026` | yok |
| rota-renk-ozellik-kapsama | Renk istasyon bazlı KISIT DEĞİL: rota adımı renk seçicisi tüm aktif kataloğu gösterir; `StationColor` deprecated — hiçbir karar/filtre ondan türemez (yetenek sayfası satırları listeler ve `setCapabilities` yazar, o kadar). Renk kısıtını geri EKLEME. | ÇEKİRDEK | yasak | `R:2026-08-02__renk-istasyon-bazli-kisit-degil-2026` | test_station_capability.ts (8b) + test_helpers.ts… |
| rota-renk-ozellik-kapsama | Özellik/değer kodu ASCII yazılır (`TUP`, `TÜP` değil); karşılaştırma `toUpperCase()`, `toLocaleUpperCase("tr")` YASAK (2026-08-02 `showIf` dersinin ikizi). | ÇEKİRDEK | yasak | `R:2026-08-10__2026-08-10-11-uretim-karakteristigi` | test_fold_catalog.ts (24) |
| superadmin-gizlilik-ayar-sifresi | Dev sunucusunu `pkill -f "tsx src/server.ts"` ile öldürme (kullanıcının :4000'ini düşürdü, iki ajan iki tur): yalnız kendi PID'in, paralel ajanlara ayrı port (tasarım §12-12). | ÇEKİRDEK | yasak | `R:2026-09-03__2026-09-03-superadmin-p2-satici` | yok |
| surum-yayin | Ham `npm run build:win` ve elle `gradlew assembleRelease` YASAK. Panel yalnız `deploy/electron-paketle.sh <müşteri>` (derleme SONRASI paketteki `app-update.yml` adresi ARGÜMANLA kıyaslanır); tablet yalnız `scripts/build-apk.mjs` (önbellek siler, bundle'dan adresi geri okur; düşerse `.DOGRULANMADI`). | ÇEKİRDEK | yasak | `R:undated__surum-yayinlama-sahaya-guncelleme-cikarma`, `M:undated__apk-derleme-sahaya-kurulacak-paket-tek` | electron-paketle.sh gömülü adres kapısı; build-ap… |
| surum-yayin | nginx uzun-cache (`.exe`/`.apk`/OTA varlık) kurallarında `add_header … always` YASAK — 404'e de eklenir, Cloudflare 404'ü bir hafta tutar; ikinci hat `error_page 404 → no-store`. Yapılandırma REPODA (`deploy/guncelleme-sunucusu/`), sunucuda elle düzenlenmez. | ÇEKİRDEK | yasak | `R:2026-08-26__2026-08-26-mobil-uzaktan-guncelleme`, `R:2026-08-26__2026-08-26-electron-dagitimi-guncelleyici` | yok (default.conf yorumu) |
| tambur-finalize-kesim | Genel 'X / stoktan kaldır' tuşu YASAK — stok kaydı silinmez; sebebi söyleyen TİPLİ olay yazılır (iptal CANCELLED qtyOut=0 · fire SCRAP qtyOut=qtyIn · storno · sayım farkı · tersleme) ve düğme o olayın adını taşır. | ÇEKİRDEK | yasak | `R:2026-08-12__2026-08-12-gece-single-restore` | yok |
| top-duzeltme-iptal-fire-geri-alma | Genel 'X / stoktan kaldır' tuşu YASAK: stok kaydı silinmez, sebebi söyleyen TİPLİ olay yazılır (iptal/storno · fire · sayım farkı · tersleme) ve buton olayın adını taşır (Kesimi Geri Al · Topu İptal Et · İş Emrine Geri Al · Tümden Geri Al · Kaydı İptal Et · Fire). Ön seçim YOK. | ÇEKİRDEK | yasak | `R:2026-08-12__2026-08-12-gece-single-restore`, `R:2026-08-25__2026-08-25-saha-deploy-sonrasi` | yok |
| yari-mamul | Aynı URL parametresine (`entrySource`) yazan iki ayrı filtre bulundurma — birbirlerini sessizce ezerler; seçenekler TEK filtrede toplanır. | ÇEKİRDEK | yasak | `R:2026-08-26__2026-08-26-yari-mamul-kendi` | yok |
| yetki-izin-rol | Sır hijyeni: süperadmin parolası/PIN/TOTP ve ayar şifresi/hash repoya, log'a, sürüm notuna, audit diff'ine, yanıt gövdesine GİRMEZ; `SECRET_FIELDS` yalnız `changes`i maskeler (oldData/newData/payload HAM) → audit yükü çağıranda temiz. CLAUDE.md'ye yalnız seed/dev kimliği yazılır. | ÇEKİRDEK | yasak | `R:undated__test-kullanicilari`, `A:2026-09-03__2026-09-03-superadmin-p2-gizli`, `A:2026-09-03__2026-09-03-ayar-sifresi-p3` | scripts/test_superadmin.ts (sır hijyeni AST) + te… |

## sozluk (11)

| Küme | Kural | Sınıf | Tür | Kaynak | Bekçi |
|---|---|---|---|---|---|
| belge-etiket-sablon | 'İçerik donuk · sunum canlı' — belge ailesinin ortak ilkesi: hukuki/tarihsel kayıt (kart planı, irsaliyedeki ad) snapshot'a DONAR; kâğıdın çizimi (şablon, punto, sayfa boyutu, ad kolonu) her baskıda canlı. Rejim de donarsa ayar eski belgeye ulaşmaz; içerik de canlı olursa geçmiş değişir. | ÇEKİRDEK | karar | `R:2026-08-05__refakat-karti-plan-canli-sunum-donmus`, `R:2026-09-04__2026-09-04-sevk-belgesinde-musterideki` | test_shipment_doc_customer_name §3 |
| parti-no-batch | `WorkOrder.workOrderNumber` (İE+GGAAYY+NNNN) ÜRETİM EMRİDİR; `Batch` üretime aynı anda giren top grubudur — bir WO N parti içerir. İkisi ayrı kimliktir, birbirinin yerine kullanılmaz. | ÇEKİRDEK | değişmez | `R:2026-07-13__is-emri-no-parti-2026-07` | yok |
| raporlar | ÖLÜ STOK = ESKİ **VE** SİPARİŞSİZ (kesişim); ikisinden biri tek başına ölü stok değildir (bekçi ölçümü: yalnız eskiye +400, yalnız siparişsize +500, doğrusu +300). Siparişsizlik SPEC bazındadır — hangi FİZİKSEL topun karşılıksız olduğu iddia edilmez. | ÇEKİRDEK | değişmez | `R:2026-08-09__2026-08-09-rapor-temeli-finalizedat` | scripts/test_stock_scorecard.ts (13 kontrol) |
| raporlar | Karnelerde GERİYE DÖNÜK DÜZELTME (restatement) BİLİNÇLİDİR: depo topu sonradan kesilirse ebeveyn üretildiği dönemin karnesinden düşer (metrajı çocuklarında), kalite sonradan 'Düzelt' ile değişirse eski dönem yeni kaliteyle okunur. Metraj currentQty'dir — finalize anındaki değer saklanmıyor. | ÇEKİRDEK | karar | `R:2026-08-09__2026-08-09-rapor-temeli-finalizedat` | yok |
| sevkiyat-brut-storno-cuval | Sipariş görünümü üç kolon: İstenen \| Sevk \| Açık (Açık = quantity − shippedQty). `OrderLine.packedQty`/`Order.packedQty`/`rebalanceCustomerPool` KALDIRILDI, geri getirilmez. | ÇEKİRDEK | değişmez | `A:undated__not-tarihsiz`, `R:2026-07__cuval-depo-modeli-2026-07` | yok |
| sevkiyat-brut-storno-cuval | Versiyon rozetindeki 'Güncel' = 'en son versiyon, hiç revize edilmedi' demektir; 'içerik güncel' DEMEZ. İrsaliye diyaloğu 'sevk sonrası N top (M m) iade alınmıştır' bandını, Excel aynı bilgiyi dipnot olarak (`SheetSpec.notes`) basar — ekranda sessizlik yok. | ÇEKİRDEK | tuzak | `R:2026-08-02__2026-08-02-sevk-rakami-brut` | yok |
| tambur-finalize-kesim | Katalog değerinde `code` KİMLİKTİR, `label` serbest: satırı yeniden adlandırmak ile yeni değer eklemek farklı işlerdir (TUP→'6 Kat' vakası); tablet/filtre/metre eşleşmesi kodla çalışır. | ÇEKİRDEK | değişmez | `A:2026-08-13__2026-08-13-kesimde-kat-sessizce` | yok |
| top-duzeltme-iptal-fire-geri-alma | qtyOut=0 (storno: mal bu istasyondan HİÇ geçmedi) ile qtyOut=qtyIn (finalize / Kurtar / kapanış dispozisyonu / fire: mal geçti ve çıktı) karıştırılmaz — 0 yazmak hacimden metraj siler (hayalet kayıp), qtyIn yazmak hiç var olmamış metrajı hacme ekler. | ÇEKİRDEK | tuzak | `R:2026-08-05__2026-08-05-manuel-top-geri`, `R:2026-07-27__tambur-finalize-wo-disiplini-2026-07`, `R:2026-07-30__wo-kapatma-kapanis-dispozisyonu-2026-07` | yok |
| top-duzeltme-iptal-fire-geri-alma | Üç statü kümesi ÜÇ AYRI soru, elle kopyalanmaz: K18_DEAD_STATUSES (SCRAP YOK — 'lane/etikette göster', 'parti açık mı') · CHILD_CANCELABLE_STATUSES (SCRAP VAR — Tambur çocuğu kesimle geri alınır) · STATUS_GROUPS.ARCHIVE (4 tüketilmiş + CANCELLED + SCRAP — arşiv sekmesi). | ÇEKİRDEK | değişmez | `R:2026-07-27__tambur-finalize-wo-disiplini-2026-07`, `R:2026-08-04__2026-08-04-roll-foldtype-entryreason`, `R:2026-08-25__2026-08-25-saha-deploy-sonrasi` | Rolls/service.test.ts (arşiv kümesi) · batch.serv… |
| top-duzeltme-iptal-fire-geri-alma | Barkod topun KİMLİĞİdir ve doğduğu an verilir ('bitmiş ürün işareti' değil); erken barkodun sanılan riskleri barkod varlığıyla değil STATÜ ile kapalıdır (IN_PRODUCTION ∈ NON_SACKABLE_STATUSES). Barkod anlamı değiştirilmez — üçüncü kez sorulmasın. | ÇEKİRDEK | karar | `R:2026-08-05__2026-08-05-manuel-top-geri` | yok |
| yari-mamul | Terim TDK yazımıyla 'Yarı Mamul'dür ('mamül' yazılmaz); tekdüzen hesap planında 151 Yarı Mamuller, SAP'de HALB. 'Ham bez' boyasız malın adıdır, bu kovayı adlandırmaz. | ÇEKİRDEK | karar | `R:2026-08-26__2026-08-26-yari-mamul-kendi` | yok |

## alt-backend (192)

| Küme | Kural | Sınıf | Tür | Kaynak | Bekçi |
|---|---|---|---|---|---|
| belge-etiket-sablon | Üç sınır: ACTIVE olmayan kart (VOIDED/COMPLETED/REPRINTED) revize EDİLMEZ; iş emri okunamazsa eldeki snapshot'a düşülür (baskı yolu düşürülmez); snapshot NULL eski kartta sürüm ARTMAZ — buildSnapshot yalnız bu fallback + doğuş/reprint'te çağrılır, sonucu canlı sunumla giydirilir. | ÇEKİRDEK | değişmez | `R:2026-08-05__refakat-karti-plan-canli-sunum-donmus` | scripts/test_traveler_card_stale.ts §6 |
| belge-etiket-sablon | Partiler kart snapshot'ına YAZILMAZ; baskıda resolveLiveBatches ile canlı (kart WO açılışında, parti attachRolls'ta doğar). Üç süzgeç: mergedIntoId!=null basılmaz, sayım/metrajda K18_DEAD_STATUSES dışlanır, sevk yalnız iptal edilmemişlerden. 'Sevk brüt/donmuş' kuralı karta UZANMAZ. | ÇEKİRDEK | değişmez | `R:2026-08-03__refakat-karti-a5-varsayilan-parti-blogu` | scripts/test_traveler_card_a5_batches.ts §C |
| belge-etiket-sablon | Sayfaya bağlı HER ölçü tek kaynakta traveler-card.density.ts; tek transform:scale() YASAK — genişlik ~0.68 (132/194mm) ve yazı ~0.85 (okunabilirlik) iki ayrı oran; A4 sütunu eski sabitlerin aynısı, çıktı bayt-bayt; kapalı blokta tek bayt basılmaz (${…} kendi satırına konmaz). | ÇEKİRDEK | değişmez | `R:2026-08-03__refakat-karti-a5-varsayilan-parti-blogu` | scripts/test_traveler_card_a5_batches.ts §A/§E |
| belge-etiket-sablon | Tek seferlik baskı bayrakları — kart ?pageSize=A4\|A5, belge ?rowNotes=1 / ?rowTags=1 / ?sections= — kalıcı ayarı EZER (pure OR), hiçbir yere yazılmaz, yeni sürüm doğurmaz; OR yalnız renderer'da TEK noktada; geçersiz değer sessizce yok sayılır (baskı yolu 400'e düşmez). rowNotes ≠ rowTags. | ÇEKİRDEK | değişmez | `R:2026-08-03__refakat-karti-a5-varsayilan-parti-blogu`, `E:undated__tek-seferlik-baski-bayragi`, `R:2026-07-30__2026-07-30-cuval-notu-sack` | scripts/test_sack_note_document.ts; test_traveler… |
| belge-etiket-sablon | TravelerCard.contentDirty'nin tek yazarı markTravelerCardDirtyTx (fazla işaretlemek güvenli, eksik hata). K18'in 'ilk parti ataması bayraklanmaz' kuralı karta KOPYALANMAZ — kart parti doğmadan basılabilir, ilk doğuş bayatlatır. | ÇEKİRDEK | değişmez | `R:2026-08-05__2026-08-05-parti-no-k`, `R:2026-08-21__2026-08-21-aksam-tutarlilik-taramasi` | scripts/test_label_dirty_sources.ts + test_order_… |
| belge-etiket-sablon | GET /traveler-cards/:id/html bayrağı TEMİZLEMEZ (önizlemeyi de besler, GET yan etkisiz); temizleyen POST /print-event (istemci yalnız baskı BAŞARIYLA bitince çağırır, iptal temizlemez) ve reprint. Rozet yüzeyi kaldırıldı (08-06), backend mekanizması DURUR; geri istenirse planKey karşılaştırması. | ÇEKİRDEK | değişmez | `R:2026-08-05__2026-08-05-parti-no-k`, `R:2026-08-05__refakat-karti-plan-canli-sunum-donmus` | scripts/test_traveler_card_stale.ts |
| belge-etiket-sablon | Kart şablonu üç kademe: BUILTIN · SECTIONS (stüdyoda bölüm sırası/aç-kapa) · RAW_HTML (tüm HTML kullanıcıda, yerleşik CSS yüklenmez). Çözüm: açık seçim > varsayılan şablon > sistem ayarı; tablo boşken Faz 2 öncesiyle birebir (şablon oluşturmak aktif karar); tek giriş renderTravelerCard. | ÇEKİRDEK | değişmez | `R:2026-08-03__refakat-karti-sablonu-uc-kademe-2026` | scripts/test_traveler_template.ts |
| belge-etiket-sablon | Şablon çözümü FAIL-CLOSED: açık templateId çözülemez/pasifse 404, yerleşiğe SAPMAZ; varsayılanın kaldırılması sapma DEĞİL, yerleşiğe dönüş. Gövdesi boş RAW_HTML yerleşiğe düşer (boş kâğıt basmak en kötü sonuç). ⚠️ Bugün üç baskı çağıranı da null geçer — açık seçim dalının canlı çağıranı YOK. | ÇEKİRDEK | değişmez | `R:2026-08-03__refakat-karti-sablonu-uc-kademe-2026` | scripts/test_traveler_template.ts §F (F1/F2 — res… |
| belge-etiket-sablon | traveler_card_templates_isDefault_key partial unique WHERE isDefault=true OLMAK ZORUNDA (düz unique sistemde toplam iki şablona izin verirdi); test_db_invariants envanterinde. | ÇEKİRDEK | değişmez | `R:2026-08-03__refakat-karti-sablonu-uc-kademe-2026` | scripts/test_db_invariants.ts |
| belge-etiket-sablon | Belge A4→A5 ölçeklemesi tek scale() DEĞİL, iki oran: genişlik ~0.68 (geometrik), yazı ~0.85 (okunabilirlik). İki yoğunluk profili: doc-density.ts (altı belge ORTAK) + fason-ceki.density.ts (grid) + traveler-card.density.ts; belge kendi A4 sayısını verir, scaleW/scaleF uygular. | ÇEKİRDEK | değişmez | `R:2026-08-05__2026-08-05-belge-yerlesimi-a5`, `R:2026-08-03__refakat-karti-a5-varsayilan-parti-blogu` | scripts/test_doc_density_fields.ts + test_fason_c… |
| belge-etiket-sablon | Belgeye özel CSS kuralı ortak katmandan SONRA basılır (eşit özgüllükte sonra gelen kazanır; sıra bozulursa belgeler sessizce birbirine benzer). DOC_PAGINATION_CSS ortak katmanda (çok sayfalı tabloda 2. sayfa başlıksız kalmasın). totRow:false iade irsaliyesine özeldir. | ÇEKİRDEK | reçete | `R:2026-08-05__2026-08-05-belge-yerlesimi-a5` | scripts/test_doc_density_fields.ts |
| belge-etiket-sablon | Belge dondurma sürümü freezeForSource'ta max+1 hesaplanır — sabit 1 yazmak storno sonrası yeniden sevkte docType_sourceId_version unique'ine çarpıp 500 verir. | ÇEKİRDEK | tuzak | `R:2026-08-05__2026-08-05-sevk-storno-iade` | BELİRSİZ — bekçi adı bu turda doğrulanmadı |
| belge-etiket-sablon | Toplu iade: N RollReturn satırı + TEK irsaliye (returnGroupId = grup liderinin id'si); belge üye satırın id'siyle ÇÖZÜLMEZ, yanıttaki documentSourceId (returnGroupId ?? id) kullanılır — aksi hâlde lazy-init aynı grubun ikinci resmi kopyasını dondurur. | ÇEKİRDEK | yasak | `R:2026-08-05__2026-08-05-sevk-storno-iade` | BELİRSİZ |
| belge-etiket-sablon | Fason çekisinde parti TEKİL batchNumber, kabul makbuzunda ÇOĞUL batchNumbers (bir kabul birden çok sevki kapsar); çeki tablosunda parti ÇUVAL değil TOP başına. Eski donmuş belge geriye dönük DOLDURULMAZ — alan yoksa satır/kolon basılmaz, tazeleme yalnız reissue ile. | ÇEKİRDEK | değişmez | `R:2026-08-05__2026-08-05-parti-no-k` | scripts/test_fason_ceki_html.ts |
| belge-etiket-sablon | gridRows sözleşmesi: sayı aralığa KIRPILIR, sayı olmayan varsayılana düşer — baskı yolu düşmez. Eski donmuş çekiler (anahtarsız snapshot) 50'ye düşer — bilinçli; geçmiş görünüm isteniyorsa o belgeye gridRows:20. Slot kontrolü hasSlot ile KESİN (düz includes('>100<') metraj hücresine de uyuyordu). | ÇEKİRDEK | tuzak | `R:2026-08-05__2026-08-05-belge-yerlesimi-a5` | scripts/test_fason_ceki_html.ts |
| belge-etiket-sablon | Snapshot JSON'ları (PrintedDocument.snapshot, Manifest.snapshot) liste sorgusunda ÇEKİLMEZ — yalnız detay/print select'ine alınır. | ÇEKİRDEK | yasak | `B:undated__database-performance-rules-her-zaman-uygula` | yok |
| belge-etiket-sablon | Çuval notu labelDirty'yi YALNIZ etkin çuval şablonu sackNote basıyorsa işaretler (sackNoteAppearsOnLabel — şablon-koşullu); belgedeki satır notları her baskıda canlı çözülür (resolveLiveRowNotes). | ÇEKİRDEK | reçete | `R:2026-08-21__2026-08-21-aksam-tutarlilik-taramasi`, `R:2026-07-30__2026-07-30-cuval-notu-sack` | scripts/test_label_dirty_sources.ts |
| belge-etiket-sablon | Etiket fontu raster: opentype.js (DejaVu TTF → 1bpp glif, fontlar assets/fonts/); barkod bwip-js — izinli paket listesinin etiket kalemleri. | ÇEKİRDEK | karar | `B:undated__allowed-npm-packages` | yok |
| deploy-kurulum | `migrate resolve --applied` SQL'in KOŞTUĞUNU DOĞRULAMAZ (yalnız applied_steps_count=0 satırı yazar; 50s timeout'ta yarım kalan DDL ve hiç çalışmayan db execute de 'uygulandı' görünür) → resolve sonrası doğrulama (\d+ / pg_enum / pg_index.indisvalid) opsiyonel değil. | ÇEKİRDEK | tuzak | `B:undated__database-performance-rules-her-zaman-uygula`, `B:2026-07-30__kopyaya-geri-yukleme` | scripts/apply-migration.ts (doğrulama adımı içeri… |
| deploy-kurulum | Üç migration bekçisi üç ayrı soru sorar ve üçü de gerekir: `npm run check:migrations` (commit edildi mi) · `test_migration_hygiene.ts` (defter tutarlı mı) · `test_schema_drift.ts` (DB gerçekten şema gibi mi; iki bilinen DEFERRABLE FK dışındaki her fark kırmızı). | ÇEKİRDEK | değişmez | `B:undated__database-performance-rules-her-zaman-uygula` | kendileri |
| deploy-kurulum | Pull/yeni dev sonrası ZORUNLU sıra: npm install → prisma:generate → prisma:migrate (= migrate deploy) → dev. Pull'lanmış migration'ı `deploy` uygular, `dev` DEĞİL. generate atlanırsa TS derlenmez; migration atlanırsa P2022 ile audit sessizce kaybolur, sunucu çalışmaya devam eder. | ÇEKİRDEK | reçete | `B:undated__pull-sonrasi-senkronizasyon-yeni-dev-sonrasi`, `B:undated__commands`, `B:undated__prisma-7` | yok |
| deploy-kurulum | `migrate dev` bu repoda her diff'te iki DEFERRABLE composite FK'yı (`rolls_sackId_shipmentId_consistency_fkey`, `swatches_...`) DROP etmek ister → yeni migration `--create-only` ile üretilir, `DropForeignKey` satırları SİLİNİR. Commands'taki çıplak `npx prisma migrate dev` satırı bu şerhi taşımaz. | ÇEKİRDEK | tuzak | `B:undated__database-performance-rules-her-zaman-uygula`, `B:undated__pull-sonrasi-senkronizasyon-yeni-dev-sonrasi`, `B:undated__commands` | Teks-Erp/scripts/test_db_invariants.ts (DEFERRABL… |
| deploy-kurulum | Prisma komutları `Teks-Erp/` İÇİNDEN koşulur; kökten koşulacaksa `--config Teks-Erp/prisma.config.ts`. 'The datasource.url property is required' hatası config'i değil .env'in bulunamamasını gösterir; prisma.config.ts dotenv'i kendi dizinine sabitler. | ÇEKİRDEK | tuzak | `B:undated__pull-sonrasi-senkronizasyon-yeni-dev-sonrasi` | yok |
| deploy-kurulum | Canlı DB'de index migration VARDİYA DIŞINDA deploy edilir (CREATE INDEX büyük tabloda yazma kilidi). App DB'de `statement_timeout=50s` aktif ve uzun DDL'i keser → yüz binlerce satıra index ekleyen migration'ın EN BAŞINA `SET statement_timeout = 0;`. | ÇEKİRDEK | yasak | `B:undated__database-performance-rules-her-zaman-uygula`, `B:2026-06-12__x` | yok |
| deploy-kurulum | timestamptz GERİ ALMASI ileri yönün aynası değil: iki tarih kolonunu karşılaştıran CHECK tip geçişinde oturum tz'sine duyarlı → geri alma script'i `SET timezone='UTC'; BEGIN;` ile başlar. `ALTER … TYPE timestamptz` USING'siz yazılmaz (`USING c AT TIME ZONE 'UTC'`); maliyet satırla doğrusal. | ÇEKİRDEK | tuzak | `B:2026-08-01__sema-tip-konvansiyonu-o-11-2026` | yok |
| deploy-kurulum | Kopyaya geri yükleme: yedek `<canlı>_restore_<damga>`ya yüklenir, doğrulanır, iki `ALTER DATABASE RENAME` ile takas (DATABASE_URL değişmez). Üç sezgiye aykırı kural: per-DB ayar OID'ye bağlı, rename ile TAŞINMAZ · `TEMPLATE template0` ŞART · `applied_steps_count` KULLANILMAZ. Silme allowlist'li. | ÇEKİRDEK | reçete | `B:2026-07-30__kopyaya-geri-yukleme` | yok |
| deploy-kurulum | [PROFİL] `statement_timeout=50s` DB-level `ALTER DATABASE` ile MANUEL uygulanır (migration ile değil). DB adı ortama göre: dev `tekserp_demo` (.env, port 55433), sahadaki Windows sunucu `tekserp` — adı dokümana sabitleme, `.env`'den doğrula. | PROFİL | ölçüm | `B:2026-06-12__x`, `R:2026-09-02__2026-09-02-03-modul-anahtarlari` | yok |
| deploy-kurulum | DB'ye YAZAN bekçiler hedef-DB env-override kapısından geçer (`scripts/lib/hedef-db-kapisi`) — `.env` artık demo DB'yi gösterir, yazan test yanlış DB'ye düşmesin. | ÇEKİRDEK | değişmez | `R:2026-09-02__2026-09-02-03-modul-anahtarlari` | scripts/lib/hedef-db-kapisi.ts (kapının kendisi) |
| deploy-kurulum | Sağlık sondası: `/health` PUBLIC ve `auditGuard` alanı YOK — o alan `/api/admin/health`'te (verifyToken + admin:settings). Runbook/prod notundaki `/health \| findstr auditGuard` hiç geçemez. | ÇEKİRDEK | tuzak | `R:2026-08-25__2026-08-25-prod-un-uc` | yok |
| deploy-kurulum | İstemci sürüm politikası tek kaynak `CLIENT_VERSION_POLICIES`: değer KODDA sabit (panelde ayar DEĞİL) · uç `GET /api/client-policy/:istemci` PUBLIC (giriş öncesi sorulur) · istemci FAIL-OPEN (bilinçli istisna) · tanımsız istemci 404, boş politika değil · yeni istemci defter satırı. | ÇEKİRDEK | değişmez | `B:undated__istemci-surum-politikasi-panel-tablet-uyumu` | Teks-Erp/scripts/test_client_policy.ts |
| deploy-kurulum | UZUN KOŞAN dev backend başkasının migration'ıyla sessizce bayatlar: DB'ye gelen yeni enum değerini eski Prisma client P2023 ile okuyamaz, belirti 'uç 400 veriyor' — sebep ÜRETİLMİŞ CLIENT → `prisma generate` + restart. (`pkill -f "tsx src/server.ts"` YASAK — tasarım §12-12.) | ÇEKİRDEK | tuzak | `A:2026-08-26__2026-08-26-aksam-sebep-listesi`, `R:2026-09-03__2026-09-03-superadmin-p2-satici` | yok |
| deploy-kurulum | Şema değişikliği sonrası `npm run prisma:generate` ZORUNLU (Prisma 7). Aynı kural Commands, Pull-sonrası ve Version Gotchas'ta üç kez yazılı — tek satır yeter. | ÇEKİRDEK | değişmez | `B:undated__prisma-7`, `B:undated__commands`, `B:undated__pull-sonrasi-senkronizasyon-yeni-dev-sonrasi` | yok |
| deploy-kurulum | Kök tsconfig yalnız `src/` (lint de `eslint src`) → `npm test` ÖN KOŞUL olarak `tsconfig.scripts.json` (scripts/+prisma/+src/, noEmit) geçidini koşar; tek test koşarken atlanır, acil kaçış `SKIP_TYPECHECK=1`. Build kök tsconfig ile; çıktıyı grepleyeceksen `typecheck:plain`. | ÇEKİRDEK | değişmez | `B:undated__ve-tip-kontrolu-in-on-kosulu`, `B:undated__commands` | Teks-Erp/scripts/run-all-tests.ts:176 |
| deploy-kurulum | Commands listesi BAYAT: `npm run build` = `tsc && node scripts/build-araclar.mjs` (ikinci adım yazılı değil); `npm run seed` 'test verisi' DEĞİL 'temiz fabrika kurulumu seed'i (tek kullanıcı admin + master data); listede yok: `test`, `superadmin:kur`, `seed:fixtures`, `check:migrations`. | ÇEKİRDEK | ölçüm | `B:undated__commands` | yok |
| deploy-kurulum | Backend izinli npm paketleri package.json ile birebir; kural 'paket ekleme, kendin yaz' (node-cron→setInterval, handlebars→kendi motor). Tek gerekçeli istisna `bonjour-service` 1.4.4 SABİT (^ yok — sonraki ana sürüm ESM-only olup require'ı sessizce kırar), tek dosyada tembel require + try/catch. | ÇEKİRDEK | yasak | `B:undated__allowed-npm-packages` | yok |
| deploy-kurulum | DB performans kuralları (14 madde): FK index zorunlu (audit FK'ları istisna) · composite'te eşitlik önce range sonra · sık birlikte filtre = tek composite · yüksek hacimde cursor + MAX_OFFSET=10000 · JSON sorgusuna GIN önce · include yerine select · aggregation $queryRaw · createMany · EXPLAIN. | ÇEKİRDEK | değişmez | `B:undated__database-performance-rules-her-zaman-uygula` | yok (MAX_OFFSET guard runtime 400) |
| fason | 'Kalan gelmeyecek' = `POST /api/subcontractor/close-remainder` (ELLE, sebep zorunlu, zaman aşımı YOK): top `SUBCONTRACTOR_CONSUMED`, kalan RollVariance SCRAP source `SUBCONTRACTOR_REMAINDER` (ROLL_SCRAP kataloğu, fail-closed), kaleme `remainderClosedAt` damgası (damgasız kalem hep açık). | ÇEKİRDEK | reçete | `R:2026-08-19__2026-08-19-fason-kismi-kabul` | scripts/test_fason_partial_receive.ts P6 |
| fason | Kısmi kabulde movement AÇIK kalır; SON teslimatta kapanır, `qtyOut = COALESCE(qtyIn, currentQty)` (sevk edilen TOPLAM). `RollOperation` `SUBCONTRACTOR_RETURNED` unique (rollId, stepId, opType) → ikinci teslimatın op satırı düşer; teslimat izi MAKBUZ KALEMLERİNDEDİR, op 'ilk dönüş olayı'dır. | ÇEKİRDEK | değişmez | `R:2026-08-19__2026-08-19-fason-kismi-kabul` | scripts/test_fason_partial_receive.ts P4 |
| fason | Fazla dönen (`receivedQty` > kalan ya da verilmez) → TAM kabul: deftere kalan yazılır (clamp), `isPartial=false`, top tüketilir — eski APK davranışı birebir. Fiziksel olarak fazla gelen metraj ayrı eksende OVERAGE sapması olur; defter (düşülen) ile fiziksel (doğan) iki ayrı büyüklüktür, karıştırma. | ÇEKİRDEK | değişmez | `R:2026-08-19__2026-08-19-fason-kismi-kabul`, `R:2026-08-21__2026-08-21-fason-kabulu-cekme` | scripts/test_fason_partial_receive.ts P7 |
| fason | Fason karnesi: fire YALNIZ KAPANMIŞ kalemlerden; dönen = kabul defteri `SUM(COALESCE(receivedQty, currentQty))` (DISTINCT ON DEĞİL) + sapma defteri neti (SCRAP −, OVERAGE +, `reversedAt IS NULL`); açık bakiye = giden − defter, çekme `openQty`'ye GİRMEZ; doğrudan sevk edilenler fire hesabına girmez. | ÇEKİRDEK | değişmez | `R:2026-08-19__2026-08-19-fason-kismi-kabul`, `R:2026-08-21__2026-08-21-fason-kabulu-cekme` | scripts/test_fason_partial_receive.ts P9 |
| fason | Fason firma TEST FIXTURE'ı seed'den ÇÖZÜLMEZ, `fixture-subcontractor.ts` ile ÜRETİLİR (`ensureTestDyeHouse`/`ensureTestSander`/`ensureTestKartela`; `TEST-FASON-*` kodlu, idempotent upsert, SİLİNMEZ). 'Herhangi bir aktif firma bul' ÇÖZÜM DEĞİL — pasif kayıt bulunur, ilk çağrı patlar. | ÇEKİRDEK | değişmez | `B:undated__fixture-seed-master-data-si-business` | yok |
| fason | `scripts/`+`prisma/` tip kontrolü `npm test`'in ÖN KOŞULU (`tsconfig.scripts.json`; tek testte atlanır, kaçış `SKIP_TYPECHECK=1`; build kök tsconfig). Sessiz desen: olmayan enum üyesiyle süzme (`StationKind.EXTERNAL` — fason `StationType.EXTERNAL`) Prisma'da koşulu ATAR, garanti yok olur. | ÇEKİRDEK | tuzak | `B:undated__ve-tip-kontrolu-in-on-kosulu` | scripts/run-all-tests.ts typecheckGate |
| fason | K1 (2026-08-21): Fason Sevk WO picker'ı `excludeWithOpenDispatch=true` ile açık+outstanding sevki olan WO'ları gizler ve koşul `OPEN_OUTSTANDING`'den gelir → kabul iptali (LIFO) sonrası yeniden açılan sevk de gizlenir, tamamen doğrudan-sevk edilmiş WO listeye döner. | ÇEKİRDEK | karar | `R:2026-08-21__2026-08-21-aksam-tutarlilik-taramasi` | scripts/test_fason_open_dispatch_single_source.ts |
| filtre-liste-arama | Statü ÇOKLU daraltması kapsam anahtarı `status`a DOKUNMADAN ayrı anahtarla yapılır: rolls'ta `statusIn[]` (öncelik statusIn > status > varsayılan STOCK) — 2026-08-25 Arşiv 'Kayıt Türü' emsali. | ÇEKİRDEK | reçete | `R:2026-08-06__2026-08-06-filtrelerde-coklu-secim`, `A:2026-08-05__2026-08-05-elle-eklenen-topu` | yok |
| filtre-liste-arama | `GET /rolls/entry-users` + `/rolls/entry-stations` lookup uçları kataloğu değil GERÇEK veriyi döner (top girmiş kullanıcı/istasyon, groupBy); izin `roll:read \| MOBILE_ROLL_READ` (kullanıcı listesi admin:users arkasında — yeni bilgi sızmaz); route'lar `/:id`den ÖNCE. | ÇEKİRDEK | değişmez | `R:2026-08-12__2026-08-12-giris-izlenebilirligi-kim` | test_filter_multi_select.ts §2b (lookup sayımı) |
| filtre-liste-arama | Kesim çocukları `createdMachineId` damgası alır (dört TAMBUR_SPLIT doğum yolu: cutWarehouseRoll · finalizeWarehouseCut · cutOpenFabric · finalizeOpenFabric) — yoksa 'Bu makine' süzgeci kesimleri hiç görmez; eski kesimler geriye doldurulmadı (entryStationId emsali). | ÇEKİRDEK | reçete | `R:2026-08-12__2026-08-12-top-listesi-filtresi` | test_tambur_cut_idempotency.ts (+1 damga) · test_… |
| filtre-liste-arama | Sevkiyat listesi union'ında alan adı İKİ tarafta FARKLI: `Shipment.dispatchedAt` ≙ `DirectShipment.shippedAt` (NON-NULL) — sıralama, tarih aralığı ve keyset cursor where'i direct dalda `shippedAt`e çevrilir, aynı `cw` nesnesi paylaşılmaz. `attachTotals` metraj+adet BRÜT (kg geri-ekleme İSTEMEZ). | ÇEKİRDEK | tuzak | `R:2026-08-02__2026-08-02-brut-kurali-liste`, `R:2026-08-06__2026-08-06-filtrelerde-coklu-secim` | test_shipment_list_gross.ts |
| filtre-liste-arama | İstemci sürüm politikası KODDA sabit (`CLIENT_VERSION_POLICIES`, panelde ayar DEĞİL); uç `GET /api/client-policy/:istemci` PUBLIC; istemci FAIL-OPEN (uç okunamazsa kilitlenmez — bilinçli istisna); tanımsız istemci 404 (boş politika değil); `minVersion` sahadaki sürümden BÜYÜK OLAMAZ, yalnız gerçek kırılmada yükselir. | ÇEKİRDEK | değişmez | `B:undated__istemci-surum-politikasi-panel-tablet-uyumu` | test_client_policy.ts |
| filtre-liste-arama | Yedek ön ekleri yaşam döngüsüdür (TEK KAYNAK backup-naming.helper.ts): `tekserp_` rotasyona GİRER, `premigrate_`/`pre-restore_` rotasyon DIŞI; rotasyon filtresi yalnız `tekserp_`e bakar; cutoff = min(ad damgası, mtime) — ad damgası dump BAŞLANGICI, mtime BİTİŞ. (Bu küme konusu değil — yedek kümesine ait.) | ÇEKİRDEK | değişmez | `B:undated__yedek-on-ekleri-yasam-dongusu` | yok |
| finans-saglamlik | `dispatchedAt` sıralamasında union'ın iki tarafında alan adı FARKLI (`Shipment.dispatchedAt` ↔ `DirectShipment.shippedAt`): direct dalı için orderBy VE cursor where AYRI kurulur, aynı `cw` nesnesi paylaşılmaz; DirectShipment.shippedAt NON-NULL → sortNullable yok. | ÇEKİRDEK | tuzak | `R:2026-08-02__2026-08-02-brut-kurali-liste` | test_shipment_list_sort.ts |
| finans-saglamlik | Ekstre satırı ters-kayıt bağını KENDİSİ taşır (`reversedByTxnId`, `reverses` ilişkisinden türer) — panel yüklemleri kesin bilgiyle çalışır; sezgisel sayım yalnız eski-backend fallback'idir. | ÇEKİRDEK | değişmez | `R:2026-08-14__bes-saglamlik-sinifi-2026-08-14` | yok |
| finans-saglamlik | Negatif kasa guard'ında asıl serileştirici 8028 advisory kilididir (kasa/banka dönem kilidi uzayı); `FOR UPDATE` yalnız derinlik savunmasıdır. Storno/ters yollar guard'dan MUAF — muafiyet bekçinin asıl negatif sondasıdır. | ÇEKİRDEK | değişmez | `R:2026-08-14__bes-saglamlik-sinifi-2026-08-14` | test_cash_negative_guard.ts |
| finans-saglamlik | `ItemPrice` satırı fiziksel SİLİNİR (soft-delete'in bilinçli istisnası); şemada `isActive` YOK ve EKLENMEZ — pasif satır `resolveItemPrice` sırasına ('müşteri istisnası > kart varsayılanı > null') üçüncü durum ekler. Geçmiş `InvoiceLine.unitPrice`/`Roll.purchasePrice` ile DONMUŞ. | ÇEKİRDEK | karar | `R:undated__sadece-soft-delete-veya-gercek-fire` | yok |
| is-emri-siparis-bag | Son bağ kaldırmada tek 400: hedef kumaşı NULL olan ORDER iş emri (STOK'un değişmezi). Sipariş iptali `UNLINK_ONLY` da kalan bağ 0 ise STOK'a döndürür (CANCELLED/SUPERSEDED hariç, `targetItemId NOT NULL`). Geçmiş tutarsızlık migration DEĞİL, dry-run `fix_workorder_type_from_links.ts --apply`. | ÇEKİRDEK | reçete | `R:2026-08-21__2026-08-21-is-emri-tipi` | test_workorder_order_link.ts §5b (hedef-kumaşsız … |
| is-emri-siparis-bag | `WorkOrderStep.status` türetilmiş, sedsiz alan; §20 mutabakatı yalnız `test_consistency.ts`te (SQL dosyasında YOK). §20 kırmızısı otomatik drift değildir: tek topu sonradan iptal edilen COMPLETED adım beklenen PENDING'e çöker — tarihsel olgu; test bu istisnayı süzmez, satır elle yorumlanır. | ÇEKİRDEK | tuzak | `B:undated__3-ayda-bir`, `A:2026-08-22__2026-08-22-13-kok-nedeni`, `A:2026-08-04__2026-08-04-giris-noktasi-kurali` | test_consistency §20 (kör nokta dahil ölçer, süzm… |
| is-emri-siparis-bag | Mutabakat takvime bağlı DEĞİL: `npm test` (`run-all-tests.ts`, tüm `test_*.ts`) `test_consistency`i koşar, drift KIRMIZI (psql hep exit 0). `consistency-check.sql` operatör satırları GÖRSÜN diye durur; mantık ÖNCE orada değişir. §10.2 hâlâ 3 aylık elle. | ÇEKİRDEK | reçete | `B:undated__3-ayda-bir` | run-all-tests.ts (test_* keşfi) → test_consistency |
| is-emri-siparis-bag | `Order.branchId` opsiyoneldir (eski kayıtlar null; Zod `.optional().nullable()`), zorunlu varsayan sorgu/tip yazma; bir sipariş TEK şubeye gider. | ÇEKİRDEK | değişmez | `R:undated__sube-bazli-planlama` | yok |
| is-emri-siparis-bag | Sipariş kalemi rengi genel update ile DEĞİL dar uç `PATCH /orders/:id/lines/:lineId/color` ile değişir (order:write + sebep + audit). | ÇEKİRDEK | reçete | `R:2026-08-21__2026-08-21-uretim-rengi-tek` | yok |
| is-emri-siparis-bag | WO kapaması YALNIZ terminal-guard'lı `completeWorkOrderIfStepsDone` ile (CANCELLED/SUPERSEDED asla COMPLETED'a dirilmez — `notIn`). Üretim çıktısı kümesi TEK KAYNAK `producedOutputWhere`: liste ÇIKAN metriği ile detay `producedRolls` aynı kümeden, elle kopyalanmaz. | ÇEKİRDEK | değişmez | `R:2026-07-27__tambur-finalize-wo-disiplini-2026-07` | yok |
| is-emri-siparis-bag | Ölü stok TEK tanım: eşikten ESKİ **ve** spec'i SİPARİŞSİZ (kesişim). Top→sipariş bağı olmadığı için siparişsizlik SPEC düzeyinde ölçülür (talebi aşan metraj), fiziksel top eşlenmez; iptal edilmiş sipariş kalemi talep SAYILMAZ → spec siparişsize düşer. | ÇEKİRDEK | değişmez | `R:2026-08-09__2026-08-09-rapor-temeli-finalizedat` | yok |
| kalite-istasyon-yetenek | Faz A köprüsü: yüklem `kind === PROCESS_QC \|\| appliesQuality` okur ve backfill'e GÜVENMEZ; seed'e açıkça `appliesQuality: true` yazılır (seed migration'dan SONRA boş tabloda koşar — 2026-08-10 appliesColor tuzağı). Faz B'de dal düşmeden seed + prod ölçümü ŞART. | ÇEKİRDEK | tuzak | `R:2026-09-03__2026-09-03-kalite-istasyon-yetenegi` | test_station_quality_capability §2 + §7 |
| kalite-istasyon-yetenek | `assertWoAtStepKind(PROCESS_QC)` (kursun-qc.service.ts:254) DOKUNULMAZ: `roll-step.helper` Tambur ile paylaşılıyor (tambur.service:454/:3746); imzayı yetenekleştirmek Tambur kart-okutma yolunu da değiştirir. Bekçide gerekçeli MUAF (KIND_MUAF, sayı 1) — Faz B R1. | ÇEKİRDEK | yasak | `R:2026-09-03__2026-09-03-kalite-istasyon-yetenegi`, `R:undated__kursun-qc2-tek-fiziksel-istasyon` | test_station_quality_capability §4 KIND_MUAF (iki… |
| kalite-istasyon-yetenek | Seed ham prisma kullanır, `StationService`in 'kategoriden tohumla' adımı KOŞMAZ → yetenek bayrakları (appliesColor/appliesProperty/appliesQuality) seed'de AÇIKÇA yazılır; kolon varsayılanına güvenen seed taze kurulumda boyahaneyi renksiz, KK istasyonunu kalitesiz doğurur. | ÇEKİRDEK | tuzak | `R:2026-08-10__2026-08-10-11-uretim-karakteristigi`, `R:2026-09-03__2026-09-03-kalite-istasyon-yetenegi` | test_station_quality_capability §7 (canlı DB seed… |
| kalite-istasyon-yetenek | Final statü kaliteden çözülür: `resolveFinalStatus(code, statusByCode)` → `QualityGrade.targetStatus`; kod yok ya da katalogda yoksa WAREHOUSE. ⚠️ Kolon default'u `targetStatus @default(SCRAP)` (yıkıcı, bilinçli) — create ucunda ZORUNLU olduğu için (F212) hiç uygulanmaz; 'varsayılan WAREHOUSE' finalize kuralıdır, kolo… | ÇEKİRDEK | değişmez | `R:undated__uretim-akisi` | test_finalize_last_step (kalite-null→WAREHOUSE öl… |
| kalite-istasyon-yetenek | `StationProperty.mode` AUTO\|OPTIONAL\|REQUIRED çalışma-zamanı davranışıdır (varsayılan OPTIONAL); kurşun bypass ataması AUTO arar — satırın varlığı yetmez; TAMBUR kat modu OPTIONAL (REQUIRED yalancı beyandı — Tambur akışı capability kapısını çağırmaz). | ÇEKİRDEK | değişmez | `R:2026-08-10__2026-08-10-11-uretim-karakteristigi` | test_property_targetable / test_property_value_se… |
| kesif-cihaz-ag | helmet İKİ AYRI ÖRNEK kurulur (HSTS/CSP-upgrade LAN'a sızarsa panel kullanıcının HSTS önbelleğinde kilitlenir) ve dispatcher'ın adı `helmetMiddleware` olmak ZORUNDADIR (bekçi ada bakar). | ÇEKİRDEK | değişmez | `R:2026-09-01__2026-09-01-patron-modulu-fabrikaya` | Teks-Erp/scripts/test_middleware_order.ts:37 |
| kesif-cihaz-ag | TOTP kendi kodumuzdur ve RFC 4226/6238 vektörleriyle DIŞARIDAN doğrulanır; kurulumun TEK yolu yöneticinin açtığı 15 dakikalık penceredir (TOFU'nun 2FA'da koruduğu tek senaryoyu kapatır). | ÇEKİRDEK | karar | `R:2026-09-01__2026-09-01-patron-modulu-fabrikaya` | yok |
| kesif-cihaz-ag | Katalog ATAMA İÇERMEZ — 'katalog koda, atama script'e/panele'. Unutulan atamayı görünür kılan tek yüzey 'N yetki hiçbir kullanıcıda yok' bandıdır. Yeni ekran görünmüyorsa sırayla bak: (1) satır DB'de mi, (2) kullanıcıya atanmış mı, (3) kullanıcı yeniden giriş yaptı mı (JWT bayat). | ÇEKİRDEK | değişmez | `B:undated__katalog-yine-atama-icermez` | yok |
| kesif-cihaz-ag | Katman sırası Routes → Controllers → Services → Prisma; alt katman atlamak ve route/controller'da `lib/prisma` import'u YASAK. Bilinçli istisna: ince read/ayar uçları (admin/dashboard/feature-flag/customer-branch/production-balance/station-capability) controller'sızdır — Zod parse + servise delege. | ÇEKİRDEK | yasak | `B:undated__architecture-ozet`, `B:undated__is-mantigi-transaction`, `B:undated__swagger-jsdoc` | Teks-Erp/eslint.config.mjs:59-73 (bugün geçmiyor) |
| kesif-cihaz-ag | Testler SERVER'SIZ entegrasyondur: service sınıfı + prisma doğrudan import edilir, HTTP yok; `npx tsx scripts/test_X.ts` tek tek koşar, toplu koşucu `npm test` = `tsx scripts/run-all-tests.ts` (tüm `test_*.ts`'i toplar). | ÇEKİRDEK | reçete | `B:undated__server-siz-entegrasyon-service-sinifi-prisma` | yok |
| kesif-cihaz-ag | DB adını dokümana SABİTLEME, `.env`den doğrula (`\l` / `SELECT current_database()`) — bayat komut örneği var olmayan DB'ye `ALTER` çalıştırtır. Bugün dev `tekserp_demo` (port 55433), saha `tekserp`. Slow query log (>500ms) yalnız üretimde açık, dev'de -1. | PROFİL | yasak | `B:undated__slow-query-log` | yok |
| kk1-idempotency-kuyruk | Sayaç bazlı kartela düşümünün idempotency'si ayrı `SwatchStockReduction` olay modelindedir; token ondadır, `KartelaDispatch`'te BİLİNÇLİ olarak YOKTUR. | ÇEKİRDEK | değişmez | `R:2026-07-14__idempotency-2026-07-14`, `B:undated__78-model-35-enum-idempotency-katmani` | yok |
| kk1-idempotency-kuyruk | `kk1.duplicateGuardEnabled` varsayılan KAPALI (satır yoksa false) ve ENGELLEME değil ONAYLATMA: 409 `POSSIBLE_DUPLICATE` → `confirmDuplicate:true` ile geçilir (eşit metrajlı ardışık toplar meşru). `createInitialEntry`'de `opts.duplicateGuard` ile OPT-IN (F221) — dahili çağıran etkilenmez. | PROFİL | karar | `R:2026-07-14__idempotency-2026-07-14` | Teks-Erp/scripts/test_feature_flag_contract.ts |
| kk1-idempotency-kuyruk | Script çıkışında `$disconnect()` TEK BAŞINA YETMEZ: havuzun `idleTimeoutMillis: 600_000`'i event loop'u 10 dk açık tutar, script 'bitti ama çıkmadı'da kalır (koşucu 180 sn'de SIGTERM → ZAMAN AŞIMI). İki geçerli kapanış: `process.exit(fail>0?1:0)` ya da `$disconnect()` + `pool.end()`. | ÇEKİRDEK | tuzak | `B:undated__cikis-tek-basina-yetmez` | yok |
| kk1-idempotency-kuyruk | Uzun rapor basan scriptlerde `pool.end()` tercih edilir — `process.exit` boruya yazarken stdout'u kırpabilir. | ÇEKİRDEK | reçete | `B:undated__cikis-tek-basina-yetmez` | yok |
| kk1-idempotency-kuyruk | SystemLog arşivi OTOMATİK (`jobs/archive-scheduler.ts`): start +60 sn, 24 saatte bir kontrol, 30 günde bir 6 aydan eskiyi taşır. Manuel `POST /api/admin/system-logs/archive {monthsToKeep:6}` yalnız acil disk baskısında (idempotent). | ÇEKİRDEK | değişmez | `B:undated__systemlog-arsivi-otomatik` | yok |
| kk1-idempotency-kuyruk | Master Data CRUD için yeni kod yazma — `BaseController` + `BaseService` kullan (`searchFields` config'i yeterli). | ÇEKİRDEK | reçete | `B:undated__78-model-35-enum-idempotency-katmani` | yok |
| kk1-idempotency-kuyruk | Rol (yetki şablonu) kataloğu üç parçalıdır: tek kaynak `constants/role-template-catalog.ts` + boot uzlaştırması (izin uzlaştırmasından SONRA, FK sırası) + mekanik bekçi. Rol SAYISINI koda/nota sabitleme — kanonik sayı dosyadadır. | ÇEKİRDEK | değişmez | `B:2026-08-06__rol-yetki-sablonu-katalogu-ayni-uc` | Teks-Erp/scripts/test_role_template_catalog.ts |
| kucuk-kumeler | Backend'de yalnız izinli paket listesindeki paketler kullanılır; yeni ihtiyaçta önce 'kendin yaz' denenir (node-cron→setInterval, handlebars→kendi şablon motoru, CSV paketi→hiç). | ÇEKİRDEK | yasak | `B:undated__allowed-npm-packages` | yok |
| kucuk-kumeler | bonjour-service bilinçli istisnadır: sürüm SABİT (`1.4.4`, `^` YOK — sonraki ana sürümler ESM-only olup `require()` yolunu sessizce kırar) ve kullanım tek dosyada tembel `require` + try/catch (paket kaybolsa da sunucu ayakta kalır). | ÇEKİRDEK | değişmez | `B:undated__allowed-npm-packages` | yok |
| kucuk-kumeler | helmet İKİ AYRI ÖRNEK kurulur (HSTS/CSP-upgrade LAN'a sızarsa panel kullanıcının HSTS önbelleğinde kilitlenir; geri dönüş sunucuda değil tarayıcıdadır) ve dispatcher'ın FONKSİYON ADI `helmetMiddleware` OLMAK ZORUNDA — Express katman adını fonksiyondan alır, isimsiz arrow sıra bekçisini kör eder. | ÇEKİRDEK | değişmez | `R:2026-09-01__2026-09-01-patron-modulu-fabrikaya` | scripts/test_middleware_order.ts |
| kucuk-kumeler | `GET /api/boss/overview` beş bölümü TEK uçtan döner, izin süzmesi SERVİSTE yapılır (yetkisiz bölümün sorgusu HİÇ koşmaz) ve YENİ İZİN KODU AÇILMAZ; patron üretim kartı ADET basar — metraj ikinci bir 'üretilen' tanımı doğururdu. | PROFİL | karar | `R:2026-09-01__2026-09-01-patron-modulu-fabrikaya` | scripts/test_boss_overview.ts |
| kursun-planlama-bypass | Bayrak YALNIZ yeni atama oluşturmayı kapılar: rejim ATAMA SATIRINDA kalıcıdır, ayarda değil — dağıtılmış iş emri bayrak sonradan kapansa da bypass ile biter. | ÇEKİRDEK | değişmez | `R:2026-08-05__2026-08-05-kursun-planlama-birlesik` | scripts/test_kursun_bypass.ts |
| kursun-planlama-bypass | Makine içi sıra `listDistribution` içinde JS ile kurulur (`sortByPlanOrder`), Prisma `orderBy` ile DEĞİL — kaynak `assignedAt asc` yüklenir, sıralama anahtarları adımdadır. Satır düşerse sürükleme DB'ye doğru yazar, ekran hiç değişmez: hata yok, log yok; tek belirti 'sürüklüyorum, geri zıplıyor'. | ÇEKİRDEK | tuzak | `R:2026-08-05__2026-08-05-kursun-planlama-birlesik` | scripts/test_kursun_machine_order.ts (12 kontrol;… |
| kursun-planlama-bypass | Sıralama İKİ kümede yaşar (bekleyen havuz · makine içi) ve ikisi de aynı alanı (`WorkOrderStep.priority`) aynı uçtan yazar; çakışma yok çünkü bir adım aynı anda ya bekleyendir ya bir makinededir ya tablet `open-cards`'ındadır — yeniden numaralama yalnız kendi kümesine dokunur. | ÇEKİRDEK | değişmez | `R:2026-08-05__2026-08-05-kursun-planlama-birlesik` | Electron/src/pages/Operations/KursunDagitim/queue… |
| kursun-planlama-bypass | Kuyruk sıralaması bir PLANLAMA kararıdır, kalite kararı değil: `PATCH /kursun-qc/queue/reorder` üç izni birden kabul eder — `quality:write` ∨ `workorder:distribute` ∨ `mobile:kursun-dagitim`. Daraltma dağıtımcıyı kendi ekranında kilitler. | PROFİL | karar | `R:2026-08-05__2026-08-05-kursun-planlama-birlesik` | yok |
| kursun-planlama-bypass | Kurşun tableti bayrak açıkken salt-okunurdur ama YALNIZ dağıtıma UYGUN adımda — kör bayrak kilidi Tambur'suz rotada çıkmaz üretir. Tek kapı `assertKursunTabletMayWrite`, beş tablet yazma yolunu da kapsar; guard UI'ya güvenmez (offline kuyruk bayrak çevrildikten sonra flush edilir). | ÇEKİRDEK | değişmez | `R:2026-08-05__2026-08-05-kursun-planlama-birlesik` | scripts/test_kursun_regime_lock.ts (21; kilit 7 /… |
| kursun-planlama-bypass | Uygunluk kuralı TEK KAYNAK: `resolveBypassBlockReason` + `loadBypassEligibilitySignals`. `listDistribution` toplu, tablet tekil çağırır; kopyalanırsa ekran 'Ata' derken tablet de yazabilir duruma düşer. | ÇEKİRDEK | değişmez | `R:2026-08-05__2026-08-05-kursun-planlama-birlesik` | scripts/test_kursun_regime_lock.ts |
| kursun-planlama-bypass | `listOpenCards` filtresini uygunlukla GENİŞLETME: uç tablet tarafından 5 saniyede bir yoklanıyor (rota + üç iz sorgusu maliyeti) ve uygun OLMAYAN adımlar listede KALMALI — sebep kartın içindeki bilgi panelinde söylenir. | ÇEKİRDEK | yasak | `R:2026-08-05__2026-08-05-kursun-planlama-birlesik` | yok |
| kursun-planlama-bypass | `GET /kursun-bypass/visibility` TARİHSEL bir uçtur: hiçbir yeni istemci çağırmaz, yalnız sahadaki eski APK'lar için duruyor (silmek deploy penceresinde 404 üretir). Swagger'ındaki 2026-08-02 görünürlük kuralı BAYAT — yeni yüzey ondan beslenmez. | PROFİL | tuzak | `R:2026-08-05__2026-08-05-kursun-planlama-birlesik` | yok |
| kursun-planlama-bypass | Milestone confirmation: Tambur okutması dağıtılmamış kurşun adımını da kapatır — dağıtım MAKİNE ATFI için planlama kolaylığıdır, işin ilerlemesinin ön koşulu DEĞİL. Atama yoksa `findPendingForTambur` SANAL bekleyen üretir (`source:"UNASSIGNED"`), `completeUnassignedFromTambur` kapatır. | PROFİL | karar | `R:2026-08-06__2026-08-06-dagitim-on-kosul` | scripts/test_kursun_unassigned_close.ts (53 kontr… |
| kursun-planlama-bypass | Marker `KURSUN_BYPASS_FINISHED:UNASSIGNED:<uuid>` BASE ön ekinden TÜRETİLİR (`KURSUN_BYPASS_MARKER_PREFIX`), elle yazılmaz: tanıyıcılar `startsWith(BASE)` ile bakar, uyum koparsa çok partili işin İKİNCİ turu 'bypass dışı kapanmış hareket var' diye uygunluğunu yitirir. | ÇEKİRDEK | değişmez | `R:2026-08-06__2026-08-06-dagitim-on-kosul` | scripts/test_kursun_unassigned_close.ts (ön ek uy… |
| kursun-planlama-bypass | Dağıtımsız kapanışta atama satırı yok, atomik claim de GEREKMEZ: yerini `closeBypassMovementsTx`'in `exitedAt IS NULL` guard'ı + kapsam paritesi tutar (N paralel okutma → 1 kapanış). İdempotent tekrarın izi marker'dır (`findCompletedUnassignedClosure`); o dal olmadan offline replay 404 alır. | ÇEKİRDEK | değişmez | `R:2026-08-06__2026-08-06-dagitim-on-kosul` | scripts/test_kursun_unassigned_close.ts (EŞZ: 3 p… |
| kursun-planlama-bypass | Kurşun bypass ataması istasyon özelliğinde MOD `AUTO` arar — `StationProperty` satırının varlığı YETMEZ (OPTIONAL/REQUIRED atlanır). | ÇEKİRDEK | değişmez | `R:2026-08-10__2026-08-10-11-uretim-karakteristigi` | yok |
| kursun-planlama-bypass | Boot uzlaştırması yalnız EKLER: silmez, mevcut satırın `description`/`module` alanını EZMEZ ve hiçbir kullanıcıya izin ATAMAZ ('katalog koda, atama panele'). Kaldırma/yeniden adlandırma bilinçli veri migration'ı ister; sahada izin atanmasını unutmak hâlâ mümkündür. | ÇEKİRDEK | değişmez | `B:2026-08-01__yeni-izin-eklemek-tek-dosya-2026` | scripts/test_permission_catalog.ts |
| kursun-planlama-bypass | İzin sayısını dokümana SABİTLEME — kanonik sayı `permission-catalog.ts`'tedir (2026-09-05 ölçümü 86). `20260801020000_kursun_bypass_permission_catalog` migration'ı duruyor (uygulanmış migration IMMUTABLE) ama EMSAL DEĞİL: yeni izin için benzerini yazma. | ÇEKİRDEK | yasak | `B:2026-08-01__yeni-izin-eklemek-tek-dosya-2026` | yok |
| kursun-planlama-bypass | `scripts/sync-kursun-bypass-permissions.ts` TARİHSELDİR: katalog artık boot uzlaştırmasından gelir, script'in kalan işi admin'e ATAMA + veri ön koşul teşhisi (bayrak · istasyon yeteneği · fiziksel kurşun makineleri). Makineleri YARATMAZ — fabrikaya özgü veridir, yalnız raporlar. | PROFİL | reçete | `B:2026-08-01__yeni-izin-eklemek-tek-dosya-2026`, `R:2026-08-05__2026-08-05-kursun-planlama-birlesik` | yok |
| kursun-planlama-bypass | RBAC modeli: `requirePermission(code)` → `req.user.permissions[]`; izinler DOĞRUDAN kullanıcıya bağlanır (`UserPermission`), ROL MODELİ YOKTUR — tekrar kullanım için `PermissionTemplate`. Kurşun dağıtımının izni `workorder:distribute`, mobil ikizi `mobile:kursun-dagitim`. | ÇEKİRDEK | değişmez | `B:undated__rbac-permission-kodlari` | scripts/test_permission_catalog.ts |
| kursun-planlama-bypass | Alt CLAUDE'daki RBAC modül TABLOSU bayat: FINANCE modülü (6 kod, `finance:read/write/invoice/payment/cheque/close`) hiç yok ve MOBILE listesinde `mobile:siparis`, `mobile:kumas`, `mobile:tambur-duzelt`, `mobile:kk1-yari-mamul` eksik. Tabloyu güncelle ya da kaldır — kanonik liste dosyadadır. | ÇEKİRDEK | karar | `B:undated__rbac-permission-kodlari` | yok |
| modul-bayraklari-profiller | Modül DB anahtarı `<alan>.enabled` kalıbında (7 anahtar); `modul.*` koda girmez; API alanı camelCase. TEK KAYNAK `src/constants/module-flags.ts` (düz string kümeler — dairesel import yasak); Electron `lib/module-flags.ts` metin-birebir AYNA, akıllı şey eklenmez. | ÇEKİRDEK | değişmez | `R:2026-09-02__2026-09-02-03-modul-anahtarlari` | test_module_flags + Electron module-flags.test.ts |
| modul-bayraklari-profiller | Bağımlılık (iplik→ticaret, tezgah→production) İKİ yerde: middleware ÖNCE ön koşulu ölçer (403 `modul:"ticaret", dependent:"iplik"`) + `setFeatureFlags` yazma doğrulaması (400 `MODULE_DEPENDENCY`). Okuyucular HAM değer döner; etkin değer Electron ctx'te tek yerde (`ticaret && iplik`). | ÇEKİRDEK | değişmez | `R:2026-09-02__2026-09-02-03-modul-anahtarlari` | test_module_flags |
| modul-bayraklari-profiller | Ham ayar ucu `PUT /admin/settings/:key` modül anahtarına 400 `MODULE_KEY_RESERVED` döner (düz `"true"` string'i `flagWriteGuard`ı atlardı); modül anahtarının tek yazma kapısı `PATCH /api/feature-flags`. | ÇEKİRDEK | değişmez | `R:2026-09-02__2026-09-02-03-modul-anahtarlari`, `R:2026-09-03__2026-09-03-tamlik-bekcisi-profiller` | test_module_flags (K7) |
| modul-bayraklari-profiller | Grandfathering migration KOŞULLU (`EXISTS rolls` / aktif depo>1) ve değer = DÜNKÜ DAVRANIŞ (ticaret/iplik := finance.enabled, depo := aktif depo>1) — sabit `false` yasak. `readProductionEnabled` satır-yok sigortası (satır yok → true) KORUNUR, `asBoolean`a sadeleştirme yasak. | ÇEKİRDEK | reçete | `R:2026-09-02__2026-09-02-03-modul-anahtarlari` | test_module_grandfathering (SQL VALUES ayrıştırma) |
| modul-bayraklari-profiller | DB'ye YAZAN bekçiler hedef-DB env-override kapısından geçer (`scripts/lib/hedef-db-kapisi`); `Teks-Erp/.env` `tekserp_demo`yu gösterir; hedef `tekserp` (fabrika prod) ise `BEKCI_PROD_ONAY=1` olmadan durur; global durum yazan bekçiler eşzamanlı koşmaz, ajan başına port. | ÇEKİRDEK | reçete | `R:2026-09-02__2026-09-02-03-modul-anahtarlari` | hedef-db-kapisi (koşum kapısı) |
| modul-bayraklari-profiller | Katman sırası Routes → Controllers → Services → Prisma; route/controller'da `lib/prisma` import'u YASAK (ESLint `no-restricted-imports`); ince ayar uçları controller'sız bilinçli istisna, iş mantığı serviste. ⚠️ admin.routes.ts:53/783 bugün ihlal ediyor (bkz. unresolved). | ÇEKİRDEK | yasak | `B:undated__architecture-ozet` | eslint no-restricted-imports (delinmiş) |
| modul-bayraklari-profiller | `PATCH /feature-flags` zinciri: `verifyToken` → `flagWriteGuard` → `requireSettingsPassword` (ASYNC, kapıdan SONRA; süperadmin/belge-only gövde/hash-yok muaf; şifre yalnız `X-Settings-Password` başlığında). Ayar yazan uç kapsamı elle sayılmaz — `src/routes/**` AST tripwire. | ÇEKİRDEK | değişmez | `R:2026-09-03__2026-09-03-ayar-sifresi-p3`, `R:2026-09-03__2026-09-03-tamlik-bekcisi-profiller` | test_settings_password (AST tripwire) |
| modul-bayraklari-profiller | `ScreenEntry.modul` ZORUNLU aidiyet beyanı (`ModulKey` · `cekirdek:*` · `planlanan:fason\|kartela`); GÖRÜNÜRLÜK kuralı DEĞİL — gizleme `visibleWhen`in işi. Tüketicisi 'kapatırsan gizlenir' önizlemesi + tamlık bekçisi; `MODULESIZ_EKRANLAR` muafı bilerek yazılmadı. | ÇEKİRDEK | değişmez | `R:2026-09-03__2026-09-03-tamlik-bekcisi-profiller` | test_screen_catalog + derleme (zorunlu alan) |
| modul-bayraklari-profiller | Kurulum profilleri TS sabiti `constants/module-profiles.ts` (`deploy/` pakete girmez); profil YALNIZ modül anahtarlarını taşır: davranış bayrakları profile GİRMEZ (seed `upsert.update` ile ezer, iki yazar olmaz); fason/kartela anahtarsız → yazılmaz; yedi anahtar her profilde açıkça yazılır. | ÇEKİRDEK | yasak | `R:2026-09-03__2026-09-03-tamlik-bekcisi-profiller` | test_module_profile (§2 tamlık) |
| modul-bayraklari-profiller | Profil job'ı `TEKSERP_PROFIL` yoksa HİÇ yazmaz; satır varsa dokunmaz (tek `createMany skipDuplicates`); bağımlılık/audit/K7 job'da saf ikizle yeniden kurulur; açıklama metinleri üç yazarda birebir; profil uygulama yazma ucu YOK — `GET /admin/module-profile` salt okuma. | ÇEKİRDEK | reçete | `R:2026-09-03__2026-09-03-tamlik-bekcisi-profiller` | test_module_profile (§6 açıklama üçlüsü) |
| mukerrer-namefold | Mükerrer kararı ÇİFT bazlıdır (`duplicate_reviews`, `pairKey = min(aId,bId):max(aId,bId)`): NOT_DUPLICATE çifti kalıcı gizler, DEFERRED işaretler, MERGED'i motor yazar (`MasterDataMergeService.merge` sonrası best-effort hook). | ÇEKİRDEK | değişmez | `R:2026-08-22__2026-08-22-mukerrer-paneli-v2` | test_duplicate_detection |
| parti-no-batch | Sayaç SAKLANMAZ, veriden türetilir: en son doğan KISA parti (`createdAt DESC LIMIT 1`, destek index `batches_createdAt_idx`). Ayar değişimi mevcut numaraları yeniden hesaplamaz; bayrak kapatılıp açılınca sayaç kaldığı yerden devam eder. | ÇEKİRDEK | değişmez | `R:2026-07-13__is-emri-no-parti-2026-07` | scripts/test_batch_number_format.ts §6 (batches_c… |
| parti-no-batch | Sayaç sorgusunun regex'i `^P(0[1-9]\|[1-9][0-9])$` LOAD-BEARING: gevşerse eski günlük kodlar sızar, parse null döner ve sayaç her seferinde P01'e düşer (canlı P01 dururken ikinci P01 doğar). | ÇEKİRDEK | tuzak | `R:2026-07-13__is-emri-no-parti-2026-07` | scripts/test_batch_number_format.ts §1 (SQL süzge… |
| parti-no-batch | `GET /api/batches/number-state`'in 'sıradaki' değeri ÖNİZLEMEDİR, rezervasyon DEĞİL — kilit dışında okunur, arada parti doğarsa gerçekleşen numara farklı olur. Yüzey bunu 'ayrılmış' diye sunmaz. | ÇEKİRDEK | değişmez | `R:2026-07-13__is-emri-no-parti-2026-07` | yok |
| raporlar | _shipped.ts'teki status='DISPATCHED' süzgecini SİLME: kaybını hiçbir bekçi göremez (ölçüldü, yeşil kalıyor) çünkü storno dispatchedAt'i NULL'lar ve aralık süzgeci PLANNED'ı zaten eler. Süzgeç o invariant'a GÜVENMEMEK için duruyor. | ÇEKİRDEK | tuzak | `R:2026-08-09__2026-08-09-rapor-temeli-finalizedat` | yok (bilinçli olarak bekçisiz — arşiv bunu açıkça… |
| raporlar | Fason atfı ÜÇ KOVALIDIR (fabrika içi · firma bilinen fason · firma belirsiz fason): yalnız parentReceiptId JOIN'ine bakmak EKSİK VERİ değil YANLIŞ ATIF üretir (ölçüm: 21 fason dönüşü topun yalnız 6'sında parentReceiptId dolu); entrySource='SUBCONTRACTOR_RETURN' sorguda load-bearing. | ÇEKİRDEK | değişmez | `R:2026-08-09__2026-08-09-rapor-temeli-finalizedat` | test_quality_scorecard.ts (fason kovası kalkınca … |
| raporlar | Parti izlemede İADE EDİLMİŞ TOP müşteri listesinden DÜŞMEZ (iade shipmentId'yi NULL'lar; RollReturn.fromShipmentId ile geri eklenir) — iade eden müşteri şikâyette en çok arananıdır. Kesim çocuğu batchId'yi miras alır → tek batchId sorgusu yeter. | ÇEKİRDEK | değişmez | `R:2026-08-09__2026-08-09-rapor-temeli-finalizedat` | scripts/test_batch_trace.ts |
| raporlar | Kalite Karnesi'nin ÖLÇÜSÜ METREDİR, adet değil (1000 m 1. kalite ile 5 m 2. kaliteyi 1'e 1 saymak oranı anlamsız yapar); kapsam K18_DEAD_STATUSES'i dışlar (tüketilmiş ebeveyn + çocuk = aynı kumaşı iki kez saymak) ama SCRAP'i DIŞLAMAZ — fire gerçek bir üretim sonucudur. | ÇEKİRDEK | değişmez | `R:2026-08-09__2026-08-09-rapor-temeli-finalizedat` | test_quality_scorecard.ts (metraj yerine adet say… |
| raporlar | Fire Karnesi'nde İKİ ÇIPA İKİ BİRİM: hurda finalizedAt + METRE, hata tespiti RollError.detectedAt + ADET — toplanmaması gereken iki sayı aynı birimde basılmaz. Hata bağı LEFT JOIN LATERAL … LIMIT 1 olmak zorunda: düz JOIN iki hatalı 100 m'lik topu 200 m hurda gösterir. | ÇEKİRDEK | tuzak | `R:2026-08-09__2026-08-09-rapor-temeli-finalizedat` | yok |
| raporlar | 'Kesim kaybı' metriği BİLİNÇLİ OLARAK YOK: tüketilen ebeveynin currentQty'si sıfırlandığı için naif Σ(initial−current) 2522 m'lik HAYALİ kayıp raporluyordu; kesim-olayı kaydı doğana kadar yazılmayacak. | ÇEKİRDEK | karar | `R:2026-08-09__2026-08-09-rapor-temeli-finalizedat` | yok |
| raporlar | İade Karnesi'nin PAYDASI BRÜT'tür (brüt kuralının 5. tüketicisi): net paydayla oran şişer, en çok da iadenin yoğun olduğu dönemde. Sebep ÜÇ DURUMLU (katalog/serbest metin/boş), serbest metinler TEK kovada; oran KOHORT DEĞİL ve bu ekranda yazılı. | ÇEKİRDEK | değişmez | `R:2026-08-09__2026-08-09-rapor-temeli-finalizedat` | yok |
| raporlar | Fason Karnesi'nde FİRE yalnız KAPANMIŞ kalemlerden hesaplanır (açık kalem paydada olsaydı dün sevk edilen parti %100 fire görünürdü; bekçide %4↔%68); dönen metraj TÜM kabul satırlarının toplamıdır (DISTINCT ON 52 m sahte fire yazar). | ÇEKİRDEK | değişmez | `R:2026-08-09__2026-08-09-rapor-temeli-finalizedat` | yok |
| raporlar | OTIF'te TERMİNSİZ SİPARİŞ ORANA GİRMEZ AMA GİZLENMEZ: 'zamanında' saymak oranı sahte yükseltir, 'geç' saymak haksız düşürür → paydadan çıkarılır ve sayısı ayrıca basılır (bekçide %66,7 ↔ %50). 'Şu an termini geçmiş ve hâlâ açık' metriği DÖNEMDEN BAĞIMSIZ anlık sayıdır. | ÇEKİRDEK | değişmez | `R:2026-08-09__2026-08-09-rapor-temeli-finalizedat` | yok |
| raporlar | WIP ekranında İKİ ZAMAN ANLAYIŞI: BEKLEYEN anlık snapshot'tır (tarih filtresi ETKİLEMEZ), GEÇEN dönemseldir; istasyon listesi iki kümenin BİRLEŞİMİ; ortalama bekleme TOP AĞIRLIKLI. qtyIn/qtyOut fark kolonu basılmaz (qtyOut=qtyIn tasarım gereği). | ÇEKİRDEK | değişmez | `R:2026-08-09__2026-08-09-rapor-temeli-finalizedat` | scripts/test_wip_scorecard.ts (19 kontrol) |
| raporlar | Stok & Ölü Stok yaş çıpası Roll.statusChangedAt; ÇIPASIZ top yaş kovalarına DAĞITILMAZ ama metrajı toplama girer ve sayısı ayrıca basılır ('yaşı bilinmiyor' ≠ 'yeni'). [status,statusChangedAt] index BİLİNÇLİ yok; tetikleyici dosya başlığında. | ÇEKİRDEK | değişmez | `R:2026-08-09__2026-08-09-rapor-temeli-finalizedat` | scripts/test_stock_scorecard.ts |
| raporlar | Dönem karşılaştırması ORTAK KATMANDA (_shared.resolveCompareRange): prev = aynı uzunlukta önceki pencere (takvim ayı DEĞİL) · prevYear = TAKVİM yılı kaydırması · custom. prev'in bitişi ana dönemin 1 ms öncesi; karşılaştırma yoksa 2. sorgu koşmaz. | ÇEKİRDEK | reçete | `R:2026-08-09__2026-08-09-rapor-temeli-finalizedat` | yok |
| raporlar | resolveDateRange sınırı İSTEMCİNİNDİR: Electron filtresi seçilen günün YEREL 00:00 / 23:59:59.999 anını gönderir (useReportDateRange.ts); backend ekstra gün yuvarlaması YAPMAZ — yaparsa istemcinin niyeti iki kez yorumlanır. Gruplama ayrı sorudur ve fabrika gününe göre kesilir. | ÇEKİRDEK | yasak | `B:2026-08-01__fabrika-gunu-takvim-gunu-tek-kaynak` | yok |
| raporlar | Backend katman haritası: services/ (+ helpers/ + reports/) iş mantığı · transaction · AuditService.log() taşır; routes/ (+ routes/reports/) Swagger JSDoc + verifyToken + requirePermission taşır. Yeni rapor = servis reports/ altına, uç routes/reports/ altına. | ÇEKİRDEK | reçete | `B:undated__is-mantigi-transaction`, `B:undated__swagger-jsdoc` | yok |
| refakat-karti | Basılan HER sürüm `printed_documents` defterine kart satırıyla AYNI tx'te arşivlenir (`archivePrintedVersionTx`; önceki sürümler tarihsel kopyaya düşer, koşulsuz); `GET /html?version=N` arşiv kopyasını içerik donuk / sunum canlı basar. | ÇEKİRDEK | değişmez | `R:2026-08-05__refakat-karti-plan-canli-sunum-donmus` | yok |
| refakat-karti | `planKey` config/template'i DIŞLAR (tasarım değişikliği sürüm artırmaz) ve anahtar sırasından bağımsızdır (düz JSON.stringify YASAK); `buildPlan` dizileri `orderBy` ile deterministik; `snapshot` NULL eski kartta sürüm ARTMAZ; `buildSnapshot` yalnız kart doğuşu + `reprint` içindir. | ÇEKİRDEK | tuzak | `R:2026-08-05__refakat-karti-plan-canli-sunum-donmus` | scripts/test_traveler_card_stale.ts §6 |
| refakat-karti | Sayfaya bağlı HER ölçü tek kaynak `traveler-card.density.ts`; tek `transform: scale()` YASAK — iki oran (genişlik ~0.68 geometrik, yazı ~0.85 okunabilirlik); A4 sütunu eski sabitlerin aynısı, A4 çıktısı bayt-bayt korunur. Aynı patern altı belgeye `doc-density.ts`/`fason-ceki.density.ts` ile yayıldı. | ÇEKİRDEK | değişmez | `R:2026-08-03__refakat-karti-a5-varsayilan-parti-blogu`, `A:2026-08-05__2026-08-05-belge-yerlesimi-a5` | scripts/test_traveler_card_a5_batches.ts §A |
| refakat-karti | `resolveConfigPageSize`→A5 / `resolveFrozenPageSize`→A4 ikilisi kodda duruyor ve 'tekleştirme' yorumu taşıyor; ANCAK A4 dalı hiçbir baskı yolundan ulaşılamaz (sunum canlı: dress() config'i normalize edilmiş canlı config ile ezer). Eski kartın 'A4 basılır' diye varsayılmasına yaslanan yeni kod YAZMA. | ÇEKİRDEK | tuzak | `R:2026-08-03__refakat-karti-a5-varsayilan-parti-blogu`, `R:2026-08-05__refakat-karti-plan-canli-sunum-donmus` | yok |
| refakat-karti | Kart şablonu üç kademe `TravelerCardTemplate`: BUILTIN (dokunmaz) · SECTIONS (bölüm sırası/aç-kapa) · RAW_HTML (tüm HTML kullanıcıda, yerleşik CSS yok). Baskı çözümü: varsayılan şablon > sistem ayarı; tablo boşken çıktı Faz 2 öncesiyle BİREBİR. Tek giriş `renderTravelerCard`. | PROFİL | karar | `R:2026-08-03__refakat-karti-sablonu-uc-kademe-2026` | scripts/test_traveler_template.ts (31 check) |
| refakat-karti | `traveler_card_templates_isDefault_key` şema-DIŞI PARTIAL unique `WHERE isDefault=true` olmak ZORUNDA — düz unique sistemde toplam iki şablona izin verirdi; `test_db_invariants` envanterinde (depo/etiket emsalleri aynı kalıp). | ÇEKİRDEK | değişmez | `R:2026-08-03__refakat-karti-sablonu-uc-kademe-2026` | scripts/test_db_invariants.ts |
| refakat-karti | `TravelerCard.printedAt` `@default(now())` KALDIRILDI (K8, 2026-08-29): kart açılışta doğduğu için hiç basılmadan 'basım tarihi' taşıyordu (sahada 153 kart); alanı yalnız baskı yolu (`print-event`/reprint) yazar — 'printedAt var = basıldı' yeni satırlarda doğru, eski satırlarda değil. | ÇEKİRDEK | karar | `R:2026-07-14__refakat-karti-traveler-card` | yok |
| refakat-karti | `middlewares/` envanteri (auth·rbac·error·device·uuid-param·latency·login-lockout) EKSİK: dizinde 15 dosya var (client-info, demo, finance, module, remote-access, settings-password, system-account, web-hardening listede yok) — liste güncellenmeli ya da 'başlıcaları' diye daraltılmalı. | ÇEKİRDEK | reçete | `B:undated__verifytoken-requirepermission-apperror-prisma-zod` | yok |
| rota-renk-ozellik-kapsama | Fason KABUL rengin/özelliğin FİİLEN uygulandığını `requiredCategory` bayrağından okumaya DEVAM EDER — istasyon bayrağı 'yapabilir' (planlama), kategori bayrağı 'yaptı' (çalışma zamanı) der; ikisini birleştirme. | ÇEKİRDEK | değişmez | `R:2026-08-10__2026-08-10-11-uretim-karakteristigi` | test_station_capability_flags.ts:218-221 (kaynak … |
| rota-renk-ozellik-kapsama | `setCapabilities` renk tarafına yalnız `colorIds` AÇIKÇA gönderilirse dokunur (`[]` hepsini siler, alan yoksa dokunmaz); `properties[].mode` verilmezse mevcut satırın modu korunur (replace = `deleteMany notIn` + `createMany skipDuplicates`, mevcut satır GÜNCELLENMEZ). | ÇEKİRDEK | tuzak | `R:2026-08-02__renk-istasyon-bazli-kisit-degil-2026`, `R:2026-08-10__2026-08-10-11-uretim-karakteristigi` | test_station_capability.ts 8b |
| rota-renk-ozellik-kapsama | Rota adımı renk yeteneğinde ADIMIN kategorisi de okunur; adımda kategori seçilmemişse istasyonun VARSAYILAN kategorisi kullanılır (planlamacının niyeti 'her zamanki hizmet'). Yalnız istasona bakmak, bayrağı açılmamış fason boyahane adımını reddeder. | ÇEKİRDEK | değişmez | `R:2026-08-06__2026-08-06-rota-sablonu-hedef` | test_route_step_targets.ts |
| rota-renk-ozellik-kapsama | Hedefsiz rota yolu bayt-bayt korunur (hedef yoksa ek sorgu/pivot satırı/payload alanı yok); reçetenin KENDİ rotası hedefsiz kalır — hedef zaten `ProductRecipe`'te, ikinci kopya iki kaynak demektir. | ÇEKİRDEK | karar | `R:2026-08-06__2026-08-06-rota-sablonu-hedef` | yok |
| rota-renk-ozellik-kapsama | Kapsama uyarısı 'mal zaten öyle geliyorsa' susar: nitelik topların HEPSİNDE (`every`) varsa — `some` kapıyı sessizce açar; create'te `goods` yalnız `quickStart` verir (mal bilgisi yoksa muafiyet YOK — F221), replace bağlı toplardan CANLI okur; muafiyet hedef rengi SİLMEZ. | ÇEKİRDEK | yasak | `R:2026-08-27__2026-08-27-rota-kapsamasi-hedef` | test_wo_route_coverage_goods.ts (iki negatif sond… |
| rota-renk-ozellik-kapsama | COMPLETED iş emrinde plan (renk/en/uyumsuz-bağ override) DONAR ama uyumlu 'Sipariş Bağla' AÇIK: `PLAN_CHANGE_FROZEN_STATUSES` ↔ link servisinin `FROZEN_STATUSES` (yalnız CANCELLED+SUPERSEDED) İKİ AYRI LİSTE — birleştirme. | ÇEKİRDEK | karar | `R:2026-08-21__2026-08-21-uretim-rengi-tek` | test_wo_target_color_guard.ts §1 |
| rota-renk-ozellik-kapsama | Planlamacı (`workorder:write`) iş emrinin TÜM açık kumaşlarını tek hamlede düzeltir: `POST …/apply-attribute-to-rolls` any-perm (`roll:manual-adjust` ∨ `workorder:write`); `workorder:write` ile gelen çağrıda tekil motora izin listesi VERİLMEZ (dahili çağrı, F221); kumaş (cins) farkı HER ZAMAN 400. | ÇEKİRDEK | reçete | `R:2026-08-21__2026-08-21-uretim-rengi-tek`, `R:2026-08-19__2026-08-19-tambur-plan-sapma` | test_wo_target_color_guard.ts §10 |
| rota-renk-ozellik-kapsama | Tambur 'Sipariş Bağla' (`workorder:write`; gri buton çizilmez); uyumsuz satır YETKİYLE açılır (`roll:manual-adjust`), soruyla değil → `POST /:id/order-links/override` (plan düzelt → toplar → bağ; route'ta ÇİFT `requirePermission` AND); TEK TX DEĞİL — bilinçli. | ÇEKİRDEK | karar | `R:2026-08-19__2026-08-19-tambur-plan-sapma` | test_tambur_plan_gate.ts + test_wo_target_color_g… |
| rota-renk-ozellik-kapsama | 'Boyahaneye Geri Gönder' hedefini SUNUCU çözer (`/tambur/manual/send-to-dye-preview` → `send-to-dye`): kanonik `stepCanApplyColor`, mevcut adımdan ÖNCEKİ en yakın boya adımı (max stepSequence); yoksa 400 `NO_DYE_STEP_IN_ROUTE`; `AT_SUBCONTRACTOR` YAZILMAZ — fiziksel çıkış Fason Sevk'ten. | ÇEKİRDEK | değişmez | `R:2026-08-19__2026-08-19-2-paket-sektor` | yok (bu turda ölçülmedi) |
| rota-renk-ozellik-kapsama | KÜME DIŞI (backend): Katman Routes → Controllers → Services → Prisma; route/controller'da `lib/prisma` import YASAK (ESLint no-restricted-imports); ince read/ayar uçları bilinçli controller'sız. ⚠️ `admin.routes.ts:53` yasağı deliyor (fazA). | ÇEKİRDEK | değişmez | `B:undated__architecture-ozet` | eslint.config.mjs:59-73 (no-restricted-imports '*… |
| rota-renk-ozellik-kapsama | KÜME DIŞI (backend): Yedek ön ekleri yaşam döngüsü (`backup-naming.helper.ts` TEK KAYNAK): `tekserp_` rotasyona girer, `premigrate_`/`pre-restore_` girmez; cutoff = min(ad damgası, mtime). `BACKUP_DIR` yoksa yedek YOK; saat `backup.hour`; rotasyon GÜN bazlı (30 / min 3) — '14'lük' BAYAT. | ÇEKİRDEK | değişmez | `B:undated__yedek-on-ekleri-yasam-dongusu`, `B:2026-07-30__yedekleme-backend-e-ait` | yok (bu turda ölçülmedi) |
| sebep-kataloglari | Gizlenmiş (`isActive=false`) kod doğrulamada GEÇERLİ sayılır — gizleme bir görünürlük kararıdır, geçerlilik kararı değil; eski kayıtların kodu okunmaya devam eder. | ÇEKİRDEK | değişmez | `R:2026-08-19__2026-08-19-hazir-sebep-kataloglari` | test_reason_presets |
| sebep-kataloglari | Yazma yetkisi için YENİ izin kodu üretilmez: `roll:manual-adjust` ∨ `mobile:tambur-duzelt` (create/update/duplicate/reorder aynı kapıdan); `GET /api/reason-presets` yalnız token ister. | ÇEKİRDEK | karar | `R:2026-08-19__2026-08-19-hazir-sebep-kataloglari` | yok |
| sebep-kataloglari | `KIND_STORES_TEXT` true olan kind'ta satıra GÖRÜNEN metin de yazılır (bugün ÜÇ: ROLL_MANUAL_ENTRY · ROLL_CANCEL · ORDER_CANCEL); 2026-08-21'den beri satır KODU DA taşır, yani etiket düzenlemesi geçmiş raporu BÖLMEZ. 'Kod saklanmaz' anlamı düşmüştür. | ÇEKİRDEK | değişmez | `R:2026-08-21__2026-08-21-namefold-db-seddi` | derleme (`as const satisfies Record<ReasonPresetK… |
| sebep-kataloglari | Sebep KODUNU sunucu türetir (`resolveReasonCode`): açık kod katalogda doğrulanır (yoksa 400 `REASON_CODE_INVALID`), yoksa metin `label`/`fullText`/`legacyTexts` ile KATLANMIŞ eşlenir; serbest metne kod UYDURULMAZ (NULL kalır). | ÇEKİRDEK | değişmez | `R:2026-08-21__2026-08-21-namefold-db-seddi` | test_reason_presets §5, §6 |
| sebep-kataloglari | `legacyTexts[]` eski-ad sözlüğüdür: tek yazar `update()`, sınır 20 kayıt, güncel ad öncelikli, iki satır aynı eski metni taşıyorsa sonuç null (belirsizlik uydurulmaz). | ÇEKİRDEK | reçete | `R:2026-08-21__2026-08-21-namefold-db-seddi` | test_reason_presets §6 |
| sebep-kataloglari | `customers` · `items` · `subcontractors` ad seddi partial UNIQUE `<tablo>_nameFold_key` + `WHERE "mergedIntoId" IS NULL`'dır (tombstone aynı adı meşru taşır); RENK bilinçli HARİÇ (`foldColorNameForCompare` DB fold'undan zengin, düz kısıt yanlış güven verir). | ÇEKİRDEK | değişmez | `R:2026-08-21__2026-08-21-namefold-db-seddi` | test_db_invariants §1 · test_master_data_name_dup… |
| sebep-kataloglari | Sed migration'ı mükerrer bulunan tabloda index'i ATLAR (RAISE NOTICE) ve deploy'u DÜŞÜRMEZ; temizlik sonrası aynı dosya yeniden koşulur. Enforce bekleyen kurulumda `test_db_invariants` §1'in kırmızı kalması BİLİNÇLİDİR — bekçiyi bu yüzden daraltma. | ÇEKİRDEK | karar | `R:2026-08-21__2026-08-21-namefold-db-seddi` | test_db_invariants §1 |
| sebep-kataloglari | Uygulama bekçisi `assertNameNotDuplicate` KALDIRILMAZ — anlaşılır 409'u o verir; DB seddi yalnız yarış, config unutması, içe aktarım ve elle SQL yollarını kapatan sessiz son hattır. Yeni tabloya sed eklemeden önce `find_fold_duplicates.ts` koşturulur. | ÇEKİRDEK | değişmez | `R:2026-08-21__2026-08-21-namefold-db-seddi` | test_master_data_name_dup §9 |
| sevkiyat-brut-storno-cuval | Depo çuvalı içeriğine dokunan yol sırayla: `touchWarehouseSackTx` (WHERE shipmentId IS NULL; atanmış çuval 409) → PLANNED küme değişirse `touchShipmentPlannedTx` → içerik değişince `markSackContentChangedTx` (eski adı resetSackWeightsTx; kg sıfırlar, `labelDirty` AYRI updateMany). | ÇEKİRDEK | değişmez | `B:undated__depo-cuvali-icerigine-dokunuyorsa-once-where`, `R:2026-08-21__2026-08-21-aksam-tutarlilik-taramasi` | scripts/test_label_dirty_sources.ts (labelDirty y… |
| sevkiyat-brut-storno-cuval | `Sack.notes` annotation'dır: `setSackNotes` `touchWarehouseSackTx` KULLANMAZ (sevk edilmiş çuvala da yazılır), `markSackContentChangedTx` nota DOKUNMAZ; guard'ı 'eksik' sanıp EKLEME (özellik 409'a düşer). Not yalnız etkin şablon `sackNote` basıyorsa `labelDirty` yazar (`sackNoteAppearsOnLabel`). | ÇEKİRDEK | yasak | `R:2026-07-30__2026-07-30-cuval-notu-sack`, `B:undated__depo-cuvali-icerigine-dokunuyorsa-once-where`, `R:2026-08-21__2026-08-21-aksam-tutarlilik-taramasi` | scripts/test_sack_notes.ts (8b: PLANNED sevkiyatt… |
| sevkiyat-brut-storno-cuval | Toplu muhasebe export'unda sevk satırları BRÜT (`RollReturn` geri-ekleme; snapshot değil — 2000 sevkiyat × çeki satırı perf kuralı 13) ve ayrı 'İade' sayfası: 'sevk − iade = net' doğru; iki kümenin kapsamı bilinçli farklı (sevk = dönemde sevk edilen, iade = dönemde iade alınan). | ÇEKİRDEK | değişmez | `R:2026-08-02__2026-08-02-sevk-rakami-brut`, `A:2026-08-02__2026-08-02-brut-kurali-liste` | yok |
| sevkiyat-brut-storno-cuval | Sevkiyat iptal gövdesi TEK KAYNAK `cancelPlannedShipmentTx` (atomik `expectedStatus` claim): `cancelShipment` ('İptal Et', yalnız PLANNED) ve `undoDispatch({releaseSacks})` onu çağırır — ikinci kopya yazılmaz (ayrışınca storno kapanışı çuvalı unutur). DISPATCHED iptal edilemez, storno gerekir. | ÇEKİRDEK | değişmez | `R:2026-08-22__2026-08-22-sevk-kapisi-bayragin` | scripts/test_shipment_undo_dispatch.ts §10 |
| sevkiyat-brut-storno-cuval | Storno kapsamı DAR ve sebebi SÖYLENİR: `resolveUndoBlockReason` TEK KAYNAK (önizleme + mutasyon aynı yüklem; ayrışırsa ekran 'yapılabilir' der, uç 409). Engeller: faturalanmış · iadesi alınmış · `shipping.undoDispatchSameDayOnly` (varsayılan KAPALI; takvim günü, `factoryDayStart()`). | ÇEKİRDEK | reçete | `R:2026-08-05__2026-08-05-sevk-storno-iade` | scripts/test_shipment_undo_dispatch.ts |
| sevkiyat-brut-storno-cuval | `weighRequiredEnabled` / `manualWeightRestrictedEnabled` yazıldı AÇILMADI (manual kısıtı eski istemcileri 403'e düşürür); düz okuyucu, resolver YOK (§3.6 yalnız ebeveynli bayraklara). Açılınca MANUAL yalnız `shipping:write` taşıyana serbest (`assertManualWeightAllowed`, F221). | ÇEKİRDEK | değişmez | `R:2026-09-03__2026-09-03-dilim-2-davranis`, `M:2026-07-30__cihazda-yazma-2026-07-30` | scripts/test_shipping_flags.ts (önkoşul cümlesi m… |
| superadmin-gizlilik-ayar-sifresi | Süperadmine özel uçlarda 404 değil 403 (varlık açık; 404 yanlış bilgi). `requireSystemAccountOr404` gövdesi forbidden döner — adı bayat, davranışa değil ada güvenme. | ÇEKİRDEK | karar | `R:2026-09-04__2026-09-04-en-yetkili-hesap`, `R:2026-09-03__2026-09-03-ayar-sifresi-p3` | test_settings_password (:428 'fabrika admini → 40… |
| superadmin-gizlilik-ayar-sifresi | Supap defteri `SystemAccountRegistry` (system-account.registry.ts) DB'den beslenir, boot job tazeler; `/auth/me` `systemAccountExists`i guard ile ORTAK yüklem `resolveSystemAccountLock`tan okur (panel↔kapı ayrışmasın); defter TEK YÖNDE tazelenir — hesabı kaldırmak restart ister. | ÇEKİRDEK | değişmez | `R:2026-09-03__2026-09-03-superadmin-p2-satici` | test_superadmin (supap 4 durum, /auth/me tembel d… |
| superadmin-gizlilik-ayar-sifresi | Ayar şifresi hash'i `security.settingsPasswordHash`; `systemSettingService.set()` ile YAZILMAZ (audit oldData/newData HAM) — doğrudan upsert + ayrı olay. `security.*` ön eki ayar listesi ve `config-bundle`dan dışlanır, `PUT /admin/settings/:key` reddeder; `settingsPasswordRequired` bayrak değil. | ÇEKİRDEK | yasak | `R:2026-09-03__2026-09-03-ayar-sifresi-p3` | test_settings_password (§A-§L; system_logs+arşiv+… |
| superadmin-gizlilik-ayar-sifresi | Ayar şifresi YALNIZ `X-Settings-Password` başlığında; gövde/query/cookie fallback YASAK (ölçülmüş sızıntı: morgan URL'i, USED audit yolu). Karakter kümesi ASCII `^[\x21-\x7E]+$`, 8–72 (başlık non-ASCII taşımaz; bcrypt 72 bayt kırpar); `.trim()` yerine RED; backend↔Electron sabitleri ayna bekçili. | ÇEKİRDEK | değişmez | `R:2026-09-03__2026-09-03-ayar-sifresi-p3` | test_settings_password §C (davranış: gövde/query/… |
| superadmin-gizlilik-ayar-sifresi | `requireSettingsPassword` ASYNC ve `flagWriteGuard`dan SONRA (izin önce — yetkisiz kilit sayacını dolduramasın; guard senkron). Muaf: süperadmin · belge-only gövde (`.every`) · hash yok → UYUR. Kilit `bcrypt.compare`den ÖNCE (429); başlık yok 403 REQUIRED; yanlış 403 INVALID+audit; doğru USED audit. | ÇEKİRDEK | değişmez | `R:2026-09-03__2026-09-03-ayar-sifresi-p3` | test_settings_password (§A-§L) |
| superadmin-gizlilik-ayar-sifresi | Ayar şifresi kilit kovası GİRİŞ kovasından AYRI ve ayrım KEY'de (`sp:` ön eki, `resolveSettingsLockoutKeys`): `resolveLoginLockoutKeys` varsayılan `ip` kapsamında kimliği DÜŞÜRÜR — 5 yanlış ayar şifresi tüm IP'nin LOGIN'ini kilitliyordu. Aynı şalter `auth.pinLockoutEnabled`, ayrı SAYAÇ. | ÇEKİRDEK | tuzak | `R:2026-09-03__2026-09-03-ayar-sifresi-p3` | test_settings_password (ayar 429 iken login 200 ö… |
| superadmin-gizlilik-ayar-sifresi | Kapı audit/hata desenleri: USED audit yolu `${req.baseUrl}${req.path}` (originalUrl query sızdırır, salt path mount'a göreli '/'); LOCKED olayı kilit BAŞINA tek satır (`justLocked`, login hizalı); `Retry-After` tek noktadan (error middleware); oturum jti ↔ userId bağı, uymazsa 401 SESSION_INVALID. | ÇEKİRDEK | değişmez | `R:2026-09-03__2026-09-03-ayar-sifresi-p3` | test_settings_password (harness Express mount'unu… |
| superadmin-gizlilik-ayar-sifresi | Kurulum script'i İDEMPOTENT: hesap varsa DOKUNMAZ. Saf `provisionSuperadmin(input, deps)` HİÇBİR ŞEY YAZDIRMAZ — sırlar yalnız DÖNÜŞ değerinde, basma interaktif katmanın işi. `SUPERADMIN_PROVISIONED/ROTATED` audit çağrısı BİLEREK `src/` altında (`test_audit_labels` §3 yalnız src tarar). | ÇEKİRDEK | değişmez | `R:2026-09-03__2026-09-03-superadmin-dogusu-p8`, `R:2026-09-03__2026-09-03-superadmin-dogusu-p8__2` | test_superadmin_provision (stdout 0 bayt · rotasy… |
| superadmin-gizlilik-ayar-sifresi | TTY KAPISI FAIL-LOUD: `interaktif()` `--help` dalından hemen SONRA `process.stdin.isTTY` yoksa `TTY_GEREKLI_MESAJI` + exit 1. `readline` TTY'siz girdide ilk sorudan sonra SESSİZCE SONSUZA KADAR bekler (ölçüldü 120 sn, DB'ye satır yok) — `ssh` (-t'siz)/`docker exec`/pm2/CI kurulumu hesapsız kalırdı. | ÇEKİRDEK | tuzak | `R:2026-09-03__2026-09-03-superadmin-dogusu-p8`, `R:2026-09-03__2026-09-03-superadmin-dogusu-p8__2` | test_superadmin_provision §7 (zaman aşımlı non-TT… |
| surum-yayin | Mobilde sürüm İKİ EKSENLİ: `minVersion` (APK) + `minPaketTarihi` (OTA paketi, `Updates.createdAt`; JS düzeltmesi versionName'i değiştirmez); `paketTarihi=null` ESKİ SAYILMAZ; tanımsızsa yalnız minVersion. Tablet kilidi KOŞULLU (kurulabilir düzeltme + gönderilmemiş kayıt yok). Önce paket çıkar. | ÇEKİRDEK | değişmez | `B:undated__mobil-guncelleme-istemci-surum-politikasi`, `M:undated__uzaktan-guncelleme-ota-apk-sahaya-nasil` | Teks-Erp/scripts/test_client_policy.ts; mobil cli… |
| tambur-finalize-kesim | İş emri ÜRETİM ÇIKTISI kümesi tek kaynak `workorder.service.producedOutputWhere` (Tambur birinci-nesil çocukları + çocuğa bölünmeden final statüye ulaşan finalize çıktıları); liste ÇIKAN metriği ve detay `producedRolls` aynı kümeyi kullanır. | ÇEKİRDEK | yasak | `R:2026-07-27__tambur-finalize-wo-disiplini-2026-07` | scripts/test_produced_buckets.ts + test_consisten… |
| tambur-finalize-kesim | 'Konumu Düzelt' topu IN_PRODUCTION + `currentStepId=hedef` bırakır, hedef adım ACTIVE, COMPLETED WO IN_PROGRESS'e dirilir ve kart yeniden aktifleşir; geri taşımada hedef-sonrası kalite/kurşun kararları VOID (grade→Belirsiz), hayalet movement silinir, SKIPPED→PENDING. | ÇEKİRDEK | reçete | `R:2026-07-30__manuel-tasima-saha-surekliligi-2026-07` | scripts/test_manual_move_field_continuity.ts |
| tambur-finalize-kesim | Kesim çocukları `createdMachineId` damgası alır (dört TAMBUR_SPLIT doğum yolu; controller `stamp?.machineId ?? req.device?.machineId`); eski kesimler geriye doldurulmadı (entryStationId emsali) — süzgeçte görünmezler, dürüst. | ÇEKİRDEK | reçete | `R:2026-08-12__2026-08-12-top-listesi-filtresi` | scripts/test_tambur_cut_idempotency.ts (+1 damga) |
| tambur-finalize-kesim | `SINGLE_RESTORE` ('İş Emrine Geri Al') tek topun metrajıyla dirilme: çocuk iptal, kaynak adıma dirilir, kapalı WO+kart yeniden açılır; KARDEŞLERE dokunulmaz; kapanış sapmaları TERSLENMEZ; `RollError` yeniden açılmaz; ek izin/sebep istemez; arşivli kaynakta ön seçim yok. | ÇEKİRDEK | değişmez | `R:2026-08-12__2026-08-12-gece-single-restore` | scripts/test_tambur_undo.ts |
| top-duzeltme-iptal-fire-geri-alma | Geri alma rafı preCancelStatus'tan (resolveRestoreTargetStatus); NULL/eski kayıt → STOCK (en kısıtsız raf; bloklamak 42 topta regresyon olurdu). Geri alma iptal izi kolonlarını TEMİZLER, geçmiş audit CANCEL_RESTORED'da — açık iş kararı. Mesaj entrySource'a bakar ('yarı mamul stoğunda'). | ÇEKİRDEK | tuzak | `R:2026-08-25__2026-08-25-saha-deploy-sonrasi`, `R:2026-08-27__2026-08-27-3-tur-yari` | test_roll_cancel_undo |
| top-duzeltme-iptal-fire-geri-alma | restoreCancelledRoll tx AÇMAZ ve adım recompute KOŞMAZ — guard hareketli topu reddettiği için (hareketsiz top hiçbir adım sayacına girmez; recomputeStepStatus partiye HİÇ bakmaz). Guard gevşetilirse (hareketli top da geri alınsın) recompute EKLENMEK ZORUNDA. | ÇEKİRDEK | tuzak | `R:2026-08-25__2026-08-25-saha-deploy-sonrasi` | yok |
| top-duzeltme-iptal-fire-geri-alma | Elle eklenen topun geri alınması (MANUAL) DAR: yalnız elle eklenmiş + hiç işlem görmemiş top (MANUAL_UNDOABLE_STATUSES; kesilmemiş, RollOperation yok, tek hareket, çuval/sevk yok); ihlalde çıkmaz bırakmayan blockReason. Parti bağı topta KALIR. Ekleyen kendisi geri alır (mobile:tambur-duzelt). | ÇEKİRDEK | değişmez | `R:2026-08-05__2026-08-05-manuel-top-geri` | test_manual_roll_undo (24) |
| top-duzeltme-iptal-fire-geri-alma | Elle eklenen top PARTİSİZ doğmaz: tek açık parti → sormadan bağla · çok → 400 BATCH_REQUIRED + seçenek listesi (reddeden tarafın ağzından; mobil önden yüklemez) · hiç yok → NULL. 'Açık' = veride canlı top (K18_DEAD_STATUSES); SCRAP partiyi KAPATMAZ. Sıra: payload tutarlılığı ÖNCE, parti SONRA. | ÇEKİRDEK | değişmez | `R:2026-08-04__2026-08-04-roll-foldtype-entryreason` | yok (fazA: bekçi adı verilmemiş) |
| yetki-izin-rol | Uzlaştırma YALNIZ EKLER — silmez, `description`/`module` ezmez; katalogdan kod çıkarmak DB'den kaldırmaz, kaldırma/yeniden adlandırma bilinçli veri migration'ı ister. İzin için ayrı veri migration'ı YAZMA (`20260801020000` emsal değil); canlı DB'ye elle INSERT yok. | ÇEKİRDEK | yasak | `B:2026-08-01__yeni-izin-eklemek-tek-dosya-2026` | scripts/test_permission_catalog.ts (DB \ katalog … |
| yetki-izin-rol | Şablon kimliği `permission_templates.code`, AD DEĞİL (fabrika yeniden adlandırır; ada bakan uzlaştırma ikiz doğurur). `code=null` fabrikanın şablonu, dokunulmaz. Sahiplenme YALNIZ `LEGACY_TEMPLATE_NAME_TO_CODE` — `entry.name`e açmak fabrika şablonunu yutar; ad çakışmasında oluşturma ATLANIR. | ÇEKİRDEK | değişmez | `B:undated__kimlik-dur-ad-degil-migration-fabrik`, `B:undated__sahiplenme-yalniz-uzerinden-kodsuz-eski-seed`, `R:2026-08-06__2026-08-06-rol-sablonlari-koda` | scripts/test_role_template_catalog.ts (legacy eşl… |
| yetki-izin-rol | Sistem rolü SİLİNMEZ, PASİFLEŞTİRİLİR (`deleteTemplate` → isActive=false): sert silme sonraki restart'ta DİRİLİŞ; pasif satır 'kullanmıyorum' kararını kalıcı kılar. `applyTemplate` pasif şablonu 400 ile reddeder, panel gizler; geri açma `PATCH … {isActive:true}`. | ÇEKİRDEK | yasak | `B:undated__sistem-rolu-silinmez-pasiflestirilir-sert-silme`, `R:2026-08-06__2026-08-06-rol-sablonlari-koda` | yok |
| yetki-izin-rol | `mode:"all"` YALNIZ 'Admin (Tam Yetki)' şablonundadır — tanımı liste değil KURAL ('her şey'), her boot katalogla eşitlenir, bayatlayamaz (bekçi: tam bir tane). Diğer rollerde eksik izin EKLENİR, fazlalar KORUNUR, hiçbir izin çıkarılmaz. | ÇEKİRDEK | değişmez | `B:undated__yalniz-admin-tam-yetki-icindir` | scripts/test_role_template_catalog.ts §1 |
| yetki-izin-rol | Kapsam bekçisi ('her izin dar bir rolde mi') 'Admin (Tam Yetki)' DIŞINDAKİ roller üzerinde koşar — hariç tutulmazsa vakumen yeşil. Muaflar `ROLE_COVERAGE_EXEMPT`'te GEREKÇEYLE (bugün 4: admin:*, mobile:tambur-duzelt/kk1-desen/kk1-yari-mamul) ve iki yönlü denetlenir (ölü muaf da düşürür). | ÇEKİRDEK | tuzak | `B:undated__bekcinin-en-kolay-kaybedilen-kurali` | scripts/test_role_template_catalog.ts §2 |
| yetki-izin-rol | Görev ayrılığı (SoD) üçlüsü `shipping:invoice` / `shipping:undo-dispatch` / `roll:manual-adjust` yalnız Muhasebe ve Üretim Süpervizörü şablonlarında; günlük iş rollerine dağıtılmaz. `shipping:invoice` fatura İŞARETİ koyar (ERP fatura kesmez) ve `shipping:write` VERMEDEN atanır. | ÇEKİRDEK | karar | `R:2026-08-06__2026-08-06-rol-sablonlari-koda`, `R:2026-08-02__2026-08-02-brut-kurali-liste`, `B:undated__rbac-permission-kodlari` | scripts/test_role_template_catalog.ts §2 (kapsam)… |
| yetki-izin-rol | `DOCUMENT_DESIGN_FLAG_KEYS` bilerek DAR: `companyName`/`companyLetterhead`/belge logosu firmanın KİMLİĞİDİR, şablon değil — dar kümeye EKLENMEZ. Yeni anahtar eklerken soru 'belge ekranında görünüyor mu' değil, 'yanlış girilirse etkisi belge çıktısıyla SINIRLI mı'. | ÇEKİRDEK | yasak | `R:2026-08-05__belge-tasarimi-ayri-bir-yetkidir-sistem` | yok |
| yetki-izin-rol | Ayar şifresi KAPSAMI elle sayılmaz: yüklem 'ayar ekranı' değil '`system_settings`e YAZIYOR mu' — `src/routes/**` (özyineli) içinde `systemSettingService.set(`/`setFeatureFlags(`/`systemSetting.upsert(` çağıran her zincir `requireSettingsPassword` taşır (AST tripwire, iki yönlü muaf). | ÇEKİRDEK | değişmez | `A:2026-09-03__2026-09-03-ayar-sifresi-p3` | scripts/test_settings_password.ts §J tripwire |
| yetki-izin-rol | Hash `security.settingsPasswordHash` `systemSettingService.set()` ÜZERİNDEN DEĞİL (audit oldData/newData'yı HAM yazar) — doğrudan upsert + ayrı olay (`SETTINGS_PASSWORD_SET/ROTATED/REVOKED`); `security.*` ön eki listeden/config-bundle'dan dışlanır, `PUT /admin/settings/:key` reddeder. | ÇEKİRDEK | değişmez | `A:2026-09-03__2026-09-03-ayar-sifresi-p3` | scripts/test_settings_password.ts (config-bundle/… |
| yetki-izin-rol | Oturum jti'si KULLANICIYA bağlıdır: `verifyToken` 'canlı mı' yanında 'KİMİN' diye sorar (`session.userId !== payload.userId` → 401 `SESSION_INVALID`, aynı kod/mesaj — orakül yok); yoksa JWT_SECRET'ı bilen biri başkasının jti'siyle süperadmin kimliği üretir. | ÇEKİRDEK | değişmez | `A:2026-09-03__2026-09-03-ayar-sifresi-p3` | scripts/test_superadmin.ts §J(c) (yabancı jti 401… |
| yetki-izin-rol | Ayar şifresi yönetim uçları (`GET/PUT/DELETE /admin/settings-password`) yalnız süperadmin; aksi 2026-09-04'ten beri 403 (404 değil). ⚠️ Middleware'in adı hâlâ `requireSystemAccountOr404` — ada bakıp 404 varsayma, gövde `AppError.forbidden`. `verifyToken`dan SONRA takılır (kimliksiz 401). | ÇEKİRDEK | tuzak | `A:2026-09-03__2026-09-03-ayar-sifresi-p3` | scripts/test_settings_password.ts (404→403, 134/0) |
| yetki-izin-rol | Yeni kullanıcı `POST /api/admin/users` (`admin:users`) ile açılır ve VARSAYILAN `grantOperatorDefaults=true` ile üç mobil istasyon izni (`mobile:kk1/kk2-kursun/tambur`) + PIN/QR alır; web kullanıcısı için panel `false` gönderir. 'Yetkisiz başlar' YANLIŞ; masaüstü izinsiz panele giremez doğru. | ÇEKİRDEK | değişmez | `E:undated__test-kullanicilari`, `R:undated__test-kullanicilari` | yok |
| yetki-izin-rol | `admin:*` kısayolu KORUNUR: `tambur-undo`/`import`/`demo` kapıları `matchesPermission(p, X) \|\| matchesPermission(p, "admin:*")` yazımındadır — 3 kullanıcı `admin:*` taşıyor, düz `matchesPermission(p, X)`e çevirmek yetki DÜŞÜRÜR; düz `includes("admin:*")` ise `["*"]`ı tanımaz, yasak. | ÇEKİRDEK | karar | `A:2026-09-03__2026-09-03-superadmin-p2-gizli` | scripts/test_superadmin_visible.ts (`includes("ad… |
| yetki-izin-rol | Kimliksiz (PUBLIC) uç yalnız GEREKÇEYLE: login, cihaz el sıkışması, `/api/mobile/updates/*`, `/api/client-policy/*` gibi giriş-öncesi kurtarma yolları `test_route_auth_coverage` muaf listesinde gerekçeyle kayıtlı; beyansız public uç bekçiyi kırmızıya düşürür. Koruma kimlik değil imza/salt-okunurluk. | ÇEKİRDEK | değişmez | `B:undated__mobil-guncelleme-istemci-surum-politikasi`, `R:2026-08-06__2026-08-06-rol-sablonlari-koda` | scripts/test_route_auth_coverage.ts |

## alt-electron (98)

| Küme | Kural | Sınıf | Tür | Kaynak | Bekçi |
|---|---|---|---|---|---|
| belge-etiket-sablon | Toplu belge ('Belgeleri Çıkar'): liste TEK KAYNAKTAN GET /work-orders/:id/documents — ekran belgeleri VARSAYMAZ, tür kutuları bulunanlardan doğar; Yazdır = tek baskı işi (print-merge.ts), PDF Kaydet = klasöre her belge AYRI dosya. Saf Electron: backend/migration/izin YOK. | ÇEKİRDEK | değişmez | `E:2026-08-06__toplu-belge-cok-belgeyi-tek-baski` | Electron/src/lib/print-merge.test.ts (kısmi) |
| belge-etiket-sablon | Toplu belgede atlanan hiçbir şey sessiz değil: belgesi olmayan iş emri, listesi alınamayan iş emri, HTML'i alınamayan belge, birleştirilemeyen belge AYRI AYRI raporlanır (failed listesi). | ÇEKİRDEK | değişmez | `E:undated__atlanan-hicbir-sey-sessiz-degil`, `E:2026-08-06__toplu-belge-cok-belgeyi-tek-baski` | yok |
| belge-etiket-sablon | Belge birleştirmede getBulkRollLabelsHtml paterni (ilk <head> + gövdeler) YANLIŞTIR — etiketlerde doğru (CSS birebir aynı), belgelerde generik sınıflar (.sheet/.company/.cell/table) birbirini ezer, resmi belge sessizce yanlış basılır. Her belge kendi GÖLGE KÖKÜNE: <template shadowrootmode="open">. | ÇEKİRDEK | yasak | `E:undated__paterni-ilk-belgenin-i-govdeler-burada` | Electron/src/lib/print-merge.test.ts |
| belge-etiket-sablon | Birleştirme kabı iframe DEĞİL (iframe içeriği sayfalara BÖLÜNMEZ, taşan kırpılır; gölge kök doğal akar). TEK belgede birleştirme YAPILMAZ — HTML aynen döner (tekil baskı bayt-bayt korunur). Gövdesinde </template> geçen belge unmergeable olarak DIŞARIDA bırakılır ve kullanıcıya söylenir. | ÇEKİRDEK | yasak | `E:undated__paterni-ilk-belgenin-i-govdeler-burada` | Electron/src/lib/print-merge.test.ts |
| belge-etiket-sablon | @page gövde CSS'inde KALAMAZ (belge geneline uygulanır, son yazan kazanır → A5 kart A4'e basılır) → adlandırılmış sayfaya taşınır: @page wdocN {…} + sarmalayıcıda page:wdocN (ölçüm: 2×A5 + 1×A4 → 420/420/595pt). | ÇEKİRDEK | tuzak | `E:undated__govde-css-inde-kalamaz-belge-geneline` | Electron/src/lib/print-merge.test.ts |
| belge-etiket-sablon | CSS YORUMLARI ÖNCE silinir (stripCssComments) — SIRA ZORUNLU: tarayıcılar düz metne bakar, yorumdaki '@page' blok sayacını kaydırıp komşu @media screen'i yutuyordu → kart A5'te 1 yerine 2 sayfa (hata yok, log yok). pageSizeOf'ta da yorum silinir. | ÇEKİRDEK | tuzak | `E:undated__css-yorumlari-once-silinir-tarayicilar-duz` | Electron/src/lib/print-merge.test.ts 'YORUM içind… |
| belge-etiket-sablon | Birleştirmede position:fixed → absolute: sabit filigran (İPTAL/TASLAK) görüntü alanına göre yerleşip gölge kökten TAŞAR ve işteki YABANCI belgelerin sayfalarına basardı. Bedel bilinçli: filigran her sayfada değil, belgede bir kez. | ÇEKİRDEK | değişmez | `E:undated__sabit-filigran-iptal-golge-kokten-tasar` | Electron/src/lib/print-merge.test.ts |
| belge-etiket-sablon | Etiketler kartı ve route aynı ikiliyi taşır ["station:read","label-template:read"] (2026-08-14 hizalandı — ticaret kullanıcısı istasyon kavramı olmadan kartı göremiyordu); notlardaki 'kart hâlâ station:read ile süzülüyor, bilinen hiza sorunu' BAYAT. | ÇEKİRDEK | karar | `R:2026-08-05__belge-tasarimi-ayri-bir-yetkidir-sistem` | yok |
| belge-etiket-sablon | Salt-okuma yüzeyi GERÇEK çizilir: document-template:read ile ekran + canlı önizleme çalışır, Kaydet kapalıdır, stüdyoda yazma aksiyonları HİÇ ÇİZİLMEZ (gri buton olmayan bir yolu vaat eder). | ÇEKİRDEK | reçete | `R:2026-08-05__belge-tasarimi-ayri-bir-yetkidir-sistem` | yok |
| deploy-kurulum | [PROFİL] Electron paket kimliği (productName 'Adnan Şahin ERP' + appId `com.etkiliyazilim.adnan-sahin-erp`) SABİTTİR, müşteriyle türemez; `deploy/electron-paketle.sh` yalnız `shared/musteri.json` yazar. İkinci müşteride çözüm `if (musteri)` değil, kimliği musteri.json'dan türetmek (ayrı karar). | PROFİL | değişmez | `E:undated__adnan-sahin-erp-admin` | yok |
| deploy-kurulum | Electron: yeni paket için ONAY. `bonjour-service` 1.4.4 sabit, `dependencies`'te durmak ZORUNDA (`externalizeDepsPlugin()` yalnız orayı okur; devDependencies'te kurulu uygulamada MODULE_NOT_FOUND); saf JS → rebuild/asarUnpack gerekmez. Tablo bayat: 4 paket eksik, `@electron/rebuild`. | ÇEKİRDEK | tuzak | `E:undated__allowed-packages` | Electron/src/test/discovery-ipc-contract.test.ts … |
| filtre-liste-arama | Electron 'tümünü göster' picker'ları `loadAllForPicker(service)` kullanır (PICKER_MAX_PAGE_SIZE=500 ↔ backend MAX_PAGE_SIZE); inline `pageSize: N` YAZILMAZ (sınır değişince 400). Toplam 500'ü aşarsa helper AÇIK hata fırlatır → arama tabanlı combobox + listCursor; UI hatayı yakalar. | ÇEKİRDEK | reçete | `E:undated__picker-dropdown-veri-cekme-kurali` | runtime: picker-loader.ts:74-79 throw; test yok |
| filtre-liste-arama | Envanter listesi TEK okunur değer basar: TEK tarih kolonu = sıralanan kolon ('Giriş' createdAt giriş sekmelerinde, 'Son İşlem' updatedAt diğerlerinde; diğeri Sütunlar'dan) · TEK metraj = currentQty (giriş metrajı + 'kesik' işareti listede YOK, detayda 'Başlangıç') · izlenebilirlik sütunları (entrySource/createdBy/entr… | PROFİL | karar | `R:2026-07-30__buraya-gelis-olusturma-2026-07-30`, `A:2026-08-05__2026-08-05-elle-eklenen-topu`, `R:2026-08-12__2026-08-12-giris-izlenebilirligi-kim` | yok |
| filtre-liste-arama | `updatedAt` gerçek 'hareket' DEĞİL (etiket yeniden basımı/not düzenlemesi de günceller) → kolon adı 'Son İşlem', 'Son Hareket' değil; FIFO/stok yaşlandırma için updatedAt KULLANILMAZ — statü geçişinde damgalanan `Roll.statusChangedAt` (trigger, 2026-08-09) var ama envanter listesi onu henüz kullanmıyor. | ÇEKİRDEK | yasak | `R:2026-07-30__buraya-gelis-olusturma-2026-07-30` | yok |
| filtre-liste-arama | Arşiv sekmesi: `STATUS_GROUPS.ARCHIVE` TEK KAYNAK (ALTI statü: RETURNED_FROM_SUBCONTRACTOR, TAMBUR_CONSUMED, SUBCONTRACTOR_CONSUMED, KARTELA_CONSUMED, CANCELLED, SCRAP), Sistem → Top Arşivi (`/system/roll-archive`, admin:settings). Sekme silinmedi TAŞINDI — bir kısmı barkodsuz, tek liste yüzeyi. | PROFİL | karar | `A:2026-08-05__2026-08-05-elle-eklenen-topu` | yok |
| filtre-liste-arama | Dört istasyon/kaynak filtresi DÖRT ayrı soru: 'İstasyon' (currentStationId) = top ŞU AN nerede — YALNIZ üretim sekmelerinde (Üretimde/Kurşun Bekleyen/Tambur Bekleyen; depoda top istasyonda durmaz, daima boş liste) · 'Giriş İstasyonu' (entryStationId) = kalıcı köken · 'Giriş Kaynağı' (entrySource) = tür · 'Ekleyen' (cr… | PROFİL | reçete | `R:2026-08-12__2026-08-12-giris-izlenebilirligi-kim`, `A:2026-08-05__2026-08-05-elle-eklenen-topu` | yok |
| filtre-liste-arama | Electron servis yolları TAM yazılır (`/api/...` — `apiClient.baseURL` /api İÇERMEZ); öneksiz yol 404 alır. FilterBar artık 'Sonuç yok.' yazısını YALNIZ başarılı+boş yanıtta basar (404/hata 'sonuç yok' değildir). | ÇEKİRDEK | tuzak | `R:2026-08-12__2026-08-12-giris-izlenebilirligi-kim` | yok |
| filtre-liste-arama | Tekil id çözen yardımcılar CSV'de kırılır (`find(id)` → undefined → rozet filtre AKTİFKEN sessizce yanlış etikete düşer): tek seçimde AD, çok seçimde 'N firma'; react-query anahtarı `filter-lookup-multi` ile FilterBar'la hizalı (ayrışırsa aynı veri iki kez çekilir). `dependent-lookup` üst filtresi CSV'yi aynen geçirir… | ÇEKİRDEK | tuzak | `R:2026-08-06__2026-08-06-filtrelerde-coklu-secim` | yok |
| filtre-liste-arama | TabHost her sayfayı `absolute inset-0 overflow-auto` ile sarar → sayfa kökü `PageShell` (flex h-full min-h-0 flex-col); TEK kaydırıcı `PageBody` (min-h-0 flex-1 overflow-auto); başlık/toolbar/footer shrink-0, eylemler `PageFooter`da. DataTable sayfaları ayrı PageBody istemez; tam-ekran editörler PageShell+PageFooter. | ÇEKİRDEK | reçete | `E:undated__sayfa-iskeleti-ortak-layout-kritik`, `E:undated__sonsuz-kaydirma-otomatik` | yok |
| filtre-liste-arama | 'Geri gidilebilir mi' `location.key` ile ÇÖZÜLMEZ (liste sayfaları açılışta `setSearchParams(replace:true)` ile URL yazar → anahtar değişir, geçmiş büyümez). Tek kaynak sekme başına derinlik defteri `tabs/history-depth.ts`: PUSH +1 · POP −1 · REPLACE DEĞİŞMEZ; sekme kimliği `useTabId()`; `PageHeader` sırası onBack > s… | ÇEKİRDEK | değişmez | `E:undated__geri-gidilebilir-mi-ile-cozulmez` | history-depth.test.ts + PageHeader.test.tsx |
| is-emri-siparis-bag | `ApiResponse.warnings?: string[]` engel olmayan nottur ve üç tipte de var (backend api.types.ts, Electron/mobil api.ts) — istemci toast basar, akışı durdurmaz. Electron CLAUDE 'API Kontratı' bloğu `warnings` ve `CursorPaginatedResponse`'u yazmıyor: BAYAT, güncellenmeli. | ÇEKİRDEK | değişmez | `E:undated__api-kontrati`, `R:2026-08-21__2026-08-21-uretim-rengi-tek` | yok |
| is-emri-siparis-bag | Masaüstü 'Üretime Al / Yeniden Üretime Al' ÜÇ sekmede (RAW_STOCK · SEMI_FINISHED · FINISHED_STOCK; fark yalnız başlık/toast, `reworkMode`). 'Ham Stok'ta gösterilmez' kararı 2026-08-26'da DÖNDÜ — gizlenen buton olmayan bir yolu taklit ediyordu (form top almaz, attach ucu yok). | ÇEKİRDEK | karar | `A:2026-08-25__2026-08-25-bitmis-kumas-tekrar`, `A:2026-08-26__2026-08-26-yari-mamul-filtre` | ReworkRollsDialog.test.tsx (7) |
| is-emri-siparis-bag | `ReworkRollsDialog`'da fason firma seçicisi LOAD-BEARING: backend adımın firmasını çözemezse sevki SESSİZCE atlar ('Fasona gönder' no-op). Firma `stepPlanning` overlay'iyle gider; buton metni de aynı yüklemden (`willDispatch`) beslenir. Fason modülüne bağımlı uyarı. | ÇEKİRDEK | tuzak | `A:2026-08-25__2026-08-25-bitmis-kumas-tekrar` | ReworkRollsDialog.test.tsx (firmasız sevk vaadi y… |
| is-emri-siparis-bag | Toplu belge: liste TEK KAYNAKTAN (`GET /work-orders/:id/documents`), ekran belge türlerini VARSAYMAZ; atlanan hiçbir şey sessiz değil (belgesiz WO · listesi alınamayan WO · HTML'i alınamayan · birleştirilemeyen belge AYRI raporlanır). Saf Electron, backend/izin yok. | ÇEKİRDEK | değişmez | `E:2026-08-06__toplu-belge-cok-belgeyi-tek-baski`, `E:undated__atlanan-hicbir-sey-sessiz-degil` | yok |
| kalite-istasyon-yetenek | Panelde 'Kalite kontrol uygular' kutusu HER istasyon türünde görünür — gizli kural icat edilmez (KK1/Sevkiyat WO adımı olmadığı için orada etkisiz). 'KK1 WO adımı olamaz' bir İSTEMCİ sözleşmesidir (`allowAsWorkOrderStep` filtresi Electron seçicilerinde); backend onu okumaz, guard eklemek davranış değişikliği olur. | ÇEKİRDEK | karar | `R:2026-09-03__2026-09-03-kalite-istasyon-yetenegi` | yok |
| kesif-cihaz-ag | Keşif adayı kendi PORTUNU taşır (`baseUrl` = `http://host:5000`); hangi portların gerçekten tarandığı `DiscoveryState.scan.ports` ile tanı ekranlarında görünür. | ÇEKİRDEK | değişmez | `R:2026-09-04__2026-09-04-kesif-kademeli-port` | discovery-logic.test.ts (aday portu taşımayınca 1… |
| kesif-cihaz-ag | `dist-web` backend paketine GİRER (drift imkânsız); eksikse kök sessizce durum sayfasına düşer — bu yüzden derleme kapıları vardır. `BossShell` sekme sistemini bypass eder ama router altyapısını KULLANIR (kendi memory router'ı geri okunu sessizce öldürür); hash `useHashPath` ile REAKTİF okunur. | PROFİL | tuzak | `R:2026-09-01__2026-09-01-patron-modulu-fabrikaya` | Electron/src/test/boss-shell.test.ts |
| kesif-cihaz-ag | Renderer (src/) `electron`, `fs`, `path`, `child_process`, `os`, `net` import ETMEZ; native API'ye yalnız `window.api` üzerinden ulaşır ve `window.api` yalnız `electron/preload.ts`te `contextBridge.exposeInMainWorld` ile kurulur. Main process HTTP BAŞLATMAZ — tek geçit `src/services/apiClient.ts`. | ÇEKİRDEK | yasak | `E:undated__process-boundary-pazarlik-disi` | yok |
| kesif-cihaz-ag | `webPreferences`ta `sandbox: true`, `contextIsolation: true`, `nodeIntegration: false` ZORUNLU — üçünden biri gevşetilmez. | ÇEKİRDEK | değişmez | `E:undated__process-boundary-pazarlik-disi` | yok |
| kesif-cihaz-ag | Electron renderer'ında token localStorage'a YAZILMAZ — main tarafında `safeStorage` + `electron-store` ile şifreli saklanır. İSTİSNA: `window.api` olmayan WEB derlemesinde (dist-web/patron) `webStore` bilinçli olarak localStorage kullanır; `window.api` varsa oraya asla düşülmez. | ÇEKİRDEK | yasak | `E:undated__process-boundary-pazarlik-disi` | yok |
| kesif-cihaz-ag | `bonjour-service` sürümü SABİT ('^' yok — sonraki ana sürüm ESM-only olup `createRequire` yolunu kırar) ve `dependencies`te DURMAK ZORUNDA: `externalizeDepsPlugin()` yalnız orayı okur, devDependencies'te kurulu uygulamada MODULE_NOT_FOUND ('dev'de çalışır, kurulumda ölü'). | ÇEKİRDEK | tuzak | `E:undated__allowed-packages` | Electron/src/test/discovery-ipc-contract.test.ts |
| kesif-cihaz-ag | mDNS yüklemesi TEMBEL + try/catch'tir (`electron/discovery/mdns-browser.ts`): paket kaybolsa bile uygulama açılır, keşif alt ağ taramasına düşer. `bonjour-service` saf JS'tir — `electron:rebuild` ve `asarUnpack` GEREKMEZ (asarUnpack yalnız serialport/node-hid/bindings için). | ÇEKİRDEK | değişmez | `E:undated__allowed-packages` | yok |
| kesif-cihaz-ag | Donanım paketleri (serialport, node-hid) `npm run electron:rebuild` ile yeniden derlenir; native seri okuma TEMBEL + try/catch `createRequire` ile yüklenir (rebuild yapılmamışsa `available:false`, çökme yok). ⚠️ Paket bugün `@electron/rebuild`, komut adı hâlâ `electron-rebuild`. | ÇEKİRDEK | reçete | `E:undated__komutlar`, `E:undated__allowed-packages` | yok |
| kesif-cihaz-ag | Panel sürüm çıkarırken `package.json > version` ARTIRILMALI — artırılmazsa kurulu paneller 'en güncelim' der ve yeni setup'ı HİÇ indirmez. Yayın adresi TEK KAYNAK: `shared/update-feed.ts` ↔ `package.json > build.publish`; ayrışma SESSİZ arıza üretir. | PROFİL | değişmez | `E:2026-08-26__otomatik-guncelleme-2026-08-26` | Electron/src/test/update-feed-url.test.ts |
| kesif-cihaz-ag | `build.<mac\|win>.artifactName` ASCII kalmalı, `${productName}` KULLANILMAZ — ad `latest.yml` içinde URL'dir; 'Ş' + boşluk aktarımda bozulup 404 üretir. | ÇEKİRDEK | tuzak | `E:2026-08-26__otomatik-guncelleme-2026-08-26` | Electron/src/test/update-feed-url.test.ts:67-73 |
| kesif-cihaz-ag | `autoUpdater`a MODÜL GÖVDESİNDE dokunulmaz — o bir getter, ilk erişimde Electron `app`ine dokunur; erişim `updater()` ile ertelenir. `autoInstallOnAppQuit` KAPALI kalır (kapanışta cevapsız UAC); şerit YALNIZ `ready`de çizilir (error şeridi internetsizde körleştirir). | ÇEKİRDEK | tuzak | `E:2026-08-26__otomatik-guncelleme-2026-08-26` | yok |
| kesif-cihaz-ag | `nsis.perMachine: true` (uygulama Program Files'ta) → her güncellemede bir kez Windows izin penceresi; o makinedeki hesap YÖNETİCİ DEĞİLSE güncelleme o makinede kurulmaz ve panel sessizce eski sürümde kalır. | PROFİL | tuzak | `E:2026-08-26__otomatik-guncelleme-2026-08-26` | yok |
| kesif-cihaz-ag | Ayar kategorisinin modül bağını `moduleKey` (KİLİT, `superadminOnly` ikizi) ile kur; `regime` (rejim gizlemesi) alanını modül anahtarlarına AÇMA — §14 bekçisi kırmızı verir. Kilit İSTEMCİ-TARAFLIDIR: bant 'dondu' der, API hâlâ yazar; bant 'etkisiz' DEMEZ. | ÇEKİRDEK | yasak | `R:2026-09-03__2026-09-03-panel-modul-kapilari` | settings-groups.test.ts + flag-modules.test.ts |
| kesif-cihaz-ag | Süperadmin kapısını `Electron/src/lib/superadmin-gate.ts`ten import et; İKİNCİ BİR KOPYA YAZMA — kopyaların birinde supap unutulursa süperadminsiz kurulumda ekran hiç açılmaz. Karo döngüsünün DIŞINDA kalan palet girdilerine kapıyı ELLE taşı (karosuz ekranda palet tek keşif yoludur). | ÇEKİRDEK | yasak | `R:2026-09-03__2026-09-03-panel-modul-kapilari` | yok |
| kesif-cihaz-ag | Ekran manifestosu okunamazsa 'gizlenecekler' listesini HİÇ ÇİZME — boş liste 'hiçbir şey gizlenmeyecek' YALANI basar. 'Kapatırsan gizlenir' önizlemesinde masaüstü ve tablet ekranları AYRI sayılır (tableti yutmak satıcıya kapatmanın bedelini gizler). | ÇEKİRDEK | değişmez | `R:2026-09-03__2026-09-03-panel-modul-kapilari` | yok |
| kesif-cihaz-ag | Tek seferlik baskı bayrağı (`?rowNotes=1`) kalıcı ayarı pure OR ile EZER; ne ayara ne donmuş snapshot'a yazılır, yeni belge versiyonu doğurmaz. OR yalnız renderer'da TEK yerde (efektif kolon ayarı kurulurken) uygulanır; checkbox `DocDef.supportsRowNotes` ile çizilir. | ÇEKİRDEK | değişmez | `E:undated__tek-seferlik-baski-bayragi` | Teks-Erp/scripts/test_sack_note_document.ts |
| kesif-cihaz-ag | Electron yığını: Tailwind 4 (`@import "tailwindcss"` + `@theme`, `tailwind.config.js` YOK) · main ESM (`import.meta.url` + `path.dirname(fileURLToPath(...))`, `__dirname` yok) · electron-store 11 ESM-only (`Store` import). | ÇEKİRDEK | değişmez | `E:undated__tailwind-4`, `E:undated__electron-33-esm-main`, `E:undated__electron-store-11` | npm run typecheck |
| kucuk-kumeler | Electron yerleşimi sabittir: `electron/` main süreci (main/preload/menu + `ipc/` DOMAİN BAŞINA BİR DOSYA), `shared/` main↔renderer sözleşmeleri, `src/` renderer; her modül kendi `pages/<Modul>/` klasörüne yazılır ve küçük dosyalara bölünür. | ÇEKİRDEK | reçete | `E:undated__mimari` | yok |
| kucuk-kumeler | Route haritası `src/router.tsx`te DEĞİL `src/routes/content-routes.tsx`tedir (router.tsx 39 satır: yalnız login/forbidden/totp/AuthLanding); `src/` altında ağaçta yazmayan data/, providers/, routes/, test/ de vardır. | ÇEKİRDEK | tuzak | `E:undated__mimari` | yok |
| kucuk-kumeler | `BossShell` sekme sistemini bypass eder ama router altyapısını KULLANIR — kendi memory router'ını kurma (sayfalar useTabId/TabPortalProvider/history-depth'e bağlı; PageHeader geri oku sessizce ölür, modaller yanlış portallanır). Hash `useHashPath` ile REAKTİF okunur. | PROFİL | tuzak | `R:2026-09-01__2026-09-01-patron-modulu-fabrikaya` | Electron/src/test/boss-shell.test.ts |
| kursun-planlama-bypass | Kurşun sırası + kurşun dağıtımı TEK ekranda kalır (`/operations/kursun-dagitim`); eski `operations/kursun-queue` adresi oraya yönlendirilir, ikinci bir kurşun ekranı açılmaz. | PROFİL | karar | `R:2026-08-05__2026-08-05-kursun-planlama-birlesik` | Electron/src/pages/Operations/tile-visibility.tes… |
| kursun-planlama-bypass | `production.kursunBypassEnabled` REJİM anahtarıdır, görünürlük anahtarı DEĞİL: karo ona bağlanmaz. Karo izinle (`quality:write` ∨ `workorder:distribute`; mobilde `mobile:kursun-dagitim`) ve MODÜL kapısıyla (`production.enabled` → `isKursunPlanningVisible`, ikizi `requireProductionEnabled`) süzülür. | PROFİL | değişmez | `R:2026-08-05__2026-08-05-kursun-planlama-birlesik` | Electron/src/pages/Operations/production-regime.t… |
| kursun-planlama-bypass | Sekme şeridi MAKİNE listesinden doğar, dağıtılmış satırlardan DEĞİL: işi olmayan makinenin de sekmesi vardır (boş makine tam da iş verilecek yerdir) ve pasifleşmiş ama üstünde açık iş kalan makine `(pasif)` etiketiyle çizilir — yoksa o işlere ulaşılamaz. | ÇEKİRDEK | reçete | `R:2026-08-05__2026-08-05-kursun-planlama-birlesik` | Electron/src/pages/Operations/KursunDagitim/machi… |
| kursun-planlama-bypass | Her makine panelinin KENDİ `DndContext`'i vardır: satırın makineler arası sürüklenmesi yapısal olarak imkânsızdır — makine değiştirmek bir yeniden ATAMA'dır (kaldır → tekrar ata), kazara sürüklemeyle yapılmamalı. | ÇEKİRDEK | değişmez | `R:2026-08-05__2026-08-05-kursun-planlama-birlesik` | yok |
| modul-bayraklari-profiller | Satıcı yüzeyi görünürlüğü VE yazılabilirliği TEK supaplı yüklem `isSuperadminGateOpen` (`superadmin-gate.ts`, kopya yasak): karo · route · palet · FeatureFlagSection · SettingsSurfacePage · ModuleProfilePage. Supapsız yükleme dönmeden ÖNCE ikinci yazma yüzeyi kur — yoksa modüller açılamaz. | ÇEKİRDEK | tuzak | `R:2026-09-04__2026-09-04-profil-sistem-hub`, `R:2026-09-03__2026-09-03-panel-modul-kapilari`, `R:2026-09-04__2026-09-04-cekirdek-kapali-modulun` | superadmin-gate.test.ts + settings-surface.test.t… |
| modul-bayraklari-profiller | Modül anahtarlarının TEK EVİ Sistem → Modüller (`/system/module-profile`, path DEĞİŞMEZ; demo + ayar şifresi kartı orada; karo `superadminOnly`); Genel Ayarlar'da sekmesi YOK. Davranış bayrakları Sistem → Özellik Anahtarları; Güncelleme çoklu kapı (`admin:settings` ∨ `settings:workstation`). | PROFİL | karar | `R:2026-09-04__2026-09-04-profil-sistem-hub` | settings-surface.test.ts (19) + tile-route-permis… |
| modul-bayraklari-profiller | Ayar yüzeyi kategoriye AYRI ALAN olarak yazılmaz, bölümden türer (`SECTION_SURFACE`); adres tek kaynak `settingsCategoryPath` (elle `?tab=` yasak — olmayan sekmeye düşer); karo başlığı ↔ `SURFACE_LABEL` ↔ route birebir; üç yüzey tek kabuk `SettingsSurfacePage`. | ÇEKİRDEK | yasak | `R:2026-09-04__2026-09-04-profil-sistem-hub` | settings-surface.test.ts (negatif sonda ①③ kırmız… |
| modul-bayraklari-profiller | Ayar kategorisinde modül = KİLİT (`moduleKey`, `superadminOnly` ikizi); `regime` (kategoriyi gizleme) yalnız `productionEnabled\|financeEnabled` içindir, modül anahtarlarına genişletilmez (§14 kırmızı). Kilit İSTEMCİ-TARAFLI: bant 'dondu' der, 'etkisiz' DEMEZ — API alt bayrağı yazmaya devam eder. | ÇEKİRDEK | tuzak | `R:2026-09-03__2026-09-03-panel-modul-kapilari`, `R:2026-09-03__2026-09-03-dilim-2-davranis` | test_feature_flag_contract §14 |
| modul-bayraklari-profiller | Görünürlük SATIRDAN türer, `moduleKey`den DEĞİL (karma kategori gerçek). Tek kaynak `flag-modules.ts` `FLAG_MODULE: Record<FlagRowKey\|SystemSettingKey, FlagOwner>` — tamlık derlemede (TS2741), küme bilerek geniş; söz dağarcığı screen-catalog aynası (modül · `cekirdek` · `planlanan:fason\|kartela`). | ÇEKİRDEK | değişmez | `R:2026-09-04__2026-09-04-cekirdek-kapali-modulun` | flag-modules.test.ts (tamlık·hiza·karma·planlanan… |
| modul-bayraklari-profiller | Yedi modül şalteri DAİMA `cekirdek` (kendini gizleyemez); aidiyet = satırın YÖNETTİĞİ yüzey, enforcement yeri değil (`kk1DuplicateGuardEnabled` → üretim, `pricingEnabled` → çekirdek, fason/kartela → `planlanan:*`); süzgeç ÇAĞIRANDA — görsel saklama kapalı bayrağı PATCH'e yazmaya devam ederdi. | ÇEKİRDEK | tuzak | `R:2026-09-04__2026-09-04-cekirdek-kapali-modulun` | flag-modules.test.ts (şalter·çekirdek) + Settings… |
| modul-bayraklari-profiller | Demo (kurulum beyanı) kartına `superadminOnly` VERİLMEZ — backend `flagWriteGuard` yalnız `MODULE_FLAG_KEYS`i kapsar, `demoModeEnabled` onda değil; olmayan kapıyı çizmek yalandır. Demo'yu fabrikadan uzak tutan şey sayfanın kimlik kapısıdır. | ÇEKİRDEK | yasak | `R:2026-09-04__2026-09-04-profil-sistem-hub` | settings-surface.test.ts (negatif sonda ①) |
| modul-bayraklari-profiller | Karo döngüsü DIŞINDAKİ palet girdileri (`ops:work-order-new`, `def:station-capabilities`) modül kapısını ELLE taşır (karo bekçisi görmez); 'kapatırsan gizlenir' önizlemesi MASAÜSTÜ ile TABLETİ AYRI sayar; manifesto okunamazsa liste HİÇ çizilmez (boş liste yalanı). | ÇEKİRDEK | reçete | `R:2026-09-03__2026-09-03-panel-modul-kapilari` | kısmen: command-entries.test; palet elle kapı içi… |
| raporlar | Rapor dışa aktarımı TEK SPEC → ÜÇ ÇIKTI: Excel·PDF·Yazdır aynı ReportExportSpec'ten türer; ayrı yazmak 'aynı başlık altında farklı rakam' demektir. Rapor PDF'i document-render/ dünyasına GİRMEZ (orası donmuş/sürümlü resmi belgelerin alanı). | ÇEKİRDEK | değişmez | `R:2026-08-09__2026-08-09-rapor-temeli-finalizedat` | Electron reportExport.test.ts (kolon kümesi eşitl… |
| refakat-karti | Electron'da belge satırının varsayılan dalı generic `PrintedDocDialog` ÖNİZLEMESİDİR — hiçbir satır tıklanınca doğrudan yazıcıya gitmez (kart/fason sevk kendi zengin diyaloğunda); backend listeye yeni tip eklerse 'önizlemesiz' doğmaz; revizyon izni `workorder:write` DOC_PERMISSIONS write ile hizalı. | ÇEKİRDEK | yasak | `R:2026-08-05__2026-08-05-is-emri-belgeleri` | yok |
| refakat-karti | Toplu baskıda `@page` gövde CSS'inde KALAMAZ (son yazan kazanır → A5 kart A4'e basılır): adlandırılmış sayfaya taşınır (`@page wdocN` + `page: wdocN`); CSS YORUMLARI taramadan ÖNCE silinir (`stripCssComments`, sıra zorunlu — yorumdaki `@page` kartı 2 sayfa bastırıyordu); tek belge HTML AYNEN döner. | ÇEKİRDEK | tuzak | `E:undated__govde-css-inde-kalamaz-belge-geneline`, `E:undated__css-yorumlari-once-silinir-tarayicilar-duz` | Electron/src/lib/print-merge.test.ts ('YORUM için… |
| refakat-karti | Sonsuz kaydırma OTOMATİK — 'Daha Fazla Yükle' butonu YOK: `useDataTable`+`DataTable` hazır alır; özel `useInfiniteQuery` listelerinde `useInfiniteScroll` (rootRef→PageBody, sentinelRef→`AutoLoadMore`); tam-ekran editörler `PageShell`+`PageFooter`. | ÇEKİRDEK | reçete | `E:undated__sonsuz-kaydirma-otomatik` | yok |
| refakat-karti | Sidebar'a tanım satırı EKLENMEZ — tek 'Tanımlar' girişi `/definitions` hub'ına gider; yeni master data: `pages/Definitions/tile-config.ts`'e kart + `src/routes/content-routes.tsx`'e route (notun 'router.tsx' ifadesi BAYAT: router.tsx 39 satırlık auth router). | ÇEKİRDEK | reçete | `E:undated__sidebar-kurali-kritik`, `E:undated__tanimlar-sayfasiysa-e-kart-eklendi` | yok |
| refakat-karti | Tık hedefi İKİ sözleşme: kenar menüsü `useTabTarget` (sol tık sekme aç/odakla — başka işe geçiş), hub kartı `useDrillTarget` (AYNI sekmede yerinde in — aynı işin adımı); karıştırılmaz; ikisinde de sağ/orta tık arka planda yeni sekme. | ÇEKİRDEK | değişmez | `E:2026-08-17__tik-hedefi-iki-sozlesme-karistirma` | Electron/src/components/hub/HubCard.test.tsx |
| rota-renk-ozellik-kapsama | 'Bu istasyon renk/özellik uygulamaz' ile 'yetenek listesi boş' AYRI cümlelerdir, tek mesajda birleştirme. Rota adımındaki 'Yeni özellik tanımla' istasyonu bağlamdan alır → özellik BAĞLI doğar ve anında seçili gelir. `hasDefaultCategory` DTO'da kalır (ipucu metni) ama karar vermez. | ÇEKİRDEK | reçete | `R:2026-08-02__ozellik-istasyon-kisiti-kalir-ama-bos`, `R:2026-08-10__2026-08-10-11-uretim-karakteristigi` | yok |
| rota-renk-ozellik-kapsama | KÜME DIŞI (Electron): İçerik route'ları `src/routes/content-routes.tsx`'te `<ProtectedRoute requirePermission>`; `router.tsx` yalnız oturum-dışı router (`createHashRouter`, file:// zorunlu). Electron/CLAUDE.md 'router.tsx route map' ağacı + checklist satırı BAYAT. | ÇEKİRDEK | reçete | `E:undated__mimari`, `E:undated__route-e-ile-eklendi`, `E:undated__react-router-7` | yok |
| rota-renk-ozellik-kapsama | KÜME DIŞI (Electron): `useNavigate()` ile gezinme sekme defterini GÜNCELLEMEZ; `TabRouter` router'a abone olup `syncTabLocation` ile eşitler — yalnız yol DEĞİŞTİYSE (sayfaların `updateTabTitle` başlığı korunsun). | ÇEKİRDEK | tuzak | `E:undated__router-dan-gecen-gezinme-sekme-defterini` | store/tabs.back.test.ts |
| rota-renk-ozellik-kapsama | KÜME DIŞI (Electron): Master data picker'ları `loadAllForPicker(service)` kullanır; inline `pageSize: N` YAZILMAZ (backend `MAX_PAGE_SIZE` değişince 400); helper `PICKER_MAX_PAGE_SIZE` ile senkron, aşımda AÇIK hata fırlatır → UI yakalar; >500 için arama tabanlı combobox (`listCursor`). | ÇEKİRDEK | yasak | `E:undated__picker-dropdown-veri-cekme-kurali` | yok |
| sevkiyat-brut-storno-cuval | Sevkiyat detayında iade satırı ÇUVALDA KALIR: `returned` işaretli, soluk + amber 'İade' rozeti, salt-okunur, metraj üstü ÇİZİLMEZ (brüt toplama dahildir, irsaliyede durur); ayrı 'Bu sevkiyattan iade edilenler' kartı AYNEN kalır — rozet onun yerine geçmez, topun konumunu söyler. | PROFİL | karar | `R:2026-08-03__2026-08-03-brut-kurali-sevkiyat` | yok |
| sevkiyat-brut-storno-cuval | Sevk Kapısı karosu `visibleWhen` SAF bayrak (`ctx.shipmentConfirmationEnabled`); 'VEYA çıkış bekleyen PLANNED' dalı ve `board?limit=1` sondası KALKTI, `OperationsVisibilityContext` tek alan — sayaç geri EKLENMEZ. Route `/operations/sack-store` bayrağa bakmaz: derin bağlantı açılır. | ÇEKİRDEK | değişmez | `R:2026-08-22__2026-08-22-sevk-kapisi-bayragin` | Electron/src/pages/Operations/tile-visibility.tes… |
| sevkiyat-brut-storno-cuval | PLANNED sevkiyat Sevk Kapısı'na muhtaç DEĞİL: Sevkiyatlar detayı (sheet + tam sayfa) ortak `DispatchConfirmDialog` ile 'Sevk Et' verir (`shipping:write`; çuvallar canlı listelenir, irsaliye başarı panelinden basılır). Bayrak açıkken kurulup sonra kapatılmış eski PLANNED'lar da buradan çözülür. | ÇEKİRDEK | reçete | `R:2026-08-22__2026-08-22-sevk-kapisi-bayragin` | Electron Shipments + SackStore vitest (38) |
| sevkiyat-brut-storno-cuval | 'Sevkiyatı da kapat' kutusunun varsayılanı İSTEMCİDE, önizlemenin `confirmationEnabled` alanından: kapalı rejim → işaretli (PLANNED beklemenin karşılığı yok), açık → işaretsiz; kullanıcı değiştirir. Kapatılan sevkiyat aynı numarayla çıkmaz — Paketleme'den YENİ sevkiyat (eski irsaliye VOIDED). | PROFİL | karar | `R:2026-08-22__2026-08-22-sevk-kapisi-bayragin`, `A:2026-08-22__2026-08-22-sifirlama-rafa-kalkti` | scripts/test_shipment_undo_dispatch.ts §10 (önizl… |
| sevkiyat-brut-storno-cuval | Yeniden üretime almada fason firma seçicisi LOAD-BEARING: firma `stepPlanning` overlay'iyle gitmezse 'Fasona gönder' sessiz no-op (backend firmayı çözemezse sevki atlar); buton metni ile davranış AYNI yüklemden (`willDispatch`). Ölü etiket uyarısı burada KESİN ve DAR (etiketli VE ilk adım fason). | ÇEKİRDEK | tuzak | `R:2026-08-25__2026-08-25-bitmis-topu-yeniden`, `A:2026-08-25__2026-08-25-bitmis-kumas-tekrar` | Electron ReworkRollsDialog.test.tsx |
| sevkiyat-brut-storno-cuval | [PROFİL] 'Sevkiyatlar' (operasyon: tüm statüler, iptal) ile 'Sevkiyatlar (Muhasebe)' (salt-okunur DISPATCHED + fiş + dönem Excel + fatura izi) ayrı ekran ama izinle AYRILMADI (sevkiyatçının belgelere erişimi gerekir); muhasebe ekranı finance.enabled açıkken terfi eder (dış muhasebe köprüsü). | PROFİL | karar | `A:2026-08-02__2026-08-02-brut-kurali-liste`, `R:2026-08-02__2026-08-02-sevk-rakami-brut` | yok |
| superadmin-gizlilik-ayar-sifresi | Electron satıcı kapısı TEK YÜKLEM `isSuperadminGateOpen` = isSystemAccount \|\| !systemAccountExists (`lib/superadmin-gate.ts`); karo (`superadminOnly`), route (`requireSystemAccount`), palet, FeatureFlagSection, ModuleProfilePage ondan İTHAL eder. Supapsız `isSystemAccountIdentity` geri gelmez. | ÇEKİRDEK | değişmez | `R:2026-09-04__2026-09-04-profil-sistem-hub`, `R:2026-09-03__2026-09-03-panel-modul-kapilari` | superadmin-gate.test.ts (tüketiciler bu dosyadan … |
| superadmin-gizlilik-ayar-sifresi | Sistem → Modüller (`/system/module-profile`, path değişmez) satıcı ekranı: hesap DOĞMUŞSA fabrika yöneticisi sayfayı HİÇ açamaz, hesap yoksa açar ve yazar; salt-okunur bant teorik dal, bilerek duruyor. Demo kartı `superadminOnly` ALMAZ (guard yalnız MODULE_FLAG_KEYS). | PROFİL | karar | `R:2026-09-04__2026-09-04-profil-sistem-hub` | SystemHubPage.superadmin.test.tsx + settings-surf… |
| superadmin-gizlilik-ayar-sifresi | Ayar kategorisinde modül bağı = `moduleKey` (KİLİT, `superadminOnly` ikizi); `regime` modül anahtarlarına açılmaz (§14). Kilit İSTEMCİ-TARAFLI: bant 'dondu' der, 'etkisiz' DEMEZ (§3.6 resolver yok). Fabrika görünümünde kapalı modülün satırı çizilmez; satıcıda satır + bant. | ÇEKİRDEK | değişmez | `R:2026-09-03__2026-09-03-panel-modul-kapilari` | flag-modules.test.ts (12) + SettingsSurfacePage.m… |
| superadmin-gizlilik-ayar-sifresi | Karo döngüsü DIŞINDAKİ palet girdileri (`ops:work-order-new`, `def:station-capabilities`) modül/süperadmin kapısını ELLE taşır (karosuz ekranda palet tek keşif yolu). 'Kapatırsan gizlenir' önizlemesi masaüstü/tableti AYRI sayar; manifesto okunamazsa liste ÇİZİLMEZ. | ÇEKİRDEK | reçete | `R:2026-09-03__2026-09-03-panel-modul-kapilari` | BELİRSİZ — notta bu üç kural için adlı bekçi yok … |
| superadmin-gizlilik-ayar-sifresi | Electron `withSettingsPassword` sarmalayıcısı: istek önce şifresiz gider; 403 REQUIRED/INVALID → `SettingsPasswordDialog` (App'te bir kez mount) → başlıkla tekrar; LOCKED kalan süre. Her kayıtta sorulur, oturumda HATIRLANMAZ; şifre state'te yalnız diyalog açıkken, log/toast/localStorage'a girmez. | ÇEKİRDEK | reçete | `R:2026-09-03__2026-09-03-ayar-sifresi-p3` | yok (backend↔Electron ayna bekçisi yalnız sabitle… |
| surum-yayin | Panel yayın adresi TEK KAYNAK: `shared/update-feed.ts` ↔ `package.json > build.publish` (ayrışma SESSİZ arıza; `publish` bloğu kalkarsa electron-builder `latest.yml` HİÇ üretmez). `artifactName` ASCII kalır, `${productName}` YASAK (ad `latest.yml`de URL'dir; `Ş`+boşluk 404 üretir). | ÇEKİRDEK | değişmez | `R:2026-08-26__2026-08-26-electron-dagitimi-guncelleyici`, `E:2026-08-26__otomatik-guncelleme-2026-08-26` | Electron/src/test/update-feed-url.test.ts:39-73 +… |
| surum-yayin | Electron güncelleyici: `autoUpdater`a modül gövdesinde DOKUNMA (`updater()` getter); `autoInstallOnAppQuit=false`; şerit YALNIZ `ready` (error körleştirir); güncelleme ZORUNLU (`UpdateGate` + 2 dk geri sayım, 20 sn kurulum bekçisi, 5 dk tekrar); zamanlayıcı YALNIZ main'de: 30 sn + 15 dk. | ÇEKİRDEK | karar | `R:2026-08-26__2026-08-26-electron-dagitimi-guncelleyici`, `E:2026-08-26__otomatik-guncelleme-2026-08-26` | yok (kod yorumları) |
| surum-yayin | `nsis.perMachine:true` KALIR (kullanıcı kararı): her güncellemede bir Windows izin penceresi; o makinedeki hesap yönetici DEĞİLSE güncelleme o makinede kurulmaz ve panel sessizce eski sürümde kalır. Değiştirmek `perMachine:false` + bir elle tur. | PROFİL | tuzak | `R:2026-08-26__2026-08-26-electron-dagitimi-guncelleyici`, `E:2026-08-26__otomatik-guncelleme-2026-08-26` | yok |
| surum-yayin | Makineye özel adres ezmesi (`config.updateFeedUrl`, secure-store) bulunur: adres pakete derleme anında gömüldüğü için yanlış gömülen adresin tek çıkışıdır; bozuk ezme sessizce yok sayılıp varsayılana dönülür. | ÇEKİRDEK | reçete | `R:2026-08-26__2026-08-26-electron-dagitimi-guncelleyici` | yok |
| top-duzeltme-iptal-fire-geri-alma | Top Arşivi (Sistem → /system/roll-archive, admin:settings — depo/üretim personeli göremez, bilinçli) CANCELLED+SCRAP'ı da listeler; barkodsuz iptal top ancak buradan bulunur. 'Kayıt Türü' daraltması statusIn anahtarı (status DEĞİL: backend önceliği statusIn>status). | ÇEKİRDEK | karar | `R:2026-08-05__2026-08-05-manuel-top-geri`, `R:2026-08-25__2026-08-25-saha-deploy-sonrasi` | Electron Rolls/service.test.ts |
| top-duzeltme-iptal-fire-geri-alma | roll:history izni yoksa top detayındaki yaşam döngüsü bölümü HİÇ çizilmez (boş kutu = 'geçmiş yok' yalanı). RollOperation yaşam döngüsü günlüğü DEĞİL, istasyon işlem log'udur — geçmiş GET /rolls/:id/history'den okunur. İzin boot'ta gelir, atama elle. | ÇEKİRDEK | değişmez | `R:2026-08-05__2026-08-05-manuel-top-geri` | yok |
| top-duzeltme-iptal-fire-geri-alma | Arayüzde iki buton ('veriyi mi değiştiriyorum, kâğıt mı basıyorum'): 'Düzelt' (RollEditDialog — rollId + GET /api/rolls/:id/relabel-context, barkodsuz açık kumaş da düzeltilebilsin) · 'Etiket' (önizle/bas/farklı müşteri için bas). RelabelDialog ve ManualAttributesDialog silindi, geri gelmez. | ÇEKİRDEK | karar | `R:2026-07-30__top-duzeltme-tek-sozlesme-2026-07` | yok |
| top-duzeltme-iptal-fire-geri-alma | Sayfa içi arama (Ctrl+F / findInPage) KALDIRILDI — listenin sunucu araması kullanılır. Yeniden denenirse: Electron findInPage seçeneğindeki findNext 'sonraki eşleşme' DEĞİL 'YENİ OTURUM BAŞLAT' demektir; ters yazım sessiz 0/0 üretir. | PROFİL | tuzak | `A:2026-08-06__2026-08-06-sayfa-ici-arama` | yok |
| yari-mamul | Sipariş karşılamada `netGap` DEĞİŞMEZ (ham havuzu zaten sayılmıyordu); yalnız bilgi alanı `freeSemiFinished` eklenir. Electron onu OPSİYONEL okur (`?? 0`) ve backend ÖNCE deploy edilir, yoksa ipucu sessizce kaybolur. | ÇEKİRDEK | reçete | `R:2026-08-27__2026-08-27-3-tur-yari` | test_semi_finished_surfaces.ts:131-150 |
| yari-mamul | 'Üretime Al' düğmesi Ham Stok ve Yarı Mamul sekmelerinde de çizilir; backend `quickStart` baştan beri STOCK/WAREHOUSE/A1_STOCK kabul ediyordu, yeni izin kodu açılmadı (`workorder:write` yeter). | ÇEKİRDEK | karar | `R:2026-08-26__2026-08-26-yari-mamul-kendi` | yok |
| yari-mamul | Sekmeye bağlı aksiyonlar AÇIK sekme listesiyle yazılır; 'X değilse göster' gibi negatif koşul, aynı gövdeyi kullanan Kartela ve Top Arşivi sayfalarına buton sızdırır. | ÇEKİRDEK | yasak | `R:2026-08-26__2026-08-26-yari-mamul-kendi` | yok |
| yari-mamul | `ROLL_TABS`'a sekme eklenip `buildRollForceFilters`'a dal yazılmaması TİP HATASI VERMEZ ve kapsamsız liste doğurur; ayrıca 'Ham Stok' sekmesi birleşik `RAW_STOCK` kapsamını KULLANMAZ (dar ikizi gönderir). | ÇEKİRDEK | tuzak | `R:2026-08-26__2026-08-26-yari-mamul-kendi` | Electron/src/pages/Operations/Rolls/service.test.… |
| yari-mamul | Manuel Giriş'te renk seçili + yarı mamul kutusu işaretsizse AMBER UYARI çıkar (engel DEĞİL — renkli bitmiş mal girmek meşru); Yarı Mamul sekmesinden açılınca kutu ön-işaretli gelir ama GÖRÜNÜR kalır. | PROFİL | reçete | `R:2026-08-26__2026-08-26-yari-mamul-kendi` | yok |
| yari-mamul | Stok Karnesi'nde beşli kart sırası yalnız 1280px'ten itibaren açılır (`lg:grid-cols-3 xl:grid-cols-5`); 1024px'te 5 kart sıkışıp rakamı iki satıra kırıyordu. | PROFİL | reçete | `R:2026-08-27__2026-08-27-2-tur-yari` | yok |
| yetki-izin-rol | Yeni ayar kategorisinde varsayılan DARdır (`permissionAny` verilmezse `admin:settings`) — izni unutmak gizler, SIZDIRMAZ. 'İzni verdim ama yolu vermedim': dar izinli kullanıcı ekrana kişisel Ayarlar'daki 'Bu Bilgisayar' kartından ya da paletten ulaşır; karo dışı palet girdisi kapıyı ELLE taşır. | ÇEKİRDEK | tuzak | `E:undated__rbac`, `A:2026-09-04__2026-09-04-profil-sistem-hub` | Electron settings-surface.test.ts |
| yetki-izin-rol | Panelde satıcı kapısı TEK yüklem `isSuperadminGateOpen` (supaplı: `isSystemAccount \|\| !systemAccountExists`) — karo·route (`ProtectedRoute.requireSystemAccount`)·palet·FeatureFlagSection ondan beslenir; supapsız görünürlük yüklemi (`isSystemAccountIdentity`) geri getirilmez (kilitlenme). | ÇEKİRDEK | değişmez | `A:2026-09-04__2026-09-04-profil-sistem-hub`, `A:2026-09-03__2026-09-03-superadmin-p2-gizli` | Electron superadmin-gate.test.ts (tüketiciler bu … |
| yetki-izin-rol | Panelde `superadminOnly`/kilit bandı yalnız backend'de karşılığı OLAN kapı için çizilir: `flagWriteGuard`ın süperadmin dalı yalnız `MODULE_FLAG_KEYS`i kapsar → demo beyanı `superadminOnly` ALMADI (sayfanın kimlik kapısı yeter). Modül kilidi istemci-taraflı: bant 'dondu' der, 'etkisiz' DEMEZ. | ÇEKİRDEK | karar | `A:2026-09-04__2026-09-04-profil-sistem-hub` | Electron SystemHubPage.superadmin.test.tsx |
| yetki-izin-rol | Electron'da ayar YAZAN her istek `withSettingsPassword` sarmalayıcısından geçer: önce şifresiz; 403 REQUIRED/INVALID → `SettingsPasswordDialog` (App'te tek mount) → başlıkla tekrar; LOCKED → kalan süre. Her kayıtta sorulur, oturumda hatırlanmaz; feature-flag/logo/settings/backup offsite hepsi ondan. | ÇEKİRDEK | reçete | `A:2026-09-03__2026-09-03-ayar-sifresi-p3` | yok (ayna bekçisi yalnız şifre kuralını ölçer) |
| yetki-izin-rol | UI yetki kontrolü `useRoleAccess` + `<PermissionGate>`; `isAdmin` yalnız adminOnly nav/tile GÖRÜNÜRLÜĞÜDÜR, yetki kısayolu DEĞİL (Y6: 'isAdmin ise her şey true' kısmi admine 403 yağdırıyordu); `DefinitionsHubPage` kısa devresi korunur. Admin route'ları `admin:settings`/`admin:users` ister. | ÇEKİRDEK | reçete | `E:undated__rbac` | Electron/src/types/auth.test.ts |
| yetki-izin-rol | Salt-okuma GERÇEK olmalı: ekran READ ile açılır (`PermissionGate anyOf={DOCUMENT_DESIGN_READ}`), canlı önizleme çalışır, Kaydet `canWrite` ile kapanır, stüdyodaki yazma aksiyonları `readOnly` ile ÇİZİLMEZ — gri buton olmayan bir yolu vaat eder. | ÇEKİRDEK | reçete | `E:undated__rbac`, `R:2026-08-05__belge-tasarimi-ayri-bir-yetkidir-sistem` | yok |

## alt-mobil (104)

| Küme | Kural | Sınıf | Tür | Kaynak | Bekçi |
|---|---|---|---|---|---|
| belge-etiket-sablon | Mobil etiket yazıcısı OTURUMDAN çözülür: LabelPrinter peripheralService.getForSession('LABEL_PRINTER') (oturum yoksa BOŞ — fail-closed); taşıma BT-Classic/SPP (hal/btClassic.transport; Argox + HC-05/06), yalnız BLUETOOTH_SPP device-direct; manuel yazıcı seçimi/btPrinterStore KALDIRILDI. | ÇEKİRDEK | değişmez | `M:undated__cozumleme-oturum-kapsamli` | yok |
| belge-etiket-sablon | Mobil çuval kartında yalnız sık kullanılan kalır; 'elle gir / etiket bas / not ekle / sil' SackActionsSheet ⋮ menüsünde (AppModal position=bottom, satır ≥56dp); yıkıcı aksiyon menüye taşınsa da ONAY DİYALOĞU korunur. Tartı tek dokunuş: ⚖ → kantardan oku → doğrudan kaydet. | ÇEKİRDEK | reçete | `M:undated__manuel-ikincil-yol-menusunde`, `R:2026-07-30__2026-07-30-cuval-notu-sack` | yok |
| belge-etiket-sablon | Çuval kg'si sevk irsaliyesi ve çeki listesine BASILIR (müşteri/gümrük belgesi) → uydurulmuş değer canlı veriye GİRMEZ: istemci source:'SIMULATED' beyan eder, backend shipping.simulatedWeightEnabled kapalıyken 400; METER simüle doğar, SCALE doğmaz; MANUAL muaf (kantarsız kaçış). | ÇEKİRDEK | karar | `M:2026-07-30__cihazda-yazma-2026-07-30` | scripts/test_sack_weigh_source.ts + useSackWeigh.… |
| deploy-kurulum | [PROFİL] APK elle kurulumunda imza uyuşmazlığı (OTA mührü) → kaldır+kur → cihaz kimliği değişir; `devicePairingRequired` AÇIKSA tablet PENDING düşer, onay ister (2026-09-04'ten beri KAPALIYSA APPROVED doğar) — sahaya çıkarken planlanır. Native değişiklik APK ister; saf JS OTA ile gider. | PROFİL | reçete | `A:2026-08-26__2026-08-26-aksam-sebep-listesi`, `R:2026-09-04__2026-09-04-cihaz-onay-kapisi`, `M:undated__test-kaliciligi-bozan-sondayla-kirmizi-verdigi` | Teks-Erp/scripts/test_device_pairing_flag.ts (bay… |
| fason | Mobil kısmi kabul TEK soru 'Fasonda kalan var mı?' + tek metraj; `resolveReturns` kalanı BÜYÜK TOPTAN en az topa yığar, tamamı kalan top dönüş listesine GİRMEZ, sıra deterministik. Satır başına 'Gelen (m)' kaçış kapısı; Tek Parça modunda satır HER ZAMAN 1 (`switchReceiveMode`). | ÇEKİRDEK | değişmez | `R:2026-08-21__2026-08-21-fason-kabulu-cekme` | mobil FasonKabul/receivePayload.helper.test.ts + … |
| fason | RN Paper `SegmentedButtons` `flexDirection:'row'` kabının doğrudan çocuğu OLAMAZ (Yoga onu satırın tamamına açar, yanındaki başlık sıfır genişliğe iner, soru ekrana çıkmaz); yanına bir şey konacaksa AÇIK `width` (minWidth YETMEZ). Yerleşim şikâyetinde tahmin değil cihaz ölçümü. | ÇEKİRDEK | yasak | `A:2026-08-25__2026-08-25-aksam-mobil-yeniden` | mobil/src/test/segmented-buttons-row.guard.test.ts |
| fason | Top okutma geri bildirimi ekran ekran KOPYALANMAZ (Fason Sevk dahil): `scanFeedback.signalScan` (ses+titreşim; ekran Haptics'i doğrudan çağırmaz), `useScanFeedback` (signalScan'i KENDİSİ çağırır; flash + rejects), `ScannerRollStrip` (kamera altı şerit). Yüzeyler birbirinin yerine geçmez. | ÇEKİRDEK | değişmez | `M:2026-08-06__top-okutma-yuzeyi-uc-katman-ucu` | yok |
| fason | Yeniden üretim sebebi İKİ hedefe: metin → 1. adım notu → FASON ÇEKİSİNE TALİMAT; kod → `WorkOrder.parameters.rework` (migration YOK). Ölü etiket uyarısı onay adımında, DAR koşul (etiketli VE ilk adım fason) — fason kabulünde top SUBCONTRACTOR_CONSUMED olur, barkod geçersizleşir; bilgi, engel değil. | ÇEKİRDEK | reçete | `R:2026-08-25__2026-08-25-aksam-mobil-hizli`, `A:2026-08-25__2026-08-25-aksam-mobil-yeniden` | mobil reworkPayload.test.ts (8) |
| filtre-liste-arama | Mobil 'Son Kayıtlar' HER ZAMAN kişiye özel (`filter[createdById]=ben`, bayraktan bağımsız); 'Tüm Girişler' `kk1.historyAllEntriesEnabled` KAPALIYKEN yalnız kendi kayıtları (başlık '· yalnız senin girişlerin', Personel çipi HİÇ çizilmez), AÇIKKEN herkes + çip. Yetki duvarı DEĞİL ekran sadeleştirmesi; bayrak yüklenemezs… | PROFİL | karar | `R:2026-08-12__2026-08-12-giris-izlenebilirligi-kim` | mobil rollHistoryFilter.test.ts (+7, kapsam kural… |
| filtre-liste-arama | Mobil tarih seçici için YENİ PAKET EKLENMEZ — takvim ızgarası elle yazıldı, sıfır bağımlılık (expo-audio peer'ı bir kez expo-asset'i köke çekip APK'yı açılışta çökertmişti). | ÇEKİRDEK | yasak | `R:2026-08-12__2026-08-12-top-listesi-filtresi` | yok |
| filtre-liste-arama | `NumpadHost` `autoActivate` aynı ekranda yalnız TEK alana verilir — ikisine birden verilirse kazananı MOUNT SIRASI belirler ve operatör metraj beklerken tuşlar sessizce En'i değiştirir (metraj `!compact && manualMode`, En `!compact && !manualMode`). | ÇEKİRDEK | yasak | `R:2026-08-12__2026-08-12-top-listesi-filtresi` | yok |
| filtre-liste-arama | Mobil picker'da 'yeni ekle' tetiği `PickerModal.leadingAction`: listenin İLK hücresindeki mor aksiyon kartı (`colors.action`, kartlarla aynı geometri, sıralama/aramadan bağımsız ilk sıra); basılınca picker KAPANMAZ, form `quickAddSlot`ta açılır; listenin üstüne ayrı outlined buton KOYMA. Mor bilinçli: indigo 'seçili k… | ÇEKİRDEK | reçete | `M:2026-07-30__picker-ici-yeni-ekle-2026-07` | mobil PickerModal.test.tsx (leadingAction 3 geçiş) |
| is-emri-siparis-bag | Mobil ayna `canUnlinkOrderLine(type, linkCount, hasTargetItem=true)` → `{allowed, reason, becomesStock}`; ikinci kural kümesi DEĞİLDİR, son söz backend (yarışta 400 toast). `hasTargetItem` bilinmiyorsa engellemez. Backend ÖNCE deploy güvenli — eski APK yalnız fazladan engeller. | ÇEKİRDEK | değişmez | `R:2026-08-21__2026-08-21-is-emri-tipi`, `A:2026-08-19__2026-08-19-tambur-paketi-2` | mobil canUnlinkOrderLine.test.ts (6) |
| is-emri-siparis-bag | Mobil Hızlı İş Emri okutma kararı SAF yüklem `scanClassify` (DB'siz sınanır); SIRA load-bearing: iptal → statü → çuval/sevkiyat → kumaş kilidi — `SHIPPED` topa 'çuvaldan çıkarın' demek malın müşteride olduğunu gizler. `RollPickerModal.scopeTabs` sekme anahtarı sorgu anahtarına GİRMELİ. | ÇEKİRDEK | değişmez | `A:2026-08-25__2026-08-25-aksam-mobil-yeniden` | scanClassify.test.ts (12) |
| is-emri-siparis-bag | Kamera kullanım yerleri: Refakat Kartı, top barkodu, iş emri QR (`expo-camera` CameraView; `expo-barcode-scanner` kullanma). | PROFİL | karar | `M:undated__kullanim` | yok |
| kalite-istasyon-yetenek | Mobil oturumlu ekran seçimi istasyon TÜRÜNDEN: `SCREEN_BY_STATION_KIND` (RAW_QC→KK1, PROCESS_QC→KursunQc, TAMBUR→Tambur, SHIPPING→TartiPaket) backend `SESSIONABLE_STATION_KINDS`'ın aynasıdır, bijektif; yeteneğe geçiş Faz B R4 (APK işi, bijektiflik bozulur) — bu tabloya yetenek sokma. | ÇEKİRDEK | değişmez | `M:undated__cozumleme-oturum-kapsamli`, `R:2026-09-03__2026-09-03-kalite-istasyon-yetenegi` | mobil/src/constants/stationScreens.test.ts |
| kalite-istasyon-yetenek | Tambur 'Boyahaneye Geri Gönder': hedef adımı SUNUCU çözer (`/tambur/manual/send-to-dye-preview`, KANONİK `stepCanApplyColor` — tablette rota yok, 'hangi adım boya veriyor' yüklemi ikinci kez yazılmaz); önizlemesiz uygulama YOK, sebep ≥3; fason adımına taşıma AT_SUBCONTRACTOR YAPMAZ, çıkış Fason Sevk'ten. | ÇEKİRDEK | değişmez | `M:2026-08-19__tambur-boyahaneye-geri-gonder-2026-08` | yok |
| kalite-istasyon-yetenek | Mobil okuma yolu TEK: HAL (`src/services/hal/`); eski `hardware.service.ts` mock'u KALDIRILDI, geri getirilmez. | ÇEKİRDEK | yasak | `M:undated__tum-istasyonlar-tambur-kk1-okuma-icin` | yok |
| kesif-cihaz-ag | SCALE cihazları simüle DOĞMAZ, METER cihazları doğar. Gerekçe sınıf farkı: çuval kg'si sevk irsaliyesine ve çeki listesine BASILIR (müşteri/gümrük belgesi); metraj iç ölçümdür. `shipping.simulatedWeightEnabled` kapalıyken (varsayılan) backend 400 döner, demo/eğitim kurulumu açar. | PROFİL | karar | `M:2026-07-30__cihazda-yazma-2026-07-30`, `M:undated__simulasyon__3` | Teks-Erp/scripts/test_sack_weigh_source.ts |
| kesif-cihaz-ag | Tablet keşif bütçesi masaüstünden DARDIR: yedek portlarda TAM SÜPÜRME yalnız İLK yedekte (5000) koşar; 3000/8080 yalnız öncelik listesini görür (`SCAN_MAX_HOSTS` 512 ↔ 1022 ile aynı gerekçe). | ÇEKİRDEK | reçete | `R:2026-09-04__2026-09-04-kesif-kademeli-port` | mobil/src/services/discovery.service.test.ts (15) |
| kesif-cihaz-ag | Mobilde yedek portlar OPT-IN'dir (`extraPorts`, varsayılan KAPALI) ve yalnız kullanıcının 'Ağda Ara' dediği yolda açılır; arka plan self-heal turu (`serverReachability.trySelfHeal`) bunu AÇMAZ — port avı bir KURULUM sorunudur, kesinti sorunu değil. | ÇEKİRDEK | yasak | `R:2026-09-04__2026-09-04-kesif-kademeli-port` | mobil/src/services/discovery.service.test.ts §6 '… |
| kesif-cihaz-ag | Modal primitifi `src/components/AppModal.tsx`tir (Portal + Reanimated); `react-native-modal` KALDIRILDI ve geri getirilmez. `SimplePortal`ın host'a bildirimi BİLEREK bir mikrotask'a ertelenmiştir (SM-X230 çökme dersi) — bu erteleme KALDIRILAMAZ. | ÇEKİRDEK | yasak | `M:undated__allowed-packages`, `M:2026-09-04__modal-icindeki-dolu-metin-kutusu-2026` | yok |
| kesif-cihaz-ag | `AppModal` içindeki metin kutusu ÖN-DOLDURULMUŞ açılıyorsa `components/ModalTextInput.tsx` kullan (taslak portalın İÇİNDE, dış state yalnız yankı). Eşitleme koşulu `value` PROP'UNUN DEĞİŞMESİDİR, 'taslaktan farklı olması' DEĞİL — ikincisi hatanın ikizini üretir. | ÇEKİRDEK | reçete | `M:2026-09-04__modal-icindeki-dolu-metin-kutusu-2026` | mobil/src/components/ModalTextInput.test.tsx (§2b… |
| kesif-cihaz-ag | Cihaz katalogu VERİDİR: `PeripheralDevice` (admin → Cihaz Kaydı) kind/connectionType/address + protokol (pollCommand/terminator/decimals/scale/role) + per-cihaz `simulate` taşır. Tablette device-local seçim YOK; HAL `src/services/hal/`, `buildIoFromPeripheral(row)` transport+codec kurar. | ÇEKİRDEK | reçete | `M:undated__donanim-veri`, `M:undated__hal` | yok |
| kesif-cihaz-ag | Cihaz ömürlü tercihler (ekran state'i DEĞİL): `deviceSettingsStore.kk1ManualEntry` / `tamburCutMode` / `cameraFacing` (varsayılan back). Ölçüt: tercih tabletin fiziksel DURUŞUNU ya da istasyonun makine durumunu yansıtıyorsa cihaza; okutulan YERE bağlıysa oturuma (fener). | PROFİL | karar | `M:2026-08-02__manuel-otomatik-metraj-tercihi-cihazda-kalici`, `M:2026-08-19__yon-tercihi-cihazda-2026-08-19` | mobil/src/store/deviceSettingsStore.test.ts |
| kesif-cihaz-ag | `initialFacing` 'başlangıç değeri' değil ZORLAMADIR: verildiğinde cihaz tercihi ne OKUNUR ne YAZILIR (tek kullanıcısı kilit ekranı). Yazsaydı duvardaki tablette yapılan tek bir flip fabrikanın TÜM okutma ekranlarını sessizce çevirirdi. | ÇEKİRDEK | değişmez | `M:2026-08-19__yon-tercihi-cihazda-2026-08-19` | mobil/src/components/BarcodeScannerView.test.tsx … |
| kesif-cihaz-ag | Kısa kesim → otomatik A1: bayrak + eşik FABRİKA ayarıdır, cihazda yalnız ÜÇ DURUMLU override yaşar (`tamburShortCutA1Override`: server\|on\|off; bilinmeyen değer `server`a düşer). 'Hangi değer geçerli'nin TEK cevabı `resolveShortCutConfig`; `'on'`da eşik cihazınkidir, fabrikaya SIZMAZ. | ÇEKİRDEK | değişmez | `M:2026-08-19__tambur-kisa-kesim-otomatik-a1-2026` | mobil/src/screens/Modules/Tambur/resolveShortCutC… |
| kesif-cihaz-ag | Kısa-kesim kuralının TAMAMI `screens/Modules/Tambur/shortCutQuality.ts`tedir; üç yol (elle yazım · makine ölçümü · 'kalanı kes') aynı fonksiyonu çağırır — KOPYALAMA. Yalnız VARSAYILAN kaliteyle ateşler; A1 katalogdan çözülür, yoksa hiç ateşlemez (fail-closed). | ÇEKİRDEK | yasak | `M:2026-08-19__tambur-kisa-kesim-otomatik-a1-2026` | shortCutQuality.test.ts (12) + deviceSettingsStor… |
| kesif-cihaz-ag | APK yalnız `scripts/build-apk.mjs` ile derlenir — `cd android && ./gradlew assembleRelease` ELLE ÇAĞRILMAZ. Script adresi çözer → doğrular → önbellekleri siler → derler → APK içindeki gömülü adresi TEKRAR okur; düşerse paket `app-release.DOGRULANMADI.apk` olur, exit 1. | ÇEKİRDEK | yasak | `M:undated__apk-derleme-sahaya-kurulacak-paket-tek` | npm run build:apk:check / build:apk:verify |
| kesif-cihaz-ag | ERP adresi zinciri: `EXPO_PUBLIC_API_URL` → `src/constants/api.ts` → axios baseURL; değer DERLEME ANINDA gömülür (uygulama içi 'API Adresi' yalnız o cihazı düzeltir). Çözüm sırası `--api-url` → ortam → `.env*`; script değeri Gradle'a AÇIKÇA geçirir (.env.local sahaya sızmasın). | ÇEKİRDEK | değişmez | `M:undated__apk-derleme-sahaya-kurulacak-paket-tek`, `M:undated__api-baglantisi` | npm run build:apk:verify (APK içindeki gömülü adr… |
| kesif-cihaz-ag | Metro/Gradle önbelleği env değerini anahtarına ALMAZ — hızlı biten build iyi haber değil, BAYAT ADRESTİR (ölçüm: sıcak önbellek 17,9 sn ESKİ adres ↔ temiz 69,9 sn YENİ adres). Release bundle Hermes bytecode'dur: Türkçe dizeler UTF-16'ya gider ve grep BULMAZ — bundle'da hep ASCII dize ara. | ÇEKİRDEK | tuzak | `M:undated__apk-derleme-sahaya-kurulacak-paket-tek` | yok |
| kesif-cihaz-ag | `usesCleartextTraffic` app.json'da `android` ALTINDA GEÇERSİZDİR (Expo SDK 54 sessizce atar) — `expo-build-properties` eklentisinin `android` bloğuna yazılmak ZORUNDA. Doğrulama tek satır: `grep -o 'usesCleartextTraffic="[^"]*"' android/app/src/main/AndroidManifest.xml`. BU TUZAĞIN BEKÇİSİ YOK. | ÇEKİRDEK | tuzak | `M:undated__apk-derleme-sahaya-kurulacak-paket-tek` | yok |
| kesif-cihaz-ag | Mobil temel: RN + Expo 54, Android tablet (yatay) + telefon (dikey), YÖN KİLİDİ YOK — her ekran iki form factor'da da çalışmalı. Backend `Teks-Erp/` port 4000; kök CLAUDE.md domain kuralları mobilde de geçerlidir. | ÇEKİRDEK | değişmez | `M:undated__tekserp-mobil` | yok |
| kk1-idempotency-kuyruk | Retry, formu/makineyi YENİDEN OKUMAZ; düşen payload'ı birebir gönderir (otomatik modda yeniden ölçmek metrajı değiştirip gereksiz 409 üretir). | ÇEKİRDEK | değişmez | `R:2026-07-14__idempotency-2026-07-14` | yok |
| kk1-idempotency-kuyruk | İstemci uçuş penceresi `INFLIGHT_REUSE_WINDOW_MS = 90_000` ve backend penceresiyle BİLEREK AYNIdır: uçuşta AYNI yük gelirse uçuştaki kimliği (token + damga) yeniden kullan, FARKLI yük gelirse yeni top say. Körü körüne collapse etmek sıradaki GERÇEK topu düşürür. | ÇEKİRDEK | reçete | `R:2026-08-05__2026-08-05-kk1-mukerrer-korumasi` | mobil/src/offline/entryAttempt.test.ts |
| kk1-idempotency-kuyruk | Çevrimdışı sebebi `'link'` ise uçuş kimliğini BIRAK (operatör çevrimdışı olduğunu biliyor, basışlar ayrı toplardır), `'server'` ise KORU (panik basışı olabilir). Ayrım ekrandaki bir `if`'te değil SAF katmanda (`shouldReleaseInFlight`) yaşar. | ÇEKİRDEK | değişmez | `R:2026-08-05__2026-08-05-kk1-mukerrer-korumasi` | mobil/src/offline/entryAttempt.test.ts |
| kk1-idempotency-kuyruk | 5xx 'sunucuya ulaşılamadı' DEĞİLDİR — yanıt geldiyse sunucu cevap vermiştir. Çevrimdışı hükmü yalnız bir isteğin YANITSIZ düşmesiyle verilir (link AND erişilebilirlik); aksi hâlde tek hatalı uç tüm kuyruğu durdurur. | ÇEKİRDEK | tuzak | `R:2026-08-05__2026-08-05-kk1-mukerrer-korumasi` | mobil/src/offline/serverReachability.test.ts |
| kk1-idempotency-kuyruk | `onMutate`'te yeşil 'kaydedildi' basma — operatörü tekrar basmaya davet eder. Online'da nötr 'Kaydediliyor…', yeşil yalnız operatörün BEKLEDİĞİ deneme onaylanınca (`onSuccess`). | ÇEKİRDEK | yasak | `R:2026-08-05__2026-08-05-kk1-mukerrer-korumasi` | yok |
| kk1-idempotency-kuyruk | Kalıcı düşen istasyon kaydı ANLIK TOAST ile duyurulur (`announceFailure`) ve HİÇBİR kalıcı kuyruğa/kutuya yazılmaz. Ölü mektup kutusunu (`failedOps` + `OutboxModal` + `retryFailedOp`) geri getirme — iki bambaşka olayı tek kırmızı başlıkta topluyordu. | PROFİL | yasak | `R:2026-08-12__2026-08-12-kayit-kuyrugu-kaldirildi` | mobil/src/offline/announceFailure.test.ts |
| kk1-idempotency-kuyruk | Çakışma 409'u (`POSSIBLE_DUPLICATE`/`CLIENT_TOKEN_COLLISION`) EKRAN VARKEN toast basmaz — tek yüzeyi ekranın modalıdır; ama EKRANSIZ replay'de (kuyruktan diriltilen kayıt) DUYURULUR, yoksa 409 hiçbir yerde görünmez ve fiziksel top sistemde doğmaz. Belirsizlikte DUYUR. | ÇEKİRDEK | değişmez | `R:2026-08-12__2026-08-12-kayit-kuyrugu-kaldirildi` | mobil/src/offline/announceWire.test.ts |
| kk1-idempotency-kuyruk | Toast metni 'kayıt oluşmadı' DİYEMEZ — zaman aşımında sunucu COMMIT etmiş olabilir. Metin ÖNCE doğrulatır: '… Listede yoksa tekrar girin.' | ÇEKİRDEK | değişmez | `R:2026-08-12__2026-08-12-kayit-kuyrugu-kaldirildi` | mobil/src/offline/announceFailure.test.ts |
| kk1-idempotency-kuyruk | Kalıcı düşüş köprüsü `MutationCache.onError`'da kalır; `setMutationDefaults`'a TAŞINAMAZ — component `onError` onu ezer (query-core option sırası) ve kayıt 'bazen' yakalanır. | ÇEKİRDEK | yasak | `R:2026-08-12__2026-08-12-kayit-kuyrugu-kaldirildi` | mobil/src/offline/mutations.test.ts |
| kk1-idempotency-kuyruk | Sunucu yoklama adresi HER çağrıda canlı okunur (`getCurrentBaseUrl`), derleme zamanı sabitine bağlanmaz; ayrıca elle 'Şimdi dene' (`revalidateServer`) ve adres değişince otomatik yeniden değerlendirme bırakılır — yoksa uygulama asla çevrimiçiye dönmez. | ÇEKİRDEK | tuzak | `R:2026-08-12__2026-08-12-kayit-kuyrugu-kaldirildi` | mobil/src/offline/serverReachability.test.ts |
| kk1-idempotency-kuyruk | Yazıcı kuyruğu (`printQueue`) DURUYOR ve kaldırılmaz — 'top KAYITLI, etiketi çıkmadı' ayrı ve KALICI bir yüzeydir; kayıt kuyruğuyla aynı kefeye koyma. | PROFİL | karar | `R:2026-08-12__2026-08-12-kayit-kuyrugu-kaldirildi` | mobil/src/offline/printQueue.test.ts |
| kk1-idempotency-kuyruk | Modal içinde sayı girişi `NumpadInput` + `useNativeKeyboard`: büyük numpad bir `NumpadHost` ister, modalda host yoktur, tuşlar görünmez kalır. FAIL-SOFT yalnız Provider HİÇ YOKKEN devreye girer (`nativeMode = useNativeKeyboard \|\| numpad === null`) — bayrak hâlâ zorunlu. | ÇEKİRDEK | tuzak | `M:undated__modal-icinde-sayi-girisi` | yok |
| kk1-idempotency-kuyruk | `useNativeKeyboard` sistem decimal-pad'ini açar VE virgül→nokta normalizasyonunu korur ('40,5' → 40.5). | ÇEKİRDEK | değişmez | `M:undated__modal-icinde-sayi-girisi` | yok |
| kk1-idempotency-kuyruk | `NumpadHost` `autoActivate`'i aynı ekranda yalnız TEK alana ver — ikisine birden verilirse kazananı MOUNT SIRASI belirler ve operatör metraj beklerken tuşlar sessizce En'i değiştirir. | ÇEKİRDEK | yasak | `R:2026-08-12__2026-08-12-top-listesi-filtresi` | yok |
| kk1-idempotency-kuyruk | Çok top okutan YÜZEY `trigger="tap"` kullanır (kamera kendiliğinden okumaz); tek okuyup kapanan yüzey `auto` kalır. Ölçüt EKRAN ADI DEĞİL YÜZEY — KK1'in etiket-doğrulama tarayıcısı da tap. Sorun 'yanlış okuma' değil 'İSTENMEDEN okuma'dır. | ÇEKİRDEK | değişmez | `M:2026-08-05__cok-top-okutan-her-ekran` | mobil/src/components/BarcodeScannerView.test.tsx |
| kk1-idempotency-kuyruk | Picker'dan seçenek eklenebiliyorsa tetik listenin İLK hücresindeki `leadingAction` kartıdır (diğer kartlarla aynı geometri, `colors.action` mor zemin — marka indigo'su 'seçili kart' vurgusudur); basılınca picker KAPANMAZ, asıl form `quickAddSlot`'ta açılır. Listenin üstüne ayrı outlined buton KOYMA. | ÇEKİRDEK | reçete | `M:2026-07-30__picker-ici-yeni-ekle-2026-07` | yok |
| kk1-idempotency-kuyruk | Metraj kaynağı (elle ↔ makineden oku) ekran state'i değil CİHAZ ayarı: `kk1ManualEntry` + `tamburCutMode`; ömür OTURUM değil CİHAZ (operatör değişse de kalır). Diskteki BOZUK değer güvenli varsayılana düşer; setter ÖNCE state'i SONRA diski yazar. | ÇEKİRDEK | değişmez | `M:2026-08-02__manuel-otomatik-metraj-tercihi-cihazda-kalici`, `M:undated__varsayilanlar-korundu-kk1-manuel-kapali-tambur` | mobil/src/store/deviceSettingsStore.test.ts |
| kk1-idempotency-kuyruk | Cihaz ayarı varsayılanları: KK1 manuel giriş KAPALI, Tambur kesim modu MANUEL. | PROFİL | karar | `M:undated__varsayilanlar-korundu-kk1-manuel-kapali-tambur` | mobil/src/store/deviceSettingsStore.test.ts |
| kk1-idempotency-kuyruk | Ölçüm aleti varsa okuma TEK DOKUNUŞ olmalı: ⚖ → oku → DOĞRUDAN kaydet → kartta göster. Araya input modalı KOYMA (operatörün eli maldadır); yanlışsa tekrar basar, idempotent üzerine yazar. | ÇEKİRDEK | reçete | `M:undated__olcum-aleti-varsa-okuma-tek-dokunus` | yok |
| kursun-planlama-bypass | Tablet salt-okunur kararını SUNUCU verir ve istemci yalnız uygular (`StepSummary.tabletReadOnly` + `bypassAssignment`); ekran kart açılınca yazma yüzeyini hiç çizmez. Backend + Electron + APK aynı pencerede gider — alanı görmeyen eski APK yazmaya çalışıp ham 409 yer. | ÇEKİRDEK | değişmez | `R:2026-08-05__2026-08-05-kursun-planlama-birlesik` | yok |
| modul-bayraklari-profiller | Mobilde `MODULE_DISABLED`'ın kullanıcı yüzü HÂLÂ YOK (`useVisibleScreens.conditional` boş, `announceFailure` dalı yazılmadı) — P1'in açık maddesi; tablet kapalı modülün ekranını çizer, istek 403 alır. | BELİRSİZ | karar | `R:2026-09-02__2026-09-02-03-modul-anahtarlari` | yok |
| modul-bayraklari-profiller | Kısa-kesim → A1: bayrak + eşik FABRİKA ayarı (panel → Üretim → Tambur); cihazda yalnız üç durumlu override (`tamburShortCutA1Override` server\|on\|off; varsayılan/bilinmeyen → server); birleştirme TEK yerde `resolveShortCutConfig`; 'on'da eşik cihazınki, fabrikaya SIZMAZ; override yalnız süpervizöre. | PROFİL | değişmez | `M:2026-08-19__tambur-kisa-kesim-otomatik-a1-2026`, `R:2026-08-19__2026-08-19-2-paket-sektor` | resolveShortCutConfig.test.ts + deviceSettingsSto… |
| modul-bayraklari-profiller | Kısa-kesim kuralı `shortCutQuality.ts` tek fonksiyon — ekrandaki üç yol (elle yazım · makine ölçümü · kalanı kes) aynı fonksiyonu çağırır, kopyalanmaz; yalnız VARSAYILAN kaliteyle ateşler, otomatik A1 eşik üstünde geri döner (`shortCutRevert`); A1 katalogda yoksa ateşlemez (fail-closed). | ÇEKİRDEK | değişmez | `M:2026-08-19__tambur-kisa-kesim-otomatik-a1-2026` | shortCutQuality.test.ts (12) |
| refakat-karti | Baskı iki uçtan: TRAVELER_CARD → `/traveler-cards/:id/html` (+ `print-event`), diğerleri → `/printed-documents/:docType/:sourceId/html`; mobilde dallanma TEK yerde (`workOrderDocuments.printDocument`), mobil BİLİNÇLİ doğrudan basar. Yeni tip: backend listeye ekler, mobilde yalnız printDocument. | ÇEKİRDEK | reçete | `R:2026-08-05__2026-08-05-is-emri-belgeleri` | yok |
| refakat-karti | Çok top okutan her ekran `trigger="tap"` (kamera kendiliğinden okumaz — sorun 'istenmeden okuma', çözüm taramayı kapatmak); tek okuyup kapanan ekranlar (refakat kartı/iş emri QR dahil) `auto`. Not: KK1 ana giriş auto, scan-back modalı çok-okutmalı olduğu için tap — kural tutarlı. | ÇEKİRDEK | değişmez | `M:2026-08-05__cok-top-okutan-her-ekran` | yok |
| refakat-karti | Mobilde tıklanabilir her alan `react-native-paper` ile kurulur (sıfırdan yazma): `TouchableRipple` (Pressable/TouchableOpacity yerine) · `Button` · `IconButton` · `Appbar.Action` · `List.Item` · pasif kart `Surface`. | ÇEKİRDEK | reçete | `M:undated__tiklanabilir-her-alan` | yok |
| refakat-karti | Ölçüm aleti varsa okuma TEK DOKUNUŞ (⚖ → oku → doğrudan kaydet → kartta göster; araya input modalı KOYMA, tekrar basış idempotent ezer); manuel/ikincil yollar ⋮ menüsünde `SackActionsSheet` (bottom AppModal, satır ≥56dp) — yıkıcı aksiyon menüye taşınsa da ONAY DİYALOĞU KORUNUR. | ÇEKİRDEK | reçete | `M:undated__olcum-aleti-varsa-okuma-tek-dokunus`, `M:undated__manuel-ikincil-yol-menusunde` | yok |
| refakat-karti | Picker içi 'yeni ekle' tetiği `PickerModal.leadingAction`: listenin İLK hücresinde mor (`colors.action`, indigo 'seçili' vurgusuyla çakışmasın) aksiyon kartı, sıralama/aramadan bağımsız; basılınca picker KAPANMAZ, form `quickAddSlot`'ta açılır; listenin üstüne ayrı outlined buton KOYMA. | ÇEKİRDEK | reçete | `M:2026-07-30__picker-ici-yeni-ekle-2026-07` | yok |
| rota-renk-ozellik-kapsama | Geri Gönder tuşu yalnız saha düzeltme yetkisi olanda ve ÇEVRİMİÇİYKEN çizilir (uç online-only); önizlemesiz uygulama YOK, sebep ≥3 (TamburBringRollModal sözleşmesi); başarı toast'ı hedef istasyon adını MUTLAKA taşır (top Tambur listesinden düşer, operatör 'kayboldu' demesin). | ÇEKİRDEK | reçete | `M:2026-08-19__tambur-boyahaneye-geri-gonder-2026-08` | yok |
| rota-renk-ozellik-kapsama | KÜME DIŞI (mobil): `AppModal` içinde ÖN-DOLU açılan metin kutusu `ModalTextInput` kullanır; `SimplePortal` mikrotask ertelemesi KALDIRILAMAZ; eşitleme koşulu `value` PROP'unun değişmesi, 'taslaktan farklı' değil. mobil/CLAUDE.md klasör haritası bayat (data/lib/offline/test/theme eksik). | ÇEKİRDEK | tuzak | `M:2026-09-04__modal-icindeki-dolu-metin-kutusu-2026`, `M:undated__klasor-yapisi` | components/ModalTextInput.test.tsx |
| sebep-kataloglari | Mobil çevrimdışı zemin: metin SAKLAYAN kind'larda kodlar sentetiktir (`BUILTIN_<i>`) ve sunucuya GÖNDERİLMEZ (sunucu metinden türetir); metin SAKLAMAYAN kind'ta (WORK_ORDER_REWORK) zemin GERÇEK katalog kodlarını taşır — uydurma kod rapor anahtarını çöpe çevirir. | ÇEKİRDEK | yasak | `R:2026-08-25__2026-08-25-aksam-mobil-hizli` | yok |
| sebep-kataloglari | Tablette katalog okuması ÜÇ KADEMELİDİR ve sırası anlamlıdır: sunucu → diskteki son bilinen liste → APK'ya gömülü zemin. Üçüncü kademe KALDIRILAMAZ (sebep fire kararında zorunlu; boş liste = malın tamburda kilitlenmesi). Her kademe KARARLI referans döndürür. | ÇEKİRDEK | değişmez | `R:2026-08-19__2026-08-19-hazir-sebep-kataloglari` | yok |
| sebep-kataloglari | Sebep seçimi TEK bileşenden (`ReasonPresetPicker`) geçer: serbest metin kutusu chip listesinin ÜSTÜNDE; yazmaya başlamak `requiresText` satırını kendiliğinden seçer, chip'e dokunmak serbest metni temizler, seçili chip'e tekrar dokunmak (optional iken) seçimi kaldırır. | ÇEKİRDEK | reçete | `R:2026-08-25__2026-08-25-aksam-mobil-hizli` | ReasonPresetPicker.test.tsx (6) |
| sebep-kataloglari | Yıkıcı aksiyon taşıyan sebep listesine (top iptal chip'leri: dokununca topu iptal eder) satır içi düzenleme kalemi KONMAZ; düzenleme ayrı yüzeyde (`ReasonPresetManagerSheet`) açılır. | ÇEKİRDEK | yasak | `R:2026-08-19__2026-08-19-hazir-sebep-kataloglari` | yok |
| sebep-kataloglari | Sebep adımı ÜÇ BÖLGEDİR: başlık + serbest metin SABİT · sebep listesi kayan (`Animated.ScrollView`, `flexShrink:1`) · Geri/Kaydet SABİT footer. Liste büyüyen tek bölge olduğu için footer'ın ayrı yaşaması zorunludur (sheet tavanı aşılınca kırpılan taraf hep en alttır). | ÇEKİRDEK | değişmez | `A:2026-08-26__2026-08-26-aksam-sebep-listesi` | yok |
| sebep-kataloglari | Sıra basılı tut-sürükle ile değişir ve `PATCH /api/reason-presets/reorder` ile kalıcıdır; sunucu o kind'ın TÜM id'lerini ister, ekran yalnız AKTİF satırları çizer → köprü `mergeVisibleOrder` (gizli satır KENDİ YUVASINDA kalır); tam liste YAZMA ANINDA çekilir (`list(true)`). | ÇEKİRDEK | değişmez | `A:2026-08-26__2026-08-26-aksam-sebep-listesi` | mobil/src/services/reasonPreset.order.test.ts (8;… |
| sebep-kataloglari | Yerel sıra (`localOrder`) BAŞARIDA temizlenmez (temizlenirse liste bir kare eski sırayı gösterir), hatada temizlenir + toast; sürükleme yalnız `canEditPresets` olan kişide açık ve `builtin:` zemin satırlarında KAPALI (sunucuda karşılığı yok). | ÇEKİRDEK | tuzak | `A:2026-08-26__2026-08-26-aksam-sebep-listesi` | yok |
| sebep-kataloglari | Hızlı İş Emri okutma kararı satır içi `if` zinciriyle değil TEK saf yüklemde verilir (`scanClassify`) ve sıra load-bearing: iptal → STATÜ → çuval/sevkiyat → kumaş kilidi. `SHIPPED` topa 'çuvaldan çıkarın' demek malın müşteride olduğunu gizler. | ÇEKİRDEK | tuzak | `R:2026-08-25__2026-08-25-aksam-mobil-hizli` | scanClassify.test.ts (12) |
| sebep-kataloglari | Hızlı İş Emri bitmiş topu da alır (WAREHOUSE/A1_STOCK); `if (status !== 'STOCK') reject` elemesi kaldırıldı. Ham + bitmiş aynı WO'da serbesttir (rozetle ayrılır), hedef renk BOŞ gelir, sebep İSTEĞE BAĞLIDIR, 2. kalite dahildir. | PROFİL | karar | `R:2026-08-25__2026-08-25-aksam-mobil-hizli` | scanClassify.test.ts |
| sebep-kataloglari | `RollPickerModal.scopeTabs` sekme anahtarı sorgu anahtarına (`effectiveFilters`) GİRMELİ; girmezse sekme değişince liste tazelenmez. | ÇEKİRDEK | tuzak | `R:2026-08-25__2026-08-25-aksam-mobil-hizli` | yok |
| sebep-kataloglari | Yeniden üretim sebebi İKİ hedefe yazılır: metin → 1. rota adımının notu → fason çekisine TALİMAT · kod+metin → `WorkOrder.parameters.rework` (rapor anahtarı). Migration açılmaz, `parameters` zaten JSON ve `quick-start` Zod şeması kabul eder. | ÇEKİRDEK | reçete | `R:2026-08-25__2026-08-25-aksam-mobil-hizli` | reworkPayload.test.ts (8) |
| sebep-kataloglari | Ölü etiket uyarısı DAR koşulla gösterilir (top etiketli VE ilk adım fason) — orada barkodun geçersizleşmesi KESİNDİR (fason kabulünde top `SUBCONTRACTOR_CONSUMED` olur, mal yeni barkodla döner); uyarı engel değil bilgidir, geniş tutmak gürültüye çevirir. | PROFİL | değişmez | `R:2026-08-25__2026-08-25-aksam-mobil-hizli` | ReworkRollsDialog.test.tsx (7) |
| sebep-kataloglari | Tambur 'Boyahaneye Geri Gönder' hedef adımını SUNUCU çözer (`/tambur/manual/send-to-dye-preview`) — tablette rota yok ve olsaydı bile 'hangi adım boya veriyor' yüklemi ikinci kez yazılırdı; önizlemesiz uygulama YOK, sebep ≥3 karakter, tuş yalnız yetkilide ve ÇEVRİMİÇİYKEN çizilir. | ÇEKİRDEK | değişmez | `M:2026-08-19__tambur-boyahaneye-geri-gonder-2026-08` | yok |
| sebep-kataloglari | Başarı toast'ı hedef istasyon adını MUTLAKA taşır — top Tambur listesinden düşer, operatör nereye gittiğini görmezse 'kayboldu' der. | ÇEKİRDEK | reçete | `M:2026-08-19__tambur-boyahaneye-geri-gonder-2026-08` | yok |
| sevkiyat-brut-storno-cuval | Mobil istemci brüt rakamı ELLE KURMAZ (`rollCount + returnedCount` çift sayar); çuval kartındaki işaret `sack.returnedCount` ile konur, iade dipnotu backend'in `summary.returnedCount/returnedMeters` alanından basılır. | ÇEKİRDEK | yasak | `R:2026-08-03__2026-08-03-brut-kurali-sevkiyat` | yok |
| sevkiyat-brut-storno-cuval | Mobil cihaz çözümü OTURUM-KAPSAMLI: `useMachinePeripherals('METER'\|'SCALE')` → `GET /peripherals/for-session` (makinesiz SHIPPING istasyon-oturumu → istasyon donanımı; oturum yoksa BOŞ, fail-closed). Çuval tartısı SCALE; `btPrinterStore` KALDIRILDI; 409 `WORK_SESSION_REQUIRED` oturumu düşürür. | ÇEKİRDEK | değişmez | `M:undated__cozumleme-oturum-kapsamli` | mobil/src/hooks/useMachinePeripherals.test.ts |
| sevkiyat-brut-storno-cuval | Tartı FAIL-CLOSED: cihaz yok / bond edilemedi / okunamadı / değer ≤ 0 → NET Türkçe toast, mutasyon HİÇ ÇAĞRILMAZ; sessiz sahte değer yok. | ÇEKİRDEK | değişmez | `M:undated__fail-closed`, `M:2026-07-30__donanim-okumasi-tek-dokunus-tasan-aksiyonlar` | mobil/src/hooks/useSackWeigh.test.tsx |
| sevkiyat-brut-storno-cuval | BT tek soket: ikinci dokunuş `busyRef` ile SESSİZCE yok sayılır, kuyruğa ALINMAZ (hangi kg hangi çuvala gitti karışır); spinner yalnız işlem gören satırda (`weighingSackId`). Donanım okuması TEK DOKUNUŞ; elle gir/etiket/not/sil ⋮ `SackActionsSheet`e (≥56dp) taşınır, yıkıcı aksiyonda onay KORUNUR. | ÇEKİRDEK | değişmez | `M:undated__eszamanlilik`, `M:undated__manuel-ikincil-yol-menusunde`, `M:2026-07-30__donanim-okumasi-tek-dokunus-tasan-aksiyonlar` | mobil/src/hooks/useSackWeigh.test.tsx |
| sevkiyat-brut-storno-cuval | [PROFİL] Mobil ekranlar: `TartiPaket/` etiketi 'Sevkiyat', `Sevkiyat/` etiketi 'Sevk Çıkışı'; izinler `mobile:tarti-paket` / `mobile:sevkiyat`; backend `/api/shipping`. Ekran KEY ≠ görünen ETİKET. 'Sevk Çıkışı' izin kapılı, `confirmationEnabled`a bakmaz. 'İmplementasyon sırası' listesi tarihsel. | PROFİL | karar | `M:undated__ekran-onceligi-implementasyon-sirasi`, `R:2026-08-22__2026-08-22-sevk-kapisi-bayragin` | yok |
| sevkiyat-brut-storno-cuval | Okutma geri bildirimi ekran ekran KOPYALANMAZ — aynı okutma iki ekranda iki şey söylerse operatör hangisine güveneceğini bilemez; tek kaynak üç ortak katman (`signalScan` · `useScanFeedback` · `ScannerRollStrip`). | ÇEKİRDEK | değişmez | `M:2026-08-06__top-okutma-yuzeyi-uc-katman-ucu` | yok |
| surum-yayin | Tablet kanalı TAHMİN EDİLMEZ: `npm run yayinla:check -- --musteri=<kod>` → 'parmak izi tutarlı' = OTA, 'NATIVE DEĞİŞTİ' = `runtimeVersion` artır + APK. Parmak izi değişip runtimeVersion aynıysa yayın DURUR (`--parmak-izini-kabul-et` bilinçli); şüphede runtimeVersion ARTIR. Kayıt `alg` taşır. | ÇEKİRDEK | reçete | `M:undated__guncelleme-dagitma-once-bu-bolum`, `M:undated__uzaktan-guncelleme-ota-apk-sahaya-nasil`, `R:2026-08-26__2026-08-26-mobil-uzaktan-guncelleme` | yayinla-ota.mjs parmakIziKapisi (--check yan etki… |
| surum-yayin | `runtimeVersion` (app.json) JS↔native uyum kimliğidir, backend sözleşmesini KAPSAMAZ; süzmesi SUNUCUNUN işidir (istemci indirmede bakmaz, yanlış sürümü indirip sessizce eler) → adres sürümü İÇERİR (`/<müşteri>/mobil/ota/<rv>/manifest`), her APK yalnız kendi paketini görür. | ÇEKİRDEK | değişmez | `M:undated__uzaktan-guncelleme-ota-apk-sahaya-nasil`, `R:2026-08-26__2026-08-26-mobil-uzaktan-guncelleme` | Teks-Erp/scripts/test_mobile_update.ts (37; backe… |
| surum-yayin | OTA turu da yama numarası alır; tablette görünen sürüm APK'dan DEĞİL PAKETTEN okunur (`Constants.expoConfig.version` → manifestin `extra.expoClient`i), native'e dokunmadan değişir. Yayındaki sürüm `multipart/mixed` gövdenin `manifest` parçasından çözülür ('ilk { son }' kestirmesi ÇALIŞMAZ). | ÇEKİRDEK | değişmez | `R:undated__surum-yayinlama-sahaya-guncelleme-cikarma` | scripts/test_surum.mjs (manifestGovdesindenSurum) |
| surum-yayin | `usesCleartextTraffic` app.json → `android` altında GEÇERSİZ (SDK 54 sessizce atar); yalnız `expo-build-properties` android bloğu geçerli. `android/` git dışı prebuild ÇIKTISI: eski klasör doğru görünür, prebuild'de bayrak düşer → APK hiç istek yollayamaz. BEKÇİ YOK: prebuild sonrası grep'le bak. | ÇEKİRDEK | tuzak | `M:undated__apk-derleme-sahaya-kurulacak-paket-tek` | yok (bilinçli; script yalnız grep komutunu hatırl… |
| surum-yayin | Bundle doğrulama: release bundle Hermes bytecode — ASCII dizeler düz metin, Türkçe karakterli dizeler UTF-16 tablosuna gider ve grep BULMAZ; dizeler uç uca ('sonrasında harf gelmesin' sondajı eşleşmeyi eler). Hep ASCII dize ara. Sıcak Metro/Gradle önbelleği env'i görmez: HIZLI build bayat adrestir. | ÇEKİRDEK | tuzak | `M:undated__apk-derleme-sahaya-kurulacak-paket-tek` | build-apk.mjs önbellek silme + bundle geri okuma |
| surum-yayin | APK arm64, güncelleme kanalından yayınlanır (`mobil-yayinla.mjs --apk --surum --vc`; önce apk sonra künye). Tablet künyeyi (`apk/surum.json`) kanaldan okur (`apiClient` DEĞİL), `versionCode` ile kıyaslar, indirir, Android kurulum ekranını AÇAR — sessiz kurulum yok, operatör 'Yükle'ye dokunur. | ÇEKİRDEK | karar | `R:2026-08-26__2026-08-26-mobil-uzaktan-guncelleme`, `R:undated__surum-yayinlama-sahaya-guncelleme-cikarma`, `M:undated__guncelleme-dagitma-once-bu-bolum` | mobil-yayinla.mjs künye doğrulaması (versionCode … |
| surum-yayin | 'İki adres farklı' (ERP ↔ güncelleme kanalı) uyarısı EKLENMEZ — kanallar ayrılınca her cihazda kalıcı yanlış alarma dönüşürdü; iki bağlantı etiketli bilgi olarak basılır. Hep bağıran uyarı gerçek sapmada da susar. | PROFİL | karar | `R:2026-08-26__2026-08-26-mobil-uzaktan-guncelleme` | yok |
| tambur-finalize-kesim | Mobil filtre şeridi sözleşmesi: `last7` BUGÜNÜ içerir; özel aralık kısa yolu EZER; query anahtarı `itemLabel`/`now`dan bağımsız; gün sınırı cihazın yerel günü; filtre yoksa tek parametre üretilmez; Tambur'da modal kapanınca filtre sıfırlanır; tarih seçici PAKETİ EKLENMEZ. | ÇEKİRDEK | değişmez | `R:2026-08-12__2026-08-12-top-listesi-filtresi` | mobil/src/components/filters/rollHistoryFilter.te… |
| tambur-finalize-kesim | `NumpadHost` `autoActivate` aynı ekranda yalnız TEK alana verilir — ikisine verilirse kazananı MOUNT SIRASI belirler ve tuşlar sessizce yanlış alanı değiştirir; hedef boşalırsa manuel modda metraja dönen effect korunur. | ÇEKİRDEK | yasak | `R:2026-08-12__2026-08-12-top-listesi-filtresi` | yok |
| tambur-finalize-kesim | Tüm istasyonlar (Tambur, KK1) okuma için HAL'i kullanır; eski `hardware.service.ts` mock'u KALDIRILDI, geri getirilmez; simülasyon per-cihaz veri bayrağı (`simulate`), donanım yoksa NET HATA. | ÇEKİRDEK | yasak | `M:undated__tum-istasyonlar-tambur-kk1-okuma-icin` | yok |
| tambur-finalize-kesim | Responsive: tablet yatayda iki sütun/split view, telefon dikeyde tek sütun; yön kilidi YOK (`orientation: default`), her ekran iki yönde çalışmalı. | ÇEKİRDEK | değişmez | `M:undated__responsive-layout` | yok |
| tambur-finalize-kesim | Modal içinde sayı girişi `NumpadInput` + `useNativeKeyboard` — büyük özel numpad bir `NumpadHost` ister, modalda host YOK, tuşlar görünmez kalır; `useNativeKeyboard` sistem decimal-pad'ini açar ve virgül→nokta normalizasyonunu korur. | ÇEKİRDEK | tuzak | `M:undated__modal-icinde-sayi-girisi` | yok |
| tambur-finalize-kesim | Tambur ana kesim ve 'Top Kesme' modalı TEK tercihi paylaşır (`recutMode = cutMode`, aynı metre makinesi); yan etki BİLİNÇLİ: modalda mod değişince ana kesim de değişir. Varsayılanlar: KK1 manuel KAPALI, Tambur MANUEL [PROFİL]; bozuk disk değeri güvenli varsayılana; setter önce state sonra disk. | ÇEKİRDEK | değişmez | `M:undated__tambur-un-iki-gecisi-ana-kesim`, `M:undated__varsayilanlar-korundu-kk1-manuel-kapali-tambur` | mobil/src/store/deviceSettingsStore.test.ts (recu… |
| tambur-finalize-kesim | Çok top okutan her ekran `trigger="tap"` (kamera kendiliğinden okumaz — sorun 'istenmeden okuma', onay değil taramayı KAPATARAK çözülür): Hızlı İş Emri, Fason Sevk, KK1 etiket-doğrulama. Tek okuyup kapananlar (Tambur, Kartela, refakat kartı) `auto`. | ÇEKİRDEK | değişmez | `M:2026-08-05__cok-top-okutan-her-ekran` | mobil/src/components/BarcodeScannerView.test.tsx |
| tambur-finalize-kesim | Kısa kesim → A1: bayrak+eşik FABRİKA ayarı; cihazda 3 durumlu `tamburShortCutA1Override` (server\|on\|off, bilinmeyen→server); birleştirme TEK yer `resolveShortCutConfig`; 'on'da eşik cihazınki, fabrika eşiğine SIZMAZ; override yalnız süpervizöre (`roll:manual-adjust \|\| mobile:tambur-duzelt`). | PROFİL | değişmez | `M:2026-08-19__tambur-kisa-kesim-otomatik-a1-2026` | mobil/src/screens/Modules/Tambur/resolveShortCutC… |
| tambur-finalize-kesim | Kısa kesim kuralının TAMAMI `Tambur/shortCutQuality.ts`te — üç yol (elle yazım · makine ölçümü · 'kalanı kes') aynı fonksiyon; yalnız VARSAYILAN kaliteyle ateşler (seçilmiş A1/FIRE'a dokunmaz); eşik üstüne çıkınca yalnız OTOMATİK A1 geri döner (`shortCutRevert`); A1 katalogdan, yoksa fail-closed. | ÇEKİRDEK | değişmez | `M:2026-08-19__tambur-kisa-kesim-otomatik-a1-2026` | mobil/src/screens/Modules/Tambur/shortCutQuality.… |
| tambur-finalize-kesim | `AppModal` içinde ÖN-DOLU açılan metin kutusu `components/ModalTextInput.tsx` kullanır (taslak portalın içinde, dış state yankı); `SimplePortal` mikrotask ertelemesi KALDIRILAMAZ (SM-X230); eşitleme koşulu `value` PROP'UNUN değişmesi, 'taslaktan farklı' DEĞİL; boş açılan kutular etkilenmez. | ÇEKİRDEK | reçete | `M:2026-09-04__modal-icindeki-dolu-metin-kutusu-2026` | mobil/src/components/ModalTextInput.test.tsx |
| yari-mamul | Mobil Depo 'Ham'/'Yarı Mamul' sekmeleri `rollScope=RAW_STOCK_PURE`/`SEMI_FINISHED` + `status:'ALL'` gönderir, düz `status:'STOCK'` GÖNDERMEZ; 'Tümü' sekmesi bilerek statü tabanlı kalır. Ham Stok ve Yarı Mamul kolon/sekme renkleri AYRI tondadır (komşu kolonlarda sayaçlar karışır). | PROFİL | reçete | `R:2026-08-27__2026-08-27-2-tur-yari` | yok |
| yari-mamul | Liste sayfalamada VARSAYILAN cursor (keyset) + infinite scroll'dur; offset/`page`+`pageSize` yalnız açıkça istenirse ya da kalıcı küçük tablolarda. `Roll`/`RollMovement`/`RollOperation` ve log geçmişleri HER ZAMAN cursor — offset `MAX_OFFSET=10000`'de 400 verir, her sayfada COUNT(*) tazeler. | ÇEKİRDEK | değişmez | `M:undated__liste-sayfalama-default-cursor-infinite-scroll` | yok |
| yari-mamul | Cursor sözleşmesi: `GET /rolls?mode=cursor&limit=N&cursor=...&withTotal=true`; toplam YALNIZ ilk sayfada ve YAKLAŞIKtır. Benzer uçlarda aynı `mode=cursor` sözleşmesi izlenir (`utils/cursor.ts`, `rollService.getAllCursor`). | ÇEKİRDEK | reçete | `M:undated__liste-sayfalama-default-cursor-infinite-scroll` | yok |
| yetki-izin-rol | Mobil `usePermissions().has()` üç jokeri tanır: global `*` (süperadmin, backend'den `["*"]`), `mobile:*`, `admin:*`; `allowedScreens` `MOBILE_SCREENS.filter(has)` — joker dalı olmadan giriş başarılı görünür, ekran yoktur ('yetkin yok'). Saf JS → OTA ile gider, APK gerekmez. | ÇEKİRDEK | değişmez | `A:2026-09-03__2026-09-03-superadmin-p2-gizli`, `B:undated__katalog-yine-atama-icermez` | mobil/src/hooks/usePermission.test.ts |

## arşiv (7)

| Küme | Kural | Sınıf | Tür | Kaynak | Bekçi |
|---|---|---|---|---|---|
| fason | Deploy sırası (kısmi kabul + çekme turları): migration + backend ÖNCE (eski APK tam kabulle çalışmaya devam eder), Electron + APK sonra. Tek seferlik dağıtım notu, canlı kural değil. | ÇEKİRDEK | reçete | `R:2026-08-19__2026-08-19-fason-kismi-kabul` | yok |
| finans-saglamlik | Sevkiyatlar (operasyon) ve Sevkiyatlar (Muhasebe) ekranları ayrı kişilerce kullanılır ama İZİNLE AYRILMADI (sevkiyatçının da belgelere erişimi gerekir); muhasebe listesi varsayılanı `dispatchedAt desc`, dönem bandı `?withSummary=true`. | ÇEKİRDEK | karar | `R:2026-08-02__2026-08-02-brut-kurali-liste` | yok |
| parti-no-batch | Parti no ile ARAMA tanım gereği çok sonuç döndürür (numara dönüyor) — bu kararın doğal sonucudur, `contains` artefaktı değil; tam-eşleşmeye çekmek düzeltmez. Gerçek çözüm arama + tarih aralığıdır (ayrı iş). | PROFİL | karar | `R:2026-07-13__is-emri-no-parti-2026-07` | yok |
| parti-no-batch | Eski 'dal' / `batchSplitId` kavramı KALKTI — kolon şemada ve `src/` içinde yok; bu kavrama yaslanan yeni yüzey yazma (soy bağı bugün `Batch.splitFromId` / `mergedIntoId`). | ÇEKİRDEK | yasak | `R:2026-07-13__is-emri-no-parti-2026-07` | yok (Prisma şeması: kolon yok) |
| rota-renk-ozellik-kapsama | TARİHSEL kararlar (kural değil): `TargetPropertyPicker` silindi (özellik seçiminin yolu rota adımı istasyon chip'leri); 08-06 paketi migration+backend+Electron aynı pencerede, APK zorunlu değil; 08-19 plan kapısı backend+Electron+APK BİRLİKTE; 08-21/08-27 backend ÖNCE, migration/izin yok. | ÇEKİRDEK | karar | `R:2026-08-02__ozellik-istasyon-kisiti-kalir-ama-bos`, `R:2026-08-06__2026-08-06-rota-sablonu-hedef`, `R:2026-08-19__2026-08-19-tambur-plan-sapma` | yok |
| yari-mamul | Renkli yarı mamul topu iş emrine bağlıyken hedef renk değiştirilirse `workorder-target-color.helper` onu 'zaten boyandı' sayıp `COLOR_DYED_BLOCKED`/`COLOR_PARTIAL_CONFIRM` sorar — doğru semantiktir, düzeltilecek hata değil. | ÇEKİRDEK | karar | `R:2026-08-26__2026-08-26-yari-mamul-kendi` | yok |
| yari-mamul | Ölçüm (prod yedeği 2026-08-25, 2431 top): `SEMI_FINISHED` kaydı SIFIR; 3. tur da 'prod'da 0 yarı mamul kaydı varken' kapatıldı. Tarihsel ölçüm — bugünkü değeri doğrulanmadı, karar gerekçesi olarak arşivde kalır. | PROFİL | ölçüm | `R:2026-08-26__2026-08-26-yari-mamul-kendi`, `R:2026-08-27__2026-08-27-3-tur-yari` | yok |

## sil (3)

| Küme | Kural | Sınıf | Tür | Kaynak | Bekçi |
|---|---|---|---|---|---|
| belge-etiket-sablon | BLE (react-native-ble-plx, services/bluetooth.service.ts) etiket yazıcı/okuyucu yolunda KULLANILMIYOR — servisi mobil/src'de hiçbir dosya import etmiyor; 'BLE: barkod okuyucu veya etiket yazıcı' satırı yanlış. Paket app.json'da durur (kaldırmak APK ister). | ÇEKİRDEK | karar | `M:undated__kullanim__2` | yok |
| fason | Teks-Erp/CLAUDE.md:376-379 'admin/123123 için hâlâ AÇIK: test_direct_ship_api, test_quickstart_dispatch_api, test_http_api, smoke_fason_http' paragrafı ÖLÜ — dördü de `fixture-test-user.ts` (ensureTestAdmin) ile kendi kullanıcısını üretiyor. Silinmeli. | ÇEKİRDEK | karar | `B:undated__fixture-seed-master-data-si-business` | yok |
| is-emri-siparis-bag | Alt CLAUDE'daki '~78 model, ~35 enum, clientToken (Roll/Order/WorkOrder)' cümlesi BAYAT: ölçüm 124 model / 68 enum / 16 modelde `clientToken @unique`. Sayıyı sil, 'schema.prisma + ARCHITECTURE §8.1' de; `clientToken`/`SwatchStockReduction` kuralı idempotency kümesinde yaşar. | ÇEKİRDEK | ölçüm | `B:undated__78-model-35-enum-idempotency-katmani`, `R:2026-07-14__idempotency-2026-07-14` | yok |