# Veritabanı — migration yazımı ve şema-dışı nesneler

Bu dosya [`VERITABANI.md`](VERITABANI.md)'nin §7 ve §8'idir; 2026-09-13'te oradan **bölünerek** geldi. Gerekçe: o dosya belge boyut tavanına 23 bayt kalmıştı ve **tavan yükseltilmedi** — tavan bir borç sayacı değil, bağlam diyetinin tasarım kısıtıdır (kök `CLAUDE.md` 42k→12k turu, kapı `scripts/check-docs.mjs`).

⚠️ **Bölüm numaraları KORUNDU.** Burası §1 değil §7 diye başlar, çünkü `VERITABANI.md §7` / `§8` diye işaret eden çapalar (`docs/RECETELER.md`, arşiv notları) taşınan şeyi bulmaya devam etmelidir: *bir çapa, işaret ettiği şey taşındığında kopmamalıdır.*

**Orada kalanlar:** model şablonu (§1) · model sınıfları (§2) · künye FK (§3) · index (§4) · enum (§5) · `///` gerekçe (§6) · soft delete (§9) · Decimal/zaman (§10) · kapı asimetrisi (§11) · bilinen boşluklar (§12) · boyut (§13) — `docs/standart/VERITABANI.md`.

Kural biçimi ve zorlama etiketleri: [`README.md`](README.md). Reçete hâli: `docs/RECETELER.md` § Yeni migration ve canlıya çıkarma.

> **[DB-39] 2026-09-13'e kadar [DB-31] diye yazılıydı** ve §9'daki hard-delete kuralıyla ÇAKIŞIYORDU (aynı kimlik, iki kural). Arşivde iki atıf da bu kimliği kullanır: `perf-index-migration-sonuc.json` (migration dosyası değişmez → bugün **[DB-39]**) ve `kapatma-dusen-is.json` (§9 hard delete → **[DB-31]** olarak kalır).

---

## 7 · Migration yazımı

- **[DB-20]** Dizin adı `prisma/migrations/<14 hane>_<snake_slug>/migration.sql` · zorlama: insan:ad kalıbını ölçen kapı yok — `--create-only` damgayı zaten üretir, slug insana kalır · kanıt: 232/232 dosya `^[0-9]{14}_[a-z0-9_]+$` · devralınan: yok
- **[DB-21]** SQL'i ELLE yaz: `prisma migrate dev` koşma; `--create-only` ile üret ve iki DEFERRABLE composite FK'nın `DropForeignKey` satırlarını SİL · zorlama: hook:`scripts/claude-hooks/bash-guard.mjs:36` (depo KÖKÜ; create-only'siz `migrate dev` durdurulur) + insan:silme adımının yapıldığı ölçülmez · kanıt: 135 dosya (%58) tamamen elle; tuzağın kanonik anlatımı `prisma/migrations/20260904120442_cuval_izleri_etiket/migration.sql:6-16` · devralınan: yok
- **[DB-22]** Dosya bir gerekçe bloğuyla açılır: ne yapıyor · ADDITIVE mi · üretilmiş çıktıdan hangi satır elle silindi · zorlama: insan:başlığın içeriği ölçülemez (`ILKELER.md` [IL-25] ile aynı sınıf) · kanıt: `20260904120442_cuval_izleri_etiket/migration.sql:1-16` · devralınan: yok
- **[DB-23]** Elle koşulan SQL IDEMPOTENT yazılır (`IF NOT EXISTS` / `IF EXISTS`) — `apply-migration.ts` defterde satır varsa `resolve`'u atlar ama SQL'i YİNE koşar · zorlama: insan:idempotency SQL semantiğidir, grep'le ölçülmez · kanıt: 34 dosya `IF NOT EXISTS`, 29 dosya `IF EXISTS`; script sözleşmesi `scripts/apply-migration.ts:1-23` · devralınan: yok
- **[DB-24]** Canlı veriye tekillik/sed eklerken YUMUŞAK KAPI kur: `DO $etiket$` bloğu mükerrerleri sayar → varsa `RAISE NOTICE` + ATLA, yoksa `CREATE UNIQUE INDEX IF NOT EXISTS`; sert `RAISE EXCEPTION` prod deploy'unu kilitler · zorlama: insan:blok yapısı AST'den değil SQL'den okunur · kanıt: `20260821150000_name_fold_unique_live/migration.sql:52-76`; 5 dosya `RAISE NOTICE` · devralınan: yok
- **[DB-25]** Büyük tabloya DDL koyan migration EN BAŞA `SET statement_timeout = 0;` yazar · zorlama: insan:"büyük tablo mu" satır sayımıdır, şemadan okunmaz · kanıt: 42/232 dosya; DB ayarı `Teks-Erp/docker-compose.yml:26` (`statement_timeout=50s`) · devralınan: yok
- **[DB-26]** ⚠️ `CREATE INDEX CONCURRENTLY` YAZMA — Prisma migration'ı tek transaction içinde koşar, orada çalışmaz · zorlama: insan:migration SQL'i hiçbir lint kapsamında değil · kanıt: repoda 0 gerçek kullanım; gerekçe yorumları `20260829140000_denetim_sema_kisitlari/migration.sql:64-65` ve `20260830090000_items_ad_kod_seddi/migration.sql:14-15` · devralınan: yok
- **[DB-27]** İfade index'inin fonksiyonu `IMMUTABLE` (+`STRICT`) beyan edilir ve karakter sınıfı AÇIK yazılır (`\s` locale bağımlıdır — kök `CLAUDE.md` yasak listesi) · zorlama: insan:aynı gerekçe — migration SQL'i lint dışında · kanıt: `20260819060000_search_fold/migration.sql:89` (`LANGUAGE sql IMMUTABLE STRICT PARALLEL SAFE`) ve `:101` (`'[ \t\n\r\f\v]+'`) · devralınan: yok
- **[DB-28]** Uygulama TEK komuttur: `npx tsx scripts/apply-migration.ts <ad> [--apply]` (varsayılan dry-run; git add → SQL → `resolve` → drift + hijyen doğrulaması); UYGULANMIŞ dosyaya bir daha dokunulmaz, düzeltme yeni migration ile gelir · zorlama: bekçi:`scripts/test_schema_drift.ts` + `scripts/test_migration_hygiene.ts` (script ikisini kendisi koşar) · kanıt: `scripts/apply-migration.ts:1-23` (2026-08-13 vakası: `resolve` SQL'e HİÇ bakmaz) · devralınan: yok
- **[DB-29a]** Şema provası EN ESKİ CANLI DUMP üzerinde koşulur: migration o verinin üstünde UYGULANIYOR MU · zorlama: insan:prova bir ops adımıdır, repoda izi yoktur · kanıt: sed/unique/CHECK gerçek veriye takılabilir — `20260821150000_name_fold_unique_live` tam bu yüzden yumuşak kapıya çevrildi (mükerrer varken index DÜŞÜYORDU) · devralınan: yok
- **[DB-29d]** Prova `migrate deploy` ile BİTMEZ: `seed:fixtures` + bekçiler de o veri üstünde koşar — fixture ADLARI canlı kayıtlarla `nameFold` seddine çarpar · zorlama: insan · kanıt: ölçüm 2026-09-06 — fixture "Moda Tekstil" fabrikanın gerçek "MODA TEKSTİL" kaydıyla çarptı (`FABRIKA-KOPYASI.md` §2) · devralınan: yok
- **[DB-29b]** Süre ölçümü yalnız YÜKSEK HACİMLİ tabloya DDL yazan migration için gerekir ve ölçtüğü şey KURULUM PENCERESİNİN uzamasıdır, saha kesintisi değil · zorlama: insan:tablonun "yüksek hacimli" olması veriye bağlıdır, metinden okunmaz · kanıt: `deploy/kur.ps1` — migration'lar **[4/9] pm2 durdurulduktan SONRA**, [7/9]'da koşar; eşzamanlı yazan olmadığı için `CREATE INDEX`in aldığı kilit kimseyi bloklamaz. Pencere zaten tam bir `pg_dump` yedeği + doğrulaması içerir (`[3/9]`). **ÖLÇÜLDÜ 2026-09-06:** fabrika yedeğinde 6 migration <1 sn, pencere uzaması yok (`docs/history/test-ortami-2026-09-06/FABRIKA-KOPYASI.md` §1) · devralınan: yok
- **[DB-39]** Uygulanmış bir migration dosyası **hiç değişmez — yorumu bile**: `_prisma_migrations` checksum tutar ve tek karakterlik fark `migrate deploy`u "migration modified after it was applied" ile KİLİTLER · zorlama: bekçi:`scripts/check-migrations.mjs` GATE 2 · kanıt: 2026-09-05'te bu kural bir yorum düzeltmesini yakaladı — açıklama migration'a değil BU dosyaya yazılır · devralınan: yok
- **[DB-29c]** `migrate deploy` GERİ ALINAMAZ EŞİKTİR ve uygulama o anda DURMUŞTUR: bu noktada düşen bir migration fabrikayı kapalı bırakır · zorlama: insan · kanıt: `deploy/kur.ps1:48` ve `[7/9]` başlığı; Prisma şema motoru vakası (2026-09-04) tam burada çıkacaktı

## 8 · Şema-dışı nesne envanteri — DOKUZ liste, iki yönlü

- **[DB-30]** Şema-dışı nesne kuran migration, aynı commit'te `scripts/test_db_invariants.ts` envanterine satırını yazar · zorlama: bekçi:`scripts/test_db_invariants.ts` (İKİ YÖNLÜ — envanterde olup DB'de olmayan da, DB'de olup envanterde olmayan da kırmızı) · kanıt: dokuz liste ve satırları (2026-09-05'te yeniden ölçüldü) — `PARTIAL_INDEXES:100` · `CHECK_CONSTRAINTS:322` · `DEFERRABLE_FKS:449` · `EXT_STATS:462` · `EXPRESSION_UNIQUES:474` · `TRIGGERS:506` · `REQUIRED_EXTENSIONS:549` · `EXPECTED_FUNCTIONS:559` · `EXPECTED_COLLATIONS:581`; küme boyları bekçinin kendi özet satırından okunur (`test_db_invariants.ts:594`) · devralınan: yok

Kırmızı bir envanter kontrolü "bu nesneyi kim koydu ve neden" sorusudur; cevabı nesneyi silmek değil satırı yazmaktır (`Teks-Erp/CLAUDE.md` DB kuralı 4). ⚠️ Kanonik sayı **bekçinin kendisidir**: envanteri "beş liste" diye anan cümleler bayattır.
