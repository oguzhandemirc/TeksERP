# Kod yazım standardı — BEKÇİ: güvence ve yazım (§4–§5)

[`TEST-VE-DERLEME.md`](TEST-VE-DERLEME.md)'nin §4 ve §5'i; 2026-09-13'te oradan
**bölünerek** geldi (o dosya tavana 1,5 KB kalmıştı ve haftanın en çok yazılan standart
dosyasıydı — 7 günde 12 commit; tavan yükseltilmedi).

⚠️ **Bölüm numaraları KORUNDU** — `TEST-VE-DERLEME.md §4` / `§5` diye işaret eden
çapalar hedefini bulmalıdır. `[TD-xx]` kimlikleri de değişmedi; kimlik dosya değiştirmez.

**Çizgi:** burada bir bekçinin **GÜVENCESİ** (yeşilin ne kanıtladığı, körlük zemini,
negatif sonda, kapsam ölçülmez) ve **YAZIMI** (fixture, temizlik, yarış, ortam
bağımsızlığı, taban/tavan, silme) var. Kadans, commit kapısı, bekçi paketinin mekanik
gerçekleri ve derleme orada kaldı: `docs/standart/TEST-VE-DERLEME.md` §1–§3, §6.
Bilinen sınırlar ve bilerek kırmızı bekçiler
[`TEST-VE-DERLEME-SINIRLAR.md`](TEST-VE-DERLEME-SINIRLAR.md)'de (§7, §8).
Kural biçimi [`README.md`](README.md), katman-üstü ilkeler [`ILKELER.md`](ILKELER.md).
Adım adım reçete: `docs/RECETELER.md` § Yeni bekçi.

## 4 · Yeşil ≠ kapsandı

- **[TD-11]** Sunucu isteyen bekçi bölümünü atlarsa bunu ÇIKTIDA söyler ve koşucu özeti atlanan kontrol sayısını basar · zorlama: bekçi:`run-all-tests.ts` (`⚠️ N atlandı`) · kanıt: ölçüm — 8 dosyada 65 kontrol sessizce atlanıyordu (`test_superadmin` tek başına 21)
- **[TD-11a]** Atlanan kontrol sayısı **ÖZET SATIRINDA** beyan edilir (`=== Sonuç: N geçti, M başarısız, K atlandı ===`); serbest metindeki "atlandı" kelimesi bir sayaç DEĞİLDİR · zorlama: bekçi:`run-all-tests.ts` regex'i `Sonuç:` satırına DEMİRLİ · kanıt: ölçüm 2026-09-06 — serbest regex üç dosyada HAYALET sayı üretiyordu (`test_label_bulk_seed` hiçbir şey atlamadan "3 atlandı", `test_finance_flag_off` gerçek 6 iken "5", `test_label_dirty_sources` gerçek 1 iken "7")
- **[TD-11b]** `TEKSERP_STRICT=1` koşumunda ATLANAN KONTROL exit kodunu DÜŞÜRÜR: koşucu özetteki `⚠️ N atlandı` satırını kırmızıya çevirir ve `process.exit(1)` verir. Strict kapalıyken davranış aynıdır (sayı basılır, exit düşmez) — anahtarın anlamı [TD-10c]'de tanımlıdır ve ikinci bir strict bayrağı açılmaz · zorlama: bekçi:`run-all-tests.ts` (`strictMi()` tek kaynak `lib/http-bekci-kapisi.ts`ten import edilir) · gerekçe: "yeşil = kapsandı" iddiası ancak 0 atlamayla kurulur; `test_module_flag_off` sunucusuz koşumda 18 kontrolü atlayıp 86/0 yeşil görünüyordu, kendi sunucuyla 104/0
- **[TD-12]** Ayrı sunucu isteyen bekçiler kendi portunu bekler: finance 4100 · module_flag_off 4101 · superadmin 4104 · settings_password 4112 · module_profile 4122. Sunucu `PORT=<port> npx tsx src/server.ts` ile ayrı bir süreçte kaldırılır; `pkill` YASAK · zorlama: hook:`bash-guard.mjs` · kanıt: `Teks-Erp/CLAUDE.md`
- **[TD-12a]** Bu beş sunucu **CI'da otomatik başlatılır** (`ci.yml` → sağlık kapısı: kalkmayan sunucu job'ı DÜŞÜRÜR, sessizce devam etmez); yerel koşucuya süreç yönetimi SOKULMAZ ("ikinci Node süreci yasak" + `pkill` yasağı orada geçerli) · zorlama: bekçi:`ci.yml` adımı · kanıt: ölçüm 2026-09-06 — sunucular ayaktayken atlanan kontrol **58 → 20**'ye düştü ve açılan 38 kontrolün içinde **iki gerçek arıza** saklıydı (`test_superadmin`in HTTP bölümü GERİ ALINMIŞ bir davranışı ölçüyordu ve kendi fixture'ıyla çelişen bir yüklem taşıyordu)
- **[TD-13]** Her sayım bekçisi **körlük zemini** taşır: "0 bulgu" ile "hiç bakılmadı" ayrımını kendisi ölçer · zorlama: insan:zeminin GERÇEK bir körlük ölçüsü olup olmadığı anlamla bilinir · kanıt: `test_station_quality_capability` (dosya bazlı ilk yazım kördü, sayım bazlıya çevrildi)

## 5 · Yeni bekçi

- **[TD-14]** Yeni bekçi **negatif sondayla** yazılır: korunan davranış kasten bozulunca KIRMIZI verdiği ölçülür ve kaç kontrol/kaç sonda olduğu commit mesajına yazılır · zorlama: insan:sondanın kendisi mekanik ölçülemez · kanıt: `docs/RECETELER.md` § bekçi
- **[TD-15]** Sondanın kendisi kataloğa girebilecek GERÇEK bir kod/değer kullanmaz — kullanırsa sonda değil, veri üretir · zorlama: insan:sondanın 'gerçek kod kullanıp kullanmadığı' kataloğu bilmeyi ister · kanıt: 2026-08-13 kat kataloğu vakası
- **[TD-16]** Fixture **business-key** ile kurulur, `TEST-`/`TST-` ön ekiyle damgalanır ve **damga ADI da kapsar**: ad üzerinde canlı partial UNIQUE sedleri vardır (`colors_nameFoldColor_key`, `items/customers/subcontractors_nameFold_key`), damgasız ad yarım kalan bir koşumdan sonra ikinci koşumu P2002'ye düşürür · zorlama: insan:fixture'ın adının da damgalı olup olmadığını ölçen bir kapı YOK — `test_master_data_name_dup §10/§10a-d` SEDDİN kendisini ölçer (hangi `nameFold` tablosunda partial UNIQUE var, ısırıyor mu), bekçilerin fixture adlandırmasını değil · kanıt: sed envanteri `Teks-Erp/scripts/test_master_data_name_dup.ts:418-499`, ısırma ispatı `:499-568`; 2026-09-05'te dört bekçi tam bu yüzden kırmızıydı
- **[TD-17]** Bekçi ortamdaki veriye bağımlı olmaz: "herhangi bir kayıt bul" (`findFirst`) üstüne kurulan test temiz CI veritabanında düşer ya da vakumen yeşil kalır — sayım kontrolleri kendi fixture'ını süzer · zorlama: insan:'ortam verisi mi fixture mı' ayrımı sorgunun niyetinde · kanıt: `test_field_address` / `test_fason_visibility` emsali
- **[TD-18]** Temizlik `finally` içinde ve FK sırasına göre yapılır; fason kabulü yapan her temizlik `rollVariance.deleteMany` içerir (RESTRICT FK) · zorlama: insan:FK sırası şemadan türetilebilir ama temizliğin TAMLIĞI türetilemez · kanıt: 2026-08-21 (23 dosya güncellendi)
- **[TD-18a]** GLOBAL AYAR (sistem ayarı · modül bayrağı · numara serisi biçimi/satırı) yazan bekçi geri almayı `finally`de (ya da `.finally(cb)`de) ve BİREBİR yapar — önceki satırı okur, yoksa siler; `.then`/`.catch`de ya da akışın ortasında geri alma düşüşte SIZAR ve suç sonraki bekçiye kalır; biçim satırı yazan bekçi başta var olan satır id'lerini alıp sonda kümede olmayanı siler · zorlama: bekçi:`test_bekci_ayar_geri_alma` (AST; sınıf + ANAHTAR düzeyi, bayrak alanı → DB anahtarı eşlemesi servisten türetilir; 9 kalıcı sonda; hızlı mandal) <sub>(2026-09-24; SIGTERM `finally` koşturmaz — ölçüldü; koşucunun "önce SIGTERM, sonra bekle" değişikliği tek başına çözmez)</sub>
- **[TD-19]** Yarış bekçisi elle açık tutulan bir transaction ile kurulur; henüz `await` edilmemiş gate promise'ine no-op `.catch` konur — yoksa unhandled rejection süreci Sonuç satırı basılmadan öldürür (en sessiz kırmızı) · zorlama: insan:pencerenin gerçekten açıldığı yalnız koşumla görülür · kanıt: `test_payment_allocation §12m`
- **[TD-20]** Kod değişikliği ve bekçisi AYNI commit'te gelir · zorlama: insan:aynı commit kuralı git geçmişinde ölçülür, kodda değil · kanıt: ölçüm — src'ye dokunan commit'lerin %78'i aynı commit'te test de değiştiriyor
- **[TD-26]** Bekçi iş verisini `seed`den değil `seed:fixtures`tan ya da kendi fixture'ından çözer; `npm run seed` yalnız TEMİZ FABRİKA iskeletini kurar (müşteri/renk/ürün YOK) · zorlama: bekçi:`ci.yml` ayrı `seed:fixtures` adımı · kanıt: `.github/workflows/ci.yml` — adım eksikken 73 bekçi "Seed fixture eksik: PATOS" ile düştü ve ~2 hafta görülmedi
- **[TD-27]** Paylaşılan yardımcı/fixture dosyasına `test_` ön eki VERİLMEZ (`fixture-*.ts` kalıbı) — koşucunun süzgeci `^test_.*\.ts$` onu bir bekçi sanır · zorlama: bekçi:`run-all-tests.ts` süzgeci · kanıt: `Teks-Erp/scripts/fixture-test-user.ts`; bugün 5 dosya bu kalıpta
- **[TD-28]** Yeniden deneme YALNIZ altyapı arızasında yapılır (DB bağlantısı); assertion hatası ASLA yeniden denenmez ve "ikinci denemede geçti" satırı yeşil değil BORÇTUR · zorlama: bekçi:`run-all-tests.ts` `looksInfrastructural` deseni + flake listesi · kanıt: koşucu gerekçesi — paket sırayla 458 havuz açar, `connectionTimeoutMillis=5s` ara sıra aşılır
- **[TD-29]** Bir bekçi, ölçtüğü davranış kalktığında AYNI commit'te SİLİNİR ve sebebi commit mesajına yazılır; "ihtimale karşı" bırakılan bekçi bir NİYETTİR · zorlama: insan:davranışın gerçekten kalktığı yalnız anlamla bilinir · kanıt: bugün politika yok — `BEKCI-HARITASI.md` 455 sayarken dizinde 458 var (2026-09-06)
- **[TD-30]** Kapsam (coverage) bu repoda ÖLÇÜLMEZ ve hedef konmaz; güvence ölçüsü negatif sonda ([TD-14]) + körlük zeminidir ([TD-13]) · zorlama: insan:beyan · kanıt: ölçüm 2026-09-06 — üç projede de coverage aracı/konfigürasyonu YOK (grep 0 isabet)
- **[TD-32]** Kayıtlı negatif sonda sayısı bir TABAN taşır ve taban yalnız **YÜKSELİR** · zorlama: bekçi:`test_negatif_sonda_kapsami.ts` (körlük zemini 400 dosya) · kanıt: ölçüm 2026-09-06 — 461 bekçinin yalnız **79'unda** (%17,1) kayıtlı sonda var. ⚠️ Bu bir KAPSAM CIRCIRIDIR, ispat değil: metin yazarak kandırılabilir, ölçtüğü şey gerilemenin durmasıdır. Lint tavanının TERSİ yönde çalışır (orada borç düşer, burada güvence artar)
- **[TD-33]** Ortam bağımlılığı ([TD-17]) bir TAVAN taşır ve tavan yalnız **DÜŞER**: "herhangi bir aktif kayıt" 109 dosya · ham `username: "admin"` 129 dosya · kapısız Prisma-yıkıcı betik 42 · zorlama: bekçi:`test_ortam_bagimliligi_tavani.ts` + `test_script_guards.ts` §6 · kanıt: ölçüm 2026-09-06; bu sınıf aynı gün ÜÇ bekçiyi düşürdü (`test_auto_draft_shipment` · `test_roll_po_line_trace` · `test_sack_customer_gate`)
- **[TD-34]** Artık temizleyen bir betik, sildiği satırın ÖZETİNİ tutan üst kaydı da hesaba katar; aksi hâlde temizlik bir TUTARSIZLIK ÜRETİR · zorlama: bekçi:`test_consistency` §19 · kanıt: ölçüm 2026-09-06 — yalnız fason sevk KALEMLERİ silinince 429 sevk kalemsiz kaldı ve §19 429 drift satırıyla kırmızı verdi
- **[TD-35]** Commit kapısının git'e sorduğu her soru **GERÇEK indekse** sorulur (`GIT_INDEX_FILE=<gitdir>/index`), oluşmakta olan commit'in geçici indeksine değil · zorlama: bekçi:`test_commit_gate_scope.ts` §1+§3 · kanıt: 2026-09-13 arşiv notu (kısmi commit'te başkasının SAHNELENMİŞ dosyası `??` görünüyordu)
- **[TD-36]** Commit kapısı **commit'in kendi dosyalarını** yargılar: taranan küme DARALMAZ (tavan aynı kümeyi ölçmeli, [TD-23]), yalnız verdikt daralır — commit'e giren dosyada hata KIRMIZI, yalnız yabancı dosyada UYARI + çıkış 0 ve **sahip uydurulmaz**; karışıksa KIRMIZI. Lint tavanı da aynı kipte (sayım proje geneli, verdikt commit'e bakar). Arka durak CI'ın mevcut adımlarıdır, yeni kapı değil · zorlama: bekçi:`test_commit_gate_scope.ts` §2+§3 · kanıt: 2026-09-13 arşiv notu. ⚠️ Kip YALNIZ hook yolunda yaşar (`--commit-kapisi`): CI'da hiçbir şey staged değildir, bayrak oraya sızarsa kapı yapısal olarak yeşil olur — sızmadığı ölçülür. ⚠️ `.gitignore` dosyayı git'ten gizler, **linter/derleyiciden gizlemez**
- **[TD-37]** Takipsiz-dosya kapıları (GATE 1/4) commit kapısında yalnız commit **`prisma/migrations`a dokunuyorsa** SERT; dokunmuyorsa uyarı + çıkış 0. Bayraksız çağrı (CI · `npm test`) sert kalır · zorlama: bekçi:`test_commit_gate_scope.ts` §1(④–⑥) · kanıt: 2026-09-13 arşiv notu — koruma kaybolmaz, **doğru kişiye taşınır**. ⚠️ Bu dalın CI'da arka durağı YOKTUR (takipsiz dosya temiz checkout'ta görünmez); bilinçli kabul
- **[TD-39]** EXPLAIN/plan bekçisi hacmini KENDİ kurar; eşik SATIR SAYISINDADIR ve ölçülmeden yazılmaz. `system_logs`: dönüş 240–260 satır ⇒ tohum **600**, bedel **0,12 sn**. ⚠️ Eşiği belirleyen ANALYZE tazeliği DEĞİL SAYFA SAYISIDIR. ⚠️ Negatif sonda "tohumlamayı kapat" değil **"indeksi düşür"**dür (tohum silinince sayfalar hemen geri gelmez) · zorlama: bekçi:`test_audit_depth.ts` §0 · kanıt: 2026-09-13 arşiv notu
- **[TD-41]** Bir alt projenin tip kapısı `package.json > scripts`ten OKUNUR, komut UYDURULMAZ — ve kapının KAPSAMI sayıyla bilinir (`npx tsc --noEmit --listFilesOnly -p <config> | grep -c '\.tsx\?$'`, node_modules/lib.d.ts hariç). Ölçüm 2026-09-13: `Teks-Erp` **486** (`tsconfig.json`) + **1131** (`tsconfig.scripts.json`) · `Electron` **26** + **1407** (`tsconfig.node/web.json`) · `mobil` **394**. ⚠️ Electron'da ÇIPLAK `npx tsc --noEmit` **0 DOSYA** derler (kök config `files: []` + `references`; `-b` yoksa referansa girilmez) ⇒ her zaman RC 0 — *komut yalan söylemez, HİÇ BAKMAZ*; bir oturum bu yüzden üç kırık sayfayla ilerledi. ⚠️ Ve `typecheck` ADI backend'de kapsamın yarısından azını ölçer (`scripts/` + `prisma/` yalnız `typecheck:scripts`te; commit kapısı `typecheck:plain` koşar ⇒ 645 dosya o kadansta ölçülmez, arka durak `npm test`). Kadans DEĞİŞTİRİLMEDİ — 1131 dosyayı commit kadansına eklemek *doğru davranışı pahalı kılmak* olurdu; çözüm kapsamı GÖRÜNÜR kılmaktır · zorlama: bekçi:`test_kapi_kapsami.ts` (config listesi `package.json`dan TÜRETİLİR, elle yazılmaz) · kanıt: 2026-09-13 arşiv notu. ⚠️ Eşik SIFIRDIR, 'makul bir taban' değil: *sıfır* yanlışlanabilir, *'en az 50'* bir tahmindir
- **[TD-40]** Mandal (yalnız düşen tavan) **yalnız İZLENEN dosyaları** sayar: takipsiz taslak tavanı geçici şişirir, şişme mandala kalıcı yazılırsa taslak silinince tavan bir daha düşürülemez. CI'da hiçbir şey takipsiz değildir ⇒ CI'da sıfır fark · zorlama: bekçi:`test_script_guards.ts` §0 · kanıt: 2026-09-13 arşiv notu (`scripts/out/` 8 dosya → §6b 42→50)
- **[TD-38]** Bekçi ön koşulunu ORTAMDAN beklemez KENDİ kurar; ön koşul kontrolü gövdenin ihtiyacıyla AYNI ÇÖZÜNÜRLÜKTE olur (§7 sınıf ⑤) ve eksik ön koşul **atlama sebebi değildir** — `atla()` bir ölçüm kaybıdır · zorlama: bekçi:`test_module_profile.ts` §7a3/§7d0 · kanıt: 2026-09-13 arşiv notu
