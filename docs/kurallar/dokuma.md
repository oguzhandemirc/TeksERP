# Dokuma · Dokuma işi · Doff

> Alan kural dosyası — bu alana dokunmadan ÖNCE okunur. Hikâye, ölçüm ve gerekçe tasarım belgelerinde; burada yalnız bugün geçerli kural. Sınıf: **[ÇEKİRDEK]** her kurulumda aynı · **[PROFİL]** bu fabrikanın seçimi.

> ✅ **ŞEMA DİZİSİ TAMAMLANDI — P1…P3'ün hepsi İNDİ** (ölçüldü 2026-09-14, ağaç `08e20215`; önceki başlık "KISMEN KÂĞITTADIR / Hâlâ YOK" listesi BAYATTI ve üç kalemi de inmişti). `WeavingOrder` (P1, `77b69da9`) · `MachineRun` (P2, `c07396e6`) · `MachineSpec` + `LoomShedType` + `MachineMonitoringState` (P4, `20260913230000`) · `Machine.productionLineCount` (P4b, `20260913235000`) · `MachineStopEvent` + `MachineStopReclass` + `ShiftDefinition` + `ShiftInstance` + `MachineCollector` + `MachineCollectorLink` + 4 enum (P2b-1, `20260913240000`) · `ReasonPresetKind.MACHINE_STOP` + `ReasonPreset.stopLossClass` + CHECK `reason_presets_machine_class_chk` + katalog (P2b-2, `20260913242000` + `20260913243000`) · `DoffEvent` + `RollEntrySource.WEAVING` + `StationKind.WEAVING` (P3, `20260913250000` + `20260913251000` + `20260914010000`) · `Machine.warpBeamSlots` (F4, `20260914090000`) · `MachineShiftStat` + `MachineShiftStatSeal` (karne, `20260914130000`). Sıra: **P1 ✅ → P2 ✅ → P4 ✅ → P4b ✅ → P2b-1 ✅ → P2b-2 ✅ → P3 ✅.**
>
> ⚠️ **KÂĞITTA KALAN ÜÇ ŞEY** (ölçüldü 2026-09-14, hepsi YOKLUKLA doğrulandı): **telemetri kovası** — `MachineInterval` modeli şemada YOK; `unobservedSec` · `gapPicks` · `watchdogSec` · `anomalyAck` kolonları `MachineShiftStat`ta VAR ama Faz 1a/1b'de **beyanla** dolar (`unobservedSec` notu: "0 = BEYAN, gözlemci insan"), artık yöntemi ve budayıcı Faz 2 · ~~**hat kırılımı** — `MachineShiftStat`ta hat boyutu YOK (`productionLineCount` yalnız `Machine`ta), karne hat başına kırılmaz~~ **İNDİ 2026-09-15** (`MachineShiftLineStat` eklemeli çocuk, migration 20260915010000; ebeveyn unique bölünmedi) · **`tezgah.*` davranış bayrakları** — mola POT içi/dışı, mikro duruş eşiği, gece yarısı atfı sabit; `tezgah.enabled` bir MODÜL anahtarıdır (`module-flags.ts:53`), davranış bayrağı değil.
>
> ✅ **P4b İNDİ:** `Machine.productionLineCount` (`@default(1)`, CHECK `>= 1`) + `assertProductionLineValid` yüklemi. ~~⚠️ Yüklem BUGÜN HİÇBİR YERDEN ÇAĞRILMIYOR~~ **KOŞUL SAĞLANDI 2026-09-13 (P2b koşum yüzeyi):** ilk çağrı yeri `openMachineRun` → `helpers/machine-run-open.helper.ts` (`resolveOpenContext`); "çağrıldığı YOL" ayağı `test_production_line §4` + `test_machine_run §7` (negatif sonda: çağrı satırı kaldırılınca 3 kırmızı).
> ✅ **P2b KOŞUM YAZMA YÜZEYİ İNDİ (2026-09-13, 6e):** `POST /api/machine-runs` · `/:id/close` · `/:id/revoke` — izin `loom:run` / `loom:run-revoke`, kapı `requireDokumaEnabled` (2026-09-13 ekran dilimiyle `requireProductionEnabled`ten geçti). Ekran YOK; tablet dilimi `requireAnyPermission("loom:run", ...MOBILE)` biçimine geçirir.
> 📌 Bir **guard** bir YOLU korur (yol yoksa erişilemez dal); bir **yüklem** bir SORUYU cevaplar ve çağrısız da doğru ya da yanlıştır ⇒ yazma yüzeyinden önce inebilir, ama bekçisi **davranışını** ölçmek zorundadır, varlığını değil.

---

## Ortak (backend + panel + tablet)

### Değişmezler

- **[ÇEKİRDEK]** Dokuma topun rotasında bir ADIM DEĞİLDİR; kendi kimliği, kendi yaşam döngüsü ve kendi defteri olan ayrı bir VARLIKTIR — `MachineRun` bir `WorkOrderStep`e bağlanmaz, `WeavingOrder`a bağlanır. <sub>(DOKUMA-IS-EMRI §1)</sub>
- **[ÇEKİRDEK]** `StationKind.WEAVING` yalnız OTURUM istasyonudur: tablet oturumu istasyon tabanlı olduğu için tezgah bir WEAVING istasyonudur (`SESSIONABLE_STATION_KINDS`, oturum izni `mobile:dokuma` — satır yoksa kapı fail-OPEN olur) ve YALNIZ MAKİNEYLE açılır (tezgah = makine; istasyon-modu 400 `STATION_MACHINE_ONLY`); rota şablonuna ve iş emri rotasına GİREMEZ — tek helper `assertStationsRoutable` (400 `STATION_NOT_ROUTABLE`), iki çağıran (`route.service.validateSteps` · `workorder.service.assertRouteRefsActive`). Panel istasyon formu türü yalnız `dokuma.enabled` açıkken çizer (mevcut değer korunur); tabletin `SessionStationKind`i ekran dilimine kadar WEAVING taşımaz (fail-closed, çökme yok); minVersion GEREKMEZ (referans fabrikada WEAVING istasyonu yok, onu yeni panel açar). · bekçi: `test_station_kind_weaving` (§1–§4, dört negatif sonda) + `test_audit_labels §4` (WEAVING çakışması `STATION.kind` override) + Electron `stationKindVisibility.test.ts` <sub>(DOKUMA-IS-EMRI §3.3 · §3.9 J.4; enum reçetesi 13 adım; 1e hükmü 2026-09-14; arşiv:2026-09-14)</sub>
- **[ÇEKİRDEK]** `StationKind.WARPING` (Devere) İSTASYON KARTI TÜRÜDÜR, yetenek değil: devere makinesini motor türden değil `Station.producesWarpBeam`den tanır (`listDevereMachines`, sarım kapısı) — tür yalnız keşfedilebilirlik + formun önerdiği yetenek (WARPING seçilince `producesWarpBeam` işaretlenir; "Diğer + Levent sarar" yolu bozulmaz). WEAVING'in ikizi olarak rota şablonuna ve iş emri rotasına GİREMEZ (`NON_ROUTABLE_STATION_KINDS`, 400 `STATION_NOT_ROUTABLE`); ama OTURUM istasyonu DEĞİLDİR (ölçüldü: tabletin Devere ekranı gezici, `SESSION_SCREEN_KEYS` dışında — ekransız türe oturum açtırmak fail-open olurdu). Panel formu türü yalnız `devere.enabled` açıkken çizer (mevcut değer korunur), etiket `stationKindLabel` fail-soft (eski panel ham adı basar); mobil `StationKind` union'ı aynalı, `SessionStationKind` DEĞİL; minVersion GEREKMEZ. · bekçi: `test_station_kind_warping` (§1–§4, üç negatif sonda) + `stationKindVisibility.test` + `visibleStationKinds.test` + `stationKindDefaults.test` <sub>(kullanıcı bulgusu 2026-09-18 02:35, 1e kararı, 6e)</sub>
- **[ÇEKİRDEK]** TABLET ÖN-DOLGU YALNIZ ÖNERİDİR (Z5, kullanıcı ilkesi 2026-09-18 "daha kolay yol"): sunucu bildiği kimliği/değeri bağlama koyar, istemci ön-doldurur, kilitlemez; hiçbir öneri stok/defter yazmaz, hiçbir alan zorunlu olmaz, bayrak yok, eski istemci okumayınca bugünkü davranış. Kaynaklar: doff satırı koşum → iş zinciri (`item`/`color`/`weavingOrder`, koşumun deseni işin kumaşını ezer), koşum açılışı aynı desenin SON KAPANMIŞ koşumu (önce bu tezgah; `sourceMachineId` söyler) + `MachineSpec.nominalUnitsPerMin`, devere bağlamı çözgü kartı başına son IN_HOUSE sarım + `autoConsume` aynası, kumaş kartı varsayılan çözgü kartı (`Item.warpSpecId`, yalnız FABRIC + aktif kart; dokuma işi boş gönderirse sunucu uygular, `null` = kartsız). Veri yoksa alan null — UYDURULMAZ. · bekçi: `test_tablet_context_prefill` (§1–§4, dört sonda) + `itemPayload.helper.test` <sub>(6e, 1e Z5)</sub>
- **[ÇEKİRDEK]** Tezgah LİSTELERİ backend'de doğar, ilk dilim yerel durumla YAPILMAZ: `GET /api/machine-runs?machineId&open=true` (açık koşumlar) · `GET /api/machine-doffs?machineId[&date]` (fabrika gününün indirmeleri, geri alınmış kapsam dışı) · `GET /api/machine-doffs?unlinked=true[&machineId][&sinceDays≤30]` (hiç top doğurmamış, geri alınmamış — KK1'in açık liste seçimi; makine OPSİYONEL, masa KK1 her tezgahı görür); üçü de `requireDokumaEnabled` altında ve `requireAnyPermission(loom:run · loom:doff · mobile:dokuma)` ile (ayrı `loom:read` YOK — liste yazma yüzeyinin yüzüdür); süzme SUNUCUDA, liste 200'de kırpılır `meta.total` kırpılmaz. · bekçi: `test_loom_lists` (§1–§5, dört negatif sonda) <sub>(DOKUMA-IS-EMRI §3.9 J.2 "Backend ÖNCE"; 1e 2026-09-14)</sub>
- **[ÇEKİRDEK]** Bir top ya fabrikanın DIŞINDAN gelir ya iş emri içinde BAŞKA BİR TOPTAN doğar (ana top tüketilir, metre korunur); dokuma bu ikisine ait olmayan ÜÇÜNCÜ sınıftır — iplikten metre YARATIR ve korunacak ana topu yoktur. <sub>(DOKUMA-IS-EMRI §1.1)</sub>
- **[ÇEKİRDEK]** Doff bir olay yazar, TOP DOĞURMAZ: dokunan kumaş `Roll` olarak KK1'de doğar (`entrySource = WEAVING`) ve oradan mevcut rotaya girer — ikinci bir giriş motoru yazılmaz, yol `createInitialEntry`den geçer. · <sub>(DOKUMA-IS-EMRI §3.5; `goods-receipt.service.ts:8`)</sub>
- **[ÇEKİRDEK]** Tezgahtan türetilen metre (`unitDelta ÷ unitsPerCm`) HİÇBİR YERDE stok yazmaz; tek miktar gerçeği `Roll` ölçümüdür. <sub>(DOKUMA-TEZGAH §5.6)</sub>
- **[ÇEKİRDEK]** İlerleme İKİ SAYIDIR ve toplanmaz: ÜRETİLEN (doff edilen topların ölçülen metresi — stok yazar, rapora girer) ve TEZGAHTA (türetilen — yalnız canlı ekran); `plannedM − ÜRETİLEN` kalanı verir, TEZGAHTA bu çıkarmaya girmez. <sub>(DOKUMA-IS-EMRI §2.4)</sub>
- **[ÇEKİRDEK]** Levent bitince koşum BİTMEZ — koşumu bitiren üçtür: desen/renk değişti · hedef devir değişti · iş bitti; levent değişimi `WarpBeamEvent`e yazılır ve koşum sürer. <sub>(DOKUMA-IS-EMRI §2.3)</sub>
- **[ÇEKİRDEK]** Talep metre, icra levent, çıktı toptur — üç ayrı eksen tek sayıya bindirilmez; metre↔levent çevrimi take-up ister ve take-up bugün YOKTUR, yani "bu iş kaç levent eder" HESAPLANMAZ. <sub>(DOKUMA-IS-EMRI §2.1)</sub>
- **[ÇEKİRDEK]** `MachineDataSource` alanları `@default` ALMAZ — sayaç/ölçüm değerini yazan her yol kaynağını AÇIKÇA beyan eder, `SIMULATED` dahil; kararı backend verir. <sub>(DOKUMA-IS-EMRI §3.8)</sub>
- **[ÇEKİRDEK]** Açık koşum tekilliği MAKİNE değil **ÜRETİM HATTI** başınadır (`machine_runs_one_open_per_prod_line_uq`) — çift enli tezgah yan yana iki ayrı kumaş koşar; yalnız makineye kilitlemek bunu yapısal olarak imkânsız kılar. Tek hatlı makinede `productionLineNo` sabit 1'dir ⇒ reddedilen küme değişmez. <sub>(DOKUMA-TEZGAH §2.8, §10/#23b)</sub>
- **[ÇEKİRDEK]** İzleme hâli TEK kolondur (`MachineSpec.monitoringState`, `OFF` doğar) — `isMonitored` gibi ikinci bir boolean AÇILMAZ: iki kolon "çift yüklem" sınıfıdır ve tek boolean, kanal kabul testinden bağımsız açılabildiği için ters bağlı bir röleyi %100 randımanla MÜHÜRLETİRDİ. Gölge mod bir reçete cümlesi değil bir DURUMDUR. <sub>(DOKUMA-TEZGAH §2.3)</sub>
- **[ÇEKİRDEK]** `MachineSpec` satırının VARLIĞI *"bu makine izleniyor"* demektir, *"bu makine bir dokuma tezgahıdır"* DEĞİL; ne olduğu ayrı bir etiketten okunur ve o etiket zorunlu bile değildir (`shedType IS NULL` zaten "bu makine o sınıftan değil" der). `Machine.kind` enum'u AÇILMAZ. <sub>(DOKUMA-TEZGAH §2.3)</sub>
- **[ÇEKİRDEK]** Duruşun kimliği SAAT DEĞİL TOKEN'DIR (`stopKey`, ajan üretimi UUID): açılış, kapanış ve yeniden gönderim AYNI anahtarla gelir. Kimliği `(makine, başlangıç, kaynak)` üçlüsünden kurup saniyeye yuvarlamak bir kimlik çözümü değil semptom bastırıcısıdır — 1 sn kayan tekrar gönderim yine ikinci satır açar. <sub>(DOKUMA-TEZGAH §2.7)</sub>
- **[ÇEKİRDEK]** `machine_stop_events`in iki seddi de (`one_open_per_machine` · `key_uq`) `revokedAt IS NULL` yüklemi taşır — geri alınmış duruş yer işgal etmez, yoksa yeni duruş açılamaz ve yeniden gönderim sonsuza dek reddedilir. <sub>(DOKUMA-TEZGAH §2.7)</sub>
- **[ÇEKİRDEK]** Duruşun BEŞ yazma yolu (aç · kapa · sınıfla · yeniden sınıfla · geri al) TEK serviste yaşar (`machine-stop.service`), her biri atomik claim + count-0 tanısıdır ve ikinci yazar doğmaz: ilk sınıflandırma `reasonCode IS NULL` claim'iyle YERİNDE yapılır ve kayıp sınıfı katalogdan KOPYALANIP DONAR; değer→değer değişimi kayıtlı KARARI değiştirir ve değişimin kendisi AYNI tx'te `MachineStopReclass` (from→to) satırıdır — ters yolu karşı kayıttır (aynı fonksiyon to→from), silme ya da damga değil; duruşun olguları (`startedAt` · `endedAt` · `durationSec` · `stopKey`) hiçbir yolda değişmez; geri alma damgadır ve seddi boşaltır. · bekçi: `test_machine_stop_manual.ts` (§4 claim · §5 defter + karşı kayıt · §6 kopya donmuş · §7 damga) · `test_defter_ters_yol.ts` §5 <sub>(arşiv:2026-09-14 Faz 1b; DOKUMA-TEZGAH §2.7)</sub>
- **[ÇEKİRDEK]** Mühür sınırı TEK kapıdan geçer: duruşu açan, kapatan, sınıflandıran, yeniden sınıflandıran ve geri alan her yol `assertStopShiftWritableTx(tx, { shiftInstanceId, machineId })`ten geçer (`helpers/machine-stop-context.helper.ts`) — iptal edilmiş vardiya 409 `SHIFT_CANCELLED`; mühür MAKİNE×VARDİYA karnesinin durumudur (`MachineShiftStat.sealState`, `findUnique({ machineId_shiftInstanceId })`, satır yoksa OPEN) ve `SEALED` → 409 `SHIFT_SEALED` (yol: `loom:shift-unseal` → satır → RESEAL), çağıranlar değişmedi; vardiya iptali ≠ mühür, ikisi de burada sorulur. **KAPANDI 2026-09-14 (Dilim 3):** eski "Kapanır: sealState şemaya inince…" borcu kapandı — bekçi `test_machine_shift_seal §4` beş yolda 409 `SHIFT_SEALED` (negatif sonda: kapıdan `sealState` okuması düşürülünce §4a–§4e ❌) ve §5h unseal→reseal sonrası kırılım YENİ kuşakta. · bekçi: `test_machine_stop_manual.ts` §8 (iptal ayağı) · `test_machine_shift_seal` §4 (mühür ayağı) <sub>(DOKUMA-TEZGAH §2.7 mühür kutusu, §2.10; Dilim 3)</sub>
- **[ÇEKİRDEK]** İnsan kararlı duruş satırı HİÇBİR yoldan silinemez — DB seddi `machine_stop_events_block_classified_delete` (BEFORE DELETE, `classifiedById` dolu ∨ `reasonSource ∈ {OPERATOR,SUPERVISOR}` → RAISE); makine sınıflı satır (TELEMETRİ) bu sedden geçer ve kovayla budanır. Geri alma `revokedAt` damgasıdır. Bekçi fikstürü insan kararlı satırı silmeden ÖNCE kararı null'lar (üretim yolu değil). · bekçi: `test_machine_stop_manual.ts` §11 · `test_db_invariants.ts` (trigger + fonksiyon gövdesi) <sub>(DOKUMA-TEZGAH §2.7/§4 sed ④; migration 20260914091000)</sub>
- **[ÇEKİRDEK]** `Machine.warpBeamSlots` GİRDİ eksenidir (`productionLineCount` ÇIKTI; ikisi bağımsız — raşel N levent tek kumaş, çift enli tezgah 1–2 levent iki kumaş): duruş `beamSlot`u yalnız `warpBeamSlots > 1` makinede ve `1..warpBeamSlots` aralığında yazılır (`assertBeamSlotValid`, 400); `<= 1` makinede alan çizilmez; NULL her zaman serbest ("atanmamış" kovası tahminle doldurulmaz). CHECK `>= 0` (cağlıktan beslenen çözgü makinesinde yuva 0 — karşı örnek var), varsayılan 1 = bugünkü davranış. · bekçi: `test_machine_stop_manual.ts` §12 · `test_db_invariants.ts` <sub>(DOKUMA-TEZGAH §2.7, §10/#23; migration 20260914090000)</sub>
- **[ÇEKİRDEK]** Duruş damgası iki sözleşmedir ve ayrım `source`tan okunur (`resolveStopStamp`, tek yer): OPERATÖR (tablet) basılan anı gönderir → makul aralık (36 sa/5 dk) dışı SUNUCU saatine düşer + `warnings` (bozuk RTC üretimi durdurmaz); AMİR (`loom:manual-entry`, panel) bir ZAMAN BEYAN EDER → aralık (7 gün geri / 5 dk ileri, `STOP_DECLARED_MAX_PAST_MS`) dışı **400 `STOP_STAMP_OUT_OF_RANGE`**, beyan sessizce değiştirilmez, deftere uydurulmuş an yazılmaz; asıl geçmiş sınırı mühür kapısıdır, 7 gün yalnız yazım hatası seddi. · bekçi: `test_machine_stop_manual.ts §13 (§13a 8 gün geride · §13b 10 dk ileride · §13e kapanış beyanı → 400)`
- **[ÇEKİRDEK]** `MachineStopLossClass.MINOR` bir SÜRE sınıfıdır, SEBEP sınıfı DEĞİL: yalnız mikro-duruş eşiğinin altından TÜRER ve `ReasonPreset.stopLossClass`a ASLA yazılmaz. Denetim ekranında da ayrı Türkçe taşır (`MACHINE_STOP_EVENT.lossClass` override'ı) — global "Küçük" `DefectSeverity` dilindedir. <sub>(DOKUMA-TEZGAH §2.7, §10/#7)</sub>
- **[ÇEKİRDEK]** Bir sırrı taşıyan kolon, onu koruyan KURALLARLA aynı dilimde iner; tablo daha erken inebilir, sır inemez. `MachineCollector`ın token kolonları bu yüzden P2b-1'de YOK ve yokluğu şemada BEYAN EDİLİR — yazma yüzeyi olmadan güvenli görünen bir sır kolonu, kurallarından önce var olur ve ilk yazan kişi kuralları arayıp bulamaz. <sub>(1e hükmü 2026-09-13)</sub>
- **[ÇEKİRDEK]** `machine_runs`ın iki sedi `revokedAt IS NULL` yüklemini ŞART koşar (geri alınmış koşum yer işgal etmez), silme guard'ı ise `revokedAt`i BİLEREK SÜZMEZ (geri alınmış koşum da o makinede üretim yapıldığının kanıtıdır) — **iki ters yön, iki ayrı soru**; gerekçeleri `guarded-hard-remove.ts` → `machineRunCount`ta yan yana yazılıdır ve biri ötekine bakılarak "tutarlı" yapılmaz. <sub>(DOKUMA-TEZGAH §2.8)</sub>
- **[ÇEKİRDEK]** Koşum açılışında iki 409 kodu AYRI üretilir ve ayrım ölçümdür: precheck açık koşumu görürse `PRODUCTION_LINE_OCCUPIED` (kim koşuyor, `openRunId`), precheck geçip sed yakalarsa `MACHINE_RUN_RACE` — ikincisi yalnız TOCTOU penceresi gerçekten açılıp sedde kapandığında doğar; advisory kilit YOKTUR ve eklenmez. · bekçi: `test_machine_run §5` (gate-tx + `pg_blocking_pids`; pencere kurulamazsa "ölçülmedi" kırmızıdır) <sub>(`machine-run.service.ts`; `work_sessions` `MACHINE_OCCUPIED`/`SESSION_RACE` emsali)</sub>
- **[ÇEKİRDEK]** Koşum kapanışının TEK yazarı `closeMachineRunTx`tir; claim `WHERE closedTermsAt IS NULL ∧ startedAt ≤ endedAt`, count-0 tanısı tx içinde taze okunur (`RUN_ALREADY_CLOSED` · `RUN_REVOKED` · `RUN_END_BEFORE_START`). Duruş terimleri (`stopSecAtClose`/`stopCountAtClose`) bu dilimde NULL kalır — "ölçülmedi", 0 değil; ingest dilimi bu yazara ekler, ikinci yazar açmaz. · bekçi: `test_machine_run §9` <sub>(DOKUMA-TEZGAH §4 "kapanışta donar")</sub>
- **[ÇEKİRDEK]** Koşum geri alma bir DAMGADIR (`revokedAt`+`revokedById`+`revokeReason`, claim `WHERE revokedAt IS NULL`); açık ya da kapanmış koşum geri alınabilir, ileri satır (başlangıç · bitiş · donmuş terimler) DEĞİŞMEZ, ikinci geri alma sebebi EZMEZ (409 `RUN_ALREADY_REVOKED`). Ayrı izin `loom:run-revoke` (randımanın paydasını değiştirir). · bekçi: `test_machine_run §10` <sub>(kök `CLAUDE.md` § ters kayıt; `shipping:undo-dispatch` emsali)</sub>
- **[ÇEKİRDEK]** Koşum `clientToken` replay'i dört durumludur ve `assertMachineRunReplayAlive` `token-replay.helper`da yaşar: aynı yük → özgün koşum, başka yük → `CLIENT_TOKEN_COLLISION`, geri alınmış → `RUN_REVOKED` (canlıymış gibi dönmez). · bekçi: `test_machine_run §11` <sub>(`token-replay.helper.ts` başlığı)</sub>
- **[ÇEKİRDEK]** Dokuma işine bağlı koşum açılışı iş satırını `updateMany` claim'iyle KİLİTLER (`status ∈ {PLANNED, IN_PROGRESS} ∧ IN_HOUSE`, tx'in ilk ifadesi): eşzamanlı iptal/kapanış claim'i ya önce biter (409 `WEAVING_ORDER_NOT_OPEN`) ya bizim commit'imizi bekler ve açık koşumu görür. ⚠️ Karşı taraf SÖZLEŞMESİ: iş emri kapatma/iptal yolu koşum saymadan ÖNCE kendi claim'ini almalı, yoksa iki tx birbirini görmez. Fasona verilmiş işe koşum 400 `WEAVING_ORDER_SUBCONTRACTED`. · bekçi: `test_machine_run §12` (iptalle yarış gate-tx ile kurulur) <sub>(`touchWorkOrderTx` emsali)</sub>
- **[ÇEKİRDEK]** `startedAt`/`endedAt` istemci BEYANIDIR (`resolveEntryStamp` penceresi: ≤36 sa geçmiş, ≤5 dk gelecek); aralık dışı beyan 400 vermez, sunucu saatine düşer ve `ApiResponse.warnings` ile söylenir — bozuk RTC'li tablet üretimi durdurmaz. Aynı hatta önceki koşumun bitişinden ÖNCE başlayan beyan 409 `PRODUCTION_LINE_OVERLAP` (uygulama kontrolü, exclusion seddi yok). <sub>(`duplicate-guard.helper.ts` "NEDEN 400 DEĞİL")</sub>

- **[ÇEKİRDEK]** LEVENT (devere Faz 1b) kendi VARLIĞI ve kendi DEFTERİDİR: durum `WarpBeam` (PLANNED → READY → CANCELLED; F1: READY ⇄ SHIPPED_OUT fason çevrimi, terminal DEĞİL; Faz 3: READY ⇄ MOUNTED, terminal EXHAUSTED · SCRAPPED — aşağıdaki tezgah bağı kuralı), defter `WarpBeamEvent` append-only (`updatedAt` yok; türler WOUND · WOUND_CANCEL · F1 fason dörtlüsü SHIP_OUT · SHIP_OUT_CANCEL · RETURNED_IN · RETURNED_IN_CANCEL, `WARP_BEAM_EVENT_KINDS` ↔ `warp_beam_events_kind_ck` iki yönlü; fason satırı sevk kalemine bağlı, `fason_item_ck` iki yönlü); ters yol tipli `WOUND_CANCEL` + `reversesEventId @unique` (çift iptal DB'de imkânsız), durum CANCELLED terminaldir — PLANNED'a DÖNMEZ (`one_wound_uq`: bir levent bir kez doğar, iptal edilen yeniden sarılmaz, yeni levent açılır). TEK YAZAR `helpers/warp-beam-event.helper` `applyWarpBeamEventTx` (ilk ifade devere kapısı; sarım ve fason servisleri onu çağırır, fason servisi iplik defterine ulaşmaz — claim `updateMany WHERE status=from`; plan/liste/taslak `warp-beam.service`). Kalan metre KOLON DEĞİL (Σ `warpBeamLengthSign` × lengthM). `beamNo` LV+GGAAYY+NNNN, 8029 kod-kapsam kilidi — yeni advisory uzayı YOK. · bekçi: `test_warp_beam_lifecycle` (§0 statik · §4 claim · §5 net ters/çift iptal · §9 paralel) + `test_db_invariants` (10 nesne) + `test_defter_ters_yol` beyanı <sub>(DEVERE-LEVENT-TARAMASI §4.3/§4.4/§4.6/§4.7; 1e hükmü 2026-09-14, 0c)</sub>
- **[ÇEKİRDEK]** Levent KÖKENİ aksiyon anında SEÇİLİR, kurulumda değil (üç köken tek tabloda): `IN_HOUSE` (taraf yok, `WOUND.machineId` devere istasyonunda ZORUNLU — `Station.producesWarpBeam`) · `SUBCONTRACT` (yalnız fasoncu) · `PURCHASED` (tedarikçi XOR fasoncu, TAM BİRİ). XOR'un TEK KAPISI `resolveOriginParty` (Türkçe mesaj, 400 `WARP_BEAM_ORIGIN_PARTY`), DB CHECK `warp_beams_origin_party_ck` İKİNCİ HAT. Fason/hazır alım kökeninde iplik satırı YAZILMAZ (iplik tüketimi bizim defterde değil, §3.9) — yazılırsa 400. · bekçi: `test_warp_beam_lifecycle §2/§7/§8` <sub>(§3.9 · 9.7c)</sub>
- **[ÇEKİRDEK]** DEVERE modülü İPLİK/TİCARETE BAĞLI DEĞİLDİR (`MODULE_DEPENDENCIES.devereEnabled` YOK; `requireDevereEnabled` yalnız devere okur): hazır alınan / fasona sardırılan levent iplik tüketmez, "iplik KAPALI + devere AÇIK" kurulumu meşrudur. İplik kapısı AKSİYON ANINDA koşar — içeride sarım (`WARP_ISSUE` yazan tek yol) `applyYarnMovementTx`in içindeki kapıyla iplik kapalıysa 403 `MODULE_DISABLED`(iplik) alır, fason/hazır alım sarımı iplik defterine dokunmaz. Panel/rol ekranı bağımlılık okunda devereyi iplike BAĞLAMAZ. · bekçi: `test_devere_regime_gate §2/§4` + `test_iplik_regime_gate §2 (muaf) / §4a (beyan)` + `test_warp_beam_lifecycle §10` <sub>(DEVERE-LEVENT §5 · §9.7d; 1e K3 hükmü 2026-09-14 — eski `devere → iplik` bağımlılığı GEÇERSİZ → 2026-09-14)</sub>
- **[ÇEKİRDEK]** ÜRETİM BELGE ZİNCİRİ (Z1, 2026-09-18): üç belge kalır (`WarpBeam` · `WeavingOrder` · `WorkOrder`), aralarındaki üç bağ OPSİYONEL doğar ve varsayılanı bugünkü davranıştır — Y1 dokuma işi ↔ sipariş satırı PİVOT (`WeavingOrderToOrderLine`, ③b saf yapılandırma: gövde `orderLines` REPLACE, audit ebeveyn eylemde) · Y2 `WarpBeam.weavingOrderId` (yalnız açık + IN_HOUSE iş, 400 `WEAVING_ORDER_NOT_LINKABLE`; çözgü kartı farkı UYARI `[WARP_SPEC_MISMATCH]`, red değil) · Y3 KOLON YOK (top → `doffEventId → MachineRun.weavingOrderId`, tek okuyucu `weavingOrderOfRoll`). Zorunluluk yalnız üç davranış bayrağıyla (`devere.beamWeavingLinkRequired` sarımda · `dokuma.runWeavingOrderRequired` koşumda · `dokuma.orderLineLinkRequired` işte; varsayılan KAPALI; etkin değer §3.6 resolver, modül kapalıyken kapı koşmaz), kapılar TEK helper'da (`helpers/production-chain-gates.helper.ts`). Koşum açarken sunucu iş UYDURMAZ: `GET /api/machine-runs/tablet-context` takılı leventin işini ÖN-DOLGU olarak verir, operatör onaylar (+0 dokunuş). · bekçi: `test_uretim_belge_zinciri` (§0–§6, iki sonda) <sub>(tasarım: 5e’nin URETIM-BELGE-ZINCIRI tasarım belgesi (dal 5e32-belge-zinciri, ayrı iner) §3.1/§4/§7; arşiv: 2026-09-18 Z1)</sub>
- **[ÇEKİRDEK]** İPLİK LOTU (devere Faz 2, A1): `YarnLot` DURUM kaydıdır (`[itemId, lotNo]` tekil; `lotNo` irsaliye metni — TRIM, AYRIŞTIRILMAZ, normalize YOK; tedarikçi NULL olabilir), bakiyesi AYRI TABLOYA İNMEZ — `Σ yarnMovementSign × qtyKg WHERE lotId` ile TÜRETİLİR (`yarnLotBalanceTx`), `YarnStock` tek yazar kuralı değişmez. Lot ZORUNLU DEĞİL: lotsuz/karışık satır `ApiResponse.warnings` üretir, reddedilmez; **lot etiketli çıkış lot × depo bakiyesini AŞAMAZ (409 `YARN_LOT_BALANCE_EXCEEDED`, eksi-bakiye bayrağından BAĞIMSIZ — lot bir beyandır)**; lot kalemi hareket kalemiyle aynı olmalı (400, mutabakat §40 ikinci hat); ters yollar (WOUND_CANCEL · mal kabul iptali) lotu KOPYALAR (net lot bazında). Lot doğuşu tek kapı `ensureYarnLotTx` (mal kabul satırı `lotNo` + elle `POST /yarn/lots`; 8029 `yarnLot` kilidi ilk ifade). `devere.lotRequired` [PROFİL] DEFAULT false = bugünkü davranış (satır yok ↔ false BİREBİR ölçüldü); açıkken içeride sarım ve mal kabul iplik satırı lotsuz 400/düşer. İzin yeni kod yok (`yarn:write` · `warehouse:read`). · bekçi: `test_yarn_lot` (§0–§9, dört sonda) + `test_consistency §40/§41` + `test_master_data_merge_fk_coverage` (YarnLot CONFLICT/MOVE) <sub>(DEVERE-LEVENT §3.5/§4/§7 Faz 2; 1e hükmü L1–L5 2026-09-14, 6e)</sub>
- **[ÇEKİRDEK]** İPLİK LOTU KALİTE BEKLETME / KARANTİNA (2026-09-18, 01): `YarnLot.qualityStatus` (`RELEASED` varsayılan · `ON_HOLD` · `BLOCKED`) + karar damgası (`qualityDecidedAt/ById/Note`) lotun DURUMUDUR, defter satırı değil. [PROFİL] `goodsReceipt.yarnQualityHoldEnabled` (varsayılan KAPALI = bugünkü davranış; satır yok ↔ false BİREBİR ölçüldü; etkin değer `resolveYarnQualityHoldEnabled` = ticaret ∧ iplik ∧ bayrak) AÇIKKEN mal kabulde doğan lot (`ensureYarnLotTx` `source: "RECEIPT"`, yalnız `created`) ON_HOLD doğar — elle açılan lot (`source: "MANUAL"`) serbest; bekletmedeki/bloke lottan ÇIKIŞ yazılamaz: kapı `assertLotQualityReleasedTx` TEK YAZARIN (`applyYarnMovementTx`) içinde, kimlik denetiminden SONRA, yalnız `YARN_LOT_QUALITY_GATED_KINDS` = {OUT · WARP_ISSUE · SUBCONTRACT_OUT} için (giriş · iade · storno · sayım düzeltmesi serbest) → 400 `YARN_LOT_ON_HOLD` (`details.lotId/lotNo/qualityStatus`); bayrak kapalıyken tek sorgu bile koşmaz. Karar `PATCH /api/yarn/lots/:id/quality {status, note?}` izin `quality:write` (yeni kod yok), bayraktan BAĞIMSIZ yazılır (claim `updateMany WHERE {id, isActive}`; pasif lot 400), audit UPDATE `YARN_LOT` `changes[qualityStatus]`; liste `qualityStatus=` CSV (`readFilterList` + Zod nativeEnum, tanınmayan 400); tablet bağlamı `yarnLots[].qualityStatus` + `yarnQualityHold` (rozet kararı sunucudan, kapı sunucuda); mobil `YarnLotQualityStatus` aynası. · bekçi: `test_yarn_lot_quality` (§0–§4, dört sonda) + `test_feature_flag_contract` + `test_module_flags` §8b/§8c + `test_mobil_enum_aynasi`
- **[ÇEKİRDEK]** LEVENT TEZGAH BAĞI (devere Faz 3, E1): durum `MOUNTED` ⇔ (`currentMachineId` ∧ `currentPosition`) — CHECK `warp_beams_mounted_ck`, bir yuvada tek bağlı levent `warp_beams_machine_position_uq` (partial, yalnız MOUNTED; yarışta P2002 → 409 `WARP_SLOT_BUSY`, F61 `p2002Mentions`), "şu an ne" kolonu OLAYLA AYNI claim'de yazılır (`applyWarpBeamEventTx` `place` + `whereMachineId`). Bağlanabilir makine: `Station.consumesWarpBeam` (yeni kapı, DEFAULT false) + yuva 1..`Machine.warpBeamSlots`. Olay ailesi +12: MOUNTED/DISMOUNTED/EXHAUSTED/SCRAPPED + tipli `*_CANCEL` **LIFO** (yalnız en yeni aktif DURUM olayı geri alınır — `activeForwardStatusEventTx` ↔ `ACTIVE_FORWARD_EVENT_SQL` boğaz-ikiz, mutabakat §42), CONSUMED↔CONSUMED_CANCEL (LIFO dışı), ADJUST_IN/OUT karşı düzeltmeyle kapanır (`WARP_BEAM_ADJUST` sebep ZORUNLU). Kalan metre türetilir; **CONSUMED · ADJUST_OUT · EXHAUSTED kalanı eksiye DÜŞÜREMEZ** (tek kapı `assertCoversRemaining`, 409 `WARP_BEAM_REMAINING_EXCEEDED`; §43 ikinci hat). Söküm/bitişte ölçülen kalan verilirse fark ÖNCE tipli olayla kapanır (`closeToMeasuredTx`: R>M → CONSUMED, R<M → ADJUST_IN `OLCUM_FARKI`), sonra durum olayı; EXHAUSTED `lengthM` = artık (0 ise null), SCRAPPED `lengthM` = kalan (`WARP_BEAM_SCRAP` sebep ZORUNLU, önizlemeli, `warpbeam:cancel`); terminalde kalan 0 (§44). Tartıdan metre `warpLengthFromWeight` = (brüt − dara) ÷ (tel × denye ÷ 9e6); dara yoksa ESTIMATED 0 + uyarı (#13). Söküm/bitiş KAPISI (tezgah §7.3): makinede açık koşum varken son levent sökülemez 409 `WARP_DISMOUNT_OPEN_RUN`, yuvadan az kalırsa UYARI; koşum açılışı leventsiz/eksik yuvada UYARIR, reddetmez (`runOpenBeamWarning`). Koşum penceresi leventleri TEK SQL `beamsMountedDuring` (`setupStartedAt ?? createdAt` → ilk aktif kapatıcı; tezgah raporları kopyalamaz, AST tripwire). Bayraklar [PROFİL]: `devere.mountTracking` DEFAULT false = bugün (satır yok → tak/tüket/bitir 409 `WARP_MOUNT_TRACKING_OFF`, levent READY kalır — ÖLÇÜLDÜ; kapalı bayrak yüzey istemez, E1 yüzeysiz iner) · `devere.mountTrackingRequired` (yöntem + başlangıç saati zorunlu, 400 `WARP_MOUNT_METHOD_REQUIRED`). İzin yeni kod yok: tak/düzelt `warpbeam:write`|`mobile:devere`; sök/tüket/bitir + `mobile:dokuma`; hurda/geri almalar `warpbeam:cancel`|`mobile:devere-iptal`. Uçlar `warp-beam-mount.routes.ts` (alt yönlendirici, aynı üç kapı; `GET /loom-machines` bağlanabilir makineler). PANEL (E2): Leventler satır menüsü Tak/Sök/Tüket/Düzelt/Bitir/Hurda/Geri al YALNIZ `devere.mountTracking` açıkken çizilir (kapalıyken ekran Faz 1b ile birebir), "Tezgah / yuva" sütunu + kalan, yedi durum rozeti; geri alma adayı panelde `undoCandidates` LIFO aynası; Genel Ayarlar › Devere iki satır; istasyon formu `producesWarpBeam`/`consumesWarpBeam`, makine formu `warpBeamSlots` (yalnız devere açıkken çizilir) — gövde `BaseController`dan geçer, doğrulama AÇIK route middleware'i `devere-station-fields.middleware` (yuva 0..32 tam sayı, yetenek boolean; istasyon TÜRÜ kuralı YOK — ölçülecek). TABLET (E3): Levent Sarım ekranı "Tezgahta" sekmesi (MOUNTED bilgi + READY'ye TAK; `mobile:devere`) ve Tezgah ekranı "Levent" paneli (makinedeki bağlı leventler: Sök · Tüket · Bitir; `mobile:dokuma`, makine oturumdan) YALNIZ `devere.mountTracking` açıkken çizilir — bağlam ucu `tablet-context` +`loomMachines` (allowlist) + `mountTracking` + `mountTrackingRequired` SUNUCUDAN, tablet tahmin etmez; mobil `FeatureFlags.devereMountTracking` aynası (DEFAULT false, devere modülüne bağlı hook) Tezgah panelinin kapısıdır; zorunlu ayarda tablet başlangıç saatini bağlama anı gönderir (saat elle girilmez); düzeltme/hurda/tartıyla bitiş/geri alma panelde. Panel durum rozeti tanımadığı değeri "Bilinmeyen durum (ham ad)" basar, çökmez (`statusMeta`, 5e bulgusu — E2'de eski panel F1 SHIPPED_OUT'ta patlıyordu). · bekçi: `test_station_devere_fields` (14, iki sonda) + `beam-undo.test.ts` (LIFO aynası + rozet yedeği, iki sonda) + `test_warp_beam_tablet §5` (loomMachines allowlist + iki bayrak satır-yok false) + `tezgahPayload.test.ts` (Tak/Sök/Tüket/Bitir kuralları, üç sonda) + `settings-groups.test` (devere üç satır) + `test_warp_beam_mount` (§0–§9, dört sonda) + `test_machine_run_beam_overlap` (pencere + AST) + `test_consistency §42–§44` + `test_db_invariants` (+5 nesne) + `test_defter_ters_yol` beyanı + `test_mobil_enum_aynasi` (3 union) <sub>(DEVERE-LEVENT §4.7/§7.3/§9.6/§13; 1e hükmü Faz 3 2026-09-14, 6e — E2 panel · E3 tablet ayrı sha)</sub>
- **[ÇEKİRDEK]** LEVENT OTOMATİK TÜKETİM (devere Faz 4): tezgahtan inen top KK1'de doğarken (`createInitialEntry`, WEAVING + `doffEventId`, AYNI tx, `tx.roll.create`ten SONRA) `autoConsumeForRollTx` bağlı leventlere `CONSUMED.rollId` yazar — TEK yazar, tx açmaz alır. Levent seçimi DEFTERDEN, DOFF ANINDA (`beamsMountedDuring(makine, doffedAt, doffedAt)`; "şu an bağlı" DEĞİL — KK1 sökümden sonra gelse de doğru levent düşer). Her bağlı levente AYRI satır, pay bölünmez (çift levent = iki satır); çözgü = kumaş ÷ (1 − `WarpSpec.takeUpPct`/100) [VERİ; NULL = bilinmiyor → çözgü = kumaş + UYARI], kaynak ESTIMATED, `fabricLengthM` = topun `initialQty` (take-up türetilir, ayrı kolon yok); çok hatlı makine hat payı `÷ productionLineCount` (BEYANLI yaklaşıklık, uyarıda söylenir; kardeş hat topları toplamı = tam boy). Kalan yetmezse KK1 ENGELLENMEZ: kalana kadar yazılır + uyarı adıyla, 0 kalanda satır yok + uyarı (elle yol `assertCoversRemaining` sert kalır). Ters yol: top İPTALİ (`softDelete` CANCEL) → her aktif `CONSUMED(rollId)` için `CONSUMED_CANCEL` (`cancelConsumedForRollTx`); **SCRAP'ta YAZILMAZ** (çözgü gerçekten tüketildi); `restoreCancelledRoll` → yeniden ileri yol (yeni CONSUMED, aynı hesap). Doff'suz WEAVING (fason dokuma) satır yazmaz. `Roll`a levent kolonu AÇILMAZ (N:M, tek kaynak olay satırı); `WarpBeamEvent.rollId` FK Restrict. Bayrak [PROFİL] `devere.autoConsume` DEFAULT false = bugün (KK1 yanıtı bayt bayt, `warnings` anahtarı bile yok — ÖLÇÜLDÜ). UYARI YÜZEYİ: sunucu uyarıları (take-up yok · kalan yetmedi · kalan 0 · bağlı levent yoktu) KK1 tablet (`KK1Screen onSuccess` → `buildEntryWarningToast`, info toast 10 sn, geciken flush'ta barkodla) ve panel elle giriş (`ManualEntryDialog onSuccess` → `entryWarnings`, `toast.warning`) ekranlarında OLDUĞU GİBİ basılır — metin yeniden yazılmaz (5e bulgusu 2026-09-15: mekanizma vardı, yüzeyi yoktu; H3 kısmi tüketim sessiz kalıyordu). · bekçi: `test_warp_beam_auto_consume` (§0–§7, dört sonda) + `test_consistency §45` + `test_db_invariants` (`warp_specs_take_up_pct_range`) + `test_defter_ters_yol` beyanı <sub>(DEVERE-LEVENT §1.3-6/8 · §4.4 · §7 Faz 4 · §14; 1e hükmü H1–H5 2026-09-15, 6e)</sub>
- **BORÇ (Faz 4, 2026-09-15 — H5):** top METRE DÜZELTMESİ (`roll:manual-adjust` / `initialQty` değişimi) levent tüketimini yeniden hesaplamaz — otomatik CONSUMED doğuş anındaki metreyle donar; fark leventte elle düzeltme (`ADJUST_*`, sebep) ile kapanır. Kapanış: metre düzeltme yolunda `rollId`li CONSUMED'a karşı düzeltme yazmak (ters bağ değil, karşı olay) — ölçülmeden yazılmaz.
- **[ÇEKİRDEK]** RAŞEL TAKIMI (#23, "N adet"): sarım `count` 1..24 (DEFAULT 1 = bugün, istek/yanıt bayt bayt) — N>1 → planlı levent WOUND + (N−1) KARDEŞ levent AYNI tx'te DOĞAR ve SARILIR (`nextBeamNoTx` 8029 sıralı LV no; klon: kart/köken/taraf/plan m/not; gövde no `physicalBeamNoPrefix` → `${önek}-${k}`, ilkinin kendisi korunur; K5 gövde ön kontrolü HER kardeşte, biri meşgulse HİÇBİRİ doğmaz — atomik). Kardeş bağı AÇIK kolon `WarpBeam.setKey` (index; `beamRole` Faz 5 kataloğuna saklı, aşırı yüklenmez). İPLİK PAYI: raşel takımında N levent aynı cağlıktan aynı anda sarılır, cağlığa yüklenen kg TOPLAMDIR, levent başına nominal TAM boy ⇒ çıkış ve dip iadesi satırları `÷ N` (`splitShares`: 3 hane, kuruş SON levente, Σ = toplam BİREBİR; lot/depo/sebep aynen kopyalanır); Σ çıkış N×nominalden >%25 sapıyorsa UYARI (red değil). Token yalnız ilk leventin WOUND'unda; replay ilk levent + `siblings` döner. Her kardeş BAĞIMSIZ levent (kendi WOUND/tüketim/iptal; takım iptali yok). Yazım kolu `helpers/warp-beam-wind-write.helper` (`writeWoundTx`, N kez), kardeş doğuşu audit CREATE WARP_BEAM. Panel `WindDialog` + tablet `WindModal` "Adet" + önek; adet 1'de alanlar GÖNDERİLMEZ. · bekçi: `test_warp_beam_set` (§0–§6, üç sonda) + `test_warp_beam_lifecycle` (count=1 değişmez) + `beamPayload.test` (adet yükü) <sub>(DEVERE-LEVENT §8 #23 · §11-6 borcu; 1e hükmü S1–S3 2026-09-15, 6e)</sub>
- **[ÇEKİRDEK]** METAL GÖVDE (`physicalBeamNo`, tr_fold) tekilliği yalnız CANLI leventte (`physical_live_uq`, sarımda 409 `WARP_BEAM_PHYSICAL_BUSY`); plan = rezervasyon, plan/düzenle REDDETMEZ ama `ApiResponse.warnings` ile UYARIR — gövdede canlı levent (adı + durumu) ve aynı gövdeye çift plan; canlı sorgusu tek helper (`findLivePhysicalBeamTx`, fırlatan/uyaran iki kapı aynı yüklemi okur), panel uyarıyı toast basar. · bekçi: `test_warp_beam_lifecycle §12/§12b` + `useWarpBeamMutations.test` <sub>(K5 · K5b kullanıcı bulgusu 2026-09-18 03:30, 1e kararı, 6e)</sub>
- **BORÇ (lot, 2026-09-14):** sayım (`stock-count`, `stock-count-reversal`) ve fatura (`invoice.service` iplik çıkışı/tersi) yolları LOT-AGNOSTİK — lotsuz OUT/ADJUST yazarlar, lotlu stok lotsuz düşebilir ve o kg'nin lot izi kopar (türetilen lot bakiyesi şişer, §41 yakalamaz çünkü eksiye düşmez). Kapanış: bu yollara lot seçimi/lot bazlı netleme (aynı `applyYarnMovementTx` allowlist'i); sahibi devere alanı, dilim ayrı — A2/A3 kapsamı DIŞI (1e ek şart ④). · bekçi: yok (borç görünür yazıldı)
- **[ÇEKİRDEK]** Sarımda iplik BRÜT çıkar, dip AYRI satırla döner: `WARP_ISSUE` (cağlığa yüklenen kg, ≥ 1 satır, IN_HOUSE) · `WARP_RETURN` (bobin dibi; `ReasonPresetKind.WARP_RETURN` kodu ZORUNLU — DEPOYA_IADE · ATKILIK_AKTARIM · TELEF; CHECK `yarn_movements_warp_return_reason_ck`), ikisi de `warpBeamId` taşır (CHECK `yarn_movements_warp_link_ck` ⇔) ve levent doğuşuyla AYNI tx'te yazılır. Sarım iptali NET ters: `WARP_ISSUE_REVERSAL` (kalem × depo) + `WARP_RETURN_REVERSAL` (× sebep) — iki toplam AYRI AYRI sıfırlanır, birbirini götürmez (§4.9-4). İşaret TEK KAYNAK `yarnMovementSign` (mutabakat SQL'i ve fiş tersleri `yarnInboundKinds`ten türer; elle `IN/ADJUST_IN` listesi GEÇERSİZ); eksi-bakiye kapısı bakiyeyi DÜŞÜREN ileri yolları kapılar (OUT · WARP_ISSUE · WARP_RETURN_REVERSAL). Elle iplik ucu (`POST /yarn/movements`) WARP_* KABUL ETMEZ; liste süzgeci 8 türü tanır. `WOUND.kgSource` "sarım kg'ı nasıl bilindi" (WEIGHED | THEORETICAL); nominal kg = tel × denye × m / 9.000.000 (`warpTheoreticalKg`, panel aynası), denye TEK helper `resolveDenier` (1a′ yalnız oraya dokunur). · bekçi: `test_warp_beam_lifecycle §3/§5` + `test_yarn_stock` + `test_consistency §27` <sub>(§3.7 · §3.8 · §4.8 · 9.7b)</sub>
- **[ÇEKİRDEK]** Taslak levent (PLANNED — hiç olayı, hiç iplik satırı yok) ④ sınıfı hard delete ile silinir (`deleteMany WHERE status=PLANNED`, count-0 → taze okuma → 409 `WARP_BEAM_NOT_PLANNED`); sarılmış levent SİLİNMEZ, sarımı iptal edilir (önizlemeli: dönecek iplik satırları ADIYLA, gerekçe zorunlu, ayrı yetenek `warpbeam:cancel`). Panel Leventler ekranı dört kapıyla doğdu (`operations/warp-beams`, `devereEnabled`, `warpbeam:read` · yetenek `warpbeam:write` / `warpbeam:cancel`); Tablet LEVENT SARIM ekranı aynı kapıların tablet ikizidir (`Devere` · `devereEnabled` HAM bayrak, üretime bağlı değil · `mobile:devere` · yetenek `mobile:devere-iptal`; PATCH tablet izni almaz; form bağlamı tek uç `GET /warp-beams/tablet-context` (`warp-beam-tablet.service`, opt-in allowlist = `map`); OTURUMSUZ; kuyruk YOK — tanım ve hükümler `docs/design/DEVERE-LEVENT-TARAMASI.md` §11). · bekçi: `test_warp_beam_lifecycle §6` (④ claim) + `test_screen_catalog` (dört kapı) + `warp-beam-regime.test.ts` (karo rejimi) + `test_warp_beam_tablet` (tablet kapıları + allowlist) + mobil `screenModules.test` / `useVisibleScreens.test` (kart rejimi) <sub>(0c 2026-09-14; tablet 6e 2026-09-14)</sub>
### Yasaklar

- **[ÇEKİRDEK]** `Roll.doffEventId` ve `MachineRun.weavingOrderId` ASLA `null`'lanmaz — ileri damgayı `null`'lamak ters kayıt DEĞİLDİR (`dispatchedAt` · `weighedAt` · `invoicedAt` ailesi). <sub>(kök `CLAUDE.md` § Veri ve defter)</sub>
- **[ÇEKİRDEK]** `plannedM`e ulaşmak dokuma işini KAPATMAZ: o sayı geç gelen bir ölçüme (KK1 top metresi) dayanır ve otomatik kapanış tezgah koşarken işi kapatır — eşik bir BİLDİRİMDİR, bir karar değil. <sub>(DOKUMA-IS-EMRI §2.2a)</sub>
- **[ÇEKİRDEK]** Dokuma işine sipariş bağı açılmaz: karşılama `SackAllocation` ile sevk anında yazılır ve istenen şey aslında REZERVASYONDUR — rezervasyon bu üründe yoktur (profil kararı). <sub>(DOKUMA-IS-EMRI §2.2c; `MODUL-BAYRAK-TASARIM.md` #6)</sub>
- **[ÇEKİRDEK]** Kopuş/duruş sayaçları ÇOĞULLAŞTIRILMAZ (`warpStopCount1/2` yazılmaz); levent kırılımı deftere yazılır (`MachineStopEvent.beamSlot`) ve sayaçlar aynı `kind`ın TÜM slotlarını toplar. <sub>(DOKUMA-TEZGAH §5.4)</sub>

### Kararlar

- **[ÇEKİRDEK]** Dokuma işinin kapanışı AÇIK BİR KARARDIR (operatör kapatır), türetilmez; açık koşum varken kapanmaz ve 409 koşumları ADIYLA söyler (`{code, machines[], runIds[]}`). <sub>(DOKUMA-IS-EMRI §2.2a)</sub>
- **[ÇEKİRDEK]** Dokuma işinin iptali yalnız kendi üstünde bir DURUM GEÇİŞİDİR: kapanmış koşum, duruş, doff ve doğmuş top DOKUNULMAZ — "bunu şu iş için dokuduk, sonra iş iptal edildi" doğru bir cümledir. <sub>(DOKUMA-IS-EMRI §2.2b)</sub>
- **[ÇEKİRDEK]** Dokuma işi YAZMA YÜZEYİ `/api/weaving-orders` (liste · detay · oluştur · düzenle · kapat · iptal; `weavingorder:read/write`, kapı `requireDokumaEnabled`): numara `DK`+GGAAYY+NNNN ve 8032 kilidi üretecin İLK ifadesi; `clientToken` replay dört durumlu (`WEAVING_ORDER_CANCELLED`); kapat/iptal/düzenle `status IN (PLANNED, IN_PROGRESS)` üzerinden ATOMİK CLAIM (`WEAVING_ORDER_NOT_OPEN` / `WEAVING_ORDER_NOT_EDITABLE`); açık koşum `endedAt IS NULL AND revokedAt IS NULL`. `PLANNED → IN_PROGRESS`in TEK YAZARI `markWeavingOrderInProgressTx` (helper; iki adımlı claim: PLANNED→IN_PROGRESS, düşerse IN_PROGRESS satırına DOKUNMA = satır kilidi, o da düşerse 404/409), tetikleyicisi koşum-açma ucu (AYNI tx'te çağırır; audit çağıranın, `transitioned` ile) — manuel `start` ucu YOKTUR, "devam ediyor" demek koşum var demektir. Kapat/iptal claim'i koşum sayımından ÖNCE alır (satır kilidi): iki tx birbirini görür. · bekçi: `test_weaving_order` §0 (yazan yol tam 1) · §9 (eşzamanlılık iki yönlü) <sub>(1e hükmü 2026-09-13; 6e koşum yüzeyi sözleşmesi)</sub>
- **[ÇEKİRDEK]** Taraf XOR (SUBCONTRACTED ⇔ `subcontractorId` dolu) İKİ hat: serviste 400 + DB CHECK `weaving_orders_party_ck` (migration `20260913241000`, P1'in ertelediği sed yazma yüzeyiyle indi). · bekçi: `test_weaving_order` §8, `test_db_invariants` <sub>(migration 20260913180000 notu)</sub>
- **[ÇEKİRDEK]** Dokuma işi DEFTERE YAZMAZ (durum tablosu; koşum/duruş/doff/top kendi defterinde) ⇒ iptal bir durum geçişidir ve ters mekanizma KAPSAM DIŞIDIR — reopen/undo-cancel YOK, eksiklik değil. <sub>(kök `CLAUDE.md` ters yol ekseni; 1e 2026-09-13)</sub>
- **[ÇEKİRDEK]** Dokuma üçlüsü (`weaving-order` · `machine-run` · `machine-doff` router'ları) TEK kapının arkasındadır: `requireDokumaEnabled` (ön koşul `production` ÖNCE ölçülür, 403 eksik olanı söyler), `verifyToken`DAN SONRA ve her uçtan ÖNCE; kapı ekran dilimiyle doğdu ve `SCREEN_CATALOG` `operations/weaving-orders` (`modul: "dokumaEnabled"`) aynı commit'te indi, `SCREENLESS_PERMISSIONS`taki iki `weavingorder:*` muafı düştü. Dokuma uçlarını çağıran her istemci dosyası bekçinin allowlist'indedir ve karosu `dokumaEnabled`e bağlıdır — kapalı fabrikada ne karo, ne route, ne istek. · bekçi: `test_dokuma_regime_gate` §2–§7 <sub>(1e 2026-09-13; borç 01 P4 → kapandı 0c ekran dilimi)</sub>
- **[ÇEKİRDEK]** VARDİYA KARNESİ (`MachineShiftStat`) DURUM'dur, taneciği MAKİNE × VARDİYA (`@@unique([machineId, shiftInstanceId])`, `productionLineNo` unique'te YOK — hat kırılımı gerekirse EKLEMELİ çocuk tablo, bu unique bölünmez); geçmişi iki DEFTER taşır: `MachineShiftStatSeal` (SEAL/UNSEAL/RESEAL, tipli enum çifti, doğal anahtar `(statId, sealGeneration, action)`) ve `MachineShiftStopBreakdown` (sebep × kayıp sınıfı, mühür KUŞAĞI başına; ters yol = yeni kuşak; sınıflandırılmamış kova `COALESCE(reasonCode,'')` ifade indeksiyle tekil — NULLS NOT DISTINCT değil, Prisma onu DROP diye önerir). `factoryDay` `ShiftInstance.factoryDayKey`ten kopya (`@db.Date` muafı); `source` `@default`suz; `sealedAt` null'lanmaz. Vardiya penceresi TEK job'dan doğar (`jobs/shift-calendar.job.ts`: `dokuma.enabled` kapalıyken `"disabled"` — referans fabrikada `shift_instances` boş; 30 gün ileri, `@@unique([shiftDefinitionId, factoryDayKey])` ile idempotent, kilit yok; tanım değişince MÜHÜRSÜZ pencere yeniden yazılır, mühürlü DEĞİŞMEZ, iptal korunur; pencere `constants/time.ts`ten — `factoryMinuteOfDay`). · bekçi: `test_shift_calendar_job` (§0–§7, iki negatif sonda) · `test_db_invariants` (4 CHECK + ifade unique) · `test_snapshot_kolonlari` (dört donmuş kolon) · `test_defter_ters_yol` (Seal ENUM_CIFTI · Breakdown SATIR_EBEVEYN) <sub>(DOKUMA-RAPOR-BACKEND-TASARIM-OZETI §1–§2, §4 M0; 1e hükmü 2026-09-14; migration 20260914130000; tasarım §4 ③b/N9/P-yapı GEÇERSİZ → 2026-09-14)</sub>
- **[ÇEKİRDEK]** Karne TERİMLERİ tek helper'dan doğar (`helpers/loom-shift-terms.helper.ts` `computeShiftTermsPure`, DB yükleyici `machine-shift-stat.service.ts`): duruş PENCEREYE KIRPILIR (`clipToWindow`, açık duruş `now`a kadar + uyarı); KOVA = `lossClass` (NON_SCHEDULED hiçbir paydada yok · PLANNED POT'tan · SETUP/UNPLANNED kullanılabilirlikten · sınıfsız → UNPLANNED + `unclassifiedSec` + uyarı); MINOR SÜRE sınıfı: yalnız KAPALI, `durationSec < MINOR_STOP_THRESHOLD_SEC` (20 sn, `constants/loom-shift.ts`, karneye DONAR) ve kaybı kullanılabilirlikte olan duruş — PLANNED/NON_SCHEDULED eşik altı da olsa sınıfını korur; atkı Σ `picksAtClose` koşumun KAPANDIĞI pencereye (orantılama YOK, açık/sonra kapanan koşum katılmaz + uyarı, `picksAtClose` NULL → uyarı 0 değil); kapasite Σ target_i × (koşum ∩ pencere)nin POT/APT dakikası, target = koşum ?? künye nominal ?? ÖLÇÜLEMEDİ (kapasiteye girmez), planlı mola koşumlara PENCERE PAYIYLA dağıtılır (tek tam koşumda birebir); BOŞ TEZGAH → `nonScheduledSec` = takvim, `source` INFERRED; `source` önceliği SIMULATED > SUPERVISOR > OPERATOR > INFERRED (`resolveShiftSource`). ORANLAR tek helper (`helpers/loom-efficiency.helper.ts`): A=APT/POT · P=(picks−gap)/capApt · E=(picks−gap)/capPot, payda 0 → `null` (BEYAN), P>100 uyarı düzeltme yok, toplam Σ/Σ ortalama değil; AST tripwire: `/ potSec|aptSec|targetUnitCapacity*` bölmesi başka dosyada, `…Pct ?? 0`, `avg(…Pct)` YASAK. M1 okuyucu `GET /api/machine-shift-stats?from&to[&machineId][&sealState]` (`requireDokumaEnabled` + `report:production`): tezgah kümesi = aktif makine ∧ istasyonu WEAVING (künye/izleme hâli SÜZMEZ, kopyalanır — elle giriş birinci sınıf), mühürsüz satır ANLIK (`live:true`), mühürlü DB'den. · bekçi: `test_machine_shift_terms` (§1–§10, üç negatif sonda) · `test_machine_efficiency_formula` (§1–§5, iki negatif sonda) <sub>(DOKUMA-RAPOR-BACKEND-TASARIM-OZETI §3–§5; DOKUMA-TEZGAH §5.2; 1e hükmü ④; Dilim 2, 2026-09-14)</sub>
- **[ÇEKİRDEK]** KARNE MÜHÜR ÇEVRİMİ (`machine-shift-seal.service.ts`): kapanış job'u M2 (`jobs/machine-shift-close.job.ts`; `dokuma.enabled` kapalı → "disabled"; `endsAt + 60 dk` geçmiş son 3 günün vardiyaları × M1 ile AYNI tezgah kümesi) satır yoksa OPEN yazar, OPEN ∧ `source ≠ SUPERVISOR` satırı yeniden hesaplar, elle düzeltilmiş (SUPERVISOR) ve SEALED satıra DOKUNMAZ, MÜHÜRLEMEZ; M3 `PUT /machine-shift-stats/:id/terms` (`loom:manual-entry`) claim `updateMany WHERE {id, sealState: OPEN}` → count 0 → 409 `SHIFT_SEALED`, kaynak SUPERVISOR, POT/APT yeniden, kapasite TEK hedeften; M4 `POST /:id/seal` (`loom:manual-entry`) tek tx: claim OPEN→SEALED (`sealGeneration+1`, `sealedAt/ById`) → count 0 → 409 `SHIFT_SEAL_RACE`, oranlar `computeMachineKpis` ile DENORMALİZE, kırılım duruş defterinden YENİ KUŞAK (eski kuşak durur), `MachineShiftStatSeal(SEAL|RESEAL)` terim fotoğrafı; M5 `POST /:id/unseal` (`loom:shift-unseal` — YENİ kod, `WEB_PRODUCTION_SUPERVISOR` + SCREENLESS gerekçeli, SoD üçlüsüne GİRMEZ; gerekçe ≥ 3) claim SEALED→OPEN → 409 `SHIFT_NOT_SEALED`, `sealGeneration` KALIR, `sealedAt` ASLA null'lanmaz (`Seal(UNSEAL)`); `GET /:id/seals` mühür defteri. Yarışta iki paralel mühür → tam biri 200, öteki 409; advisory uzay AÇILMADI (claim + unique yeter). · bekçi: `test_machine_shift_seal` (§0–§6, üç negatif sonda) · `test_permission_catalog`/`test_role_template_catalog`/`test_screen_catalog` (`loom:shift-unseal` üçlüsü) <sub>(DOKUMA-RAPOR-BACKEND-TASARIM-OZETI §4 M2–M6, §6 1–3; 1e hükmü ③; Dilim 3, 2026-09-14)</sub>
- **[ÇEKİRDEK]** HAT KIRILIMI eklemeli ÇOCUK tablodur (`MachineShiftLineStat`, `@@unique([statId, productionLineNo])`), ebeveynin unique'i BÖLÜNMEZ ve tetiği VERİDİR — satır yalnız `Machine.productionLineCount > 1` makinede doğar (bayrak yok; tek hatlıda çocuk satır YAZILMAZ = bugünkü davranış, negatif sonda). Hat yalnız ÜRETİM terimlerini taşır (koşum ekseni `MachineRun.productionLineNo`: atkı · kapasite · hedef · metraj); süre terimleri (POT/APT/duruş) makinede kalır — duruş makineyi durdurur hattı değil ⇒ hattın A'sı ebeveynin, P/E'si hattın. Hat kümesi `1..count ∪ koşumların hatları` (küme dışı hattın koşumu kaybolmaz, Σhat = makine); M2 ebeveynle aynı tx'te claim'in ARKASINDA upsert eder, silmez; kendi mührü YOK — ebeveynle atomik, fotoğrafı Seal `terms.lines`; M3 elle düzeltme hatlara dokunmaz, Σhat ≠ karne olunca `byLine` okuması UYARIR. Okuma OPT-IN `?byLine=1` (randıman · vardiya karnesi · M1 listesi; Pareto almaz — duruş ekseni hat bilmez): opt-in yoksa gövde bayt bayt eski, mühürlüde çocuk tablodan (donmuş), açıkta anlık. · bekçi: `test_machine_shift_line_stat` (§0–§5, üç negatif sonda) · `test_db_invariants` (iki CHECK) · `test_snapshot_kolonlari` (`targetUnitCapacityApt` DONMUS_ILERI) <sub>(DOKUMA-RAPOR-BACKEND-TASARIM-OZETI §1/1 "hat kırılımı gerekirse eklemeli çocuk tablo"; 1e görevi 2026-09-15; migration 20260915010000)</sub>
- **[ÇEKİRDEK]** TEZGAH KÜNYESİ yazma yüzeyi ve GÖLGE MOD durum makinesi (B3): `/api/machine-specs` (`requireDokumaEnabled` + `loom:spec-manage` — tasarımın `requireTezgahEnabled`i ekransız doğamaz, `test_screen_catalog §10b`; Faz 2 "Devreye Alma" ekranıyla taşınır); künye gövdesi ALLOWLIST (`shedType` · `nominalUnitsPerMin` · `baselineRunHours/At` · `notes`) — `monitoringState` ve damgalar gövdeden YAZILAMAZ; OFF→SHADOW claim (409 `SPEC_NOT_OFF`), SHADOW→LIVE ÜÇ ŞART sırası ÖLÇÜLEBİLİRDEN ÖLÇÜLEMEYENE: ② ≥ `SHADOW_MIN_SHIFTS` (15, `constants/loom-shift.ts`, Faz 2'de bayrak) mühürlü ∧ donmuş `monitoringState=SHADOW` karne → 409 `SHADOW_TOO_SHORT` "14/15" · ① kanal kabulü → `PeripheralSignal` Faz 2'de, KANIT YOK ⇒ FAIL-CLOSED 409 `SIGNAL_NOT_ACCEPTED` ("sinyal kabulü Faz 2 ile açılır") ⇒ **LIVE bugün ERİŞİLEMEZ — beyanlı kapı, çıkışsız değil: SHADOW erişilir, karneler normal mühürlenir** · ③ anomali kaynağı Faz 2; LIVE→SHADOW sebepli demote (`acceptedAt` ASLA null'lanmaz, ayrı damga). Bakiyesi (`baselineRunHours`) dolu künye makine kalıcı silmeyi 409 ile durdurur (`machineSpecBaselineCount`; P4 muafı düştü — guard ilk yazma yüzeyiyle doğdu). · bekçi: `test_machine_shadow_mode` (§0–§6 + S2/S3'; S1/S3/S5/S6 ⏭ beyanlı; dört negatif sonda) <sub>(DOKUMA-TEZGAH §2.3, §9 S1–S6; 1e hükmü (a) 2026-09-14)</sub>
- **[PROFİL]** `cancelReason` serbest metin (zorunlu, ≤300), `ReasonPreset`e BAĞLANMAZ — **ÖLÇÜLDÜ 2026-09-14, `kind` AÇILMADI:** kullanım popülasyonu 0 (model 2026-09-13'te indi, `dokuma.enabled` referans fabrikada kapalı, canlı dokuma kurulumu yok ⇒ tekrar sayılamaz — ÖLÇÜLEMEDİ, "hayır" değil) VE `kind`ın iki yapısal gerekçesi de yok: tablet yüzeyi yok (iptal yalnız panel `WeavingOrderActionDialogs`, serbest metin; `ROLL_CANCEL` emsali KK1 tablet listesi) · rapor okuyucusu yok (0 rapor servisi; `ORDER_CANCEL` emsali `order-cancellation.report` anahtarı). **Yeniden açılma tetiği (biri yeter):** tablet iptal yüzeyi doğarsa · iptal sebebi raporu istenirse · ilk dokuma kurulumunda ≥ 30 iptal biriktiğinde `SELECT lower(trim("cancelReason")), count(*) FROM weaving_orders WHERE "cancelReason" IS NOT NULL GROUP BY 1 ORDER BY 2 DESC` en sık 5 metin toplamın ≥ %60'ıysa — o gün reçeteli iş (`RECETELER` § enum değeri), ayrı dilim; eşik yazarın seçimi, ilk gerçek ölçümde sayıyla yeniden bakılır. <sub>(1e hükmü 2026-09-13; ölçüm arşiv 2026-09-14)</sub>
- **[ÇEKİRDEK]** `DOFF_CANCEL` yalnız o doff'a bağlı HİÇBİR top yokken açıktır (`NOT EXISTS (rolls WHERE doffEventId = :id)`); statüye BAKILMAZ — iptal/fire edilmiş top da sayılır, çünkü ölçüt "top var mı" değil "doff ileri sonuç yazdı mı"dır; 409 mesajı topu ADIYLA söyler ve çare ÖNERMEZ ("indirme artık geri alınamaz; yanlış top kendi iptal yolundan gider" — "önce topu iptal et" yanlış çareydi, iptal yüklemi değiştirmez), liste 20'de kırpılır SAYI kırpılmaz (`details.total`); KK1 kilidi tx zaman aşımını aşarsa P2028 → 409 `DOFF_LINK_IN_PROGRESS` "tekrar deneyin" (503 değil — durum çatışması). · bekçi: `test_machine_doff_source §5b' · §5e · §5f` <sub>(DOKUMA-IS-EMRI §3.8 · §3.8b ④; 1e kararı C.1/C.5, 2026-09-13)</sub>
- **[ÇEKİRDEK]** Koşumsuz doff MEŞRUDUR (`machineRunId` nullable) — fiziksel olarak olmuş bir olayı kaydetmeyi reddetmek, onu atıfsız kaydetmekten kötüdür; koşum yoksa `ApiResponse.warnings`, 400 değil. <sub>(DOKUMA-IS-EMRI §3.8; kurşun `machineId=null` emsali)</sub>
- **[ÇEKİRDEK]** Koşumsuz doff kendi kovasını alır ve ana sayıya KARIŞMAZ: rapor DÖRT sayı basar (iş emrine bağlı doff metresi · koşumsuz doff · koşumlu ama İŞSİZ doff · doff'suz top) ve `warnings` metni KAYBI söyler — "koşum açılmadığı için bu indirme iş emri metresine girmiyor"; uyarı REPLAY cevabında da döner (çevrimdışı yeniden gönderimde tek cevap odur; ilk kayıt ve replay tek kaynak `deriveRunWarnings`). · bekçi: `test_machine_doff_source §1d · §1e · §2c` <sub>(DOKUMA-IS-EMRI §3.8 · §3.8b ②; §3.8c A.3/B.5, 2026-09-13)</sub>
- **[ÇEKİRDEK]** İndirme kodu (`DF+GGAAYY+NNNN`) günlük sıradır ve oku-sonra-yaz'dır ⇒ 8029 "kod tekilliği" uzayında serileşir (`nextDoffCodeTx`, kilit tx'in İLK ifadesi; yeni uzay açılmaz), `withBarcodeRetry` yalnız `code_key` P2002 kemeridir — kilitsiz sürüm 25 paralelde tükeniyordu (10'da tutan 25'te tutmaz). KK1 HTTP ucu `doffEventId` alanını ŞEMADA taşır (düz `z.object`: listede olmayan alan sessizce düşer ve bağsız 201 döner) ve verilince `entrySource=WEAVING` zorlar; `semiFinished` ile birlikte 400. · bekçi: `test_machine_doff_source §4f · §6b` <sub>(DOKUMA-IS-EMRI §3.8b ② adım 4 · ③; 1e kararı C.2, 2026-09-13)</sub>
- **[ÇEKİRDEK]** Doff↔top bağı KK1'de AÇIK LİSTE seçimidir; makine eşleşmesi YALNIZ damgadaki makinenin istasyonu `WEAVING` ise denetlenir (tezgah başı KK1: farklı tezgah → 409 `DOFF_NOT_LINKABLE`); muayene makinesi damgası (RAW_QC/PROCESS_QC — masa KK1'in gerçek hâli, 47 ölçtü: KK1 toplarının 2190/2205'i böyle) ve damgasız oturum → KABUL; "damga doluysa karşılaştır" masa KK1'i hep 409'a düşürürdü, "damga yoksa 400" ise hiç geçemeyen ÖLÜ kapıydı. · bekçi: `test_machine_doff_source §4c · §4c2 · §4e` <sub>(DOKUMA-IS-EMRI §3.8b ③; 1e hükmü 2026-09-13, 47 ölçtü — aynı gün verilen "makinesiz KK1'de 400" kararı GEÇERSİZ → 2026-09-13, arşiv)</sub>
- **[ÇEKİRDEK]** `MachineRun.weavingOrderId` koşumda tutulur ama `DoffEvent`te TUTULMAZ — doff'un iş emri koşumdan türetilir, çünkü ikinci bir FK ikinci bir kaynak olur. <sub>(DOKUMA-IS-EMRI §3.8; `MachineRun.warpBeamId` emsali)</sub>
- **[ÇEKİRDEK]** Üretim hattı numarası kolonu `productionLineNo` adını taşır (`lineNo` DEĞİL) — `lineNo` şemada `InvoiceLine`/`PurchaseOrderLine`ta belge satır numarası anlamında yaşıyor ve iki anlam çarpıştığında geri adım atan YENİ olandır. <sub>(DOKUMA-IS-EMRI §1.6)</sub>
- **[PROFİL]** `dokuma.enabled` `tezgah.enabled`in KARDEŞİDİR, çocuğu değil (ön koşul `production.enabled`); fasona dokutan firmada dokuma vardır tezgah yoktur, yalnız devere makinelerini izleyen firmada tersi. Varsayılan `false` = bugünkü davranış (ölçüldü: fabrikada 0 İPLİK kalemi, 0 çözgü kartı). <sub>(DOKUMA-IS-EMRI §2.5)</sub>

## Panel (Electron)

### Değişmezler

- **[ÇEKİRDEK]** Panel TEZGAH DURUŞLARI ekranı (`operations/machine-stops`, Faz 1b web yüzeyi) dört kapıyla doğar: `SCREEN_CATALOG` satırı (`modul: dokumaEnabled`, `requires: [loom:manual-entry, loom:classify]` — İKİSİNDEN BİRİ açar, eylemler ekran içinde `PermissionGate`) · karo (`permissionAny` route ile birebir, yüklem SAF `isMachineStopsVisible`) · route (`requireAnyPermission`) · `ROUTE_MODULE` aynası; `loom:manual-entry`/`loom:classify` ekransız muafından DÜŞTÜ. Sınıflandırma kuyruğu = `requiresReason ∧ reasonCode null` ve GÜN süzgecinin DIŞINDADIR (borç hangi günden olursa olsun görünür); sebep atama İLK karar, yeniden sınıflandırma web-only ve `MachineStopReclass` DEFTERİ ekranda okunur (`GET /machine-stops/:id/reclasses` — from→to · kim · ne zaman · gerekçe, salt okuma); elle giriş `clientToken` diyalog başına bir kez. Süzme SUNUCUDA: gün (`factoryDay` YYYY-MM-DD → @db.Date UTC gece yarısı) · tezgah · vardiya (`shiftInstanceId`; seçenekler o günün satırlarından türer — vardiya listesi ucu yok) · kapsam. · bekçi: `test_screen_catalog` (manifesto/route/karo/`ROUTE_MODULE` iki yönlü) + `test_dokuma_regime_gate §7` (allowlist satırı `izinler` ile parametreli: §7c manifesto, §7d saf yüklem, §7e route) + `stop-regime.test.ts` + `tile-visibility.test.ts` <sub>(DOKUMA-PANEL-EKRAN-KAPILARI sözleşmesi; 1e ataması 2026-09-14, 0c)</sub>
- **[ÇEKİRDEK]** Panel DOKUMA RAPORLARI (`reports/dokuma`, Raporlar hub'ı karosu; 01 Dilim 5) Raporlar hub'ının kendi biçimiyle dört kapıdan geçer: `Reports/tile-config.ts` karosu `featureFlag: "dokumaEnabled"` + `permission: "report:production"` (saf `visibleWhen` yüklemi YAZILMAZ — hub bayrağı `ctx[featureFlag]` ile süzer, ayrı yüklem ölü kod olurdu) · beş route `requirePermission="report:production"` (üç segmentli adres) · `SCREEN_CATALOG` `reports/dokuma` (`modul: dokumaEnabled`, `capabilities: [loom:manual-entry, loom:shift-unseal]`; `loom:shift-unseal` ekransız muafından DÜŞTÜ) · `ROUTE_MODULE` aynası. Karne eylemleri ekran içinde `PermissionGate` ile ve backend uçlarının GERÇEK izniyle birebir (`SHIFT_STAT_ACTION_PERMISSIONS`: düzelt/mühürle `loom:manual-entry`, aç `loom:shift-unseal`); `statId` yoksa eylem yok. Oran `null` → "ölçülemedi" tek yerde (`formatPct`), kaynak kırılımı her sayfada, `SIMULATED` ayrı. · bekçi: `test_dokuma_regime_gate §7` (`karo: "reports"` kolu — `featureFlag` düşürülünce ❌) + `test_screen_catalog` + `dokuma-regime.test.ts` + `command-entries.test.ts` (kategori izni istisnası adıyla) <sub>(DOKUMA-PANEL-EKRAN-KAPILARI § İNDİ 2026-09-14 üçüncü ekran; 1e hükmü Dilim 5)</sub>
- **[ÇEKİRDEK]** `MachineStopDto` makineyi (`code/name`), vardiyayı (`shiftInstance` + tanım adı, iptal bayrağı) ve sınıflandıranı (`classifiedBy.fullName` + `classifiedAt`) ADIYLA taşır — panel "kim/ne zaman" sorusunu audit'e uzanmadan cevaplar (defter kolonu); tablet aynı DTO'yu okur (ek alan, kırmaz). · bekçi: `test_machine_stop_manual` + `test_loom_lists` <sub>(0c 2026-09-14)</sub>

## Tablet (mobil)

### Değişmezler

- **[ÇEKİRDEK]** Dokuma ekranı bir KUYRUK değil bir TEZGAH gösterir: önünde bekleyen iş listesi yoktur, bir (ya da `productionLineCount` kadar) tezgah vardır ve o ya koşuyordur ya durmuştur. <sub>(DOKUMA-IS-EMRI §3.2)</sub>
- **[ÇEKİRDEK]** Top indirme (doff) tablette KUYRUĞA GİRMEZ — mutation online-only (`networkMode:'always'`), çevrimdışıyken İNDİR kilitli ve sebebi ayrı anlatılır (ağ yok / sunucu yok), kalıcı düşüş anlık toast; `STATION_MUT`/`setMutationDefaults`/`stationLabels` üçlüsüne girmez. Koşum aç/kapat/geri al da AYNI kararla online-only (`useRunMutations.ts`); duruş · levent kendi diliminde karar alır. · bekçi: `doffAttempt.test.ts` (token yalnız belirsiz hatada yapışır) <sub>(1e hükmü J.5 2026-09-14 — §3.6 "dördü de kuyruğa girer" GEÇERSİZ; kk1.md kuyruk kararı)</sub>
- **[ÇEKİRDEK]** Tablet TEZGAH ekranı (`Dokuma`, izin `mobile:dokuma`) WEAVING istasyonunda MAKİNEYLE açılan oturumdan çalışır (`stationScreens.ts` iki harita, `SessionGate`); makine ve hat ekrandan SEÇTİRİLMEZ — hat yalnız `SessionMachine.productionLineCount > 1` iken seçilir (oturum yükü bu alanı taşır), tek hatta seçici çizilmez. · bekçi: `stationScreens.test.ts` (5 oturumlu ekran, iki yön) <sub>(DOKUMA-IS-EMRI §3.2/§3.7; 0c 2026-09-14)</sub>
- **[ÇEKİRDEK]** Doff formu backend `openSchema` ile BİREBİRDİR (`doffPayload.ts` saf): `pieceCount` 1..1000 tam sayı, `counterAtDoff` boş = okunmadı (null), `counterSource` HER yükte beyan edilir (`@default` yok) — cihazdan `MACHINE`, elle `OPERATOR`, simüle cihazdan **`SIMULATED`** (KK1'in beyansız metre yolu TEKRARLANMAZ); `doffedAt` operatörün İNDİR'e bastığı an; koşum bağı `machineRunId` açık koşum seçiciden, atıf uydurulmaz (seçilmezse null + amber uyarı). · bekçi: `doffPayload.test.ts` <sub>(DOKUMA-IS-EMRI §3.9 B; reçete ㉓)</sub>
- **[ÇEKİRDEK]** Doff `clientToken` MANTIKSAL DENEME başına bir kez (`doffAttempt.ts`, Fason Kabul ikizi): parmak izi backend `resolveReplay`in üç alanı (makine · hat · parça sayısı; sayaç ve koşum bağı GİRMEZ), token yalnız belirsiz hatada (ağ/zaman aşımı/5xx) yapışır, kesin 4xx'te yapışmaz, başarıdan sonra biter; `CLIENT_TOKEN_COLLISION` toast değil MODAL ("yeni indirme olarak gönder" taze token). · bekçi: `doffAttempt.test.ts` <sub>(DOKUMA-IS-EMRI §3.9 B; `entryAttempt.ts` sözleşmesi)</sub>
- **[ÇEKİRDEK]** Doff geri alma tablette AYRI YETENEK iznidir (`mobile:dokuma-geri-al`; katalog `capabilities`, dar rol muafı, uç guard'ı `requireAnyPermission("loom:doff-revoke", "mobile:dokuma-geri-al")`): izni olmayanda buton çizilmez; `DOFF_HAS_ROLLS` modal barkodları ADIYLA listeler ve "geri alınamaz; yanlış top kendi iptal yolundan gider" der ("önce topu iptal edin" YAZILMAZ); listede `rollCount > 0` satır kilitli görünür. · bekçi: `test_mobile_screen_permissions` (yetenek muafı) + `test_role_template_catalog §2` (muaf) + `doffPayload.test.ts` (`classifyDoffFailure`) <sub>(1e hükmü J.3; §3.9 C)</sub>
- **[ÇEKİRDEK]** Tablet KOŞUM formu backend `openSchema` (machine-run) ile BİREBİRDİR (`runPayload.ts` saf): dokuma işi OPSİYONEL (numune koşumu meşru; açık işler `GET /weaving-orders?status=PLANNED,IN_PROGRESS`, fason işler listeden düşer — yalnız `executionKind==='IN_HOUSE'`), desen/renk işten ÖN-DOLU ama kilitli DEĞİL (`prefillFromOrder`), `targetUnitsPerMin` boş = null (`MachineSpec` yedeği; o da yoksa performans HESAPLANMAZ, uydurulmaz), `unitsPerCm` (ham atkı/cm) boş = null (metre türetilmez, randıman yine hesaplanır — koşumun TEK elle girdisi, 2026-09-14), `startedAt`/`endedAt` operatörün bastığı an; kapanışta yalnız `picksAtClose` sorulur, boş = ölçülmedi (null, 0 DEĞİL) — üretilen metre KK1'den gelir. `clientToken` deneme başına bir (`doffAttempt` yardımcıları; parmak izi makine · hat · iş · desen). 409 → eylem: `PRODUCTION_LINE_OCCUPIED`/`RUN_*` koşum listesini tazeler, `WEAVING_ORDER_NOT_OPEN`/`SUBCONTRACTED` iş listesini tazeler, `CLIENT_TOKEN_COLLISION` modal ("yeni koşum olarak aç" token'ı sıfırlar). · bekçi: `runPayload.test.ts` <sub>(DOKUMA-IS-EMRI §3.9 (1) · §2.3; 0c 2026-09-14)</sub>
- **[ÇEKİRDEK]** Koşum uçları tablet için AYRI izin kodu AÇMAZ: `POST /machine-runs` · `/:id/close` → `requireAnyPermission("loom:run", "mobile:dokuma")`, `/:id/revoke` → `mobile:dokuma-geri-al` (doff geri alma ile AYNI yetenek — ikisi de defterden satır düşürür, geri alma modalı sebep zorunlu); iş emri (`GET /weaving-orders`, `/:id`) · desen · renk listeleri `mobile:dokuma` ile okunur. `loom:run`/`loom:run-revoke` ekransız kalır (panel koşum yüzeyi yok, kod API/entegrasyon için). · bekçi: `test_mobile_screen_permissions` (revoke = yetenek muafı) + `test_route_auth_coverage` <sub>(1e hükmü J.3 genişletildi; 0c 2026-09-14)</sub>
- **[ÇEKİRDEK]** Tablet DURUŞ paneli MAKİNE bazlıdır (hat değil — `one_open_per_machine` seddi): açık duruş geçen süresiyle çizilir; "Duruş bildir" `ReasonPresetPicker(MACHINE_STOP)` ile sebep İSTEĞE BAĞLI (verilmezse sunucu `requiresReason` borcu yazar, panel "sebep ata" çizer — ilk sınıflandırma tablette `mobile:dokuma`, YENİDEN sınıflandırma yalnız web `loom:classify`); "Çalıştı" kapatır (`endedAt` basılan an); kimlik SAAT DEĞİL TOKEN (`clientToken` = `stopKey`, deneme başına bir; parmak izi makine · sebep). `beamSlot` bu dilimde HİÇ gönderilmez — `Machine.warpBeamSlots` kolonu yok, yuva sorulamaz, NULL = "atanmamış" kovası. 409 ADIYLA: `SHIFT_CANCELLED`/`SHIFT_SEALED` "vardiya iptal/mühürlü", `CLIENT_TOKEN_COLLISION` modal ("yeni duruş olarak aç"), `STOP_*` liste tazeler, `REASON_CODE_INVALID` katalog tazeler. · bekçi: `stopPayload.test.ts` <sub>(DOKUMA-IS-EMRI §3.9 (2); 1e hükmü 2026-09-14: `source` izinden — `loom:manual-entry` taşıyan SUPERVISOR, değilse OPERATOR; yuva F4 şema penceresine kadar NULL)</sub>
- **[ÇEKİRDEK]** Duruş uçları tablet için AYRI izin kodu AÇMAZ (`mobile:tezgah-durus` taslağı DOĞMADI): `GET/POST /machine-stops`, `/:id/close`, `/:id/classify` → `requireAnyPermission(<web>, "mobile:dokuma")`, `/:id/revoke` → `mobile:dokuma-geri-al` (doff · koşum · duruş aynı yetenek: üçü de defterden satır düşürür), `/:id/reclassify` web-only. `loom:manual-entry`/`loom:classify` ekransız kalır. · bekçi: `test_mobile_screen_permissions` (revoke muafı) + `test_route_auth_coverage` <sub>(0c 2026-09-14)</sub>
- **[ÇEKİRDEK]** `dokuma.enabled` tabletteki kapısı `useVisibleScreens` koşuludur: `SCREEN_MODULE.Dokuma = 'dokumaEnabled'` (kataloğun aynası, `test_screen_catalog §3b`), etkin değer `production && dokuma`, satır-yok yönü KAPALI (fail-closed) — referans fabrikada Tezgah kartı ne kartta ne navigator'da; KK1'deki iki soru ("dokuma mı · hangi indirmeden") AYRI dilim. · bekçi: `useVisibleScreens.test.ts` + `screenModules.test.ts` <sub>(§3.9 E; 1e hükmü J.7)</sub>
- **[ÇEKİRDEK]** `ReasonPresetKind.MACHINE_STOP` için APK'ya gömülü zemin ZORUNLUDUR (`constants/loomStopReasons.ts`, GERÇEK katalog kodları — kind metin saklamaz) — zeminsiz kalırsa sunucusuz katalog boş döner, sebep zorunlu olan duruş kaydedilemez ve tezgah ekranda kilitlenir. · bekçi: `Teks-Erp/scripts/test_reason_preset_kind_parity.ts` (on iki aynayı Prisma enum'una karşı iki yönlü ölçer) + `test_loom_stop_zemin` (zemin İÇERİĞİ: kod · etiket · sıra sunucu kataloğuyla birebir, hook dalı zemini okuyor; eski `LOOM_STOP` adı 0c 2026-09-14'te düzeltildi — enum adı `MACHINE_STOP`) <sub>(DOKUMA-IS-EMRI §3.4)</sub>
- **[ÇEKİRDEK]** `warpBeamSlots > 1` olan tezgahta duruş HANGİ YUVA sorar; bilinmiyorsa NULL kalır ve Pareto'da "atanmamış" kovasında AYRI gösterilir — tahminle bir levende yazılmaz. `<= 1` ise alan hiç çizilmez. <sub>(DOKUMA-TEZGAH §2.7)</sub>
- **[ÇEKİRDEK]** Ham/dokuma ayrımı KK1'de ÇIKARILMAZ, SORULUR: `dokuma.enabled` açık fabrikada gelen top ya dokunmuş ya satın alınmıştır ve ikisi aynı gün olur; bayrak kapalıyken KK1 bugünküyle birebir aynıdır. KK1 dilimi (0c 2026-09-14): iki soru `DoffLinkPicker` ("Bu top dokuma mı?" → "hangi indirmeden?", `unlinked=true` listesi makine VERİLMEDEN — masa KK1 hepsini görür, tezgah başı eşleşmesini backend denetler, hüküm (a)); bağ AÇIK LİSTE seçimi, cevap yoksa `doffEventId: null`; yarı mamulle birlikte verilemez; bayrak kapalıyken payload'a alan HİÇ girmez. · bekçi: `doffLink.test.ts` (kapalı → alan yok · dokuma+seçim yok → null · yarı mamulde gizli) + `test_mobile_screen_permissions` (KK1 → `GET /machine-doffs` guard `mobile:kk1`) <sub>(DOKUMA-IS-EMRI §3.5, §3.9 D; 1e hükmü (a))</sub>
- **[ÇEKİRDEK]** Kapalı modülün YAZMA yolu yoktur, TARİHSEL BAĞ dahil: `POST /rolls/initial-entry` `doffEventId` taşıyorsa dokuma bayrağı gövdede OKUNUR ve kapalıyken 403 `MODULE_DISABLED` (modul `dokuma`) — KK1 rotası `requireDokumaEnabled` taşımaz (bayrak kapalı fabrikada KK1 birebir), kapı bu yüzden gövdededir ve servis çağrısından ÖNCE durur. · bekçi: `test_dokuma_regime_gate §8` <sub>(1e E1 hükmü 2026-09-14; 47 İŞ 4)</sub>

### Tuzaklar

- **[ÇEKİRDEK]** `source:'SIMULATED'` beyanı bugün YALNIZ kantar (SCALE) yolunda vardır, metre (METER) yolunda YOKTUR — doff sayacı bir ölçüm cihazıdır ve beyanı baştan taşımalıdır, yoksa aynı boşluk yeni bir yerde kurulur. <sub>(DOKUMA-IS-EMRI §3.7)</sub>
- **[ÇEKİRDEK]** Doff anında sayaç değeri (`counterAtDoff`) kaydedilmezse operatörün sayacı sıfırlaması ile 16-bit sarma AYIRT EDİLEMEZ; değeri kaydetmek belirsizliği kaynağında kapatır ve sıfırlamayı beyan edilmiş bir olaya çevirir. <sub>(DOKUMA-TEZGAH § sayaç kalitesi; DOKUMA-IS-EMRI §3.8)</sub>
- **[ÇEKİRDEK]** Beş dev ekran (Tambur · FasonKabul · KK1 · KursunQc · FasonSevk) `max-lines` MUAFIDIR ve emsal ALINMAZ — muafiyet bir borçtur, bir kalıp değil; yeni ekranın emsali `screens/Modules/HizliIsEmri/`dir. <sub>(`docs/standart/MOBIL.md` [MO-03])</sub>

## Bekçiler — bu alana dokununca koş

`cd Teks-Erp && npx tsx scripts/run-all-tests.ts <ad-parçası>`

Backend: `test_uretim_belge_zinciri`, `test_tablet_context_prefill`, `test_station_kind_warping`, `test_machine_stop_manual`, `test_loom_stop_zemin`, `test_warp_beam_lifecycle`, `test_warp_beam_tablet`, `test_mobile_screen_permissions`, `test_mobil_enum_aynasi`, `test_devere_regime_gate`, `test_dokuma_regime_gate`, `test_audit_labels`, `test_reason_preset_kind_parity`, `test_db_invariants`, `test_timestamptz_contract`, `test_machine_run`, `test_hard_delete_guard_coverage`, `test_master_data_merge_fk_coverage`, `test_production_line`, `test_machine_stop`, `test_weaving_order`, `test_loom_stop_ground`, `test_dokuma_rapor_onkosullari`, `test_production_regime_gate`, `test_permission_catalog`, `test_role_template_catalog`, `test_swagger_spec`, `test_loom_lists`, `test_machine_doff_source`, `test_station_kind_weaving`, `test_screen_catalog`, `test_defter_ters_yol`, `test_shift_calendar_job`, `test_schema_drift`, `test_snapshot_kolonlari`, `test_machine_shift_terms`, `test_machine_efficiency_formula`, `test_machine_shift_seal`, `test_dokuma_rapor_cikti`, `test_machine_shadow_mode`, `test_yarn_lot`, `test_yarn_stock`, `test_feature_flag_contract`, `test_subcontractor_dispatch_beam`, `test_subcontractor_weaving`, `test_iplik_regime_gate`, `test_warp_beam_mount`, `test_machine_run_beam_overlap`, `test_consistency`, `test_machine_shift_line_stat`, `test_station_devere_fields`, `test_subcontractor_dispatch_yarn`, `test_warp_beam_auto_consume`, `test_warp_beam_set`, `test_yarn_lot_quality`

İstemci: `doffLink.test.ts` (KK1 dokuma bağı: bayrak kapalı → alan yok · seçim yok → null · yarı mamulde gizli), `doffAttempt.test.ts` (token yalnız belirsiz hatada yapışır; parmak izi üç alan), `doffPayload.test.ts` (openSchema birebir · SIMULATED beyanı · 409 kodları eylem · replay BAYRAKTAN `idempotent`, metinden değil), `runPayload.test.ts` (koşum yükü openSchema birebir · iş ön-dolu kilitsiz · hedef devir boş → null · kapanış sayacı boş → null · parmak izi dört alan · 409 kodları eylem), `stopPayload.test.ts` (duruş yükü openSchema/classifySchema birebir · sebep açılışta isteğe bağlı, atamada zorunlu · beamSlot gönderilmez · parmak izi makine · sebep · geçen süre biçimi · 409 kodları adıyla), `stop-regime.test.ts` (Tezgah Duruşları karosu yalnız `dokumaEnabled` ile; süre/tarih biçimi), `warp-beam-regime.test.ts` (Leventler karosu yalnız `devereEnabled` ile; yedi durum ↔ rozet sözlüğü; nominal kg formülü aynası 816,667), `entry-warnings.test.ts` (top girişi sunucu uyarıları aynen; boş satır düşer), `beam-undo.test.ts` (Faz 3 geri alma adayları LIFO aynası: geri alınmış olay aday değil, WOUND/SHIP_OUT bu uçtan değil, tüketimler LIFO dışı; tezgah hücresi; canlı levent yüklemi; bilinmeyen durum rozeti), `entryWarnings.test.ts` (KK1 kayıt uyarı toast'ı: yoksa null · metin aynen birleşik · geciken kayıtta 'Kaydedildi' demez), `beamPayload.test.ts` (plan/sarım yükü backend şemasıyla birebir; raşel takımı adet 1 → alan yok, N → count+önek, iplik TOPLAM gider), `tezgahPayload.test.ts` (tablet Faz 3: Tak yükü mountSchema birebir · yöntem yalnız zorunlu ayarda · zorunlu ayarda başlangıç "şimdi" · yuvasız makine red · tüketim kalanı aşamaz · söküm/bitiş boş artık → null), `tile-visibility.test.ts` (karo ↔ palet aynı yüklem nesnesi; `permissionAny` route ile birebir), `stationScreens.test.ts` (WEAVING ↔ Dokuma iki yön), `useVisibleScreens.test.ts` (Dokuma kartı yalnız dokuma açıkken; üretime bağlı), `screenModules.test.ts`, `weaving-regime.test.ts` (Dokuma İşleri karosu yalnız `dokumaEnabled` ile; yüklem üretime bakmaz — zincir bağlamı kuran hook'ta), `tile-visibility.test.ts` (karo ↔ palet aynı yüklem nesnesi, koşullu karo kümesi açık sayılır), `dokuma-regime.test.ts` (Raporlar hub'ı Dokuma karosu `featureFlag: dokumaEnabled` + `report:production`; `regimePredicate` kapalıyken false; `formatPct(null)` = "ölçülemedi"; SIMULATED ≠ OPERATOR etiketi; dört alt rapor üç segment), `command-entries.test.ts` (rapor girişi kategori izni; dokuma istisnası `report:production` adıyla)

> ⚠️ **Yukarıdaki `Backend:` satırı KAPININ okuduğu biçimdir, süs değil.** `test_identity_ledger.ts:158` alan koşum listesini yalnız `## Bekçiler` başlığından sonraki `Backend:` satırından ayrıştırır (`` `ad` `` aralarında). Aşağıdaki madde imli açıklamalar **insan içindir ve kapı onları GÖRMEZ**: bu dosya 2026-09-13'te madde imli listeyi taşıyordu ve `test_machine_run` tarif edilmiş olduğu hâlde kapı onu **B-d (KAPSAM eksiği)** saydı. ⇒ *Bir belge bölümünü doldurmak, o bölümü okuyan kapıyı beslemek değildir — kapı bölümü değil BİÇİMİ okur.* Yeni bekçi **iki yere birden** yazılır: `Backend:` satırına (bağ) ve aşağıya (tarif).

**Bugün VAR olanlar:**
- `test_devere_regime_gate` — devere kapısının üç halkayı (ticaret → iplik → devere) ELLE ölçtüğü; dokuma inince kardeş zincir aynı biçimde ölçülür.
- **`test_dokuma_regime_gate`** (2026-09-13, ekran dilimiyle doğdu; 33 ölçüm, DB'siz) — §2 ön koşul (production) ÖNCE · §3 403 eksik olanı söyler · §4 bağımlılık tablosu + tezgah KARDEŞLİĞİ (biri diğerinin ön koşulu değil) · §5 satır yokken KAPALI · §6 üç router kapılı, `verifyToken`dan sonra, her uçtan önce, taşıyan ⊆ liste, dokuma modeline dokunan her router kapılı · **§7 "kapalıyken sıfır fark"**: dokuma uçlarını çağıran her istemci dosyası allowlist'te (bugün tek: `WeavingOrders/service.ts`), o ekranın manifestosu `dokumaEnabled`, karosu saf yüklemle `dokumaEnabled` okuyor, route'u `weavingorder:read`. Beş negatif sonda (3/1/1/1/1 ❌, md5 ile geri alındı) başlıkta.
- `test_audit_labels` — şemadaki her enum değerinin Electron karşılığı (yeni dokuma enum'ları beyan edilmeden geçemez).
- `test_reason_preset_kind_parity` — `LOOM_STOP` aynalarının iki yönlü eşitliği.
- `test_db_invariants` — yeni CHECK/partial index/trigger envantere yazılmadan geçmez.
- `test_timestamptz_contract` — yeni `DateTime` alanları.
- **`test_machine_run`** (2026-09-13, P2 ile doğdu; P2b'de 11 → 39 ölçüm) — §1–§4 sedler + CHECK · **§5 TOCTOU penceresi** (gate-tx + `pg_blocking_pids`, kaybedenin kodu `MACHINE_RUN_RACE`; ~~SED ölçümüdür~~ 2026-09-13'te pencere ölçümüne çevrildi) · §6 silme guard'ı ters yönü · §7 yüklem çağrısı · §8 precheck (§5'in körlük zemini) · §9 kapanış terimleri · §10 geri alma · §11 token replay dört durum · §12 dokuma işi bağı + iptalle yarış. Negatif sondalar: yüklem çağrısı kaldırılınca 2 ❌ · iş claim'i kaldırılınca 3 ❌ · kapanış claim'inden `closedTermsAt IS NULL` düşünce 2 ❌.
- **`test_machine_doff_source`** (2026-09-13, P3b doff backend ile doğdu; §3.8c kalemleriyle 20 → **31 ölçüm**) — §1 replay dört durum + replay `warnings` (sıralı §1d, paralel P2002 dalı §1e) · §2 koşum bağı (koşumsuz → `warnings` kaybı söyler; başka makine 409; koşumlu-işsiz dördüncü kova §2c) · §3 tx-A stok defterine satır/top yazmaz (fikstür ürününe daraltılmış sayım) · §4 KK1 bağı: üç red + masa KK1'de bağ kabul (§4e) + HTTP şeması alanı taşır/yarı mamulle reddeder (§4f) · §5 `DOFF_CANCEL` (iptal edilmiş topla da 409; mesaj çare önermez §5b'; 21 topta sayı kırpılmaz §5e; P2028 → 409 §5f) · **§5b iki pencere** (tx-B `FOR UPDATE`; kanıt bloklanan çağrının sırası, taban 3 ms) · §6 kod biçimi + **8029 sıra kanıtı** (elle tutulan kilit kaydı bloklar §6b-1/2) + 25 paralel 25 tekil / 0 hata (§6b-3). İlk koşumda gerçek kusur: revoke claim'i kilitte bloklanıp ESKİ snapshot'la yüklemi geçiyordu (EvalPlanQual alt sorguyu tazelemez) — kilit ayrı ifadede önce alındı. Dokuz negatif sonda, her biri hedeflediği bölümü kırmızı yaptı (tablo `DOKUMA-IS-EMRI` §3.8b ⑤).
- `test_production_line` — §1–§3 yüklem/CHECK/varsayılan + **§4 çağrıldığı yol** (P4b borcunun kapanışı; koşum açan servisten 3. hat → 400).
- `test_hard_delete_guard_coverage` — `Machine`e gelen her FK ya guard'da sayılıyor ya gerekçeli muaf. ⚠️ Guard'ları **REGEX** okur: sayaç `prisma.<model>.count({ where: { <kolon>:` biçiminde **DÜZ** yazılmalı, helper'a sarmalanırsa bekçi "guard yok" sayar (ölçüldü 2026-09-13, negatif sonda).
- `test_master_data_merge_fk_coverage` — şemadan türer: birleştirilebilir varlığa (`item`·`color`·`customer`·`subcontractor`) giden her yeni FK `MERGE_MAP`e satır ister.
- **`test_weaving_order`** (2026-09-13, yazma yüzeyiyle doğdu) — §0 `IN_PROGRESS` yazan yol 0 (dal ERİŞİLEMEZ; P2b açınca KIRMIZI verir, o gün davranış yazılır) · §1 numara/XOR/kumaş/plannedM/tarih · §2 replay dört durum · §3 düzenleme yalnız açıkken · §4 kapanış 409 koşum ADIYLA + `plannedM` kapatmaz · §5 iptal defter satırına dokunmaz · §6 liste süzgeci sunucuda + cursor · §7 audit tx dışında · §8 DB CHECK servisi atlayan yazımı reddeder. ⚠️ HTTP katmanı (Zod/izin/kapı) ÖLÇÜLMEZ — statik kapılar + d9 sondaları.

**HENÜZ YOK (şema inerken yazılacak, ne ölçeceği yazılı):**

## Raporların sözleşmesi — üç soru cümlesi

> **Bir rapor bir ÖLÇÜM TANIMIDIR.** Yanlış tanımlanmış bir rapor yanlış bir bekçiden
> pahalıdır: kimse onu sorgulamaz, **karar ondan verilir.** Bu yüzden her cümle
> **yanlışlanabilir** yazılır ve yanlışlanma koşulu yanına konur.

**① RANDIMAN** — **İNDİ 2026-09-14 (Dilim 4): `GET /api/reports/dokuma/randiman`** (`report:production`, `requireDokumaEnabled`; satırlar A·P·E AYRI + toplam Σ/Σ + `olculemedi` + `kaynakKirilimi`; bekçi `test_dokuma_rapor_cikti ①`)
> *"Şu tezgah, şu vardiyada, planlı süresinin %A'sında çalıştı (KULLANILABİLİRLİK); çalıştığı sürede hedef devrin %P'sini tutturdu (PERFORMANS) — ve bu iki sayı **ÇARPILMAZ, ayrı sunulur**."*
> **Yanlışlanır:** kova toplamı planlı süreyi aşarsa · `targetUnitsPerMin` NULL iken bir performans sayısı basılırsa.

⚠️ **PAYDA TEK DEĞİL ÜÇ** ve hangisinin hangi orana girdiğini **kova** belirler (ISO 22400-2 + SEMI E10 + Nakajima; `MachineStopLossClass`):
`NON_SCHEDULED` → **hiçbir paydada yok** · `PLANNED` → planlı süreden düşülür · `UNPLANNED`/`SETUP` → KULLANILABİLİRLİK kaybı · `MINOR` → **PERFORMANS** kaybı, kullanılabilirlik DEĞİL.
⇒ *"Randıman"* tek bir sayı değildir; tek yüzdeye çökertilirse üç farklı büyüklük aynı adla anılır.
⚠️ **DÖRDÜNCÜ DURUM: "ölçülemedi".** `MachineRun.targetUnitsPerMin` NULL → `MachineSpec.nominalUnitsPerMin` yedeği → o da NULL ise **performans HESAPLANMAZ**, rapor *"P: ölçülemedi"* der. Boş hücre değil **beyandır**. (`picksPerRev` yalnız TAVANA girer, paydaya ASLA.)

**② DURUŞ PARETO** — **İNDİ 2026-09-14 (Dilim 4): `GET /api/reports/dokuma/durus-pareto`** (`sebepler` sebep×sınıf · `mikroDuruslar` ayrı blok · `siniflandirilmamis` · `atanmamis` kesişen levent ekseni · mühürlü karnede son kuşak, etiket donmuş; bekçi `test_dokuma_rapor_cikti ②`)
> *"Şu tezgahta şu aralıkta en çok süreyi yiyen duruş SEBEBİ şudur (N olay, T dakika), ve bu sebep şu KOVAYA düşer."*
> **Yanlışlanır:** `NON_SCHEDULED` bir paydaya girerse · `MINOR` bir SEBEP gibi listelenirse · serbest metin bir satır üretirse.

⚠️ **İKİ EKSEN, KARIŞTIRILMAZ:** `ReasonPreset.code` = **SEBEP** (sıralama ekseni) · `MachineStopLossClass` = **SÜRE SINIFI** (gruplama ekseni). Tasarımın şerhi: *"`MINOR` bir SEBEP SINIFI DEĞİL, bir SÜRE SINIFIDIR"* — yalnız `tezgah.stopEventMinSeconds` altındaki duruşlardan türer ve `ReasonPreset.stopLossClass`a ASLA yazılamaz.
⇒ Tek eksende sunulursa *"mikro duruşlar"* bir sebep gibi görünür ve **operatöre yanlış iş verir** — raporun yanlış olmasının en pahalı biçimi.
⚠️ Ayrım denetim ekranında bir override'la yaşar (`MACHINE_STOP_EVENT.lossClass` → *"Mikro duruş (eşik altı)"*); override silinirse ekran duruşa `DefectSeverity` dilinde *"Küçük"* der. Bu **korumasızdı** (ölçüldü 2026-09-13: silince Electron 75/75, backend 22/0 yeşil kaldı) — `test_dokuma_rapor_onkosullari` §6 kapatır.
⚠️ Sebep **serbest metin DEĞİL**: `reasonCode` → `ReasonPreset.code` (FK'SIZ, `Roll.cancelReasonCode` emsali) ve etiket **kopyalanıp DONAR** — katalog değişse geçmiş rapor değişmez.

**③ VARDİYA KARNESİ** — **İNDİ 2026-09-14 (Dilim 4): `GET /api/reports/dokuma/vardiya-karnesi?factoryDay[&shiftDefinitionId]`** (`ozet: olculen · elle · simule · cikarim · olculemedi · toplamSatir`; bekçi `test_dokuma_rapor_cikti ③`)
> *"A vardiyası (fabrika günü X, 08:00+8s) şu tezgahlarda şu üretimi yaptı; satırların K'sı ÖLÇÜLDÜ, L'si ELLE girildi, M'si ölçülemedi."*
> **Yanlışlanır:** gün sınırı `src/constants/time.ts` dışından gelirse · `source` kırılımı toplamda erirse · mühürlenmiş karne sonradan değişirse.

## Raporların İKİ DEĞİŞMEZİ

**① ELLE GİRİŞ BİRİNCİ SINIFTIR ve toplamda ERİMEZ.**
Veri çekilemeyen tezgahlar için elle giriş olacak ⇒ raporun **her toplam satırı** `MachineShiftStat.source` kırılımını taşır. **Tek yüzdeye çökertme YASAK:** *"%78"* denmez, *"%78 (ölçülen 62, elle 16)"* denir.
⚠️ `SIMULATED` `OPERATOR`dan **AYRI** durur — farklı güven sınıfı; kök `CLAUDE.md` *"uydurulmuş değer `source:'SIMULATED'` beyanıyla gider"* diyor ve birleştirmek o beyanı **yok eder**.
**Bekçisi İKİ KATMANLI.** ÇIKTI bekçisi (toplam = Σkırılım **ve** kırılım basılıyor mu) **İNDİ 2026-09-14: `test_dokuma_rapor_cikti`** (①d/①e/③b/③c; negatif sonda: `SIMULATED` `OPERATOR`a çökertilince ❌, kırılım tek kovaya çökertilince ❌); `test_dokuma_rapor_onkosullari §4` mandalı ÜÇ SONUÇLUDUR (uç yok → ⏭ beyan · uç var ∧ bekçi yok → ❌ · bekçi var → ✅) ve bugün ✅. ÖN KOŞUL bekçisi bugün koşar (`SIMULATED` ile `OPERATOR` ayrı iki enum değeri · her `MachineDataSource` kolonu `@default`suz) ve envanter-dışı bir kaynak kolonu doğduğu gün KIRMIZI vererek SINIFLANDIRMA ister. ⚠️ Mandal 2026-09-13'te ilk kez uyandı (`doff_events.counterSource`, dokuma P3) ve **mesajı yanıldı**: *"taşıyıcı geldi"* diyordu, oysa kolon bir OLAYIN kökeniydi (doff'taki sayaç okuması). ⇒ Yeni kaynak kolonu iki sınıftan biridir ve şemadan OKUNARAK ayrılır: raporun SATIRINDA yaşayan (taşıyıcı) ↔ bir OLAYIN kökenini taşıyan. ~~**Kapanır:** `machine_shift_stats` … indiğinde ③'ün ÇIKTI bekçisi yazılır~~ **KAPANDI 2026-09-14 (Dilim 1 taşıyıcı `machine_shift_stats.source` (a) sınıfı · Dilim 4 `test_dokuma_rapor_cikti`):** iki yönlü sonda ölçüldü — (a) kırılım tek kovaya çökertilince ①d/③b ❌ (b) `SIMULATED` `OPERATOR`a katılınca ①d/②i/③b ❌.

**② UFUK YAZILIR — ve HER UFKUN TEK KAYNAĞI, AYRI ADI VAR.**
Rapor *"her şey tutuyor"* demez, *"şu tarihten sonrası ölçülü"* der.
⚠️ **Dokuma ufku ≠ defter ufku.** Ölçüldü 2026-09-13: `DEFTER_UFKU` **bekçi tarafında** yaşıyor (`scripts/test_consistency.ts:121`), ürün tarafında karşılığı **yok** ve bugün değeri **`null`**. İkisi farklı sorulara cevap verir — *defter ufku* = depoya yazan son kapısız yolun kapandığı gün; *dokuma ufku* = tezgah verisinin toplanmaya başladığı gün. ⇒ Dokuma raporu `DEFTER_UFKU`yu **OKUMAZ**; kendi ufkunu `src/constants/` altında **ayrı adla** ilan eder. **İNDİ 2026-09-14 (Dilim 4): `constants/dokuma-ufku.ts` `LOOM_HORIZON_DAY = "2026-09-14"`** (belgelerdeki adıyla DOKUMA_UFKU; kod tanımlayıcısı İngilizce — `test_identifier_language`; Faz 1b yazma yüzeyi + karnenin indiği gün; değiştirmek hükümdür), her rapor `meta.ufuk` + `meta.ufukOncesiSatir` basar; `ledger-horizon`/`DEFTER_UFKU` import'u dokuma raporunda YASAK (`test_dokuma_rapor_cikti ④a`).
📌 Birleştirmek, iki farklı güvenilirlik sınırını tek sayıya çökertmek olurdu — ①'de `source` için reddedilen şeyin aynısı.

~~⚠️ **Beş tablonun beşi de bugün ŞEMADA YOK**~~ **Beşi de İNDİ (2026-09-14; sonuncusu `MachineShiftStat` + `MachineShiftStopBreakdown` + `MachineShiftStatSeal`, migration `20260914130000`, dokuma raporları Dilim 1)** — bu bölüm raporların SÖZLEŞMESİDİR; uygulama Dilim 2–4'te (terim helper'ı · mühür · üç uç), tasarım özeti `docs/design/DOKUMA-RAPOR-BACKEND-TASARIM-OZETI.md`.

## Geçersiz kılınan kurallar — bunlara UYMA

- **KISMI** `DOKUMA-IS-EMRI §3.6` ("dört tablet eyleminin dördü de çevrimdışı KUYRUĞA girer") → `R:2026-09-14__tablet-doff-dilimi` + `R:2026-09-14__tablet-kosum-dilimi`: doff ve koşum ONLINE-ONLY (`networkMode:'always'`), kuyruğa girmez; çevrimdışıyken buton kilitli, kalıcı düşüş anlık toast; token yine yalnız belirsiz hatada yapışır. Duruş · levent kendi diliminde karar alır — bu satır ONLARI kapsamaz. GEÇERSİZ → 2026-09-14 (1e hükmü J.5; `kk1.md` "kuyruk kalktı" kararının uzantısı).

## Açık sorular

- **Koşumsuz doff oranı ölçülmedi.** Sık çıkıyorsa bu bir veri modeli kararı değil bir ARAYÜZ kusurudur (tablet koşumu kendisi açmalı) — karne bu oranı basmalı ve sayı görülmeden karar verilmemeli.
- **`unitsPerCm`in kalıcı evi belirsiz** (kaynak: `WarpSpec` ailesi; **sert bağımlılık eklenmez**). Faz 1–2'de elle girilir ve donar. <sub>(DOKUMA-TEZGAH §10/#9)</sub>
- ~~**Fason dokuma bloke:** `SubcontractorDispatchItem.rollId` **NOT NULL**~~ **F1 İNDİ 2026-09-14** — kalem polimorfik (ROLL | WARP_BEAM), levent fasona gider ve döner (`fason.md` kuralı, bekçi `test_subcontractor_dispatch_beam`); ~~iplik (`YARN`) F3~~ **G1 İNDİ 2026-09-15** (iplik kalemi kg + `SUBCONTRACT_*` defteri, bekçi `test_subcontractor_dispatch_yarn`), haşıl verisi F2. **G2 İNDİ 2026-09-14** — fason sevk/makbuz BAŞLIĞI da polimorfik (`weavingOrderId` XOR iş emri kolonları): dokuma işi adına levent fasona gider, TOP makbuzla `entrySource=WEAVING` doğar (`subcontractor-weaving.service`, uç `/api/subcontractor-weaving`, `fason.md` kuralı, bekçi `test_subcontractor_weaving`); panel/tablet yüzeyi G2p.
- ~~**Advisory uzay numarası kesinleşmedi**~~ **KAPANDI 2026-09-13** — ölçüldü: `WEAVING_ORDER_LOCK_NS = 8032` indi (`helpers/weaving-order.helper.ts:24`, envanter `period-guard.helper.ts:57`). ⚠️ Önerilen `8034` **kullanılmadı** ve bu satır bir gün boyunca bayat kaldı. ⇒ *Numara çakışması sessizdir* uyarısı doğruydu ama **eksikti: bayat REZERVASYON da sessizdir.** Sonraki uzay için numara buradan değil, **`period-guard.helper.ts` başlığından** okunur.
  ⚠️ **`MachineRun` advisory uzayı ALMADI ve almayacak** (2026-09-13, P2): koşum tekilliği DB seddiyle kurulur (iki partial unique), yarışın kaybedeni P2002 alır ve servis 409'a çevirir — `work_sessions` emsali. *Her tekillik sorusu bir kilit istemez; sed yeten yerde kilit ikinci bir yazar yolu açar.*
