# Dokuma · Dokuma işi · Doff

> Alan kural dosyası — bu alana dokunmadan ÖNCE okunur. Hikâye, ölçüm ve gerekçe tasarım belgelerinde; burada yalnız bugün geçerli kural. Sınıf: **[ÇEKİRDEK]** her kurulumda aynı · **[PROFİL]** bu fabrikanın seçimi.

> ⚠️ **BU ALAN KISMEN KÂĞITTADIR — şema PARÇA PARÇA iniyor.** Ölçüldü 2026-09-13: **`WeavingOrder` İNDİ** (P1, `77b69da9`) · **`MachineRun` İNDİ** (P2, `c07396e6`) · **`MachineSpec` + `LoomShedType` + `MachineMonitoringState` İNDİ** (P4, migration `20260913230000`). **`MachineStopEvent` + `MachineStopReclass` + `ShiftDefinition` + `ShiftInstance` + `MachineCollector` + `MachineCollectorLink` + 4 enum İNDİ** (P2b-1, migration `20260913240000`). **Hâlâ YOK:** `Machine.warpBeamSlots` (LEVENT belgesinin tek parçalı sürümüne ait) · `ReasonPresetKind.MACHINE_STOP` + `ReasonPreset.stopLossClass` + CHECK + katalog (P2b-2) · `DoffEvent` + `RollEntrySource.WEAVING` (P3). Sıra: **P1 ✅ → P2 ✅ → P4 ✅ → P4b ✅ → P2b-1 ✅ → P2b-2 → P3.**
>
> ✅ **P4b İNDİ:** `Machine.productionLineCount` (`@default(1)`, CHECK `>= 1`) + `assertProductionLineValid` yüklemi. ~~⚠️ Yüklem BUGÜN HİÇBİR YERDEN ÇAĞRILMIYOR~~ **KOŞUL SAĞLANDI 2026-09-13 (P2b koşum yüzeyi):** ilk çağrı yeri `openMachineRun` → `helpers/machine-run-open.helper.ts` (`resolveOpenContext`); "çağrıldığı YOL" ayağı `test_production_line §4` + `test_machine_run §7` (negatif sonda: çağrı satırı kaldırılınca 3 kırmızı).
> ✅ **P2b KOŞUM YAZMA YÜZEYİ İNDİ (2026-09-13, 6e):** `POST /api/machine-runs` · `/:id/close` · `/:id/revoke` — izin `loom:run` / `loom:run-revoke`, kapı `requireDokumaEnabled` (2026-09-13 ekran dilimiyle `requireProductionEnabled`ten geçti). Ekran YOK; tablet dilimi `requireAnyPermission("loom:run", ...MOBILE)` biçimine geçirir.
> 📌 Bir **guard** bir YOLU korur (yol yoksa erişilemez dal); bir **yüklem** bir SORUYU cevaplar ve çağrısız da doğru ya da yanlıştır ⇒ yazma yüzeyinden önce inebilir, ama bekçisi **davranışını** ölçmek zorundadır, varlığını değil.

---

## Ortak (backend + panel + tablet)

### Değişmezler

- **[ÇEKİRDEK]** Dokuma topun rotasında bir ADIM DEĞİLDİR; kendi kimliği, kendi yaşam döngüsü ve kendi defteri olan ayrı bir VARLIKTIR — `MachineRun` bir `WorkOrderStep`e bağlanmaz, `WeavingOrder`a bağlanır. <sub>(DOKUMA-IS-EMRI §1)</sub>
- **[ÇEKİRDEK]** `StationKind.WEAVING` yalnız OTURUM istasyonudur: tablet oturumu istasyon tabanlı olduğu için tezgah bir WEAVING istasyonudur (`SESSIONABLE_STATION_KINDS`, oturum izni `mobile:dokuma` — satır yoksa kapı fail-OPEN olur) ve YALNIZ MAKİNEYLE açılır (tezgah = makine; istasyon-modu 400 `STATION_MACHINE_ONLY`); rota şablonuna ve iş emri rotasına GİREMEZ — tek helper `assertStationsRoutable` (400 `STATION_NOT_ROUTABLE`), iki çağıran (`route.service.validateSteps` · `workorder.service.assertRouteRefsActive`). Panel istasyon formu türü yalnız `dokuma.enabled` açıkken çizer (mevcut değer korunur); tabletin `SessionStationKind`i ekran dilimine kadar WEAVING taşımaz (fail-closed, çökme yok); minVersion GEREKMEZ (referans fabrikada WEAVING istasyonu yok, onu yeni panel açar). · bekçi: `test_station_kind_weaving` (§1–§4, dört negatif sonda) + `test_audit_labels §4` (WEAVING çakışması `STATION.kind` override) + Electron `stationKindVisibility.test.ts` <sub>(DOKUMA-IS-EMRI §3.3 · §3.9 J.4; enum reçetesi 13 adım; 1e hükmü 2026-09-14; arşiv:2026-09-14)</sub>
- **[ÇEKİRDEK]** Tezgah LİSTELERİ backend'de doğar, ilk dilim yerel durumla YAPILMAZ: `GET /api/machine-runs?machineId&open=true` (açık koşumlar) · `GET /api/machine-doffs?machineId[&date]` (fabrika gününün indirmeleri, geri alınmış kapsam dışı) · `GET /api/machine-doffs?unlinked=true[&machineId][&sinceDays≤30]` (hiç top doğurmamış, geri alınmamış — KK1'in açık liste seçimi; makine OPSİYONEL, masa KK1 her tezgahı görür); üçü de `requireDokumaEnabled` altında ve `requireAnyPermission(loom:run · loom:doff · mobile:dokuma)` ile (ayrı `loom:read` YOK — liste yazma yüzeyinin yüzüdür); süzme SUNUCUDA, liste 200'de kırpılır `meta.total` kırpılmaz. · bekçi: `test_loom_lists` (§1–§5, dört negatif sonda) <sub>(DOKUMA-IS-EMRI §3.9 J.2 "Backend ÖNCE"; 1e 2026-09-14)</sub>
- **[ÇEKİRDEK]** Bir top ya fabrikanın DIŞINDAN gelir ya iş emri içinde BAŞKA BİR TOPTAN doğar (ana top tüketilir, metre korunur); dokuma bu ikisine ait olmayan ÜÇÜNCÜ sınıftır — iplikten metre YARATIR ve korunacak ana topu yoktur. <sub>(DOKUMA-IS-EMRI §1.1)</sub>
- **[ÇEKİRDEK]** Doff bir olay yazar, TOP DOĞURMAZ: dokunan kumaş `Roll` olarak KK1'de doğar (`entrySource = WEAVING`) ve oradan mevcut rotaya girer — ikinci bir giriş motoru yazılmaz, yol `createInitialEntry`den geçer. · <sub>(DOKUMA-IS-EMRI §3.5; `goods-receipt.service.ts:8`)</sub>
- **[ÇEKİRDEK]** Tezgahtan türetilen metre (`pickDelta ÷ unitsPerCm`) HİÇBİR YERDE stok yazmaz; tek miktar gerçeği `Roll` ölçümüdür. <sub>(DOKUMA-TEZGAH §5.6)</sub>
- **[ÇEKİRDEK]** İlerleme İKİ SAYIDIR ve toplanmaz: ÜRETİLEN (doff edilen topların ölçülen metresi — stok yazar, rapora girer) ve TEZGAHTA (türetilen — yalnız canlı ekran); `plannedM − ÜRETİLEN` kalanı verir, TEZGAHTA bu çıkarmaya girmez. <sub>(DOKUMA-IS-EMRI §2.4)</sub>
- **[ÇEKİRDEK]** Levent bitince koşum BİTMEZ — koşumu bitiren üçtür: desen/renk değişti · hedef devir değişti · iş bitti; levent değişimi `WarpBeamEvent`e yazılır ve koşum sürer. <sub>(DOKUMA-IS-EMRI §2.3)</sub>
- **[ÇEKİRDEK]** Talep metre, icra levent, çıktı toptur — üç ayrı eksen tek sayıya bindirilmez; metre↔levent çevrimi take-up ister ve take-up bugün YOKTUR, yani "bu iş kaç levent eder" HESAPLANMAZ. <sub>(DOKUMA-IS-EMRI §2.1)</sub>
- **[ÇEKİRDEK]** `MachineDataSource` alanları `@default` ALMAZ — sayaç/ölçüm değerini yazan her yol kaynağını AÇIKÇA beyan eder, `SIMULATED` dahil; kararı backend verir. <sub>(DOKUMA-IS-EMRI §3.8)</sub>
- **[ÇEKİRDEK]** Açık koşum tekilliği MAKİNE değil **ÜRETİM HATTI** başınadır (`machine_runs_one_open_per_prod_line_uq`) — çift enli tezgah yan yana iki ayrı kumaş koşar; yalnız makineye kilitlemek bunu yapısal olarak imkânsız kılar. Tek hatlı makinede `productionLineNo` sabit 1'dir ⇒ reddedilen küme değişmez. <sub>(DOKUMA-TEZGAH §2.8, §10/#23b)</sub>
- **[ÇEKİRDEK]** İzleme hâli TEK kolondur (`MachineSpec.monitoringState`, `OFF` doğar) — `isMonitored` gibi ikinci bir boolean AÇILMAZ: iki kolon "çift yüklem" sınıfıdır ve tek boolean, kanal kabul testinden bağımsız açılabildiği için ters bağlı bir röleyi %100 randımanla MÜHÜRLETİRDİ. Gölge mod bir reçete cümlesi değil bir DURUMDUR. <sub>(DOKUMA-TEZGAH §2.3)</sub>
- **[ÇEKİRDEK]** `MachineSpec` satırının VARLIĞI *"bu makine izleniyor"* demektir, *"bu makine bir dokuma tezgahıdır"* DEĞİL; ne olduğu ayrı bir etiketten okunur ve o etiket zorunlu bile değildir (`shedType IS NULL` zaten "bu makine o sınıftan değil" der). `Machine.kind` enum'u AÇILMAZ. <sub>(DOKUMA-TEZGAH §2.3)</sub>
- **[ÇEKİRDEK]** Duruşun kimliği SAAT DEĞİL TOKEN'DIR (`stopKey`, ajan üretimi UUID): açılış, kapanış ve yeniden gönderim AYNI anahtarla gelir. Kimliği `(makine, başlangıç, kaynak)` üçlüsünden kurup saniyeye yuvarlamak bir kimlik çözümü değil semptom bastırıcısıdır — 1 sn kayan tekrar gönderim yine ikinci satır açar. <sub>(DOKUMA-TEZGAH §2.7)</sub>
- **[ÇEKİRDEK]** `machine_stop_events`in iki seddi de (`one_open_per_machine` · `key_uq`) `revokedAt IS NULL` yüklemi taşır — geri alınmış duruş yer işgal etmez, yoksa yeni duruş açılamaz ve yeniden gönderim sonsuza dek reddedilir. <sub>(DOKUMA-TEZGAH §2.7)</sub>
- **[ÇEKİRDEK]** Duruşun BEŞ yazma yolu (aç · kapa · sınıfla · yeniden sınıfla · geri al) TEK serviste yaşar (`machine-stop.service`), her biri atomik claim + count-0 tanısıdır ve ikinci yazar doğmaz: ilk sınıflandırma `reasonCode IS NULL` claim'iyle YERİNDE yapılır ve kayıp sınıfı katalogdan KOPYALANIP DONAR; değer→değer değişimi kayıtlı KARARI değiştirir ve değişimin kendisi AYNI tx'te `MachineStopReclass` (from→to) satırıdır — ters yolu karşı kayıttır (aynı fonksiyon to→from), silme ya da damga değil; duruşun olguları (`startedAt` · `endedAt` · `durationSec` · `stopKey`) hiçbir yolda değişmez; geri alma damgadır ve seddi boşaltır. · bekçi: `test_machine_stop_manual.ts` (§4 claim · §5 defter + karşı kayıt · §6 kopya donmuş · §7 damga) · `test_defter_ters_yol.ts` §5 <sub>(arşiv:2026-09-14 Faz 1b; DOKUMA-TEZGAH §2.7)</sub>
- **[ÇEKİRDEK]** Mühür sınırı TEK kapıdan geçer: duruşu kapatan, sınıflandıran ve geri alan her yol `assertStopShiftWritableTx(tx, { shiftInstanceId, machineId })`ten geçer (`helpers/machine-stop-context.helper.ts`) — bugün yalnız iptal edilmiş vardiyayı reddeder (409 `SHIFT_CANCELLED`); mühür MAKİNE×VARDİYA karnesinin durumudur (`MachineShiftStat.sealState`, 01 sözleşmesi 2026-09-14: `findUnique({ machineId_shiftInstanceId })`, satır yoksa OPEN), inince aynı fonksiyon `SEALED` → 409 `SHIFT_SEALED` verir (yol: `loom:shift-unseal` → satır → RESEAL), çağıranlar değişmez; vardiya iptali ≠ mühür, ikisi de burada sorulur. · bekçi: `test_machine_stop_manual.ts` §8 (iptal vardiya ayağı) · Kapanır: `MachineShiftStat.sealState (makine×vardiya, 01 Faz 1a karne dilimi, migration 20260914040000+) şemaya inince assertStopShiftWritableTx mührü buradan okur ve test_machine_shift_seal iki ayak alır — mühürlü karnede kapa/sınıfla/geri al 409 SHIFT_SEALED · unseal sonrası revoke/reclass breakdown YENİ kuşakta düşer (ölçüldü 2026-09-14: şemada seal yok)` · Çapa: `test_machine_stop_manual` · Öncül: ölçüldü <sub>(DOKUMA-TEZGAH §2.7 mühür kutusu, §2.10)</sub>
- **[ÇEKİRDEK]** `MachineStopLossClass.MINOR` bir SÜRE sınıfıdır, SEBEP sınıfı DEĞİL: yalnız mikro-duruş eşiğinin altından TÜRER ve `ReasonPreset.stopLossClass`a ASLA yazılmaz. Denetim ekranında da ayrı Türkçe taşır (`MACHINE_STOP_EVENT.lossClass` override'ı) — global "Küçük" `DefectSeverity` dilindedir. <sub>(DOKUMA-TEZGAH §2.7, §10/#7)</sub>
- **[ÇEKİRDEK]** Bir sırrı taşıyan kolon, onu koruyan KURALLARLA aynı dilimde iner; tablo daha erken inebilir, sır inemez. `MachineCollector`ın token kolonları bu yüzden P2b-1'de YOK ve yokluğu şemada BEYAN EDİLİR — yazma yüzeyi olmadan güvenli görünen bir sır kolonu, kurallarından önce var olur ve ilk yazan kişi kuralları arayıp bulamaz. <sub>(1e hükmü 2026-09-13)</sub>
- **[ÇEKİRDEK]** `machine_runs`ın iki sedi `revokedAt IS NULL` yüklemini ŞART koşar (geri alınmış koşum yer işgal etmez), silme guard'ı ise `revokedAt`i BİLEREK SÜZMEZ (geri alınmış koşum da o makinede üretim yapıldığının kanıtıdır) — **iki ters yön, iki ayrı soru**; gerekçeleri `guarded-hard-remove.ts` → `machineRunCount`ta yan yana yazılıdır ve biri ötekine bakılarak "tutarlı" yapılmaz. <sub>(DOKUMA-TEZGAH §2.8)</sub>
- **[ÇEKİRDEK]** Koşum açılışında iki 409 kodu AYRI üretilir ve ayrım ölçümdür: precheck açık koşumu görürse `PRODUCTION_LINE_OCCUPIED` (kim koşuyor, `openRunId`), precheck geçip sed yakalarsa `MACHINE_RUN_RACE` — ikincisi yalnız TOCTOU penceresi gerçekten açılıp sedde kapandığında doğar; advisory kilit YOKTUR ve eklenmez. · bekçi: `test_machine_run §5` (gate-tx + `pg_blocking_pids`; pencere kurulamazsa "ölçülmedi" kırmızıdır) <sub>(`machine-run.service.ts`; `work_sessions` `MACHINE_OCCUPIED`/`SESSION_RACE` emsali)</sub>
- **[ÇEKİRDEK]** Koşum kapanışının TEK yazarı `closeMachineRunTx`tir; claim `WHERE closedTermsAt IS NULL ∧ startedAt ≤ endedAt`, count-0 tanısı tx içinde taze okunur (`RUN_ALREADY_CLOSED` · `RUN_REVOKED` · `RUN_END_BEFORE_START`). Duruş terimleri (`stopSecAtClose`/`stopCountAtClose`) bu dilimde NULL kalır — "ölçülmedi", 0 değil; ingest dilimi bu yazara ekler, ikinci yazar açmaz. · bekçi: `test_machine_run §9` <sub>(DOKUMA-TEZGAH §4 "kapanışta donar")</sub>
- **[ÇEKİRDEK]** Koşum geri alma bir DAMGADIR (`revokedAt`+`revokedById`+`revokeReason`, claim `WHERE revokedAt IS NULL`); açık ya da kapanmış koşum geri alınabilir, ileri satır (başlangıç · bitiş · donmuş terimler) DEĞİŞMEZ, ikinci geri alma sebebi EZMEZ (409 `RUN_ALREADY_REVOKED`). Ayrı izin `loom:run-revoke` (randımanın paydasını değiştirir). · bekçi: `test_machine_run §10` <sub>(kök `CLAUDE.md` § ters kayıt; `shipping:undo-dispatch` emsali)</sub>
- **[ÇEKİRDEK]** Koşum `clientToken` replay'i dört durumludur ve `assertMachineRunReplayAlive` `token-replay.helper`da yaşar: aynı yük → özgün koşum, başka yük → `CLIENT_TOKEN_COLLISION`, geri alınmış → `RUN_REVOKED` (canlıymış gibi dönmez). · bekçi: `test_machine_run §11` <sub>(`token-replay.helper.ts` başlığı)</sub>
- **[ÇEKİRDEK]** Dokuma işine bağlı koşum açılışı iş satırını `updateMany` claim'iyle KİLİTLER (`status ∈ {PLANNED, IN_PROGRESS} ∧ IN_HOUSE`, tx'in ilk ifadesi): eşzamanlı iptal/kapanış claim'i ya önce biter (409 `WEAVING_ORDER_NOT_OPEN`) ya bizim commit'imizi bekler ve açık koşumu görür. ⚠️ Karşı taraf SÖZLEŞMESİ: iş emri kapatma/iptal yolu koşum saymadan ÖNCE kendi claim'ini almalı, yoksa iki tx birbirini görmez. Fasona verilmiş işe koşum 400 `WEAVING_ORDER_SUBCONTRACTED`. · bekçi: `test_machine_run §12` (iptalle yarış gate-tx ile kurulur) <sub>(`touchWorkOrderTx` emsali)</sub>
- **[ÇEKİRDEK]** `startedAt`/`endedAt` istemci BEYANIDIR (`resolveEntryStamp` penceresi: ≤36 sa geçmiş, ≤5 dk gelecek); aralık dışı beyan 400 vermez, sunucu saatine düşer ve `ApiResponse.warnings` ile söylenir — bozuk RTC'li tablet üretimi durdurmaz. Aynı hatta önceki koşumun bitişinden ÖNCE başlayan beyan 409 `PRODUCTION_LINE_OVERLAP` (uygulama kontrolü, exclusion seddi yok). <sub>(`duplicate-guard.helper.ts` "NEDEN 400 DEĞİL")</sub>

### Yasaklar

- **[ÇEKİRDEK]** `Roll.doffEventId` ve `MachineRun.weavingOrderId` ASLA `null`'lanmaz — ileri damgayı `null`'lamak ters kayıt DEĞİLDİR (`dispatchedAt` · `weighedAt` · `invoicedAt` ailesi). <sub>(kök `CLAUDE.md` § Veri ve defter)</sub>
- **[ÇEKİRDEK]** `plannedM`e ulaşmak dokuma işini KAPATMAZ: o sayı geç gelen bir ölçüme (KK1 top metresi) dayanır ve otomatik kapanış tezgah koşarken işi kapatır — eşik bir BİLDİRİMDİR, bir karar değil. <sub>(DOKUMA-IS-EMRI §2.2a)</sub>
- **[ÇEKİRDEK]** Dokuma işine sipariş bağı açılmaz: karşılama `SackAllocation` ile sevk anında yazılır ve istenen şey aslında REZERVASYONDUR — rezervasyon bu üründe yoktur (profil kararı). <sub>(DOKUMA-IS-EMRI §2.2c; `MODUL-BAYRAK-TASARIM.md` #6)</sub>
- **[ÇEKİRDEK]** Kopuş/duruş sayaçları ÇOĞULLAŞTIRILMAZ (`warpStopCount1/2` yazılmaz); levent kırılımı deftere yazılır (`MachineStopEvent.beamSlot`) ve sayaçlar aynı `kind`ın TÜM slotlarını toplar. <sub>(DOKUMA-TEZGAH §5.4)</sub>

### Kararlar

- **[ÇEKİRDEK]** Dokuma işinin kapanışı AÇIK BİR KARARDIR (operatör kapatır), türetilmez; açık koşum varken kapanmaz ve 409 koşumları ADIYLA söyler (`{code, machines[], runIds[]}`). <sub>(DOKUMA-IS-EMRI §2.2a)</sub>
- **[ÇEKİRDEK]** Dokuma işinin iptali yalnız kendi üstünde bir DURUM GEÇİŞİDİR: kapanmış koşum, duruş, doff ve doğmuş top DOKUNULMAZ — "bunu şu iş için dokuduk, sonra iş iptal edildi" doğru bir cümledir. <sub>(DOKUMA-IS-EMRI §2.2b)</sub>
- **[ÇEKİRDEK]** Dokuma işi YAZMA YÜZEYİ `/api/weaving-orders` (liste · detay · oluştur · düzenle · kapat · iptal; `weavingorder:read/write`, kapı `requireDokumaEnabled`): numara `DK`+GGAAYY+NNNN ve 8032 kilidi üretecin İLK ifadesi; `clientToken` replay dört durumlu (`WEAVING_ORDER_CANCELLED`); kapat/iptal/düzenle `status IN (PLANNED, IN_PROGRESS)` üzerinden ATOMİK CLAIM (`WEAVING_ORDER_NOT_OPEN` / `WEAVING_ORDER_NOT_EDITABLE`); açık koşum `endedAt IS NULL AND revokedAt IS NULL`. `PLANNED → IN_PROGRESS`in TEK YAZARI `markWeavingOrderInProgressTx` (helper; iki adımlı claim: PLANNED→IN_PROGRESS, düşerse IN_PROGRESS satırına DOKUNMA = satır kilidi, o da düşerse 404/409), tetikleyicisi koşum-açma ucu (AYNI tx'te çağırır; audit çağıranın, `transitioned` ile) — manuel `start` ucu YOKTUR, "devam ediyor" demek koşum var demektir. Kapat/iptal claim'i koşum sayımından ÖNCE alır (satır kilidi): iki tx birbirini görür. · bekçi: `test_weaving_order` §0 (yazan yol tam 1) · §9 (eşzamanlılık iki yönlü) <sub>(1e hükmü 2026-09-13; 6e koşum yüzeyi sözleşmesi)</sub>
- **[ÇEKİRDEK]** Taraf XOR (SUBCONTRACTED ⇔ `subcontractorId` dolu) İKİ hat: serviste 400 + DB CHECK `weaving_orders_party_ck` (migration `20260913241000`, P1'in ertelediği sed yazma yüzeyiyle indi). · bekçi: `test_weaving_order` §8, `test_db_invariants` <sub>(migration 20260913180000 notu)</sub>
- **[ÇEKİRDEK]** Dokuma işi DEFTERE YAZMAZ (durum tablosu; koşum/duruş/doff/top kendi defterinde) ⇒ iptal bir durum geçişidir ve ters mekanizma KAPSAM DIŞIDIR — reopen/undo-cancel YOK, eksiklik değil. <sub>(kök `CLAUDE.md` ters yol ekseni; 1e 2026-09-13)</sub>
- **[ÇEKİRDEK]** Dokuma üçlüsü (`weaving-order` · `machine-run` · `machine-doff` router'ları) TEK kapının arkasındadır: `requireDokumaEnabled` (ön koşul `production` ÖNCE ölçülür, 403 eksik olanı söyler), `verifyToken`DAN SONRA ve her uçtan ÖNCE; kapı ekran dilimiyle doğdu ve `SCREEN_CATALOG` `operations/weaving-orders` (`modul: "dokumaEnabled"`) aynı commit'te indi, `SCREENLESS_PERMISSIONS`taki iki `weavingorder:*` muafı düştü. Dokuma uçlarını çağıran her istemci dosyası bekçinin allowlist'indedir ve karosu `dokumaEnabled`e bağlıdır — kapalı fabrikada ne karo, ne route, ne istek. · bekçi: `test_dokuma_regime_gate` §2–§7 <sub>(1e 2026-09-13; borç 01 P4 → kapandı 0c ekran dilimi)</sub>
- **[PROFİL]** `cancelReason` serbest metin (zorunlu, ≤300), `ReasonPreset`e BAĞLANMADI — **BORÇ:** **Kapanır:** P2b-2 `ReasonPresetKind.MACHINE_STOP` indikten sonra `cancelReason` metinleri ölçülüp (tekrarlayan sebep sayısı) dokuma işi iptalinin kendi `kind`ini hak edip etmediğine karar verildiğinde — geri alınamaz enum değeri ihtiyaç ölçülmeden açılmaz. **Çapa:** `WeavingOrder.cancelReason`. **Öncül:** VARSAYILDI (yüzey bugün doğdu, kullanım verisi yok). <sub>(1e hükmü 2026-09-13)</sub>
- **[ÇEKİRDEK]** `DOFF_CANCEL` yalnız o doff'a bağlı HİÇBİR top yokken açıktır (`NOT EXISTS (rolls WHERE doffEventId = :id)`); statüye BAKILMAZ — iptal/fire edilmiş top da sayılır, çünkü ölçüt "top var mı" değil "doff ileri sonuç yazdı mı"dır; 409 mesajı topu ADIYLA söyler ve çare ÖNERMEZ ("indirme artık geri alınamaz; yanlış top kendi iptal yolundan gider" — "önce topu iptal et" yanlış çareydi, iptal yüklemi değiştirmez), liste 20'de kırpılır SAYI kırpılmaz (`details.total`); KK1 kilidi tx zaman aşımını aşarsa P2028 → 409 `DOFF_LINK_IN_PROGRESS` "tekrar deneyin" (503 değil — durum çatışması). · bekçi: `test_machine_doff_source §5b' · §5e · §5f` <sub>(DOKUMA-IS-EMRI §3.8 · §3.8b ④; 1e kararı C.1/C.5, 2026-09-13)</sub>
- **[ÇEKİRDEK]** Koşumsuz doff MEŞRUDUR (`machineRunId` nullable) — fiziksel olarak olmuş bir olayı kaydetmeyi reddetmek, onu atıfsız kaydetmekten kötüdür; koşum yoksa `ApiResponse.warnings`, 400 değil. <sub>(DOKUMA-IS-EMRI §3.8; kurşun `machineId=null` emsali)</sub>
- **[ÇEKİRDEK]** Koşumsuz doff kendi kovasını alır ve ana sayıya KARIŞMAZ: rapor DÖRT sayı basar (iş emrine bağlı doff metresi · koşumsuz doff · koşumlu ama İŞSİZ doff · doff'suz top) ve `warnings` metni KAYBI söyler — "koşum açılmadığı için bu indirme iş emri metresine girmiyor"; uyarı REPLAY cevabında da döner (çevrimdışı yeniden gönderimde tek cevap odur; ilk kayıt ve replay tek kaynak `deriveRunWarnings`). · bekçi: `test_machine_doff_source §1d · §1e · §2c` <sub>(DOKUMA-IS-EMRI §3.8 · §3.8b ②; §3.8c A.3/B.5, 2026-09-13)</sub>
- **[ÇEKİRDEK]** İndirme kodu (`DF+GGAAYY+NNNN`) günlük sıradır ve oku-sonra-yaz'dır ⇒ 8029 "kod tekilliği" uzayında serileşir (`nextDoffCodeTx`, kilit tx'in İLK ifadesi; yeni uzay açılmaz), `withBarcodeRetry` yalnız `code_key` P2002 kemeridir — kilitsiz sürüm 25 paralelde tükeniyordu (10'da tutan 25'te tutmaz). KK1 HTTP ucu `doffEventId` alanını ŞEMADA taşır (düz `z.object`: listede olmayan alan sessizce düşer ve bağsız 201 döner) ve verilince `entrySource=WEAVING` zorlar; `semiFinished` ile birlikte 400. · bekçi: `test_machine_doff_source §4f · §6b` <sub>(DOKUMA-IS-EMRI §3.8b ② adım 4 · ③; 1e kararı C.2, 2026-09-13)</sub>
- **[ÇEKİRDEK]** Doff↔top bağı KK1'de AÇIK LİSTE seçimidir; makine eşleşmesi YALNIZ damgadaki makinenin istasyonu `WEAVING` ise denetlenir (tezgah başı KK1: farklı tezgah → 409 `DOFF_NOT_LINKABLE`); muayene makinesi damgası (RAW_QC/PROCESS_QC — masa KK1'in gerçek hâli, 47 ölçtü: KK1 toplarının 2190/2205'i böyle) ve damgasız oturum → KABUL; "damga doluysa karşılaştır" masa KK1'i hep 409'a düşürürdü, "damga yoksa 400" ise hiç geçemeyen ÖLÜ kapıydı. · bekçi: `test_machine_doff_source §4c · §4c2 · §4e` <sub>(DOKUMA-IS-EMRI §3.8b ③; 1e hükmü 2026-09-13, 47 ölçtü — aynı gün verilen "makinesiz KK1'de 400" kararı GEÇERSİZ → 2026-09-13, arşiv)</sub>
- **[ÇEKİRDEK]** `MachineRun.weavingOrderId` koşumda tutulur ama `DoffEvent`te TUTULMAZ — doff'un iş emri koşumdan türetilir, çünkü ikinci bir FK ikinci bir kaynak olur. <sub>(DOKUMA-IS-EMRI §3.8; `MachineRun.warpBeamId` emsali)</sub>
- **[ÇEKİRDEK]** Üretim hattı numarası kolonu `productionLineNo` adını taşır (`lineNo` DEĞİL) — `lineNo` şemada `InvoiceLine`/`PurchaseOrderLine`ta belge satır numarası anlamında yaşıyor ve iki anlam çarpıştığında geri adım atan YENİ olandır. <sub>(DOKUMA-IS-EMRI §1.6)</sub>
- **[PROFİL]** `dokuma.enabled` `tezgah.enabled`in KARDEŞİDİR, çocuğu değil (ön koşul `production.enabled`); fasona dokutan firmada dokuma vardır tezgah yoktur, yalnız devere makinelerini izleyen firmada tersi. Varsayılan `false` = bugünkü davranış (ölçüldü: fabrikada 0 İPLİK kalemi, 0 çözgü kartı). <sub>(DOKUMA-IS-EMRI §2.5)</sub>

## Tablet (mobil)

### Değişmezler

- **[ÇEKİRDEK]** Dokuma ekranı bir KUYRUK değil bir TEZGAH gösterir: önünde bekleyen iş listesi yoktur, bir (ya da `productionLineCount` kadar) tezgah vardır ve o ya koşuyordur ya durmuştur. <sub>(DOKUMA-IS-EMRI §3.2)</sub>
- **[ÇEKİRDEK]** Top indirme (doff) tablette KUYRUĞA GİRMEZ — mutation online-only (`networkMode:'always'`), çevrimdışıyken İNDİR kilitli ve sebebi ayrı anlatılır (ağ yok / sunucu yok), kalıcı düşüş anlık toast; `STATION_MUT`/`setMutationDefaults`/`stationLabels` üçlüsüne girmez. Koşum · duruş · levent kendi diliminde karar alır. · bekçi: `doffAttempt.test.ts` (token yalnız belirsiz hatada yapışır) <sub>(1e hükmü J.5 2026-09-14 — §3.6 "dördü de kuyruğa girer" GEÇERSİZ; kk1.md kuyruk kararı)</sub>
- **[ÇEKİRDEK]** Tablet TEZGAH ekranı (`Dokuma`, izin `mobile:dokuma`) WEAVING istasyonunda MAKİNEYLE açılan oturumdan çalışır (`stationScreens.ts` iki harita, `SessionGate`); makine ve hat ekrandan SEÇTİRİLMEZ — hat yalnız `SessionMachine.productionLineCount > 1` iken seçilir (oturum yükü bu alanı taşır), tek hatta seçici çizilmez. · bekçi: `stationScreens.test.ts` (5 oturumlu ekran, iki yön) <sub>(DOKUMA-IS-EMRI §3.2/§3.7; 0c 2026-09-14)</sub>
- **[ÇEKİRDEK]** Doff formu backend `openSchema` ile BİREBİRDİR (`doffPayload.ts` saf): `pieceCount` 1..1000 tam sayı, `counterAtDoff` boş = okunmadı (null), `counterSource` HER yükte beyan edilir (`@default` yok) — cihazdan `MACHINE`, elle `OPERATOR`, simüle cihazdan **`SIMULATED`** (KK1'in beyansız metre yolu TEKRARLANMAZ); `doffedAt` operatörün İNDİR'e bastığı an; koşum bağı `machineRunId` açık koşum seçiciden, atıf uydurulmaz (seçilmezse null + amber uyarı). · bekçi: `doffPayload.test.ts` <sub>(DOKUMA-IS-EMRI §3.9 B; reçete ㉓)</sub>
- **[ÇEKİRDEK]** Doff `clientToken` MANTIKSAL DENEME başına bir kez (`doffAttempt.ts`, Fason Kabul ikizi): parmak izi backend `resolveReplay`in üç alanı (makine · hat · parça sayısı; sayaç ve koşum bağı GİRMEZ), token yalnız belirsiz hatada (ağ/zaman aşımı/5xx) yapışır, kesin 4xx'te yapışmaz, başarıdan sonra biter; `CLIENT_TOKEN_COLLISION` toast değil MODAL ("yeni indirme olarak gönder" taze token). · bekçi: `doffAttempt.test.ts` <sub>(DOKUMA-IS-EMRI §3.9 B; `entryAttempt.ts` sözleşmesi)</sub>
- **[ÇEKİRDEK]** Doff geri alma tablette AYRI YETENEK iznidir (`mobile:dokuma-geri-al`; katalog `capabilities`, dar rol muafı, uç guard'ı `requireAnyPermission("loom:doff-revoke", "mobile:dokuma-geri-al")`): izni olmayanda buton çizilmez; `DOFF_HAS_ROLLS` modal barkodları ADIYLA listeler ve "geri alınamaz; yanlış top kendi iptal yolundan gider" der ("önce topu iptal edin" YAZILMAZ); listede `rollCount > 0` satır kilitli görünür. · bekçi: `test_mobile_screen_permissions` (yetenek muafı) + `test_role_template_catalog §2` (muaf) + `doffPayload.test.ts` (`classifyDoffFailure`) <sub>(1e hükmü J.3; §3.9 C)</sub>
- **[ÇEKİRDEK]** `dokuma.enabled` tabletteki kapısı `useVisibleScreens` koşuludur: `SCREEN_MODULE.Dokuma = 'dokumaEnabled'` (kataloğun aynası, `test_screen_catalog §3b`), etkin değer `production && dokuma`, satır-yok yönü KAPALI (fail-closed) — referans fabrikada Tezgah kartı ne kartta ne navigator'da; KK1'deki iki soru ("dokuma mı · hangi indirmeden") AYRI dilim. · bekçi: `useVisibleScreens.test.ts` + `screenModules.test.ts` <sub>(§3.9 E; 1e hükmü J.7)</sub>
- **[ÇEKİRDEK]** `ReasonPresetKind.LOOM_STOP` için APK'ya gömülü zemin ZORUNLUDUR — zeminsiz kalırsa sunucusuz katalog boş döner, sebep zorunlu olan duruş kaydedilemez ve tezgah ekranda kilitlenir. · bekçi: `Teks-Erp/scripts/test_reason_preset_kind_parity.ts` (on iki aynayı Prisma enum'una karşı iki yönlü ölçer) <sub>(DOKUMA-IS-EMRI §3.4)</sub>
- **[ÇEKİRDEK]** `warpBeamSlots > 1` olan tezgahta duruş HANGİ YUVA sorar; bilinmiyorsa NULL kalır ve Pareto'da "atanmamış" kovasında AYRI gösterilir — tahminle bir levende yazılmaz. `<= 1` ise alan hiç çizilmez. <sub>(DOKUMA-TEZGAH §2.7)</sub>
- **[ÇEKİRDEK]** Ham/dokuma ayrımı KK1'de ÇIKARILMAZ, SORULUR: `dokuma.enabled` açık fabrikada gelen top ya dokunmuş ya satın alınmıştır ve ikisi aynı gün olur; bayrak kapalıyken KK1 bugünküyle birebir aynıdır. KK1 dilimi (0c 2026-09-14): iki soru `DoffLinkPicker` ("Bu top dokuma mı?" → "hangi indirmeden?", `unlinked=true` listesi makine VERİLMEDEN — masa KK1 hepsini görür, tezgah başı eşleşmesini backend denetler, hüküm (a)); bağ AÇIK LİSTE seçimi, cevap yoksa `doffEventId: null`; yarı mamulle birlikte verilemez; bayrak kapalıyken payload'a alan HİÇ girmez. · bekçi: `doffLink.test.ts` (kapalı → alan yok · dokuma+seçim yok → null · yarı mamulde gizli) + `test_mobile_screen_permissions` (KK1 → `GET /machine-doffs` guard `mobile:kk1`) <sub>(DOKUMA-IS-EMRI §3.5, §3.9 D; 1e hükmü (a))</sub>

### Tuzaklar

- **[ÇEKİRDEK]** `source:'SIMULATED'` beyanı bugün YALNIZ kantar (SCALE) yolunda vardır, metre (METER) yolunda YOKTUR — doff sayacı bir ölçüm cihazıdır ve beyanı baştan taşımalıdır, yoksa aynı boşluk yeni bir yerde kurulur. <sub>(DOKUMA-IS-EMRI §3.7)</sub>
- **[ÇEKİRDEK]** Doff anında sayaç değeri (`counterAtDoff`) kaydedilmezse operatörün sayacı sıfırlaması ile 16-bit sarma AYIRT EDİLEMEZ; değeri kaydetmek belirsizliği kaynağında kapatır ve sıfırlamayı beyan edilmiş bir olaya çevirir. <sub>(DOKUMA-TEZGAH § sayaç kalitesi; DOKUMA-IS-EMRI §3.8)</sub>
- **[ÇEKİRDEK]** Beş dev ekran (Tambur · FasonKabul · KK1 · KursunQc · FasonSevk) `max-lines` MUAFIDIR ve emsal ALINMAZ — muafiyet bir borçtur, bir kalıp değil; yeni ekranın emsali `screens/Modules/HizliIsEmri/`dir. <sub>(`docs/standart/MOBIL.md` [MO-03])</sub>

## Bekçiler — bu alana dokununca koş

`cd Teks-Erp && npx tsx scripts/run-all-tests.ts <ad-parçası>`

Backend: `test_machine_stop_manual`, `test_devere_regime_gate`, `test_dokuma_regime_gate`, `test_audit_labels`, `test_reason_preset_kind_parity`, `test_db_invariants`, `test_timestamptz_contract`, `test_machine_run`, `test_hard_delete_guard_coverage`, `test_master_data_merge_fk_coverage`, `test_production_line`, `test_machine_stop`, `test_weaving_order`, `test_loom_stop_ground`, `test_dokuma_rapor_onkosullari`, `test_production_regime_gate`, `test_permission_catalog`, `test_role_template_catalog`, `test_swagger_spec`, `test_loom_lists`, `test_machine_doff_source`, `test_station_kind_weaving`, `test_screen_catalog`, `test_defter_ters_yol`

İstemci: `doffLink.test.ts` (KK1 dokuma bağı: bayrak kapalı → alan yok · seçim yok → null · yarı mamulde gizli), `doffAttempt.test.ts` (token yalnız belirsiz hatada yapışır; parmak izi üç alan), `doffPayload.test.ts` (openSchema birebir · SIMULATED beyanı · 409 kodları eylem), `stationScreens.test.ts` (WEAVING ↔ Dokuma iki yön), `useVisibleScreens.test.ts` (Dokuma kartı yalnız dokuma açıkken; üretime bağlı), `screenModules.test.ts`, `weaving-regime.test.ts` (Dokuma İşleri karosu yalnız `dokumaEnabled` ile; yüklem üretime bakmaz — zincir bağlamı kuran hook'ta), `tile-visibility.test.ts` (karo ↔ palet aynı yüklem nesnesi, koşullu karo kümesi açık sayılır)

> ⚠️ **Yukarıdaki `Backend:` satırı KAPININ okuduğu biçimdir, süs değil.** `test_identity_ledger.ts:158` alan koşum listesini yalnız `## Bekçiler` başlığından sonraki `Backend:` satırından ayrıştırır (`` `ad` `` aralarında). Aşağıdaki madde imli açıklamalar **insan içindir ve kapı onları GÖRMEZ**: bu dosya 2026-09-13'te madde imli listeyi taşıyordu ve `test_machine_run` tarif edilmiş olduğu hâlde kapı onu **B-d (KAPSAM eksiği)** saydı. ⇒ *Bir belge bölümünü doldurmak, o bölümü okuyan kapıyı beslemek değildir — kapı bölümü değil BİÇİMİ okur.* Yeni bekçi **iki yere birden** yazılır: `Backend:` satırına (bağ) ve aşağıya (tarif).

**Bugün VAR olanlar:**
- `test_devere_regime_gate` — devere kapısının üç halkayı (ticaret → iplik → devere) ELLE ölçtüğü; dokuma inince kardeş zincir aynı biçimde ölçülür.
- **`test_dokuma_regime_gate`** (2026-09-13, ekran dilimiyle doğdu; 33 ölçüm, DB'siz) — §2 ön koşul (production) ÖNCE · §3 403 eksik olanı söyler · §4 bağımlılık tablosu + tezgah KARDEŞLİĞİ (biri diğerinin ön koşulu değil) · §5 satır yokken KAPALI · §6 üç router kapılı, `verifyToken`dan sonra, her uçtan önce, taşıyan ⊆ liste, dokuma modeline dokunan her router kapılı · **§7 "kapalıyken sıfır fark"**: dokuma uçlarını çağıran her istemci dosyası allowlist'te (bugün tek: `WeavingOrders/service.ts`), o ekranın manifestosu `dokumaEnabled`, karosu saf yüklemle `dokumaEnabled` okuyor, route'u `weavingorder:read`. Beş negatif sonda (3/1/1/1/1 ❌, md5 ile geri alındı) başlıkta.
- `test_audit_labels` — şemadaki her enum değerinin Electron karşılığı (yeni dokuma enum'ları beyan edilmeden geçemez).
- `test_reason_preset_kind_parity` — `LOOM_STOP` aynalarının iki yönlü eşitliği.
- `test_db_invariants` — yeni CHECK/partial index/trigger envantere yazılmadan geçmez.
- `test_timestamptz_contract` — yeni `DateTime` alanları.
- **`test_machine_run`** (2026-09-13, P2 ile doğdu; P2b'de 11 → 39 ölçüm) — §1–§4 sedler + CHECK · **§5 TOCTOU penceresi** (gate-tx + `pg_blocking_pids`, kaybedenin kodu `MACHINE_RUN_RACE`; ~~SED ölçümüdür~~ 2026-09-13'te pencere ölçümüne çevrildi) · §6 silme guard'ı ters yönü · §7 yüklem çağrısı · §8 precheck (§5'in körlük zemini) · §9 kapanış terimleri · §10 geri alma · §11 token replay dört durum · §12 dokuma işi bağı + iptalle yarış. Negatif sondalar: yüklem çağrısı kaldırılınca 2 ❌ · iş claim'i kaldırılınca 3 ❌ · kapanış claim'inden `closedTermsAt IS NULL` düşünce 2 ❌.
- **`test_machine_doff_source`** (2026-09-13, P3b doff backend ile doğdu; §3.8c kalemleriyle 20 → **31 ölçüm**) — §1 replay dört durum + replay `warnings` (sıralı §1d, paralel P2002 dalı §1e) · §2 koşum bağı (koşumsuz → `warnings` kaybı söyler; başka makine 409; koşumlu-işsiz dördüncü kova §2c) · §3 tx-A stok defterine satır/top yazmaz (fikstür ürününe daraltılmış sayım) · §4 KK1 bağı: üç red + masa KK1'de bağ kabul (§4e) + HTTP şeması alanı taşır/yarı mamulle reddeder (§4f) · §5 `DOFF_CANCEL` (iptal edilmiş topla da 409; mesaj çare önermez §5b'; 21 topta sayı kırpılmaz §5e; P2028 → 409 §5f) · **§5b iki pencere** (tx-B `FOR UPDATE`; kanıt bloklanan çağrının sırası, taban 3 ms) · §6 kod biçimi + **8029 sıra kanıtı** (elle tutulan kilit kaydı bloklar §6b-1/2) + 25 paralel 25 tekil / 0 hata (§6b-3). İlk koşumda gerçek kusur: revoke claim'i kilitte bloklanıp ESKİ snapshot'la yüklemi geçiyordu (EvalPlanQual alt sorguyu tazelemez) — kilit ayrı ifadede önce alındı. Dokuz negatif sonda, her biri hedeflediği bölümü kırmızı yaptı (tablo `DOKUMA-IS-EMRI` §3.8b ⑤).
- `test_production_line` — §1–§3 yüklem/CHECK/varsayılan + **§4 çağrıldığı yol** (P4b borcunun kapanışı; koşum açan servisten 3. hat → 400).
- `test_hard_delete_guard_coverage` — `Machine`e gelen her FK ya guard'da sayılıyor ya gerekçeli muaf. ⚠️ Guard'ları **REGEX** okur: sayaç `prisma.<model>.count({ where: { <kolon>:` biçiminde **DÜZ** yazılmalı, helper'a sarmalanırsa bekçi "guard yok" sayar (ölçüldü 2026-09-13, negatif sonda).
- `test_master_data_merge_fk_coverage` — şemadan türer: birleştirilebilir varlığa (`item`·`color`·`customer`·`subcontractor`) giden her yeni FK `MERGE_MAP`e satır ister.
- **`test_weaving_order`** (2026-09-13, yazma yüzeyiyle doğdu) — §0 `IN_PROGRESS` yazan yol 0 (dal ERİŞİLEMEZ; P2b açınca KIRMIZI verir, o gün davranış yazılır) · §1 numara/XOR/kumaş/plannedM/tarih · §2 replay dört durum · §3 düzenleme yalnız açıkken · §4 kapanış 409 koşum ADIYLA + `plannedM` kapatmaz · §5 iptal defter satırına dokunmaz · §6 liste süzgeci sunucuda + cursor · §7 audit tx dışında · §8 DB CHECK servisi atlayan yazımı reddeder. ⚠️ HTTP katmanı (Zod/izin/kapı) ÖLÇÜLMEZ — statik kapılar + d9 sondaları.

**HENÜZ YOK (şema inerken yazılacak, ne ölçeceği yazılı):**

## Raporların sözleşmesi — üç soru cümlesi

> **Bir rapor bir ÖLÇÜM TANIMIDIR.** Yanlış tanımlanmış bir rapor yanlış bir bekçiden
> pahalıdır: kimse onu sorgulamaz, **karar ondan verilir.** Bu yüzden her cümle
> **yanlışlanabilir** yazılır ve yanlışlanma koşulu yanına konur.

**① RANDIMAN**
> *"Şu tezgah, şu vardiyada, planlı süresinin %A'sında çalıştı (KULLANILABİLİRLİK); çalıştığı sürede hedef devrin %P'sini tutturdu (PERFORMANS) — ve bu iki sayı **ÇARPILMAZ, ayrı sunulur**."*
> **Yanlışlanır:** kova toplamı planlı süreyi aşarsa · `targetPicksPerMin` NULL iken bir performans sayısı basılırsa.

⚠️ **PAYDA TEK DEĞİL ÜÇ** ve hangisinin hangi orana girdiğini **kova** belirler (ISO 22400-2 + SEMI E10 + Nakajima; `MachineStopLossClass`):
`NON_SCHEDULED` → **hiçbir paydada yok** · `PLANNED` → planlı süreden düşülür · `UNPLANNED`/`SETUP` → KULLANILABİLİRLİK kaybı · `MINOR` → **PERFORMANS** kaybı, kullanılabilirlik DEĞİL.
⇒ *"Randıman"* tek bir sayı değildir; tek yüzdeye çökertilirse üç farklı büyüklük aynı adla anılır.
⚠️ **DÖRDÜNCÜ DURUM: "ölçülemedi".** `MachineRun.targetPicksPerMin` NULL → `MachineSpec.nominalPicksPerMin` yedeği → o da NULL ise **performans HESAPLANMAZ**, rapor *"P: ölçülemedi"* der. Boş hücre değil **beyandır**. (`picksPerRev` yalnız TAVANA girer, paydaya ASLA.)

**② DURUŞ PARETO**
> *"Şu tezgahta şu aralıkta en çok süreyi yiyen duruş SEBEBİ şudur (N olay, T dakika), ve bu sebep şu KOVAYA düşer."*
> **Yanlışlanır:** `NON_SCHEDULED` bir paydaya girerse · `MINOR` bir SEBEP gibi listelenirse · serbest metin bir satır üretirse.

⚠️ **İKİ EKSEN, KARIŞTIRILMAZ:** `ReasonPreset.code` = **SEBEP** (sıralama ekseni) · `MachineStopLossClass` = **SÜRE SINIFI** (gruplama ekseni). Tasarımın şerhi: *"`MINOR` bir SEBEP SINIFI DEĞİL, bir SÜRE SINIFIDIR"* — yalnız `tezgah.stopEventMinSeconds` altındaki duruşlardan türer ve `ReasonPreset.stopLossClass`a ASLA yazılamaz.
⇒ Tek eksende sunulursa *"mikro duruşlar"* bir sebep gibi görünür ve **operatöre yanlış iş verir** — raporun yanlış olmasının en pahalı biçimi.
⚠️ Ayrım denetim ekranında bir override'la yaşar (`MACHINE_STOP_EVENT.lossClass` → *"Mikro duruş (eşik altı)"*); override silinirse ekran duruşa `DefectSeverity` dilinde *"Küçük"* der. Bu **korumasızdı** (ölçüldü 2026-09-13: silince Electron 75/75, backend 22/0 yeşil kaldı) — `test_dokuma_rapor_onkosullari` §6 kapatır.
⚠️ Sebep **serbest metin DEĞİL**: `reasonCode` → `ReasonPreset.code` (FK'SIZ, `Roll.cancelReasonCode` emsali) ve etiket **kopyalanıp DONAR** — katalog değişse geçmiş rapor değişmez.

**③ VARDİYA KARNESİ**
> *"A vardiyası (fabrika günü X, 08:00+8s) şu tezgahlarda şu üretimi yaptı; satırların K'sı ÖLÇÜLDÜ, L'si ELLE girildi, M'si ölçülemedi."*
> **Yanlışlanır:** gün sınırı `src/constants/time.ts` dışından gelirse · `source` kırılımı toplamda erirse · mühürlenmiş karne sonradan değişirse.

## Raporların İKİ DEĞİŞMEZİ

**① ELLE GİRİŞ BİRİNCİ SINIFTIR ve toplamda ERİMEZ.**
Veri çekilemeyen tezgahlar için elle giriş olacak ⇒ raporun **her toplam satırı** `MachineShiftStat.source` kırılımını taşır. **Tek yüzdeye çökertme YASAK:** *"%78"* denmez, *"%78 (ölçülen 62, elle 16)"* denir.
⚠️ `SIMULATED` `OPERATOR`dan **AYRI** durur — farklı güven sınıfı; kök `CLAUDE.md` *"uydurulmuş değer `source:'SIMULATED'` beyanıyla gider"* diyor ve birleştirmek o beyanı **yok eder**.
**Bekçisi İKİ KATMANLI.** ÇIKTI bekçisi (toplam = Σkırılım **ve** kırılım basılıyor mu) rapor yüzeyi indiğinde yazılır — bugün YOKTUR ve `test_dokuma_rapor_onkosullari` bunu *"ÖLÇÜLEMEDİ"* diye BEYAN eder (boş hücre değil). ÖN KOŞUL bekçisi bugün koşar (`SIMULATED` ile `OPERATOR` ayrı iki enum değeri · her `MachineDataSource` kolonu `@default`suz) ve envanter-dışı bir kaynak kolonu doğduğu gün KIRMIZI vererek SINIFLANDIRMA ister. ⚠️ Mandal 2026-09-13'te ilk kez uyandı (`doff_events.counterSource`, dokuma P3) ve **mesajı yanıldı**: *"taşıyıcı geldi"* diyordu, oysa kolon bir OLAYIN kökeniydi (doff'taki sayaç okuması). ⇒ Yeni kaynak kolonu iki sınıftan biridir ve şemadan OKUNARAK ayrılır: raporun SATIRINDA yaşayan (taşıyıcı) ↔ bir OLAYIN kökenini taşıyan. **Kapanır:** `machine_shift_stats` (ya da raporun satırında yaşayan bir kaynak kolonu) indiğinde ③'ün ÇIKTI bekçisi yazılır — toplam = Σkırılım **ve** kırılım basılıyor, İKİ YÖNLÜ sondayla: (a) kırılım basılmazsa kırmızı (b) `SIMULATED` `OPERATOR`a katılırsa kırmızı. **Öncül: ölçüldü** (2026-09-13: rapor servisi ve taşıyıcı tablo yok; `test_dokuma_rapor_onkosullari` §4 bunu ⏭ ile beyan eder).

**② UFUK YAZILIR — ve HER UFKUN TEK KAYNAĞI, AYRI ADI VAR.**
Rapor *"her şey tutuyor"* demez, *"şu tarihten sonrası ölçülü"* der.
⚠️ **Dokuma ufku ≠ defter ufku.** Ölçüldü 2026-09-13: `DEFTER_UFKU` **bekçi tarafında** yaşıyor (`scripts/test_consistency.ts:121`), ürün tarafında karşılığı **yok** ve bugün değeri **`null`**. İkisi farklı sorulara cevap verir — *defter ufku* = depoya yazan son kapısız yolun kapandığı gün; *dokuma ufku* = tezgah verisinin toplanmaya başladığı gün. ⇒ Dokuma raporu `DEFTER_UFKU`yu **OKUMAZ**; kendi ufkunu `src/constants/` altında **ayrı adla** ilan eder.
📌 Birleştirmek, iki farklı güvenilirlik sınırını tek sayıya çökertmek olurdu — ①'de `source` için reddedilen şeyin aynısı.

⚠️ **Beş tablonun beşi de bugün ŞEMADA YOK** (`ShiftDefinition` · `MachineShiftStat` · `MachineStopEvent` · `MachineRun` · `MachineSpec`) — bu bölüm raporların SÖZLEŞMESİDİR, uygulama değil.

## Açık sorular

- **Koşumsuz doff oranı ölçülmedi.** Sık çıkıyorsa bu bir veri modeli kararı değil bir ARAYÜZ kusurudur (tablet koşumu kendisi açmalı) — karne bu oranı basmalı ve sayı görülmeden karar verilmemeli.
- **`unitsPerCm`in kalıcı evi belirsiz** (kaynak: `WarpSpec` ailesi; **sert bağımlılık eklenmez**). Faz 1–2'de elle girilir ve donar. <sub>(DOKUMA-TEZGAH §10/#9)</sub>
- **Fason dokuma bloke:** `SubcontractorDispatchItem.rollId` **NOT NULL** ⇒ fasona yapısal olarak yalnız TOP gidebilir, iplik/levent gidemez. Ayrı dilim; dokuma kararından bağımsız ve Faz 1'i bloklamıyor.
- ~~**Advisory uzay numarası kesinleşmedi**~~ **KAPANDI 2026-09-13** — ölçüldü: `WEAVING_ORDER_LOCK_NS = 8032` indi (`helpers/weaving-order.helper.ts:24`, envanter `period-guard.helper.ts:57`). ⚠️ Önerilen `8034` **kullanılmadı** ve bu satır bir gün boyunca bayat kaldı. ⇒ *Numara çakışması sessizdir* uyarısı doğruydu ama **eksikti: bayat REZERVASYON da sessizdir.** Sonraki uzay için numara buradan değil, **`period-guard.helper.ts` başlığından** okunur.
  ⚠️ **`MachineRun` advisory uzayı ALMADI ve almayacak** (2026-09-13, P2): koşum tekilliği DB seddiyle kurulur (iki partial unique), yarışın kaybedeni P2002 alır ve servis 409'a çevirir — `work_sessions` emsali. *Her tekillik sorusu bir kilit istemez; sed yeten yerde kilit ikinci bir yazar yolu açar.*
