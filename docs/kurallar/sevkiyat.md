# Sevkiyat · Çuval · Brüt · Storno/İade

> Alan kural dosyası — bu alana dokunmadan ÖNCE okunur. Kaynak: anlama turu 2026-09-05 (kök `CLAUDE.md` + `docs/history/CLAUDE-NOT-ARSIVI.md` notlarından ayrıştırıldı). Hikâye, ölçüm ve gerekçe arşivde; burada yalnız bugün geçerli kural. Sınıf: **[ÇEKİRDEK]** her kurulumda aynı · **[PROFİL]** bu fabrikanın seçimi.

> Hakem notu: 45 üye (23 primary). Çekirdek gövde (çuval modeli → brüt kuralı 4 tur → storno/iade → Sevk Kapısı) tutarlı ve kodla birebir; 6 kısmi ezilme, hepsi koddan çözüldü — en önemlileri 2026-08-05 'Sevk Kapısı VEYA PLANNED' kuralının 2026-08-22'de saf bayrağa inmesi ve 'freeze tek koruma' varsayımının çürümesi. Bayat metin: Teks-Erp/CLAUDE.md:325-326 + schema.prisma:4499/4517 hâlâ resetSackWeightsTx diyor (kod: markSackContentChangedTx). Görünür çelişki 'tahsis silinmez/silinir' iki ayrı yol (düz storno ↔ releaseSacks kapanışı). En riskli: mobil/CLAUDE.md:329 'MANUAL muaf' cümlesi manualWeightRestrictedEnabled şerhi taşımıyor. archiveOnly = kök dizinde satır gerektirmeyen alt-CLAUDE notları. 22 ikincil üye başka kümelerin sahipliğinde; yalnız sevkiyata değen cümleleri değerlendirildi.


## Ortak (backend + panel + tablet)


### Değişmezler

- **[ÇEKİRDEK]** Çuval bir DEPO nesnesidir: `Sack.customerId` opsiyonel (açılışta ya da sevkte atanır), `shipmentId` NULL = havuzda; sevkiyat depodan çuval seçilerek kurulur (`createShipment({sackIds, customerId, orderIds?})`), top↔sipariş satırı bağı YOKTUR. <sub>(CLAUDE.md:41, arşiv:24, CLAUDE.md:17)</sub>
- **[ÇEKİRDEK]** `SackAllocation` sevk ANINDA yazılır (spec+şube FIFO, `distributeSacksToLines`); stok yalnız DISPATCH'te `SHIPPED` düşer ve tahsis o anda `shippedQty`ye terfi eder; PLANNED tahsis SAYILMAZ; `shippedQty` defterden TÜRER, elle yazılmaz. · bekçi: `scripts/test_shipment_undo_dispatch.ts` <sub>(CLAUDE.md:41, arşiv:24, CLAUDE.md:63)</sub>
- **[ÇEKİRDEK]** `WorkOrder` yalnız üretimi yönetir (istasyonlar, kurşun/QC2, tambur); tartı/paket/sevkiyat AYRI domaindir (`shipping.service.ts`) ve WO'ya değil depoya/çuvala bağlanır. <sub>(CLAUDE.md:123)</sub>
- **[ÇEKİRDEK]** SACK etiketinin barkodu/QR'ı `Sack.sackNo`dur — Sack'e ayrı barcode kolonu EKLENMEZ; baskı FAIL-CLOSED: SACK şablonu çözülemez ya da bağlama uymazsa (çuvala ROLL şablonu atanmış) Türkçe 400, roll/swatch etiketine SAPMAZ. · bekçi: `scripts/test_label_context_fit.ts, scripts/test_sack_label.ts` <sub>(CLAUDE.md:43, arşiv:24)</sub>
- **[ÇEKİRDEK]** Sevk rakamı BRÜT: sevk edilmiş sevkiyatın metrajı/adedi canlı çuval sorgusundan ÜRETİLMEZ; iade çıkış belgesini geriye dönük DEĞİŞTİRMEZ, ayrı belgeyle kapanır (`RETURN_DISPATCH` iade ANINDA tx içinde donar, iptalde VOIDED). Yanıttaki `returns {count, meters}` düşmek için değil DİPNOT içindir. · bekçi: `scripts/test_dispatch_report_gross.ts` <sub>(CLAUDE.md:45, arşiv:53, CLAUDE.md:1)</sub>
- **[ÇEKİRDEK]** Brütün TEK KAYNAĞI `RollReturn` (iptal edilmemiş; `prevSackId` geri-ekleme), snapshot DEĞİL — beş tüketici: `attachTotals` · `accounting-export` · `getShipmentById` · `collectShipmentDocContent` · `_shipped.ts`; altıncı kaynak altıncı rakam. Yalnız `getDispatchReport` donmuş snapshot okur. · bekçi: `scripts/test_shipment_list_gross.ts · test_shipment_detail_gross.ts · test_dispa` <sub>(CLAUDE.md:45, arşiv:53, CLAUDE.md:47)</sub>
- **[ÇEKİRDEK]** Fatura izi `invoiceNo`+`invoicedAt`+`invoicedById` DIŞ muhasebe belgesinin izidir (tutar/KDV yok): yalnız DISPATCHED (atomik claim), `null` tarihi de temizler; izin `shipping:invoice`. finance.enabled AÇIKKEN ERP taslak fatura doğurur; çakışma `shipping.invoiceMode`. · bekçi: `scripts/test_shipment_invoice.ts` <sub>(arşiv:53, CLAUDE.md:45, CLAUDE.md:106)</sub>
- **[ÇEKİRDEK]** 'Dönemde sevk edilen metraj' TEK tanım `reports/_shipped.ts` (RollReturn geri-eklemeli, DirectShipment dahil, iade satırı kendi spec snapshot'ını taşır); ikinci tanım yazma. `finalizedAt` trigger kaynak listesinde SHIPPED/CANCELLED BİLEREK YOK — storno eski topu bugünün karnesine sokar. · bekçi: `scripts/test_quality_scorecard.ts, test_db_invariants.ts (fazA)` <sub>(CLAUDE.md:69)</sub>
- **[ÇEKİRDEK]** Storno ≠ iade (SAP VL09): İade = mal müşteriye ULAŞTI → çıkış belgesi düzeltilmez, iade irsaliyesi + iade defteri (`RollReturn`, sebep zorunlu). Storno = mal HİÇ ÇIKMADI → çıkış belgesi İPTAL (VOIDED), stok rafına döner, iade defterine GİRMEZ; iade yazmak kalite verisini kirletir. · bekçi: `scripts/test_shipment_undo_dispatch.ts` <sub>(CLAUDE.md:63)</sub>
- **[ÇEKİRDEK]** Düz storno (DISPATCHED→PLANNED) tahsisi SİLMEZ: `ShipmentOrder.isActive=true` + recompute; plaka/şoför KORUNUR. `releaseSacks` ile storno aynı tx'te `cancelPlannedShipmentTx` → CANCELLED: tahsis silinir, çuval havuza, toplar ÇUVALDA kalır (`sackId` korunur), irsaliyeler VOIDED, iade defteri YOK. · bekçi: `scripts/test_shipment_undo_dispatch.ts §10 (50)` <sub>(CLAUDE.md:63, CLAUDE.md:79, arşiv:24)</sub>
- **[ÇEKİRDEK]** İzin ayrımı (SoD): `shipping:undo-dispatch` (storno + `releaseSacks` kapanışı; `shipping:write` KAPSAMAZ, aranmaz) · `shipping:invoice` (faturalandı işareti) · `shipping:write` (çuval/irsaliye/sevk). Üçlü yalnız Muhasebe/Süpervizör rol şablonlarında; boot uzlaştırması izni DB'ye getirir, atama ELLE. · bekçi: `scripts/test_shipment_undo_dispatch.ts, test_shipment_invoice.ts` <sub>(CLAUDE.md:63, CLAUDE.md:79, arşiv:53)</sub>
- **[ÇEKİRDEK]** Uydurulmuş kg canlı veriye GİRMEZ: istemci `source:'SIMULATED'` BEYAN eder (toast'ta 'SİMÜLASYON'), kararı backend verir — `shipping.simulatedWeightEnabled` KAPALIYKEN (varsayılan) 400. Kg müşteri belgesine basıldığı için SCALE simüle DOĞMAZ, METER doğar. `source:'MANUAL'` bu kapıdan MUAF. · bekçi: `scripts/test_sack_weigh_source.ts + mobil useSackWeigh.test.tsx 5/6/7` <sub>(CLAUDE.md:329)</sub>
- **[ÇEKİRDEK]** `shipping.orderRequirement` off|warn|block (varsayılan `warn` = bugünkü: sevkiyat kurulur, `warnings[]` döner); `block` BUGÜN AÇILAMAZ — tablet `orderIds` göndermiyor. Kapsam FASON DOĞRUDAN SEVK'i de içerir (`orderless` kaçışı); kapı takarken 'BAŞKA çıkış yolu var mı' kardeş bayrağa bak. · bekçi: `scripts/test_shipping_flags.ts` <sub>(CLAUDE.md:106)</sub>
- **[ÇEKİRDEK]** Sevk belgesinde müşterideki ürün adı: ad DONAR (snapshot `products[].customerName`, `cekiRows[].customerDesen/Varyant`), REJİM DONMAZ (`shipping.docItemNameMode` her baskıda canlı); zincir ETİKETLE AYNI (customerItemName > CustomerItemAlias > yok); `(itemId,colorId)` DETERMİNİSTİK; gruplama BİZİM ad · bekçi: `scripts/test_shipment_doc_customer_name.ts (37, yedi negatif sonda)` <sub>(CLAUDE.md:111)</sub>
- **[ÇEKİRDEK]** Çeki listesi bölümünün ad rejimi AYRI bayraktır (`shipping.docCekiNameMode`: `devral` varsayılan | `bizdeki` | `musterideki` | `ikisi`) ve YALNIZ çeki bölümünü çevirir; `devral` genel rejimi izler, yani bayrak yazılmadıkça çıktı bayt-bayt aynıdır. Gerekçe: çeki listesi tek başına da basılıyor ve ambar kontrol listesi olarak kullanılıyor — orada iki ad anlamlı, müşteriye giden ürün listesinde değil; tek global rejim ikisini birden çeviriyordu. Bekçi: `test_shipment_doc_customer_name` §7 (üç negatif sonda: 5/3/1 kırmızı).
- **[ÇEKİRDEK]** Sipariş bağı İKİ DİK EKSENDE ölçülür: `shipping.orderRequirement` **niyeti** (sipariş seçildi mi), `shipping.orderCoverage` **sonucu** (mal seçili siparişlere yazılabildi mi). İkisi ayrı bayraktır ve ayrı ayarlanır; tek merdivene indirmek "bağ sıkılığı arttıkça kapsama da sıkılaşır" diye yanlış bir sözleşme kurar ve bugün meşru olan "sipariş seçilsin ama fazla mal serbest kalsın" düzenini ifade edilemez kılar. Kapsama kapısı YALNIZ kurulumda çalışır, `dispatchShipment`te DEĞİL — sevk anında `throw` malı bina içinde kilitler. Varsayılan `off` = bugünkü davranış. Ölçüm gerekçesi: 89 sevk edilmiş sevkiyatın 88'inde sipariş zaten seçiliydi, yani `orderRequirement=block` 42 boşluklu sevkiyatın (11.384,7 m) hiçbirini durdurmazdı. Bekçi: `test_shipping_flags` §7 (7 kontrol, dört negatif sonda).
- **[ÇEKİRDEK]** "Müşterideki ad" sorusunu EKRAN ve BELGE aynı yüklemle cevaplar: `override > CustomerItemAlias/CustomerColorAlias (master) > yok`. Sevkiyat detayı ve sipariş seçim ekranı projeksiyonları master kademesini de çözer; karşılığı yoksa alan `null` döner (bizim adımız "müşterideki ad" diye basılmaz). Ölçüm: sevk edilmiş 1.778 topun 424'ünde (%24) master alias vardı ama override yoktu — irsaliyede müşteri adı basılıyor, ekranda görünmüyordu. Bekçi: `test_shipment_doc_customer_name` §8.
- **[ÇEKİRDEK]** Çuval listesinden TOPLU "dağıtma" yıkıcı işlemdir: önce `POST /sacks/distribute/preview` (yazmaz) etkilenen HER topu satır satır döner, arayüz onu gösterir; uygulama `POST /sacks/distribute/bulk` engelli çuvalı (sevkiyata atanmış) ATLAR ve sebebini yanıtta taşır — hepsi-ya-hiçbiri DEĞİL, sessiz atlama da DEĞİL. Çuval kaydı SİLİNMEZ, yalnız içerik depoya çıkar. Bekçi: `test_sack_bulk_distribute` (16 kontrol, iki negatif sonda).
- **[ÇEKİRDEK]** İrsaliyenin ÜRÜN LİSTESİNDE müşteri adı bugün tek hücrede birleşiktir (`müşteri kumaş adı + renk + en`); müşterinin o renge KENDİ adı yoksa bizim renk adımız girer. ⚠️ Bu çoğu zaman DOĞRUDUR — **müşterilerin çoğu bizim renk adımızı kullanır**, yani karşılığın olmaması eksik veri değil normal hâldir (kullanıcı beyanı 2026-09-06). Ayarın (`shipping.docProductColorSplit`, varsayılan `false` = bugünkü çıktı) çözdüğü şey dardır: kumaş adı müşterinin, renk adı bizim olduğunda tek hücrede yan yana durdukları için okuyucu hangi yarının kime ait olduğunu ayırt edemez. Ayrık kipte sütun YALNIZ müşterinin kendi renk adı varsa dolar — fallback YOK, çünkü bizim adımızı "müşteri varyantı" diye etiketlemek yanlış olurdu. Bekçi: `test_shipment_doc_customer_name` §9.
- **[ÇEKİRDEK]** Tahsiste **kumaş ve renk KESİN eşleşir; yalnız EN gevşetilebilir** (`shipping.allocWidthToleranceEnabled` + `…Cm`, varsayılan kapalı = tam eşitlik). Yanlış rengi bir siparişe yazmak defteri sessizce bozar; en sapması sahada kabul edilen bir gerçektir (kullanıcı beyanı 2026-09-06). Ölçüm: en yüzünden yazılamayan 950 m / 25 top; gözlenen farklar 0,1 cm ve 5 cm. Bekçi: `test_allocation_repair` §2/§3 — §3 renk toleransının OLMADIĞINI ölçer.
- **[ÇEKİRDEK]** Tahsis sipariş miktarını **aşamaz**; aşan mal hiçbir satıra yazılmaz (`shipping.allowOverAllocation`, varsayılan kapalı = bugünkü davranış). Açıkken fazlalık eşleşen satıra yazılır ve `shippedQty` ısmarlananı geçebilir — fazla sevk KAYIT ALTINA alınır, sessiz kalmaz. Ölçüm: bu sınıfta 15.723 m / 398 top. Bekçi: `test_allocation_repair` §4.
- **[ÇEKİRDEK]** Sipariş defterine yazılamamış sevkiyat **onarılabilir**: `GET/POST /api/shipping/repair/allocations` — motor `setShipmentOrders`tır (sipariş kümesi DEĞİŞMEZ, yalnız tahsis bugünün verisiyle yeniden kurulur, irsaliye v+1 donar). Ayrı izin `shipping:repair-allocation` (`shipping:write` YETMEZ). Ekran: Operasyon → Siparişe Yazılamayanlar; onarımdan önce ne değişeceği gösterilir. Ölçüm: onarılabilir küme 15.905 m / 407 top. Bekçi: `test_allocation_repair` §5/§6.
- **[ÇEKİRDEK]** Sevkiyat detayı **`defterBoslugu`** taşır (çıkan mal − siparişe yazılan; siparişsiz sevkiyatta `null`) ve panel bunu rozetle gösterir. Bu sayı 2026-09-06'ya kadar YALNIZ audit izindeydi, hiçbir ekranda görünmüyordu — sipariş "Açık" kaldığı için aynı mal yeniden üretime verilebiliyordu.
- **[ÇEKİRDEK]** Bitmiş top (WAREHOUSE/A1_STOCK) yeniden üretime alınabilir: `attachRolls`/`quickStart` kabul listesi STOCK/WAREHOUSE/A1_STOCK; masaüstü Bitmiş Depo 'Yeniden Üretime Al' (`ReworkRollsDialog` → `quick-start`), tablet `scanClassify` — statü kontrolü ÇUVALDAN ÖNCE (SHIPPED topa 'çuvaldan çıkarın' deme). · bekçi: `mobil scanClassify.test.ts (12), Electron ReworkRollsDialog.test.tsx` <sub>(CLAUDE.md:83, arşiv:756)</sub>

### Yasaklar

- **[ÇEKİRDEK]** Rezervasyon gelirse (`shipping.reservationEnabled`, MODUL-BAYRAK karar #6 + §9) KENDİ append-only defteriyle kurulur; `SackAllocation`a DOKUNULMAZ — o sevk muhasebesidir. Bayrak bugün kodda YOK; 'rezerv YOK' profil seçimidir, kalıcı domain kuralı değil. <sub>(CLAUDE.md:41, CLAUDE.md:17)</sub>
- **[ÇEKİRDEK]** Brüt/donmuş-belge kuralı refakat kartına UZANMAZ — o kural para/irsaliye yüzeyine aittir; kart malla gezen operasyon kâğıdıdır: parti bloğu baskı anında canlı (`resolveLiveBatches`), sevk yalnız iptal edilmemişlerden (en yenisi + `(+N)`). · bekçi: `scripts/test_traveler_card_a5_batches.ts` <sub>(CLAUDE.md:137)</sub>
- **[ÇEKİRDEK]** İade belgesi ÜYE id'siyle ÇÖZÜLMEZ (`buildReturnDispatchDoc` null; aksi hâlde lazy-init grubun İKİNCİ resmi kopyasını dondurur) → istemci `documentSourceId` (`returnGroupId ?? id`) kullanır, alan `select`ten düşürülmez. Grup iptali VOID DEĞİL REVİZE: `reissueForSourceTx` v+1; son kalem → VOIDED. · bekçi: `scripts/test_return_bulk_group.ts (üye-id kapısı kaldırılınca 2 kontrol kırmızı)` <sub>(CLAUDE.md:63)</sub>

### Reçeteler

- **[ÇEKİRDEK]** Yeni sevk-içeriği yüzeyi eklerken sor: 'sevkten SONRA da koşar mı?' — freeze tek koruma DEĞİLDİR: `reissue` ve lazy-init aynı üreticiyi sevkten sonra çağırır. Sevk tx'i içindeki freeze ve PLANNED taslak önizlemesi NO-OP (iade `roll.status=SHIPPED` istediği için orada RollReturn doğamaz). · bekçi: `scripts/test_dispatch_report_gross.ts §2b` <sub>(CLAUDE.md:48, CLAUDE.md:47)</sub>

### Kararlar

- **[PROFİL]** [PROFİL] Bu kurulumda mühür ve rezerv YOK; `shipping.confirmationEnabled` varsayılan KAPALI → sevkiyat doğrudan DISPATCHED (yanıt dispatched=true); AÇIK → PLANNED kalır, çıkış `dispatchShipment` ile, kapı önü ara adımı YOK (PLANNED→DISPATCHED). <sub>(CLAUDE.md:41, arşiv:24, CLAUDE.md:17)</sub>
- **[ÇEKİRDEK]** Toplu iade: defter SATIR bazlı kalır (N top = N `RollReturn`; brüt kuralı ve `prevSackId` geri-ekleme buna dayanır), belge GRUP bazlı: tek irsaliye, `returnGroupId` = grup liderinin id'si; `POST /api/returns` `rollIds[]` (+ `rollId` aynen, APK değişmedi), `GET /api/returns/lookup-sack?sackCode=`. · bekçi: `scripts/test_return_bulk_group.ts (29)` <sub>(CLAUDE.md:63)</sub>
- **[PROFİL]** [PROFİL] `docItemNameMode`: `bizdeki` (varsayılan, bayt-bayt bugünkü çıktı) | `musterideki` (FAIL-OPEN — karşılığı yoksa bizimki) | `ikisi`; `columns[tablo].labels` kullanıcı girdisi → `applyColumnCfg` tek noktada kaçırır, boş dize = varsayılan; fasondan DOĞRUDAN sevk + Excel KAPSAM DIŞI. · bekçi: `scripts/test_shipment_doc_customer_name.ts` <sub>(CLAUDE.md:111)</sub>

## Backend


### Değişmezler

- **[ÇEKİRDEK]** Depo çuvalı içeriğine dokunan yol sırayla: `touchWarehouseSackTx` (WHERE shipmentId IS NULL; atanmış çuval 409) → PLANNED küme değişirse `touchShipmentPlannedTx` → içerik değişince `markSackContentChangedTx` (eski adı resetSackWeightsTx; kg sıfırlar, `labelDirty` AYRI updateMany). · bekçi: `scripts/test_label_dirty_sources.ts (labelDirty yönü); guard sırası için bekçi y` <sub>(CLAUDE.md:325, CLAUDE.md:75)</sub>
- **[ÇEKİRDEK]** Toplu muhasebe export'unda sevk satırları BRÜT (`RollReturn` geri-ekleme; snapshot değil — 2000 sevkiyat × çeki satırı perf kuralı 13) ve ayrı 'İade' sayfası: 'sevk − iade = net' doğru; iki kümenin kapsamı bilinçli farklı (sevk = dönemde sevk edilen, iade = dönemde iade alınan). <sub>(CLAUDE.md:45, arşiv:53)</sub>
- **[ÇEKİRDEK]** Sevkiyat iptal gövdesi TEK KAYNAK `cancelPlannedShipmentTx` (atomik `expectedStatus` claim): `cancelShipment` ('İptal Et', yalnız PLANNED) ve `undoDispatch({releaseSacks})` onu çağırır — ikinci kopya yazılmaz (ayrışınca storno kapanışı çuvalı unutur). DISPATCHED iptal edilemez, storno gerekir. · bekçi: `scripts/test_shipment_undo_dispatch.ts §10` <sub>(CLAUDE.md:79)</sub>
- **[ÇEKİRDEK]** `weighRequiredEnabled` / `manualWeightRestrictedEnabled` yazıldı AÇILMADI (manual kısıtı eski istemcileri 403'e düşürür); düz okuyucu, resolver YOK (§3.6 yalnız ebeveynli bayraklara). Açılınca MANUAL yalnız `shipping:write` taşıyana serbest (`assertManualWeightAllowed`, F221). · bekçi: `scripts/test_shipping_flags.ts (önkoşul cümlesi metinden ölçülür)` <sub>(CLAUDE.md:106, CLAUDE.md:329)</sub>

### Yasaklar

- **[ÇEKİRDEK]** `Sack.notes` annotation'dır: `setSackNotes` `touchWarehouseSackTx` KULLANMAZ (sevk edilmiş çuvala da yazılır), `markSackContentChangedTx` nota DOKUNMAZ; guard'ı 'eksik' sanıp EKLEME (özellik 409'a düşer). Not yalnız etkin şablon `sackNote` basıyorsa `labelDirty` yazar (`sackNoteAppearsOnLabel`). · bekçi: `scripts/test_sack_notes.ts (8b: PLANNED sevkiyattaki çuvala yazıldı, 409 değil)` <sub>(CLAUDE.md:43, CLAUDE.md:325, CLAUDE.md:75)</sub>

### Reçeteler

- **[ÇEKİRDEK]** Storno kapsamı DAR ve sebebi SÖYLENİR: `resolveUndoBlockReason` TEK KAYNAK (önizleme + mutasyon aynı yüklem; ayrışırsa ekran 'yapılabilir' der, uç 409). Engeller: faturalanmış · iadesi alınmış · `shipping.undoDispatchSameDayOnly` (varsayılan KAPALI; takvim günü, `factoryDayStart()`). · bekçi: `scripts/test_shipment_undo_dispatch.ts` <sub>(CLAUDE.md:63)</sub>

## Panel (Electron)


### Değişmezler

- **[ÇEKİRDEK]** Sevk Kapısı karosu `visibleWhen` SAF bayrak (`ctx.shipmentConfirmationEnabled`); 'VEYA çıkış bekleyen PLANNED' dalı ve `board?limit=1` sondası KALKTI, `OperationsVisibilityContext` tek alan — sayaç geri EKLENMEZ. Route `/operations/sack-store` bayrağa bakmaz: derin bağlantı açılır. · bekçi: `Electron/src/pages/Operations/tile-visibility.test.ts (ctx anahtar listesi kilit` <sub>(CLAUDE.md:79)</sub>

- **[ÇEKİRDEK]** Çuvalın içi ÜÇ ADI yan yana gösterir (bizdeki · müşterideki · TOPUN ETİKETİNDE yazan); müşteri karşılığı yoksa sütun BOŞ kalır (bizim adımız müşterinin adıymış gibi basılmaz) ve etiket adı `lastLabelSnapshot`tan gelir, canlı kayıttan TÜRETİLMEZ. Ekran ve PDF/Excel dökümü AYRI uçlardan beslenir, ikisi birlikte değişir. · bekçi: `scripts/test_sack_contents_uc_ad.ts` <sub>(arşiv:2026-09-07 saha turu)</sub>
- **[ÇEKİRDEK]** 'Siparişe yazılamayanlar' ekranında numaralar TIKLANABİLİR ve sipariş SOLDAN, sevkiyat SAĞDAN, İKİSİ AYNI ANDA açılır (`hideOverlay`); liste ucu numaranın yanında `orders: [{id, orderNumber}]` taşır — numaradan id aramak mükerrer numarada yanlış siparişi açardı. · bekçi: `Electron RepairTable.test.tsx` + `scripts/test_allocation_repair.ts §5` <sub>(arşiv:2026-09-07 saha turu)</sub>

### Tuzaklar

- **[ÇEKİRDEK]** Yeniden üretime almada fason firma seçicisi LOAD-BEARING: firma `stepPlanning` overlay'iyle gitmezse 'Fasona gönder' sessiz no-op (backend firmayı çözemezse sevki atlar); buton metni ile davranış AYNI yüklemden (`willDispatch`). Ölü etiket uyarısı burada KESİN ve DAR (etiketli VE ilk adım fason). · bekçi: `Electron ReworkRollsDialog.test.tsx` <sub>(CLAUDE.md:83, arşiv:756)</sub>

### Reçeteler

- **[ÇEKİRDEK]** PLANNED sevkiyat Sevk Kapısı'na muhtaç DEĞİL: Sevkiyatlar detayı (sheet + tam sayfa) ortak `DispatchConfirmDialog` ile 'Sevk Et' verir (`shipping:write`; çuvallar canlı listelenir, irsaliye başarı panelinden basılır). Bayrak açıkken kurulup sonra kapatılmış eski PLANNED'lar da buradan çözülür. · bekçi: `Electron Shipments + SackStore vitest (38)` <sub>(CLAUDE.md:79)</sub>

### Kararlar

- **[PROFİL]** Sevkiyat detayında iade satırı ÇUVALDA KALIR: `returned` işaretli, soluk + amber 'İade' rozeti, salt-okunur, metraj üstü ÇİZİLMEZ (brüt toplama dahildir, irsaliyede durur); ayrı 'Bu sevkiyattan iade edilenler' kartı AYNEN kalır — rozet onun yerine geçmez, topun konumunu söyler. <sub>(CLAUDE.md:47)</sub>
- **[PROFİL]** 'Sevkiyatı da kapat' kutusu YALNIZ `confirmationEnabled` AÇIKKEN çizilir (2026-09-07); kapalı rejimde seçenek yoktur — geri alma daima serbest bırakır ve pencere bunu cümleyle söyler (o rejimde PLANNED bir ara durak değildir, işaretsiz seçim karşılığı olmayan duruma düşürüyordu). Kapatılan sevkiyat aynı numarayla çıkmaz — Paketleme'den YENİ sevkiyat (eski irsaliye VOIDED). · bekçi: `scripts/test_shipment_undo_dispatch.ts §10 (önizleme confirmationEnabled boolean` <sub>(CLAUDE.md:79, arşiv:469, arşiv:2026-09-07 saha turu)</sub>
- **[PROFİL]** [PROFİL] 'Sevkiyatlar' (operasyon: tüm statüler, iptal) ile 'Sevkiyatlar (Muhasebe)' (salt-okunur DISPATCHED + fiş + dönem Excel + fatura izi) ayrı ekran ama izinle AYRILMADI (sevkiyatçının belgelere erişimi gerekir); muhasebe ekranı finance.enabled açıkken terfi eder (dış muhasebe köprüsü). <sub>(arşiv:53, CLAUDE.md:45)</sub>

## Tablet (mobil)


### Değişmezler

- **[ÇEKİRDEK]** Mobil cihaz çözümü OTURUM-KAPSAMLI: `useMachinePeripherals('METER'|'SCALE')` → `GET /peripherals/for-session` (makinesiz SHIPPING istasyon-oturumu → istasyon donanımı; oturum yoksa BOŞ, fail-closed). Çuval tartısı SCALE; `btPrinterStore` KALDIRILDI; 409 `WORK_SESSION_REQUIRED` oturumu düşürür. · bekçi: `mobil/src/hooks/useMachinePeripherals.test.ts` <sub>(CLAUDE.md:220)</sub>
- **[ÇEKİRDEK]** Tartı FAIL-CLOSED: cihaz yok / bond edilemedi / okunamadı / değer ≤ 0 → NET Türkçe toast, mutasyon HİÇ ÇAĞRILMAZ; sessiz sahte değer yok. · bekçi: `mobil/src/hooks/useSackWeigh.test.tsx` <sub>(CLAUDE.md:328, CLAUDE.md:323)</sub>
- **[ÇEKİRDEK]** BT tek soket: ikinci dokunuş `busyRef` ile SESSİZCE yok sayılır, kuyruğa ALINMAZ (hangi kg hangi çuvala gitti karışır); spinner yalnız işlem gören satırda (`weighingSackId`). Donanım okuması TEK DOKUNUŞ; elle gir/etiket/not/sil ⋮ `SackActionsSheet`e (≥56dp) taşınır, yıkıcı aksiyonda onay KORUNUR. · bekçi: `mobil/src/hooks/useSackWeigh.test.tsx` <sub>(CLAUDE.md:330, CLAUDE.md:331, CLAUDE.md:323)</sub>
- **[ÇEKİRDEK]** Okutma geri bildirimi ekran ekran KOPYALANMAZ — aynı okutma iki ekranda iki şey söylerse operatör hangisine güveneceğini bilemez; tek kaynak üç ortak katman (`signalScan` · `useScanFeedback` · `ScannerRollStrip`). <sub>(CLAUDE.md:385)</sub>

### Yasaklar

- **[ÇEKİRDEK]** Mobil istemci brüt rakamı ELLE KURMAZ (`rollCount + returnedCount` çift sayar); çuval kartındaki işaret `sack.returnedCount` ile konur, iade dipnotu backend'in `summary.returnedCount/returnedMeters` alanından basılır. <sub>(CLAUDE.md:47)</sub>

### Kararlar

- **[PROFİL]** [PROFİL] Mobil ekranlar: `TartiPaket/` etiketi 'Sevkiyat', `Sevkiyat/` etiketi 'Sevk Çıkışı'; izinler `mobile:tarti-paket` / `mobile:sevkiyat`; backend `/api/shipping`. Ekran KEY ≠ görünen ETİKET. 'Sevk Çıkışı' izin kapılı, `confirmationEnabled`a bakmaz. 'İmplementasyon sırası' listesi tarihsel. <sub>(CLAUDE.md:262, CLAUDE.md:79)</sub>

## Geçersiz kılınan kurallar — bunlara UYMA

- **KISMI** `A:2026-08-02__2026-08-02-sevk-rakami-brut` → `R:2026-08-05__2026-08-05-belge-yolu-da`: 'collectShipmentDocContent hâlâ canlı okur — sevk anı garantisini freeze adımı verir' cümlesi geçersiz: belge üreticisi reissue + lazy-init ile sevkten SONRA da koşuyor; artık iptal edilmemiş RollReturn'ü prevSackId ile geri ekleyen BRÜT üretir. ✅ çürütmeden geçti
- **KISMI** `R:2026-08-03__2026-08-03-brut-kurali-sevkiyat` → `R:2026-08-05__2026-08-05-belge-yolu-da`: 'Kapsam DIŞI: reissue + lazy-init yolları' açık maddesi kapatıldı (arşivde üstü çizili). sack-search / çuval etiketi yeniden basımının canlı sack.rolls okuması kapsam dışı KALIR. ✅ çürütmeden geçti
- **KISMI** `R:2026-08-05__2026-08-05-sevk-storno-iade` → `R:2026-08-22__2026-08-22-sevk-kapisi-bayragin`: Faz 0 yan düzeltmesi 'Sevk Kapısı karosu: bayrak açık VEYA çıkış bekleyen PLANNED sevkiyat var' KALDIRILDI → karo SAF bayrak, sack-store/board?limit=1 sondası kalktı. Kapalı rejimde PLANNED'ın çözümü storno releaseSacks ve Sevkiyatlar detayındaki 'Sevk Et'. Paketleme kilit uyarısı metni de değişti. ✅ çürütmeden geçti
- **KISMI** `B:undated__depo-cuvali-icerigine-dokunuyorsa-once-where` → `R:2026-08-21__2026-08-21-aksam-tutarlilik-taramasi`: Üçüncü guard'ın adı ve kapsamı değişti: resetSackWeightsTx → markSackContentChangedTx (kg sıfırlar + labelDirty'yi AYRI updateMany ile yazar). Sack.notes istisnası KALIR, ama not artık etkin şablon sackNote basıyorsa labelDirty yazar (sackNoteAppearsOnLabel). Alt-backend metni (Teks-Erp/CLAUDE.md:325-326) ve schema yorumları (4499/4517) hâlâ eski adı taşıyor. ⚠️ çürütücü itiraz etti — ihtiyatla
- **KISMI** `A:2026-08-02__2026-08-02-brut-kurali-liste` → `R:2026-09-03__2026-09-03-dilim-2-davranis`: 'ERP fatura KESMEZ' yalnız finance.enabled KAPALIYKEN doğru: finans açık kurulumda sevk sonrası taslak fatura ERP içinde doğar (maybeAutoDraftInvoiceAfterDispatch, üç dispatch yolunun ortak kancası); invoiceNo dış izi ile iç faturanın çakışması shipping.invoiceMode (dis|ic|ikisi) ile yönetilir. ✅ çürütmeden geçti
- **KISMI** `R:2026-08-25__2026-08-25-bitmis-topu-yeniden` → `A:2026-08-25__2026-08-25-aksam-mobil-yeniden`: 'AÇIK: tablet hâlâ bitmiş topu okutamıyor (ayrı karar, APK ister)' kapandı: mobil Hızlı İş Emri okutması saf scanClassify yüklemine alındı ve WAREHOUSE/A1_STOCK topu da kabul eder (APK 2.9.6/vc53). Masaüstü ReworkRollsDialog ve fason firma seçicisi kuralı değişmedi. ✅ çürütmeden geçti

## Çözülmüş çelişkiler

- `R:2026-08-05__2026-08-05-sevk-storno-iade` ↔ `R:2026-08-22__2026-08-22-sevk-kapisi-bayragin`: Görünür çelişki, gerçek değil — iki ayrı yol: DÜZ storno (DISPATCHED→PLANNED) tahsisi KORUR, yalnız ShipmentOrder.isActive=true + recompute (shippedQty PLANNED tahsisi saymadığı için kendiliğinden düşer). releaseSacks ile storno aynı tx'te cancelPlannedShipmentTx çağırır; KAPANIŞ (CANCELLED) olduğu için 2026-07 modelinin 'iptalde tahsis silinir' kuralı geçerli. Kod ikisini de uygular.
- `M:2026-07-30__cihazda-yazma-2026-07-30` ↔ `R:2026-09-03__2026-09-03-dilim-2-davranis`: Bugün çelişki yok: MANUAL, SIMULATED kapısından muaf KALIR; manualWeightRestrictedEnabled AYRI bir kapı ve varsayılan KAPALI (assertManualWeightAllowed: restricted false → no-op). Açılırsa MANUAL yalnız shipping:write taşıyana serbest, tablet operatörü 403 MANUAL_WEIGHT_RESTRICTED alır; weighSack + openSack aynı kapıdan. mobil/CLAUDE.md:329 'muaf' cümlesine bayrak şerhi eklenmeli.

## Doğrulama turu ekleri (eski CLAUDE.md ↔ yeni yapı karşılaştırması, 2026-09-05)

- **[PROFİL]** Sipariş görünümü üç kolon: İstenen | Sevk | Açık (PLANNED tahsis sayılmaz). Tasarım: `docs/design/CUVAL-HAVUZU-TASARIM.md`. <sub>(kök 2026-07)</sub>

## Bekçiler — bu alana dokununca koş (115 backend · 21 istemci)

`cd Teks-Erp && npx tsx scripts/run-all-tests.ts <ad-parçası>` (tek testte tip kapısı atlanır) · Electron `npx vitest run <yol>` · mobil `npx jest <yol>`.

**Ne ölçtükleri, DB gerektirip gerektirmedikleri ve bayatlık işaretleri: `Teks-Erp/docs/BEKCI-HARITASI.md` → bu alanın bölümü.** ⚠️ = orada gerekçesi yazılı bayatlık şüphesi.

Backend: `test_accounting_direct_ship`, `test_accounting_export`, `test_audit_followups`, `test_auto_draft_shipment`, `test_barcode_free_flow`, `test_batch_multibatch_dispatch`, `test_batch_partial_dispatch_autosplit`, `test_batch_split_new_wo_modes`, `test_batch_trace`, `test_branch_code_docs`, `test_bulk_dispatch_rollids`, `test_client_token_collision`, `test_consistency`, `test_customer_sack_delete_guard`, `test_denetim_s2_paketi`, `test_depo_multi_regime_gate`, `test_depo_roll_cancel_permission`⚠️, `test_direct_ship_api`⚠️, `test_direct_ship_fason`, `test_direct_ship_scenarios`, `test_dispatch_allocation_fresh`, `test_dispatch_claim_step_match`, `test_dispatch_cross_wo_batch_guard`, `test_dispatch_print_options`, `test_dispatch_report`, `test_dispatch_report_gross`, `test_doc_sample_html`, `test_document_customization`, `test_e2e_full_flow`, `test_fason_dispatch_picker`⚠️, `test_fason_visibility`⚠️, `test_filter_multi_select`, `test_finance_invoice`, `test_goods_receipt`, `test_goods_receipt_invoice`, `test_gr_order_price_currency`, `test_hide_cancelled_lists`, `test_iade_enhancements`, `test_inventory_shipment_scope`, `test_kartela_stock_and_ship`, `test_label_context_fit`, `test_label_dirty_sources`, `test_manual_move`, `test_manual_roll_undo`, `test_manual_sack_count`, `test_new_documents`, `test_null_quality_visibility`, `test_o19_operator_trace`, `test_open_orders_search`, `test_order_leadtime`, `test_order_shipments`, `test_p2_inventory`, `test_performance`, `test_printed_documents`, `test_production_flow_columns`, `test_purchase_order`, `test_quick_shipment`, `test_race_conditions`, `test_raw_sale_quick_order`, `test_raw_tambur_cut`, `test_return_bulk_group`, `test_return_scorecard`, `test_roll_po_line_trace`, `test_roll_qty_adjust`, `test_roll_relabel`, `test_roll_relabel_context`, `test_roll_search_barcode`, `test_roll_warehouse_stamp`, `test_sack_content_dump`, `test_sack_customer_gate`, `test_sack_label`, `test_sack_mismatch`, `test_sack_note_document`, `test_sack_notes`, `test_sack_pool_lifecycle`, `test_sack_reassign_customer`, `test_sack_search`, `test_sack_search_filters`, `test_sack_split_and_relabel`, `test_sack_status_invariant`, `test_sack_tag_document`, `test_sack_tags`, `test_sack_weigh_source`, `test_scan_code_case`, `test_semi_finished_entry`, `test_semi_finished_surfaces`, `test_shipment_detail_gross`, `test_shipment_dispatch_document`, `test_shipment_doc_batch_column`, `test_shipment_doc_customer_name`, `test_shipment_filters`, `test_shipment_invoice`, `test_shipment_list_gross`, `test_shipment_list_sort`, `test_shipment_order_ledger`, `test_shipment_scope_lock`, `test_shipment_scorecard`, `test_shipment_undo_dispatch`, `test_shipping_client_token`, `test_shipping_flags`, `test_single_warehouse_parity`, `test_spec_availability`, `test_status_labels`, `test_stock_count`, `test_stock_scorecard`, `test_ticaret_links_and_filters`, `test_ticaret_regime_gate`, `test_token_replay_cancelled`, `test_warehouse_crud`, `test_warehouse_ledger`, `test_warehouse_movements`, `test_warehouse_sack_transfer`, `test_warehouse_transfer`, `test_wo_warehouse_attach`, `test_yarn_stock`

İstemci: `FilterBar.sentinel.test.tsx`⚠️, `shipping-flags.test.ts`⚠️, `ShipmentInvoiceDraft.test.tsx`, `accounting-export.test.ts`, `invoiceDraftLines.test.ts`, `invoiceDraftVisibility.test.ts`, `OrderShipmentsCard.test.tsx`, `returnInvoice.test.ts`, `quickShip.test.ts`⚠️, `SackEditorView.ship.test.tsx`, `SackEntryGate.test.ts`, `ShipmentOrderSelect.test.tsx`, `dumpSheets.test.ts`, `sackTagBulk.test.ts`, `DispatchConfirmDialog.test.tsx`, `SackStorePage.test.tsx`, `shipmentDetailError.test.tsx`, `DirectShipModal.test.tsx`, `tile-visibility.test.ts`, `SackTagsPage.test.tsx`, `useSackWeigh.test.tsx`⚠️

## Arşiv notları (tam metin, gerekçe ve ölçüm)

- — · NOT (tarihsiz) — `CLAUDE-NOT-ARSIVI.md:24-27`
- 2026-08-02 · 2026-08-02 — sevk rakamı BRÜT'tür; iade onu geriye dönük değiştiremez — `CLAUDE-NOT-ARSIVI.md:45-52`
- 2026-08-02 · 2026-08-02 — brüt kuralı LİSTE yüzeyine de uzandı + muhasebe ekranı tamamlandı — `CLAUDE-NOT-ARSIVI.md:53-61`
- 2026-08-03 · 2026-08-03 — brüt kuralı ÇUVAL İÇERİĞİ yüzeyine de uzandı; üçüncü ve son yüzey — `CLAUDE-NOT-ARSIVI.md:62-71`
- 2026-08-05 · 2026-08-05 — BELGE YOLU da brütleşti; "canlı okuyor" ≠ "net üretiyor" ayrımı — `CLAUDE-NOT-ARSIVI.md:72-79`
- 2026-08-05 · 2026-08-05 — SEVKİ GERİ AL (STORNO) ≠ İADE + çuval bazlı TOPLU İADE tek belgeyle — `CLAUDE-NOT-ARSIVI.md:251-266`
- 2026-08-05 · 2026-08-05 — PARTİ NO KÂĞIDA BASILIR + refakat kartı "bayat" bayrağı — `CLAUDE-NOT-ARSIVI.md:230-240`
- 2026-08-21 · 2026-08-21 akşam — Tutarlılık taraması: "türetilmiş alan / ayrışan yüzey" sınıfı kapatıldı):** `WorkOrder.type — `CLAUDE-NOT-ARSIVI.md:464-468`
- 2026-08-22 · 2026-08-22 — SEVK KAPISI = BAYRAĞIN EKRANI; storno kapalı rejimde sevkiyatı KAPATIR — `CLAUDE-NOT-ARSIVI.md:473-480`
- 2026-08-25 · 2026-08-25 (akşam) — Mobil "Yeniden Üretime Al" + Fason Kabul boşluğunun GERÇEK sebebi — `CLAUDE-NOT-ARSIVI.md:840-891`