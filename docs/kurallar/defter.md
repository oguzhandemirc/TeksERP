# Defter · Hareket tablosu · Ters kayıt · Hard delete

> Alan kural dosyası — bu alana dokunmadan ÖNCE okunur. Kaynak: 2026-09-10 defter-öncelikli mimari notu (`docs/history/CLAUDE-NOT-ARSIVI.md`). Hikâye, ölçüm ve gerekçe arşivde; burada yalnız bugün geçerli kural. Sınıf: **[ÇEKİRDEK]** her kurulumda aynı · **[PROFİL]** bu fabrikanın seçimi.

> Bu dosya 2026-09-10'da AÇILDI ve kök `CLAUDE.md`'nin "hard delete DÖRT sınıf" cümlesini EZDİ. Doktrin üç ilkedir: hard delete yok · izlenebilirlik defterle · geri alma ters kayıttır. Alan kesişimlidir: sevkiyat, fason, top düzeltme, finans ve yetki dosyalarının hepsini bağlar.

## Doktrin — tek cümlelik hâli

Durum tabloları **"şu an ne"**yi tutar; defterler **"ne oldu"**yu tutar ve "ne oldu" asla değişmez. Bir şeyi geri almak, onu yok etmek değil **tersini yazmaktır**.

## Ortak (backend + panel + tablet)

### Değişmezler

- **[ÇEKİRDEK]** Audit (`system_logs` · `system_log_archives`) yalnız AYAK İZİDİR: çalışan programda onu yalnız ayak izini bir İNSANA gösteren yüzeyler okur (denetim ekranı · denetim raporu · kayıt geçmişi/künye · yedek etki tanısı · audit'in kendi arşiv/boyut altyapısı). Audit'ten karar, hesaplanan iş sayısı, durum, geri alma ya da join TÜRETİLMEZ — bilgi iş kararına giriyorsa kendi hareket tablosunda ya da kalıcı kolonunda durur (iş emri ↔ iş emri hareketleri gibi) ve audit'e uzanma ihtiyacı bir defter eksikliğidir. Tek istisna, geçmişi yeni bir deftere BİR KEZ aktaran beyanlı ve tarihli göç script'idir; yeni allowlist satırı yalnız yönetici onayıyla. · bekçi: `test_audit_okuma_kaynagi.ts` (backend `dosya#işlev` allowlist'i eşitlikle · istemci audit ucu çağıranları · borç cırcırı K-A1…A4, hedef 0) <sub>(arşiv:2026-09-25)</sub>
- **[ÇEKİRDEK]** Geri alma, ileri işlemin TERS KAYDINI bugüne yazar; ileri kaydı ne siler ne değiştirir. İşlem sonrası DURUM ileri-öncesi durumla aynı olabilir, DEFTER asla aynı olmaz. · bekçi: `YOK` · Kapanır: `test_defter_ters_yol beyanlı her tersYazan sembol gövdesi için ileri damgaya null ataması 0 (dispatchedAt · weighedAt · invoicedAt · remainderClosedAt · closedTermsAt · finalizedAt) ayağı kazanıp yeşil verince; silme yarısı bugün §10 (silen beyanı) ile ölçülü, null'lama yarısı ölçülmüyor (2026-09-13, yüklem grep -n 'null' scripts/test_defter_ters_yol.ts → damga taraması yok)` · Çapa: `test_defter_ters_yol` · Öncül: ölçüldü <sub>(arşiv:2026-09-10)</sub>
- **[ÇEKİRDEK]** Malın çıkışı BRÜT yazılır ve dönen kısım AYRI satırla kapanır (sebep kodu zorunlu); tek bir "net" satır, fabrikanın ölçtüğü iki sayıdan birini kalıcı kaybeder ve bir daha türetilemez. Bu kural sevk topuna olduğu kadar İPLİK ÇIKIŞINA da uygulanır — iplik çıkışı da bir çıkıştır. Dönenin nereye gittiği (depoya iade / başka kullanıma aktarım / telef) bayrak değil SEBEP KATALOĞU satırıdır ve her işlemde seçilir; tartmayan fabrikada iade satırı hiç doğmaz — yokluk zaten cevaptır. · bekçi: `YOK` · Kapanır: `sevk yarısı test_dispatch_report_gross ile ölçülü (fiş brüt kalır, iade ayrı özet); kalan ikisi kapanınca: (a) roll_returns'a 'reasonId ya da reasonText dolu' CHECK'i inip test_db_invariants envanterine yazılır — create yolu return.service:471'de zorunlu, update yolu :1072'de ikisini de boşaltabiliyor, fabrika kopyasında roll_returns WHERE reasonId IS NULL AND coalesce(reasonText,'')='' → 0/5; (b) yarn_movements'a reasonCode kolonu iner (ReasonPreset.code, FK'sız — Roll.cancelReasonCode emsali) ve dönüş satırlarında (bugün ayrı RETURN kind yok, dönüş ADJUST_IN ile yazılıyor — ölçüldü 2026-09-13, YarnMovementKind = IN·OUT·ADJUST_IN·ADJUST_OUT) reasonCode IS NULL sayısı 0 olur; test_yarn_stock 'ADJUST_IN sebep kodsuz 400' + 'karne dönüşü kodla kırar' iki ayak alır — fabrika kopyasında yarn_movements 0 satır (iplik modülü kapalı), sayı ancak iplik açık kurulumda anlamlı` · Çapa: `test_dispatch_report_gross` · Öncül: ölçüldü <sub>(arşiv:2026-09-12 devere)</sub>
- **[ÇEKİRDEK]** Deftere yazan her ileri olayın **beyan edilmiş bir ters MEKANİZMASI** vardır — damga · ters bağ · tipli enum çifti · net karşı olay · **karşı kayıt** (aynı deftere from↔to takaslanmış ikinci satır; yazanı ileri yolun KENDİSİDİR — `MachineStopReclass`/`reclassifyStop`, 2026-09-14); `*_CANCEL`/`*_REVERSAL` adı bunlardan yalnız biridir ve **ad kalıbı ölçüt DEĞİLDİR** (ölçüldü: 13 olay türünün 6'sının tersi mekanizma olarak var, adı `_CANCEL` değil). Yeni ileri olay ya da sebep kodu eklenirken ters mekanizması AYNI commit'te beyana girer — tersi olmayan ileri olay, geri alınamayan ya da izsiz geri alınan bir iş demektir. · bekçi: `test_defter_ters_yol.ts` §3 (mekanizma şemada gerçek; §3k karşı kayıtta yön kolonları + ters yazanın İLERİ yazan dosyada olması; §3e İKİNCİ YÖN — enum'un ŞEMADAKİ her değeri bir çiftte ya da `CIFT_DISI_DEGERLER`de gerekçeli, sınıf kapalı küme DOGUS/TERMINAL/BORC ve BORC'un sahibi zorunlu) · §6 (mekanizması yok ⇒ borç, iki yönlü) · §13 (her sebep kodu beyanlı · ileri↔ters simetrik · yazarı var) <sub>(arşiv:2026-09-10; eksen 2026-09-13'te düzeltildi, kök `9d2e2bb1`)</sub>
- **[ÇEKİRDEK]** Bir varlığın durumunu/miktarını değiştiren HER kod yolu o varlığın defterine satır yazar; tek-kayıt yolu yazıp toplu yol yazmıyorsa bu bir DELİKTİR, üslup farkı değil. Ham `updateMany` ile durum değiştirip defter kapısını atlamak yasaktır. · bekçi: `YOK` · Kapanır: `stok defteri yazar-kümesi cırcırı test_stok_defteri_bag_olcumu §4e K_TABAN 2 → 0'a inince (üye kümesi adıyla: rescueStuckRoll · cutOpenFabric çocuğu — giriş yönü, 01 doff sonrası yazar) — 2026-09-13 öğle 'K=0' iddiası aynı gece ÇÜRÜDÜ (1c): liste beş yolu saymıyordu VE §4 gerçek-ağaç K'yı yalnız bilgi satırına basıyordu, hiç assert etmiyordu (assert etmeyen kapı); çıkış üçlüsü aynı gece postProductionIssuesTx tek yazıcısına bağlandı ve listede KALDI; K'nın üçüncü ekseni (yerinde miktar değiştiren yol) §4g ile SERT ve bağlı (adjustRollQty, 2026-09-13); kural HER defter için: iplik (YarnMovement) ve fason sapması (RollVariance) yazar kümeleri için aynı K/V tarayıcısı kurulup K=0 verince — o ikisi ölçülmedi` · Çapa: `test_stok_defteri_bag_olcumu` · Öncül: ölçüldü <sub>(arşiv:2026-09-10)</sub>
- **[ÇEKİRDEK]** Append-only defter satırı GÜNCELLENMEZ ve SİLİNMEZ; bu yüzden `updatedAt` almaz ve kronolojisi `createdAt`'tir. Bir tablo hem defter hem durum kaynağı OLAMAZ — ikisi gerekiyorsa iki tablodur. · bekçi: `test_db_invariants.ts (kısmi)` <sub>(arşiv:2026-09-10, [DB-09])</sub>
- **[ÇEKİRDEK]** Ters kaydın BİÇİMİ deftere göre değişir: negatif/karşı satır ancak DB izin veriyorsa yazılır — `payment_allocations_amount_positive` gibi bir CHECK varsa doğru yol `revokedAt`/`revokedById`/`revokeReason` DAMGASIDIR. Damgalı defterde okuyan HER yol aktif yüklemi TEK helper'dan alır (`PaymentAllocation` → `ACTIVE_ALLOCATION`; çuval tahsisinin ikizi ayrı addır — `ACTIVE_SACK_ALLOCATION`). · bekçi: `test_payment_allocation.ts` §15s <sub>(arşiv:2026-09-11)</sub>
- **[ÇEKİRDEK]** Tekrar edebilen bir çevrimin izi TEK KOLONA sığmaz: sevk → geri al → sevk turunda `undispatchedAt` gibi tek damga ikinci turda birincisini ezer. Böyle çevrimler OLAY DEFTERİ ister (`ShipmentEvent`, `ChequeEvent` emsali); durum kolonu güncel gerçeği, defter geçmişi taşır. <sub>(arşiv:2026-09-11)</sub>
- **[ÇEKİRDEK]** Tekil kısıt taşıyan bir defteri damgaya çevirirken kısıt PARTIAL'a döner (`WHERE "revokedAt" IS NULL`) — yoksa geri alınmış satır dururken aynı anahtar yeniden yazılamaz ve iş TEKRARLANAMAZ hâle gelir. ⚠️ Prisma'nın `@@unique`i CONSTRAINT değil INDEX üretir: düşürmek için `DROP INDEX` gerekir, `DROP CONSTRAINT IF EXISTS` SESSİZCE hiçbir şey yapmaz. Şemada `@@unique` `map:` ile adlandırılır ki drift yalnız predicate farkını görsün. · bekçi: `test_roll_operation_revoke.ts` §2/§4 <sub>(arşiv:2026-09-11)</sub>
- **[ÇEKİRDEK]** Durumu defterden SAYARAK türeten yol (`recomputeStepStatus`) her sayımında — açık, kapalı, bekleyen aday, giriş noktası — aktif yüklemi taşır; geri alınmış satır sayılırsa adım hata vermeden yanlış duruma geçer. · bekçi: `test_roll_movement_revoke.ts` §5 <sub>(arşiv:2026-09-11 B-4b)</sub>
- **[ÇEKİRDEK]** Geri alınmış defter satırı bir daha DEĞİŞTİRİLMEZ — kapatılmaz, yeniden açılmaz, damgası silinmez; tek istisna izin yeniden bağlanmasıdır (iş emri bölmede hareket/operasyon izi, geri alınmışlar dahil, topla birlikte aynı istasyonlu klon adıma taşınır). · bekçi: `test_roll_movement_revoke.ts` §5e/§6e <sub>(arşiv:2026-09-11 B-4b)</sub>
- **[ÇEKİRDEK]** Bir kaydın GEÇMİŞİNİ koruyan guard (adım silme, başlamış adımın istasyon/sıra değişimi, makine/istasyon kalıcı silme) geri alınmış defter satırını da SAYAR: geri alma "hiç olmadı" demez, "yapıldı ve geri alındı" der. · bekçi: `test_roll_movement_revoke.ts` §6e · `test_roll_operation_revoke.ts` §7d <sub>(arşiv:2026-09-11 B-4b)</sub>
- **[ÇEKİRDEK]** Defter İŞ VERİSİDİR: arşivlenmez, budanmaz. Büyüme index/partition ile karşılanır, satır silerek değil. <sub>(arşiv:2026-09-10)</sub>
- **[ÇEKİRDEK]** Çalışma oturumu (`WorkSession`) geçmişi hiçbir yoldan silinmez; oturumu olan istasyon/makine kalıcı silinemez (409 `workSessionCount`), pasife alınır. · bekçi: `test_work_session_history_guard.ts` <sub>(arşiv:2026-09-11)</sub>
- **[ÇEKİRDEK]** Sayaç-bazlı düşüm belgesi ("N adet düş") etkilediği kayıtları KALEM satırına yazar ve stornosu kalemden okur; kalemsiz düşüm geri alınamaz (409), geçmişi audit'ten uydurulmaz. · bekçi: `test_swatch_stock_reduction_reversal.ts` §1/§6 <sub>(arşiv:2026-09-11)</sub>
- **[ÇEKİRDEK]** Tamamlanmış sayımın fark fişi TEK BELGEDE stornolanır: yalnız deponun en son sayımı (LIFO); sayımın düşürdüğü top hâlâ onun iptaliyle duruyorsa rafına döner, elle geri alınmışsa YALNIZ defter karşılığı yazılır (statüsüne dokunulmaz), İLERİ SATIRI ZATEN TERSLENMİŞSE yalnız sapma damgalanır — "terslenmiş mi" sorusunun TEK cevabı `reversesMovementId` bağıdır (tip sayma ya da belge bağıyla eşleme yaklaşıktır ve kardeş sayımın satırını sedde takar); ileri CANCEL satırı çıkış ucunu `fromStatus` + sebep koduyla yazar, ters satır `CANCEL_REVERSAL` tipini BETİMLEYİCİ olarak taşır, sapmaya `reversedAt`, iplikte net ters ADJUST; sayım satırı değişmez. · bekçi: `test_stock_count_reversal.ts` <sub>(arşiv:2026-09-12)</sub>
- **[ÇEKİRDEK]** 0 metrajlı top "eksik stok" DEĞİL BOŞ KAYITTIR: sayım onu farkla kapatmaz, satırı gerekçesiyle KAPSAM DIŞI bırakır (top iptal edilmez, ne defter ne sapma satırı doğar) — çözümü top ekranındaki iptal/fire'dır. Eşik TEK yüklemden (`qtyYazilabilir`) okunur; KURAL kapsam kararıdır, defter yazımındaki süzgeç yalnız KEMERDİR ve sessizce atlamaz, "kapsam kararı atlanmış" diye ADIYLA fırlatır (sessiz kemer ikinci bir sessiz atlamadır). · bekçi: `test_stock_count.ts` §14 <sub>(arşiv:2026-09-12)</sub>
- **[ÇEKİRDEK]** Satır TAŞIYAN yıkıcı işlem (master-data birleştirme) taşıdığı satırın KİMLİĞİNİ deftere yazar: taşıma kaynak başına `RETURNING` ile koşar, silinen çakışma satırının TAM fotoğrafı saklanır; geri alma defterden okur, LIFO'dur ve geri yazılamayan satırı SESSİZ bırakmaz. · bekçi: `test_master_data_merge_revert.ts` <sub>(arşiv:2026-09-12)</sub>
- **[ÇEKİRDEK]** Çekin her ileri olayının tipli stornosu vardır (`CANCELLED` hariç her terminalden tek çıkış): ters cari satır orijinaline `reversesTxnId` ile bağlanır, bugüne yazılır, durum en yeni ileri olayın (`createdAt`) `fromStatus`una döner; olay/satır bulunamazsa 409. · bekçi: `test_cheque_reversal.ts` <sub>(arşiv:2026-09-11)</sub>
- **[ÇEKİRDEK]** DURUM BAYRAĞI ≠ DAMGA: bir kolon kim/neden/miktar taşımıyorsa ve "ne oldu" append-only bir defterde satırsa o kolon DURUM BAYRAĞIDIR — `null`'a dönebilir ve geri alma defter satırını TERSLER (`remainderClosedAt` ⇄ `RollVariance(source=SUBCONTRACTOR_REMAINDER)`, `reversedAt`); kim/neden/miktar taşıyorsa DAMGADIR ve `null`'lanmaz. · bekçi: `scripts/test_consistency_derived.ts §24d` <sub>(arşiv:2026-09-12)</sub>
- **[ÇEKİRDEK]** ÜÇÜNCÜ SINIF — SNAPSHOT: durum/belge tablosunda OLAY ANI değerini taşıyan kolon ("kaydın kendi satırındaki snapshot", audit'ten türetilmez — şema şerhi `Roll.preShipStatus`) iki alt türdür ve ikisi de ne DURUM BAYRAĞI ne DAMGADIR: (a) geri almanın GİRDİSİ — yazılır, tüketilince null'lanır (`preShipStatus` · `preTamburCloseQty/Status`); (b) DONMUŞ İLERİ DEĞER — ileri kaydın parçasıdır, geri alma onu KÜÇÜLTMEZ ve yalnız sapma defteri satırı eşliğinde, yalnız aşım yönünde büyütür (`Roll.initialQty` — kuralı `top-duzeltme.md` metraj satırında YAŞAR, burada yalnız sınıf; `SubcontractorDispatchItem.dispatchedQty` · `RollReturn.qty/prevSackId/prevQualityGrade` · `RollError.errorType` · duruş `lossClass` kopyası). Snapshot'ı canlı değerden yeniden türetmek ya da "düzeltmek" ileri kaydı değiştirmektir. Sınıf BEYAN EDİLİR (`scripts/lib/snapshot-kolonlari.ts`, 82'nin olay beyanından ayrı: o defter olayının tersini, bu durum kolonunun sınıfını söyler) ve beyan ŞEMADAN TÜRER: şerhinin ilk cümlesinde snapshot imzası taşıyan her skaler alan beyansızsa kırmızı, beyandaki kolon şemadan düşmüşse kırmızı; kolonu yazan/tüketen dosya kümesi AST ile ölçülür ve beyanla iki yönlü eşittir — beyansız yeni yazıcı (bir geri alma yolu) kırmızı. · bekçi: `test_snapshot_kolonlari.ts` (şema ⊆ beyan ⊆ şema · sınıf tutarlılığı · yazıcı kümesi · üç sonda her koşumda) · `test_consistency_derived.ts` §27 (a: tüketilince null / eşik sonrası tüketilmeden null değil) · §28 (b: canonical çiftiyle ayrışmamış, silinmemiş, kalemden kopmamış; `initialQty` kendi ayağı `test_consistency §33`) <sub>(arşiv:2026-09-13 initialQty şişmesi; 2026-09-14 snapshot beyanı; `schema.prisma` preShipStatus şerhi)</sub>

- **[ÇEKİRDEK]** Raftan (stok kümesinden) üretime giren HER yol stok defterine `PRODUCTION_ISSUE` çıkışı yazar — üretime alma · manuel taşıma ("Konumu Düzelt") · elle top (Faz 2 bağlama) · redye ayırma (iki dal); yazıcı TEK (`helpers/production-issue-ledger.helper.ts`), görüntü claim ÖNCESİ (statü/depo/metraj), zaten üretimdeki top satır almaz. Yazılmadığı sürede top üretimde, defter "rafta" sayıyordu (ölçüldü 2026-09-13: üç yol satırsız; K=0 kapısı bu yolları göremiyordu — kendini ilan etmeyen yol elle listede yoktu). · bekçi: `test_stock_ledger_manual_move` (§1–§4 dört yol, §H mutabakat; negatif sonda: yazıcı susturulunca 12 kırmızı) <sub>(arşiv:2026-09-13; hüküm: docs/design/TAMBUR-GERI-ALMA-HUKUM-2026-09-13.md §5)</sub>
- **[ÇEKİRDEK]** Para okuyucusu DURUM kolonundan türetilmez: kabul-anı metrajı (alış faturası taslağı satırı · alış siparişi karşılaması) `Roll.initialQty`den değil depo defterinin `RECEIPT_QTY_REASONS` listesindeki satırlarından (bugün yalnız ENTRY_RECEIPT; giriş düzeltmesi satırı yazıcısı + ters-yol beyanıyla TEK commit'te girer) işaretli toplanarak okunur, tek helper `helpers/receipt-qty.helper.ts`, üç sonuç (defter · ufuk-öncesi `initialQty` yedeği + `warnings` cümlesi · ufuk-sonrası satırsız top 409 `RECEIPT_LEDGER_ROW_MISSING`); ufuk tek kaynak `constants/ledger-horizon.ts`; aşım bump'ı satır yazmaz, MANUAL_ADJUST toplanmaz. · bekçi: `test_receipt_qty_readers.ts` (altı durum × iki okuyucu) + `test_receipt_qty_single_source.ts` (çıplak `initialQty` yasak) <sub>(arşiv:2026-09-13; hüküm `TAMBUR-GERI-ALMA-HUKUM-2026-09-13.md` §10.6/§11)</sub>
- **[ÇEKİRDEK]** Üretimden rafa (stok kümesine) giren HER yol stok defterine `PRODUCTION` GİRİŞİ yazar — son adım finalize (`PRODUCTION_RECEIPT`) · Tambur finalize çocuğu (`TAMBUR_FINALIZE`) · açık kumaş per-cut çocuğu ve finalize KALANI (`TAMBUR_CUT` — `cutOpenFabric` · `finalizeOpenFabric`) · WO kapanış dispozisyonu (`DISPOSITION`) · takılı top kurtarma (`RESCUE`); "kesim çocuğuna satır yazılmaz" kuralı DEPO kesimine aittir (ebeveyn de stokta, toplam değişmez) — ebeveyn IN_PRODUCTION iken çocuğun doğuşu stok kümesine ilk giriştir. Giriş ikilisi (`rescueStuckRoll` · `cutOpenFabric`) 2026-09-14'te bağlandı ⇒ gerçek K 2 → 0 (taban yazımı 1e'de); her yazıcı `postStockMove`, 0 metrajda satır yazılmaz (taşınacak mal yok). · bekçi: `test_stock_ledger_production_entry` (§1–§7: TAMBUR_CUT giriş + SINGLE/arşiv dalı TAMBUR_UNDO bağlı ters · RESCUE giriş · 0 metraj atlama; negatif sonda: iki yazıcı susturulunca §1–§3 / §4 kırmızı) + `test_defter_ters_yol §13` (TAMBUR_CUT BAGLI_TERS → TAMBUR_UNDO · RESCUE KARSI_OLAY → PRODUCTION_ISSUE) <sub>(hüküm: docs/design/TAMBUR-GERI-ALMA-HUKUM-2026-09-13.md §11 ③ giriş kalemi; arşiv:2026-09-14)</sub>

### Yasaklar

- **[ÇEKİRDEK]** Defter satırını (`RollOperation` · `RollMovement` · `WarehouseMovement` · `CariTransaction` · `CashTransaction` · `ChequeEvent` · `YarnMovement` · `SwatchStockReduction` · `PaymentAllocation` · `SackAllocation`) `delete`/`deleteMany` ile silmek YASAK — geri alma ters satır yazar. <sub>(arşiv:2026-09-10)</sub>
- **[ÇEKİRDEK]** İleri işlemin damgasını `null`'lamak ters kayıt DEĞİLDİR ve YASAKTIR — `dispatchedAt`/`dispatchedById`, `weighedAt`/`weightKg`, `invoicedAt`/`invoiceNo` gibi damgalar "bu iş yapıldı" beyanıdır; geri alma bunları silmez, üstüne ters olay yazar. <sub>(arşiv:2026-09-10, 2026-09-12'de daraltıldı)</sub>
- **[ÇEKİRDEK]** Yeni bir `delete`/`deleteMany` yolu açmak KARARDIR: aşağıdaki iki meşru sınıftan birine girmiyorsa yazılmaz; girdiği sınıf kod yorumunda adıyla anılır. <sub>(arşiv:2026-09-10)</sub>

### Hard delete — meşru sınıflar

**Bugün İKİ sınıf meşrudur** (③b saf yapılandırma pivotu · ④ deftere hiç yazmamış taslak); aşağıdaki tablo kanonik listedir. Eski dört sınıf 2026-09-10'da ikiye indi — yani bu sayı bir kez değişti ve yine değişebilir, bu yüzden BAŞLIKTA taşınmaz: başlık saydığı şeyle birlikte tazelenmezse alıntıya döner. Diğer her "sil" bir DURUM GEÇİŞİDİR.

| Sınıf | Ne yapar | Neden meşru | Emsal |
|---|---|---|---|
| **④ Deftere hiç yazmamış taslak** | atomik claim'li fiziksel silme | hiçbir bakiye/defter satırı üretmedi — geri alınacak bir "olay" yok | `invoice.service.ts:883` (`deleteMany({id, status: DRAFT})`, `count===0` → taze okuma → 409) |
| **③b Yapılandırma pivotu replace** | üst kayıt yaşar, ayar kümesi yenilenir | satırın parasal/ticari/kalite sonucu yok; cevaplanan soru "bu ayarı kim değiştirdi"dir ve onun yeri KARAR satırıdır, pivot satırı değil | `StationProperty` · `ItemAllowedColor` · `ItemAllowedProperty` · `RouteStepProperty` · `PermissionTemplateItem` · `CustomerTemplateRoute` · `LabelContextDefault` · `PeripheralTemplateRoute` · `SubcontractorToCategory` · `StationColor` · `CustomerItemAlias` · `CustomerColorAlias` |

- **[ÇEKİRDEK]** ③b'de pivot satırı silinebilir ama DEĞİŞİKLİĞİN KENDİSİ bir karar defterine yazılır; yüzlerce ayar satırını versiyonlamak tabloyu şişirir, sorulan soruyu cevaplamaz. <sub>(arşiv:2026-09-10)</sub>
- **[ÇEKİRDEK]** İçe aktarım geri sarması DOSYADAN değil YAZILAN SATIRLARDAN çalışır: her uygulanan satır `ImportRunLine`a yazılır (`CREATE`/`UPDATE`/`REVIVE` + yalnız dokunulan alanlar + REPLACE edilen çocukların fotoğrafı), geri alma ileri koşumu silmez/değiştirmez ve "yalnız o alan hâlâ aynıysa" koşulu ATOMİK CLAIM ile kurulur (`count===0` → satır gerekçesiyle ATLANDI). `REVIVE`ın tersi pasife atma DEĞİL, önceki değer + önceki durumdur; alias pivotlarında yalnız O KOŞUMUN yarattığı satır fiziksel silinir (③b). · bekçi: `test_import_revert.ts` <sub>(arşiv:2026-09-12, tasarım `docs/design/IMPORT-EXPORT-TASARIM.md` §8)</sub>
- **[ÇEKİRDEK]** ③a TİCARİ pivot (`ItemPrice` · `SackAllocation` · `PaymentAllocation` · `WorkOrderToOrderLine` · `RollProperty` · `WorkOrderTargetProperty` · belge satırları) sil-yaz YAPILMAZ: damgayla versiyonlanır (`revokedAt`/`clearedAt` + partial unique — evin deseni; `validFrom`/`validUntil` DEĞİL, bkz. OZELLIK-PIVOT-SURUMLEME-PLAN §1) ya da ters kayıt alır. `RollProperty`/`WorkOrderTargetProperty` 2026-09-11'de bilinçle ③a'ya kondu — topun özelliği rota kapsamasını belirleyen GERÇEK kısıttır, ayar değil — ve 2026-09-14'te sürümlendi (Faz 1–2e, `test_roll_property_revoke`). <sub>(arşiv:2026-09-10)</sub>
- **[ÇEKİRDEK]** Eski ① (bağımlılık-guard'lı silme) ve ② (alias/karar satırı) sınıfları KALKTI: guard kalır, silme gider — `isActive:false` / `VOIDED` statüsü / `revokedAt`+`revokedById`. <sub>(arşiv:2026-09-10)</sub>
- **[ÇEKİRDEK]** Kalıcı silme uçlarının izlenen model listesi ELLE tutulmaz: uçlar route dosyalarından keşfedilir, "bu uç fiziksel siliyor" iddiası servis dosyasında çapalanır (mezar taşı yazan uç izlenmez) ve bir modele gelen her FK ya guard'da sayılır ya gerekçeli muaftır — uç adında "permanent" geçmesi fiziksel silme KANITI değildir. · bekçi: `test_hard_delete_guard_coverage.ts` <sub>(arşiv:2026-09-12 K5)</sub>

### Budanan satırın kuralları — telemetri ekseni

**Bu bölüm bir hard-delete sınıfı AÇMAZ.** Yukarıdaki iki sınıf kanonik kalır. Budanabilirlik bir *silme izni* değil, **satırın yaşam süresidir** — ve yaşam süresi, model sınıfından (`KATALOG` · `DEFTER` · `PİVOT`) **bağımsız bir eksendir**. Üçüncü bir *sınıf* açmak (öneri ⑤ numarasıyla geldi ve REDDEDİLDİ) iki ekseni tek listeye bindirir ve *"append-only bir katalog budanabilir mi"* gibi cevapsız sorular doğurur. Doktrinin tam metni ve altı kural: § **Telemetri ≠ defter** (bu dosyanın sonunda) — TEK hedef, bu bölüm yalnız kural satırlarını taşır.

- **[ÇEKİRDEK]** **Yaşa göre budanan bir tablo, SINIFI BEYAN EDİLMEDEN budanamaz** — ve telemetri bir hard-delete sınıfı değil, defter-OLMAYAN satırın yaşam döngüsüdür (§ *Telemetri ≠ defter*, altı kural). Budama yüklemi YAŞA bağlıdır (tarih eşiği), ada ya da kimliğe değil; budayan yol TEK dosyadadır ve beyanla birebir eşleşir (ölü beyan da kırmızı). ⚠️ Ölçüldü 2026-09-14: bugün budanan iki tablodan `EndpointLatencyDaily` beyan tablosunda HİÇ YOKTU ve `Session` hiçbir evrende sınıflanmamıştı — iki kapı da doğru çalışıyordu, boşluk ARALARINDAYDI. · bekçi: `test_telemetri_defter_degil.ts` (§2 sınıf beyanı · §3 budayan küme birebir · §4 tek dosya · §5 defter budanmaz; altı saf sonda) <sub>(tasarım: `docs/design/DOKUMA-TEZGAH-IZLEME-TASARIMI.md` §4; arşiv:2026-09-14)</sub>
- **[ÇEKİRDEK]** Bir satırın telemetri olup olmadığı YARGIYLA değil YANLIŞLANABİLİR BİR SONDAYLA belirlenir: **ham örnek silindiğinde raporlanan hiçbir sayı değişmiyorsa o satır telemetridir — ama bir KARARI besliyorsa saklama süresi o kararın UFKUYLA sınırlıdır.** *"İş kararına girmez"* bir yargıdır ve ölçüt olarak kullanılmaz. Kuralın "sayı" kelimesi 2026-09-13'te dar bulundu: `TravelerCardScan` hiçbir rapora girmiyor ama istasyon KALICI SİLME guard'ı okutma sayar (`guarded-hard-remove.ts` `scanCount` → 409) — budama o kararı sessizce serbest bırakır ve silinemeyen bir istasyon silinebilir hâle gelir. Budamadan önce ya guard'a kalıcı ikinci tanık verilir (`machineHistoryCount` zaten orada) ya budama o ufku aşmaz. (⚠️ Kök `CLAUDE.md` hâlâ dar hâli taşıyor.) · bekçi: `YOK (sonda yarısı; karar-ufku yarısı test_defter_ters_yol §7'de ölçülüyor — TELEMETRİ beyanı kararUfku taşımazsa kırmızı)` · Kapanır: `budanan bir tabloda "ham satır sil → raporlanan hiçbir sayı değişmez" sondası BEKÇİ olarak yeşil verir (tasarım §4 sed ①, dört ayak). Ölçüldü 2026-09-13: bugün budanan tek tablo EndpointLatencyDaily ve test_latency_persist yalnız retention'ın KOŞTUĞUNU ölçüyor, güvenliğini değil; tezgah telemetrisi tabloları şemada yok` · Çapa: `test_machine_prune_safety` · Öncül: ölçüldü <sub>(arşiv:2026-09-12 devere · tasarım: `docs/design/DOKUMA-TEZGAH-IZLEME-TASARIMI.md` §4 "Saklama, budama ve iki sınıfın tek tablodaki bedeli")</sub>
- **[ÇEKİRDEK]** İş kararına giren her sayı **budamadan ÖNCE kalıcı kolona DONAR**; budayıcı açık koşumun penceresine, açık vardiyaya ve MÜHÜRSÜZ pencereye DOKUNMAZ. Geçmiş bir özet yeniden hesaplandığında değişiyorsa, budama değil o özetin kalıcılığı kusurludur. · bekçi: `YOK` · Kapanır: `budayıcı indiğinde test_machine_prune_safety dört ayağı yeşil (açık koşum · açık vardiya · mühürsüz pencere DOKUNULMAZ · donmuş terim değişmez) ve yüklemden mühür koşulu düşürülünce kırmızı; donma yarısı 2026-09-13'te closeMachineRunTx ile indi (test_machine_run §9a picks/producedM/observedSec donuyor), budayıcı ve tezgah kovası şemada yok — bugün budanan tek tablo EndpointLatencyDaily` · Çapa: `test_machine_prune_safety` · Öncül: ölçüldü <sub>(arşiv:2026-09-12 devere)</sub>
- **[ÇEKİRDEK]** Aynı ölçüt **audit'i de kapsar**: kalıcı sayaç/rapor `SystemLog`tan değil kalıcı kolondan okunduğu için audit satırı silindiğinde raporlanan hiçbir sayı değişmez ⇒ audit telemetri yaşam süresindedir ve **6 aylık arşivlenmesi bir istisna değil bu kuralın örneğidir.** `DEFTER` "iş defteri / kanıt defteri" diye BÖLÜNMEZ. <sub>(arşiv:2026-09-12 devere)</sub>

⚠️ **Budama izni bekçi yeşil olmadan verilmez** (tasarım §4 kararı): retention işi, yukarıdaki iki sondayı ölçen bekçi yazılıp yeşil vermeden sürüme çıkmaz. Bekçi bugün **YOK** — bu satırlar kuralı sabitler, uygulamayı değil.

### Tuzaklar

- **[ÇEKİRDEK]** Silmeyi bırakınca UNIQUE kısıtları kırılır: geçersiz satır yaşayanla çakışır. Kısıt `WHERE "revokedAt" IS NULL` partial'ına çevrilir ve `scripts/test_db_invariants.ts` envanterine YAZILIR (iki yönlü, [DB-30]). Emsal: `item_price_default_uq`. <sub>(arşiv:2026-09-10)</sub>
- **[ÇEKİRDEK]** Ters kayıt yazan yol, ileri kaydı iptal eden BELGEYİ de kilitler (`FOR SHARE`, kilitten SONRA taze okuma): kilitsiz okuma READ COMMITTED'da ölü belgenin kaydını diriltir ve karşı yol bayat listesiyle onu hiç görmez. İptal yolu da kapsamını tx İÇİNDE, claim'in arkasında çözer. · bekçi: `test_swatch_stock_reduction_reversal.ts` §10 <sub>(arşiv:2026-09-12)</sub>
- **[ÇEKİRDEK]** Yeni "geçersiz" kolonu açan her model, o kolonu süzen okuma yolunu TEK helper'a bağlar; elle kopyalanan `WHERE` bir gün unutulur ve geçersiz satır listeye sızar (ayrışan yüzey sınıfı). <sub>(arşiv:2026-09-10)</sub>
- **[ÇEKİRDEK]** Süzgeç yalnız delegate çağrısında aranmaz: İLİŞKİ okumaları (`include`/`select`/`_count`, `some`/`none`, tipsiz include sabitleri) ve ham SQL'deki HER tablo başvurusu (alias'ıyla) aynı yüklemi taşır; `every` yazılmaz (`none: { ...ACTIVE, NOT: X }`). Ölçüm tip denetleyicili tarama ile yapılır (`scripts/revoke-ast-tarama.ts`). · bekçi: `test_roll_movement_revoke.ts` §6 · `test_roll_operation_revoke.ts` §7 <sub>(arşiv:2026-09-11 B-4b)</sub>
- **[ÇEKİRDEK]** Partial unique'li damgalı defterde tekil anahtarla `upsert` yazan her çağrı `where`e aktif yüklemi koyar (`{ <üçlü>, ...ACTIVE_OPERATION }`); koymazsa anahtar geri alınmış satırı bulur, `update: {}` hiçbir şey yazmaz ve iş TEKRARLANAMAZ — birden çok geri alınmış satırda Prisma hata atar. · bekçi: `test_roll_operation_revoke.ts` §7a/§8 <sub>(arşiv:2026-09-11 B-4b)</sub>
- **[ÇEKİRDEK]** Şema-dışı partial unique'in predicate'i değişirken sed kesintisiz takas edilir: yeni index geçici adla kurulur → eskisi `DROP INDEX` → `ALTER INDEX … RENAME`; `test_db_invariants.ts` envanterindeki predicate aynı commit'te güncellenir. <sub>(arşiv:2026-09-11 B-4b, migration 20260911190000)</sub>
- **[ÇEKİRDEK]** Bir defter "var" diye yeterli değildir: ileri yolu yazıp geri yolu yazmayan defter, hiç olmayandan daha tehlikelidir — toplamı sessizce kayar. Ters yol yazılmadan ileri yol sürüme çıkmaz. <sub>(arşiv:2026-09-10)</sub>
- **[ÇEKİRDEK]** Ters kayıt KAPSAM ister ve kapsam tip düzeyinde zorunludur: "topun tüm izini tersle" (yalnız top tümden öldürülüyorsa) ile "şu işlemi geri al" AYRI fonksiyonlardır. Kapsam sorgusu iki adımlıdır (önce damgalı satır, bulunamazsa damgasız); `OR workOrderStepId IS NULL` tek yüklemde YASAK — başka bir iş emrinin girişini aday yapar ve geçmişi değiştirir. · bekçi: `test_stock_ledger_kursun_reopen.ts` §4/§8/§9 <sub>(arşiv:2026-09-12)</sub>
- **[ÇEKİRDEK]** Defter kapısında yazılamaz satırın davranışı ÇAĞRI YERİNDE beyan edilir (`onUnwritable: "skip" | "throw"`), varsayılanı yoktur ve politika satırın yazılamamasının İKİ sebebini de (0 metraj · depo ucu yok) kapsar: aynı durum bazı yollarda meşru atlama, bazılarında tutarsızlık sinyalidir. Meşru atlama bile SAYILIR ve bir yüzeye basılır. İki sebebi kapsayan politikaya yalnız birinin adını vermek (`onZeroQty`) okuyanı öteki dalın politikasız olduğuna inandırır. · bekçi: `test_stock_ledger_helper.ts` §10a–§10d · `test_warehouse_ledger.ts` §E1 <sub>(arşiv:2026-09-13)</sub>
- **[ÇEKİRDEK]** Stok hareketinin bir ucu İKİ biçim alır ve kuralı tek bir iff'tir: `warehouseId === null ⇔ status ∉ WAREHOUSE_STOCK_STATUSES`. Stok dışı uç (SHIPPED · AT_KARTELA · CANCELLED) **depo taşımaz ama statü taşır** — "WAREHOUSE'tan SHIPPED'e gitti" cümlesi yazılabilir olmak zorunda. En az bir uç stok kümesinde olmalı: stok dışından stok dışına satır YAZILMAZ (DB ikizi `warehouse_movements_direction_present`). · bekçi: `test_stock_ledger_helper.ts` §12 <sub>(arşiv:2026-09-13)</sub>
- **[ÇEKİRDEK]** Stok kümesinden çıkan yolda (sevk · transfer · kartela · iptal) ve iade hedefinde deposuz top **409 + barkod listesi** ile durdurulur (`assertRollsHaveWarehouse`, tek kaynak); defter kapısındaki uç denetimi (`assertEndShape`) bu kapının SON AĞIdır ve 500 verir — oraya ulaşmak "kapı atlandı" demektir, mesajında barkod OLAMAZ çünkü topu tanımaz. ⚠️ Deposuz topu stok kümesine ÇEKEN statü terfisi yolları bu kapıyı garanti etmez; "deposuz top stok kümesinde olamaz" bugün kodun değil POPÜLASYONUN (0) sonucudur ve `backfill_roll_warehouse` deploy zincirinde DEĞİLDİR. · bekçi: `test_stock_ledger_warehouseless.ts` <sub>(arşiv:2026-09-13)</sub>
- **[ÇEKİRDEK]** Statüsüz (eski kapıdan yazılmış) ileri satırın tersi BAĞLI yazılır: bağ ileri satıra, uçlar CANLI veriden, metraj ileri satırdan (`reverseLegacyStockMove`). Dal yalnız satır statüsüz **ve** (`preEpoch` damgalı **ya da** epoch henüz çizilmemiş) ise açılır; epoch çizildikten sonra statüsüz satır bir HATA SİNYALİDİR ve dal onu susturmaz. "Epoch çizildi mi" tek olgudan okunur: `OPENING_BALANCE` satırının varlığı. · bekçi: `test_stock_ledger_helper.ts` §13 <sub>(arşiv:2026-09-13)</sub>
- **[ÇEKİRDEK]** Defter bekçisinin fikstürü GERÇEK yazma yolundan geçmek zorundadır: kendi kurduğu dünyayı doğrulayan bekçi yeşil kalırken hatayı göremez (ölçüldü: aynı bekçi kör fikstürle 7/0, gerçek yolda 3/6). · bekçi: `test_stock_ledger_kursun_reopen.ts` <sub>(arşiv:2026-09-12)</sub>
- **[ÇEKİRDEK]** Kesim/bölünme malı TAŞIMAZ, BÖLER: defter TEK satır değil ÇİFT yazar (ebeveyn çıkışı + çocuk girişi) ve ikisi aynı `transformGroupId` altında toplanır — grup neti SIFIR olduğu için toplam dönüşümden etkilenmez. Çiftin bir ucu yazılamıyorsa (deposuz defter-öncesi top) HİÇBİRİ yazılmaz: tek başına çocuk girişi, karşılığı olmayan bir ARTI'dır. · bekçi: `test_stock_ledger_transform.ts` §A/§C/§G <sub>(arşiv:2026-09-12)</sub>
- **[ÇEKİRDEK]** Stok defterinin sebep kodu SAPMA defterinin kararından DEĞİL, malın YERİNDEN türetilir: bitmiş topta `scrap` kararı fire KPI'sına yazılır ama çocuk WAREHOUSE statüsünde doğduğu için mal raftadır ve stok ÇIKIŞI yoktur. Aynı ayrım aşımda da geçerlidir — aşım bir DEVİR değil KEŞİFtir ve dönüşüm grubuna GİRMEZ, girerse "grup neti sıfır" sessizce kırılır. · bekçi: `test_stock_ledger_transform.ts` §B/§E/§F <sub>(arşiv:2026-09-12)</sub>

- **[ÇEKİRDEK]** Deftere `RESTRICT` ile bağlı bir satır eklemek, o ebeveyni SİLEN HER YERİ ödev yapar — üretim kodu, script ve **bekçi fikstürlerinin temizliği** dâhil: defter satırı ebeveyninden ÖNCE silinir. Temizlik süzgeci kendi koşumunun id'lerine değil FİKSTÜR DAMGASINA bağlanır, yoksa önceki başarısız temizliğin bıraktığı kalıntı bir sonraki koşumu düşürür. ⚠️ Temizlik hatası `.catch(() => {})` ile YUTULMAZ: yutulan hata "temizlik başarılı" ile aynı çıktıya iner, kalıntı büyür ve KOMŞU bekçileri kirletir (ölçüldü 2026-09-12: `ImportRunLine` indi, `test_import_framework` düştü, `test_import_idempotency` aynı hatayı sessizce yuttu ve artık satırlar yönetici oturumun "önceden vardı" ölçümünü bozdu). · bekçi: `test_import_revert.ts` (doğru sıra emsali) <sub>(arşiv:2026-09-12)</sub>

- **[ÇEKİRDEK]** Karar defteri de defterdir: bir satır raporlanan bir sayıya ya da bir KARARA giriyorsa ters yolu olmak zorundadır. `RollPlanDeviation` bugün ikisine de giriyor (karne `plan-deviation-scorecard` ham SQL ile sayıyor; Tambur kapısı `fason-receipt` satırını görürse soruyu sormuyor) ama damga kolonu YOK, `tambur-undo` ona dokunmuyor ve kabul iptali + diriltme zinciri geri alınmış bir onayı CANLI bırakıyor (`cancelReceipt` hareketi geri alır, restore guard'ı `ACTIVE_MOVEMENT` saydığı için görmez, `currentStepId` null'a çekilir, `cancelReasonCode` hiç yazılmaz ⇒ doğan top diriltilebilir). Onarımın iki ayağı AYRILAMAZ: okuyan yollara `revokedAt IS NULL` süzgeci **ve** kabul iptalinin `fason-receipt` satırlarını damgalaması — süzgeç tek başına boş küme üzerinde çalışır. **Çözüm TASARLANDI, uygulanmadı** (`docs/design/PLAN-SAPMA-GERI-ALMA-TASARIM.md`): karar DAMGA (karşı kayıt reddedildi — karne `COUNT(DISTINCT confirmationId)` sayıyor), tanecik satır değil **`confirmationId`**, damgalayan dal DÖRT (FULL · SINGLE · SINGLE_RESTORE · `cancelReceipt` cascade; `finalize` satırı SINGLE'da damgalanmaz), süzen okuyucu üç + kapı. · bekçi: `test_defter_ters_yol.ts` §6 (borç + tasarım durumu görünür, §6c ölü tasarım atfını düşürür) <sub>(arşiv:2026-09-13)</sub>
- **[ÇEKİRDEK]** Mali etkisi olan bir defterin geçmişi AUDIT'e yazılamaz — yeniden hesaplanan tahsis satırı SİLİNMEZ, `clearedAt`/`clearedShipmentId`/`clearedById` ile damgalanır (`SackAllocation`, K2 2026-09-14; `SackTagAssignment` deseni); üç yeniden hesap yolu TEK boğazdan geçer (`clearShipmentAllocationsTx`), etkin satırın tekilliği PARTIAL unique'tedir (`sack_allocations_active_uq`) ve Σ okuyan HER yüzey `ACTIVE_SACK_ALLOCATION` süzer — süzmeyen yüzey karşılamayı damgalı satırla şişirir. Geri alma ucu yok: yeni tahsis yeni satırdır, damgalı satır tarihtir. · bekçi: `test_sack_allocation_cleared.ts` (§1 damga · §2 partial unique · §3 iptal · §4 AST+tip: her okuma `ACTIVE_SACK_ALLOCATION`, silme 0 · §5 SQL ikizi) · `test_defter_ters_yol.ts` §3/§4/§10 <sub>(arşiv:2026-09-13; kapandı 2026-09-14)</sub>

### Reçeteler

- **[ÇEKİRDEK]** Yeni defter açarken sekiz soru: ① hangi olaylar (enum) · ② her ilerinin tersi var mı · ③ append-only mi (`updatedAt` YOK) · ④ aktör kolonu (`createdById`) · ⑤ kaynak belge FK'ları · ⑥ miktar `Decimal` + ölçek kataloğu ([DB-33]) · ⑦ index: `(varlıkId, createdAt)` + olay+tarih · ⑧ mutabakat bekçisi (Σ hareket ↔ canlı durum). <sub>(arşiv:2026-09-10)</sub>
- **[ÇEKİRDEK]** Var olan deftere yeni ileri olay eklerken enum'a ters değeri AYNI commit'te ekle ve geri alma yolunu yaz; "sonra ekleriz" ters yolu olmayan olay üretir. <sub>(arşiv:2026-09-10)</sub>

## Alınmış kararlar — henüz UYGULANMADI (2026-09-11)

Kullanıcı kararı; uygulaması ayrı iştir. Bu bölüm iş bitince silinir, kural satırına dönüşür.

- **`WarehouseMovement` GERÇEK STOK DEFTERİNE dönüşecek.** `warehouseId` atayan HER yol deftere bağlanır — statü terfisi (`STOCK → WAREHOUSE`) dahil; bugün terfi bilinçli olarak satır yazmıyor ve defter yalnız dışarıdan gelen malı görüyor. Hedef: Σhareket ↔ canlı stok mutabakatı ve as-of kesit. Ön koşulun İKİ YARISI DA KAPANDI: elle "iptali geri al" 2026-09-12'den beri ters kayıt yazıyor (`restoreCancelledRoll` → `reverseLatestScopedStockMove`, bekçi `test_stock_ledger_cancel_restore`) ve iade ters yolu 2026-09-13'te indi (`cancelReturn` → `RETURN_CANCEL`, bağlı). ⚠️ Bu maddenin KENDİSİNİN kapandığı ayrı bir ölçümdür (terfi yollarının deftere bağlanması) ve bu satırı yazan oturumun işi değildir — aşağıdaki 2026-09-11 şerhi de o ölçümle birlikte tazelenir.

## Mevcut defter envanteri (2026-09-13 ölçümü — kapısı `test_defter_ters_yol`)

> ⚠️ **`WarehouseMovement` bir STOK DEFTERİDİR (2026-09-13) ama GEÇMİŞİ EKSİKTİR — ve EKSİK KALACAK (kullanıcı kararı 2026-09-13: *"geçmiş verileri onarmak veya backfill yapmak mümkün değil; bundan sonraki kayıtlar sağlam olsun yeter"*). Açılış fotoğrafı da bu kararın içindedir (işlevi backfill) ⇒ `docs/ops/STOK-DEFTERI-ACILIS-RUNBOOK.md` ARŞİVDİR. Bu cümle artık bir BORÇ değil bir SINIR bildirir: eksik geçmiş onarılmayacak, yalnız bundan sonrası deftere bağlanacak (ilerleme ölçüsü `K`).** Yazar kümesi kapandı — 10 terfi yolu depo damgalıyor, stok kümesinden çıkan yollar deposuz topu 409 ile durduruyor, ters yollar `reversesMovementId` ile bağlı. **Kapalı KALMASINI ölçen kapı: `test_stok_defteri_bag_olcumu` (K = 0 ∧ V).** ⚠️ *Kapısız bir "tamdır", tarihsiz bir sayıdan farksızdır* — on birinci bir terfi yolu yazılırsa cümleyi o kapı yalanlar, okuyanın güvenmesi gerekmez.
>
> **Eksik olan GEÇMİŞTİR ve bu bir VERİ olgusudur; yazar düzelterek kapanmaz.** Ölçüm — taban: ağaç `810331bf`, DB fabrikanın dev kopyası (fabrikanın canlı yedeği, salt okuma), 2026-09-13:
>
> · Defter başlangıcından sonra doğan **1.259** topun **516**'sı `WAREHOUSE` statüsünde olup defterde **hiç satırı yok**. ⚠️ **EPOCH TANIMI LOAD-BEARING:** "defter başlangıcı" = İLK defter satırının **ANI** (`2026-09-04T17:48:24.741Z`), gün başı DEĞİL — gece yarısı sınırıyla ölçülürse aynı sorgu **1.507 / 538** verir. Bir sayı, sınırı yazılmadan taşınamaz. · Günlük seri (epoch anı tanımıyla): **68 · 117 · 31 · 127 · 173** (09-04 · 09-06 · 09-07 · 09-08 · 09-09); **09-05 yok, 09-10 ve 09-11 sıfır** ⇒ *"günde ~100"* bir ortalama DEĞİL bir **aralıktır**. · ⚠️ **"Yazar kümesinin kapanması boşluğu durdurdu" bir ÇIKARIMDIR, bu yedekte GÖZLEM DEĞİL** (düzeltme 2026-09-13): yedeğin ufku `2026-09-11T12:20`, yazar kümesini kapatan commit'ler ise 09-12/09-13'te indi ⇒ bu DB etkinin görülebileceği ufkun GERİSİNDE. Doğru cümle: *yazar kümesi kapatıldı, etkisi bu yedekte ölçülemez, canlıda yeniden ölçülecek.* Ölçüm ve karar sırası: `docs/ops/STOK-DEFTERI-ACILIS-RUNBOOK.md` §3a.
> · **368** `CANCELLED` topun karşı `CANCEL` satırı yok.
> · Canlı stok ↔ defter neti mutabakatı **epoch'tan ÖNCE TANIMSIZDIR** (778 satırın 721'i statüsüz ⇒ stok-kümesi uçlu Σ onları tanım gereği dışarıda bırakır; karşılaştırmanın bir tarafı yok). Hesaplanabilir bir fark üretilebilir ama **anlamlı değildir** — Σ ve as-of ancak açılış fotoğrafından sonra okunur.
>
> ⚠️ **Geçmişin onarımı KULLANICI KARARIDIR** (geçmiş veriye dokunur); ölçüldü ve bekliyor.

| Defter | Kapsam | Append-only | Ters yol |
|---|---|---|---|
| `WarehouseMovement` | depo giriş/çıkış, **13 olay** (`EXTERNAL` 2026-09-13'ten beri YAZILIYOR — kartela ×3 + fason sevki ×1; `OPENING_BALANCE` yalnız açılış fotoğrafı script'inden) | ✅ ölçüldü (`update`/`delete` yolu yok) | ✅ `reversesMovementId` + dört ters helper (`reverseStockMove` · `reverseLegacyStockMove` · `reverseAllRollStockMoves` · `reverseLatestScopedStockMove`) · **`RETURN` ✅ kapandı** (`RETURN_CANCEL`, bağlı, 2026-09-13) · TRANSFER ✅ bağsız · SHIPMENT ✅ bağsız · `EXTERNAL`/kartela ✅ bağlı (`KARTELA_CANCEL`) · CANCEL ⚠️ yalnız sayım stornosu bağlı (elle "iptali geri al" `ROLL_CANCEL` kapsamıyla yazar, sayımın satırını bulamaz) · `EXTERNAL`/fason sevki ✅ bağlı (`FASON_DISPATCH_CANCEL`, `510081bf`; eski borç "39 ileri / 0 ters" KAPANDI) · **fason kabulü ✅ bağlı** (`FASON_RECEIPT_CANCEL`, `b27ac459`: `cancelReceipt` doğan toplar için `reverseLatestScopedStockMove` çağırır, bekçi `test_stock_ledger_fason`) · olay düzeyinde açık borçlar `test_defter_ters_yol` §13'te görünür: **`CUT_SPLIT`** (ebeveyn çıkışı geri almada terslenmiyor — düzeltme `c2a10e88` tren #4'te), **`OVERAGE`** (stok satırı çocukta terslenir, sapma satırı ebeveynde terslenmez — hüküm P3 sonrası), **`CUT_DISCARD`** (ters yazıcı yok; terminal mi hüküm), **`DISPOSITION`** (kısmi) |
| `CariTransaction` | cari borç/alacak, 11 kaynak | ✅ | ✅ `reversesTxnId` |
| `ChequeEvent` | çek durum defteri, 14 olay | ✅ | ✅ `*_CANCEL` (2026-09-11) — CANCEL'ın tersi yok (kendisi storno) |
| `CashTransaction` | kasa/banka | yarı (`status: CANCELLED`) | ✅ |
| `YarnMovement` | iplik stoğu | ✅ | ✅ ADJUST_IN/OUT |
| `RollMovement` | topun adım içi giriş/çıkışı | yarı — `updatedAt` (açık satır çıkışta kapanır, Faz 2 açık) · `revokedAt` damgası | ✅ damga + PARTIAL unique (`exitedAt IS NULL AND revokedAt IS NULL`) |
| `RollOperation` | kurşun/QC2/tambur/fason kanıtı | ✅ `revokedAt` damgası | ✅ damga + PARTIAL unique |
| `RollVariance` | fire · düzeltme · aşım | ✅ | ✅ `reversedAt` |
| `SwatchStockReduction` + `SwatchStockReductionItem` | kartela düşümü + kalemleri | ✅ `reversedAt` damgası | ✅ damga (negatif satır CHECK yüzünden yasak); kalemi ölü kabule bağlı kartela dönmez (2026-09-11) |
| `PaymentAllocation` | fatura kapama | ✅ `revokedAt` damgası | ✅ damga (negatif satır CHECK yüzünden yasak) |
| `RollProperty` | topun özelliği + SEÇİM değeri (③a ticari pivot → sürümlü, 2026-09-14) | ✅ | ✅ `revokedAt`/`revokedById`/`revokeReason` damgası, partial unique (`roll_properties_active_pair_uq`); değer değişimi eski satırın damgası + yeni satır, un-revoke yok; okurlar `ACTIVE_ROLL_PROPERTY` (AST kapısı `test_roll_property_revoke` §13) |
| `WorkOrderTargetProperty` | iş emrinin hedef özelliği (③a → sürümlü, 2026-09-14) | ✅ | ✅ `revokedAt` damgası (`WO_REPLACE` · `WO_TARGET_UPDATE`), partial unique; replace/updateTargetProperties fark bazlı, kilitli hedef listeye daima birleşir (plan §3.2) |
| `WorkOrderToOrderLine` | iş emri ↔ sipariş kalemi bağı + `allocatedQty`; `WorkOrder.type` AÇIK bağ sayısının aynası | yarı (`updatedAt`; `allocatedQty` replace'te yerinde) | ✅ `unlinkedAt`/`unlinkedById`/`unlinkReason` damgası (2026-09-14), vekil `id` + partial unique (`work_order_to_order_lines_active_pair_uq`); yeniden bağlama YENİ satır, un-unlink yok; okurlar `ACTIVE_ORDER_LINK` (AST kapısı `test_order_link_unlink` §13; arama yolu `withActiveOrderLinks`) |
| `SackAllocation` | sipariş karşılama; damga `clearedAt`/`clearedById`, adres `clearedShipmentId` | ✅ (2026-09-14: sil-yaz bitti, tek boğaz `clearShipmentAllocationsTx`; etkin tekillik `sack_allocations_active_uq` partial) | ✅ damga — geri alma ucu yok, yeni tahsis yeni satır (SackTagAssignment deseni) |
| `PrintedDocument` | belge versiyonu | yarı — `updatedAt` VAR (2026-09-13'te düzeltildi; eskiden ✅ yazıyordu) | ✅ `supersededAt`/`voidedAt` |
| `ShipmentEvent` **(yeni)** | sevkiyat durum defteri, 6 olay | ✅ | ✅ DISPATCHED↔UNDISPATCHED · INVOICED↔INVOICE_CLEARED |
| `SackWeighing` **(yeni)** | çuval tartı ölçümü | ✅ | ✅ CLEARED olayı |
| `RollPlanDeviation` | plan-dışı kimlikle inen metrajın KARAR defteri | ✅ | ❌ **BORÇ — damga kolonu bile yok** (aşağıdaki kural satırına bak) |
| `TravelerCardScan` | okutma | ✅ | — TELEMETRİ (sonda koşuldu: hiçbir yüzey okutma SAYISI basmıyor) · budama ufku guard'a bağlı |
| `ImportRunLine` | içe aktarım geri sarmasının tek kaynağı (ileri satır **iç içe** `lines.createMany` ile yazılır) | ✅ | ✅ `revertedAt` + `revertSkipReason` |
| `MergeOperation` (+`Source`/`Ref`) | master-data birleştirme karar defteri | ✅ | ✅ `revertedAt` (LIFO, fotoğraftan) |
| `ShipmentOrder` | sevkiyat ↔ sipariş bağı; `isActive` = `shipment.status = PLANNED` **denormu** | ✅ | ✅ borç KAPANDI (2026-09-13, öncülü yanlıştı) — `undoDispatch` `isActive`i `true`ya çevirir, yani o bir silme damgası değil; `deleteMany` yalnız `setShipmentOrdersTx` kapsam replace'i. AÇIK SORU (borç değil): sevk SONRASI kapsam değişimi bir deftere yazılıyor mu |
| `MachineRun` | tezgah koşumu; kapanışta DONAN üretim/duruş terimleri (`picksAtClose` · `producedM` · `closedTermsAt`) — kova telemetri, koşum onun donmuş defteri | yarı (`updatedAt`: açık koşum durum, kapalı koşum kayıt) | ✅ `revokedAt` damgası, atomik claim (`WHERE revokedAt IS NULL`, `b27ac459`); yazan tek dosya `machine-run.service.ts` (`openMachineRun` · `closeMachineRunTx` · `revokeMachineRun`); bekçi `test_machine_run` §2/§9/§10 |
| `MachineStopEvent` | duruş defteri; olguları değişmez, KARARI (`reasonCode`) değişir ve değişim `MachineStopReclass`a düşer | yarı (`updatedAt`) | ✅ `revokedAt` damgası (şema `d4cc3ea0`); yazma yüzeyi HENÜZ YOK (P2b-1 şema-only) — ⚠️ iki yaşam süresi tek tabloda (tasarım §4): insan kararlı BUDANMAZ, makine sınıflı kovayla budanır; budayıcı indiği gün kapı §10 `silen` beyanını ister |
| `WarpBeamEvent` | levent olay defteri (devere 1b + fason F1) — WOUND doğuş gerçekleri (tel · denye · metre · nominal kg · kgSource) değişmez; fason dörtlüsü (SHIP_OUT · RETURNED_IN + tersleri) sevk kalemine bağlı; `updatedAt` YOK | ✅ (`updatedAt` yok) | ✅ tipli `WOUND_CANCEL` + `reversesEventId @unique` (tek ters), durum CANCELLED terminal; iplik çifti `WARP_ISSUE↔_REVERSAL` · `WARP_RETURN↔_REVERSAL` (`YarnMovement`, brüt/ayrı iade); yazan tek dosya `warp-beam-wind.service.ts` (`windWarpBeam` · `cancelWound`); bekçi `test_warp_beam_lifecycle` |
| `DoffEvent` | doff (top alma) olayı — topu DOĞURAN kayıt (`Roll.doffEventId`, `Restrict`: doff silinemez, top ona bağlı); `counterAtDoff` sayaç fotoğrafı; `clientToken` idempotent | ✅ (`updatedAt` yok) | ✅ `revokedAt` + `revokedById` damgası (şema `44b34d23`, P3); yazma yüzeyi HENÜZ YOK (`yazan: []` ölçüldü) — beyansız ilk yazıcı `test_defter_ters_yol` §5'te kırmızı (sonda ile doğrulandı 2026-09-13) |
| `MachineStopReclass` | sebep DEĞİŞİM defteri (`from→to`); "ne oldu değişmez"in doğru yeri: karar revize edilir, revizyonun kendisi değişmez | ✅ | ✅ **KARSI_KAYIT** (2026-09-14, borç KAPANDI) — ters yol aynı deftere `to→from` ikinci satır; yazanı ileri yolun KENDİSİ (`reclassifyStop`, `machine-stop.service`), bu yüzden mühür kapısını (`assertStopShiftWritableTx`) taşımaması yapısal olarak imkânsız. Kapı: §3k1 yön kolonları şemada · §3k2 ters yazan İLERİ yazan dosyada. Kapının SEALED ayağı 01 Faz 1a ile iner (bugün yalnız 409 `SHIFT_CANCELLED`) |
| `WorkOrderEvent` **(yeni, 2026-09-25)** | iş emrinin KENDİ durum/plan değişimi — bir eylem = bir `groupId`, alan başına from→to satırı; kendi defteri olan olaylar kopyalanmaz | ✅ (`updatedAt` yok; `defter_block_tamper` trigger'ı UPDATE ve doğrudan DELETE'i reddeder) | ✅ KARŞI KAYIT — yeniden açılma tamamlanma satırının takaslısıdır; yazan tek dosya `helpers/workorder-event.helper.ts`, statü yalnız `claimWorkOrderStatusTx`ten (`test_workorder_event_yazar`) |
| `WorkOrderCloseSnapshot` (+`Line`) **(yeni, 2026-09-25)** | iş emri kapanış künyesi — COMPLETED geçişiyle aynı tx'te donan üretim çıktısı fotoğrafı + verim; sürümlü (`version`) | ✅ (`defter_block_tamper`) | ✅ SATIR_EBEVEYN → `WorkOrderEvent`: ters yol yeniden açma karşı kaydı; künye değişmez, yeni kapanış yeni sürüm; yazan tek dosya `helpers/workorder-close-snapshot.helper.ts` |
| `SackTagAssignment` | çuval izi (etiket) ataması | ✅ | ✅ `clearedAt`+`clearedById` damgası (2026-09-14; sevk temizliği `clearedShipmentId` adresi yazar, elle kaldırma NULL bırakır — storno yalnız adresliyi diriltir, yeniden bırakma ikisini de) |
| `RollStatusEvent` **(yeni, 2026-09-25)** | topun her durum geçişi (doğuş dahil); operatör aktivitesindeki iptallerin kaynağı — yazarı DB trigger'ı `rolls_write_status_event`, kodda yazan 0 | ✅ mühür `defter_block_tamper` (doğrudan UPDATE/DELETE RED, top silinince FK kaskadı) | ✅ **KARSI_KAYIT** — geri alma aynı trigger'ın from↔to takaslı yeni satırı; ⚠️ aktör v1 yalnız iptal/fire satırında `cancelledById`den: 5 yol (arşiv · tambur geri alma ×3 · fason ×2) yazmıyor → aktörsüz satır, ayrı dilim · `restoreCancelledRoll` kolonları null'lamaya devam eder (defter geçmişi korur; B-RM borcuyla birleşecek) |

## Telemetri ≠ defter

> **Telemetri bir hard-delete SINIFI DEĞİLDİR.** Hard-delete sınıfları (③b saf yapılandırma
> pivotu · ④ deftere hiç yazmamış taslak) *"bir DEFTER satırını hangi koşulda fiziksel
> silebilirsin"* sorusunun cevabıdır; telemetri satırı defter satırı değildir, o listeye
> girmesi kategori hatasıdır. Üçüncü bir sınıf açmak listeyi "silinebilir şeyler" listesine
> çevirir ve bir sonraki tasarımcı kendi tablosunu oraya yazmak için gerekçe arar —
> doktrinin aşınma yolu tam olarak budur. (Yönetici kararı 2026-09-12; ⑤ sınıfı önerisi REDDEDİLDİ.)

**Altı çekirdek kural:**

1. **Telemetri, defter-olmayan satırın YAŞAM DÖNGÜSÜDÜR** — yeniden yazılabilir, tavanlıdır,
   budanır. ⚠️ Bu özellikler SAYILIR, ŞART KOŞULMAZ: telemetri hem yeniden yazılan
   (`EndpointLatencyDaily`, `updatedAt`) hem append-only biriken (`TravelerCardScan` ·
   `SystemLog`) biçimde olur; ikisini birden zorunlu kılan bir kontrol betimlemeyi yanlış bir
   değişmeze çevirir (ölçüldü 2026-09-14). Sınıfın ölçütü **budanabilirliktir**.
2. **Deftere ya da bir İŞ KARARINA giren her sayı, budamadan ÖNCE kalıcı kolona DONAR**
   (karne terimleri · `MachineShiftStopBreakdown` · koşumun kapanış terimleri).
3. **Budama izni manifest bekçisiyle ÖLÇÜLEREK verilir** — bekçi yeşil değilken retention job
   sürüme çıkmaz.
4. **Telemetri okuyan yol TEK DOSYADA yaşar** ve AST tripwire ile sabitlenir.
5. **Aynı tabloda defter + telemetri varsa yüklem TEK helper'dadır** ve DB seddiyle ikizdir.
6. **Retention açık koşuma, açık vardiyaya ve MÜHÜRSÜZ pencereye DOKUNMAZ.**

⚠️ **Bugün ölçülen ile ölçülemeyen AYRILIR** (`test_telemetri_defter_degil`): ①'in sınıf
beyanı · ④'ün tek-dosya kuralı · "tavanlı" olmanın yapısal karşılığı (budama yüklemi YAŞA
bağlı, ada değil) bugün ölçülür; ② donan sayı · ③ manifest · ⑤ helper-sed ikizi · ⑥ açık
pencere Faz 2'de `test_machine_prune_safety` ile gelir. *Kapsamı yazılmayan bir bekçi,
kapsamadığı kuralı da kapsıyor sanılır.*

⚠️ **Beyan tablosunun EVRENİ o tablonun sözleşmesidir.** `DEFTER_BEYANI`nin evreni append-only
modellerdir; yaşa göre budanan ama defter evreninde olmayan tablo (`Session`) oraya yazılamaz —
denendi (2026-09-14) ve bir beyan yüzünden başka bir kapının borcu arttı (test script'lerindeki
oturum temizlikleri §10b2'ye girdi). Böyle tablolar bekçinin kendi `BUDANAN_DEFTER_DISI`
listesinde durur.

## Geçersiz kılınan kurallar

- **ESKİ → YENİ:** "Deftere yazan her ileri olayın tipli ters karşılığı enum'da BULUNUR (`*_REVERSAL` / `*_CANCEL`)" → **ters yol bir MEKANİZMADIR, ad kalıbı değil** (damga · ters bağ · tipli enum çifti · net karşı olay); enum çifti dört biçimden biridir. Ölçüm: `WarehouseEventType`'ın 13 değerinin 9'u ad kalıbına göre "tersi yok" görünür, 6'sının tersi mekanizma olarak VARDIR (`ADJUST` → net ters `ADJUST`, `TRANSFORM` → karşı grup, `RollVariance` → `reversedAt`, `WarehouseMovement` → `reversesMovementId`). Eski cümle kapısızdı ve kapı yazılınca eksenin kendisi yanlış çıktı. Kaynak: arşiv 2026-09-13 "DEFTER TERS YOL KAPISI", kök `CLAUDE.md` `9d2e2bb1`.

- **ESKİ → YENİ:** "Depo ucu olmaması politikaya TABİ DEĞİL — defter öncesi doğan topların deposu NULL'dur (ölçüldü: 4.553 top) ve onların sevki çalışmak zorundadır" → **uçsuzluk politikaya TABİDİR** (`onUnwritable`). Gerekçe iki ayaktan da boşaldı: popülasyon 0'a indi (`backfill_roll_warehouse`) ve yeni deposuz top doğamaz (`resolveTargetWarehouseId` `null` dönmüyor). Sessiz atlamanın ölçülen bedeli: iki top sevk edildi, tek `SHIPMENT` satırı yazıldı. ⚠️ Eski cümledeki **4.553 bir ölçümdü ve süresi doldu**; bir backfill onu boşalttı, cümle yerinde kalıp bir davranışı gerekçelendirmeye devam etti. Kaynak: arşiv 2026-09-13.

- **ESKİ → YENİ:** "Hard delete DÖRT ölçülmüş sınıfla sınırlıdır (① bağımlılık-guard'lı · ② alias/karar satırı · ③ pivot replace · ④ taslak)" → **İKİ sınıf** (④ taslak · ③b yapılandırma pivotu); ① ve ② kalktı, ③ ticari/yapılandırma diye bölündü. Kaynak: kök `CLAUDE.md` (2026-09-10'da düzeltildi) ve `docs/standart/VERITABANI.md` §9.
- **ESKİ → YENİ:** "Yalnız oturum izi olan makine/istasyon kalıcı silinebilir, oturum satırları tx içinde temizlenir (denetim SystemLog'da kalır)" (`guarded-hard-remove.ts` yorumu) → oturum geçmişi silinmez ve kalıcı silmeyi ENGELLER; audit arşivlendiği için gerekçe geçersizdi. Kaynak: arşiv 2026-09-11.
- **ESKİ → YENİ:** "Tamamlanmış sayım (`StockCount.COMPLETED`) TERMİNALDİR; geri alma yolu kendi ters kayıtlarıdır (topta 'iptali geri al', iplikte ADJUST tersi)" (şema şerhi + servis yorumu, 2026-08-15) → fark fişi TEK BELGEDE stornolanır; statü COMPLETED kalır, storno `reversedAt` damgasıdır. Eski gerekçe "üçüncü bir yol açardı" diyordu; ölçüldü ki mevcut iki yol da defter yazmıyordu (elle geri alma hâlâ yazmıyor). Kaynak: arşiv 2026-09-11 sayım stornosu notu.
- **ESKİ → YENİ:** "Audit teknik izdir ve iş kaynağı OLARAK OKUNAMAZ" (kapısız; allowlist adayında **import geçmişi** ve **record-info künye backfill'i** vardı) → **audit yalnız ayak izidir, kapıyla**: import koşum kayıtları (`getRunRecords`) allowlist'ten ÇIKTI ve borç oldu (K-A4, kaynak `ImportRunLine`); künye ayak izi olarak kalır (kolonsuz tabloda kim/ne zaman gösterimi). Kaynak: arşiv 2026-09-25.
- **ESKİ → YENİ:** `VERITABANI.md` §9 "sekiz hard delete sitesinin sekizi de meşru" ölçümü BAYAT çıktı — 2026-09-10'da 87 site sayıldı (17 `.delete()` + 70 `.deleteMany()`); eski ölçüm `.deleteMany()`i hiç görmemişti.

## Bekçiler

`cd Teks-Erp && npx tsx scripts/run-all-tests.ts <ad-parçası>`

Backend: `test_stock_ledger_cancel_restore`, `test_stock_ledger_entry`, `test_stock_ledger_helper`, `test_stock_ledger_issue`, `test_stock_ledger_kursun_reopen`, `test_stock_ledger_manual_move`, `test_stock_ledger_production`, `test_stock_ledger_tambur`, `test_stock_ledger_tambur_undo`, `test_stock_ledger_transform`, `test_roll_movement_revoke`, `test_roll_operation_revoke`, `test_cheque_reversal`, `test_stock_count_reversal`, `test_swatch_stock_reduction_reversal`, `test_work_session_history_guard`, `test_warehouse_ledger`, `test_warehouse_movements`, `test_master_data_merge_revert`, `test_snapshot_kolonlari`, `test_consistency_derived`, `test_stock_ledger_production_entry`

**Bu alanın genel kapısı `scripts/test_defter_ters_yol.ts`** (2026-09-13): her append-only modelin SINIFINI ve her defterin ters yol MEKANİZMASINI beyandan okuyup şemaya ve koda karşı doğrular — 12 bölüm, beyan tablosu `scripts/lib/defter-beyan.ts`. Mekanizma ÇIKARILMAZ BEYAN EDİLİR (dört biçim: damga · ters bağ · tipli enum çifti · net karşı olay); tek kurallı bir kapı en az altı yanlış kırmızı üretirdi. Kapı ayrıca satır yaratan İLERİ yol kümesini beyanla karşılaştırır (yeni yol → kırmızı, ölü beyan → kırmızı), deftere beyansız `delete`i düşürür ve "satırın ters yolu ebeveynindedir" / "saf yapılandırma pivotu" çürütmelerinin zayıf halkasını ölçer. Tip denetleyicili: iç içe ilişki yazımı bağlamsal tipten çözülür (`prisma.importRunLine.create` HİÇ YOK, satırlar `lines: { createMany }` ile doğuyor). Tek tek ölçülen kurallar:

- `scripts/test_roll_movement_revoke.ts` — hareket damgası, partial unique, geri alma sonrası adım durumu (recompute), AST+tip taraması (§6).
- `scripts/test_roll_operation_revoke.ts` — operasyon damgası, partial unique, upsert tuzağı (§8), AST+tip taraması (§7).
- `scripts/test_cheque_reversal.ts` — çek ters yolları: bağ (`reversesTxnId`), bugüne yazım, `createdAt` kronolojisi, kasa/cari mutabakatı, tek kaynak tripwire.
- `scripts/test_stock_count_reversal.ts` — sayım stornosu: ileri bağlar, CANCEL durur + ters satır `reversesMovementId` ile bağlı (tespit BAĞDAN, tipten değil: §6h2 bağsız satır seddi açmaz, §6m terslenmiş ileri satır seçilmez), ileri satır `fromStatus` + sebep kodu taşır, sapma damgası, iplik net ters, LIFO, eski-satır dalı (§6l), hep-ya-hiç.
- `scripts/test_swatch_stock_reduction_reversal.ts` — kartela düşüm stornosu: kalem = iptal kümesi, satır değişmez + ters damga, çift storno 409, ölü kabulün kartelası dirilmez, kalemsiz eski düşüm 409.
- `scripts/test_import_revert.ts` — içe aktarım geri sarma: yazılan her satır `ImportRunLine`a düşer, skaler alanlar `changedFields`te / REPLACE çocukları `childSnapshot`ta `{from,to}` çiftiyle (aynı gerçek iki kolonda durmaz), yaratılan kayıt PASİFE alınır, sonradan değişen alan ATLANIR ve gerekçesi deftere yazılır, ileri satır silinmez/değişmez (yalnız damga kolonları), çift geri sarma 409. **Körlük zemini bekçinin çıktısında basılır:** route adım ağacı ve alias pivotu DB fixture'ıyla koşulmuyor.
- `scripts/test_roll_status_events.ts` — top durum defteri: trigger her durum geçişine bir satır yazar, durum dışı güncelleme yazmaz, iptal aktörü, geri alma karşı kaydı, mühür (doğrudan UPDATE/DELETE RED) + kaskad, kodda yazan 0, göç script'i uçtan uca.
- `scripts/test_audit_okuma_kaynagi.ts` — audit yalnız ayak izi: `system_logs` okuyan her backend işlevi ve audit ucu çağıran her istemci dosyası beyanlı (`scripts/lib/audit-okuma-beyan.ts`, sınıf kapalı küme), allowlist dışı iş okumaları borç cırcırında (hedef 0). DB'siz.
- `scripts/test_work_session_history_guard.ts` — oturum geçmişi silinmez (istasyon/makine 409 + önizleme dökümü + AST: `src/`de `workSession.delete*` ve ham `DELETE work_sessions` yok). Panel metni: `Electron/src/pages/Stations/machineDeleteDescription.test.ts`.
- `scripts/test_snapshot_kolonlari.ts` — ÜÇÜNCÜ SINIF (snapshot) kolonlarının beyanı şemadan türer (ilk-cümle imzası: snapshot · anındaki · donar · donmuş), sınıf alanları tutarlı, yazan/tüketen dosya kümesi AST ile iki yönlü eşit; DB ayağı `test_consistency_derived` §27/§28 (SQL tek kaynak beyan dosyası, SQL ikizinde yok).
- `scripts/test_stok_defteri_bag_olcumu.ts` — açılış fotoğrafı (`scripts/acilis_fotografi_stok_defteri.ts`) SIKI modunun ⑥ kapısı: "sevk/storno/iade/transfer stok defterine bağlı mı" sorusu beyanla değil iki bağımsız araçla ölçülür (K: eski kapı çağıranı AST ile, pozitif kontrol helper tanımı · V: statüsüz/statülü en yeni satır); gevşek mod yalnız açık `--kirilim-beyan` bayrağıyla ve mod + güvenilirlik satır notuna yazılır.
- `scripts/test_warehouse_ledger.ts` ve `test_warehouse_movements.ts` yalnız OKUMA/süzme yüzeyini ölçüyor, "hangi olay satır yazmalı" invariant'ını DEĞİL.
- `scripts/consistency-check.sql`'de `warehouse_movements` mutabakatı HİÇ YOK.

Gereken iki kapı (ayrı iş): ① **defter mutabakatı** — Σ hareket ↔ canlı durum, depo ve iplik için · ② **AST tripwire** — yeni `delete`/`deleteMany` sitesi allowlist dışındaysa kırmızı.
