# Numaralandırma · numara serisi · ön ek

> Alan kural dosyası — bu alana dokunmadan ÖNCE okunur. Kaynak: 2026-09-22 numaralandırma turu (kullanıcı isteği: "buradaki her şey customize edilebilir olabilir mi… başka fabrikalar başka formatlama-ön ek isteyebilir"). Hikâye, ölçüm ve gerekçe arşivde; burada yalnız bugün geçerli kural. Sınıf: **[ÇEKİRDEK]** her kurulumda aynı · **[PROFİL]** bu fabrikanın seçimi.

## Ortak (backend + panel + tablet)

### Değişmezler

- **[ÇEKİRDEK]** Numara DOĞUŞ anında tam string olarak kendi kolonuna yazılır; ekran, belge, Excel ve barkod o kolonu okur — biçimlendirici sunum katmanında ÇAĞRILMAZ. "Programda P-2, çıktıda P20260202" ancak bu delinirse olur (kullanıcı değişmezi 2026-09-22). · bekçi: `scripts/test_number_series.ts §7`
- **[ÇEKİRDEK]** Render'da biçimlenen tek istisna, "eski belgeler de değişsin" bayrağı olan ve varsayılanı KAPALI olan alandır; orada da program ekranı ile belge AYNI resolver'ı çağırır (boğaz-ikiz). Bugünkü tek üyesi sevkiyat içi çuval sırası ön eki (`shipping.sackSeqPrefixLive`, ön ek `Shipment.sackSeqPrefix`te donar). · bekçi: `test_sack_seq_label §2`
- **[ÇEKİRDEK]** Serinin KİMLİĞİ koda (`src/constants/number-series-catalog.ts`), BİÇİMİ veriye (`number_series` tablosu) aittir. Boot uzlaştırması eksik seriyi tohumla doğurur ve `label`/`scanned`/`editable` alanlarını tazeler; `prefix`/`dateSegment`/`digits`/`separator`/`retiredPrefixes` alanlarına ASLA dokunmaz — fabrikanın seçtiği ön ek bir daha koda dönmez. · bekçi: `test_number_series §1`
- **[ÇEKİRDEK]** Sayaç sıfırlama dönemi ayrı bir ayar DEĞİL, tarih segmentinin sonucudur (`DDMMYY`→günlük · `YYMM`/`YYYYMM`→aylık · `YY`/`YYYY`→yıllık · `NONE`→hiç). Sayaç türetilir ("aynı sabit başla başlayanların SAYISAL max'ı + 1"); ayrı sayaç tablosu ve yeni advisory uzayı açılmaz. · bekçi: `test_number_series §6`
- **[ÇEKİRDEK]** Sıra `10^digits`'i aşarsa kod GENİŞLER, SARMAZ; tam-format doğrulaması en az `digits`, fazlasını serbest kabul eder (`\d{digits,}`) — sabit hane dayatan doğrulama 9999'u aşan günde üretilen kodu okutulamaz kılar. · bekçi: `test_number_series §5`
- **[ÇEKİRDEK]** Master veri kodunu İSTEMCİ ÜRETMEZ: kod gönderilmezse sunucu numara serisinden üretir. Fason firma (`FSN`) ve fason kategorisi (`KAT`) 2026-09-22'ye kadar panelde rastgele üretiliyordu (`<ÖNEK>-YYMMDD-rastgele`) ve fabrikanın seri ayarından bağımsız İKİNCİ bir kod rejimi oluşturuyordu; artık diğer bütün master veri kodlarıyla aynı yoldan geçer. İstemci kod gönderirse o kod aynen kullanılır (içe aktarma yolu). · bekçi: `test_subcontractor_code_autogen`
- **[ÇEKİRDEK]** İstemcide gevşek ön ek çapası, ön ekten SONRA rakam ya da ayraç ister. Eskiden bu koruma yalnız `T` ve `IE`deydi (`/^T\d/`, `/^IE\d/`); tablodan gelen bir ön eke elle istisna yazılamadığı için HEPSİNE uygulandı ve yalnız YANLIŞ eşleşmeyi azaltır. Saha maruziyeti ÖLÇÜLDÜ: 0/8.089 kod (bağımsız ikinci ölçümde de 0 — ön ekinden sonra harf gelen kod yok). ⚠️ Bu daralma aynı zamanda fason FİRMA kodunun (`FSN…`) okutulan fason SEVK BELGESİ serisine (`FS…`) düşmesini engelleyen şeydir; çakışma kapısı orada KORUMAZ çünkü yalnız tarama uzayında küreseldir ve firma kodu okutulmaz. · bekçi: `scanSeries.service.test` · `barcode-kind.test`
- **[ÇEKİRDEK]** ELLE verilen kodlar (`manualSackNo` gibi) seri biçiminin DIŞINDADIR ve sınıflandırma onları tanımayabilir; bulunmaları ARAMA yoluyla garanti edilir. Tanınmayan kod yanlış dala düşmez, aramaya düşer — okutma yüzeyinde arama sınıflandırmadan ÖNCE koşar. · bekçi: `resolvePoolScan.test`
- **[ÇEKİRDEK]** Okutulan kodun TÜRÜ sunucunun tek sınıflandırıcısından çözülür (`classifyScannedCode`); istemci ön eki REGEX olarak sabitlemez ve backend'de elle yazılmış ikinci bir tam-format tablosu bulunmaz. Ön ek değişince istemcinin sessizce yanlış dala düşmesi (çuval kodunu top sanıp "Top bulunamadı" demesi) böyle önlenir. · bekçi: `test_scan_series §2 §7`
- **[ÇEKİRDEK]** `/api/scan/series` ve `/api/scan/resolve` izin kodu TAŞIMAZ (yalnız `verifyToken`): okutma her operatör ekranının ilk adımıdır ve dar bir izin, o kodu atanmamış tablette okutmayı kırar. Karşılığında iki uç yalnız BİÇİM meta verisi döner ve `/resolve` DB'ye İNMEZ — bu kısıt bir cümle değil bir kapıdır. `/resolve` KAYIT tablolarına inmez (yalnız `number_series` yapılandırma önbelleğini tazeleyebilir) — kaydın varlığını doğrulamaz, alanını döndürmez. · bekçi: `test_scan_series §6` · `test_route_auth_coverage` (`BARE_CHAIN_BASELINE` 15)
- **[ÇEKİRDEK]** Sayacın kapsamı "bu BİÇİM yürürlüğe girdikten sonra doğan kodlar"dır (`NumberSeries.formatChangedAt`); eski rejimin kodları sayaca giremez. Gerekçe ölçüldü: tarih segmenti düşünce sabit baş kısalır (`CV220926`→`CV`) ve sıra 4 yerine 2.209.260.004 olur. `matchesSeries` ile elemek SAHTE çözümdür — hane taşması kuralı gereği on haneli kuyruk da meşrudur. Kapsam daraltması sayacı 1'e döndürebildiği için üretilen kod ayrıca VAR OLANLARIN üstünden atlar (tavan 10.000, dayanırsa açık hata). · bekçi: `test_number_series_scope §1–§3` · `test_number_series §10`
- **[ÇEKİRDEK]** İade belge no listede KİMLİK SÜTUNUDUR (satır başında) ve SUNUCUDA aranır (`RETURN_CODE_SEARCH_FIELDS`); müşteri telefonda belge numarasını söyleyince aranacak alan budur (sevk partisi kodu emsali). Panel numarayı yalnız OKUR: "lider kim" kuralı istemciye KOPYALANMAZ, biçimlendirici istemcide kurulmaz. · bekçi: `returnNo.test` · `test_belge_ekran_ayni §6`
- **[ÇEKİRDEK]** İade belge no artık TÜRETİLMEZ, `RollReturn.returnNo` kolonundan okunur; belge ve ekran aynı kolonu görür. Numara SATIRA değil BELGEYE aittir (belge `returnGroupId ?? id` ile çözülür): çok kalemli iadede üye satırlar LİDERİN numarasının kopyasını taşır, böylece her satır kendi belgesinin numarasını gösterir ve istemci "lider kim" kuralını kopyalamak zorunda kalmaz. Tekillik BELGE BAŞINADIR (`returnGroupId IS NULL OR = id`); yalnız `IS NULL` demek grupların tamamını kısıt dışında bırakır çünkü lider kendi id'sini taşır. Numara belge başına TEK KEZ üretilir — döngü içinde sayaç çağırmak aynı tx'te aynı numarayı üretip deterministik 409'a düşerdi. · bekçi: `test_return_no_backfill`
- **[ÇEKİRDEK]** Serbest metin ön eklerinde YAZMA yüklemi DAR, OKUMA yüklemi GENİŞ kalır. Yeni değer dosya/sayfa adını kıran karakterleri kabul etmez (bugün: eğik çizgi — `safeFileName` ve `sheetBase` onu `_`/boşluğa çevirip iki sevkiyatı aynı dosyaya düşürüyordu); okuma yüklemi daraltılmaz, çünkü aynı fonksiyon sevk anında DONMUŞ ön eki de süzer ve daraltmak geçmiş belgeyi bugün boş gösterirdi. Türkçe harf kalır: ön ek etiket metnidir, okutulmaz. · bekçi: `test_sack_seq_label §5`
- **[ÇEKİRDEK]** Numaralandırma ekranı KİLİTLİ serileri de çizer ve kilidi ÜÇ AYRI CÜMLEYLE anlatır (`lockKind`): yapısal (hiç açılmaz) · sayaç hazır değil (bizim işimiz) · saha eski (kullanıcının işi). Tek bir "kilitli" cümlesi, kullanıcıyı kendi yapabileceği tek şeyden habersiz bırakır; kilitliyi GİZLEMEK ise "bu seri neden burada yok?" sorusunu doğurur ve o sorunun ekranda cevabı YOKTUR. · bekçi: `lockText.test §1–§3`
- **[ÇEKİRDEK]** Kilit cümlesinin TEK KAYNAĞI sunucudur ve iki soruyu AYRI alanlarda cevaplar: NEDEN kilitli (`lockedReason`) · NE ZAMAN açılır (`lockUnlock`, ölçülebilir koşul) · eylem KİMDE (`lockActor`). Panelde yalnız tek kelimelik rozet ve vurgu kalır; panel cümle yazarsa aynı kilit iki farklı şey söyler (ölçüldü 2026-09-23: rozet "yapısal", diyalog "Faz B"). Jargon yasak — kullanıcının yapamayacağı bir şeyi tarif eden cümle ("kapsam damgasına geçirilmeli") ne olduğunu değil ne YAPILABİLDİĞİNİ söyler. · bekçi: `test_number_series_panel §3b` · `lockText.test §1–§4`
- **[ÇEKİRDEK]** Kilit yükleminin OKUMA kolu (`seriesLock`) ile YAZMA kapısı (`assertSeriesFormatWritable`) aynı fazları arar; okutulan seride iki eşik (Faz B + Faz D) AYRI AYRI yükselebildiği için tek faza bakan bir okuma, paneli açık gösterip ucu 400'e düşürürdü. · bekçi: `test_number_series_panel §3c`
- **[ÇEKİRDEK]** Kilitli seride biçim alanları GİZLENMEZ, PASİFLEŞİR ve o seride önizleme ucuna İSTEK ATILMAZ: uç biçim kilidini de doğruladığı için kilitli satırı açmak tek başına 400 döndürüyor, o hata da diyaloğun tamamını (sayaç + numara kaynağı dahil) kilitliyordu. Hata BÖLÜMÜNE aittir: biçim hatası yalnız biçim kaydını bağlar. · bekçi: `NumberingFormDialog.bolum.test §1–§4`
- **[ÇEKİRDEK]** Sürekli bağlı kalan düzenleme diyaloğunda taslak, satırın ANAHTARINA bağlanır: `row` değişince sıfırlama effect'i ile istek atan effect aynı commit'te koşar ve istek YENİ anahtarla ESKİ biçimi taşır (ölçüldü 2026-09-23: çuval açılınca "IE … iş emri ile çakışıyor" hayalet mesajı). Anahtarı uyuşmayan taslakla istek atılmaz. · bekçi: `NumberingFormDialog.bayat.test §1`
- **[ÇEKİRDEK]** Kullanıcı YAZARKEN koşan uçlar (önizleme) global toast BASTIRIR (`suppressErrorToast`) ve hatayı alanın yanında gösterir; doğrulama mesajları Zod şemasında TÜRKÇE yazılır — mesajsız şema "Çok büyük: beklenen number <=8" diyor ve alanın adını bile söylemiyordu. · bekçi: `NumberingFormDialog.bayat.test §2` · `NumberingFormDialog.bolum.test §3`
- **[ÇEKİRDEK]** Biçim değiştirme kapıları SIRALIDIR ve sıra, engelin KİME iş çıkardığına göre kurulur: YAPISAL (`lockedReason`, hiç kalkmayabilir) → SAYAÇ (`scopedCounter`, bizim hazırlığımız) → İSTEMCİ (C0b, fabrikanın çözebileceği tek engel) → DEĞER (`assertSeriesFormatAllowed`). Ters sırada, iki engel birden geçerliyken fabrika bir gün harcayıp tabletleri günceller ve ikinci duvara toslar. Panel aynı üçlüyü `lockKind` ile AYRI cümleler olarak gösterir. · bekçi: `test_number_series_panel §1 §3`
- **[ÇEKİRDEK]** Etki cümlesindeki sayının BİRİMİ beyan edilir (`countTable.birim`: kayıt ↔ belge) — satır ile belge aynı şey değildir. İade numarası çok kalemli iadede üye satırlara KOPYALANIR; `count(*)` üç satırı üç belge sayar ve cümle yalan olur. Sayım alanı ZORUNLU kolon olmalı ya da null'ları eleyen bir KAPSAM beyan etmelidir; kapsam kapalı bir kümedir (katalog adını yazar, yüklemi servis kurar — sabitler dosyası prisma'ya bağlanamaz). · bekçi: `test_number_series_panel §4` · `test_belge_ekran_ayni §6`
- **[ÇEKİRDEK]** Panelin "bugüne kadarki N …nin numarası değişmez" cümlesindeki sayı ÖLÇÜLÜR (`countTable`); kaynağı olmayan seri sayı YAZMAZ — "0" demek ölçülmemiş bir şeye sıfır demektir. · bekçi: `test_number_series_panel §4`
- **[ÇEKİRDEK]** Önizleme SUNUCUDA hesaplanır ve panel kendi biçimlendiricisini YAZMAZ; biçim kapısı (karakter · hane · ayraç · ön ek çakışması) önizlemede de koşar, yani kullanıcı hatayı "Kaydet"te değil YAZARKEN görür. · bekçi: `test_number_series_panel §5` · `lockText.test §4`
- **[ÇEKİRDEK]** OKUTULAN (`scanned`) bir serinin biçimi, sahadaki panel VE tablet sunucu tabanlı sınıflandırmayı taşımadan değiştirilemez (400 `NUMBER_SERIES_CLIENT_TOO_OLD`). Eşik ileriye dönük bir sürüm TAHMİNİ değil, **o yeteneği taşımayan SON sürümdür** (`FAZ_B_ONCESI`) — tahmin çürür ve çürüdüğü görülmez. İki eksen birlikte aranır; `web` muaftır çünkü web paketi backend'in İÇİNDE gider (drift imkânsız), *okutma yapmadığı için değil* — özet kabuğu `operations` yollarını açar ve orada yedi okutan yüzey vardır (ölçüldü). Bu kapı `lockedReason`dan AYRI bir cümledir: biri "yapısal olarak değişemez", öteki "bugün değişemez" der. · bekçi: `test_number_series_scope §6`
- **[ÇEKİRDEK]** Bir serinin biçimi, sayacı kapsam damgasına HAZIR değilse panelden değiştirilemez (`scopedCounter` beyanı yoksa 400 `NUMBER_SERIES_COUNTER_NOT_SCOPED`). Kapı KONFİGÜRASYON sınırındadır, üretim sınırında değil: üretimde fail-closed yapmak çuvalı açılamaz kılardı, oysa engellenmesi gereken riskli AYAR değişikliğidir. Beyan katalogda alanın yanında yaşar (hazır / sayaç-yok / alan yoksa ölçülmedi). · bekçi: `test_number_series_scope §4–§5`
- **[ÇEKİRDEK]** Serinin tarih ile sıra ARASINDA sabit bir parçası varsa (bugün yalnız top barkodunun faz harfi `[HF]`) bu parça `number_series` tablosuna DEĞİL katalogdaki `infix` alanına yazılır ve biçimle BİRLİKTE taşınır — yapısal özelliktir, fabrika ayarı değildir. Ayrı geçirilen bir parametre olsaydı `matchesSeries(resolveSeriesFormat(key), code)` çağıran bir yol onu unutup SESSİZCE yanlış cevap alırdı. · bekçi: `test_number_series §8`
- **[ÇEKİRDEK]** Biçim değişimi GEÇMİŞE DOKUNMAZ; eski ön ek `retiredPrefixes`e düşer ve okutulmaya devam eder. Geçmişi yeniden numaralandırma seçeneği SUNULMAZ (donmuş belge müşteride basılı · depodaki çuval etiketi eski barkodu taşıyor · numara defter satırlarında referans). · bekçi: `test_number_series §4`
- **[ÇEKİRDEK]** Biçim açılan HER seri, geriye dönük uyumluluk MATRİSİNDEN geçer: seri listesi katalogdan KEŞFEDİLİR (açılan seri kapsama kendiliğinden girer) ve her dönüşüm (ön ek · her tarih segmenti · tarihsiz · hane ± · iki ayraç) için dört iddia ölçülür — eski kodlar TANINMAYA devam eder · yeni kod geçerli ve tekildir · sayaç dizgi uzayındaki İLK BOŞ sıradan başlar (tarih rakamları sıra SANILMAZ, var olan kodun üstüne yazılmaz) · eski ve yeni kod aynı anda aranır. Matris üretim yolunun KENDİ hesabını çağırır (ikinci bir sayaç hesabı yazılmaz). · bekçi: `test_number_series_geri_uyumluluk`
- **[ÇEKİRDEK]** Emekli liste GEÇMİŞİN beyanıdır, çöp kutusu değil: yürürlükteki ön ek orada DURAMAZ (DB sedi `number_series_retired_not_current` + tek yazarın yüklemi), eski bir ön eke DÖNMEK onu listeden çıkarır ve HİÇ KOD ÜRETMEMİŞ bir ön ek (panelde denenip vazgeçilen değer) emekliye AYRILMAZ. Ölçüm ÜÇ SONUÇLUDUR ve düşürme yalnız ÖLÇÜLMÜŞ SIFIRDA yapılır: kullanım `null` ise ön ek KORUNUR, çünkü emekli listeden düşen ön ek sahadaki etiketi okutulamaz kılar. Katalogda TOHUM olarak beyan edilen emekli ön ekler (bugün `workOrder` → `RK`) hiç düşürülmez — veritabanında karşılığı olmayan basılı kartlar sahada okutuluyor ("DB'de yok" ≠ "dünyada yok"). Kirli veri için tek seferlik DEĞİL idempotent ve DAMGASIZ onarım: `scripts/fix_number_series_retired_prefixes.ts` (dry-run varsayılan, her seriyi ÖNCESİ → SONRASI ve gerekçesiyle listeler). · bekçi: `test_number_series_panel §11a–§11d` · `test_db_invariants`
- **[ÇEKİRDEK]** Ön ek çakışması kapısı yalnız TARAMA UZAYINDA küreseldir (`scanned` seriler): iki seri birbirinin ön eki olamaz, emekli ön ekler dahil. Okutulmayan serilerde çakışma meşrudur ve bugün üç yerde vardır (`KS` kartela sevk ↔ kasa kodu · `IADE` iade belgesi ↔ iade sebebi · `P` üretim partisi ↔ sevk partisi adı) — kapıyı küreselleştirmek doğduğu gün üç yanlış kırmızı verir. · bekçi: `test_number_series §3`
- **[ÇEKİRDEK]** Ön ek KARŞILAŞTIRMASININ yanında SONUÇ kapısı da koşar: aday biçimle üretilecek örnek kod (ilk sıra + hane taşmış sıra) okutulan serilerin biçimleriyle sınanır; kod BAŞKA bir okutulan türe çözülüyorsa 409 `NUMBER_SERIES_SCAN_COLLISION` ve mesaj ne olacağını söyler ("bu kod barkod okutmada '<tür>' sanılır"). Kapı OKUTULMAYAN serilerde de koşar — asıl boşluk oydu: okutulmayan bir seri, okutulan bir türün biçimine düşen kod üretebiliyordu ve ön ek kapısı susuyordu. ⚠️ Kapı YENİ ihlali engeller, BUGÜNKÜ durumu yasaklamaz: adayın düştüğü tür bugünkü biçimin de düştüğü türse devralınmıştır (kasa kodu `KS` ↔ kartela sevk `KS` emsali) — yoksa yeni bir kural var olan yapılandırmayı bir anda "kaydedilemez" yapardı. · bekçi: `test_number_series_panel §14`
- **[ÇEKİRDEK]** Kullanıcıya dönen metinlerde (seri adı · kilit gerekçesi · sayaç kilidi gerekçesi · açılma koşulu) tablo/fonksiyon/dosya adı, backtick ve İngilizce tanımlayıcı GEÇMEZ; teknik ayrıntı koddaki yoruma yazılır. "Bu harf `RollBarcodeCounter` anahtarının parçasıdır" cümlesi kullanıcıya hiçbir şey söylemiyordu (d3 panel turu 2026-09-23). · bekçi: `test_number_series_panel §13`
- **[ÇEKİRDEK]** Panel hata mesajını TEK KAYNAKTAN okur (`apiErrorMessage`): gövdedeki `message` alan doğrulamasında yalnız "Validasyon hatası" der, asıl cümle `errors[0].message`tedir. Kendi okumasını yazan ekran, sunucunun Türkçe alan mesajını kaybeder. · bekçi: `NumberingFormDialog.bolum.test §5`
- **[ÇEKİRDEK]** Okutulan serinin ön eki ASCII büyük harf + rakam, en çok 6 karakter (Code128 ve istemcilerin `toUpperCase()` varsayımı; Türkçe harf barkodu bozar). · bekçi: `test_number_series §2`

### Yasaklar

- **[ÇEKİRDEK]** Ön eki servis içinde LİTERAL yazma: `utils/code-format.ts`in biçimlendiricilerini (`dailyCodePrefix` · `buildDailyCode` · `nextDailySeq`) yalnız `services/number-series.service.ts` import eder. İkinci bir import, seri tablosunu SESSİZCE devre dışı bırakır. · bekçi: `test_number_series §7`
- **[ÇEKİRDEK]** Her seri bir panel BÖLÜMÜNE (`panelGroup`) ait olmak ZORUNDA; etiketsiz seri ekrandan sessizce düşer. Kilitli seri de çizilir — "bu seri neden burada yok?" sorusunun ekranda cevabı yoktur, "neden kilitli?" sorusununki vardır. Bölüm sırası ve BAŞLIĞI sunucudan gelir, panelde kopyalanmaz. · bekçi: `test_number_series_panel §4d`
- **[ÇEKİRDEK]** Sayım kaynağı (`countTable`) olmayan seri `uretecBagi` ile BEYAN edilir: kaynaksızlık bir KARARDIR, unutkanlık değil. Üretecini sürmeyen serinin hangi kayıtları numaraladığı bilinemez, bu yüzden sayılmaz. · bekçi: `test_number_series_panel §4e`
- **[ÇEKİRDEK]** `countTable.model` Prisma DELEGE adıdır ve yanlış yazılırsa sayım sessizce `null` döner — statik kontrol göremez, bu yüzden her kaynaklı seri bir kez GERÇEKTEN saydırılır. · bekçi: `test_number_series_panel §4f`
- **[ÇEKİRDEK]** Aynı tabloyu birden çok seri paylaşıyorsa (bugün `Invoice.docNo` ×4 · `Cheque.docNo` ×4 · `Payment.docNo` ×2) etki sayımı `kapsam: "seri-onekli"` ile BEYAN edilir; düz `count(*)` serilerin toplamını basar ve cümle `null` değil YANLIŞ bir sayı söyler. Kapsam serinin yürürlükteki ön eki + EMEKLİ ön ekleriyle sınırlıdır. · bekçi: `test_number_series_panel §4b` + `§4c`
- **[ÇEKİRDEK]** Aynı tabloyu paylaşan serilerin ön ekleri birbirinin BAŞLANGICI olamaz (`SF` yanına `SFX` yazılamaz): ön ek kapsamı ancak böyle ayırt eder, yoksa iki serinin sayısı sessizce karışır. `§9`un tarama-uzayı kuralının tablo-paylaşımı kardeşidir. · bekçi: `test_number_series_panel §4b`
- **[ÇEKİRDEK]** Tanınmayan `kapsam` değeri FAIL-CLOSED'dur: sayım `null` ("ölçülemedi") döner, "hepsini say"a düşmez; aynı unutkanlık `never` ataması ile derleme anında da yakalanır. · bekçi: `test_number_series_panel §4`
- **[ÇEKİRDEK]** Bir numara TEK OKUMADAN doğar: `resolveSeriesFormat` bir kez çağrılır, ön ek (`seriesPrefix`), sıra (`seriesSeqFrom`) ve kod (`formatSeriesCode`) AYNI biçim nesnesinden kurulur. Anahtardan kendi okumasını yapan sarmalayıcı yoktur (silindi): iki okuma arasında bir önbellek tazelemesi olursa ön ek eski sürümden, hane/adım yeni sürümden gelir. · bekçi: `test_number_series §11a` + `§11e`
- **[ÇEKİRDEK]** Ön eki biçimden alıp HANEYİ literal yazma; kodu her zaman `formatSeriesCode` kurar. Yarısı veriden yarısı koddan gelen bir biçimde fabrika haneyi değiştirir, önizleme yeni haneyi gösterir, yazılan kod eski hanede kalır. · bekçi: `test_number_series §11a` + `§11b`
- **[ÇEKİRDEK]** Numara kaynağı ÜÇ değerlidir (`FREE` · `SYSTEM` · `MANUAL`) ve varsayılan `FREE`'dir: bugünkü davranış "sistem üretir AMA elle geleni de kabul eder"dir ve iki değerli bir ayar bunu İFADE EDEMEZ — "yeni ayarın varsayılanı = bugünkü davranış" cümlesi ölçülemez hâle gelirdi. · bekçi: `test_number_series_panel §8c`
- **[ÇEKİRDEK]** REJİMİ (hangi biçim) ÜRETİM ANI seçer; TARİH SEGMENTİNİN DEĞERİNİ belge tarihi verir. Üreteçler `resolveSeriesFormat`ı TARİHSİZ çağırır ve belge tarihini yalnız biçimlendiriciye geçirir; `at` parametresi YALNIZ önizleme içindir. Ters kurgu, geçiş sonrası girilen eski tarihli bir belgeyi KAPANMIŞ bir sayaç kapsamına yazardı. · bekçi: `test_number_series_lines §7d`
- **[ÇEKİRDEK]** Biçim geçişi İLERİ tarihe yazılabilir, GEÇMİŞE yazılamaz (400 `NUMBER_SERIES_EFFECTIVE_FROM_PAST`): biçim geçmişi bir DEFTERDİR, o tarihte üretilmiş numaraların hangi rejimde doğduğu düzeltilmez. İleri tarihli yazma bugünkü biçime DOKUNMAZ; vadesi gelince önbellek tazelemesi onu yürürlüğe alır ve kolonları da günceller (ayrı zamanlayıcı YOK). · bekçi: `test_number_series_lines §7`
- **[ÇEKİRDEK]** BEKLEYEN (vadesi gelmemiş) biçim değişikliği panelde GÖRÜNÜR, aynı tarihe yeniden kayıt onu DEĞİŞTİRİR (ham tekillik hatası kullanıcıya çıkmaz) ve İPTAL edilebilir. İptal SERT SİLMEDİR ve bu defter doktrinine göre ölçülmüş bir karardır: vadesi gelmemiş satır hiç yürürlüğe girmedi, onunla numara doğmadı, silinmesi raporlanan hiçbir sayıyı değiştirmez ⇒ ④ "deftere hiç yazmamış taslak" sınıfı. `revokedAt` damgası YANLIŞ olurdu — damga satırın bir zamanlar geçerli olduğunu ima eder. Silme ATOMİK CLAIM'lidir (`effectiveFrom > now` WHERE'in içinde): yürürlüğe girmiş satır bu yoldan silinemez, 0 satır silinirse 409 `NUMBER_SERIES_NO_PENDING`. İptal audit'e yazılır. · bekçi: `test_number_series_panel §15`
- **[ÇEKİRDEK]** Panelde İKİ ayrı numara gösterilir: "biçim örneği" (kodun ŞEKLİ, hep sıra 1) ve "sıradaki numara" (bir sonraki kaydın GERÇEK numarası, sayacı TÜKETMEDEN ve ÜRETİM YOLUNUN KENDİ hesabıyla — ikinci bir sayaç hesabı yazılmaz). Tek satır varken kullanıcı örneği sıradaki sanıyordu. Ölçülemeyen seride (kendi sayaç mekanizması olan) satır "—" olur, sayı uydurulmaz. · bekçi: `test_number_series_panel §16`
- **[ÇEKİRDEK]** Örnek kod, üreteci OLAN seride o üreteçten kurulur: `previewSeriesCode` katalog `infix`ini (top barkodunun faz harfi) yazmaz ve ekranda gerçekte üretilmeyen bir kod gösteriyordu. Tükenme uyarısı da listede görünür — biçimi kilitli seride kullanıcı kapasitenin dolduğunu başka hiçbir yerde göremiyordu. · bekçi: `test_number_series_panel §16`
- **[ÇEKİRDEK]** Bugünkü biçimi okutulan bir seriyle AYNI kod şeklini üreten seri, panelde BİLGİ satırı taşır (engel değil): bilerek bırakılmış çakışma ile bilmeden bırakılmış olan arasındaki fark budur. Bugün tek üye: kasa kodu (`KS`) ↔ kartela sevk belge no (`KS`). · bekçi: `test_number_series_panel §14`
- **[ÇEKİRDEK]** Okutulan kod EMEKLİ BİÇİMLERLE de denenir: her emekli satır KENDİ segment/hane/ayracıyla sınanır. `retiredPrefixes` yalnız ÖN EK eksenini koruyordu ve hane 4 → 6 değişimi dünkü kodu TANINMAZ yapıyordu (ölçüldü). Emekli biçim eklemek ÜST KÜMEDİR: hiçbir kod eskisinden daha az tanınmaz. · bekçi: `test_number_series §13`
- **[ÇEKİRDEK]** Okutulan serinin biçim kilidi (C0b) İKİ eşiğe birden bakar — Faz B ("biçimi tablodan oku") ve Faz D ("emekli BİÇİMLERİ de dene"); biri ötekini kapsamaz ve "minVersion yükseldi" tek başına kilidi AÇMAZ. · bekçi: `test_number_series_panel §10a`
- **[ÇEKİRDEK]** Sınıflandırma tablosuna alan EKLENİR, var olan alan DEĞİŞTİRİLMEZ: `retiredFormats` opsiyoneldir ve eski istemci onu görmezden gelip bugünkü davranışını sürdürür. `prefixes`in yerine konsaydı eski istemci emekli ön eki HİÇ tanımaz olurdu. · bekçi: `test_number_series_panel §10b`
- **[ÇEKİRDEK]** Biçim satırının KÖKENİ veride beyanlıdır (`origin`): göçün `retiredPrefixes`ten ürettiği emekli satırın biçimi bir TAHMİNDİR (`MIGRATED_GUESS`), panelden doğan satır `RECORDED`. Tarihin sentinel olması (`isSentinel`) BİÇİMİN tahmin olduğunu söylemez — ikisi AYRI sorudur. · bekçi: `test_number_series_lines §6`
- **[ÇEKİRDEK]** Biçim bir DEĞER değil bir ZAMAN ÇİZGİSİDİR: gerçek kaynak `number_series_lines` satırlarıdır (Dynamics "No. Series Line" kalıbı). `number_series` üzerindeki biçim kolonları YÜRÜRLÜKTEKİ satırın ÖNBELLEĞİDİR ve TEK YAZAR ikisini AYNI TX'te yazar; `effectiveFrom` ile `formatChangedAt` aynı andır — ayrışsalardı sayaç kapsamı ile biçim geçişi farklı anlardan başlardı. · bekçi: `test_number_series_lines §2` + `§4`
- **[ÇEKİRDEK]** `retiredPrefixes` bu tablonun ilkel hâlidir ve geçmiş SATIRLARLA iki yönlü eşleşir. Tarihi bilinmeyen geçmiş satır `isSentinel` ile beyanlıdır — sentinel tarih yalnız SIRALAMA taşır, geçmiş bilgisi DEĞİLDİR. · bekçi: `test_number_series_lines §3` + `§5`
- **[ÇEKİRDEK]** İş emri numarasının kaynağı TEK yerden okunur: `workOrder.numberSource`. Eski `workorder.partyCodeAuto` bayrağı artık ondan TÜRETİLİR (FREE→true · SYSTEM→true · MANUAL→false) ve eski anahtara yazmak `numberSource`a yazar; DEĞİŞMEDİYSE hiç yazılmaz (boolean `SYSTEM`i ifade edemez, körlemesine yazmak onu sessizce düşürürdü). · bekçi: `test_number_source_migration §3` + `§4`
- **[ÇEKİRDEK]** Eski bayraktan göç TEK SEFERLİKTİR ve DAMGALIDIR (`numbering.partyCodeAutoMigratedAt`): her açılışta türetme yapılsaydı fabrika panelden seçtiği değeri bir sonraki açılışta kaybederdi. Göç, seri satırları YARATILDIKTAN SONRA koşar (önce koşsaydı satır yokken sessizce no-op olurdu). Ayar satırı HİÇ YOKSA `FREE` yazılır — oradaki `false` bir karar değil `asBoolean(undefined)` artefaktıdır. · bekçi: `test_number_source_migration §1` + `§2`
- **[ÇEKİRDEK]** Dört elle numara yolu (çuval · iş emri · sipariş · sevk partisi adı) TEK yüklemden geçer (`assertManualNumberAllowed`): `SYSTEM`de elle değer 400, `MANUAL`de eksik değer 400, `FREE`de bugünkü davranış. Beyan edilen her yolun kapıyı GERÇEKTEN çağırdığı ölçülür — beyan var çağrı yoksa ayar sessizce etkisizdir. · bekçi: `test_number_series_panel §9e`
- **[ÇEKİRDEK]** OKUTULAN serinin (bugün çuval + iş emri) elle değeri ASCII büyük harf/rakam olmalı ve `classifyScannedCode` onu KENDİ türüne çözmeli; yoksa 400. Küçük harfli kod okutulunca tanınmaz, başka serinin ön ekiyle başlayan kod YANLIŞ DALA düşer. Kural yalnız YENİ kayda uygulanır (ölçüldü: fabrika kopyasında 652 mevcut kodun HİÇBİRİ ihlal etmiyor). · bekçi: `test_number_series_panel §9a`
- **[ÇEKİRDEK]** İstemcinin form çizmek için ihtiyaç duyduğu mod listesi `GET /api/feature-flags` yanıtına eklenir (`settingsPasswordRequired` emsali); YENİ bare-auth uç AÇILMAZ — izin guard'sız uç sayısı cırcırlı bir tabandır ve onu bir form ayrıntısı için yükseltmek kapıyı gevşetir. · bekçi: `test_number_series_panel §9f` + `test_route_auth_coverage`
- **[ÇEKİRDEK]** Numara kaynağı ayarı YALNIZ elle yolu olan seride anlamlıdır (bugün 4: çuval · iş emri · sipariş · sevk partisi adı) ve bu sınır katalogda `manualEntry` ile BEYANLIDIR: yolun yeri yazılır, bekçi dosyanın gerçekten var olduğunu ölçer. Diğer 48 seride uç 400 döner ve panel alanı HİÇ ÇİZMEZ (pasif de çizmez) — kimsenin değiştiremeyeceği bir kutu gürültüdür. · bekçi: `test_number_series_panel §8a` + `§8b` + `§8d`
- **[ÇEKİRDEK]** Biçim kapısı hedef KOLONUN genişliğine bakar: üretilecek en kısa kod `@db.VarChar(n)`e sığmıyorsa 400 `NUMBER_SERIES_CODE_TOO_LONG`. Kapasite envanteri `constants/number-series-capacity.ts`tedir ve şemayla birebir tutulur; kapasitesi beyan edilmemiş seride kontrol YAPILMAZ ("bilmiyorum" ≠ "reddet"). · bekçi: `test_number_series_panel §7a` + `§7b`
- **[ÇEKİRDEK]** Tükenme uyarısı ÜÇ SONUÇLUDUR: sınıra yaklaşıldı (≥ %90) · uzak · ÖLÇÜLEMEDİ. Üst sınırı olmayan seride yüzde TANIMSIZDIR ve `null` döner; `digits` vekil sınır sayılmaz (taşma haneyi genişletir). Yüzde YÜRÜRLÜKTEKİ dönemin kullanımıdır. Eşik ve hesap tek helper'da; panel satırı da `/api/admin/health` de oradan okur. · bekçi: `test_number_series_panel §7c`
- **[ÇEKİRDEK]** Tükenme ölçümü `seriesUsedMaxFrom` ile okunur, `seriesSeqFrom` ile DEĞİL: ikincisi sıradakini verir ve sınır dolduğunda 409 fırlatır — tam %100'de "durum nedir" sorusu cevap yerine hata döndürürdü. Kullanılan sıra bir GÖZLEMDİR, bir talep değil. · bekçi: `test_number_series_panel §7c`
- **[ÇEKİRDEK]** Sayaç ayarları (başlangıç · adım · üst sınır) BİÇİM ayarından AYRI bir uçta ve AYRI bir kilide tabidir: biçimi yapısal olarak kilitli bir serinin (iş emri no) sayacı ayarlanabilir. İkisini tek kilide bağlamak, çözülebilir bir ayarı çözülemez bir gerekçeyle kapatır. · bekçi: `test_number_series_panel §6b`
- **[ÇEKİRDEK]** Sayacı mevcut kodların maksimumundan TÜRETİLMEYEN seri (`ownCounter`: top barkodu · kısa parti no · sevk partisi adı) sayaç ayarlarını 400 ile reddeder; sessiz etkisizlik yasaktır ("adımı 10 yaptım" diyen kullanıcıya 1'er artan bir üreteç). · bekçi: `test_number_series_panel §6b`
- **[ÇEKİRDEK]** Sayaç SIFIRLAMA yoktur ve bu ÖLÇÜLMÜŞ bir sonuçtur: aynı ön ek ve tarih döneminde numaralar tekil olduğu için sıra 1'e döndürülse bile atlama döngüsü ilk boş numaraya yürür ve sonuç değişmez. Ekranda GEREKÇESİYLE kapalı durur; ileri alma başlangıç değeriyle yapılır. · bekçi: `test_number_series_panel §6e`
- **[ÇEKİRDEK]** Sayaç ayarı `formatChangedAt` damgasına DOKUNMAZ: biçim değişmediği için kapsam sınırını oynatmak için sebep yoktur ve damgayı ileri almak yalnız sessiz bir "hiçbir şey olmadı" üretirdi. · bekçi: `test_number_series_panel §6d`
- **[ÇEKİRDEK]** Sayaç kolonları NULL doğar ve NULL = BUGÜNKÜ DAVRANIŞ (başlangıç 1, adım 1, sınır yok); "52 serinin hepsinde varsayılan satırla üretilen kod bugünküyle aynı" iddiası AYRI bir kolla ölçülür — tohum eşitliği bunu karşılamaz. · bekçi: `test_number_series_panel §6a`
- **[ÇEKİRDEK]** Başlangıç · adım · üst sınır hesabı TEK saf helper'dadır (`helpers/series-counter.helper.ts`); `seriesSeqFrom` de `nextSeriesNo` de oradan geçer. Ayar yokken sonuç `max + 1`dir (bugünkü davranış). Atlama döngüsü ADIM kadar ilerler ve sınır DENEME sayar — sıra birimi sayan bir sınır adıma göre sessizce daralır. · bekçi: `test_number_series §12`
- **[ÇEKİRDEK]** Hane bir DOLGU ayarıdır, kapasite değil: taşma haneyi genişletir, sarmaz. Sayacı durduran tek şey `maxValue`dur (aşımda 409 `NUMBER_SERIES_RANGE_EXHAUSTED`) ve panel bunu bu cümleyle söyler. · bekçi: `test_number_series §12d`
- **[ÇEKİRDEK]** Üretecini SÜRMEYEN katalog satırı `uretecBagi` ile BEYAN edilir ve `lockedReason` taşır; beyansız hâli sessiz bir yalandır (panel biçimi gösterir, üreteç literalini yazmaya devam eder). Bugün tek beyan: `roll`. · bekçi: `test_number_series §11c`
- **[ÇEKİRDEK]** Ön ek ile kodu İKİ AYRI `new Date()`ten kurma: `seriesCodePrefix` ve `buildSeriesCode` aynı açık tarih argümanını alır, yoksa gece yarısında dünün ön ekiyle taranıp bugünün ön ekiyle yazılır (sıra 1'e döner, `@unique` çakışır — "iki tarih" sağlamlık sınıfı). · bekçi: `test_number_series §11d`
- **[ÇEKİRDEK]** Tarih segmentinin KAÇ HANE olduğu ve NE ÜRETTİĞİ tek tablodadır (`DATE_SEGMENTS`); istemcilerin `DATE_LEN` haritaları onun aynasıdır ve ayna bekçiyle ölçülür. Tanınmayan segment fallback üretmez, hata verir. · bekçi: `test_tarih_segmenti_aynasi §1`, `§3`
- **[ÇEKİRDEK]** İstemci, sunucudan gelen seri tablosunu HEP-YA-HİÇ reddetmez: tanımadığı satırı atar, gerisini kullanır. Aksi hâlde tek bir yeni segment, istemciye anladığı serileri de kaybettirip bayat yedekle okutur. · bekçi: `test_tarih_segmenti_aynasi §2`
- **[ÇEKİRDEK]** Biçimin İKİ eklem yeri vardır (ön ek|tarih ve tarih|sayaç) ve ikisini de `seriesJoints` verir — üreteçle eşleştirici aynı karardan okur. `separator2` yoksa `separator`a düşer; tarih yoksa ikinci eklem hiç kurulmaz. · bekçi: `test_number_series §14`
- **[ÇEKİRDEK]** Biçim satırı göçü SERİ BAŞINA idempotenttir: satırı olmayan seri her boot'ta kolon önbelleğinden onarılır, satırı olana dokunulmaz. Genel bir damga "iş yapıldı" der ama satırların VARLIĞINI garanti etmez. · bekçi: `test_number_series_lines §1c`
- **[ÇEKİRDEK]** Yapılandırma paketi numara serisi taşıyabilir ama YAZMA panelin yazarından (`updateSeriesFormat`) geçer: kapıları atlayan bir içe aktarım modu yoktur. Önizleme aynı yüklemleri KURU koşar ve her seriyi ÖNCESİ → SONRASI olarak listeler; sayaç taşınmaz ve taşınmayanlar pakette beyanlıdır. · bekçi: `test_config_bundle §9`
- **[ÇEKİRDEK]** Paketin numara serisi taşıması AYAR ŞİFRESİ ister ve kapı UCA değil İÇERİĞE takılır — paket bu türü taşımıyorsa şifre sorulmaz. · bekçi: `test_config_bundle §9d`
- **[ÇEKİRDEK]** Kaynakta örnek kod LİTERALİ yazma: biçim veri olduğu için literal, fabrika ön eki değiştirdiği gün sessizce yalan olur. Örnek, o kodu GERÇEKTEN üreten yoldan türetilir — `infix` taşıyan seride (top barkodu) seri tablosundan türetmek faz harfini DÜŞÜRÜR, kendi üreteci çağrılır. · bekçi: `test_bayat_kod_literali`
- **[ÇEKİRDEK]** Seriden türetilen örnek İSTEK ANINDA üretilir, modül yüklenirken DEĞİL (fonksiyon ya da getter): yükleme anında önbellek BOŞTUR, senkron okuma katalog tohumuna düşer ve örnek fabrikanın gerçek ön ekini BİR DAHA HİÇ göstermez — kural biçimsel olarak sağlanır ama amacına ulaşmaz; ayrıca boş önbellek yükleme anında 52 seriyi tazeleyip ~55 SELECT açar ve başka bir bekçinin sorgu bütçesine taşar (ölçüldü 2026-09-23: pencere 64 → 72-95). · bekçi: `test_seri_modul_yuklemesi` · `test_number_series_panel §12`
- **[ÇEKİRDEK]** YAPISAL kilit (`lockedReason`) kümesi KAPALIDIR ve bugün İKİ üyesi vardır: top barkodu (tarih ile sıra arasında faz harfi + `RollBarcodeCounter` anahtarı) ve kısa parti no (fiziksel plaka seti, körlemesine sarar). Gerekçesi çürüyen bir kilit, kilit değil KALINTIDIR ve kaldırılır — iade belge no (`returnNo` kolonu doğunca) ve iş emri/kart no (gerekçesi "Faz B inmeden" idi, Faz B indi; kalan engeller sayaç kapsamı ile okutulan-seri kilididir) bu yüzden kümeden çıktı. Kilit eklenecekse gerekçe önce ölçülür. · bekçi: `test_number_series_panel §3b`

### Kararlar

- **[PROFİL]** Seçilebilir tarih segmentleri: tarihsiz · `GGAAYY` · `GGAAYYYY` · `YYYYAAGG` · `YYAA` · `AAYY` · `YYYYAA` · `YY` · `YYYY`. Enum KİMLİĞİ İngilizce (`DDMMYYYY`…), ekranda Türkçe etiket + örnek gösterilir — kimlik tek dilde kalır, kullanıcı kendi dilinde okur.
- **[PROFİL]** Bugünkü biçimler: `CV`/`SVK`/`KRT`/`T…H|F` + GGAAYY + NNNN · `PRT-YYMM-NNNN` · `P-n` (sevk partisi adı) · `STK-NNNNNN` (tarihsiz) · `IADE-GGAAYY-<id6>`. Hepsi `number-series-catalog.ts`te tohum olarak yazılıdır ve bekçi her birini eski üreteçle karşılaştırır.
- **[ÇEKİRDEK]** Önbellek fail-SAFE'tir, fail-closed değil ve bu BEYANLIDIR: seri okunamazsa katalog tohumuna düşülür. Gerekçe — numara üretememek üretimi durdurur (çuval açılmaz, sevk kurulmaz), tohum ise bugünkü davranıştır.

**Bekçiler:** `scripts/test_number_series_lines.ts` (DB'li, 28 kontrol) · `scripts/test_number_source_migration.ts` (DB'li, 13 kontrol) · `scripts/test_number_series.ts` (DB'siz, 81 kontrol) · `scripts/test_number_series_scope.ts` (DB'li, 15 kontrol) · `scripts/test_number_series_panel.ts` (DB'li, 87 kontrol) · `scripts/test_return_no_backfill.ts` (DB'li, 16 kontrol) · `scripts/test_scan_series.ts` (DB'siz, 30 kontrol) · `scripts/test_tarih_segmenti_aynasi.ts` (DB'siz, 13 kontrol) · `scripts/test_subcontractor_code_autogen.ts` (12 kontrol) · `scripts/test_belge_ekran_ayni.ts` (uçtan uca: ekranda görünen numara belgede birebir) · `scripts/test_sack_seq_label.ts` · `scripts/test_db_invariants.ts` (`number_series_digits_range`) · `scripts/test_packing_group.ts` · `scripts/test_sevk_partisi.ts`
