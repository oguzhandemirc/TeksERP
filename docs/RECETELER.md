# Reçeteler — "X eklerken şu yerleri güncelle"

> Koddan çıkarılmış adım listeleri (anlama turu 2026-09-05: 9 prosedür; standart turu 2026-09-05: +5 — servis metodu · Prisma modeli · ESLint kuralı · bağımlılık · ölü paket kaldırma). Her adımın kanıtı `dosya:satır`. ⚠️ işaretli adımlar yazıldıkları güne dek HİÇBİR belgede yoktu — atlandığında sessiz düşen adımlar bunlar. Rutin yazım konvansiyonlarının kendisi `docs/standart/` altındadır; buradaki reçeteler ADIM listesidir, kural metni değil. Alan-özgü küçük reçeteler en altta.


## Yeni backend route + izin kodu

Mekanik kapı: Kısmen VAR, adım başına dağınık: test_permission_catalog.ts (kod↔katalog↔DB, kolon kuralı) · test_role_template_catalog.ts (§2 kapsam + muaf) · test_screen_catalog.ts (§1/§3/§4/§5/§7a/§9 — manifesto, mobil ayna, Electron route/karo hizası) · test_route_auth_coverage.ts (verifyToken kapsamı) · test_r

1. Route dosyasını yaz: `src/routes/<x>.routes.ts` — her uç `verifyToken` + `requirePermission(kod)` ya da `requireAnyPermission(...)` taşır; tercih edilen biçim `router.use(verifyToken, ...)` ile TOPLU kapı (uç uç yazımda biri unutulur). <sub>(Teks-Erp/src/middlewares/rbac.middleware.ts:53,74 · Teks-Erp/src/routes/kursun-bypass.routes.ts:24 `router.use(verifyTok)</sub> — atlanırsa: verifyToken düşerse uç SESSİZCE public olur — hata da log da çıkmaz (fail-open desen; test_route_auth_coverage.ts:5-18).
2. Uç bir modüle aitse ADLANDIRILMIŞ modül kapısı: `requireProductionEnabled` / `requireTicaretEnabled` / `requireIplikEnabled` / `requireDepoMultiEnabled`. Kapı `verifyToken`dan SONRA ve `router.use` satırından ÖNCE tanımlanan uç kapıyı hiç görmez. <sub>(Teks-Erp/src/middlewares/module.middleware.ts:86,111,136,161 · kursun-bypass.routes.ts:17-24 (sıra load-bearing yorumu))</sub> — atlanırsa: Modül kapalı kurulumda uç açık kalır; `router.use`tan önce tanımlanan uç kapıyı görmez ve Express bunu ne loglar ne hata verir.
3. Router'ı `app.ts`e mount et: `app.use("/api/<yol>", <x>Routes)` (ya da bir üst router'ın `.use`u). <sub>(Teks-Erp/src/app.ts:832 `app.use("/api/kursun-bypass", kursunBypassRoutes);`)</sub> — atlanırsa: Uçlar 404 döner; derleme temiz, bekçiler yeşil, hiçbir log 'bu router bağlı değil' demez (test_route_mount_reachability.ts:4-20 — 9 uç böyle ölü kaldı).
4. İzin kodunu TEK KAYNAĞA ekle: `src/constants/permission-catalog.ts` → `PERMISSION_CATALOG` dizisine `{code, module, category, description}` satırı. seed.ts'e veya başka listeye KOPYALAMA. <sub>(Teks-Erp/src/constants/permission-catalog.ts:47 (dizi) · dosya başlığı satır 18-26 'YALNIZ bu dosyaya yaz')</sub> — atlanırsa: Admin dışı HERKES 403 alır ve mesaj sebebi söylemez ('yetkiniz yok' der, 'böyle bir izin tanımlı değil' demez) — 2026-08-01 kurşun bypass vakası.
5. Kod biçimi: izin kodunda TAM BİR iki nokta olmalı (`x:y`). İki kolonlu kod (`mobile:depo:write`) yasak. <sub>(Teks-Erp/scripts/test_permission_catalog.ts:375-382 `kolonIhlali` · rbac.middleware wildcard İLK iki noktadan üretir (au)</sub> — atlanırsa: `mobile:*` taşıyan HERKES yeni kodu otomatik alır ve bu, kullanıcının atanmış izin listesinde GÖRÜNMEZ.
6. Ön ek seçimi load-bearing: `mobile:` ön ekli izin masaüstü giriş kapısında SAYILMAZ (kapı `category` kolonuna değil kod ön ekine bakar). <sub>(Teks-Erp/src/services/auth.service.ts:351-358 `!permissions.some((p) => !p.startsWith("mobile:"))` → 403, token üretilme)</sub> — atlanırsa: Masaüstü ekranı için `mobile:` ön ekli kod seçilirse yalnız o izni taşıyan kullanıcı panele hiç giremez (login 403).
7. Rol şablonu: kodu `ROLE_TEMPLATE_CATALOG`ta en az bir DAR role ekle, ya da `ROLE_COVERAGE_EXEMPT`e GEREKÇEYLE yaz (muaf listesi iki yönlü denetlenir). <sub>(Teks-Erp/src/constants/role-template-catalog.ts:591 ROLE_COVERAGE_EXEMPT · scripts/test_role_template_catalog.ts:106-118)</sub> — atlanırsa: İzin DB'ye gelir ama hiçbir hazır pakete girmez; yönetici 86 kutudan tek tek işaretlemedikçe kimse yeni yetkiyi almaz (2026-08-06 ölçümü: şablon 55, katalog 67)
8. Ekran manifestosu: `SCREEN_CATALOG`a ekran girdisi ekle (`requires`/`capabilities` + ZORUNLU `modul` beyanı), ya da `SCREENLESS_PERMISSIONS`a gerekçeli muaf yaz. <sub>(Teks-Erp/src/constants/screen-catalog.ts:394 `SCREENLESS_PERMISSIONS`, :402 `permissionsWithoutScreen` · scripts/test_sc)</sub> — atlanırsa: İzin panelin 'Ekrana göre' atama görünümünde ve 'neden giremiyor' aracında hiç görünmez; `modul` boşsa tamlık bekçisi kırmızı.
9. Electron: `src/routes/content-routes.tsx`e `<ProtectedRoute requirePermission=... / requireAnyPermission={[...]}>` ekle ve İLGİLİ karoya (`pages/*/tile-config.ts` `permission` / `permissionAny`) AYNI listeyi yaz. <sub>(Electron/src/routes/content-routes.tsx:208 `requireAnyPermission={["station:read","label-template:read"]}` · Electron/sr)</sub> — atlanırsa: Kart↔route ayrışırsa kullanıcı kartı görür, tıklar ve `/forbidden`a düşer (Electron/CLAUDE.md:133).
10. Electron ortak izin kümesi kullanılıyorsa `Electron/src/lib/permissions.ts` (backend `src/constants/document-design.ts` AYNASI) güncellenir; READ kümesi WRITE kodunu da içermeli. <sub>(Electron/src/lib/permissions.ts:25-32 · Teks-Erp/src/constants/document-design.ts (ayna, test_document_template_permissi)</sub> — atlanırsa: İki liste ayrışır; READ, WRITE'ı kapsamazsa yalnız 'düzenleme' kutusu işaretlenen kullanıcı ekranı HİÇ açamaz ve sebebi hiçbir yerde yazmaz.
11. Mobil izin ise `mobil/src/types/permissions.ts`: `MobilePermission` union'a kodu, `MobileScreenKey` + `MOBILE_SCREENS`e ekran girdisini ekle (backend kataloğu import EDİLEMEZ, elle ayna). <sub>(mobil/src/types/permissions.ts:1-35 (MobilePermission), :59 MobileScreenKey, :84 MOBILE_SCREENS · Teks-Erp/scripts/test_)</sub> — atlanırsa: Union'a yazılmayan kod `has()` düz string aldığı için sonsuza dek sessizce `false` döner — kod doğru yazılmış olsa bile koruma yok (dosyanın kendi notu: `custom
12. Mobil ekran ise iki kaydı daha doldur: `MainNavigator` `SCREEN_LOADERS` ve `theme/tokens.ts` `moduleAccents` (ikisi de `Record<MobileScreenKey,…>` — eksik anahtar derlemede düşer). <sub>(mobil/src/navigation/MainNavigator.tsx:20 · mobil/src/theme/tokens.ts:156)</sub> — atlanırsa: Sessiz değil: TS `Record` tamlığı derlemede TS2741 verir. (Sessiz olan kısım union'a eklemeyi unutmaktır — 11. adım.)
13. Mobil ekranın dokunduğu HER backend ucu `requireAnyPermission('<web-izni>', 'mobile:<ekran>')` kabul etmeli — yalnız `requirePermission` yazılırsa saha kullanıcısı 403 alır. <sub>(Teks-Erp/CLAUDE.md:323 (kontrol listesi maddesi) · Teks-Erp/scripts/test_mobile_screen_permissions.ts:1-26 (2026-08-17 s)</sub> — atlanırsa: Ekran SESSİZ 403 alır; hata gösterilmez, liste boş gelir ve operatör 'veri yok' sanır.
14. Swagger `@openapi` JSDoc bloğunu route dosyasına yaz (glob `../routes/**/*.ts`). <sub>(Teks-Erp/src/config/swagger.ts:46-48 `apis: [apiGlob('../routes/**/*.ts'), …]` · örnek blok kursun-bypass.routes.ts:51-6)</sub> — atlanırsa: Blok HİÇ yazılmazsa uç Swagger'da görünmez ve bunu ölçen bekçi YOK; blok yazılıp YAML'i bozuksa swagger-jsdoc onu varsayılan ayarda sessizce atlar (test_swagger
15. Bekçileri koş: test_permission_catalog · test_role_template_catalog · test_screen_catalog · test_route_auth_coverage · test_route_mount_reachability · test_swagger_spec (+ mobil ekran ise test_mobile_screen_permissions). <sub>(Teks-Erp/scripts/ dizini: test_permission_catalog.ts, test_role_template_catalog.ts, test_screen_catalog.ts, test_route_)</sub> — atlanırsa: Bekçiler koşulmazsa yukarıdaki adımların 4-8 ve 11'i geliştirme anında değil sahada (403 / boş liste / 404) fark edilir.
16. Deploy: backend RESTART edilir — boot uzlaştırması (`permission-catalog.job` → `role-template-catalog.job` zinciri) eksik satırları DB'ye yazar. Elle SQL/veri migration'ı YAZILMAZ. <sub>(Teks-Erp/src/server.ts:14 `startPermissionCatalogReconciler` · src/jobs/permission-catalog.job.ts:153-160 (`.then(reconc)</sub> — atlanırsa: Restart edilmezse izin satırı DB'de doğmaz; test_permission_catalog'un 'DB katalogun tamamını içeriyor' kontrolü kırmızı verir (o DB'de uzlaştırma koşmamış deme
17. ATAMA elle yapılır (katalog koda, atama panele/script'e) ve kullanıcı yeniden giriş yapmalı — JWT'deki izin listesi bayat kalır; ham SQL ile yazılırsa `tokenVersion` ELLE artırılır. <sub>(Teks-Erp/scripts/test_permission_grant_source.ts:5-21 (JWT'den okunur, tokenVersion artmazsa yetki kullanılamaz; sahada )</sub> — atlanırsa: Panel ve DB 'yetki var' derken uç 403 döner; yönetici teşhis edemez.

⚠️ Belgesiz kalan adımlar (koddan çıkarıldı, hiçbir .md'de yoktu):

- Ekran manifestosu adımı (SCREEN_CATALOG girdisi + zorunlu `modul` + SCREENLESS_PERMISSIONS muafı) HİÇBİR kontrol listesinde yok — yalnız tasarım/plan metinlerinde anlatılıyor.
- Rol şablonu adımı (ROLE_TEMPLATE_CATALOG / ROLE_COVERAGE_EXEMPT) 'Yeni Endpoint Kontrol Listesi'nde YOK; ayrı bir bölümde anlatılıyor ve o bölüm listeyle bağlanmamış.
- İzin kodunda 'TAM BİR iki nokta' kuralı hiçbir .md'de yazılı değil (yalnız test_permission_catalog.ts:375 ve audit haritası K5:156).
- `mobile:` ön ekinin masaüstü giriş kapısını belirlediği (auth.service.ts:351) izin ADLANDIRMA kuralı olarak hiçbir yerde yazılı değil — Electron/CLAUDE.md:131 yalnız sonucu anlatır.
- Electron karo ↔ route izin hizası genel kural olarak yazılı değil; yalnız belge-tasarım vakası için (Electron/CLAUDE.md:133) yazılmış.
- `test_route_mount_reachability` ve `test_route_auth_coverage` bekçileri hiçbir kontrol listesinde adı geçmiyor (yalnız tarihli deploy notunda/gerekçe paragrafında).
- Swagger bloğunun TAMAMEN unutulmasını yakalayan bekçi YOK (test_swagger_spec yalnız bozuk YAML + yol sayısı zemini ölçer) — bu boşluk hiçbir belgede yazılı değil.
- Mobil `MobilePermission` union'a eklemeyi unutmanın `has()` üzerinden SESSİZ `false` ürettiği uyarısı yalnız mobil kaynak yorumunda; Teks-Erp/CLAUDE.md:244 'bekçi orayı taramaz' diyerek TERSİNİ ima ediyor.
- Adımların SIRASI hiçbir yerde yazılı değil (kapı `verifyToken`dan sonra, `router.use` satırından önce tanımlanan uç kapısız kalır — yalnız route dosyası yorumunda).

## Yeni servis metodu

Mekanik kapı: KISMİ. Var olanlar: `Teks-Erp/eslint.config.mjs` (tx closure + `Promise.all` · route/controller'da `lib/prisma` import · `notIn: []` · `orderBy: { batchNumber }` · jenerik `requireModule()`) · tip geçidi (`npm run typecheck` + `npm run typecheck:scripts`; hüküm ÇIKIŞ KODUNDAN verilir) · bekçi paketi (`Teks-Erp/scripts/run-all-tests.ts`, tip + üretim-DB + migration durumu geçitleriyle). Kapısı OLMAYANLAR: servis biçimi (fonksiyon mu class mı), zarfı kimin kurduğu, dönüşün adlandırılmış DTO'ya bağlanması — üçü de yalnız insan incelemesiyle tutulur. Standart metni: `docs/standart/BACKEND.md` (§3 anatomi · §4 biçim · §5 zarf · §6 projeksiyon · §11 boyut).

1. Yeri seç: yeni yüzey → yeni `src/services/<konu>.service.ts` ve düz `export async function`; `class` AÇMA. Mevcut mega servise metod eklemek VARSAYILAN DEĞİLDİR. <sub>(Teks-Erp/src/services/sack-tag.service.ts — 2026-09-04 referans dosyası; ölçüm: son 30 servis dosyasının 27'si düz fonksiyon, class+singleton 77 dosya DEVRALINAN)</sub> — atlanırsa: Class'lı servis derlenir ve çalışır; sapma hiçbir kapıda görünmez, mega dosya bir metod daha büyür ve boyut baseline'ı yükselir.
2. Dosyayı `// ====` banner'ı + 3–8 satır NEDEN ile aç; tarih ve ölçüm anlatısı yazma (o arşive gider). <sub>(Teks-Erp/src/services/sack-tag.service.ts:1-18 · docs/standart/ILKELER.md [IL-24]/[IL-25] · docs/KOD-KURALLARI.md § Yorum politikası)</sub> — atlanırsa: Altı ay sonra dosyanın NEDEN var olduğu yalnız git log'unda kalır; ikinci bir servis aynı işi paralel yüzeyle yeniden yazar.
3. Projeksiyonu sabitle: `const X_SELECT = {…} satisfies Prisma.XSelect` + `export interface XDto`; `include` KULLANMA, dönüşü `unknown`/`any` BIRAKMA. <sub>(34 `satisfies Prisma` sabiti; select 2.601 : include 269 · docs/standart/BACKEND.md §6)</sub> — atlanırsa: `Promise<ApiResponse<unknown>>` istemciye tip taşımaz; alan adı değişince tsc susar, hata panelde "undefined" olarak görünür (211/429 devralınan imza bu sınıftan).
4. İmza `(input: {...}, …, userId?: string)` — aktör SON parametre, parametre sayısı ≤ 4 (fazlası `opts` nesnesi). Zod BURAYA GİRMEZ; gövde route/controller'da parse edilir. <sub>(serviste zod 2/269, ikisi paylaşılan helper · docs/standart/BACKEND.md §9 · parametre sınırı docs/standart/ILKELER.md §6)</sub> — atlanırsa: Aynı gövde iki yerde doğrulanır ve şemalar ayrışır; ayrışma SESSİZDİR çünkü Zod tanımadığı anahtarı hata vermeden siler.
5. Doğrulama ve normalizasyon saf yardımcılara: `normalizeX` (biçim) · `assertX` (geçerlilik, void + throw). Kullanıcıya dönen her hata `AppError.badRequest/conflict/notFound` ve mesaj TÜRKÇE. <sub>(Teks-Erp/src/utils/app-error.ts; 1.922 `AppError.*` çağrısı, `throw new Error` yalnız 17 iç sentinel)</sub> — atlanırsa: Çıplak `throw new Error` error.middleware'de 500'e düşer; kullanıcı "Sunucu hatası" görür, gerçek sebep yalnız log'da kalır.
6. İstemcinin DALLANDIĞI hata `AppError.conflict(mesaj, { code: "X" })` taşır; kod uydurma — yalnız Electron/mobil o kodu okuyacaksa yaz. İstemci `details.code` okur, `body.code` DAİMA undefined'dır. <sub>(üretim 98 / tüketim 41 · kök CLAUDE.md § Kapılar ve sözleşme · docs/KOD-KURALLARI.md)</sub> — atlanırsa: Kodsuz 409 istemcide "bilinmeyen hata" dalına düşer; ters yönde her hataya kod uydurmak istemciyi hiç okumadığı bir sözleşmeye bağlar.
7. Yazma TEK `prisma.$transaction(async (tx) => …)` içinde ve closure parametresi DAİMA `tx`; tx içinde `Promise.all` YOK, dış I/O YOK, audit YOK. <sub>(`$transaction` serviste 196 / route 0 / controller 0 · ESLint selector'ı closure ADINA dayanır — Teks-Erp/eslint.config.mjs)</sub> — atlanırsa: Closure'ı `t`/`trx` diye adlandırırsan ESLint KÖRLEŞİR ve `Promise.all` yasağı o dosyada hiç uygulanmaz; paralel `tx.*` çağrıları bağlantıyı sırasız kullanır.
8. Durum geçişi varsa ATOMİK CLAIM: `tx.x.updateMany({ where: { id, <beklenen durum> } })` + `count === 0 → AppError.conflict`; `findUnique → if → update` YASAK. Benzersiz no/sayaç okuması tx İÇİNDE ve `withBarcodeRetry` ile. <sub>(80 claim sitesi, 74'ü count kontrollü · docs/standart/ESZAMANLILIK.md §2 karar tablosu)</sub> — atlanırsa: İki tablet aynı topu aynı anda ilerletir, ikisi de 200 alır ve defterde çift kayıt doğar — "count kontrolü yoksa bu bir claim değil DÜRTMEDİR".
9. Advisory kilit gerekiyorsa tx'in **İLK** ifadesi olur, uzayı envanterden alınır ve adlandırılmış `X_LOCK_NS` sabitiyle yazılır. <sub>(Teks-Erp/src/services/helpers/period-guard.helper.ts başlığı = uzay envanteri · docs/standart/ESZAMANLILIK.md §3)</sub> — atlanırsa: Sonradan alınan kilit TOCTOU penceresini kapatmaz; kod "kilitli" görünür, yarış aynen sürer ve bekçi yeşil kalır.
10. tx isteyen ortak parçayı `src/services/helpers/<konu>.helper.ts`'e çıkar: adı `*Tx` ve İLK parametresi `tx: Prisma.TransactionClient`. Helper `ApiResponse` KURMAZ. <sub>(48 tx-alan fonksiyonun 33'ü `*Tx` sonekli; helper'da zarf 0/106)</sub> — atlanırsa: Soneksiz tx-alan fonksiyon "bu tx ister mi" sorusunu gizler; tx dışından çağrıldığında atomiklik sessizce kaybolur (15 devralınan emsal).
11. `AuditService.log({ userId, action, tableName, recordId, oldData?, newData })` tx BİTTİKTEN sonra ve `await` ile çağrılır. <sub>(audit tx içinde 0/323; `void` yalnız finans paketinde bilinçli · kök CLAUDE.md § Veri ve defter)</sub> — atlanırsa: Tx içindeki audit rollback'te birlikte geri alınır — yani en çok ihtiyaç duyulan anda (başarısız işlem) hiç iz kalmaz.
12. Dönüş `{ success: true, data, message? }` = `ApiResponse<XDto>`; engel olmayan not `warnings[]`e girer. Route/controller yalnız `res.status(N).json(sonuc)` yapar, KENDİ `success: true` nesnesini kurmaz. <sub>(zarf üreticisi bugün üç yerde: servis 429 · inline route 106 · `reportEnvelope` 21 — yeni kodda tek üretici SERVİSTİR)</sub> — atlanırsa: Zarf iki yerde kurulunca alan adları ayrışır (`message` vs `mesaj`, `data` vs düz gövde) ve istemci hangi şekle güveneceğini bilemez.
13. Bekçi: `Teks-Erp/scripts/` köküne `test_<konu>.ts` — `check(label, ok)` + `=== Sonuç: N geçti, M başarısız ===` + `process.exit(fail > 0 ? 1 : 0)` + `$disconnect()`; NEGATİF SONDA ile kırmızı verdiği ÖLÇÜLÜR. Tam sözleşme § Yeni bekçi (scripts/test_*.ts) yazma. <sub>(Teks-Erp/scripts/run-all-tests.ts · docs/standart/TEST-VE-DERLEME.md [TD-14])</sub> — atlanırsa: Sondasız bekçi bir NİYETTİR: koştuğu ilk günden itibaren yeşil kalır ve neyi koruduğu hiç ölçülmemiş olur.

⚠️ Boyut: servis/route/controller dosyası ≤ 400 (yeni dosyada asla > 600) · helper ≤ 300 · fonksiyon ≤ 60 · inline route handler ≤ 40 — satır = KOD satırı, yorum ve boş satır sayılmaz. Kapı `eslint max-lines`/`max-lines-per-function` (warn) + `node scripts/check-lint-baseline.mjs --proje=backend`; tavan yalnız DÜŞER. <sub>(docs/standart/BACKEND.md §11 · docs/standart/ILKELER.md §6)</sub>

## Yeni feature flag / sistem ayarı

Mekanik kapı: Teks-Erp/scripts/test_feature_flag_contract.ts (1518 satır, §1-§16: A=getFeatureFlags çıktısı · B=updateSchema.shape · C=setFeatureFlags hasOwnProperty dalları · D=Electron settings-config key/numberKey/enumKey; ayrıca §11 SETTING_KEYS evreni, §12/§16 defaultOn↔okuyucu birebirliği, §14/§14b rejim+mo

1. 1) DB anahtarını `SETTING_KEYS` sözlüğüne ekle (tek kaynak; DB adı `alan.altAlanEnabled` kalıbı). <sub>(Teks-Erp/src/services/system-setting.service.ts:65 `export const SETTING_KEYS = {` — ör. :218 KK1_DUPLICATE_GUARD_ENABLE)</sub> — atlanırsa: Anahtar dizesi çağrı noktalarına elle kopyalanır; yazım hatası 400 vermez, okuyucu kaydı bulamaz ve SESSİZCE varsayılana düşer.
2. 2) `readXxx(tx?: Pick<typeof prisma,"systemSetting">)` okuyucusunu yaz — `asBoolean`/`asNumber` + kayıt-yok varsayılanı; `tx` parametresi ZORUNLU (toplu okuma buradan besleniyor). <sub>(Teks-Erp/src/services/system-setting.service.ts:3395-3404 (readKk1DuplicateGuardEnabled); asBoolean :57, asNumber :47)</sub> — atlanırsa: `tx` parametresi unutulursa getFeatureFlags her çağrıda o bayrak için fazladan bir DB round-trip atar (sessiz performans kaybı); okuyucu hiç yazılmazsa varsayıl
3. 3) Backend `FeatureFlags` arayüzüne alanı ekle (camelCase API adı). <sub>(Teks-Erp/src/services/system-setting.service.ts:1173 `export interface FeatureFlags {`)</sub> — atlanırsa: SESSİZ DEĞİL: getFeatureFlags nesnesine yazınca TS derleme hatası verir. Ama adım 4 de atlanırsa hiçbir yerde iz kalmaz.
4. 4) `getFeatureFlags()` gövdesine `xxx: await readXxx(cacheClient)` satırını ekle — `cacheClient` argümanı atlanamaz. <sub>(Teks-Erp/src/services/system-setting.service.ts:1578-1697 (satır 1601 `pricingEnabled: await readPricingEnabled(cacheCli)</sub> — atlanırsa: EN SESSİZ ADIM: bayrak API yükünde dönmez → panel ve tablet onu hiç görmez; üstelik bekçinin A kümesi getFeatureFlags ÇIKTISIDIR (scripts/test_feature_flag_cont
5. 5) `setFeatureFlags` içine yazma dalı: `hasOwnProperty.call(input,"xxx")` + tip kontrolü + `this.set(SETTING_KEYS.X, v, "açıklama", userId)`. <sub>(Teks-Erp/src/services/system-setting.service.ts:1789 `async setFeatureFlags(`; örnek dal :1808-1817 (pricingEnabled))</sub> — atlanırsa: PATCH 200 + "kaydedildi" döner, DB'ye hiçbir şey yazılmaz. Bekçi yakalar: test_feature_flag_contract.ts:239 "updateSchema'daki her anahtarın setFeatureFlags yaz
6. 6) `feature-flag.routes.ts` `updateSchema` (z.strictObject) içine Zod alanını ekle. <sub>(Teks-Erp/src/routes/feature-flag.routes.ts:193 `export const updateSchema = z.strictObject({`; gerekçe :173-192)</sub> — atlanırsa: strictObject olduğu için PATCH 400 verir — bayrak panelden ne AÇILABİLİR ne KAPATILABİLİR (2026-08-04 kk1DuplicateGuardEnabled vakası; asıl tehlike açamamak değ
7. 7) Electron `FeatureFlags` arayüzünün aynasına alanı ekle. <sub>(Electron/src/services/featureFlagService.ts:223 `export interface FeatureFlags {`)</sub> — atlanırsa: Panele satır eklemeye kalkınca `BooleanFlagKey` union'ında bulunmadığı için tsc kırmızı (gürültülü). Satır eklenmezse tamamen sessiz: bayrak panelde hiç görünme
8. 8) Electron panel satırı: `settings-config.ts` SETTINGS_CATEGORIES içindeki `flags` (`key:`) / `numberFlags` (`numberKey:`) / `enumFlags` (`enumKey:`) dizisine satır — title/summary/desc/audience + `defaultOn` ya da `defaultValue`. <sub>(Electron/src/pages/GeneralSettings/settings-config.ts:531 SETTINGS_CATEGORIES; örnek satır :907-916; alan adı kuralları )</sub> — atlanırsa: Bekçi §5 kırmızı ("yönetilemez boolean bayrak yok", test_feature_flag_contract.ts:294-298) — alternatif gerekçeli `PANEL_EXEMPT` satırıdır (:100). `numberKey`/`
9. 9) `defaultOn` / `defaultValue` beyanını backend okuyucusunun kayıt-yok değeriyle BİREBİR yaz (ikinci kaynak; bekçi boş istemciyle çağırıp doğrular). <sub>(Electron/.../settings-config.ts:66-72 (defaultOn şerhi) ve :134-135 (defaultValue şerhi); bekçi test_feature_flag_contra)</sub> — atlanırsa: Bekçi yakalar (§12/§16). Bekçi koşturulmazsa panel rozeti "Varsayılan: AÇIK" derken backend KAPALI döner; kullanıcı ayarı yanlış okuyup dokunmaz.
10. 10) Electron `flag-modules.ts` FLAG_MODULE tablosuna satırın modül AİDİYETİNİ yaz (`cekirdek` | modül anahtarı | `planlanan:*`). <sub>(Electron/src/pages/GeneralSettings/flag-modules.ts:84 `export const FLAG_MODULE: Readonly<Record<FlagRowKey | SystemSett)</sub> — atlanırsa: SESSİZ DEĞİL — Record tamlığı derlemede zorlanır (tsc TS2741, eksik anahtarı adıyla söyler). Yanlış sahip yazılırsa satır kapalı modülde gizlenir/gizlenmez (gör
11. 11) Tablet de okuyacaksa mobil aynayı güncelle: `FeatureFlags` alanı + `DEFAULT_FEATURE_FLAGS` içinde fail-open/fail-closed YÖNÜ + `useFeatureFlags` seçicisi. <sub>(mobil/src/services/featureFlag.service.ts:19 (interface), :119 DEFAULT_FEATURE_FLAGS (yön gerekçeleri :125-131, :141-147)</sub> — atlanırsa: Tablet bayrağı hiç görmez, eski davranışta kalır ve hiçbir hata çıkmaz. MEKANİK BEKÇİ YOK: test_feature_flag_contract yalnız backend src + Electron iki dosyayı 
12. 12) ENUM bayrağıysa üçlü kalıbı kur: `type X` + `X[]` değer dizisi + `DEFAULT_X` sabiti; okuyucu `includes` sigortasıyla çöp değeri varsayılana düşürsün. <sub>(Teks-Erp/src/services/system-setting.service.ts:652-697 (kalıp şerhi + SHIPMENT_ORDER_REQUIREMENTS/SHIPPING_INVOICE_MODE)</sub> — atlanırsa: Dizi/`includes` sigortası olmadan DB'ye elle yazılmış geçersiz bir metin davranışı değiştirir — "yeni bayrağın varsayılanı BUGÜNKÜ davranıştır" vaadinin tek mek
13. 13) SAYISAL ayarda "alanı temizle" semantiğine KARAR ver: `set()` null kabul etmez → ya 0 yazıp okuma `<=0 → null` çözer, ya da null'da FABRİKA VARSAYILANINA dönülür. <sub>(Teks-Erp/src/services/system-setting.service.ts:2504-2523 (fasonShrinkTolerancePct: null → DEFAULT, `0` geçerli) ve okuy)</sub> — atlanırsa: Kısa-kesim eşiğinin `null→0→kural inert` kalıbı toleransa kopyalanırsa boş bırakılan eşik "sıfır tolerans"a döner ve HER kabulde uyarı basar — sessizlik bekleye
14. 14) MODÜL anahtarıysa ek beş yer: `constants/module-flags.ts` (MODULE_FLAG_KEYS:33 · MODULE_SETTING_KEYS:44 · MODULE_DEPENDENCIES:67 · MODULE_LABELS:73), `constants/module-profiles.ts` (MODULE_FIELD_BY_SETTING_KEY:62 · MODULE_DESCRIPTIONS:84 · beş profilin `moduller`:123), adlandırılmış middleware, grandfathering migration. <sub>(Teks-Erp/src/constants/module-flags.ts:33/44/67/73; module-profiles.ts:62/84/123; Teks-Erp/src/middlewares/module.middle)</sub> — atlanırsa: MODULE_FLAG_KEYS'e yazılmazsa `flagWriteGuard`ın süperadmin dalı anahtarı tanımaz (routes/feature-flag.routes.ts:75) → `admin:settings` taşıyan fabrika yönetici
15. 15) Modül ALTI davranış bayrağıysa §3.6 tek resolver'ı yaz: `resolveXxx = modülAçık && bayrak`; enforcement noktaları HAM okuyucuyu çağırmasın (getFeatureFlags ham kalır). <sub>(Teks-Erp/src/services/system-setting.service.ts:3852-3900 (§3.6 başlığı + resolvePurchaseBlockOverReceiptEnabled); ebeve)</sub> — atlanırsa: Modül KAPALIYKEN alt bayrağın kuralı koşmaya devam eder — ölçüldü: üretim kapalıyken kk1 bayrağı /api/rolls'tan hâlâ ateşliyordu (:3855-3859).
16. 16) Feature-flag yükünde DÖNMEYECEK ham `system_settings` ayarıysa: bekçinin §11 muaf tablosuna GEREKÇE + KANIT (dosya + aranacak metin) satırı; panel yüzeyi `settingFields` ve Electron `systemSettingService.SETTING_KEYS` aynası. <sub>(Teks-Erp/scripts/test_feature_flag_contract.ts:505-544 (NON_FLAG_SETTING_SURFACE); Electron/src/services/systemSettingSe)</sub> — atlanırsa: Davranış değiştiren bir ayar panelde HİÇ yüzeyi olmadan yaşar ve tüm bekçiler yeşil kalır (2026-08-15'te shipping.toleranceMeters tam bu durumdaydı).
17. 17) Fabrika varsayılanı koddakinden FARKLI olacaksa seed satırı — ama profil job'uyla İKİ YAZAR olmasın (davranış bayrakları profile GİRMEZ). <sub>(Teks-Erp/prisma/seed.ts:376-437 SYSTEM_SETTINGS + :433 `upsert` (update: { value } EZER); iki-yazar gerekçesi Teks-Erp/s)</sub> — atlanırsa: Aynı anahtarın iki yazarı olursa hangisinin kazandığı KOŞUM SIRASINA kalır (seed elle, profil job'u boot'ta) — kurulumda bayrak beklenmedik değerle doğar.
18. 18) `npx tsx scripts/test_feature_flag_contract.ts` bekçisini koş (A=API · B=Zod şeması · C=servis yazma dalları · D=panel kümeleri; §1-§16). <sub>(Teks-Erp/scripts/test_feature_flag_contract.ts:1-1518; kümeler :166-219)</sub> — atlanırsa: Adım 5/6/8/9 ihlalleri yakalanmaz; adım 4 ve 11 ihlalleri bekçi koşsa DA yakalanmaz (kör noktalar).

⚠️ Belgesiz kalan adımlar (koddan çıkarıldı, hiçbir .md'de yoktu):

- "DÖRT KAPI" ADI BAYAT ve kodda ÜÇ FARKLI içerikle geçiyor: routes/feature-flag.routes.ts:604 ve system-setting.service.ts:617 "SETTING_KEYS + sanitize + Zod + Electron aynası" der, system-setting.service.ts:91 "servis · route şeması · Electron arayüzü · panel satırı" der; koddan çıkan gerçek adım sayısı 8 (skaler bayrak) - 16 (modül anahtarı). Üstelik skaler bir bayrakta `sanitize` adımı HİÇ YOKTUR (sanitize yalnız documentsConfig/travelerCardConfig gibi JSON ayarlarındadır) — dört-kapı formülü 2026-08-05 BELGE AYARI vakasından genelleştirilmiş, skaler bayrağa yanlış uyuyor.
- `getFeatureFlags` içinde okuyucuya `cacheClient` argümanının geçilmesi zorunluluğu hiçbir .md'de yazılı değil (yalnız kodda, system-setting.service.ts:1637-1638 ve tarihli inceleme notu docs/history/BACKEND-CODE-REVIEW.md:372).
- Panelin `defaultOn`/`defaultValue` beyanının backend okuyucusuyla BİREBİR olma zorunluluğu HİÇBİR .md'de geçmiyor (grep: `defaultOn`/`defaultValue` yalnız audit ve tarihli inceleme dosyalarında). Bu bir ikinci-kaynak riski ve bekçi §12/§16 onu ölçüyor.
- `PANEL_EXEMPT` / `NUMERIC_PANEL_EXEMPT` / ENUM muafı yazma seçeneği ve "muaf iki yönlü denetlenir" kuralı yalnız MODUL-BAYRAK-UYGULAMA-PLANI.md:221'de bir kez, modül yer tutucusu bağlamında geçiyor — genel reçete olarak yazılı değil.
- §11 `NON_FLAG_SETTING_SURFACE` sözleşmesi (feature-flag yükünde dönmeyen her SETTING_KEYS satırı gerekçe + KANIT ile muaf olmalı) hiçbir .md'de yok; yalnız bekçi kaynağında (scripts/test_feature_flag_contract.ts:505-544).
- MOBİL AYNA adımı hiçbir kural belgesinde yok — yalnız iki tarihli notta örnek olarak geçiyor (CLAUDE-NOT-ARSIVI.md:167 ve :407). mobil/CLAUDE.md'de bayrak ekleme kuralı YOK (grep: 'bayrak' yalnız android manifest ve tambur eşiği bağlamında).
- Electron tarafının kendi CLAUDE.md'sinde bayrak satırı ekleme reçetesi YOK: `settings-config.ts`, `FlagDef`, `flag-modules.ts`, `defaultOn` kelimelerinin hiçbiri Electron/CLAUDE.md'de geçmiyor.
- `flag-modules.ts` (FLAG_MODULE aidiyeti, 2026-09-04) adımı hiçbir SIRALI reçetede yok; MODUL-BAYRAK-UYGULAMA-PLANI.md:83'teki sekiz adımlı liste ondan ESKİ ve bu adımı içermiyor (liste `settings-config.ts` ile bitiyor).
- ENUM bayrağının üçlü kalıbı (type + değer dizisi + DEFAULT sabiti + okuyucuda `includes` sigortası) yalnız kod yorumunda (system-setting.service.ts:652-662); .md tarafında sadece `enumKey:` alan adı kuralı var (GECE-KARARLARI-2026-09-03.md:89).
- Seed satırı ↔ profil job'u "iki yazar olmaz" kuralı yalnız module-profiles.ts:17-22 kod yorumunda; hiçbir .md'de yok.
- §3.6 tek-resolver TASARIM KARARI olarak yazılı (MODUL-BAYRAK-TASARIM.md:86 ve :289) ama "yeni alt bayrak eklerken system-setting.service.ts §3.6 bloğuna resolver yaz ve enforcement noktasını ondan besle" şeklinde bir REÇETE adımı hiçbir yerde yok.
- Modül anahtarı eklerken gereken grandfathering migration adımı ilke olarak yazılı (MODUL-BAYRAK-TASARIM.md:290-295, karar 10 ve prova kuralı 2) ama sıralı reçetede (UYGULAMA-PLANI:83) yok.

## Prisma enum'una yeni değer

Mekanik kapı: KISMİ — tek gerçek bekçi `Teks-Erp/scripts/test_audit_labels.ts` §4 (:174-201): schema.prisma'daki HER enum değerinin Electron `ENUM_LABELS` karşılığını arar (körlük zemini `enumValues.size >= 120`). Yalnızca 9. adımı korur. İkinci, dar bekçi `Electron/src/test/login-totp.test.ts:110-118` — ClientTy

1. 1) schema.prisma'daki enum bloğuna değeri ekle (yorum satırıyla birlikte; sıra backend enum sırasıyla korunur ki istemci aynaları gözle karşılaştırılabilsin). <sub>(Teks-Erp/prisma/schema.prisma:135-153 (enum ReasonPresetKind, WORK_ORDER_REWORK :149) ve :162-179 (enum RollStatus 13 de)</sub> — atlanırsa: Atlanamaz — Prisma client tipi doğmaz, backend derlenmez. Tek sessiz varyantı: değeri DB'de elle açıp şemaya yazmamak (drift).
2. 2) AYRI ve TEK İFADELİ bir migration dosyası yaz: `ALTER TYPE "<Enum>" ADD VALUE IF NOT EXISTS '<DEGER>';`. Değeri KULLANAN hiçbir ifade (INSERT/UPDATE/DEFAULT) aynı dosyaya konmaz. <sub>(Teks-Erp/prisma/migrations/20260826130000_reason_preset_order_cancel_kind/migration.sql:12-15 (55P04 gerekçesi + tek ifa)</sub> — atlanırsa: Aynı dosyada kullanılırsa PG 55P04 ile migration düşer. `IF NOT EXISTS` yazılmazsa (repoda 10 dosyada yok) defter-dışı açılmış bir değer sonraki `migrate deploy
3. 3) Backend `Record<Enum, …>` sözlüklerini doldur (ROLL_STATUS_TR; ReasonPresetKind için KIND_STORES_TEXT + KIND_LABELS + REASON_PRESET_CATALOG üçlüsü). <sub>(Teks-Erp/src/constants/status-labels.ts:27 ROLL_STATUS_TR; Teks-Erp/src/constants/reason-presets.ts:67 (satisfies Record)</sub> — atlanırsa: Bu adım MEKANİK: tam `Record<PrismaEnum, …>` yazımı derlemeyi kırar (status-labels.ts:20-23 'tip LOAD-BEARING' notu). Ama `Partial<Record<…>>` kullanılan yerde 
4. 4) Enum üzerinde switch/case yazan yolları gez ve `default` dalına düşmemesini sağla (özellikle top geçmişi başlığı). <sub>(Teks-Erp/src/services/inventory.service.ts:2826-2848 — `entryTitle` switch'i, yorumda 'iki kez ısırdı: SUBCONTRACTOR_RET)</sub> — atlanırsa: default dalı SESSİZ YALAN üretir: dışarıdan alınan yarı mamul topun geçmişinde aylarca 'Ham Giriş' yazdı (inventory.service.ts:2828-2831 kendi yorumu).
5. 5) Backend statü KÜMELERİNE üyeliği karar ver (K18_DEAD_STATUSES, NO_LIVE_MATERIAL_STATUSES ve türevleri) — bunlar tek kaynak diye export edilir ama içerik elle yazılır. <sub>(Teks-Erp/src/services/batch.service.ts:55-60 K18_DEAD_STATUSES, :75-79 NO_LIVE_MATERIAL_STATUSES (yorumda KARTELA_CONSUM)</sub> — atlanırsa: Yeni statü hiçbir kümede yer almaz → 'canlı/ölü' sorusuna eski cevap verilir. Emsal aynı dosyada: KARTELA_CONSUMED satır-içi listeden eksikti, son topu kartelay
6. 6) Uçlardaki Zod şemalarını genişlet — repoda çoğu şema literal `z.enum([...])` kullanır, `z.nativeEnum` yalnız 4 dosyada var. <sub>(git grep -c 'z.nativeEnum' -- Teks-Erp/src → yalnız 4 dosya (reason-preset.routes.ts:35 dahil); literal örnekler workord)</sub> — atlanırsa: Yeni değer uçta 400 ile reddedilir — görünür ama uzak: bir yolda çalışır, diğerinde 'geçersiz değer' der; dispatchWithoutColor vakasının (dört Zod bloğu) aynı s
7. 7) Electron enum AYNASINI (elle yazılmış const object) güncelle: Electron/src/types/enums.ts. <sub>(Electron/src/types/enums.ts:150-165 RollStatus, :204-211 RollEntrySource, :52-60 StationKind (yorum: 'SHIPPING 2026-09-0)</sub> — atlanırsa: Derleyici SUSAR (ayna bağımsız bir const'tır). StationKind.SHIPPING eksikken panel `stationKindLabels` üzerinden undefined bastı ve form zod'u istasyonu reddett
8. 8) Electron etiket haritalarını doldur: aynı dosyadaki `rollStatusLabels` / `rollEntrySourceLabels` / `stationKindLabels`. <sub>(Electron/src/types/enums.ts:167-181 rollStatusLabels, :219-229 rollEntrySourceLabels, :62-69 stationKindLabels)</sub> — atlanırsa: Ayna (7) güncellendiyse tsc kırar; ayna da unutulduysa hem tip hem etiket eksik kalır ve hata hiç doğmaz.
9. 9) Electron audit sözlüğüne (ENUM_LABELS) Türkçe karşılığı ekle — audit ekranı ham İngilizce basmasın. <sub>(Electron/src/lib/audit-labels.ts:264 `export const ENUM_LABELS: Record<string, string>`, :417 SEMI_FINISHED girdisi; :65)</sub> — atlanırsa: Tek MEKANİK korunan adım: Teks-Erp/scripts/test_audit_labels.ts:184-201 schema.prisma'daki her enum değerini ENUM_LABELS'te arar ve kırmızı verir. Bekçi olmasa 
10. 10) Electron alan-listelerini güncelle: STATUS_GROUPS/ARCHIVE, sekme kataloğu, satır 'Tip' türetimi, Kanban kolonları, ReasonPresets sekme+sözlük+union. <sub>(Electron/src/pages/Operations/Rolls/service.ts:36-58 (ARCHIVE :56-57); columns.tsx:60-68; RollsKanban.tsx:37; ReasonPres)</sub> — atlanırsa: Statü hiçbir sekmede görünmez: 231 CANCELLED + 1 SCRAP kayıt hiçbir yüzeyde yoktu (service.ts:50-55 yorumu). 'Tip' kolonu yarı mamulü 'Bitmiş' gösterdi (columns
11. 11) Mobil union'ları ve zeminleri güncelle: types/models.ts RollStatus, services/reasonPreset.service.ts ReasonPresetKind, hooks/useReasonPresets.ts BUILTIN, utils/labels.ts etiket haritası. <sub>(mobil/src/types/models.ts:41-74 ('derleyici aynanın eksik olduğunu SÖYLEMEZ'); reasonPreset.service.ts:15-21; useReasonP)</sub> — atlanırsa: Eksik değer TS için 'imkânsız' olur → `roll.status === 'AT_KARTELA'` TS2367 verir, yani o dal YAZILAMAZ (models.ts:50-58). Bugün ölçülen canlı drift: mobil Reas
12. 12) Rapor/karne kovalarını ayır: stok karnesi, patron özeti, ürün dengesi, sipariş karşılama gibi enum değerinden türeyen sayaçlar. <sub>(Teks-Erp/src/services/reports/stock-scorecard.report.service.ts:90,308 semiQty; boss/overview.service.ts:46,178; product)</sub> — atlanırsa: Rakam ayrışır ve kimse hata görmez: 'ekran 800 / rapor 950' çelişkisi (kök CLAUDE.md 2026-08-26 ④); Kanban=48 ↔ Envanter=47.
13. 13) minVersion tetiğini AÇIKÇA cevapla: sahadaki eski istemci bu değeri tanımıyor mu; tanımıyorsa client-version-policy güncellenir ve önce istemci yayınlanır. <sub>(Teks-Erp/CLAUDE.md:41 '| Enum'a değer eklendi | Eski istemci onu tanımaz (bu repoda beş kez yaşandı) |'; :49-51 sıra kur)</sub> — atlanırsa: Eski APK/panel değeri tanımaz, ekran boş/yanlış basar; ters sırada minVersion yükseltilirse her makine kapıda kalır.

⚠️ Belgesiz kalan adımlar (koddan çıkarıldı, hiçbir .md'de yoktu):

- TEK BİR YERDE TAM LİSTE YOK: 13 adımın hiçbir belgede uçtan uca sayıldığı bir yer yok; en yakınları LabelKind için '4 yer' (CLAUDE.md:43) ve ReasonPresetKind için 'beş kapı' (CLAUDE.md:85) — ikisi de ENUM'A ÖZEL, genel prosedür değil.
- `ALTER TYPE ... ADD VALUE` kuralı (ayrı tek-ifadeli dosya + `IF NOT EXISTS` + PG 55P04) hiçbir CLAUDE.md'de, MIGRATION-DEPLOY.md'de, docs/ops veya deploy/ altında YAZILI DEĞİL — yalnız migration dosyalarının kendi başlık yorumlarında (6 dosya) ve audit raporlarında geçiyor.
- `IF NOT EXISTS` zorunluluğu bir kural olarak hiç yazılmadı: audit D-J-07 kabul kriteri 'CLAUDE.md'de yazılı olsun' diyor, bugün grep ile bakıldı — CLAUDE.md'lerde YOK; repoda 10 ADD VALUE ifadesi IF NOT EXISTS'siz.
- Electron/CLAUDE.md'de 'enum' kelimesi HİÇ geçmiyor (grep -ci enum → 0) — oysa Electron elle yazılmış ÜÇ ayrı ayna katmanı taşıyor (types/enums.ts, lib/audit-labels.ts ENUM_LABELS, sayfa-içi union'lar).
- mobil/CLAUDE.md'de tek geçiş dizin açıklaması ('constants/ … enum eşlemeleri', :176); mobil'in DÖRT elle senkron noktası (models.ts union, reasonPreset.service union, useReasonPresets BUILTIN zemin, utils/labels.ts) hiçbir yerde listelenmiyor.
- Rapor/karne kovası adımı (stok karnesi semiQty, boss overview, üretim dengesi, Kanban kolonu) hiçbir prosedür belgesinde yok; yalnız 2026-08-26/27 anlatılarının içinde vaka olarak geçiyor.
- Ölçüm gösteriyor ki adım aynı commit'te bile bitmiyor: ClientType.WEB backend'e 804dddb6'da girdi (schema + migration + auth.service + auth.controller), Electron aynası ve bekçisi AYRI commit 4a087898'de geldi — arada backend'in üretebildiği bir değeri panel tanımıyordu.
- Mobil ReasonPresetKind union'ı CANLI DRIFT taşıyor: backend enum'unda ORDER_CANCEL var (schema.prisma:145), mobil union'da yok (reasonPreset.service.ts:15-21) — 'beş kapı' kuralı ORDER_CANCEL turunda uygulanmadı ve hiçbir bekçi kırmızı vermedi.
- Enum değeri eklemenin GERİ ALINAMAZ olduğu (PG değer düşürmeyi desteklemez) yalnız migration yorumunda ve docs/ops/SURUM-2026-07-30-DEPLOY.md:260'ta geçiyor; 'yeni enum değeri yerine startedAt/finishedAt tercih et' önerisi audit içinde kaldı.

## Yeni Prisma modeli

Mekanik kapı: KISMİ ve DAR — 14 DB kuralının yalnız 4'ünde kapı var: `Teks-Erp/scripts/test_timestamptz_contract.ts` (Timestamptz, iki yönlü muaf listesiyle) · `Teks-Erp/scripts/test_schema_drift.ts` (şema ↔ DB) · `Teks-Erp/scripts/test_db_invariants.ts` (dokuz şema-dışı nesne listesi, İKİ YÖNLÜ) · `Teks-Erp/scripts/test_migration_hygiene.ts` (+ commit hijyeni `scripts/check-migrations.mjs`). Kapısı OLMAYANLAR: id kalıbı, `@@map`, model sınıfı, künye FK muafiyeti, composite index sırası, `///` gerekçesi — hepsi geçerli Prisma'dır ve sessizce yanlış doğar. Standart metni: `docs/standart/VERITABANI.md` (§1 şablon · §2 üç model sınıfı · §3 künye FK'sı · §7 migration).

1. Model SINIFINI seç — KATALOG (master-data) · DEFTER (append-only) · PİVOT (bağ). Sınıf zaman damgasını, künyeyi ve alan kümesini belirler; şablon `docs/standart/VERITABANI.md` §1'deki `SackTag` bloğudur. <sub>(docs/standart/VERITABANI.md §2 tablosu; emsaller `prisma/schema.prisma:4580` katalog · `:3249` defter · `:4616` pivot)</sub> — atlanırsa: Defter modeline `updatedAt` konur ve satırın güncellenmezlik beyanı kaybolur; katalog `isActive` almazsa silme hard delete'e kayar.
2. PK `id String @id @default(uuid()) @db.Uuid`; pivot/agregat tabloda `@@id([a, b])`. <sub>(116/117 model · docs/standart/VERITABANI.md [DB-01])</sub> — atlanırsa: Autoincrement/`String` PK geçerli Prisma'dır; drift bekçisi kolonun TİPİNİ karşılaştırır, kalıbı değil — sapma ancak ilk çoklu-kurulum birleştirmesinde görülür.
3. `@@map("snake_case_çoğul")` ZORUNLU; kolon düzeyinde `@map` YAZMA. <sub>(124/124 model · [DB-02]; tek devralınan sapma `endpoint_latency_daily` tekil ad)</sub> — atlanırsa: Tablo PascalCase doğar, tsc ve drift bekçisi susar; ham SQL yazan her rapor/migration o tabloyu tırnaklamak zorunda kalır.
4. Her `DateTime` `@db.Timestamptz` · her UUID kolon `@db.Uuid` · her metin `@db.VarChar(n)`. Bir alan TAKVİM GÜNÜ anahtarı olduğu için `@db.Date` olacaksa AYNI COMMIT'te bekçinin muaf listesine yazılır. <sub>(Teks-Erp/scripts/test_timestamptz_contract.ts (iki yönlü) · 311/315 alan · [DB-03]/[DB-05])</sub> — atlanırsa: `timestamp without time zone` sessizce doğar ve fabrika günü (Europe/Istanbul) hesabı gece yarısı civarında bir gün kayar; ters yönde bayat muaf da kırmızı verir.
5. Zaman damgasını model sınıfından al: mutasyona uğrayan model `createdAt` + `updatedAt`; append-only defter ve M:N pivot YALNIZ `createdAt`. Niyet model başlığında `///` ile YAZILI olur. <sub>([DB-04]/[DB-09]; devralınan 10 model `updatedAt`'siz ama kodda update ediliyor — docs/standart/VERITABANI.md §12)</sub> — atlanırsa: `updatedAt`'siz modelde "en son ne zaman değişti" sorusu cevapsız kalır ve giriş listelerinin `updatedAt desc` sözleşmesi o modelde kurulamaz.
6. Künye FK'sına (`createdById` · `updatedById` · `userId` ve kardeşleri) `@@index` YAZMA; DOMAIN FK'sına `@@index([fk])` ZORUNLU — composite unique/index'in ÖN EKİ index sayılır, dördüncü ağaç açma. <sub>(91 künye emsali · 272/368 FK kapsamda · [DB-11]/[DB-12])</sub> — atlanırsa: Künye FK'sına index atılırsa 91 emsale aykırı gereksiz ağaç yazma maliyetine biner; domain FK'sı index'siz kalırsa liste sorgusu seq scan'e düşer ve yavaşlık ancak sahada görülür.
7. Composite index sırası: EŞİTLİK kolonları önce, range/sıralama kolonu sonra. <sub>(106 composite'in 0'ı tarih kolonuyla BAŞLIYOR, 68'i onunla bitiyor · [DB-13])</sub> — atlanırsa: Ters sıralı index planlayıcı tarafından kullanılmaz; index VAR görünür, sorgu yine tarar.
8. Değer kümesi büyümeye açıksa pg enum DEĞİL `String @db.VarChar(n)` kullan — `ALTER TYPE` geri alınamaz. Enum yalnız KAPALI küme içindir, adı PascalCase ve değerleri SCREAMING_SNAKE olur. <sub>(`prisma/schema.prisma:3260-3264` gerekçesi · [DB-15]/[DB-16])</sub> — atlanırsa: Kapalı sandığın küme altıncı değeri isteyince migration geri alınamaz bir tip değişikliğine dönüşür ve tüm istemcilerin enum aynası kırılır.
9. Uç idempotent olacaksa `clientToken String? @unique @db.Uuid` (BİREBİR bu yazım) ekle ve replay sözleşmesini kur. <sub>(15 model, 15/15 aynı yazım · [DB-06] · docs/standart/ESZAMANLILIK.md § istemci tarafı)</sub> — atlanırsa: Tabletin ağ kopmasında yeniden gönderdiği istek İKİNCİ kaydı yaratır; mükerrer top/sevk kaydı ancak raporda fark edilir.
10. SIRA DIŞI her kararı satırın ÜSTÜNDE `///` ile gerekçele: FK yok · index yok · enum yerine `VarChar` · `onDelete` · nullable seçimi · `@db.Date` · `@@map` sapması. <sub>(87/124 model başlığında `///`; şemanın %43,6'sı yorum · [DB-18]/[DB-19])</sub> — atlanırsa: Bir sonraki geliştirici "eksik" sanıp index/FK ekler; sıra dışı karar sessizce geri alınır.
11. Migration'ı `npx prisma migrate dev --create-only` ile üret, çıktıdaki `DropForeignKey` satırlarını SİL, SQL'i ELLE ve İDEMPOTENT yaz (`IF NOT EXISTS`/`IF EXISTS`); dosyayı ne yaptığını ve ADDITIVE olup olmadığını söyleyen bir blokla aç. Tam prosedür § Yeni migration ve canlıya çıkarma. <sub>(Teks-Erp/scripts/apply-migration.ts:1-23 · [DB-21]/[DB-22]/[DB-23]; düz `migrate dev` hook'ta engellidir)</sub> — atlanırsa: Düz `migrate dev` iki DEFERRABLE composite FK'yı DÜŞÜRÜR; `apply-migration.ts` defterde satır varsa `resolve`'u atlar ama SQL'i YİNE koşar — idempotent olmayan SQL ikinci koşumda patlar.
12. Canlı veriye tekillik/sed ekliyorsan YUMUŞAK KAPI kur (`DO $etiket$` → mükerrer varsa `RAISE NOTICE` + ATLA) ve `CREATE INDEX CONCURRENTLY` YAZMA (Prisma migration'ı tek transaction içinde koşar). <sub>([DB-24]/[DB-26]; emsal `20260821150000_name_fold_unique_live`)</sub> — atlanırsa: Sert `RAISE EXCEPTION` fabrikanın deploy'unu kilitler ve geri alma yolu yedekten restore'dur.
13. Şema-dışı nesne kurduysan (partial index · CHECK · DEFERRABLE FK · ifade-UNIQUE · trigger · extension · fonksiyon · collation · istatistik) AYNI COMMIT'te `Teks-Erp/scripts/test_db_invariants.ts` içindeki DOKUZ listeden ilgilisine satırını yaz. <sub>([DB-30]; kapı İKİ YÖNLÜ — envanterde olup DB'de olmayan da kırmızı)</sub> — atlanırsa: Bekçi kırmızı verir ve doğru cevabın "nesneyi sil" olduğu sanılır; nesne kaybolur, koruduğu değişmez sessizce açılır.
14. Model 120 satırı, migration 200 satırı geçiyorsa "bu iki tablo/iki iş mi" sorusunu sor. <sub>(docs/standart/ILKELER.md §6 boyut tablosu)</sub> — atlanırsa: Tek migration'da iki iş birleşince biri geri alınamaz olur; geri alma yolu tek tek değil dosya bütünüdür.
15. Kapı: `npm run typecheck` + `npx tsx scripts/run-all-tests.ts test_schema_drift` · `test_db_invariants` · `test_timestamptz_contract` · `test_migration_hygiene`; commit öncesi `node scripts/check-migrations.mjs` (untracked migration/test yakalar — değeri YERELDEDİR). <sub>(Teks-Erp/scripts/run-all-tests.ts · Teks-Erp/scripts/test_migration_hygiene.ts)</sub> — atlanırsa: Commit edilmemiş migration prod'da SESSİZ EKSİKTİR: bu makinede çalışır, temiz klonda tablo yoktur.

## Yeni migration ve canlıya çıkarma

Mekanik kapı: VAR (dört ayrı bekçi, farklı sorular): scripts/check-migrations.mjs (GATE 1-5 · git tarafı) · Teks-Erp/scripts/test_migration_hygiene.ts (defter ↔ dizin) · Teks-Erp/scripts/test_schema_drift.ts (datamodel ↔ canlı DB) · Teks-Erp/scripts/test_db_invariants.ts (şema-dışı nesne envanteri, iki yönlü). Sı

Standart metni ve kural kimlikleri: `docs/standart/VERITABANI.md` §7 (migration yazımı, [DB-20]…[DB-29]) · §8 (şema-dışı nesne envanteri). ÇEKİRDEK on üç adım her migration'da koşar; altındaki "koşula bağlı ek adımlar" yalnız tetiği varsa.

1. Şemayı düzenle, migration SQL'ini ELLE yaz: `prisma migrate dev` bu şemada uygulanamaz, `--create-only` ile üret ve çıktıdaki `DropForeignKey` satırlarını SİL; dosyayı ne yaptığını ve ADDITIVE olup olmadığını söyleyen bir blokla aç. <sub>(Teks-Erp/prisma/schema.prisma:4550-4551 "DRIFT: migrate dev bu 2 FK'yı DROP etmek İSTER" · [DB-21]/[DB-22]; düz `migrate dev` hook'ta engelli)</sub> — atlanırsa: rolls/swatches `_sackId_shipmentId_consistency_fkey` DEFERRABLE composite FK'ları düşer; top↔çuval↔sevkiyat tutarsız satırı DB seddi olmadan yazılabilir.
2. Dizin `prisma/migrations/<14 hane damga>_<snake_slug>/migration.sql` olmalı — her dizin migration.sql taşımak zorunda. <sub>(scripts/check-migrations.mjs:115-125 (GATE 3, `emptyDirs`) · [DB-20], 232/232 dosya)</sub> — atlanırsa: Prisma dizini pending sayar ama uygulayacak SQL bulamaz; deploy adımı hiçbir şey yapmadan geçer.
3. SQL'i İDEMPOTENT yaz (`IF NOT EXISTS` / `IF EXISTS`). <sub>([DB-23]; `apply-migration.ts:80-85,102-107` defterde satır varsa `resolve`'u atlar ama SQL'i YİNE koşar)</sub> — atlanırsa: İkinci koşumda migration patlar; oysa ikinci koşum normal yoldur (dev'de yarım kalan uygulama, kopya DB, prova restore'u).
4. SQL'i DB'ye uygulamadan ÖNCE `git add` — dosya izleniyor olmalı. <sub>(Teks-Erp/scripts/apply-migration.ts:70-79 (`git ls-files --error-unmatch` kapısı); scripts/check-migrations.mjs:76,90)</sub> — atlanırsa: `migrate deploy` yalnız commit'li dosyaları uygular → prod'da kolon/enum hiç oluşmaz, deploy 'başarılı' der, kolonu okuyan her yol P2022/500 verir (2026-07-30 vakası).
5. Uygulama TEK komuttur: `npx tsx scripts/apply-migration.ts <ad> --apply` (argümansız hâli dry-run). Script git kapısını, `psql -v ON_ERROR_STOP=1`i, `migrate resolve`u ve ardından drift + hijyen doğrulamasını sırayla koşar. ⚠️ Ön koşul: PATH'te `psql` bulunmalı ve `.env`deki satır ÇİFT TIRNAKLI olmalı (`DATABASE_URL="…"`, `apply-migration.ts:41-49`) — tırnaksızsa script '.env içinde DATABASE_URL bulunamadı' der. <sub>(Teks-Erp/scripts/apply-migration.ts:1-23,80-127 · [DB-28])</sub> — atlanırsa: `ON_ERROR_STOP` olmadan psql hatalı ifadeyi atlayıp exit 0 döner; `resolve` SQL'e HİÇ bakmaz → defter dolu + nesne yok.
6. Uygulanmış migration dosyasına bir daha DOKUNMA; düzeltme yeni migration ile yapılır. <sub>(scripts/check-migrations.mjs:86,102-110 (GATE 2 — `_prisma_migrations` checksum))</sub> — atlanırsa: Deploy checksum hatasıyla KİLİTLENİR (kur.ps1 [7/9]'da, geri alınamaz eşikte).
7. Migration şema-DIŞI nesne kuruyorsa AYNI COMMIT'te `test_db_invariants.ts` envanterine yaz: PARTIAL_INDEXES · CHECK_CONSTRAINTS · DEFERRABLE_FKS · EXT_STATS · EXPRESSION_UNIQUES · TRIGGERS · REQUIRED_EXTENSIONS · EXPECTED_FUNCTIONS · EXPECTED_COLLATIONS. <sub>(Teks-Erp/scripts/test_db_invariants.ts:100,317,444,457,469,501,544,554,576 · [DB-30])</sub> — atlanırsa: Kapı İKİ YÖNLÜ: envanter-dışı nesne testi DÜŞÜRÜR ve doğru cevabın "nesneyi sil" olduğu sanılır; yazılmazsa da nesne ileride sessizce kaybolduğunda kimse görmez.
8. Yeni yazılan `scripts/test_*.ts` bekçisi de commit edilmeli. <sub>(scripts/check-migrations.mjs:129-145 (GATE 4))</sub> — atlanırsa: CI'ın gerçek şema kapısı (boş DB + migrate deploy + npm test → P2022) kaybolur; hata prod'da bulunur.
9. Kapı: `npx tsx scripts/run-all-tests.ts test_schema_drift` · `test_db_invariants` · `test_timestamptz_contract` · `test_migration_hygiene`; commit öncesi `node scripts/check-migrations.mjs`. CI aynı ikisini koşar (`.github/workflows/ci.yml:46` docs job, `:92` backend job `migrate deploy`). <sub>(Teks-Erp/scripts/run-all-tests.ts · Teks-Erp/scripts/test_migration_hygiene.ts)</sub> — atlanırsa: Commit edilmemiş migration prod'da SESSİZ EKSİKTİR: bu makinede çalışır, temiz klonda tablo yoktur.
10. Şema provası EN ESKİ CANLI DUMP üzerinde koşulur (restore → `migrate deploy` → bekçiler → profil boot). Sorulan şey **uygulanıyor mu**: sed/unique/CHECK gerçek veriye takılabilir ([DB-29a]; `name_fold_unique_live` mükerrer veri yüzünden yumuşak kapıya çevrildi). <sub>Süre ölçümü ([DB-29b]) yalnız yüksek hacimli tabloya DDL yazan migration için ve ölçtüğü şey KURULUM PENCERESİNİN uzamasıdır: migration `kur.ps1`'de pm2 DURDURULDUKTAN sonra ([4/9] → [7/9]) koşar, kilit kimseyi bloklamaz.</sub> — atlanırsa: migration geri alınamaz eşikte, uygulama kapalıyken düşer ([DB-29c]).
11. Paketleme (`deploy/paketle.ps1`): `prisma/schema.prisma` + `prisma/migrations` pakete kopyalanır ve sayı `PAKET.json.migrationSayisi`ne yazılır (seed*.ts pakete GİRMEZ); paketleme `PRISMA_CLI_BINARY_TARGETS=windows` ile koşar, `schema-engine-windows.exe` varlığı + MZ imzası doğrulanır, yabancı platform motoru atılır. <sub>(deploy/paketle.ps1:189-194, :321, :21-22, :245-276)</sub> — atlanırsa: migrations dizini pakette yoksa `migrate deploy` hiçbir şey uygulamaz; şema motoru yoksa migration Windows'ta düşer — fabrika ZATEN KAPALIYKEN, geri alınamaz eşikte.
12. `kur.ps1` üç kapısı: [1/9] motor kapısını EŞİKTEN ÖNCE tekrarlar · [3/9] `premigrate_<damga>.dump` alınır ve `pg_restore --list` ile DOĞRULANIR (alınamazsa kurulum İPTAL) · [7/9] migration `node node_modules\prisma\build\index.js migrate deploy` ile koşar, `npx` KULLANILMAZ. <sub>(deploy/kur.ps1:260-285, :362-378, :535-543)</sub> — atlanırsa: Arıza [7/9]'a kayar (pm2 durmuş, eski kurulum kenara alınmış); yedek yoksa tek geri dönüş yolu yok olur; npx CLI'yi `.bin` sembolik bağı üzerinden çözemez.
13. Migration başarısız olursa OTOMATİK GERİ ALMA YOKTUR: script durur, `migrate status` / `-GeriAl` / `pg_restore` komutlarını basar, karar insanındır. <sub>(deploy/kur.ps1:541-553 ("DB kismi degismis OLABILIR. Otomatik geri alinmiyor - karar senin."))</sub> — atlanırsa: Otomatik geri alma varsayılırsa yarım göç etmiş DB üstüne yeni kurulum denenir.

### Koşula bağlı ek adımlar (yalnız tetiği varsa)

| Tetik | Ek adım |
|---|---|
| Canlı veriye tekillik/sed ekliyorsun | YUMUŞAK KAPI: `DO $etiket$` bloğunda mükerrer say → varsa `RAISE NOTICE` + index'i ATLA, yoksa `CREATE UNIQUE INDEX IF NOT EXISTS`; temizlik sonrası aynı dosya yeniden koşulur. Sert `RAISE EXCEPTION` prod'da SONRAKİ HER deploy'u bloklar. <sub>(20260825120000_color_name_unique_live/migration.sql:58-79 · [DB-24])</sub> |
| İfade index'i yazıyorsun | Fonksiyon `IMMUTABLE` + `STRICT` beyan edilir, ayırıcı/karakter sınıfı AÇIK yazılır (`\s` yasak — PG'de ctype'a bağlı). Dev (ICU) ile saha (C locale) aynı adı farklı katlar. <sub>(aynı dosya :46-47 · [DB-27])</sub> |
| Büyük tabloya DDL koyuyorsun | EN BAŞA `SET statement_timeout = 0;` konur ve vardiya dışı deploy edilir (app DB'sinde `statement_timeout=50s` aktif). <sub>(20260730170000_roll_status_updatedat_index/migration.sql:11-13 · [DB-25]; 42/232 dosya)</sub> |
| Index'i çevrimiçi kurmak istiyorsun | `CREATE INDEX CONCURRENTLY` YAZMA — Prisma migration'ı tek transaction içinde koşar, 'cannot run inside a transaction block' ile kur.ps1 [7/9]'da düşer. Taslak psql ile elle koşulur ya da düz `CREATE INDEX`e çevrilir. <sub>([DB-26])</sub> |
| Bilinçli yumuşak CHECK (NOT VALID) ekliyorsun | Envantere `notValid` + gerekçe ile yazılır; VALIDATE edilince girdi SİLİNİR. <sub>(Teks-Erp/scripts/test_db_invariants.ts:313-316, 687-698 — ters yön de kırmızı)</sub> — 'VALIDATE etmeyi unuttuk' ile 'bilerek yumuşak' ayrımı başka türlü kaybolur. |
| Henüz onaylanmamış denetim/öneri SQL'i var | `prisma/migration-taslaklari/` altında yaşar (`migrate deploy` oraya BAKMAZ); onaylanan taslak `prisma/migrations/<damga>_<ad>/migration.sql` olarak TAŞINIR. |
| Migration öncesi yedek adı veriyorsun | Ad `premigrate_` ön eki taşımalı — gece rotasyonu yalnız `tekserp_` ön ekini siler. <sub>(Teks-Erp/src/services/helpers/backup-naming.helper.ts:10-12,19-20,196-201)</sub> |
| Migration bir GUC / `ALTER DATABASE` ayarına bağlı | O adım migration DEĞİLDİR: sürüm reçetesine yazılır (emsal `ALTER DATABASE <db> SET teks.audit_guard = 'on'` + restart). <sub>(20260819161000_audit_tamper_guard/migration.sql:16-22 — varsayılan KAPALI)</sub> — atlanırsa trigger kurulu ama koruma kapalı: hiçbir hata da vermez. |
| Migration çalışma zamanı okunan bir dizini değiştiriyor | `db-copy-verify.service.ts:314` `process.cwd()/prisma/migrations`i okur — geri-yükleme doğrulaması bu dizine bağlıdır. |

## Yeni bekçi (scripts/test_*.ts) yazma

Mekanik kapı: KISMİ. Var olanlar: `scripts/check-migrations.mjs` GATE 4 (untracked `test_*.ts` → CI kırmızı, ci.yml:47) · `run-all-tests.ts` tip geçidi (tsconfig.scripts.json) + `productionDbGate` (yalnız koşucuda) · `scripts/test_script_guards.ts` (yıkıcı betikte assertGelistirmeVeritabani kapısı, iki yönlü muaf

1. Dosyayı `Teks-Erp/scripts/` KÖKÜNE, `test_<konu>.ts` adıyla koy (alt dizin taranmaz). <sub>(Teks-Erp/scripts/run-all-tests.ts:178-181 — readdirSync(SCRIPTS_DIR).filter(f => /^test_.*\.ts$/.test(f)); özyineleme YO)</sub> — atlanırsa: Bekçi `npm test`te hiç koşmaz; kimse fark etmez, çünkü paket yine yeşil biter.
2. Paylaşılan yardımcı/fixture dosyasına `test_` öneki VERME (`fixture-*.ts` kalıbı). <sub>(Teks-Erp/scripts/fixture-test-user.ts:3 — "`test_` öneki YOK → run-all-tests koşucusu bunu bir test dosyası saymaz")</sub> — atlanırsa: Koşucu yardımcıyı test sanıp koşar; içinde check() olmadığı için 'geçti (exit 0)' basar — sahte yeşil satır.
3. Çıktı sözleşmesi: `check(label, ok)` ✅/❌ sayaçları + sonda `=== Sonuç: N geçti, M başarısız ===` (`kaldı` ve `N/T geçti` de tanınır; `düştü`/`passed` TANINMAZ). Kontrol atlıyorsan sayıyı AYNI satıra yaz (`, K atlandı`) — serbest metindeki "atlandı" bir sayaç değildir. <sub>(Teks-Erp/CLAUDE.md § bekçi sözleşmesi · kapı: `Teks-Erp/scripts/test_bekci_sozlesmesi.ts` · [TD-25]/[TD-11a])</sub> — atlanırsa: Özet tablosunda sayı yerine 'geçti (exit 0)' yazar; kaç kontrolün koştuğu görünmez, kontrol sayısı sıfıra düşse de fark edilmez (2026-09-06'da üç dosya bu durumdaydı, 46 kontrol gizliydi).
4. `process.exit(fail > 0 ? 1 : 0)` ile bitir — koşucunun DOĞRULUK KAYNAĞI exit kodudur, kazınan özet değil. <sub>(run-all-tests.ts:255 `const ok = res.status === 0 && !res.error;` · :262-267 anormal bitişte kazınan özet EZİLİR)</sub> — atlanırsa: fail sayacı >0 iken süreç 0 ile çıkar → koşucu ✅ raporlar; gerçek regresyon paketten sessizce geçer.
5. Kapanış: ya `process.exit(...)` ya da `await prisma.$disconnect(); await pool.end();` — `$disconnect()` TEK BAŞINA yetmez. <sub>(Teks-Erp/src/lib/prisma.ts:70 `idleTimeoutMillis: 600_000` · Teks-Erp/CLAUDE.md:383-390)</sub> — atlanırsa: Script 'bitti ama çıkmadı' durumunda kalır; koşucu 180 sn'de SIGTERM'ler ve bekçi ZAMAN AŞIMI olarak kırmızı düşer (gerçek arıza gibi görünür).
6. Temizlik `finally` içinde, FK sırasına göre; fason kabulü/varyans yazan HER bekçi `prisma.rollVariance.deleteMany` içerir. <sub>(prisma/schema.prisma:3194 `roll Roll @relation(fields: [rollId], references: [id])` (onDelete YOK → RESTRICT) · CLAUDE.m)</sub> — atlanırsa: Teardown 23001 ile yarıda kesilir; bir koşum 49 öksüz sevk + 60 öksüz top bıraktı ve komşu bekçiler (test_consistency §18) sonradan kırmızıya döndü.
7. Bekçinin yarattığı KULLANICI sert silinmez, pasifleştirilir (audit FK). <sub>(scripts/test_module_flag_off.ts:767-781 — 'user.delete HER koşumda P2003 ile düşüyordu (system_logs_userId_fkey)'; çözüm)</sub> — atlanırsa: `.catch(()=>{})` ile yutulur; başlıktaki 'test kullanıcısını siler' iddiası yalan olur, DB'de 27 bekçi kullanıcısı birikir.
8. Fixture damgası: master-data business-key ile çözülür, üretilen kayıt `TEST-`/`TST-` önekli benzersiz kod alır; hardcoded UUID YASAK. <sub>(Teks-Erp/CLAUDE.md:357 · scripts/test_shipment_doc_customer_name.ts:66 `const P = \`TEST-DCN-${TS}\``)</sub> — atlanırsa: Reseed/canlı-kopya DB'de fixture çözümü kırılır; sabit adla yaratılan master-data ikinci koşumda P2002'ye düşer.
9. Yeni fixture öneki, artık temizleyicinin ve tutarlılık süzgecinin ÖNEK KAYITLARINA eklenir. <sub>(scripts/clean_test_residue.ts:26-31 (ROLL_BARCODE/ORDER_NO/ITEM_CODE/... PREFIXES — 'TEK KAYNAK') · scripts/test_consist)</sub> — atlanırsa: Artık ne süpürülür ne süzülür: dev DB'de birikir (ölçülen vaka: 480 `TST-WHA` iş emri, tablonun %63'ü) ve mutabakat bölümleri kalıcı gürültü basar.
10. Fason/kartela firması ve tam yetkili kullanıcı seed'den ÇÖZÜLMEZ — `fixture-subcontractor.ts` / `fixture-test-user.ts` üretir. <sub>(Teks-Erp/CLAUDE.md:358-372 · scripts/fixture-test-user.ts:6-20 (admin/123123 varsayımı canlı kopyada çöktü, 35 check sus)</sub> — atlanırsa: Pasif ya da yanlış kategorideki firma seçilir; test YANLIŞ ŞEYİ doğrulayarak GEÇER (belirsizlik açık kırmızıdan tehlikeli).
11. Ortamdaki veriye bağlanma — `findFirst()` ile 'herhangi bir top/çuval' bulup üstüne kurma; fixture'ı bekçi kendisi yaratır. <sub>(Teks-Erp/CLAUDE.md:391-395 · .github/workflows/ci.yml:93-104 (boş DB + `migrate deploy` + `npm run seed` + `seed:fixture)</sub> — atlanırsa: Dolu dev DB'de yeşil, temiz CI DB'sinde kırmızı — ya da bölüm veri yokken sessizce ATLANIR (en tehlikeli invariant hiç ölçülmez).
12. Bayrak/ayar/global durum YAZAN bekçi ilk iş `hedefDbEngeli()` sorar ve engel varsa fail ile durur (kaçış `BEKCI_PROD_ONAY=1`). <sub>(scripts/lib/hedef-db-kapisi.ts:29-37 · scripts/test_module_flag_off.ts:316-323 (§0 hedef DB bandı) · fixture-module-flag)</sub> — atlanırsa: `.env` üretim/canlı-kopya DB'sini gösterirse bekçi fabrikanın modül bayraklarını değiştirir; kapı yalnız koşucuda (productionDbGate) olduğu için tek dosya koşum
13. Değiştirilen SystemSetting `finally`de BİREBİR geri yüklenir; satır yoksa satırsız bırakılır (yazamazsan fail++ ile bağır). <sub>(scripts/test_device_pairing_flag.ts (finally: original varsa update, yoksa delete) · test_module_flag_off.ts:760-768 ('b)</sub> — atlanırsa: Bir SONRAKİ bekçi yanlış kırmızı verir ve teşhis buraya kadar geri izlenmek zorunda kalır; 'false yazmak' denetim izini de kirletir.
14. Sunucu isteyen bölümler `atla()` ile ayrılır; asıl güvence STATİK (kaynak taraması) ayakta olur, HTTP ek hattır. <sub>(scripts/test_module_flag_off.ts:90 `const BASE = process.env.TEST_API_URL ?? "http://localhost:4101"` · :109-112 `atla())</sub> — atlanırsa: Sunucu ayakta değilken bölüm sessizce geçer; bozuk kodla yeniden başlatılmamış sunucuda HTTP ayağı 'vakumen yeşil' kalır.
15. Negatif sonda: bekçiyi bilerek kır, kaç ❌ verdiğini ÖLÇ, md5 ile birebir geri yükle, sayıyı dosya başlığına yaz. <sub>(scripts/test_shipment_doc_customer_name.ts:23-30 (yedi sonda, 'her biri md5 ile birebir geri alındı') · Teks-Erp/CLAUDE.)</sub> — atlanırsa: Koşan ama hiçbir şey ölçmeyen bekçi doğar (etkisiz sonda / kör kontrol); paket yeşil kalır, korunan invariant fiilen korumasızdır.
16. Kaynak/DB tarayan her kontrole KÖRLÜK ZEMİNİ (alt sınır) ve muaf listesine İKİ YÖNLÜ denetim koy. <sub>(scripts/test_script_guards.ts:83 (`dosyalar.length > 100`), :102 (yıkıcı iz ≥3), :115 ölü muaf, :117 hayalet dosya · Tek)</sub> — atlanırsa: 'İhlal yok' ile 'hiçbir şeye bakılmadı' aynı yeşile çıkar; tek yönlü muaf listesi düzelttiğin her şeyi sonsuza dek affeder.
17. Dosya `tsconfig.scripts.json` kapsamında derlenir; tam paket koşumunda tip geçidi paketten ÖNCE koşar (filtreli koşumda ATLANIR). <sub>(tsconfig.scripts.json include: ["scripts/**/*","prisma/**/*","src/**/*"] · run-all-tests.ts:54-75, :176 (`if (!filter &&)</sub> — atlanırsa: Var olmayan enum üyesiyle süzme gibi hatalar Prisma'da no-op'a düşer; test yeşil kalır ama doğruladığı ŞEY boşa düşer (2026-08-01'de 87 tip hatası).
18. Yeni bekçi commit edilir — untracked `test_*.ts` bir kapıdır. <sub>(scripts/check-migrations.mjs:129-145 GATE 4 (`code === "??"` + /test_[^/]*\.ts$/) · .github/workflows/ci.yml:47 `node sc)</sub> — atlanırsa: CI'ın gerçek şema kapısı kaybolur: yeni kolonu okuyan bekçi migration'sız kalır ve hata production'da bulunur.
19. Tek bekçiyi koşarken alt-küme filtresi kullan: `npx tsx scripts/run-all-tests.ts <alt-dize>`; 180 sn/test ve 32 MiB çıktı sınırına sığ. <sub>(run-all-tests.ts:170 `const filter = process.argv[2]` · :23 PER_TEST_TIMEOUT_MS=180_000 · :30 MAX_OUTPUT_BYTES=32 MiB · )</sub> — atlanırsa: Uzun bekçi ZAMAN AŞIMI ile düşer; çıktı taşmasında spawnSync status 0 döner ve `!res.error` şartı olmasa kesik çıktılı bekçi GEÇTİ sayılırdı.

⚠️ Belgesiz kalan adımlar (koddan çıkarıldı, hiçbir .md'de yoktu):

- Yardımcı/fixture dosyasına `test_` öneki verilmemesi kuralı hiçbir .md'de yok — yalnız scripts/fixture-test-user.ts:3 yorumunda.
- `hedefDbEngeli()` çağrısının 'bayrak/global durum yazan her bekçinin İLK adımı' olduğu reçete olarak hiçbir yerde yazılı değil (kök CLAUDE.md:96 tek yan cümle).
- Değiştirilen SystemSetting'in finally'de BİREBİR geri yüklenmesi (satır yoksa satırsız bırak, yazamazsan fail++) hiçbir belgede yok.
- Bekçinin yarattığı kullanıcının SERT SİLİNMEMESİ (system_logs_userId_fkey → P2003) belgesiz; CLAUDE.md yalnız 'test kendi yarattığını siler' der.
- Yeni fixture önekinin clean_test_residue.ts ve test_consistency.ts FIXTURE_PREFIXES kümelerine KAYDEDİLMESİ hiçbir belgede yok (480 TST-WHA vakası yalnız audit raporunda).
- Negatif sondanın NASIL yapılacağı (bilerek kır → ❌ sayısını ölç → md5 ile geri yükle → sayıyı başlığa yaz) reçete olarak yok; yalnız 'yapıldı' cümleleri var.
- Körlük zemini + iki yönlü muaf listesi genel bekçi standardı olarak yazılı değil (yalnız timestamptz/izin bekçisi anlatılarının içinde).
- Sunucu isteyen bölüm sözleşmesi (atla() + TEST_API_URL / varsayılan 4101 + 'asıl güvence statiktir') hiçbir belgede yok.
- productionDbGate'in YALNIZ koşucuda olduğu, yani `npx tsx scripts/test_X.ts` tek dosya koşumunun kapısız olduğu reçetede yazılı değil (yalnız audit bulgusu BULGU-T1-138).
- Koşucu sınırları (180 sn/test, 32 MiB maxBuffer, altyapı hatasında TEK yeniden deneme, assertion hatası ASLA yeniden denenmez) yalnız run-all-tests.ts yorumlarında yaşıyor.
- `process.exit(fail>0?1:0)`ın gerekçesi (koşucunun doğruluk kaynağı exit kodu + `!res.error`) belgede yok; format kuralı var, sebebi yok.
- Bekçi standardının TAMAMINI anlatan tek bir belge yok — bilgi dört .md'ye ve üç kod dosyasının başlık yorumuna dağılmış.

## Yeni ESLint kuralı

Mekanik kapı: VAR ve iki parçalı — `npm run lint` (kural `error` ise kapı) + `node scripts/check-lint-baseline.mjs --proje=<backend|electron|mobil>` (kural `warn` ise TAVAN kapısı; tavan yalnız DÜŞER). İkisi de üç yerde koşar: commit hook'u (`scripts/hooks/pre-commit.mjs`), Claude'un Bash kapısı (`scripts/claude-hooks/bash-guard.mjs` aynı hook'u çağırır) ve CI (`.github/workflows/ci.yml` üç job'da "Lint" + "Lint tavanı"). Kapısı OLMAYAN tek şey kuralın DOĞRU kural olmasıdır — onu negatif sonda ölçer. Karar tablosu: `docs/standart/TEST-VE-DERLEME.md` [TD-23]/[TD-24].

1. ÖNCE ÖLÇ: kuralı üç projede de koştur ve ihlal sayısını YAZ (`npx eslint <kapsam> --rule '{"<kural>":"error"}' -f json`). Ölçmeden kural yazılmaz — bu ölçüt `Teks-Erp/eslint.config.mjs` başlığında zaten duruyor. <sub>(üç kova ölçümü `kesif/eslint-aday.json`; sonuçlar `olcum/eslint-<proje>.json`)</sub> — atlanırsa: "Bu kural iyi olur" ile yazılan kural ya 500 ihlalle CI'ı kilitler ya da hiç ısırmaz; ikisi de ancak koştuktan sonra anlaşılır.
2. KARAR TABLOSU ile kova seç: **ihlal 0 → `error`** (bugünkü davranışı dondurur, bedava) · **ihlal küçük ve mekanik → önce DÜZELT, sonra `error`** · **ihlal büyük ya da devralınan koda içkin → `warn` + baseline** · **AST güvenilmez ya da yasak dar okunuyorsa KURAL YAZMA**, gerekçesini config başlığına yaz. <sub>(dördüncü kovanın emsalleri: `Haptics` doğrudan çağrısı 230 — kural yalnız OKUTMA yolunda geçerli; `findUnique→update` AST adayı 5, 2'si yanlış pozitif)</sub> — atlanırsa: Yanlış kovaya konan kural ya kampanya (toplu refactor) dayatır ya da `warn` olarak sonsuza dek birikir.
3. Kuralı doğru dosyaya yaz: backend `Teks-Erp/eslint.config.mjs` · Electron `Electron/eslint.config.mjs` · mobil `mobil/eslint.config.js`. `no-restricted-syntax`/`no-restricted-imports` kullanıyorsan MESAJ kuralı ANLATIR (ne yasak, neden, doğrusu ne). <sub>(mevcut 5 syntax yasağı emsali: `kind === "PROCESS_QC"` helper dışı · `!== "mobile"` · `.body.code` · `applied_steps_count` · çıplak `DATE_TRUNC`)</sub> — atlanırsa: Mesajsız selector "Unexpected code" der; kuralı gören geliştirici NEDEN'i bilmeden `eslint-disable` yazar.
4. KAPSAM TEK KAYNAK: kuralın koştuğu argv, o projenin `package.json > scripts.lint` argv'si ve `scripts/check-lint-baseline.mjs` içindeki `PROJELER` argv'si BİREBİR aynı olmalı; kapsam değişirse ÜÇÜ BİRLİKTE güncellenir (+ CI adımı). <sub>(`scripts/check-lint-baseline.mjs` başlığı · docs/standart/TEST-VE-DERLEME.md [TD-23])</sub> — atlanırsa: Baseline başka bir kümeyi ölçer ve "tavan aşılmadı" cümlesi anlamını yitirir; aynı sınıf hata tsc'de yaşandı — `scripts/` hiçbir tsconfig'in include'unda değildi ve 87 tip hatası birikmişti.
5. Kural TİP BİLGİSİ istiyorsa (`no-floating-promises` gibi) `parserOptions.projectService: true` yalnız GEREKEN blokta açılır ve lint süresi ÖNCE/SONRA ölçülür. <sub>(ölçüm: Electron 5,2 → 11,4 sn; mobil +3,3 sn (%25))</sub> — atlanırsa: Tip bilgili parser tüm config'e açılırsa commit kapısı yavaşlar ve kapı ilk atlanan şey olur.
6. NEGATİF SONDA ZORUNLU: kuralın yasakladığı kalıbı kasten yaz, `npm run lint`in KIRMIZI verdiğini ÖLÇ, geri al. Sondasız kural bir niyettir. <sub>(docs/standart/TEST-VE-DERLEME.md [TD-14] · docs/RECETELER.md § Yeni bekçi (scripts/test_*.ts) yazma)</sub> — atlanırsa: Yanlış yazılmış bir selector hiçbir düğümle eşleşmez; kural dosyada durur, ölçtüğü şey sıfırdır ve kimse fark etmez.
7. `warn` seçtiysen tavanı yaz ve COMMIT ET: `node scripts/check-lint-baseline.mjs --proje=<p> --yaz` → `<proje>/lint-baseline.json`. Otomatik indirme YOKTUR; kod düzelince tavanı ELLE indirirsin. <sub>(`scripts/check-lint-baseline.mjs` başlığı: baseline bir KARARDIR, gözden geçirilir)</sub> — atlanırsa: Tavansız `warn` kuralı hiçbir şeyi durdurmaz — eslint uyarıyla çıkış kodu 0 döner (Electron 37 / mobil 136 uyarıyla yeşil geçiyordu).
8. KÖRLÜK ZEMİNİNİ kontrol et: baseline bekçisi lint edilen dosya sayısını da ölçer; kapsam çöktüğünde "0 ihlal" yeşili SAHTEDİR. Yeni bir proje/dizin kapsama girerse `EN_AZ_DOSYA` zemini de güncellenir. <sub>(`scripts/check-lint-baseline.mjs` § körlük zemini — backend 900 · electron 600 · mobil 200)</sub> — atlanırsa: `src` ya da `scripts` argv'den düşerse kural hiçbir dosyayı taramaz ve bekçi yeşil kalır.
9. Muafiyeti ADLI `files` override'ı ile ver, heuristikle değil. <sub>(docs/standart/ELECTRON.md [EL-36] — "0 hook + yorum >%15" heuristiği `content-routes.tsx`'i KAÇIRIYOR ve iş mantığı taşıyan 10 dosyayı süpürüyordu)</sub> — atlanırsa: Heuristik muafiyet yarın yazılacak dosyayı da muaf eder; kural kendi kapsamını sessizce daraltır.
10. Ölü `eslint-disable` bırakma: `reportUnusedDisableDirectives: "error"` açıkken bastırma satırı kural düzelince kırmızı verir. Yeni kodda disable YAZILMAZ; yazılıyorsa gerekçesi aynı satırdadır. <sub>(docs/standart/MOBIL.md [MO-41]; ölü disable ölçümü: Electron 65 · mobil 42 · backend 0)</sub> — atlanırsa: Ölü bastırma satırı kuralın gerçekten uygulandığı yanılsamasını üretir.
11. Kuralı `docs/standart/` içindeki ilgili dosyaya KURAL SATIRI olarak yaz (`zorlama: eslint:<kural>` etiketiyle) — kural koda, kaydı belgeye. <sub>(kural biçimi docs/standart/README.md § Kural biçimi)</sub> — atlanırsa: Kapı var ama gerekçe yok; ilk sürtünmede kural "gürültü" diye kaldırılır.

## Yeni Electron sayfası

Mekanik kapı: KISMEN VAR, dört ayrı bekçi — hiçbiri prosedürün tamamını kapsamıyor: (1) Teks-Erp/scripts/test_screen_catalog.ts — §4 her Electron route izni manifestoda beyanlı, §7 modul zorunlu/tanınan, §9c/§9e karo↔modul bayrak hizası; (2) Electron/src/components/layout/command-entries.test.ts — her statik rout

1. Sayfa klasörü `pages/<Module>/` aç; kök `<PageShell>`, kaydırılan gövde `<PageBody>`, alt butonlar `<PageFooter>`; CRUD listesi ise orkestrasyonu `<CrudPage<T>>` yapar (state/mutation/dialog/tablo). <sub>(Electron/src/components/layout/PageShell.tsx:23,37,49 (PageShell/PageBody/PageFooter export) · Electron/src/components/l)</sub> — atlanırsa: Sayfa derlenir ve açılır; kaydırma/footer yerleşimi diğer sayfalardan sapar, hiçbir bekçi ölçmez.
2. `service.ts` → `createCrudService<T>("/api/...")`. `/api` öneki ZORUNLU: apiClient baseURL'i `/api` İÇERMEZ, her yol tam yazılır. <sub>(Electron/src/services/apiClient.ts:44 `baseURL: DEFAULT_API_BASE_URL` · crudService.ts:24-40 (`${basePath}` ile birleşti)</sub> — atlanırsa: Backend 404 döner; liste ekranı bunu 'Sonuç yok' diye yutar (api-path-prefix.test.ts başlığındaki 12 günlük saha vakası).
3. Route'u `Electron/src/routes/content-routes.tsx` içine ekle — `router.tsx` DEĞİL (o dosyada yalnız login/forbidden/2FA yaşar; uygulama içeriği sekme router'larında). <sub>(Electron/src/routes/content-routes.tsx (1218 satır, tüm sayfa ağacı) · Electron/src/router.tsx:32-38 (yalnız /login, /fo)</sub> — atlanırsa: Sayfa hiçbir adresten açılmaz; karo/palet girişi varsa tıklayınca kök yönlendirmesine düşer.
4. Route'u `<ProtectedRoute requirePermission="...">` (veya `requireAnyPermission={[...]}`) ile sar; kullanılan izin kodu backend PERMISSION_CATALOG'da BULUNMAK ZORUNDA. <sub>(Electron/src/components/ProtectedRoute.tsx:46-52 (izin dalları; admin kısayolu YOK) · Teks-Erp/scripts/test_screen_catal)</sub> — atlanırsa: Guard'sız route herkese açık kalır (FRONTEND-CODE-REVIEW.md:514 — 9 tanım rotası bu durumda ölçüldü).
5. Hub karosu ekle: ilgili `tile-config.ts` dizisine `{key,title,description,icon,to,group,permission|permissionAny}` girdisi (Definitions/Operations/Finance/Access/System/Reports). <sub>(Electron/src/pages/Definitions/tile-config.ts:39-61 (DefinitionTile arayüzü), :62-72 (örnek girdi) · Electron/src/pages/)</sub> — atlanırsa: Ekran hub'da görünmez, komut paletine de düşmez (palet karo dizilerinden türer) → yalnız adres bilenler ulaşır.
6. Karo izni ile route izni BİREBİR aynı olmalı (`tile.permission`/`permissionAny` ↔ `requirePermission`/`requireAnyPermission`). <sub>(Electron/src/routes/content-routes.tsx:202-206 (karo ile aynı liste uyarısı) · Electron/src/pages/System/tile-route-perm)</sub> — atlanırsa: Kullanıcı kartı GÖRÜR, tıklar, /forbidden'a düşer; ne hata mesajı ne log sebebi söyler.
7. Karonun `group` değeri hub'ın groups-config'inde TANIMLI olmalı (Tanımlar: catalog|partners|production|cikti). <sub>(Electron/src/pages/Definitions/groups-config.ts:9-13 (DefinitionGroupKey union) · Electron/src/components/layout/command)</sub> — atlanırsa: Tip hatası vermeyen bir grup adı yazılırsa karo hiçbir grupta çizilmez ve palet bölümü de boş kalır.
8. Ekran kapatılabilir bir modüle aitse karoya o modülün bayrak yüklemi (`visibleWhen`) konur; karo bayrağı ile manifesto `modul`u AYNI olmak zorunda (ters yön de ölçülür). <sub>(Teks-Erp/scripts/test_screen_catalog.ts:602 (§9c), :631 (§9e ters yön) · Electron/src/pages/Definitions/tile-config.ts:4)</sub> — atlanırsa: Modül kapalı kurulumda karo çizilir; kullanıcı tıklar, backend rejim middleware'i her isteği 403'ler.
9. Backend `SCREEN_CATALOG`'a masaüstü girdisi ekle: `key` = route yolu (ilk iki segment), `app:"desktop"`, `modul` (ZORUNLU: ModulKey | cekirdek:* | planlanan:*), `title`, `requires`, `capabilities`. <sub>(Teks-Erp/src/constants/screen-catalog.ts:97-118 (ScreenEntry; `modul` opsiyonel DEĞİL), :173+ desktop dizisi · test_scre)</sub> — atlanırsa: Yetki atama ekranının 'Ekrana göre' görünümünde ekran hiç görünmez; Sistem Profili 'kapatırsan şunlar gizlenir' önizlemesi eksik kalır.
10. Komut paleti: KAROSU OLAN ekran otomatik listelenir (bölümler tile-config'lerden türer). KAROSUZ ekran `command-entries.ts`e ELLE eklenir ve görünürlük yüklemi de ELLE taşınır. <sub>(Electron/src/components/layout/command-entries.ts:27-34 (kaynak kopyalanmaz taşınır), :68-84 (`ops:work-order-new` karos)</sub> — atlanırsa: Ctrl+K'da ekran hiç çıkmaz (2026-08-06 saha şikâyeti); karosuz girdide yüklem taşınmazsa modül kapalıyken palet açık kalır.
11. Sekme başlığı/ikonu otomatik: `resolveTabMeta` sabit eşleme → palet kataloğu → son-segment fallback. Hub/sistem sayfaları için `STATIC` eşlemesine elle satır gerekir. <sub>(Electron/src/components/layout/tabs/tab-meta.ts:28-46 (STATIC), :74 (allCommandEntries.find), :83 (titleFromSegment fall)</sub> — atlanırsa: Sekmede Türkçe başlık yerine slug'dan üretilmiş ad çıkar (ör. 'Station Efficiency') ve ikon jenerik FileText olur.
12. `queryKey` string'i seç ve CrudPage ile useCrudMutations'a AYNI değeri ver — invalidasyon ad alanı odur. <sub>(Electron/src/components/layout/CrudPage.tsx:33,118,204,271 · Electron/src/hooks/useCrudMutations.ts:11-13 `qc.invalidate)</sub> — atlanırsa: Kayıt/silme sonrası liste tazelenmez (kullanıcı 'kaydolmadı' sanır) ya da başka sayfanın sorgusunu tazeler; Yenile düğmesi ölü query-key'e vurur.
13. Rapor sayfasıysa palet/karo izni `report:<domain>` kalıbında olmak zorunda (domain = /reports/<domain> segmenti). <sub>(Electron/src/components/layout/command-entries.test.ts:67-71 (her /reports/ girişi `report:${domain}` taşımalı))</sub> — atlanırsa: Palet girişi görünür ama tıklayınca /forbidden — rapor domain izni ile route izni ayrışır.
14. Sidebar'a satır EKLENMEZ — giriş hub karosundan olur; sidebar satırı yalnız kök gruplar içindir ve modül bayrağı `featureFlag` alanıyla taşınır. <sub>(Electron/src/components/layout/nav-config.ts:12-48 (NavItem/featureFlag sözleşmesi), :55-70 (navGroups yalnız 6 kök satı)</sub> — atlanırsa: Sidebar şişer; ayrıca satır bayrak alanı olmadan eklenirse modül kapalı kurulumda menüde durur, backend 403 döner.
15. BOYUT sınırları yeni ve dokunulan dosyada zorunludur: dosya ≤300 · `*Page.tsx` ≤200 · `*FormDialog.tsx` ≤200 · fonksiyon ≤80 · parametre ≤4 (satır = KOD satırı; yorum ve boş satır SAYILMAZ). Muafiyet ADLI `files` override'ı ile verilir, heuristikle değil. <sub>(docs/standart/ELECTRON.md §10 [EL-35]/[EL-36]/[EL-37] · docs/standart/ILKELER.md §6 · kapı: `eslint max-lines`/`max-lines-per-function` (warn) + `node scripts/check-lint-baseline.mjs --proje=electron`)</sub> — atlanırsa: Uyarı kapı değildir (eslint uyarıyla çıkış kodu 0 döner); tavan olmadan `warn` sessizce birikir — 2026-09-03/04'te açılan sayfalar 442/304/257 satırla tam böyle geçti.
16. Kalıbın TAMAMI `docs/standart/ELECTRON.md`'dedir (iskelet · `apiClient` tek kapısı · sunucu süzmesi · TEK hata toast'ı · izin aynası · şema ↔ Prisma `VarChar` hizası · named export · testin yeri); kadans ve kapılar `docs/standart/TEST-VE-DERLEME.md`'de. Tip kontrolü `npm run typecheck` ile koşulur. <sub>(docs/standart/TEST-VE-DERLEME.md [TD-21])</sub> — atlanırsa: Çıplak `npx tsc --noEmit` kök tsconfig `files: []` + `references` yüzünden HİÇBİR dosyayı derlemez; "tsc temiz" cümlesi hiçbir şey ölçmez (CI'ın Type-check adımı 2026-09-05'e dek tam bu yüzden no-op'tu, 9 tip hatası main'de bir gün durdu).

⚠️ Belgesiz kalan adımlar (koddan çıkarıldı, hiçbir .md'de yoktu):

- Route'un yeri: hiçbir belgede `src/routes/content-routes.tsx` bir PROSEDÜR adımı olarak yazılı değil; üç yerde (Electron/CLAUDE.md:44,245,384) hâlâ `router.tsx` deniyor — orada bugün yalnız login/forbidden/2FA var.
- Backend `screen-catalog.ts`e girdi ekleme adımı hiçbir kontrol listesinde YOK; yalnız tarihli notlarda ve tasarım belgesinde anlatılıyor. 'Yeni ekran eklerken buraya da yaz' cümlesi yazılı değil.
- `ScreenEntry.modul` değerinin nasıl seçileceği (ModulKey vs cekirdek:* vs planlanan:*) prosedür olarak yazılı değil.
- Karo `visibleWhen` ↔ manifesto `modul` hizası (§9c/§9e) adım olarak hiçbir belgede yok; yalnız bekçi kodunun yorumunda.
- `queryKey` seçimi ve CrudPage↔useCrudMutations eşitliği hiçbir belgede yok (Electron/CLAUDE.md kontrol listesi queryKey'den hiç söz etmiyor).
- Komut paleti adımı hiçbir kontrol listesinde yok — 'karolu ekran otomatik, karosuz ekran elle' ayrımı yalnız kod yorumunda.
- Sekme meta (tab-meta STATIC eşlemesi) ve slug fallback davranışı hiçbir belgede yok.
- Karo `group` değerinin groups-config'te bulunma zorunluluğu yazılı değil.
- `/api` öneki kuralı Electron/CLAUDE.md 'Yeni Sayfa Kontrol Listesi'nde YOK (yalnız bekçi dosyasının kendi başlığında).
- Rapor sayfası izin kalıbı (`report:<domain>`) yalnız test dosyasında; belgede yok.

## Yeni mobil ekran / özellik

Mekanik kapı: KISMEN VAR (kayıt noktaları başına dağınık): Teks-Erp/scripts/test_screen_catalog.ts (§3 satır 415-421 mobil ekran↔manifesto+izin birebir; §5 satır 440-443 ekransız izin yok) · Teks-Erp/scripts/test_mobile_screen_permissions.ts (ekran izni ↔ çağırdığı uçların guard'ı) · mobil/src/constants/stationSc

1. Ekran anahtarını `MobileScreenKey` union'ına ve `MOBILE_SCREENS` dizisine (key/permission/label/icon/description) ekle — mobilin ekran beyanı burada tek kaynak. <sub>(mobil/src/types/permissions.ts:59 (union), :84 (MOBILE_SCREENS), :206 (SCREEN_BY_KEY))</sub> — atlanırsa: usePermissions().allowedScreens ekranı hiç üretmez (usePermission.ts:33) → ModuleSelect grid'inde kart yok, MainNavigator route'u kaydetmez. Kod repoda durur, s
2. İzin kodunu `MobilePermission` union'ına ekle (ekran izni ya da ekran-içi yetenek izni). <sub>(mobil/src/types/permissions.ts:1-40 (`MobilePermission`), kullanım usePermission.ts:26-33 `has()`)</sub> — atlanırsa: `has()` düz string alır — yanlış/eksik yazılan kod sonsuza dek sessizce `false` döner (permissions.ts:55-58'de `customer-alias:write` için birebir yaşandığı yaz
3. `moduleAccents`'a ekranın renk satırını ekle. <sub>(mobil/src/theme/tokens.ts:156 `Record<MobileScreenKey, {tint,bg}>`)</sub> — atlanırsa: Tip Record olduğu için `tsc --noEmit` yakalar; ama Metro tip denetlemez ve mobil/package.json'da typecheck script'i YOK (scripts: test/lint) → tsc koşulmazsa Mo
4. `SCREEN_LOADERS`'a lazy `require` satırı ekle (MainNavigator). <sub>(mobil/src/navigation/MainNavigator.tsx:20-36)</sub> — atlanırsa: Record tamlığı yine yalnız tsc ile ölçülür; eksikse `getComponent` undefined döner ve o rotaya navigate edildiğinde ekran çöker.
5. Rota parametresi ALIYORSA anahtarı `ParameterizedScreenKey`'e ekleyip `MainStackParamList`'te açıkça tanımla (Record'dan Exclude edilir). <sub>(mobil/src/navigation/types.ts:28 `ParameterizedScreenKey`, :30-49 `MainStackParamList & Record<Exclude<...>, undefined>`)</sub> — atlanırsa: Kesişim `{p?:string} & undefined` = `never` olur; o rotaya navigate etmek DERLENMEZ — dosyadaki yorum (types.ts:22-27) bunu açıkça yazıyor.
6. Ekran bir İSTASYON ekranıysa (yer onayı isteyen) `stationScreens.ts`teki İKİ haritaya da yaz. <sub>(mobil/src/constants/stationScreens.ts:17 SCREEN_BY_STATION_KIND, :25 STATION_KIND_BY_SCREEN; sarma MainNavigator.tsx:42-)</sub> — atlanırsa: `isSessionScreen` false → SessionGate sarmalanmaz → yer onayı olmadan ekran açılır; damgalı uçlarda backend 409 WORK_SESSION_REQUIRED döner ve oturum çözülmediğ
7. Backend: yeni izin kodunu `PERMISSION_CATALOG`'a ekle (boot uzlaştırması satırı DB'ye kendisi getirir). <sub>(Teks-Erp/src/constants/permission-catalog.ts:226 (emsal `mobile:kumas`); uzlaştırma Teks-Erp/src/jobs/permission-catalog)</sub> — atlanırsa: İzin satırı DB'de doğmaz → hiçbir kullanıcıya atanamaz → ekran hiç kimsede görünmez. Job başlığı (satır 5-9) bu hatanın 2026-08-01'de fiilen yaşandığını ve teşh
8. Backend: `SCREEN_CATALOG`'a `{ key, app:"mobile", modul, title, requires, capabilities }` satırı ekle. <sub>(Teks-Erp/src/constants/screen-catalog.ts:310-324 (mobil satırlar), `modul` alanı zorunlu :44-80)</sub> — atlanırsa: Bekçi yakalar (test_screen_catalog §3 satır 415-421 ve §5 satır 440-443: 'ekranı beyan edilmeyen izin yok'). Bekçi koşulmazsa yönetici atama ekranı yetkiyi hang
9. Backend: ekranın çağırdığı HER ucun route guard'ı ekranın iznini kabul etmeli (`requireAnyPermission`'a ekle). <sub>(Emsal Teks-Erp/src/routes/item.routes.ts:93, color.routes.ts:64; bekçi Teks-Erp/scripts/test_mobile_screen_permissions.t)</sub> — atlanırsa: Operatör SESSİZ 403 alır: ekran hata göstermez, liste boş gelir. Bekçi başlığı (satır 7-13) bunun 2026-08-17'de Tambur'da birebir yaşandığını yazıyor; servis-ka
10. İzin ATAMASI elle yapılır (katalog koda, atama panele) — gerekirse rol şablonuna ekle. <sub>(Teks-Erp/CLAUDE.md:242; rol şablonu emsali Teks-Erp/src/constants/role-template-catalog.ts:559)</sub> — atlanırsa: Kod ve DB doğru olduğu hâlde ekran hiçbir tablette görünmez; teşhis sırası (satır DB'de mi → atanmış mı → kullanıcı yeniden giriş yaptı mı) yalnız backend CLAUD
11. KAYIT YARATAN ekranda idempotency: `clientToken` + `clientEnteredAt` kimliğini `entryAttempt` durum makinesinden al (freshEntryIdentity / decideSubmit / isAmbiguousFailure). <sub>(mobil/src/offline/entryAttempt.ts:104 EntryIdentity, :126 entryFingerprint, :141 decideSubmit, :250 isAmbiguousFailure; )</sub> — atlanırsa: Her buton basışı yeni kimlik gönderir; timeout/5xx sonrası tek fiziksel top N kayıt doğurur (dosya başlığı 2026-08-03 saha vakası). Ters yönde 'tek sabit token'
12. OFFLINE kuyruğa girecek mutation için ÜÇ yer: `STATION_MUT` anahtarı + `setMutationDefaults` kaydı + `STATION_MUT_LABELS` etiketi. <sub>(mobil/src/offline/mutations.ts:44 STATION_MUT, :152-273 setMutationDefaults; mobil/src/offline/stationLabels.ts:15 STATI)</sub> — atlanırsa: (a) anahtar `['station',...]` değilse `isStationMutationKey` false → pending kayıt diske yazılmaz (persistPolicy.ts:59,65) ve app kill'de kaybolur; (b) defaults
13. Kuyruklanan mutation `mutate(vars)` ile ÇAĞRILIR — payload closure'dan okunmaz. <sub>(mobil/src/offline/mutations.ts:1-6 ('PersistQueryClientProvider mutation'ın FN referansını persist edemez (sadece variab)</sub> — atlanırsa: App restart sonrası registry fn `service(undefined)` ile resume olur → Zod 400 → 4xx fail-fast (mutations.ts:110 civarı) → kayıt kalıcı kaybolur. Kartela Sevk/K
14. Ekran hazır sebep listesi kullanacaksa `ReasonPresetPicker` + `useReasonPresets` kullan; yeni bir `ReasonPresetKind` gerekiyorsa BEŞ kapı (şema+migration · backend katalog · Electron KIND_TABS · mobil union · mobil gömülü zemin). <sub>(mobil/src/components/reasonPresets/ReasonPresetPicker.tsx; mobil/src/services/reasonPreset.service.ts:15 union, :42 KIND)</sub> — atlanırsa: Gömülü zemin yazılmazsa sunucu erişilemezken liste BOŞ gelir ve sebep zorunlu olan karar (fire/iptal) kaydedilemez — mal ekranda kilitlenir (useReasonPresets.ts
15. KANAL KARARI: `npm run yayinla:check -- --musteri=<kod>` koş — native parmak izi (dependencies + plugins + android bloğu) değiştiyse `app.json > expo.runtimeVersion` ARTIR ve APK derle; değişmediyse OTA yeter. <sub>(mobil/scripts/yayinla-ota.mjs:172 nativeParmakIzi, :193 parmakIziKapisi, :235 'NATIVE DEĞİŞTİ ama runtimeVersion AYNI KA)</sub> — atlanırsa: Kapı geçilirse (--parmak-izini-kabul-et ile bilinçsizce) yeni JS eski native'i çağırır ve sahadaki TÜM tabletler açılışta çöker — script başlığı (satır 22-27) b
16. OTA turunda `android.versionCode`a DOKUNMA; yalnız yayınlanacak bir APK için artır. <sub>(mobil/scripts/yayinla-ota.mjs:487-516 (versionCode kapısı, `yayindakiApkKunyesi` ile kıyas); mobil/app.json versionCode )</sub> — atlanırsa: OTA paketi versionCode'u da taşır; tablet onu KURULU APK sürümü sanar ve gerçek APK güncellemesini bir daha teklif etmez (sessiz + kalıcı). Ayrıca app.json ile 
17. Sürüm notu: `surum-notlari.json`'a kayıt ekle (kapsam `tablet`/`her-ikisi`) → `node scripts/surum-notlari-kopyala.mjs` → kapı `check-surum-notlari.mjs`. <sub>(mobil/scripts/yayinla-ota.mjs:520-537 (`check-surum-notlari.mjs --tablet=<sürüm>` çağrısı, kırmızıysa dur))</sub> — atlanırsa: OTA yolunda paketleme durur (mekanik). APK yolunda kapı YOKTUR: grep `surum-notlari` mobil/scripts/build-apk.mjs → 0 eşleşme, deploy/mobil-yayinla.mjs → 0 eşleş
18. Yayın komutlarında `--musteri=<kod>` ve `EXPO_PUBLIC_API_URL` AÇIKÇA verilir (varsayılan yok). <sub>(mobil/scripts/yayinla-ota.mjs:369-390 (müşteri zorunlu + musteri.json ile kıyas), :95-124 adresCoz (localhost/`/api` biç)</sub> — atlanırsa: Yanlış müşteri kodu, o fabrikanın tabletlerine BAŞKA fabrikanın güncellemesini indirtir; adres pakete derleme anında gömüldüğü için sonradan düzeltilemez, table
19. Yeni npm paketi eklemeden önce onay al; native paket = APK turu. <sub>(mobil/CLAUDE.md 'Allowed Packages' tablosu + 'Yeni paket eklemeden önce onay al'; parmak izi kapsamı yayinla-ota.mjs:173)</sub> — atlanırsa: Dependencies parmak izine girdiği için OTA kapısı durur (yakalanır); ama kapıyı `--parmak-izini-kabul-et` ile geçmek doğrudan tablet çökmesi demektir.
20. BOYUT sınırları yeni ekranda zorunludur: ekran kabuğu `*Screen.tsx` ≤250 · görünüm/panel/sheet ≤500 · ekran-hook ≤400 · tek React bileşeni fonksiyonu ≤300 satır VE ≤12 `useState` · saf mantık modülü ≤200 (+ yanında `.test.ts` ZORUNLU) · servis ≤400 · parametre ≤4; yeni ekranda ham hex 0 (renk `theme/tokens.ts`'ten). <sub>(docs/standart/MOBIL.md §12 [MO-30]…[MO-33] · docs/standart/ILKELER.md §6 · kapı: `eslint max-lines` (warn) + `node scripts/check-lint-baseline.mjs --proje=mobil`)</sub> — atlanırsa: Beş dev ekran (Tambur 9.386 · FasonKabul 4.679 · KK1 4.204 · KursunQc 3.192 · FasonSevk 2.401 = 23.862 satır) tam bu yolla doğdu; bölmek artık AYRI bir iştir ve tablette ölçüm ister.
21. Kalıbın TAMAMI `docs/standart/MOBIL.md`'dedir (ekran = ince kabuk + görünüm bileşenleri + ekran-hook + saf mantık modülü · `signalScan` okutma geri bildirimi · `AppModal` · `FlashList` + `useInfiniteQuery` · göreli import · `queryKeys`); lint TEK komutla ve tam kapsamda koşar: `npm run lint` (= `eslint .`). <sub>(docs/standart/MOBIL.md [MO-37]; `expo lint` yol verilmezse `App.tsx` · `index.ts` · `app.config.js` · `jest.config.js` · `plugins/` · `scripts/` (12 dosya) LİNT DIŞI kalır)</sub> — atlanırsa: Kapı ile baseline farklı kümeyi ölçer; "tavan aşılmadı" cümlesi anlamını yitirir ve lint dışı kalan dosyalarda kural hiç koşmaz.

⚠️ Belgesiz kalan adımlar (koddan çıkarıldı, hiçbir .md'de yoktu):

- Uçtan uca 'yeni mobil ekran ekleme' kontrol listesi HİÇBİR belgede yok; kayıt noktaları yalnız iki özellik tasarımının anlatısına gömülü (KARTELA-TASARIM:235, IADE-TASARIM:222) ve o iki liste bugün EKSİK — backend PERMISSION_CATALOG ve SCREEN_CATALOG satırlarından hiç söz etmiyorlar.
- `MainStackParamList` / `ParameterizedScreenKey` kuralı (parametre alan ekran Record'dan Exclude edilmezse tip `never` olur) hiçbir belgede yazılı değil — yalnız navigation/types.ts:22-27 yorumunda.
- Yeni oturumlu (istasyon) ekranın `stationScreens.ts`teki İKİ haritaya da yazılması gerektiği bir prosedür adımı olarak yazılı değil; mobil/CLAUDE.md'de yalnız HAL/donanım bölümünün içinde bir yan cümle.
- 'Ekranın çağırdığı her ucun guard'ı ekranın iznini kabul etmeli' kuralı ve bekçisi (`test_mobile_screen_permissions.ts`) hiçbir .md'de anılmıyor — 2026-08-17 sessiz 403 vakası yalnız bekçinin kendi başlığında yaşıyor.
- Yeni `STATION_MUT` anahtarı eklerken üç dosyaya birden dokunulacağı (mutations.ts anahtar + setMutationDefaults + stationLabels.ts) yalnız arşiv notunda; mobil/CLAUDE.md'de `STATION_MUT` geçmiyor (grep: 0).
- `mutate()`'e variables geçirme zorunluluğu (persist yalnız variables saklar; closure payload app restart'ta undefined döner) hiçbir belgede KURAL olarak yok — yalnız 2026-06 tarihli FRONTEND-CODE-REVIEW bulgusu olarak.
- APK yolunda sürüm notu kapısının OLMADIĞI hiçbir yerde yazılı değil; kök CLAUDE.md 'not yoksa paketleme DURUR' derken build-apk.mjs ve deploy/mobil-yayinla.mjs'te `surum-notlari` geçmiyor (grep: 0).
- Record tamlığının (moduleAccents, SCREEN_LOADERS, KIND_LABELS…) yalnız `npx tsc --noEmit` ile yakalandığı ve mobil/package.json'da typecheck script'i OLMADIĞI (scripts: start/dev/android/ios/web/build:apk*/yayinla*/test/lint) hiçbir yerde yazılı değil — Metro tip denetlemez.
- 'Bu ekran offline kuyruğa girmeli mi / oturum istemeli mi / clientToken taşımalı mı' sorularını cevaplayan bir karar ağacı yok; her yeni ekran bu üç kararı sıfırdan veriyor (çuval mutasyonlarının bilinçli online-only olması da yalnız tek bir madde olarak yazılı).
- Electron'da `.claude/skills/electron-admin-page` var ama mobilde eşdeğer bir 'yeni ekran' skill'i/klasörü YOK (mobil/.claude dizini mevcut değil).

## Yeni bağımlılık (npm paketi)

Mekanik kapı: KISMİ. Var olanlar: `Electron/src/test/discovery-ipc-contract.test.ts` (Electron'da pin + `dependencies` ayrımı) · `.claude/rules/kutuphane.md` (her `package.json` dokunuşunda standardı işaret eder). Yazılacak tek bekçi `test_dependency_contract.ts` (backend CJS invariantı + pin + kayıtsız paket + ölü paket üç kanalı). Onay şartı MEKANİK DEĞİLDİR: yeni paket eklemeden önce KULLANICI ONAYI alınır (kök `CLAUDE.md` § Süreç). Standart metni: `docs/standart/KUTUPHANELER.md` (§2 katman × ihtiyaç · §3 karar kaydı · §4 sürüm · §5 yükleme kalıpları).

1. ONAY AL. Yeni paket bir karardır; onaysız eklenmez. <sub>(kök CLAUDE.md § Süreç, sır, donanım)</sub> — atlanırsa: Paket sessizce kuruluma, imaja ve APK'ya girer; geri alması bir sürüm turudur.
2. `docs/standart/KUTUPHANELER.md` §2 tablosuna bak: bu ihtiyaç için ZATEN bir kütüphane var mı (ikon `lucide-react` / `@expo/vector-icons` · toast `sonner` / `react-native-toast-message` · xlsx `exceljs` · http `axios` · liste `FlashList`). Aynı ihtiyaca İKİNCİ kütüphane girmez. <sub>(docs/standart/KUTUPHANELER.md §1-§2 — ölçülen fiilî disiplin: her ihtiyaç tek kütüphane)</sub> — atlanırsa: İki toast/iki tarih kütüphanesi aynı ekranda farklı davranır; birleştirme maliyeti kaldırma turuna döner.
3. En az İKİ alternatif değerlendir ve "ELLE YAZMAK"ı bunlardan biri say; reddettiğini NEDEN reddettiğini yaz. <sub>(docs/standart/KUTUPHANELER.md [KU-07]; emsal `docs/kurallar/filtre-liste.md` — mobil takvim ızgarası elle yazıldı, sıfır bağımlılık)</sub> — atlanırsa: 40 satırla çözülecek iş için bakım yükü, lisans riski ve sürüm takibi devralınır.
4. LİSANS permissive olmalı (MIT/ISC/BSD/Apache-2.0/OFL); copyleft (GPL/LGPL/AGPL/SSPL) GİRMEZ. Çift lisanslı pakette SEÇİLEN seçenek kayda yazılır. <sub>([KU-09]/[KU-10]; üç projede 2.858 paket tarandı, doğrudan bağımlılıklarda copyleft 0)</sub> — atlanırsa: Kapalı dağıtılan fabrika paketine copyleft bir kütüphane girer ve fark ancak müşteri denetiminde çıkar.
5. BACKEND'e giriyorsa CommonJS sınavı: paket gerçek `require()` ile çözülebilmeli — ESM-only paket backend'e GİRMEZ. `require.resolve` yetmez, gerçek `require()` denenir. <sub>([KU-18]/[KU-19]; `uuid@13` bugün yalnız Node ≥ 22.12'nin `require(esm)` desteğiyle koşuyor)</sub> — atlanırsa: `npm run build` yeşil kalır, hata ÇALIŞMA ZAMANINDA `ERR_REQUIRE_ESM` olarak fabrikada çıkar.
6. MOBİLE giriyorsa native/config-plugin sorusunu sor: `app.json > plugins`e satır ekleyen ya da `android/` taşıyan paket APK ister, OTA YETMEZ; izin etkisi (BLUETOOTH/LOCATION vb.) kayda yazılır. <sub>(mobil/app.json · [KU-11]/[KU-24])</sub> — atlanırsa: OTA turu paketi bundle'a koyar ama native modül cihazda yoktur; ekran ilk açılışta çöker ve geri dönüş yolu yeni APK'dır.
7. ELECTRON main'de native modül `createRequire` ile TEMBEL ve try/catch içinde yüklenir; yüklenemezse yol `available:false` ile kapanır, uygulama ÇÖKMEZ. <sub>(Electron/electron/ipc/printer.ipc.ts:17,40-45 · [KU-16])</sub> — atlanırsa: Yazıcı/terazi sürücüsü kurulu olmayan makinede uygulama açılışta ölür.
8. 200 KB'ı aşan paketi statik import ETME; `await import()` ile ayrı chunk'a al. <sub>(Electron/src/lib/xlsx-export.ts:39 · [KU-17])</sub> — atlanırsa: İlk açılış bundle'ı şişer; maliyet her kullanıcıya her açılışta ödenir.
9. Çalışma zamanında yüklenen paket `dependencies`te olur — `devDependencies`e düşerse kurulu üründe `MODULE_NOT_FOUND` verir (`npm ci --omit=dev`). <sub>([KU-15]; `prisma` CLI backend'de bu yüzden `dependencies`te)</sub> — atlanırsa: Hata yalnız fabrikada, kurulum sırasında ve migration adımında görünür.
10. SÜRÜM yazımı: `^` varsayılan; ESM-only ya da native ABI riski taşıyan paket TAM SABİT yazılır (`^`/`~` YOK) + gerekçe yorumu + pini koruyan bekçi. Expo'nun pinlediği sürüm elle değiştirilmez. <sub>([KU-12]/[KU-13]; `bonjour-service` 1.4.4 iki projede)</sub> — atlanırsa: Caret'li native paket bir sonraki `npm ci`de sessizce sürüm atlar ve keşif/donanım yolu sahada bozulur.
11. ALTI SATIRLIK KARAR KAYDINI `docs/standart/KUTUPHANELER.md` §9 defterine yaz (problem · alternatifler · boyut/biçim · bakım · lisans · dağıtım) ve §2 tablosuna satırı ekle. <sub>([KU-08] — kaydı olmayan bağımlılık üründe kalamaz)</sub> — atlanırsa: Bir yıl sonra "bu neden burada" sorusunun cevabı yoktur; paket ne kaldırılabilir ne güvenle yükseltilebilir.
12. KAPI: o projede tip kontrolü + lint + test paketi; mobilde ayrıca `npx expo-doctor`, native değiştiyse `npx expo prebuild --clean` sonrası `AndroidManifest.xml` izin diff'i. `package-lock.json` AYNI commit'te gelir. <sub>(docs/standart/KUTUPHANELER.md §6 adım 5 · docs/standart/TEST-VE-DERLEME.md §1)</sub> — atlanırsa: Lock dosyası olmadan temiz klon/CI başka bir sürüm çözer ve "bende çalışıyor" sınıfı hata doğar.
13. ANA (major) SÜRÜM YÜKSELTMESİ bu reçetenin konusu DEĞİLDİR: bir SÖZLEŞME değişikliğidir, ayrı iş olarak açılır ve o projenin TAM bekçi paketi koşar. <sub>([KU-14]; araç zinciri drifti bilinçli açık — §8)</sub> — atlanırsa: Yükseltme bir paket değişikliği gibi commit edilir, kırılma üç projeye yayılır ve hangisinin sebep olduğu ayrıştırılamaz.

## Ölü paket kaldırma

Mekanik kapı: BUGÜN YOK (yazılacak `test_dependency_contract.ts`'in ölü-paket bölümü üç kanalı tarayacak ve istisnaları ADI + GEREKÇESİYLE allowlist'te tutacak). Bugünkü tek koruma bu reçetenin kendisidir. Hüküm tablosu ve kaldırma sırası: `docs/standart/KUTUPHANELER.md` §6-§7.

1. ÖNCE YEŞİL TABAN ölç: üç projede tip + lint + test yeşil olmadan kaldırma yapılmaz. <sub>([KU-25]; kırmızı zeminde kaldırmanın kırdığı ile devralınan kırmızı karışır)</sub> — atlanırsa: Kaldırmadan sonra çıkan kırmızının sebebi belirsizleşir ve geri alma kararı verilemez.
2. `importFiles: 0`'ı KANIT SAYMA — ölçümde 13 CANLI paket sıfır göründü (`exceljs`, `serialport`, `node-hid`, `bonjour-service`, `prisma`, `jest-expo`, `@playwright/test`…). <sub>([KU-20]; keşif 13 paketten 1'ini yanlış işaretledi — `react-native-ble-plx` CANLI çıktı)</sub> — atlanırsa: Canlı bir paket silinir; hata derlemede değil, o yolun ilk kullanıldığı anda sahada çıkar.
3. ÜÇ KANALI ayrı ayrı tara ve üçü de boş çıkmadan hüküm verme: **(a)** dinamik import `await import("<paket>")` · **(b)** `createRequire`/tembel `require("<paket>")` · **(c)** config referansı — `app.json > plugins` · babel/jest/metro/vite/vitest/playwright/stryker config · CSS `@import` · `package.json > scripts` içindeki CLI · **peerDependency** (`node_modules/<tüketici>/package.json`'ından OKUNARAK). <sub>(docs/standart/KUTUPHANELER.md §6; emsaller Electron/src/lib/xlsx-export.ts:39 · Electron/electron/ipc/printer.ipc.ts:42)</sub> — atlanırsa: (c) kanalı atlanınca `app.json` plugin'i olan native paket "ölü" ilan edilir; APK bir sonraki turda izinsiz doğar.
4. "PEER ARTIĞI" iddiasını KURULU AĞAÇTAN doğrula, varsayma. <sub>([KU-21]; `react-native-paper@5.15.1` peer listesinde `react-native-vector-icons` YOK — `mobil/CLAUDE.md`'deki "yalnız peer artığı" cümlesi ölçümle yanlış çıktı)</sub> — atlanırsa: Yanlış peer varsayımı ya canlı paketi siler ya da ölü paketi sonsuza dek korur.
5. "BİRLİKTE ÖLÜR" sanılan yardımcıyı AYRI ölç; belirsiz kaldıysa paket KALIR. <sub>([KU-22]; `buffer` — polyfill `@react-pdf/renderer`e bağlı görünüyordu ama `exceljs` bundle'ı da `Buffer` istiyor → hüküm BELİRSİZ, paket kaldı)</sub> — atlanırsa: Zincirleme kaldırma bir çalışan yolu sessizce koparır.
6. Hüküm ÖLÜ ise KALDIRMA ETKİSİNİ yaz: saf JS mi · Electron rebuild ister mi · mobilde NATIVE mi (native ise OTA ile GİTMEZ, APK turudur). <sub>([KU-24]; `react-native-svg`, `expo-sharing`, `react-native-vector-icons` bu sınıftan)</sub> — atlanırsa: Native kaldırma OTA turuna konur; bundle ile native ayrışır ve sahadaki cihaz çöker.
7. AYNI COMMIT'te paketi ANAN her bayat referansı temizle: `CLAUDE.md` paket listesi · `knip.json > ignoreDependencies` · `jest.config.js > transformIgnorePatterns` · `package.json > scripts` · gerekçe yorumu. <sub>([KU-23]; emsaller: `react-native-svg` jest config · `react-native-web` `"web"` script'i · `@faker-js/faker` knip susturması)</sub> — atlanırsa: Bayat referans derlemeyi KIRMAZ, sessiz kalır; bir sonraki okuyan paketi hâlâ kullanılıyor sanır.
8. Kaldırma sırasını `k01-olu-paket-uc-kanal.json` § `kaldirmaSirasi`'ndan al ve HER ADIMDA doğrula: backend `npm test` + `npm run typecheck` · Electron `npm run typecheck` + `npx vitest run` + `npm run build` · mobil `npm test` + `npx expo-doctor`. `package-lock.json` aynı commit'te. <sub>(docs/standart/KUTUPHANELER.md §6 adım 5)</sub> — atlanırsa: Toplu kaldırmada hangi paketin neyi kırdığı ayrıştırılamaz.
9. `docs/standart/KUTUPHANELER.md` §7 hüküm tablosunda o satırın **Durum** sütununu "kaldırıldı" yap; CANLI/BELİRSİZ çıkan paketin satırı hükmüyle birlikte KALIR. <sub>(§7 tablosu — durum sütunu 2026-09-05 ölçüm anındadır)</sub> — atlanırsa: Tablo bayatlar ve bir sonraki tur aynı üç kanalı yeniden ölçmek zorunda kalır.
10. Native kaldırma sürüm notuna girer ve SONRAKİ APK turunda sahaya iner; OTA turunda `android.versionCode`a dokunulmaz. <sub>(docs/RECETELER.md § Sürüm çıkarma adım 14/16)</sub> — atlanırsa: Not yoksa paketleme kapısı zaten durur; durmazsa sahadaki cihaz neden değiştiğini kimse bilmeden farklı davranır.

## Sürüm çıkarma

Mekanik kapı: Kısmen var: scripts/test_surum.mjs (sürüm numarası kuralı, CI'da; .github/workflows/ci.yml:53) · scripts/check-surum-notlari.mjs (not şeması+dil+kopya+yayın kapısı; yalnız electron-paketle.sh ve yayinla-ota.mjs çağırıyor, CI'da YOK) · Teks-Erp/scripts/test_backend_surum.ts §5 (paketle.ps1 backend-su

1. 0) surum-notlari.json'a bu turun kaydını ekle: id=YYYY-AA-GG(+harf), baslik, surumler.{panel|tablet}, maddeler[] (kapsam ∈ panel|tablet|her-ikisi, tip ∈ yeni|duzeltme|iyilestirme, metin ≥10 karakter, teknik terim/dosya yolu YASAK, dizi EN YENİ ÖNCE). <sub>(scripts/check-surum-notlari.mjs:31-34,88-97,103-130)</sub> — atlanırsa: Kapı derlemeyi durdurur (fail-loud). Ama kapsam↔sürüm tutarsızlığı sessizdir: panel maddesi var/sürüm yok → kayıt hiç gösterilmez; sürüm var/madde yok → boş mod
2. 1) node scripts/surum-notlari-kopyala.mjs — kaynak dosyayı Electron/src/data/ ve mobil/src/data/ altına BİREBİR kopyala (kopyalar commit edilir). <sub>(scripts/surum-notlari-kopyala.mjs:19-22,33-48; check-surum-notlari.mjs:26-29,151-158)</sub> — atlanırsa: §5 kırmızı → paketleme durur. Kapı atlanırsa pakete BAYAT not gömülür; operatör güncelleme sonrası yanlış 'neler değişti' okur.
3. 2) PANEL: ./deploy/electron-paketle.sh <müşteri> [sürüm] — müşteri kodu ^[a-z0-9][a-z0-9-]{1,30}$ ile doğrulanır. <sub>(deploy/electron-paketle.sh:29-40)</sub> — atlanırsa: Türkçe karakter/boşluklu kod URL'de sessizce bozulur; yayın adresi ulaşılamaz olur.
4. 3) PANEL sürüm numarası: taban `panel-v*` git etiketi; yama hanesi otomatik artar, HEAD etiketteyse KORUNUR; hesaplanan numara yayındaki latest.yml ile kıyaslanır (zaten-yayinda / bayat → DUR, olculemedi → yalnız uyarı). Küçük/büyük hane elle verilir. <sub>(deploy/electron-paketle.sh:59-96; scripts/lib/surum.mjs:73-94,121-171,253-258)</sub> — atlanırsa: Numara artmazsa panel 'en güncelim' der, setup'ı hiç indirmez; etiket defteri bayatsa yayındaki numaranın ÜSTÜNE yazılır ve filo eski pakete takılır.
5. 4) PANEL: müşteri kodu shared/musteri.json'a VE package.json > build.publish'e AYNI yazımda konur (adres pakete derleme anında gömülür). <sub>(deploy/electron-paketle.sh:104-123)</sub> — atlanırsa: İki dosya ayrışırsa paket başka bir fabrikanın kanalını gösterir; hata sessizdir (dosyalar kendi aralarında tutarlı kalır).
6. 5) PANEL: adres bekçisi koşar — npx vitest run src/test/update-feed-url.test.ts (musteri.json ↔ package.json ↔ shared/update-feed.ts eşitliği). <sub>(deploy/electron-paketle.sh:131-133; Electron/src/test/update-feed-url.test.ts:39-59)</sub> — atlanırsa: build.publish yoksa electron-builder latest.yml ÜRETMEZ; güncelleyici hiç çalışmaz.
7. 6) PANEL not kapısı: node scripts/check-surum-notlari.mjs --panel=<surum> — beklenen sürüm ARGÜMANDAN gelir (dairesel değil). <sub>(deploy/electron-paketle.sh:135-148; check-surum-notlari.mjs:161-175)</sub> — atlanırsa: Notsuz sürüm çıkar; operatör güncelleme sonrası hiçbir açıklama görmez.
8. 7) PANEL derleme: macOS'ta npm run build:win:cross, Linux/Windows'ta build:win. Ham `npm run build:win` elle koşulmaz. <sub>(deploy/electron-paketle.sh:150-157)</sub> — atlanırsa: Ham build bir önceki müşterinin adresiyle derler; paket BAŞKA fabrikanın güncellemesini indirir (sessiz).
9. 8) PANEL gömülü adres kapısı: release/<surum>/win-unpacked/resources/app-update.yml içindeki url beklenen müşteri adresiyle bire bir eşleşmeli; ayrıca Setup.exe + latest.yml varlığı doğrulanır. <sub>(deploy/electron-paketle.sh:159-177)</sub> — atlanırsa: Çıktıya bakan tek kontrol budur; atlanırsa yanlış müşteri kodu taşıyan paket yayına çıkar.
10. 9) PANEL yayın: ./deploy/electron-yayinla.sh — müşteri paketin KENDİ shared/musteri.json'undan çözülür, latest.yml gerçekten o sürümü göstermeli, DEĞİŞMEZLİK kapısı (sunucudaki aynı adlı exe farklı sha256 ise DUR, aynıysa yükleme atlanır). <sub>(deploy/electron-yayinla.sh:36-39,85-112)</sub> — atlanırsa: Aynı numaraya farklı bayt yazmak sahadaki delta (blockmap) indirmesini bozar ve 'hangi 1.1.1' sorusunu cevapsız bırakır.
11. 10) PANEL yükleme SIRASI: önce Setup.exe + blockmap, EN SON latest.yml; ardından sunucuda openssl ile sha512 hesaplanıp latest.yml'deki değerle kıyaslanır; sonra dışarıdan HTTP + boyut doğrulaması (önbellek-atla ile 404 ↔ CF önbelleği ayrımı). <sub>(deploy/electron-yayinla.sh:114-142,145-191)</sub> — atlanırsa: Ters sırada latest.yml henüz yüklenmemiş exe'yi gösterir; sha512 tutmazsa panel paketi reddeder ve sebep sunucuda görünmez.
12. 11) PANEL yayın sonrası: yayın defterine satır (defter/ dizini html/ DIŞINDA), son 5 sürüm bırakılıp eskiler budanır, `panel-v<surum>` etiketi atılır ve origin'e itilir (best-effort; var olan etiket TAŞINMAZ). <sub>(deploy/electron-yayinla.sh:194-245; scripts/lib/surum.mjs:185-201)</sub> — atlanırsa: Etiket atılmazsa bir sonraki tur numarayı tekrar üretir → 'ZATEN YAYINDA' / 'ETİKET DEFTERİ BAYAT' kapısına düşülür.
13. 12) TABLET OTA: cd mobil && EXPO_PUBLIC_API_URL=<erp>/api npm run yayinla -- --musteri=<kod>. --musteri ZORUNLU ve mobil/musteri.json ile eşleşmeli; adres zorunlu, http(s):// ile başlamalı, /api ile bitmeli, localhost yasak. <sub>(mobil/scripts/yayinla-ota.mjs:96-120,367-388)</sub> — atlanırsa: Adressiz/yanlış adresli paket sahadaki TÜM tabletleri bağlantısız bırakır; yanlış müşteri kodu varlık URL'lerini başka fabrikaya yollar.
14. 13) TABLET sürüm: taban `tablet-v*` etiketi, yayındaki OTA manifestiyle kıyas (zaten-yayinda/bayat → DUR); hedef sürüm app.json > expo.version'a YAZILIR. --check yan etkisizdir, --surum / --surum-artirma ile ezilir. <sub>(mobil/scripts/yayinla-ota.mjs:410-486; scripts/lib/surum.mjs:260-269)</sub> — atlanırsa: Numara sabit kalırsa tablette görünen sürüm ile içindeki JS ayrışır; sürüm notları tek numaraya yığılır.
15. 14) TABLET versionCode kapısı: app.json > android.versionCode, yayındaki apk/surum.json künyesiyle EŞİT olmalı; OTA turunda artırılmaz. Künye yoksa kapı atlanır (uyarı). <sub>(mobil/scripts/yayinla-ota.mjs:490-518; scripts/lib/surum.mjs:272-280)</sub> — atlanırsa: Tablet kendini olmadığı bir APK sürümünde sanar ve gerçek kurulum dosyası güncellemesini BİR DAHA teklif etmez — sessiz ve kalıcı.
16. 15) TABLET not kapısı: check-surum-notlari.mjs --tablet=<app.json sürümü> alt süreç olarak koşar (sürüm yazımından SONRA yeniden okunmuş değerle). <sub>(mobil/scripts/yayinla-ota.mjs:520-540)</sub> — atlanırsa: Nota bakılan sürüm ile pakete gömülen sürüm ayrışırsa kapı doğrulamadığı bir numarayı yayınlar.
17. 16) TABLET native parmak izi kapısı: bağımlılıklar+plugins+android bloğu (versionCode HARİÇ) hash'lenir; native değişip runtimeVersion aynı kaldıysa DUR; algoritma sürümü (PARMAK_IZI_ALG) farklıysa 'kıyaslanamıyor' diye durur, --parmak-izini-kabul-et ile geçilir. <sub>(mobil/scripts/yayinla-ota.mjs:172-256)</sub> — atlanırsa: Yeni JS eski native'i çağırır; sahadaki tüm tabletler açılışta çöker.
18. 17) TABLET paket üretimi: Metro + .expo önbelleği SİLİNİR → expo export (EXPO_PUBLIC_API_URL açıkça geçilir) → üretilen bundle'daki adres geri okunur → manifest DONDURULUR, keystore/ota-keys/private-key.pem ile imzalanır ve sertifikayla istemcinin yaptığı gibi doğrulanır. Çıktı: ota-cikti/<musteri>/<runtimeVersion>/<damga>. <sub>(mobil/scripts/yayinla-ota.mjs:272-297,545-560,575-612)</sub> — atlanırsa: Sıcak önbellekle koşan export ESKİ adresi gömer (ölçüm: 17,9 sn ↔ 69,9 sn); imzasız paket kod imzalama açık APK'da sessizce reddedilir.
19. 18) TABLET yükleme: node deploy/mobil-yayinla.mjs --musteri=<kod> --paket=<dizin>. Müşteri kontrolü künyeye DEĞİL manifestteki varlık URL'lerine bakar; sıra: önce paket dosyaları, SONRA manifest; ardından gerçek HTTPS ile expo-protocol-version=1 + multipart/mixed + manifest id + bundle boyutu doğrulanır; sonunda `tablet-v<paket sürümü>` etiketi atılır. <sub>(deploy/mobil-yayinla.mjs:105-122,295-320,333-345,352-395,555-575)</sub> — atlanırsa: Ters sırada manifest yüklenmemiş varlıkları gösterir; nginx başlık kuralı eksikse dosya 200 döner ama istemci 'Legacy manifests' ile SESSİZCE durur.
20. 19) TABLET APK (yalnız native değişince): npx expo prebuild → npm run build:apk -- --musteri=<kod> (güncelleme kapısı APK'nın gömülü adresini ve runtimeVersion'ı okur) → node deploy/mobil-yayinla.mjs --apk=<yol> --surum=X --vc=N --musteri=<kod>; APK adı ASCII+boşluksuz, APK içindeki adres okunur, ÖNCE apk SONRA künye yüklenir. <sub>(mobil/scripts/build-apk.mjs:645-720,1039-1075; deploy/mobil-yayinla.mjs:420-447,448-520)</sub> — atlanırsa: Prebuild atlanırsa APK eski adresi taşır ve kurulan tablet 404 alıp bir daha HİÇ güncelleme almaz; künye önce giderse olmayan dosyayı işaret eder.
21. 20) BACKEND paketi: deploy/paketle.ps1 — kirli çalışma ağacında onay sorar, node scripts/backend-surum.mjs ile yama hanesini `backend-v*` etiketinden türetip --uygula ile Teks-Erp/package.json'a yazar, paket DOĞRULANDIKTAN sonra --etiketle ile backend-v<surum> atar. Yayın sunucusu kıyası YOKTUR (paket elden taşınır). <sub>(deploy/paketle.ps1:60-90,388-392; scripts/backend-surum.mjs:9-19,38-65)</sub> — atlanırsa: Sürüm sabit kalırsa /health her kurulumda aynı numarayı basar; 'sunucuda hangi kod var' sorusunun tek cevabı commit kısaltması olur.

⚠️ Belgesiz kalan adımlar (koddan çıkarıldı, hiçbir .md'de yoktu):

- APK yayın yolunda SÜRÜM NOTU KAPISI HİÇ KOŞMUYOR: check-surum-notlari.mjs yalnız deploy/electron-paketle.sh:143 ve mobil/scripts/yayinla-ota.mjs:529'dan çağrılıyor; build-apk.mjs ve deploy/mobil-yayinla.mjs --apk yolunda çağrı yok (git grep check-surum-notlari). 'Not yazılmadan sürüm çıkmaz' cümlesi (CLAUDE.md:190, SURUM-NOTLARI.md:83-92) APK için YANLIŞ ve bu istisna hiçbir yerde yazılı değil.
- BACKEND sürüm hattı hiçbir belgede yok: scripts/backend-surum.mjs + `backend-v*` etiketi + paketle.ps1 -Surum parametresi. `git grep -n 'backend-surum' -- '*.md'` ve `git grep -n 'backend-v' -- '*.md'` SIFIR sonuç veriyor; deploy/README.md paketle.ps1'i anlatıyor ama sürüm numarasından hiç söz etmiyor.
- İKİ PANEL YAYINCISI AYRIŞMIŞ ve belge ESKİ olanı öneriyor: deploy/electron-yayinla.ps1 varsayılanları eski sunucu (SshHedef=oguzhan@91.217.119.138, UzakDizin=.../adnansahin/electron sabit) ve etiket atma / değişmezlik sha256 / sha512 doğrulama / yayın defteri / budama adımlarının HİÇBİRİNİ içermiyor (130 satır, grep: etiketAt|sha512|DEFTER|buda → 0). electron-yayinla.sh:18-26 aynı adresi 'ESKİ sunucu' diye işaretliyor. ELECTRON-OTOMATIK-GUNCELLEME.md:287-292 yayın adımı olarak ps1'i gösteriyor; aynı belgenin 234. satırı ise 'etiketi electron-yayinla.sh atıyor' diyor — belge kendi içinde çelişiyor.
- DEĞİŞMEZLİK KAPISI (electron-yayinla.sh:92-112: sunucudaki aynı adlı paket farklı sha256 ise DUR) hiçbir belgede anlatılmıyor; docs'ta 'DEĞİŞMEZLİK' geçmiyor.
- Sunucu tarafında yeniden hesaplanan sha512 ↔ latest.yml doğrulaması (electron-yayinla.sh:122-142) belgelerde yok; ELECTRON-OTOMATIK-GUNCELLEME.md:471'deki sha512 cümlesi İSTEMCİ tarafını anlatıyor, yayıncıyı değil.
- docs/ops/MOBIL-UZAKTAN-GUNCELLEME.md:54-57 çalışmayan komut veriyor: `npm run yayinla` --musteri'siz (yayinla-ota.mjs:372-380 zorunlu kılıp durur) ve paket yolu `ota-cikti/<rv>/<damga>` — kod çıktıyı `ota-cikti/<musteri>/<rv>/<damga>` altına yazıyor (yayinla-ota.mjs:553-557). Doğru komut yalnız mobil/CLAUDE.md:41'de.
- docs/ops/MOBIL-UZAKTAN-GUNCELLEME.md:128-131 geri alma komutu `ssh yenisunucu` diyor; deploy/mobil-yayinla.mjs:72-75'e göre `yenisunucu` ESKİ sunucudur, yayın 2026-09-01'de tekserp-yayin'e (80.253.255.188) taşındı.
- CLAUDE.md'nin OTA hattında YÜKLEME ADIMI eksik: 'Sürüm Yayınlama' bloğu (CLAUDE.md:214-220) yalnız `npm run yayinla` ve APK yayınını gösteriyor; `node deploy/mobil-yayinla.mjs --paket=… --musteri=…` satırı yok — oysa yayinla-ota.mjs:5-7 'Yükleme ayrı adımdır' diyor. Not yazılmış paket üretilir ama sahaya HİÇ çıkmaz.
- Sürüm notu kapısı CI'da KOŞMUYOR: .github/workflows/ci.yml:53 yalnız scripts/test_surum.mjs koşuyor; check-surum-notlari.mjs / surum-notlari-kopyala.mjs --kontrol adımı yok (grep 'surum' ci.yml → tek satır). Kopya drift'i ancak paketleme anında yakalanıyor.
- Üç yerde ADI GEÇEN ama VAR OLMAYAN bekçi: `scripts/test_surum_notlari.ts` (scripts/surum-notlari-kopyala.mjs:7, Electron/src/lib/surum-notlari.ts:8-9, surum-notlari.json:4). `find . -name 'test_surum_notlari*'` sonuç vermiyor; drift'i gerçekte check-surum-notlari.mjs §5 kilitliyor.
- electron-paketle.sh not kapısı BAŞARISIZ olsa bile package.json (version + build.publish) ve shared/musteri.json ZATEN yazılmış olur (satır 104-123 kapıdan önce, kapı 143). 'Kapı derlemeden önce koşar' (SURUM-NOTLARI.md:94) doğru ama çalışma ağacının kirli kalması hiçbir yerde yazılı değil.
- Notu yazarken gereken 'sıradaki numara' için panel/tablet tarafında salt-okunur bir komut YOK (backend'de var: `node scripts/backend-surum.mjs`); numara ancak paketleme komutunu koşup kapıya düşerek öğreniliyor (electron-paketle.sh:93-95). Bu operasyonel sıra hiçbir reçetede anlatılmıyor.
- CLAUDE.md:88 'açılıştan 30 sn sonra + 4 saatte bir kontrol' BAYAT: Electron/electron/ipc/updater.ipc.ts:28-31 'ilk kontrol 30 sn, sonra 15 dk' diyor ve süreler @shared/update-schedule'dan geliyor. Aynı bayat cümle deploy/electron-yayinla.sh:247'de operatöre de basılıyor ('en geç 4 saat içinde görür'); ELECTRON-OTOMATIK-GUNCELLEME.md:325 doğru (15 dakika).

## Alan-özgü reçeteler (Faz C)

- **belge-etiket-sablon** · `resolveSectionOrder` tanınmayan bölüm anahtarını atar ve EKSİK bölümü SONA ekler (kayıtlı sıra yarınki bölümü yutmasın). Yeni bölüm eklerken `traveler-card.sections.ts` + Electron `TravelerCardStudio/types.ts` BİRLİKTE güncellenir; alan kataloğu için de aynı (Electron backend'i import edemez). <sub>(CLAUDE.md:152)</sub>
- **belge-etiket-sablon** · Yeni DocumentConfig alanı = DÖRT KAPI, yoksa ayar SESSİZCE kaybolur: ① system-setting DocumentConfig tipi · ② sanitizeDocumentsConfig kayıt kapısı · ③ printed-document.controller.docConfigSchema (z.object bilinmeyen anahtarı ATAR → önizlemede YOK) · ④ Electron documentConfig.ts aynası. <sub>(CLAUDE.md:64, CLAUDE.md:111)</sub>
- **belge-etiket-sablon** · Dört kapının bekçisi iki ayaklı: §17 önizleme şeması + §18 kayıt kapısı round-trip — yeni alan eklerken İKİSİ genişletilir (sanitize'dan alan silinince hiçbir test kırmızı vermiyordu). columns.shown (07-30) ve labels (09-04) bu kapılardan birini atlamıştı. <sub>(CLAUDE.md:64, CLAUDE.md:111)</sub>
- **belge-etiket-sablon** · Çuval içeriğine dokunan yol üç guard: touchWarehouseSackTx (WHERE shipmentId IS NULL); PLANNED küme değişiyorsa touchShipmentPlannedTx; içerik değişiyorsa markSackContentChangedTx (eski adı resetSackWeightsTx; kg + labelDirty AYRI updateMany — kg koşuluna bağlanırsa tartılmamış çuvalda yazılmaz). <sub>(CLAUDE.md:325, CLAUDE.md:75, CLAUDE.md:43)</sub>
- **belge-etiket-sablon** · LabelKind genişletme: backend Zod z.nativeEnum(LabelKind) OTOMATİK (literal z.enum YOK). ELLE, derleme hatası VERMEZ: Electron labelTemplateService.ts LabelKind + labelKindLabels, CustomerTemplateRoutesPanel KINDS, PrinterSettingsFields ROUTE_KINDS, mobil types/models.ts LabelKind union. <sub>(CLAUDE.md:43)</sub>
- **belge-etiket-sablon** · Yeni etiket bağlamı için ayrıca label-fields.ts kataloğuna alan listesi ve (bağlamın okunur kimlik alanı varsa) label-context-fit IDENTITY_BIND girdisi eklenir — yoksa yanlış atanmış şablon fail-closed guard'ı arka kapıdan geçer. <sub>(CLAUDE.md:43)</sub>
- **deploy-kurulum** · Şema-dışı nesneler (partial index · CHECK · DEFERRABLE FK · ifade-UNIQUE · statistics) `test_db_invariants.ts` envanterinde yaşar; kapı İKİ YÖNLÜ — envanter-dışı nesne de düşürür. Kırmızıya tepki nesneyi silmek değil envantere yazmak (`EXPRESSION_UNIQUES` girdisi predicate ile ZORUNLU). <sub>(CLAUDE.md:248, CLAUDE.md:84)</sub>
- **deploy-kurulum** · Fabrika `git pull → build` ile DEĞİL, PAKETLE kurulur: build klonunda `git pull` → `.\deploy\paketle.ps1 -Cikti …` → sunucuda `kur.ps1 -Paket <zip> -Zorla`. Çalışan `app\` git klonu değildir; `kur.ps1` kaynağı `deploy/kur.ps1`, sunucuya ELLE kopyalanır (kendini güncelleyemez). Eski akış tarihsel. <sub>(CLAUDE.md:84, CLAUDE.md:101)</sub>
- **deploy-kurulum** · `kur.ps1` dokuz adım: [1/9] paket doğrula (Windows şema motoru dahil) · [3/9] `premigrate_` dump + `pg_restore --list` · [4/9] pm2 delete · [5/9] eskiyi `app.eski-*`a al · [6/9] npm ci · [7/9] `migrate deploy` GERİ ALINAMAZ EŞİK · [8/9] pm2 start · [9/9] /health. Eşik öncesi otomatik geri alma. <sub>(CLAUDE.md:84, CLAUDE.md:112)</sub>
- **deploy-kurulum** · `kur.ps1` geri alma KAYNAĞA bakar: `app.eski-*` yoksa hiçbir şey silinmez, mevcut `app\` pm2 ile GERİ KALDIRILIR (yalnız komut basmak fabrikayı kapalı bırakır); `Move-Item` `-ErrorAction Stop` (hedef doluysa düşmez, İÇİNE taşır); `KokeDon` `GeriAlOtomatik`'in ilk satırı; kilit sondası bilinçli RED. <sub>(CLAUDE.md:84)</sub>
- **deploy-kurulum** · [PROFİL] Sunucuda İKİ klon var: paket **D:\tekserp-build\tekserp** (tam klon, `main`) kökünden üretilir; `C:\Etkili-Yazilim\tekserp` sparse klon (`deploy/` diskte yok) kullanılmaz. Çalışan kurulum `C:\Etkili-Yazilim\app`, pm2 adı `tekserp-backend`, önceki sürüm `app.eski-<damga>`. <sub>(CLAUDE.md:84)</sub>
- **deploy-kurulum** · Backend sürümü de OTOMATİK artar; taban git etiketi `backend-v*` (`scripts/backend-surum.mjs`, paketle.ps1 hesaplar → uygular → yayın sonrası etiketler). Kök 'Sürüm Yayınlama' bölümü yalnız `panel-v*`/`tablet-v*`ı anlatır — backend ekseni orada YOK. <sub>(CLAUDE.md:112, CLAUDE.md:1)</sub>
- **deploy-kurulum** · Yeni izin TEK adım: `PERMISSION_CATALOG`a satır. Boot uzlaştırması her açılışta EKSİĞİ yazar — kodu deploy etmek = katalogu getirmek; yalnız EKLER (silmez, güncellemez, ATAMAZ). Elle INSERT ve izin başına migration YAZILMAZ. Rol şablonu kimliği `code`; `code=null` fabrikanın şablonu. <sub>(CLAUDE.md:191, CLAUDE.md:236)</sub>
- **fason** · Fason MODÜLÜ henüz anahtar DEĞİL (`fason.enabled` yok). Fason ekran/bayrak aidiyeti `planlanan:fason` (kartela `planlanan:kartela`) — tasarımda var, kod anahtarı yok, hiza aranmaz, ölü sayılmaz; kapalı-modül gizlemesi UYGULANMAZ. Doğduğu gün `flag-modules.ts` + `screen-catalog.ts` birlikte. <sub>(CLAUDE.md:103, arşiv:2134)</sub>
- **filtre-liste-arama** · Yeni özellik bayrağı DÖRT kapıda birlikte: system-setting (SETTING_KEYS+interface+builder+update+read helper) · feature-flag.routes · Electron featureFlagService+settings-config · mobil featureFlag.service+useFeatureFlags (emsal `kk1.historyAllEntriesEnabled`). <sub>(CLAUDE.md:55)</sub>
- **finans-saglamlik** · Yeni ayar anahtarı eklerken üç tip de sözleşme bekçisine girer: `test_feature_flag_contract` boolean + SAYISAL (2026-08-14) + ENUM (`aEnum`, 2026-09-03) ayaklarını ölçer — 'bekçi yalnız boolean bakar' varsayımı bayattır. <sub>(CLAUDE.md:286)</sub>
- **is-emri-siparis-bag** · Rework sebebi İKİ hedefe: metin → 1. adım notu → fason çekisine talimat; kod → `WorkOrder.parameters.rework` (rapor anahtarı, migration yok). Yeni `ReasonPresetKind` BEŞ kapı birlikte (şema+migration · backend katalog · Electron KIND_TABS · mobil union · mobil zemin); mobil zemin GERÇEK kod taşır. <sub>(arşiv:840)</sub>
- **kk1-idempotency-kuyruk** · `kk1.duplicateGuardEnabled` bayrağını AÇMADAN ÖNCE sahadaki tabletler 409 `POSSIBLE_DUPLICATE`'i tanıyan APK'ya güncellenmiş olmalı — yoksa ham giriş çıkışsız kalır. <sub>(CLAUDE.md:171)</sub>
- **kk1-idempotency-kuyruk** · Geçmiş mükerrer topları aramak için salt-okunur `scripts/find_duplicate_rolls.ts` kullanılır (yazan bir temizlik script'i yazma). <sub>(CLAUDE.md:171)</sub>
- **kucuk-kumeler** · Modüle ait her uç ADLANDIRILMIŞ modül kapısını taşır (requireProductionEnabled / requireTicaretEnabled / requireIplikEnabled / requireDepoMultiEnabled); jenerik requireModule("x") YASAK — bekçiler middleware ADINI arar. Kapı gerekmiyorsa gerekçe muaf listesinde YAZILI. <sub>(CLAUDE.md:315)</sub>
- **kucuk-kumeler** · `Electron/dist-web` backend paketine girer — SPA↔API sürüm drift'i imkânsız. Eksiklik SESSİZDİR (kök, panelin yerine durum sayfası basar), bu yüzden paketlemede iki kapı vardır: derleme düşerse ve `dist-web\index.html` yoksa paket ÜRETİLMEZ. <sub>(CLAUDE.md:95)</sub>
- **kursun-planlama-bypass** · Yeni izin eklemenin TEK adımı: `src/constants/permission-catalog.ts` → `PERMISSION_CATALOG`'a bir satır; başka yere kopyalanmaz. `category` Prisma enum'una bağlı (yazım hatası derlemede düşer), boot uzlaştırması eksiği getirir, AST bekçisi kaçağı yakalar. Elle DB INSERT'i gerekmez. <sub>(CLAUDE.md:191)</sub>
- **modul-bayraklari-profiller** · Modül/bayrak paketlerinde dağıtım sırası backend ÖNCE (yeni uç/anahtar/tablo eski istemciyi bozmaz); Electron+APK sonra/birlikte; 403 gerekçe metni finance→ticaret değişen dört yüzey sürüm notuna yazılır. <sub>(CLAUDE.md:96, CLAUDE.md:97, CLAUDE.md:98)</sub>
- **mukerrer-namefold** · Sed migration'ı İKİ desenden biriyle iner: ① mükerrer varsa/bilinmiyorsa YUMUŞAK KAPI — o tablonun index'i ATLANIR + NOTICE, deploy DURMAZ, dosya idempotenttir ve temizlik sonrası yeniden koşunca enforce eder; ② çakışma ölçülüp script'le 0'a indiyse sert index, SIRA (önce temizlik) pazarlık dışı. <sub>(CLAUDE.md:78, CLAUDE.md:80)</sub>
- **mukerrer-namefold** · Yeni bir `nameFold` kolonu eklendiğinde ya sed de eklenir ya da GEREKÇELİ MUAF listesine yazılır — unutmak SESSİZDİR (uygulama guard'ı normal yolda çalışır, hiçbir şey kırmızıya dönmez). Envanter İKİ YÖNLÜ: ölü muaf (sedlenmiş ama muafta) ve hayalet muaf (olmayan tablo) da kırmızı verir. <sub>(KOD:20260831120000_namefold_sed_kalan_ta)</sub>
- **mukerrer-namefold** · Sed eklerken: şemaya `map:`siz `@@unique([nameFold])` (index↔unique drift'i önler, ad `<tablo>_nameFold_key` doğar), `error.middleware` UNIQUE_COLUMN_LABELS'a kolon etiketi ('ad'), ifade-UNIQUE ise `EXPRESSION_UNIQUES` girdisi (+predicate) ZORUNLU. Drift allowlist'i GEREKMEZ (ölçüldü). <sub>(CLAUDE.md:78, CLAUDE.md:84)</sub>
- **mukerrer-namefold** · Yeni tabloya sed eklemeden ÖNCE `scripts/find_fold_duplicates.ts` koştur: soy bağlı tablolarda tombstone'u süzer, renk grubunu JS renk kuralıyla (düz nameFold ile DEĞİL) kurar ve iki keskin tarama (noktalama-sız ad, harf-duyarsız kod) yapar; temiz çıktıda enforce komutunu kendisi yazdırır. <sub>(CLAUDE.md:78, CLAUDE.md:80, CLAUDE.md:84)</sub>
- **mukerrer-namefold** · Birleştirme kararlarını toplu uygularken İNCELEME SQL'de, UYGULAMA MOTORDA: ham UPDATE 42 kurallık FK haritasını, çakışma politikalarını, etiket/kart bayatlatmasını, advisory kilidi ve audit'i atlar. `apply_merge_decisions.ts` DRY-RUN varsayılan; CSV ayırıcısı `;` (gerekçede `;` yok). <sub>(CLAUDE.md:80, CLAUDE.md:84)</sub>
- **raporlar** · sl_day_exact ifade istatistiği GRUPLAMA İFADESİNE bağlıdır: factoryDaySql'in ürettiği SQL metnini değiştirirsen YENİ BİR MIGRATION da yaz (CREATE STATISTICS yeniden) — yoksa istatistik sessizce devre dışı kalır ve sorgu ~2× yavaşlar. <sub>(CLAUDE.md:301)</sub>
- **rota-renk-ozellik-kapsama** · Yeni istasyon yetenek bayrağı eklerken BACKFILL'E GÜVENME: seed migration'dan SONRA boş tabloda koşar → seed'e açık değer + yüklemde köprü dalı (`kind===PROCESS_QC || appliesQuality`). 08-10 `appliesColor` göçünde iç istasyonlara FALSE yazıldı — bayrak DÜRÜST olmalı, true Tambur'a renk yazdırır. <sub>(arşiv:2116, CLAUDE.md:68)</sub>
- **rota-renk-ozellik-kapsama** · KÜME DIŞI (backend): Yeni route = `verifyToken` + `requirePermission(kod)` + Swagger JSDoc; kod `src/constants/permission-catalog.ts`'te OLMALI. <sub>(CLAUDE.md:314)</sub>
- **sebep-kataloglari** · Yeni `ReasonPresetKind` eklerken BEŞ KAPI birlikte güncellenir: şema+migration · backend katalog (`constants/reason-presets.ts`) · Electron `KIND_TABS`+union · mobil union · mobil çevrimdışı zemin. Eksik kapı sessiz kalır (tip hatası vermez). <sub>(CLAUDE.md:85)</sub>
- **sevkiyat-brut-storno-cuval** · `LabelKind` genişletirken 4 yer ELLE güncellenir (labelTemplateService KINDS · PeripheralDevices tipleri · 4 literal z.enum) — Electron/mobil kendi union'ını taşıdığı için derleme hatası VERMEZ. <sub>(CLAUDE.md:43)</sub>
- **sevkiyat-brut-storno-cuval** · Deploy sırası: sevkiyat sözleşmesi değişince backend ÖNCE (eski Electron `releaseSacks` göndermez → false → eski PLANNED davranışı; yeni Electron eski backend'de Zod alanı strip eder). Mobil yanıt şekli değişen turda (brüt detay) backend + Electron + APK aynı pencerede; storno turunda APK gerekmez. <sub>(CLAUDE.md:79, CLAUDE.md:47, CLAUDE.md:63)</sub>
- **surum-yayin** · Etiketi yayın script'i YAYIN BİTTİKTEN sonra atar (best-effort; var olan etiket taşınmaz). HEAD etiketin üstündeyken numara KORUNUR — tekrar koşum ve aynı turda ikinci müşteri aynı numarayı taşır. OTA ve APK AYNI çizgide (tek `tablet-v*`; `expo.version` hem paket hem versionName). <sub>(CLAUDE.md:182)</sub>
- **surum-yayin** · Yol düzeni `guncelleme.etkiliyazilim.com/<müşteri>/<ürün>/` (electron|mobil; aynı konteyner, statik nginx, yeni konteyner YOK). Müşteri segmenti ALT ALAN ADI değil (wildcard Origin CA iki seviyeyi kapsamaz). Yeni müşteri = sunucuda `mkdir` + paketle + yayınla; DNS/sertifika/servis GEREKMEZ. <sub>(CLAUDE.md:182, CLAUDE.md:88, CLAUDE.md:93)</sub>
- **surum-yayin** · Yan etkisiz ön kontroller yayın öncesi koşulur: `npm run yayinla:check`, `npm run build:apk:check` (derleme yok), `build:apk:verify` (mevcut APK'nın gömülü adresi), `electron-yayinla.sh --dogrula`, `mobil-yayinla.mjs --dogrula=<url> --boyut=<bayt>` (yükleme yok). `--check` dosya DEĞİŞTİRMEZ. <sub>(CLAUDE.md:182, CLAUDE.md:19, CLAUDE.md:66)</sub>
- **tambur-finalize-kesim** · Kat yazan her yol aynı kuralı uygular (cutOpenFabric · cutWarehouseRoll · yeniden-kesim artığı · finalizeOpenFabric · elle giriş · Düzelt); Düzelt'te üçlü sözleşme (undefined DOKUNMA · null TEMİZLE · kod YAZ), kat değişimi `labelDirty`; depo kesimi mobilde kat SORMAZ (bilinçli, fallback parent). <sub>(arşiv:80, arşiv:192)</sub>
- **yari-mamul** · Yeni `RollEntrySource` değerinde güncellenecek yerler: `entryTitle` switch'i (açık case; default 'Ham Giriş' yalanı basar) · mobil KK1 listelerinin CSV allowlist'i · Electron `rollProcessingState` haritası · Giriş Kaynağı filtre seçenekleri · gerekiyorsa `rollScope` dalı. <sub>(CLAUDE.md:92, CLAUDE.md:91)</sub>
- **yari-mamul** · Kurulum başına panelden 'Yarı Mamul (Kurşun + Tambur)' ROTASI oluşturulur ve `mobile:kk1-yari-mamul` izni atanır — rota kod değil VERİdir; ikisi eksikken giriş kapısı açık olsa da özellik sıfır kullanım üretir. <sub>(CLAUDE.md:92)</sub>
- **yari-mamul** · 'OTA'ya uygun mu' sorusunu insan değil yayın script'i cevaplar (native parmak izi kıyası; değiştiyse DURUR). Uzaktan güncelleme yalnız yeni APK kurulu tabletlere gider — elle APK turundan ÖNCE yayınlamak işe yaramaz. <sub>(CLAUDE.md:91)</sub>
- **yetki-izin-rol** · Yeni ekran canlıda görünmüyorsa sırayla bak: (1) satır DB'de mi (boot log `[permission-catalog]`), (2) kullanıcıya ATANMIŞ mı, (3) kullanıcı yeniden giriş yaptı mı (JWT'deki izin listesi bayat olabilir). <sub>(CLAUDE.md:240)</sub>
- **yetki-izin-rol** · Mobil ayrı union taşır (`mobil/src/types/permissions.ts`), Electron backend'i import edemez → izin listeleri AYNALANIR (`Electron/src/lib/permissions.ts` ↔ `document-design.ts`; `WORKSTATION_PERMISSION`). Mobil ekran izni eklerken union ELLE güncellenir (bekçi taramaz); ayna çiftleri bekçili. <sub>(CLAUDE.md:240, CLAUDE.md:117, CLAUDE.md:130)</sub>
- **yetki-izin-rol** · Yeni uç: `verifyToken` + `requirePermission(kod)` + Swagger; kod katalogda; modüle aitse ADLANDIRILMIŞ modül kapısı (`requireXEnabled`, jenerik `requireModule` YASAK); mobil dokunuyorsa `requireAnyPermission`; 403 kodu `details.code`. İzin bir uçta ZORUNLU olunca eski istemci 403 → `minVersion`. <sub>(CLAUDE.md:314, CLAUDE.md:323, CLAUDE.md:30)</sub>